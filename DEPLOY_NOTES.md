# یادداشتِ استقرار — آنچه پیش از هر deploy باید اعلام شود

این فایل **هشدار** است، نه تاریخچه. تاریخچه در
[PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md) §۱۰ می‌ماند؛ این‌جا فقط آنچه هنوز
مستقر **نشده** می‌نشیند. بعد از استقرارِ موفق، بخشش پاک می‌شود.

قالب و دلیلش در [AGENT_PLAYBOOK.md](AGENT_PLAYBOOK.md) §۸ است.

---

## در صفِ استقرار — PRهای ۳۱ تا ۳۴ (۱۴۰۵/۰۶/۲۱)

**ترتیبِ merge: #32 → #33 → #34.** (#31 مستقل است و هر وقت.)
پنج مهاجرت با هم اجرا می‌شوند.

### ۱. شماره‌های مهاجرت

`0121` تا `0125` گرفته شده‌اند. **اولین شماره‌ی آزاد: `0126`.**

| شماره | چه می‌کند |
|---|---|
| `0121` | علتِ برگشت، ردیفِ مبدأ و ابطالِ اسنادِ برگشت |
| `0122` | ماتریسِ قیمت (گروهِ فروشِ کالا، درصدِ اضافات، سیاستِ تخفیف) + ردِ قیمت روی ردیفِ فاکتور |
| `0123` | Snapshotِ طرفینِ فاکتور خرید |
| `0124` | پیش‌فاکتورِ منبع و پیوندِ تبدیلِ جزئی |
| `0125` | جداسازیِ فاکتور فروش از خروجِ انبار + جدول‌های `warehouse_issues` |

### ۲. رفتارهایی که روی مسیرِ مستقر عوض می‌شوند

**⚠️ «ثبت فاکتور» دیگر سندِ حسابداری و کسرِ موجودی نمی‌زند.**
`POST /api/sales-invoices` حالا فقط سندِ تجاری را ذخیره می‌کند. سندِ حسابداری و
خروجِ انبار دو اقدامِ **جداگانه**اند که کاربر از فهرستِ فاکتورها می‌زند
(«صدور سند حسابداری» و «صدور خروج انبار»). **این باید به کاربرها گفته شود، نه
فقط به تیم.**

مسیرهایی که **دست‌نخورده‌اند** و همچنان یک‌مرحله‌ای‌اند: صندوق (از
`/api/sales-invoices/immediate`)، بازارگاه، فروشگاه‌ساز، تبدیلِ پیش‌فاکتور، و
همگام‌سازیِ فاکتورِ آفلاین.

**فیِ فاکتور و صندوق از موتورِ سرور می‌آید.** ممکن است با عددی که دیروز خودکار پر
می‌شد فرق کند — و همان عددی است که سرور از ابتدا اعتبارسنجی می‌کرد. تا امروز فرم
یک قیمت پر می‌کرد و سرور با قاعده‌ی دیگری می‌سنجیدش.

### ۳. تغییرهای حسابداری — پیش از بستنِ ماه بخوانید

* **برگشت از فروش دیگر «فروش» را بدهکار نمی‌کند.** حسابِ تازه‌ی **۴۱۰۷ برگشت از
  فروش**. **شکلِ صورتِ سود و زیان عوض می‌شود** و «نرخِ برگشت» تازه قابلِ
  گزارش‌گیری می‌شود. سندهای تاریخی بازنویسی **نشدند**.
* دو حسابِ تازه که تنبل ساخته می‌شوند: **۴۱۹۸ اضافات فروش**، **۴۱۹۹ تخفیفات فروش**.
* **COGS و کسرِ موجودی حالا از سندِ «خروج انبار» می‌آیند**، نه از خودِ فاکتور.
  سندِ تجاری و سندِ بهای تمام‌شده از این پس دو سندِ جدا هستند.

### ۴. قابلیت‌هایی که جابه‌جا یا برداشته شده‌اند

* **«انبار ← لیست قیمت» خواندنی شد.** ورود و ویرایشِ قیمت رفت به
  **«فروش ← اعلامیه قیمت»**، که تاریخِ اجرا و همه‌ی ابعادِ قیمت را می‌شناسد.
  (آن فرم پیش از این با هر «ذخیره» کلِ ماتریسِ قیمت را بی‌صدا پاک می‌کرد.)
* **صندوق:** انتخابگرِ «لیست قیمت» جایش را به **«نوع فروش»** داد.
* فهرستِ فاکتورِ فروش دو کنشِ تازه گرفت: «صدور سند حسابداری» و «صدور خروج انبار».

### ۵. مهاجرت‌هایی که `downgrade`شان حذف می‌کند — عملاً یک‌طرفه

* **`0122`** — `DELETE FROM price_list_items WHERE item_id IS NULL`، یعنی هر
  قاعده‌ی قیمتِ **گروه‌محور** از بین می‌رود.
* **`0125`** — جدول‌های `warehouse_issues` و `warehouse_issue_lines` را
  `drop` می‌کند، یعنی تاریخچه‌ی خروج‌های انبار.

بازگشت از این دو یعنی بازیابیِ پشتیبان، نه `alembic downgrade`.

### نکته‌ی اجرایی

`0125` برای مستأجرهای موجود ردیفِ شمارنده‌ی `warehouse_issue` می‌سازد. **لازم
است** — بدونش اولین خروجِ انبار با ۵۰۰ می‌شکند. مستأجرِ تازه آن را از `seed.py`
می‌گیرد.

### و دو باگی که این استقرار می‌بندد

هر دو امروز روی تولید زنده‌اند:

* یک **برگشتِ اشتباهی، فاکتورش را برای همیشه از ابطال قفل می‌کرد** — گارد
  می‌گفت «اول سندِ برگشت را برگردانید» و هیچ راهی برای این کار وجود نداشت.
* باز کردنِ **«انبار ← لیست قیمت» و زدنِ «ذخیره»، کلِ ماتریسِ قیمت را پاک
  می‌کرد** — هر قیمتِ عمده/خرده/واحد/گروهِ مشتری و هر حدِ تغییرِ نرخ. بی‌صدا،
  بی‌خطا، و بدونِ ردِ حسابرسی.
