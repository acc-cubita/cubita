# یادداشتِ استقرار — آنچه پیش از هر deploy باید اعلام شود

این فایل **هشدار** است، نه تاریخچه. تاریخچه در
[PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md) §۱۰ می‌ماند؛ این‌جا فقط آنچه هنوز
مستقر **نشده** می‌نشیند. بعد از استقرارِ موفق، بخشش پاک می‌شود.

قالب و دلیلش در [AGENT_PLAYBOOK.md](AGENT_PLAYBOOK.md) §۸ است.

---

## در صفِ استقرار — PRهای ۳۸ تا ۴۱ (۱۴۰۵/۰۶/۲۱)

همه merge شده‌اند. آخرین استقرارِ موفق: نسخه‌ی مهاجرت `0125`.

### ۱. شماره‌های مهاجرت

سه مهاجرت با هم اجرا می‌شوند: **`0126` → `0127` → `0128`**.
**اولین شماره‌ی آزاد: `0135`** — `0129`–`0134` را سه شاخه‌ی پایین گرفته‌اند (ادعایشان برای master: `chore/claim-0129-0134`).

| شماره | چه می‌کند |
|---|---|
| `0126` | سیاستِ صدورِ فاکتور فروش (خودکار / دومرحله‌ای) روی مستأجر |
| `0127` | اعلامیه‌ی بدهکار/بستانکار دوسویه شد + ارز و نرخ روی سربرگ |
| `0128` | تفکیکِ عامل‌به‌عاملِ فیش حقوقی (`payslip_lines`) |

**هیچ‌کدام backfill ندارند** و `downgrade` هر سه به `0125` برمی‌گردد — روی اسکیمای
یک‌بارمصرف up→down→up سنجیده شد و مجموعه‌ی جدول‌ها عیناً بازگشت.

### ۲. ⚠️ دو جا عددِ روی صفحه عوض می‌شود — و این اصلاح است، نه باگ

**کارتِ حسابِ طرف حساب.** فاکتورهایی که «اضافات» یا «عوارض» داشتند، تا امروز در
کارتِ حساب **کمتر از واقع** دیده می‌شدند: سندِ حسابداری `خالص + مالیات + اضافات +
عوارض + رند` را بدهکار می‌کرد ولی گزارش فقط `خالص + مالیات` را می‌شمرد. از مهاجرتِ
`0125` که آن ستون‌ها آمدند این دو از هم جدا افتادند.

بنرِ سقفِ اعتبار از **قبل** فرمولِ کامل را داشت — پس اگر کسی دو عددِ متفاوت دیده
بود، همین بود. حالا هر سه خواننده از یک موتور می‌خوانند.

**«مرور فروش».** از یک تب (فهرستِ فاکتور) به شش نما رفت: کالا · مشتری · اسنادِ
فروش · اقلامِ فروش · انبار · پیش‌فاکتور، به‌علاوه‌ی **فاکتورهای ابطالی**. و یک ستونِ
تازه: **«فروخته و نرفته»** — مقداری که فاکتور شده ولی هنوز از انبار خارج نشده.
این عدد از `0125` وجود داشت و هیچ گزارشی نشانش نمی‌داد.

### ۳. تغییرِ کلیدهای داده در کارتِ حساب

کلیدهای `kind` به `source_type`ِ **قانونیِ** کوبیتا رسیدند:

| قبل | بعد |
|---|---|
| `check_in` / `check_out` | `check` |
| `receipt` | `receipt` (سربرگِ رسید) یا `treasury_receipt` (تراکنشِ بی‌سربرگ) |
| `payment` | `payment` یا `treasury_payment` |

کلیدهای قبلی ساختگی بودند و به هیچ سندی وصل نمی‌شدند. **اگر جایی بیرون از این
مخزن به آن کلیدها تکیه دارد، باید به‌روز شود.**

### ۴. باگ‌هایی که این استقرار می‌بندد

هر سه امروز روی تولید زنده‌اند:

* **برگشتِ فاکتورِ بی‌انبار خطای ۵۰۰ می‌داد.** `0125` ستونِ انبارِ فاکتور را
  nullable کرد و سیاستِ «دومرحله‌ای» فاکتورِ بی‌انبار را دست‌یافتنی کرد، ولی برگشت
  همان ستونِ خالی را در `stock_ledger.warehouse_id` می‌نوشت که `NOT NULL` است.
* **چکِ برگشت‌خورده به هیچ طرف حسابی نسبت داده نمی‌شد.** رویدادِ واخواست
  `contact_id` نداشت، پس در نمای اقلامِ باز چکِ برگشتی همچنان «پرداختِ مشتری» به
  نظر می‌رسید و **می‌شد بابتش فاکتور تسویه کرد**.
* **اعلامیه‌ی بدهکار/بستانکار درآمدِ فروش می‌ساخت.** سمتِ دومش همیشه «حسابِ فروش»
  بود، پس تهاترِ حساب، فروشِ ساختگی ثبت می‌کرد و شکلِ صورتِ سود و زیان را عوض
  می‌کرد. روی تولید **صفر** اعلامیه ثبت شده بود، پس داده‌ای درگیر نیست.

### ۵. حسابرسیِ تازه

چهار مدل به `audited_models()` اضافه شدند (۱۷ → ۲۱): **تنظیماتِ حقوق** (یعنی
پلکانِ مالیات)، **عاملِ حقوق**، **گروهِ مالیاتی** و **حکمِ حقوقی**. از این پس
تغییرشان ردِ حسابرسی می‌گذارد — عوض‌کردنِ یک پله، مالیاتِ همه‌ی کارکنان را عوض
می‌کند.

### ۶. نکته‌ی اجرایی

هیچ‌کدام از سه مهاجرت ردیفِ شمارنده یا حسابِ تازه نمی‌سازد، پس گاردِ
`next_document_number` این بار درگیر نیست. حساب‌های ۴۱۰۷/۴۱۹۸/۴۱۹۹ همچنان تنبل
می‌مانند و با اولین سندِ مربوطه می‌آیند.

---

> **وضعیتِ تولید پیش از این سه شاخه:** شاخه‌ی merge‌نشده‌ی `docs/deployed-0128` می‌گوید
> `0126`–`0128` روی تولید مستقر شده‌اند. اگر درست باشد، بخشِ «PRهای ۳۸ تا ۴۱» بالا دیگر
> صف نیست و `deploy.sh` فقط `0129` → `0134` را اجرا می‌کند. **سنجیده نشده** — پیش از
> استقرار `alembic current` را روی سرور ببینید. بسته‌ها را با `bash backend/scripts/pack.sh` بسازید.

## در صفِ استقرار — شاخه‌ی `feat/warehouse-receipt` (بعد از PRهای ۳۸ تا ۴۱)

شاخه master را (با `0126`، `0127` و `0128`ِ فیشِ حقوق) در خودش دارد، پس استقرارش هفت
مهاجرت را پشتِ سرِ هم اجرا می‌کند: `0126` → `0132`. همین زنجیره روی داده‌ی واقعیِ پایگاه دادهٔ
توسعه پاک اجرا شده؛ روی تولید اجرا **نشده**.

| شماره | چه می‌کند |
|---|---|
| `0129` | رسیدِ انبار بدونِ فاکتور: `warehouse_receipts.purchase_invoice_id` اختیاری، نوعِ رسید، تحویل‌دهنده/حمل‌کننده/واسطِ حمل، ارز، `journal_entry_id` |
| `0130` | `purchase_invoices.goods_in_transit` (پیش‌فرض `false`) — حسابِ «کالای در راه» |
| `0131` | بلوکِ حمل روی رسید + عرض‌دادنِ سه ستونِ بهای تمام‌شده به `Numeric(18, 4)` |
| `0132` | برگشتِ لنگرزده به رسید: ستون‌های لنگر، تحویل‌گیرنده، نوع، مبالغ توافقی؛ `purchase_returns.purchase_invoice_id` اختیاری |

### ۱. رفتاری که روی مسیرِ مستقر عوض می‌شود

* **فاکتورِ خریدِ تازه‌ی بی‌انبار** دیگر «موجودی کالا» را بدهکار نمی‌کند؛
  «کالای در راه» را می‌کند و رسیدِ انبار آن را به موجودی منتقل می‌کند. فاکتورهای
  موجود دست نمی‌خورند (`goods_in_transit = false`). حسابِ «کالای در راه» (کدِ
  پیش‌فرض `1114`) با اولین فاکتورِ بی‌انبار خودکار ساخته می‌شود.
* **رسیدِ مستقیم خودش سندِ حسابداری می‌زند.** رسیدِ فاکتوردار بدهی را دوباره
  نمی‌شناسد.
* **برگشت از خریدِ فاکتورِ بی‌انبار** به‌جای «موجودی کافی نیست (موجود: ۰)» پیام
  می‌دهد که برگشت را روی رسیدِ انبار ثبت کنید.
* ماژولِ خرید تبِ تازه‌ی «رسید انبار» دارد؛ اعلامیه پرداخت «رسید انبار» را به‌عنوان
  سندِ مرتبط می‌پذیرد.
* گزارشِ موجودی ارزش را به ریالِ صحیح گِرد می‌کند.

### ۲. باگ‌هایی که بسته می‌شوند

* روی گردشِ رسیدِ انبار، **برگشت از خرید اصلاً کار نمی‌کرد** — موجودی در انبارِ
  خالیِ فاکتور جست‌وجو می‌شد.
* دفتر و گزارشِ انبار روی بهای تمام‌شده‌ی بخش‌ناپذیر **ده‌ها ریال واگرا** می‌شدند
  (ستون‌ها ریالِ صحیح بودند).

### ۳. `downgrade` — دو دری که بعد از اولین استفاده بسته می‌شود

* `0129` و `0132` ستونِ `purchase_invoice_id` را دوباره `NOT NULL` می‌کنند؛ **بعد از
  اولین رسیدِ مستقیم یا برگشتِ رسیدِ واقعی** دیگر اجرا نمی‌شوند.
* `0131` ستون‌های بهای تمام‌شده را به `Numeric(18, 0)` برمی‌گرداند — بی‌خطا ولی
  بااتلاف: اعشارِ بها گِرد می‌شود.

### نکته‌ی اجرایی

* `0131` نوعِ `stock_ledger.unit_cost`، `items.average_cost` و
  `stock_batches.unit_cost` را عوض می‌کند. Postgres این جدول‌ها را **بازنویسی** می‌کند
  و در طولش قفلِ انحصاری می‌گیرد. اندازه‌ی `stock_ledger` تولید را نسنجیده‌ام؛ پیش از
  استقرار شمارِ ردیف‌هایش را ببینید و در ساعتِ کم‌کار بزنید.
* هیچ‌کدام backfill ندارند و ردیفی نمی‌نویسند. کلیدهای خارجیِ `0132` زیرِ
  `rls_disabled` ساخته می‌شوند (درسِ `0109`/`0120`).
* `fix/sales-return-warehouse` merge شد و تعارضِ importِ `services/returns.py` در
  همین شاخه حل شد (هر دو دسته import ماندند).

---

## در صفِ استقرار — شاخه‌ی `feat/warehouse-issue` (بعد از `feat/warehouse-receipt`)

شاخه روی `feat/warehouse-receipt` ساخته شده و `0133` را پشتِ `0132`ِ آن می‌نشاند؛
پس **اول رسید merge شود**. روی داده‌ی محلی `0132 → 0133 → 0132 → 0133` بی‌خطا اجرا
شد و backfill روی یک خروجِ واقعی سنجیده شد؛ روی تولید اجرا **نشده**.

| شماره | چه می‌کند |
|---|---|
| `0133` | خروجِ مستقل: نوع، منشأ، تحویل‌گیرنده، پیش‌فاکتور و مرکزِ هزینه روی `warehouse_issues`؛ شماره‌ی ردیف، معین، مقدارِ فرعی و بهای `Numeric(18, 4)` روی ردیف؛ `sales_invoice_id` و `sales_invoice_line_id` اختیاری؛ ستون‌های ابطال و `journal_entry_id` روی `stock_transfers` |

### ۱. رفتاری که روی مسیرِ مستقر عوض می‌شود

* **انتقال بین انبار** حالا قفل دارد، ردیف‌های یک کالا را جمع می‌زند، دوره‌ی بسته و
  انبارهای مجازِ کالا را می‌سنجد، و **میانِ دو انبار با دو معینِ موجودیِ متفاوت سند
  می‌زند**. تا امروز نمی‌زد: اگر روی تولید انبارهایی با `gl_account_id`ِ متفاوت
  هست، انتقال‌های *گذشته* میانشان سند ندارند و این استقرار گذشته را اصلاح نمی‌کند.
* **برگشتِ فروش** COGS را با بهای خروجِ واقعی برمی‌گرداند، نه میانگینِ روزِ فاکتور.
  در سیاستِ «خودکار» این دو یکی‌اند؛ فقط فاکتورِ «دومرحله‌ای» که میانِ فاکتور و
  خروجش خرید ثبت شده عددِ دیگری می‌گیرد.
* **ابطالِ خروجی که فاکتورش برگشتِ فعال دارد** ۴۰۹ می‌گیرد.
* برچسبِ منشأِ سند «خروج انبار فروش» شد «خروج انبار».
* ماژولِ انبار تبِ تازه‌ی «خروج انبار» دارد؛ فهرستِ زیرِ تبِ «انتقال» همان دفترِ خروج‌هاست.

### ۲. `downgrade`

* `0133` دو ستونِ فاکتور را دوباره `NOT NULL` می‌کند؛ **بعد از اولین خروجِ مستقیمِ
  واقعی** اجرا نمی‌شود.
* بهای واحدِ ردیفِ خروج به `Numeric(18, 0)` برمی‌گردد — بااتلاف.
* ستون‌های ابطالِ انتقال `drop` می‌شوند: انتقالِ باطل‌شده پس از بازگشت «معتبر» دیده
  می‌شود، در حالی که حرکتِ جبرانی‌اش در دفترِ انبار مانده.

### نکته‌ی اجرایی

* backfill سه `UPDATE` زیرِ `rls_disabled` دارد (تحویل‌گیرنده، شماره‌ی ردیف، معینِ COGS).
* تغییرِ نوعِ `warehouse_issue_lines.unit_cost` آن جدول را بازنویسی می‌کند؛ شمارِ
  ردیف‌هایش را پیش از استقرار ببینید.

---

## در صفِ استقرار — شاخه‌ی `feat/warehouse-issue-return` (بعد از `feat/warehouse-issue`)

شاخه روی `feat/warehouse-issue` ساخته شده و `0134` را پشتِ `0133` می‌نشاند؛ ترتیبِ merge:
رسید → خروج → برگشتِ خروج. روی داده‌ی محلی `0133 → 0134 → 0133 → 0134` بی‌خطا اجرا شد؛
روی تولید اجرا **نشده**.

| شماره | چه می‌کند |
|---|---|
| `0134` | جدول‌های `warehouse_issue_returns` و `warehouse_issue_return_lines` (با FORCE RLS)؛ ستونِ `sales_returns.stock_mode` (پیش‌فرض `inline`)؛ شمارنده‌ی `warehouse_issue_return` برای همه‌ی مستأجرها |

### ۱. رفتاری که روی مسیرِ مستقر عوض می‌شود

* **«برگشت از فروش» دو سند می‌سازد، نه یکی.** در سیاستِ «خودکار» (پیش‌فرضِ همه‌ی
  مستأجرها) کاربر فرقی نمی‌بیند: کالا و بهای تمام‌شده همان لحظه برمی‌گردند. ولی
  «موجودی / بهای تمام‌شده» از سندِ فاکتور برگشتی بیرون آمده و در سندِ «برگشت خروج
  انبار» می‌نشیند. جمعِ حساب‌ها همان است؛ اگر کسی سندِ برگشتِ فروش را برای دیدنِ COGS
  باز می‌کند، دیگر آن‌جا نیست.
* **برگشتِ فاکتورهای پیش از `0125` عوض نمی‌شود.** آن فاکتورها خروج ندارند و برگشتشان
  همان مسیرِ قدیمی را می‌رود (`stock_mode = inline`). روی تولید این یعنی **بیشترِ
  فاکتورهای موجود** — مسیرِ تازه فقط برای فاکتورهای بعد از استقرارِ `0125` است.
* **در سیاستِ «دومرحله‌ای»** فاکتور برگشتی دیگر موجودی را تکان نمی‌دهد؛ کالا با
  «ثبت برگشت به انبار» از دفترِ «فاکتورهای برگشتی» یا تبِ تازه‌ی «برگشت خروج انبار»
  برمی‌گردد.
* **سه ۴۰۹ِ تازه:** ابطالِ خروجی که برگشتِ معتبر دارد؛ ابطالِ فاکتور برگشتی‌ای که
  کاربر برایش برگشتِ انبار ثبت کرده؛ ابطالِ برگشتِ مستقیمی که خروجش بعداً فاکتور شده.
* ماژولِ انبار تبِ تازه‌ی «برگشت خروج انبار» دارد.

### ۲. باگ‌هایی که بسته می‌شوند

* **موجودیِ شبح:** خروجِ فاکتور باطل، فاکتور معتبر، بعد برگشت → موجودی بیش از خرید و
  COGSِ دو بار برگشته. حالا ۴۰۰ «هنوز از انبار خارج نشده». این مسیر از `0125` روی تولید
  هست؛ داده‌ی تولید را برای رخ‌دادنِ قبلی‌اش **نسنجیده‌ام** — پرسِ پیشنهادی: برگشت‌های
  `sales_return` روی فاکتوری که همه‌ی خروج‌هایش باطل‌اند.
* **معینِ اشتباه در برگشتِ دومرحله‌ای** (انبارِ دارای `gl_account_id`). اسنادِ گذشته
  اصلاح نمی‌شوند.

### ۳. `downgrade`

بعد از اولین برگشتِ واقعی **بااتلاف** است: دو جدول می‌روند ولی حرکت‌هایشان
(`warehouse_issue_return`) در دفترِ انبار می‌مانند، و فاکتور برگشتیِ `issue_return` پس از
بازگشت دیگر برگشتِ فیزیکی‌اش را نمی‌شناسد.

### نکته‌ی اجرایی

* `ADD COLUMN ... DEFAULT 'inline'` روی Postgres ۱۱+ بازنویسیِ جدول نیست.
* تنها نوشتنِ داده `INSERT`ِ شمارنده است، زیرِ `rls_disabled`.
