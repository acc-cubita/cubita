# یادداشتِ استقرار — آنچه پیش از هر deploy باید اعلام شود

این فایل **هشدار** است، نه تاریخچه. تاریخچه در
[PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md) §۱۰ می‌ماند؛ این‌جا فقط آنچه هنوز
مستقر **نشده** می‌نشیند. بعد از استقرارِ موفق، بخشش پاک می‌شود.

قالب و دلیلش در [AGENT_PLAYBOOK.md](AGENT_PLAYBOOK.md) §۸ است.

---

## در صف — انبارگردانی: شمارشِ کور و عکسِ لحظه‌ی شمارش (`0144`)

روی `master`، پشتِ `0143` (که مستقر شده). اولین شماره‌ی مهاجرتِ آزاد پس از این: **`0145`**.

1. **مهاجرت `0144`** — دو ستون، یک تهی‌پذیرسازی، و یک backfillِ شماره:
   * `stock_count_sessions.number` (+ یکتای `tenant_id, number`) با **backfill به ترتیبِ ساخت**،
     و `document_counters` هم از همان‌جا ادامه می‌دهد؛
   * `stock_count_lines.counted_qty` **تهی‌پذیر** می‌شود (`NULL` = نشمرده)، بدونِ backfill —
     ردیف‌های موجود عددشان را نگه می‌دارند و جلسه‌های باز مثلِ دیروز رفتار می‌کنند؛
   * `stock_count_lines.counted_at` و `counted_by_id`.
2. **رفتارِ عوض‌شده روی مسیرِ مستقر — این مهم‌ترین بخش است:**
   * **حرکتِ وسطِ شمارش دیگر دو بار شمرده نمی‌شود.** عکسِ سیستمی از «لحظه‌ی بازکردنِ جلسه»
     به **«لحظه‌ی ثبتِ شمارش»** رفت. پیش از این: موجودی ۱۰۰ → جلسه باز شد → ۱۰ تا فروخته شد →
     شمارنده ۹۰ شمرد → ثبت → **موجودی ۸۰** به‌جای ۹۰، و سندی برای کسری‌ای که وجود نداشت.
   * **جلسه‌ی تازه دیگر شمارش را از پیش پر نمی‌کند.** عدد از شمارنده می‌آید. جلسه‌ای که
     هیچ ردیفش شمرده نشده **ثبت نمی‌شود** (۴۰۰) — پیش‌تر «بدونِ مغایرت» ثبت می‌شد.
   * **دامنه‌ی پیش‌فرض تنگ‌تر شد:** فقط کالاهایی که در همان انبار سابقه‌ی حرکت دارند، نه کلِ
     کاتالوگ. `item_ids` هم پذیرفته می‌شود (شمارشِ چرخه‌ای).
   * **`PUT /api/stock-counts/{id}/counts`** حالا `counted_qty: null` می‌پذیرد («شمارش را پس بگیر»)
     و کاربرِ ثبت‌کننده را ذخیره می‌کند.
   * دو مسیرِ تازه: `GET /{id}/tags` (برگه‌های شمارش، **بدونِ موجودیِ سیستمی**) و
     `GET /{id}/drift` (کالاهایی که پس از شمارش حرکت کرده‌اند — گزارش، نه گارد).
   * `variance` و `counted_qty` در پاسخ **می‌توانند `null` باشند**. مصرف‌کننده‌ی قدیمی که
     عدد انتظار دارد باید به‌روز شود؛ تنها مصرف‌کننده رابطِ همین شاخه است.
3. **پیش از استقرار بشمارید** — جلسه‌های بازی که با مدلِ قدیمی ساخته شده‌اند شمارششان از
   پیش پر است و پس از استقرار «شمرده‌شده» حساب می‌شوند:
   ```sql
   SELECT count(*) FROM stock_count_sessions WHERE status = 'open';
   ```
   اگر ناصفر بود، بهتر است پیش از استقرار ثبت یا لغو شوند.
4. **حسابداری:** هیچ سندِ موجودی عوض نمی‌شود. `StockCountSession` به حسابرسی اضافه شد.
5. **جابه‌جایی در رابط:** تبِ «انبارگردانی» دکمه‌ی «برگه‌های شمارش» و «بررسی حرکت پس از شمارش»
   گرفت؛ ستونِ شمارش برای ردیفِ نشمرده «نشمرده» نشان می‌دهد و مغایرتش خط تیره است.
6. **`downgrade`ِ پُرافت:** «نشمرده» به عددِ سیستمی برمی‌گردد (بی‌اختلاف، تا چیزی از انبار
   بیرون نریزد) و شماره‌ی جلسه‌ها می‌رود.

## در صف — گزارش‌های انبار: ابعاد، مجوزِ بها، ردیابیِ سریال (`0145`)

**پشتِ `0144`** (شاخه‌ی `feat/stocktake-blind-count`) — آن باید اول merge شود.
اولین شماره‌ی مهاجرتِ آزاد پس از این: **`0146`**.

1. **مهاجرت `0145`** — یک جدولِ تازه، بدونِ تغییرِ جدولِ موجود:
   * `serial_events` با RLS، ایندکسِ یکتای **جزئیِ** `(tenant, serial, source_type, source_id)`
     روی `source_id IS NOT NULL`؛
   * **backfill:** برای هر سریالِ موجود یک رویدادِ `receipt` از سندِ مبدأِ بچِ خودش.
     آنچه امروز می‌دانیم از دست نمی‌رود.
2. **رفتارِ عوض‌شده روی مسیرِ مستقر:**
   * **مجوزِ گزارش‌های انبار عوض شد.** `GET /api/reports/inventory` و `/kardex/{id}` از
     `accounting.view` به **`inventory.view`** رفتند، و مبالغ برای کسی که `accounting.view`
     ندارد **`None`** برمی‌گردند (نه صفر). یعنی **انباردار از این پس گزارشِ موجودی را
     می‌بیند** — تا امروز نمی‌دید — و کسی که فقط `inventory` دارد بها را نمی‌بیند.
   * **فیلدهای مبلغ در پاسخ تهی‌پذیر شدند** (`unit_cost`, `stock_value`, `average_cost`, …).
     مصرف‌کننده‌ای که عدد انتظار دارد باید `null` را تحمل کند؛ تنها مصرف‌کننده رابطِ
     همین شاخه است.
   * افزودنِ سریال به یک بچ حالا **رویدادِ ورود** هم ثبت می‌کند.
   * سه مسیرِ تازه: `GET /api/reports/inventory-breakdown?dimension=supplier|customer|purpose`،
     `GET /api/serials/search`، `POST /api/serials/assign`.
3. **حسابداری:** هیچ سندی عوض نمی‌شود؛ این فصل فقط خواندنی است جز ثبتِ رویدادِ سریال.
4. **جابه‌جایی در رابط:** تبِ «جستجوی سریال» به ماژولِ انبار اضافه شد.
5. **`downgrade`ِ پُرافت:** کلِ `serial_events` می‌رود. رویدادِ ورود بازساختنی است (از بچ)،
   ولی **رویدادهای خروج و برگشت جای دیگری ذخیره نشده‌اند و برنمی‌گردند**.

---

## در صف — نوعِ فروش: پروفایلِ حسابداری قابلِ تنظیم شد (`0146`)

> **پشتِ `0145`.** آن باید اول برود.

1. **مهاجرت `0146` چهار ستون به `sale_types` اضافه می‌کند** — `code`، `title2`،
   `goods_return_account_id`، `service_return_account_id` — به‌علاوه‌ی یک ایندکسِ
   یکتای **جزئی** روی `(tenant_id, code) WHERE code IS NOT NULL`. **بدونِ
   backfill**: ردیف‌های موجود بی‌کد می‌مانند و حسابِ برگشتشان خالی.
   دو ستونِ حساب کلیدِ خارجی‌اند و داخلِ `rls_disabled(conn, ("sale_types", "accounts"))`
   اضافه می‌شوند — درسِ همان شبِ `0139`.
2. **رفتارِ عوض‌شده روی مسیرِ مستقر:**
   * **`PATCH /api/sales-ops/sale-types/{id}` از «همه را بنویس» به «فقط
     فرستاده‌شده‌ها» تغییر کرد.** این یک باگِ خفته را می‌بندد: بدنه‌ی بدونِ فیلدهای
     حساب، ۲۰۰ می‌گرفت و **هر هفت حساب را `NULL`** می‌کرد. هیچ کلاینتی امروز این
     مسیر را صدا نمی‌زند، پس ریسکِ واقعی صفر است.
   * **برگشت از فروش دیگر لزوماً به حسابِ سراسری نمی‌رود.** اگر نوعِ فروشِ
     فاکتورِ **اصلی** حسابِ برگشت داشته باشد، کالا و خدمت جدا می‌نشینند. اگر
     نداشته باشد — که امروز در همه‌ی کسب‌وکارها همین است — رفتار **دقیقاً** همان
     دیروز می‌ماند.
   * **برگشت از فروش حالا همان حسابِ دریافتنیِ خودِ فاکتور را می‌بندد**، نه
     دریافتنیِ سراسری. تا امروز هیچ فاکتوری حسابِ دریافتنیِ اختصاصی نداشت (هیچ
     صفحه‌ای آن را پر نمی‌کرد)، پس این هم روی داده‌ی موجود بی‌اثر است.
   * `POST`/`PATCH`ِ نوعِ فروش حالا حسابِ **گروه** یا **غیرفعال** را ۴۰۰ می‌دهند،
     به‌جای اینکه غافلگیری به لحظه‌ی ثبتِ اولین فاکتور موکول شود.
3. **حسابداری:** هیچ سندِ صادرشده‌ای عوض نمی‌شود. نگاشتِ تازه فقط روی فروش‌ها و
   برگشت‌های **بعدی** می‌نشیند — تستِ `test_changing_the_mapping_never_rewrites_a_posted_journal`
   همین را نگه می‌دارد.
4. **حسابرسی:** `SaleType` به `audited_models()` اضافه شد. از این پس تغییرِ
   حسابِ یک نوعِ فروش رد می‌گذارد؛ تا امروز نمی‌گذاشت.
5. **رابط:** صفحه‌ی «نوع فروش» هفت انتخابگرِ حساب گرفت و برای اولین بار **ویرایش**
   و **بایگانی** دارد. تا امروز فقط «ثبت» بود و آن هفت ستون هیچ دَرِ ورودی نداشتند.
6. **`downgrade`ِ پُرافت:** چهار ستون می‌روند. کد، عنوانِ دوم و **هر دو حسابِ برگشتِ
   تنظیم‌شده** برنمی‌گردند؛ سندهای صادرشده دست‌نخورده می‌مانند.

---

## در صف — عوامل حقوق: ورودیِ دوره‌ای (`0147`)

1. **مهاجرت `0147` یک جدولِ تازه می‌سازد** — `payroll_factor_inputs`
   (کارمند، دوره، عامل، مبلغ، یادداشت)، با RLS و یکتاییِ
   `(tenant, employee, period, factor)`. **بدونِ backfill**: جدول خالی متولد
   می‌شود، و جدولِ خالی یعنی هیچ فیشی عددش عوض نمی‌شود.
2. **قیدِ `ck_payslip_lines_origin` گشاد می‌شود** تا `input` را هم بپذیرد.
   اعتبارسنجیِ `CHECK` اسکنِ مستقیمِ heap است و — برخلافِ کلیدِ خارجی — مشمولِ
   RLS نیست، پس `rls_disabled` لازم ندارد.
3. **رفتارِ عوض‌شده روی مسیرِ مستقر:**
   * **عاملِ `kind = 'variable'` دیگر روی حکمِ تازه پذیرفته نمی‌شود** (۴۰۰).
     گارد فقط روی **ذخیره‌ی تازه** است — مثلِ گاردِ `is_active` در ۰۱۴۰ — پس
     حکم‌های موجود و فیش‌های صادرشده دست نمی‌خورند. **ولی اگر کسب‌وکاری امروز
     عاملِ متغیر روی حکم دارد، اولین ویرایشِ آن حکم ۴۰۰ می‌گیرد** و باید یا
     نوعِ عامل را «قراردادی» کند یا آن ردیف را بردارد. این تنها نقطه‌ی اصطکاکِ
     این مهاجرت است.
   * دو مسیرِ تازه: `GET/PUT /api/payroll-periods/{id}/factor-inputs`.
   * ردیفِ فیش می‌تواند `origin = "input"` داشته باشد؛ مصرف‌کننده‌ای که این
     چهار مقدار را سخت‌کد کرده باشد باید پنجمی را تحمل کند (تنها مصرف‌کننده
     رابطِ همین شاخه است).
4. **حسابداری:** سندِ فیش از همان تفکیک ساخته می‌شود، پس مبلغِ ورودی هم به حسابِ
   اختصاصیِ عاملش می‌رود اگر تنظیم شده باشد — تستی همین را قفل می‌کند. هیچ سندِ
   صادرشده‌ای عوض نمی‌شود.
5. **حسابرسی:** `PayrollFactorInput` به `audited_models()` اضافه شد.
6. **رابط:** جدولِ «ورودیِ عوامل این دوره» کنارِ جدولِ کارکرد آمد (هم در پنلِ
   کلاسیک هم در ویزارد). فرمِ عامل حالا صریح می‌گوید «متغیر» یعنی چه، و
   «فوق‌العاده است» برچسبِ صادق گرفت: **هیچ محاسبه‌ای از آن نمی‌خوانَد.**
7. **`downgrade`ِ پُرافت — و عمداً شکننده:** اگر فیشی با ردیفِ `origin = 'input'`
   صادر شده باشد، `downgrade` **شکست می‌خورد** (قیدِ باریکِ قدیم نمی‌پذیردش).
   پاک‌کردنِ آن ردیف‌ها یعنی بازنویسیِ فیشِ صادرشده؛ خطا بهتر از آن است. روی
   اسکیمای یک‌بارمصرف همین رفتار آزموده شد.

---

## مستقرشده — تا `0143`

آخرین استقرارِ موفقِ تولید: **۱۴۰۵/۰۶/۲۲ — نسخه‌ی `0143`**.

آنچه در آن استقرار رفت (`0136`..`0143`): ماژول حقوق و دستمزد، فاکتور خرید خدمات،
اعلامیه بدهکار/بستانکار نوبتِ دوم، قیمت‌گذاری اسناد انبار نوبتِ اول، و جداکردنِ حسابِ
«تعدیل گِرد کردن فروش» از درآمدِ فروشِ آنلاین. شرحش در
[PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md) §۱۰ است.

> **درسِ آن استقرار — برای هر که مهاجرتِ بعدی را می‌نویسد.** اولین تلاش وسطِ `0139`
> شکست خورد: اعتبارسنجیِ `ADD FOREIGN KEY` یک کوئریِ واقعی است و مشمولِ RLS، پس
> روی جدولی که ردیف دارد با `invalid input syntax for type uuid: ""` می‌ترکد. هر
> افزودنِ کلیدِ خارجی به جدولِ موجود باید داخلِ `rls_disabled` باشد — با **هر دو**
> جدولِ طرفِ قید. و **روی این ماشین قابلِ آزمون نیست**: تولید PostgreSQL 14 است و
> توسعه 17، و ۱۷ آن اسکن را حذف می‌کند. توضیحِ کامل در
> [`app/migration_utils.py`](backend/app/migration_utils.py).

بسته‌ها را با `bash backend/scripts/pack.sh <مقصد>` بسازید، نه با `git archive`ِ
دستی — سه استقرارِ پشتِ هم به‌خاطرِ شکلِ بسته شکستند و آن اسکریپت همان بررسی‌ها
را پیش از آپلود انجام می‌دهد. روی ویندوز مقصد را با مسیرِ `/c/...` بدهید، نه
`C:/...`؛ `tar` دومی را نامِ میزبانِ راه‌دور می‌خواند.
