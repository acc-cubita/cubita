# یادداشتِ استقرار — آنچه پیش از هر deploy باید اعلام شود

این فایل **هشدار** است، نه تاریخچه. تاریخچه در
[PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md) §۱۰ می‌ماند؛ این‌جا فقط آنچه هنوز
مستقر **نشده** می‌نشیند. بعد از استقرارِ موفق، بخشش پاک می‌شود.

قالب و دلیلش در [AGENT_PLAYBOOK.md](AGENT_PLAYBOOK.md) §۸ است.

---

## در صف — فاکتور خرید خدمات (`0135`)

شاخه‌ی `feat/service-purchase-invoice`. آخرین استقرارِ موفق: **۱۴۰۵/۰۶/۲۲، نسخه‌ی
`0134`**. اولین شماره‌ی مهاجرتِ آزاد پس از این: **`0136`**.

1. **مهاجرت `0135`** — هیچ `UPDATE`ی روی داده‌ی موجود ندارد:
   * `purchase_invoices.kind` (همه‌ی موجودها `goods`) و `total_deductions` (صفر)؛
   * قیدِ یکتاییِ شماره از (مستأجر، شماره) به (مستأجر، نوع، شماره) — فاکتور خرید
     خدمات سریِ شماره‌ی خودش را دارد؛
   * `purchase_invoice_lines.expense_account_id` (موجودها `NULL`)؛
   * دو جدولِ تازه با FORCE RLS: `purchase_deduction_types`، `purchase_invoice_deductions`؛
   * شمارنده‌ی `service_purchase_invoice` برای همه‌ی مستأجرها.
2. **رفتارِ عوض‌شده روی مسیرِ مستقر:**
   * فهرستِ فاکتور خرید: «تسویه‌شده» و «مانده» از این پس از **موتورِ تسویه** و **بدهیِ
     واقعی** می‌آیند. پیش از این همیشه «تسویه‌نشده» بود و مانده = جمعِ فاکتور؛ فاکتوری
     که تخصیصِ تسویه دارد حالا «تسویه جزئی/کامل» نشان می‌دهد.
   * فاکتوری که فقط خدمت دارد دیگر «تحویل‌نشده» نیست، و **رسیدِ انبار برای ردیفِ خدمت
     رد می‌شود** (۴۰۰) — پیش از این رسیدی بی‌حرکت ساخته می‌شد.
   * برگشت از خرید برای ردیفِ خدمت حسابی را بستانکار می‌کند که فاکتور خورد، نه نگاشتِ
     امروزِ خدمت (فقط ردیف‌های از این پس؛ قدیمی‌ها همان رفتارِ قبل).
   * تبِ «فاکتور خرید» فقط فاکتورهای کالا را فهرست می‌کند؛ فاکتورهای خدمات تبِ خودشان را دارند.
3. **حسابداری:** دو نقشِ تازه، با ساختِ تنبل در اولین کسر: «مالیات تکلیفی پرداختنی»
   (`2112`) و «حق بیمه پرداختنی اشخاص ثالث» (`2113`) — جدا از `2103`ِ حقوق. در فاکتور
   خرید خدمات بستانکارِ تأمین‌کننده = جمعِ فاکتور − کسورات.
4. **جابه‌جایی:** ماژولِ خرید دو تب گرفت («فاکتور خرید خدمات»، «انواع کسورات»)؛ چیزی
   برداشته نشد.
5. **`downgrade`ِ بااتلاف:** `0135` پس از اولین فاکتور خرید خدمات — کسورات و نوعِ سند
   می‌روند ولی سندِ حسابداری‌شان می‌ماند؛ اگر شماره‌ای در دو سری تکرار شده باشد،
   بازگرداندنِ قیدِ قدیمی شکست می‌خورد.

## در صف — اعلامیه بدهکار و بستانکار، نوبتِ دوم (`0136`)

شاخه‌ی `feat/credit-debit-notice`، **پشتِ `0135`** (PR #47) — آن باید اول مستقر شود. اولین
شماره‌ی مهاجرتِ آزاد پس از این: **`0137`**.

1. **مهاجرت `0136`** — ستون‌ها و یک backfill زیرِ `rls_disabled`:
   * `credit_debit_notes`: `base_amount`، `voided_by_id`، `void_reason`؛
   * `credit_debit_note_lines.base_amount` (NOT NULL، `> 0`)؛
   * `base_amount = amount` برای ردیف‌ها و سربرگ‌های موجود — همان عددی که واقعاً در دفتر
     نشسته، چون کدِ قبلی مبلغ را بدونِ نرخ به سند می‌فرستاد.
   * **پیش از استقرار روی تولید بشمارید** (در `0127` صفر بود):
     `SELECT count(*), count(*) FILTER (WHERE currency_code <> 'IRR') FROM credit_debit_notes;`
     اعلامیه‌ی ارزیِ موجود سندش با مبلغِ ارزی خورده و backfill همان را حفظ می‌کند؛ اصلاحش
     ابطال و صدورِ دوباره است، نه تغییرِ دستیِ ستون.
2. **رفتارِ عوض‌شده روی مسیرِ مستقر:**
   * **مجوز:** صدور و دیدن با `invoices` **یا** `accounting` (پیش‌تر `sales` که در رجیستری
     نیست — عملاً فقط مالک)؛ ابطال با `accounting.delete` (پیش‌تر `sales.delete`). حسابدار و
     فروشنده از این پس اعلامیه صادر می‌کنند.
   * **`GET /api/sales-ops/notes`** حالا `Page` (`items`، `next_cursor`) با فیلترِ تاریخ، طرف حساب،
     وضعیت و جست‌وجو برمی‌گرداند؛ پارامترِ `kind` برداشته شد. تنها مصرف‌کننده رابطِ همین شاخه است.
   * **ارز اعمال می‌شود** (مبلغ × نرخ، ردیف‌به‌ردیف)؛ مبلغِ اعشاری ۴۰۰.
   * **گاردِ حساب:** سمتِ دارای طرف حساب فقط دریافتنی/پرداختنی؛ سمتِ بی‌طرف‌حساب نه آن دو، نه
     حساب‌های خزانه، انبار و مالیات (با نقش یا نگاشتِ بانک، صندوق و انبار) — ۴۰۰.
   * **ابطال:** قفلِ ردیف؛ سندِ معکوس با `reverses_entry_id` و منبعِ `void_credit_debit_note`؛
     تاریخِ پیش‌فرض = تاریخِ اعلامیه (پیش‌تر امروز). اگر دوره‌ی اعلامیه بسته باشد، کاربر تاریخ می‌دهد.
   * کارتِ حساب به هر ردیف `source_id`، `entry_number` و `entry_date` افزود (فیلدِ اضافه).
3. **حسابداری:** هیچ سندِ موجودی عوض نمی‌شود.
4. **جابه‌جایی:** «اعلامیه بدهکار بستانکار» در گروهِ «تامین‌کنندگان و انبار» هم آمد (همان صفحه)
   و برای کسب‌وکارِ فقط-خرید هم دیده می‌شود؛ چیزی برداشته نشد.
5. **`downgrade`ِ بااتلاف:** `0136` معادلِ ریالیِ اعلامیه‌های ارزی و کاربر/دلیلِ ابطال را می‌برد.

## در صف — قیمت‌گذاری اسناد انبار، نوبتِ اول (بدونِ مهاجرت)

شاخه‌ی `feat/inventory-valuation`، **پشتِ `0136`** (PR #52، که خودش پشتِ #47 است). مهاجرت ندارد؛
اولین شماره‌ی آزاد همان **`0137`** می‌ماند.

1. **مهاجرت:** ندارد.
2. **رفتارِ عوض‌شده روی مسیرِ مستقر:**
   * **گاردِ خطِ زمان (۴۰۹):** ثبتِ پیش‌تاریخِ خروج، انتقال، تعدیل، برگشت از خرید، انبارگردانی یا
     مصرفِ تولید که موجودیِ یک روزِ گذشته را منفی کند رد می‌شود؛ همین‌طور ابطالِ ورودی (خرید، رسید،
     برگشت از فروش، برگشتِ خروج، انتقال) که خروجِ بعدی به آن تکیه کرده. پیش‌تر فقط موجودیِ امروز
     سنجیده می‌شد. همگام‌سازیِ فروشِ آفلاین (`X-Cubita-Offline-Replay`) معاف است.
   * **بهای سندِ پیش‌تاریخ** میانگینِ همان روز است، نه امروز.
   * **میانگین پس از ابطال یا ثبتِ پیش‌تاریخ** از بازپخش به ترتیبِ تاریخ ساخته می‌شود. روی کالایی
     که امروز حرکتِ پیش‌تاریخ یا برگشت از خرید دارد، **اولین ابطال یا ثبتِ پیش‌تاریخ میانگینش را
     جابه‌جا می‌کند** — به سمتِ عددِ درست. پیش از استقرار بشمارید (کالاهای دارای حرکتِ پیش‌تاریخ):
     `SELECT count(DISTINCT l.item_id) FROM stock_ledger l WHERE l.source_type <> 'void' AND EXISTS (SELECT 1 FROM stock_ledger e WHERE e.item_id = l.item_id AND e.source_type <> 'void' AND e.seq < l.seq AND e.entry_date > l.entry_date);`
     پس از استقرار، «بررسی یکپارچگی» همین کالاها را زیرِ «ارزش‌گذاریِ منقضی» نشان می‌دهد.
   * **`PATCH /api/items/{id}` با `average_cost`** برای کالای دارای موجودی ۴۰۹ است.
   * **`GET /api/reports/inventory`** پارامترِ `date_from` و فیلدهای اول/ورود/خروج/`book_value`/
     `stale_from` گرفت (فیلدِ اضافه؛ موبایل بی‌پارامتر همان رفتار را دارد). ارزشِ `as_of` حالا ارزشِ
     همان روز است، نه مقدارِ آن روز × میانگینِ امروز.
   * **`GET /api/reports/kardex/{id}`** به ترتیبِ تاریخ است و فیلدهای ارزش گرفت؛ `unit_cost` حالا
     بهای ارزش‌گذاری است و بهای خودِ سند `recorded_unit_cost`.
   * **بررسی یکپارچگی** دو بررسیِ تازه دارد (هشدار — «سالم» را قرمز نمی‌کنند) که روی دفترِ واقعی
     احتمالاً یافته دارند؛ با فیلترِ شماره‌ی سند یا بُعد اجرا نمی‌شوند.
3. **حسابداری:** هیچ سندِ موجودی عوض نمی‌شود و سندی نوشته نمی‌شود.
4. **جابه‌جایی:** «ارزش موجودی انبار» در گزارش‌ها از لحظه‌ای به بازه‌ای رفت؛ چیزی برداشته نشد.
5. **`downgrade`:** ندارد.

بسته‌ها را با `bash backend/scripts/pack.sh <مقصد>` بسازید، نه با `git archive`ِ
دستی — سه استقرارِ پشتِ هم به‌خاطرِ شکلِ بسته شکستند و آن اسکریپت همان بررسی‌ها
را پیش از آپلود انجام می‌دهد. روی ویندوز مقصد را با مسیرِ `/c/...` بدهید، نه
`C:/...`؛ `tar` دومی را نامِ میزبانِ راه‌دور می‌خواند.
