# یادداشتِ استقرار — آنچه پیش از هر deploy باید اعلام شود

این فایل **هشدار** است، نه تاریخچه. تاریخچه در
[PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md) §۱۰ می‌ماند؛ این‌جا فقط آنچه هنوز
مستقر **نشده** می‌نشیند. بعد از استقرارِ موفق، بخشش پاک می‌شود.

قالب و دلیلش در [AGENT_PLAYBOOK.md](AGENT_PLAYBOOK.md) §۸ است.

---

## مستقرشده — تا `0149`

**هیچ مهاجرتی در صف نیست.** آخرین استقرارِ موفقِ تولید: **۱۴۰۵/۰۶/۲۳ — نسخه‌ی `0149`**.
اولین شماره‌ی مهاجرتِ آزادِ بعدی: **`0150`**.

آنچه در آن استقرار رفت (`0149`): قیمت‌گذاری اسناد انبار، نوبتِ دوم — اجرای ثبت‌شده با
پیش‌نمایش، سندِ اصلاحی و ابطال (دو جدولِ تازه، FORCE RLS، بی `UPDATE` و بی کلیدِ خارجی
به جدولِ دارای ردیف). پس از استقرار از راهِ دور تأیید شد: `alembic current` روی سرور
`0149`، و `/health`، `/api/health`، `/` هر سه ۲۰۰. شرحش در
[PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md) §۱۰ است.

> **رفتارِ تازه روی مسیرِ مستقر:** ابطالِ سندی که بهایش در یک اجرای قیمت‌گذاری اصلاح
> شده ۴۰۹ است تا آن اجرا باطل شود (خروج، رسید، برگشت‌ها، انتقال) — تا وقتی اجرایی ثبت
> نشده، هیچ ابطالی تغییر نمی‌کند.

## مستقرشده — تا `0148`

**هیچ مهاجرتی در صف نیست.** آخرین استقرارِ موفقِ تولید: **۱۴۰۵/۰۶/۲۳ — نسخه‌ی `0148`**.

آنچه در آن استقرار رفت (`0144`..`0148`): انبارگردانیِ کور و عکسِ لحظه‌ی شمارش · ابعادِ
گزارشِ انبار و رویدادهای سریال · پروفایلِ حسابداریِ نوعِ فروش · ورودیِ دوره‌ایِ عواملِ
حقوق · رمزگذاریِ کلیدِ امضای مؤدیان در حالتِ سکون. شرحش در
[PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md) §۱۰ است.

> **خبرِ خوبِ این استقرار:** `0146` دو کلیدِ خارجی به `sale_types` اضافه کرد — جدولی که
> روی تولید ردیف دارد — و روی **PostgreSQL 14.24** بی‌خطا رد شد. یعنی الگوی
> `rls_disabled` بالاخره آزمونِ واقعی داد و جواب داد. تا امروز فقط استدلال داشتیم و یک
> شکستِ تاریخی (شبِ `0139`). **ولی هشدارِ زیر همچنان برقرار است**: اختلافِ نسخه‌ی
> Postgres بینِ توسعه و تولید سرِ جایش است، و هر مهاجرتِ تازه همچنان ناآزموده روی ۱۴
> می‌رود.

> **قدمِ دستی‌ای که ماند:** `SECRETS_KEY` حالا در `/opt/hesabdari/.env` هست و مهاجرتِ
> `0148` با آن اجرا شد. **اگر گم شود کلیدهای امضا برنمی‌گردند** — هر کسب‌وکار باید
> دوباره از کارپوشه بگیرد و آپلود کند. کنارِ نسخه‌ی پشتیبانِ دیتابیس نگه داشته نشود.

## مستقرشده — تا `0143`

آخرین استقرارِ موفقِ تولید: **۱۴۰۵/۰۶/۲۲ — نسخه‌ی `0143`**.

آنچه در آن استقرار رفت (`0136`..`0143`): ماژول حقوق و دستمزد، فاکتور خرید خدمات،
اعلامیه بدهکار/بستانکار نوبتِ دوم، قیمت‌گذاری اسناد انبار نوبتِ اول، و جداکردنِ حسابِ
«تعدیل گِرد کردن فروش» از درآمدِ فروشِ آنلاین. شرحش در
[PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md) §۱۰ است.

> **درسِ آن استقرار — برای هر که مهاجرتِ بعدی را می‌نویسد.** اولین تلاش وسطِ `0139`
> شکست خورد: اعتبارسنجیِ `ADD FOREIGN KEY` یک کوئریِ واقعی است و مشمولِ RLS، پس
> روی جدولی که ردیف دارد با `invalid input syntax for type uuid: ""` می‌ترکد. هر
> افزودنِ کلیدِ خارجی به جدولِ موجود باید داخلِ `rls_disabled` باشد — با **هر دو**
> جدولِ طرفِ قید. و **روی این ماشین قابلِ آزمون نیست**: تولید PostgreSQL 14 است و
> توسعه 17، و ۱۷ آن اسکن را حذف می‌کند. توضیحِ کامل در
> [`app/migration_utils.py`](backend/app/migration_utils.py).

بسته‌ها را با `bash backend/scripts/pack.sh <مقصد>` بسازید، نه با `git archive`ِ
دستی — سه استقرارِ پشتِ هم به‌خاطرِ شکلِ بسته شکستند و آن اسکریپت همان بررسی‌ها
را پیش از آپلود انجام می‌دهد. روی ویندوز مقصد را با مسیرِ `/c/...` بدهید، نه
`C:/...`؛ `tar` دومی را نامِ میزبانِ راه‌دور می‌خواند.
