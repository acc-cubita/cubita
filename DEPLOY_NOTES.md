# یادداشتِ استقرار — آنچه پیش از هر deploy باید اعلام شود

این فایل **هشدار** است، نه تاریخچه. تاریخچه در
[PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md) §۱۰ می‌ماند؛ این‌جا فقط آنچه هنوز
مستقر **نشده** می‌نشیند. بعد از استقرارِ موفق، بخشش پاک می‌شود.

قالب و دلیلش در [AGENT_PLAYBOOK.md](AGENT_PLAYBOOK.md) §۸ است.

---

## در صفِ استقرار — PRهای ۳۶ و ۳۷

**ترتیبِ merge: #36 → #37.** دو مهاجرت با هم اجرا می‌شوند.
آخرین استقرارِ موفق: ۱۴۰۵/۰۶/۲۱، نسخه‌ی مهاجرت `0125`.

اولین شماره‌ی مهاجرتِ آزاد: **`0128`**.

| شماره | چه می‌کند |
|---|---|
| `0126` | سیاستِ صدورِ فاکتور فروش (خودکار / دومرحله‌ای) روی مستأجر |
| `0127` | اعلامیه‌ی بدهکار/بستانکار دوسویه می‌شود + جدولِ `credit_debit_note_lines` |

### ۱. رفتاری که روی مسیرِ مستقر عوض می‌شود

**«ثبت فاکتور» دوباره خودکار شد.** مهاجرتِ `0125` آن را دومرحله‌ای کرده بود؛
`0126` سیاستش را به خودِ کسب‌وکار می‌دهد و **پیش‌فرض `immediate` است** — یعنی
همان رفتاری که کاربران سال‌ها داشته‌اند. گزینه در «تنظیمات ← شخصی‌سازی ←
صدور فاکتور فروش».

> اگر پیامِ قبلی («ثبت فاکتور دیگر خودش سند و کسرِ موجودی نمی‌زند») را به
> کاربران داده‌اید، **باید اصلاح شود**: از این استقرار به بعد دوباره می‌زند،
> مگر اینکه کسب‌وکار «دومرحله‌ای» را انتخاب کند.

### ۲. دو باگِ حسابداری که `0127` می‌بندد

هر دو امروز روی تولید زنده‌اند. **هیچ‌کدام هنوز اثر نکرده‌اند**، چون در هر شش
مستأجر صفر اعلامیه ثبت شده — ولی اولین کسی که این فرم را باز کند به هر دو
می‌خورد:

* **اعلامیه، «فروش» را می‌زد.** سمتِ دومِ هر اعلامیه حسابِ درآمدِ فروش بود، پس
  تعدیلِ مانده‌ی یک طرف حساب **درآمد می‌ساخت** و شکلِ صورتِ سود و زیان را عوض
  می‌کرد.
* **اعلامیه در کارتِ حساب دیده نمی‌شد.** سندش دریافتنی را تکان می‌داد و
  `get_contact_statement` خبر نداشت؛ صورت‌حساب و دفتر به اندازه‌ی همان اعلامیه
  از هم جدا می‌افتادند. و چون `contact_balance` هم نمی‌دیدش، اعلامیه‌ی بستانکار
  **سقفِ اعتبارِ مشتری را آزاد نمی‌کرد**.

### ۳. قابلیتی که تازه ممکن می‌شود

**تهاترِ تأمین‌کننده با مشتری.** «طلبِ ما از فلانی» در برابر «بدهیِ ما به
بهمانی»، بدونِ هیچ دریافت و پرداختی. تا امروز سمتِ تأمین‌کننده اصلاً وجود
نداشت و برای طرف‌حسابی که تأمین‌کننده بود، سند روی معینِ *دریافتنی* می‌نشست.

### ۴. `downgrade`

`0127` جدولِ `credit_debit_note_lines` را `drop` می‌کند. چون امروز خالی است،
بازگشت بی‌هزینه است — ولی **بعد از اولین اعلامیه‌ی واقعی دیگر نیست**.

### نکته‌ی اجرایی

`0126` و `0127` هیچ‌کدام ردیفی نمی‌نویسند و هیچ backfillی ندارند. شمارنده‌ی
`credit_debit_note` از مهاجرتِ `0086` برای همه‌ی مستأجرها موجود است.
