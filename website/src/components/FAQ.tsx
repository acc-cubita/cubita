import { useState } from 'react'
import { ChevronDown } from 'lucide-react'

const FAQS = [
  {
    q: 'آیا داده‌های کسب‌وکار من امن است؟',
    a: 'بله. هر مشتری روی یک نسخه‌ی کاملاً ایزوله (دیتابیس، سرویس و آدرس اختصاصی) اجرا می‌شود؛ داده‌ی هیچ کسب‌وکاری با کسب‌وکار دیگر در یک دیتابیس مشترک نیست. اتصال هم همیشه از طریق HTTPS رمزنگاری‌شده است.',
  },
  {
    q: 'نسخه‌ی دمو دقیقاً چه چیزی نشان می‌دهد؟',
    a: 'دمو نسخه‌ی واقعی نرم‌افزار کوبیتاست، فقط با داده‌ی نمونه و دسترسی فقط‌خواندنی — یعنی همه‌ی صفحات، گزارش‌ها و امکانات را می‌بینید ولی نمی‌توانید چیزی را تغییر دهید یا حذف کنید.',
  },
  {
    q: 'اگر اینترنت قطع شود چه اتفاقی می‌افتد؟',
    a: 'نسخه‌ی دسکتاپ کاملاً آفلاین کار می‌کند: فاکتور، سند حسابداری و بقیه‌ی عملیات محلی ذخیره می‌شوند و با اتصال مجدد به اینترنت، خودکار با سرور مرکزی هم‌گام‌سازی می‌شوند.',
  },
  {
    q: 'چند نفر می‌توانند هم‌زمان از نرم‌افزار استفاده کنند؟',
    a: 'بسته به پلن، از یک تا چند کاربر هم‌زمان — هرکدام با نقش مشخص (مدیر، حسابدار، فروشنده، انباردار، مسئول حقوق) و دسترسی محدود به همان بخش.',
  },
  {
    q: 'بعد از پرداخت، چقدر طول می‌کشد تا نسخه‌ی من آماده شود؟',
    a: 'معمولاً طی چند ساعت کاری. بعد از پرداخت موفق، تیم کوبیتا نسخه‌ی اختصاصی شما را راه‌اندازی و اطلاعات ورود را از طریق ایمیل یا تماس تلفنی ارسال می‌کند.',
  },
  {
    q: 'آیا امکان اتصال به سامانه‌ی مؤدیان وجود دارد؟',
    a: 'بله، در پلن سازمانی. صورتحساب‌های الکترونیکی مطابق با الزامات سازمان امور مالیاتی ارسال می‌شوند.',
  },
]

export function FAQ() {
  const [openIndex, setOpenIndex] = useState<number | null>(0)

  return (
    <section id="faq">
      <div className="container">
        <div className="section-head">
          <span className="eyebrow">سوالات متداول</span>
          <h2>هرچه لازم است بدانید</h2>
        </div>
        <div className="faq-list">
          {FAQS.map((item, idx) => {
            const isOpen = openIndex === idx
            return (
              <div className={`faq-item${isOpen ? ' open' : ''}`} key={item.q}>
                <button type="button" className="faq-question" onClick={() => setOpenIndex(isOpen ? null : idx)}>
                  {item.q}
                  <ChevronDown size={18} className="faq-chevron" />
                </button>
                {isOpen && <p className="faq-answer">{item.a}</p>}
              </div>
            )
          })}
        </div>
      </div>
    </section>
  )
}
