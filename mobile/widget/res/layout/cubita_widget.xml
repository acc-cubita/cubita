<?xml version="1.0" encoding="utf-8"?>
<!--
  چیدمانِ ویجت.

  `layoutDirection="rtl"` صریح است و به تنظیمِ زبانِ گوشی سپرده نشده: اپ خودش
  `I18nManager.forceRTL(true)` می‌کند، ولی ویجت پروسه‌ی اپ نیست — لانچر آن را
  می‌کشد. روی گوشی‌ای که زبانش انگلیسی است، بدونِ این خط ویجت چپ‌چین می‌شد در
  حالی که متنش فارسی است.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_bg"
    android:layoutDirection="rtl"
    android:padding="16dp">

    <TextView
        android:id="@+id/widget_tenant"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/widget_accent"
        android:textSize="13sp"
        android:textStyle="bold"
        android:maxLines="1"
        android:ellipsize="end" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="10dp">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/widget_sales_label"
                android:textColor="@color/widget_muted"
                android:textSize="11sp" />

            <TextView
                android:id="@+id/widget_sales"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/widget_text"
                android:textSize="17sp"
                android:textStyle="bold"
                android:maxLines="1"
                android:ellipsize="end" />
        </LinearLayout>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/widget_profit_label"
                android:textColor="@color/widget_muted"
                android:textSize="11sp" />

            <TextView
                android:id="@+id/widget_profit"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/widget_text"
                android:textSize="17sp"
                android:textStyle="bold"
                android:maxLines="1"
                android:ellipsize="end" />
        </LinearLayout>
    </LinearLayout>

    <!-- **وزن روی همین TextView است و نه یک فاصله‌گیرِ جدا.** RemoteViews یک
         layout معمولی نیست: فقط فهرستِ محدودی از کلاس‌های view را می‌پذیرد و
         `<View>`ِ ساده در آن نیست. یک بار امتحان شد و لانچر «Can't load widget»
         داد — بدونِ استثنا، فقط یک `W AppWidgetHostView: Error inflating
         RemoteViews` در لاگ. پس فضای آزاد را همین سطر می‌بلعد و زمانِ
         به‌روزرسانی ته‌نشین می‌شود. -->
    <TextView
        android:id="@+id/widget_alerts"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="8dp"
        android:gravity="top"
        android:textColor="@color/widget_accent"
        android:textSize="12sp"
        android:maxLines="1"
        android:ellipsize="end" />

    <TextView
        android:id="@+id/widget_stamp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textColor="@color/widget_muted"
        android:textSize="10sp"
        android:maxLines="1"
        android:ellipsize="end" />
</LinearLayout>
