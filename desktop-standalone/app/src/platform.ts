// آیا زیرِ پوسته‌ی دسکتاپِ Electron اجرا می‌شویم؟ نشانه‌اش پلِ کنترل‌های پنجره است که
// preload تزریق می‌کند (نوار عنوان/به‌روزرسانیِ سفارشی). در اجرای وبِ صرف تزریق نمی‌شود.
// (قبلاً به window.cubita — پلِ صف/کش آفلاینِ ابری — گره خورده بود که در نسخه‌ی محلی حذف شد.)
export const isElectron = typeof window !== 'undefined' && !!window.windowControls
