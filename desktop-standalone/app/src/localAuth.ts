// اعتبارِ اکانتِ محلیِ پیش‌فرض که موتور (engine/app/local_bootstrap.py) هنگامِ اولین
// راه‌اندازی seed می‌کند. چون این محصولِ **تک‌کاربرِ محلی** است (نه SaaSِ چندکاربره)،
// اپ به‌جای نمایشِ صفحه‌ی ورود، هنگامِ باز شدن خودکار با همین حساب لاگین می‌کند و
// مستقیم به داشبورد می‌رود. کلِ زنجیره‌ی احراز/توکنِ بک‌اند دست‌نخورده بازاستفاده می‌شود
// (API هنوز principal/tenant می‌خواهد)، فقط درِ ورودیِ کاربرپسند حذف شده.
//
// اگر روزی مقدارِ `LOCAL_OWNER_EMAIL/PASSWORD` در اسپاونِ سایدکار (electron/sidecar.ts)
// یا در بوت‌استرپ عوض شد، این‌جا هم باید همان شود. قفلِ محلیِ واقعی (رمزِ کاربر) کارِ M6
// (لایسنس/فعال‌سازی) است و آن‌وقت جایگزینِ این لاگینِ خودکار می‌شود.
export const LOCAL_OWNER_EMAIL = 'owner@hesabdari.ir'
export const LOCAL_OWNER_PASSWORD = 'owner'
