# کوبیتا در برابر پایگاه دانشِ حسابداری

**تاریخ:** ۱۴۰۵/۰۶/۲۴ · **مرجع:** `kobita_accounting_knowledge_base` (۱۴۷ قسمت، ۱۵ فصل)
**کدِ سنجیده‌شده:** `origin/master` روی `c6d7000`

این فایل نتیجه‌ی مقابله‌ی کدِ کوبیتا با پایگاه دانشِ دوره است. مرجع سه فهرست دارد
که این‌جا مبنا گرفته شده‌اند:

| فهرست | تعداد | چیست |
|---|---|---|
| `core_rules` | ۲۳ | قواعدِ پایدارِ حسابداری — نقضشان یعنی دفتر غلط است |
| `software_design_rules` | ۱۶۳ | اقتضائاتِ همان قواعد برای مدلِ داده |
| `conflicts_and_tensions` | ۲۷ | دوراهی‌هایی که دوره عمداً حلشان نکرده |

---

## روش

**هیچ موردی بدونِ شاهدِ اجراشده ثبت نشده.** هر ادعا با خواندنِ کد سنجیده شد، و سه
ادعا در همین مسیر **رد شدند** (بخشِ ۶).

تفکیکی که در کلِ فایل رعایت شده و مهم است:

| رده | معنا |
|---|---|
| **تناقض** | کد کاری می‌کند که قاعده صریحاً منعش می‌کند |
| **انحرافِ مستند** | کد فرق دارد، **و دلیلش در خودِ کد نوشته شده** |
| **غیبت** | قاعده چیزی می‌خواهد که اصلاً مدل نشده — نه غلط، نبود |

پوشش: حدود **۷۰ قاعده از ۱۶۳** به‌علاوه‌ی ۲۷ تعارض. بقیه سنجیده نشده‌اند (بخشِ ۷).

---

## ۰. وضعیتِ اصلاحات (به‌روزشده ۱۴۰۵/۰۶/۲۴)

**پنج تناقض از شش بسته و روی تولید مستقر شدند** (نسخه‌ی `0157`).

| # | تناقض | وضعیت |
|---|---|---|
| ۱٫۱ | صورت جریان وجوه نقد روی متنِ کدِ حساب | **بسته** — نقشِ `LONG_TERM_LIABILITY` + گروهِ ۲۲ در چارتِ پایه (PR #75) |
| ۱٫۲ | وارداتِ صورت‌حساب بی کنترلِ تکرار | **بسته** — `external_ref` + ایندکسِ یکتای جزئی + شمارشِ ردیف‌های هم‌شکل (PR #75، مهاجرتِ `0150`) |
| ۱٫۳ | تنخواه به دو نوعِ تراکنش فروکاسته | **بسته** — `PettyCashFund`، استردادِ مانده، مدرک (PR #87، مهاجرتِ `0157`) |
| ۱٫۴ | «امانی» بی وضعیتِ مالکیت | **باز** — نیازمندِ حساب‌های انتظامی؛ تصمیمِ محصول |
| ۱٫۵ | کامنتِ تومان/ریال | **بسته** (PR #75) |
| ۱٫۶ | `source_type` سه مفهوم در یک ستون | **بسته، و یافته کوچک‌تر شد** — پایین |

**تصحیحِ §۱٫۶:** این یافته **بزرگ‌تر از واقع** نوشته شده بود. کوبیتا از قبل دلیلِ
حرکت را دارد — `WarehouseReceipt.receipt_type` و `WarehouseIssue.issue_type`، هر دو
با قیدِ `CHECK`. یعنی قاعده‌ی ۷۸ در سطحِ **سند** رعایت شده بود و من فقط در سطحِ
ردیفِ دفتر ندیده بودمش. نقصِ واقعی باریک‌تر بود: گزارشِ «هدف حرکت» برچسب را از
`source_type` می‌ساخت، پس رسیدِ تولیدی و رسیدِ خرید یک ردیف می‌شدند. اصلاحش **بی
هیچ ستونِ تازه‌ای** انجام شد — ستونِ جدید کپیِ دومِ همان داده می‌شد.

**غیبت‌های ساختاری (بخشِ ۳) هنوز باز‌اند.** بزرگ‌ترینشان تراکنشِ مالک و شریک است
که درِ «صورت تغییرات در حقوق صاحبان سهام» را باز می‌کند.

---

## ۱. تناقض‌ها

### ۱٫۱ طبقه‌بندیِ صورت جریان وجوه نقد روی **متنِ کدِ حساب**

**قواعدِ نقض‌شده:** ۲۰، ۱۶۰ · **شاهد:** [`services/reports.py:878`](backend/app/services/reports.py#L878)

```python
if account.type == "equity" or (account.type == "liability" and code.startswith("22")):
    return "financing"
```

قاعده‌ی ۲۰ صریح است: منطقِ کسب‌وکار باید به `AccountId`/`AccountRole` بچسبد نه متنِ
کد، چون یک کدِ یکسان می‌تواند در Templateهای مختلف عنوان و Role متفاوت داشته باشد.
قاعده‌ی ۱۶۰ همین را برای جاری/غیرجاریِ بدهی تکرار می‌کند.

سمتِ دارایی ([خط ۸۷۶](backend/app/services/reports.py#L876)) **اول `system_role` را
می‌بیند** و کد فقط پشتیبان است. سمتِ بدهی هیچ بررسیِ نقشی ندارد.

**پیامد:** مشتری‌ای که چارتش را بازشماره‌گذاری کند — همان کاری که
[`chart_codes.py`](backend/app/services/chart_codes.py) در docstringِ خودش می‌گوید
حسابداران می‌کنند — وامِ بلندمدتش از «تأمین مالی» به «عملیاتی» می‌افتد و صورت جریان
وجوه نقد **بی‌صدا** غلط می‌شود.

**نکته‌ی تلخ:** همین پروژه در جای دیگر دقیقاً همین تله را تشخیص داده و دوری کرده.

---

### ۱٫۲ وارداتِ صورت‌حسابِ بانکی هیچ کنترلِ تکرار ندارد

**قواعدِ نقض‌شده:** ۵۰، ۵۴ · **شاهد:** [`services/banking.py:125`](backend/app/services/banking.py#L125) و [`models/banking.py:246`](backend/app/models/banking.py#L246)

```python
statement_lines = [
    BankStatementLine(bank_account_id=..., line_date=..., amount=..., description=...)
    for line in lines
]
db.add_all(statement_lines)          # ← هیچ بررسیِ تکراری
```

قاعده‌ی ۵۴: مغایرت‌گیریِ بانکی باید با **duplicate-reference/idempotency** و
کنترل‌های مبلغ/تاریخ/حساب تطبیق دهد.
قاعده‌ی ۵۰: `BankMovement/BankEvidence` باید **ExternalReference/BankReference** را
مستقل از `VoucherId` نگه دارد.

`BankStatementLine` **ستونِ مرجعِ بانکی ندارد** (فقط تاریخ، مبلغ، شرح) و جدول هیچ
قیدِ یکتایی ندارد.

**پیامد:** وارد کردنِ دوباره‌ی یک فایلِ صورت‌حساب، کلِ ردیف‌ها را **دوبرابر** می‌کند.
و چون تطبیقِ خودکار با «مبلغِ برابر و تاریخِ ±۳ روز» کار می‌کند
([خط ۱۴۱](backend/app/services/banking.py#L141))، نسخه‌های تکراری به تراکنش‌های
دیگری می‌چسبند و مغایرت‌گیری را وارونه می‌کنند.

> این حوزه طبقِ [`GAPS.md`](GAPS.md) **صفر تستِ مستقیم** دارد.

---

### ۱٫۳ تنخواه به دو نوعِ تراکنش فروکاسته شده

**قواعدِ نقض‌شده:** ۶۳، ۶۴، ۶۵، ۶۷ · **شاهد:** [`models/banking.py:262`](backend/app/models/banking.py#L262)

```python
PETTY_CASH_TYPES = ("charge", "expense")
```

| قاعده می‌خواهد | کوبیتا دارد |
|---|---|
| `PettyCashFund` مستقل از تنخواه‌دار، با Custodian/Location/Limit/Status (۶۳) | **هیچ** — docstring می‌گوید «یک صندوق تنخواه واحد» |
| شش رویدادِ جدا: Funding، Spend، EvidenceSubmission، ExpensePosting، Replenishment، **ReturnOfBalance** (۶۴) | دو نوع: `charge`، `expense` |
| هر هزینه به `SupportingEvidence` قابلِ ردیابی (۶۵) | فقط `description` متنی |
| ماندهٔ موردِانتظار مشتق و reconcile‌شونده؛ اختلاف **بدونِ علت و تأیید** خودکار هزینه نشود (۶۷) | تطبیقی وجود ندارد |

**پیامدِ عملی:** «استردادِ ماندهٔ تنخواه» هیچ مسیری ندارد، و تعویضِ تنخواه‌دار هویتِ
صندوق را بازنویسی می‌کند چون صندوقی به‌عنوان موجودیت وجود ندارد.

---

### ۱٫۴ «امانی» دعوتی است که مدل نمی‌تواند وفا کند

**قواعدِ نقض‌شده:** ۱۰۴، ۱۰۸، ۱۳۴ · **تعارضِ مرتبط:** C-21
**شاهد:** [`models/sales_ops.py:67`](backend/app/models/sales_ops.py#L67)

docstringِ `SaleType` می‌گوید: «نوعِ فروش: نقدی، اعتباری، **امانی**، صادراتی…».
ولی `SaleType` فقط `due_days`، `default_tax_rate` و نگاشتِ حساب‌ها را نگه می‌دارد —
**نه `OwnershipState`، نه قاعده‌ی شناسایی.**

قاعده‌ی ۱۰۴: ارسالِ امانی `RevenueRecognition` ایجاد نمی‌کند.

کاربری که نوعِ فروشِ «امانی» بسازد، درآمد را در لحظه‌ی فاکتور شناسایی می‌کند —
مثلِ فروشِ عادی.

**و راهِ درستش هم بسته است:** کوبیتا **حساب‌های انتظامی ندارد**. خودِ کد این را
می‌داند — [`models/accounting.py:144`](backend/app/models/accounting.py#L144):
«اگر روزی حساب‌های *انتظامی* اضافه شوند…». تعارضِ C-21 دقیقاً می‌گوید کنترلِ کالای
امانی جای حساب‌های انتظامی است.

**درجه:** خفیف‌تر از بقیه — «امانی» seed نمی‌شود و فقط مثالِ docstring است. ولی مدل
چیزی را نام می‌برد که نمی‌تواند انجام دهد.

---

### ۱٫۵ کامنتِ مدل «تومان» می‌گوید و کد ریال حساب می‌کند

**قاعده‌ی نقض‌شده:** ۱۱۲ · **تعارضِ مرتبط:** C-22
**شاهد:** [`models/crm.py:148`](backend/app/models/crm.py#L148)

```python
#: چند تومان خرید = ۱ امتیاز (مثلاً ۱۰۰۰۰ یعنی هر ۱۰هزار تومان یک امتیاز)
amount_per_point: Mapped[float] = ...
```

ولی [`services/crm.py:119`](backend/app/services/crm.py#L119) مبلغِ **ریالیِ** فاکتور
را تقسیم می‌کند، و برچسبِ رابط هم
([`CrmPage.tsx:644`](desktop/src/pages/CrmPage.tsx#L644)) می‌گوید **«به‌ازای هر چند
ریال خرید»**.

**پس باگِ پولی نیست — کد و رابط هم‌داستان‌اند.** تله در کامنت است: اولین توسعه‌دهنده‌ای
که به آن اعتماد کند و ضریب را «اصلاح» کند، امتیازِ همه‌ی مشتری‌ها را ۱۰ برابر می‌کند.
امتیاز یک **بدهی** است.

هم‌زمان `billing.py` واقعاً تومانی است (`price_toman`، `amount_toman`) در کنارِ
دفترِ ریالی — دقیقاً وضعیتی که قاعده‌ی ۱۱۲ می‌گوید باید `MonetaryUnit` ساختاریافته
داشته باشد، نه برچسبِ متنی.

---

### ۱٫۶ `source_type` سه مفهوم را در یک ستون جمع کرده

**قواعدِ نقض‌شده:** ۷۷، ۷۸ · **شاهد:** [`models/inventory.py:606`](backend/app/models/inventory.py#L606)

```python
source_type: Mapped[str] = mapped_column(String(50))  # sales_invoice | purchase_invoice | adjustment
```

قاعده‌ی ۷۸: `WarehouseDocumentType` باید از `InventoryMovementReason` و
`AccountingVoucher` **مستقل** باشد.

مقادیرِ واقعیِ درحالِ‌استفاده سه جنسِ متفاوت‌اند:

| جنس | نمونه‌ها |
|---|---|
| نوعِ سند | `warehouse_receipt`، `warehouse_issue`، `sales_invoice` |
| دلیلِ حرکت | `adjustment`، `stock_count`، `production` |
| مکانیزمِ اصلاح | `void` |

و هیچ قیدِ `CHECK`ی روی ستون نیست — رشته‌ی آزادِ ۵۰ کاراکتری.

**پیامد:** گزارشِ «چرا موجودی تغییر کرد» از روی نوعِ سند حدس زده می‌شود، و
دلیل‌هایی که قاعده‌ی ۷۷ صریح می‌خواهد (`Shortage`، `ConsignmentIssue`،
`SalesReturn`، `Other`) قابلِ تفکیک نیستند.

---

## ۲. انحرافِ مستند — کد فرق دارد و دلیلش نوشته شده

این‌ها **تناقض نیستند**. جایشان این‌جاست تا اگر کسی روزی خواست «اصلاحشان» کند، اول
دلیلِ ثبت‌شده را ببیند.

### ۲٫۱ گِردکردنِ میانگین در هر گام

**قاعده:** ۹۹، ۱۱۵ (حالت‌های میانیِ گِردشده نباید به‌عنوان نهایی ذخیره شوند)
**کد:** [`valuation.py:95`](backend/app/services/valuation.py#L95) — `_AVERAGE_PLACES = Decimal("0.0001")`

کوبیتا میانگینِ متحرک را در هر حرکت به چهار رقم گِرد می‌کند و **همان را ذخیره
می‌کند**. دلیلش در خودِ کد آمده: بازپخشِ ارزش‌گذاری باید با ثبتِ لحظه‌ای یکی دربیاید،
و ثبتِ لحظه‌ای از عددِ ذخیره‌شده‌ی چهاررقمی ادامه می‌دهد؛ بی‌گِردکردن، هر حرکت
«منقضی» دیده می‌شد.

دقتِ داخلی (۴ رقم) از دقتِ نمایشی (ریالِ صحیح) بیشتر است، پس **نیمه‌ی اولِ قاعده‌ی ۹۹
رعایت شده**.

### ۲٫۲ «موقت/دائم» به‌عنوان وضعیتِ سند

**قاعده:** ۲۹ — واژگانِ یک نرم‌افزار (مثل Temporary/Permanent) نباید به‌عنوان
وضعیتِ جهانیِ حسابداری hard-code شود
**کد:** [`models/accounting.py:201`](backend/app/models/accounting.py#L201)

کوبیتا دقیقاً `temporary`/`permanent` را hard-code کرده. **ولی این محلی‌سازیِ موجه
است**: «سند موقت/دائم» اصطلاحِ قانونیِ حسابداری در ایران است، نه واژه‌ی یک محصول.
قاعده هشدار می‌دهد واژه‌ی یک نرم‌افزار جهانی فرض نشود؛ این‌جا واژه از مقررات آمده،
نه از رقیب.

### ۲٫۳ سربارِ تولید یک عددِ دستی است

**تعارضِ مرتبط:** C-13 · **کد:** [`models/manufacturing.py:71`](backend/app/models/manufacturing.py#L71)

`ProductionOrder.overhead_cost` یک مبلغِ دستی است؛ نه نرخِ جذب، نه تفکیکِ موادِ
مستقیم و غیرمستقیم. **خودِ KB هم این را باز گذاشته** — C-13 می‌گوید دوره mechanics
جذبِ سربار را آموزش نداده. پس کوبیتا از چیزی عقب نیفتاده که تعریف شده باشد.

---

## ۳. غیبت‌های ساختاری

قاعده چیزی می‌خواهد که اصلاً مدل نشده. ترتیب بر اساسِ اثر.

### ۳٫۱ تراکنشِ مالک و شریک — **بزرگ‌ترین**

**قواعد:** پایدارِ ۲، طراحیِ ۵۲، ۱۶۲ · **تعارض:** C-04

[`chart_codes.py`](backend/app/services/chart_codes.py) **۳۹ نقشِ معنایی** دارد.
هیچ‌کدام آورده‌ی مالک، برداشتِ مالک، یا حسابِ جاری شرکا نیست. `RETAINED_EARNINGS`
هست؛ سرمایه نیست.

قاعده‌ی ۵۲ صریح است: `TransactionType` باید یکی از `LoanToEntity`، `LoanFromEntity`،
`CapitalContribution`، `CapitalReduction/Withdrawal` یا `Repayment` باشد، و جهتِ پول
یا نقشِ طرفِ حساب به‌تنهایی نوعِ حسابداری را تعیین نکند.

تعارضِ **C-04** دقیقاً همین را باز گذاشته: برداشتِ مالک، کاهشِ سرمایه است یا مطالبه
از مالک؟ **کوبیتا هیچ‌کدام را انتخاب نکرده — با نداشتنِ هر دو.**

سه پیامد:

1. قاعده‌ی پایدارِ ۲ (آورده ≠ درآمد، برداشت ≠ هزینه) هیچ گاردِ نرم‌افزاری ندارد؛
   کاربر دستی ثبت می‌کند و چیزی جلوی نشاندنِ برداشت در حسابِ هزینه را نمی‌گیرد.
2. **«صورت تغییرات در حقوق صاحبان سهام» غیرممکن است** — یکی از چهار صورتِ الزامیِ
   استانداردهای حسابداری ایران، و همان چیزی که [`GAPS.md`](GAPS.md) بخشِ ۷ ثبت کرده.
   حالا **دلیلِ ساختاری‌اش** روشن است: داده‌ی ورودی‌اش وجود ندارد.
3. صورت جریان وجوه نقد «تأمین مالی» را از `account.type == "equity"` حدس می‌زند
   (همان تناقضِ ۱٫۱).

### ۳٫۲ ذخیره‌ی مطالبات مشکوک‌الوصول

**قواعد:** ۱۵۸، ۱۵۹ · **تعارض:** C-27

**صفر ارجاع** در کلِ بک‌اند. قاعده‌ی ۱۵۸ می‌خواهد `Method`، `EstimateDate`،
`RateOrAgingBuckets`، `RequiredEndingAllowance`، `ExistingAllowance` و
`CurrentAdjustment` جدا و versioned باشند و **`GrossReceivable` حذف نشود**؛
قاعده‌ی ۱۵۹ می‌خواهد سوخت و بازیافت به طلبِ اصلی لینک شوند.

هم‌خانواده‌ی غیبتِ برآورد (قاعده‌ی ۱۵ — `Estimate` با `Range/Basis/Assumptions`)،
که آن هم مدل نشده.

### ۳٫۳ سیاستِ ارزش‌گذاریِ انبار

**قواعد:** ۸۴، ۹۰، ۹۵، ۱۰۱

کوبیتا **فقط میانگینِ متحرکِ دائمی** دارد و آن هم سیم‌کشی‌شده است، نه داده:

| قاعده می‌خواهد | وضعیت |
|---|---|
| `InventoryValuationPolicy` داده‌ای، Entity/ItemCategory/Warehouse-scoped و effective-dated (۸۴) | فیلدی وجود ندارد |
| FIFO/LIFO/WeightedAverage/SpecificIdentification به‌عنوان policy (۸۴) | فقط میانگین |
| `InventorySystemPolicy` ادواری/دائمی (۹۰) | فقط دائمی |
| مهاجرتِ ادواری↔دائمی با cutover و remap (۹۵) | موضوعیت ندارد |

**این لزوماً عیب نیست** — یک روش که درست پیاده شده بهتر از چهار روشِ نیم‌بند است.
ولی اگر روزی روشِ دوم اضافه شود، امروز جایی برای نشاندنش نیست.

### ۳٫۴ ابعادی که از کدِ حساب استنتاج می‌شوند

**قواعد:** ۱۵۷، ۱۶۰ · **تعارض:** C-26

سررسید، وصول‌پذیری، نوعِ ابزار و تجاری/غیرتجاری **هیچ ستونی روی حساب ندارند**.
جاری/غیرجاری فقط از پیشوندِ کد حدس زده می‌شود — همان تناقضِ ۱٫۱.

### ۳٫۵ بقیه‌ی غیبت‌ها

| قاعده | چه چیزی نیست |
|---|---|
| ۱۳ | `GoingConcernAssessment` — period-scoped و جدا از `Company.IsActive` |
| ۱۴، ۱۶۳ | `DisclosureItem` — یادداشتِ افشا، versioned و source-linked. قاعده‌ی پایدارِ ۱۷ می‌گوید افشا می‌تواند بدونِ سند وجود داشته باشد |
| ۱۵ | `Estimate` با `Range/Basis/Assumptions`، جدا از `Actual` |
| ۱۲ | `TaxPeriod` جدا از `FiscalYear` — گزارشِ فصلی بُعدِ خودش را ندارد |
| ۱۹ | انتصابِ سازمانیِ effective-dated |
| ۲۱ | `Contract`/`Project` — همان صفحه‌ی ماکتِ «پیمانکاری» |
| ۴۶، ۸۲، ۱۲۳ | پرچمِ فعالیتِ مشکوک در خزانه/انبار/فاکتور، با review و escalation |
| ۵۱، ۵۶ | انتقالِ داخلیِ خزانه (نقد↔بانک) به‌عنوان موجودیت، با وجوهِ بین‌راهی |
| ۸۷ | اعتبارسنجیِ `QtyAvailable = QtyIssued + QtyEnding` |
| ۱۱۹، ۱۲۱ | `SalesCase`/correlation id برای بستنِ فاکتور↔دریافت↔خروجِ انبار |
| ۱۶۲ | `FinancialStatementPackage` — تطبیقِ متقابلِ صورت‌ها (سود→سرمایه ✔ هست؛ ماندهٔ نقد→جریان وجوه ✗) |

---

## ۴. هم‌راستایی‌های چشمگیر

گزارشِ یک‌طرفه گمراه‌کننده است. این‌ها جاهایی‌اند که کوبیتا **دقیقاً** همان کاری را
کرده که KB می‌خواهد — و گاهی با همان استدلال.

| قاعده | شاهد |
|---|---|
| **۲۰، ۲۲، ۶۸** — منطق به نقش بچسبد نه کدِ حساب | [`chart_codes.py`](backend/app/services/chart_codes.py): «کد حساب متعلق به مشتری است، نه به ما … لحظه‌ای که مشتری صندوق را از ۱۱۰۱ به چیز دیگری می‌برد، هر ثبتِ خودکاری می‌شکست». **همان دلیلِ C-12** |
| **۵، ۷۱** — هر سند به رویدادِ منشأ لینک | `JournalEntry.source_type` + `source_id` |
| **۳۰** — قابلیت‌های حساب مستقل و با وابستگیِ اعتبارسنجی‌شده | `is_fx`، `fx_revaluable`، `accepts_tafsili` + قیدِ `NOT fx_revaluable OR is_fx` — **مصداقِ دقیقِ مثالِ خودِ قاعده** |
| **۲۹ (نیمه‌ی دوم)، C-16** | `STATEMENT_TYPE_LABELS` جدا از `nature`، و `nature_control` جدا از هر دو |
| **۷۶** — ابعاد در سطحِ **ردیف** نه سند | `cost_center_id` و `analytic_id` روی `JournalLine` |
| **۳۱** — حسابِ ارجاع‌شده hard-delete نشود | گاردِ `SAVEPOINT` + `IntegrityError` ⇒ ۴۰۹ |
| **۱۰۹، C-23** — برگشت از فروش بهای **خروجِ اصلی** را برگرداند | `issued_unit_cost_by_line` — صریحاً، با کامنتِ چرایی. قاعده می‌گوید میانگینِ متحرکِ جاری نباید مبنای پیش‌فرضِ برگشت باشد |
| **۱۵۲** — تفکیکِ اجزای حقوق | شش ردیفِ مستقل: ناخالص، خالصِ پرداختنی، بیمه/مالیات، وام، کسورات، رند |
| **۱۲۵** — پیش‌فاکتور درآمد نسازد | هیچ سندی نمی‌زند |
| **۱۰۵** — انبارگردانی: مقدارِ دفتری، شمارشِ فیزیکی، اختلاف، علت | `system_qty`/`counted_qty`/`unit_cost`/`counted_at`؛ و `counted_qty` **تهی‌پذیر** تا «نشمرده» با «صفر شمرده» یکی نشود — شمارشِ کور |
| **۱۴۱، ۱۴۴** — تخفیف با حسابِ جدا، نه ثبتِ دوگانه | نقش‌های `SALES_DISCOUNT` و `SALES_RETURN` جدا از `SALES_REVENUE` |
| **۵۸، ۵۹، ۶۰، ۶۲** — چرخه‌ی چک با گذارهای صریح | `CHECK_STATUSES` + `check_event.py` با `endorse`/`return_endorsed` |
| **پایدارِ ۱۰، ۱۷** — اصلاح نباید تاریخچه را بی‌ردپا بازنویسی کند | `VoidableMixin` + سندِ معکوس؛ docstringش همین را می‌گوید |

**تنها موردِ ناقصِ ۱۰۵:** تأییدکننده‌ی جدا وجود ندارد — همان کسی که جلسه را می‌سازد
ثبتش می‌کند.

---

## ۵. وضعیتِ ۲۷ تعارضِ KB در کوبیتا

| تعارض | موضوع | کوبیتا |
|---|---|---|
| **C-04** | برداشتِ مالک: کاهشِ سرمایه یا مطالبه؟ | **هیچ‌کدام** — مدل نشده (۳٫۱) |
| **C-21** | کالای امانی: مالکیت در برابر شناسایی | **حساب‌های انتظامی ندارد** (۱٫۴) |
| **C-22** | برچسبِ تومان/ریال | کد ریالی و سازگار؛ **کامنت غلط** (۱٫۵) |
| **C-26** | سپرده‌ی بلندمدت زیرِ مطالبات | بُعدِ سررسید ندارد (۳٫۴) |
| **C-27** | بازیافتِ طلبِ سوخت‌شده | مطالباتِ مشکوک‌الوصول ندارد (۳٫۲) |
| **C-25** | تجدیدِ ارزیابیِ دارایی | ندارد؛ فقط خطِ مستقیم |
| **C-20** | روشِ ادواری | ندارد (۳٫۳) |
| **C-13** | جذبِ سربار | عددِ دستی — **و KB هم بازش گذاشته** (۲٫۳) |
| **C-14** | شناساییِ درآمدِ پیمان | پیمانکاری مدل نشده |
| **C-07** | اصلاحِ ریشه‌ای در برابر تعدیل | **بسته** (PR #89، مهاجرتِ `0158`) — تعدیل ابطال‌پذیر شد؛ حرکتش هم بالاخره `source_id` گرفت |
| **C-19** | نگاشتِ هزینه: وسیله نقلیه ↔ ماشین‌آلات | قاعده‌ی ۷۲ اعتبارسنجیِ `EconomicPurpose` می‌خواهد؛ وجود ندارد |
| **C-09، C-12، C-15** | کد/عنوانِ حساب شناسه‌ی جهانی نیست | **حل‌شده** — `system_role` |
| **C-16** | «ماهیت» دو معنا دارد | **حل‌شده** — سه فیلدِ جدا |
| **C-17، C-23** | سمتِ بهای تمام‌شده‌ی فروش و برگشت | **حل‌شده** — COGS ثبت می‌شود و برگشت با بهای اصلی |
| بقیه (C-01…C-03، C-05، C-06، C-08، C-10، C-11، C-18، C-24) | تنش‌های درونیِ خودِ دوره | اقتضای مستقیمی برای کد ندارند |

---

## ۶. سه ادعایی که در همین بررسی **رد** شدند

ثبتشان لازم است تا دوباره شمرده نشوند.

| ادعا | چرا غلط بود |
|---|---|
| «`FixedAsset.method` انتخابی است که هر سه گزینه یک عدد می‌دهند» ([`GAPS.md`](GAPS.md) §۳٫۱) | [`DEPRECIATION_METHODS = ("straight_line",)`](backend/app/models/assets.py#L14) — **یک** روش، با قیدِ دیتابیس، و رابط انتخابگر ندارد. محاسبه دقیقاً همان است که ادعا می‌کند |
| «اصلاحِ نامِ کارمند به فیش و فایلِ بانک نمی‌رسد» ([`GAPS.md`](GAPS.md) §۴) | [`employee_identities`](backend/app/services/payroll.py#L1145) عمداً طرف‌حساب را ترجیح می‌دهد و `test_employee_identity.py` تضمینش می‌کند. واگرایی فقط در چهار نقطه‌ی نمایشی است |
| «امتیازِ باشگاه تومانی حساب می‌شود و ۱۰ برابر می‌دهد» | رابط می‌گوید «ریال» و کد ریال تقسیم می‌کند. فقط کامنتِ مدل غلط است (۱٫۵) |

**هر دو موردِ `GAPS.md` باید همان‌جا تصحیح شوند.**

---

## ۷. آنچه سنجیده نشد

- حدود **۹۳ قاعده از ۱۶۳** — عمدتاً خزانه (۳۹–۴۹)، انبارِ پیشرفته (۹۱–۱۰۳) و
  فروش (۱۱۶–۱۴۸) — هنوز مقابلِ کد گذاشته نشده‌اند.
- بررسی **ایستا** بود: کد خوانده شد، نه رفتارِ زنده. مواردِ ۱٫۲ (تکرارِ صورت‌حساب)
  و ۱٫۶ ارزشِ اثباتِ تجربی با تست دارند.
- `core_rules` شماره‌های ۱، ۳، ۹، ۱۱، ۱۸، ۱۹ و ۲۱ مستقیم سنجیده نشدند.

---

## ۸. اگر بخواهیم شروع کنیم

به ترتیبِ «اثر تقسیم بر هزینه»:

| # | کار | چرا |
|---|---|---|
| ۱ | کنترلِ تکرار در وارداتِ صورت‌حسابِ بانکی | کوچک، و امروز دوبار وارد کردن دفتر را خراب می‌کند |
| ۲ | نقشِ حساب برای سمتِ بدهیِ صورت جریان وجوه نقد | چند خط؛ گزارش را از بازشماره‌گذاریِ چارت ایمن می‌کند |
| ۳ | تصحیحِ کامنتِ `amount_per_point` | یک خط، و تله‌ای که به پول می‌رسد |
| ۴ | نقش‌های سرمایه، برداشت و جاری شرکا | درِ ورودِ «صورت تغییرات در حقوق صاحبان سهام» |
| ۵ | `CHECK` و تفکیکِ دلیلِ حرکت از نوعِ سند | قبل از اینکه داده‌ی بیشتری روی ستونِ آزاد بنشیند |
| ۶ | `PettyCashFund` + رویدادهای جدا | تنخواه امروز استرداد ندارد |

موردِ ۴ بزرگ‌ترین است و مستقیماً به «صورت تغییرات در حقوق صاحبان سهام» وصل می‌شود —
همان موردِ چهارمِ فهرستِ کارهای جاری.
