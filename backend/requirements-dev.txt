-r requirements.txt

# تست‌ها روی PostgreSQL واقعی اجرا می‌شوند (نه SQLite) — جزئیات در tests/conftest.py
pytest==9.1.1

# سقفِ زمانِ هر تست. بدونِ این، تستی که روی CI گیر کند کلِ جاب را تا سقفِ زمان
# ساکت می‌بَرد و لاگ نمی‌گوید کجا بود؛ با این، همان تست با traceback لو می‌رود.
pytest-timeout==2.4.0
