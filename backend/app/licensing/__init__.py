"""مجوزِ «کوبیتا سازمانی» (ENTERPRISE_PLAN.md، M2).

- `token`: قالبِ `CUB1.<payload>.<sig>` با امضای Ed25519.
- `keys`: کلیدهای عمومیِ مورد اعتماد — ثابتِ داخلِ کد.
- `fingerprint`: اثرانگشتِ سه‌جزئیِ ویندوز، تطبیقِ ۲ از ۳.
- `state`: وضعیتِ مجوزِ همین نصب (آزمایشی/فعال/منقضی/…) و نصبِ توکن.
- `cli`: ساختِ کلید و صدورِ مجوز — فقط روی ابر، با کلیدِ خصوصی.

**این DRM نیست، بازدارندگی است.** هدف این است که مشتریِ صادق نتواند بی‌صدا روی پنج
شعبه نصب کند، و تخطی یک کارِ عمدی باشد. خرجِ پنهان‌سازیِ کد نمی‌کنیم.
"""
