"""راه‌اندازی و نگهداریِ سرورِ «کوبیتا سازمانی» روی ویندوزِ خودِ شرکت (ENTERPRISE_PLAN.md، M4).

- `provision`: PostgreSQLِ همراه (initdb، نقش‌ها، دیتابیس)، فایلِ `.env` با رازهای تصادفی،
  مهاجرت‌ها، و سنجشِ زنده‌بودنِ RLS. بی‌خطر برای اجرای دوباره.
- `services`: سرویس‌های ویندوز با WinSW و قاعده‌ی فایروال.

کل این بسته از داخلِ `cubita-server.exe` (PyInstaller) صدا زده می‌شود و نصابِ NSIS فقط
همان exe را با آرگومان اجرا می‌کند — منطقِ نصب در پایتونِ تست‌پذیر است، نه در اسکریپتِ NSIS.
"""
