# دستورالعملِ ایجنت — کوبیتا

این فایل برای **ایجنتِ تازه‌ای** است که روی کوبیتا کار می‌کند. هدفش این است که
همان رویه‌ای را اجرا کند که تا امروز اجرا شده، نه اینکه سبکِ خودش را بیاورد.

> `CLAUDE.md` می‌گوید پروژه چیست. این فایل می‌گوید **چطور کار کن**.

---

## ۰. قانونِ صفر

**اساسِ نرم‌افزار حفظ می‌شود.** سپیدار مرجع است، نه الگویی که مو‌به‌مو کپی شود.
هر جا فصلِ سپیدار با قاعده‌ی موجودِ کوبیتا تصادم کرد، **قاعده‌ی کوبیتا می‌ماند** و
دلیلش در `OPEN_DECISIONS.md` ثبت می‌شود.

---

## ۱. قیدهای سخت — این‌ها را نقض نکن

1. **هرگز `deploy.sh demo` را اجرا نکن.** گاردش وبِ demo را بازنویسی می‌کند و درِ
   تریال را می‌شکند. تازه‌سازیِ demo فقط با
   `rsync -a --delete /opt/hesabdari/web/ /opt/cubita-demo/web/`.
2. **هرگز مستقیم روی `master` کامیت یا push نکن.** همیشه `feat/...` یا `fix/...`.
3. **اگر قرار است مهاجرتِ Alembic بسازی، اول به کاربر بگو و تأیید بگیر.**
4. **قبل از هر تغییری، بگو چه می‌خواهی بکنی.** برنامه بنویس، تأیید بگیر، بعد بزن.
5. **رازها هرگز کامیت نمی‌شوند.** کلیدِ خصوصی هیچ‌وقت داخلِ مخزن نمی‌رود.
6. **پیش از هر استقرار به توسعه‌دهنده‌ی دیگر (آرش) اطلاع بده.** دو استقرارِ همزمان
   یعنی یکی سرویس را وسطِ مهاجرتِ دیگری ری‌استارت می‌کند.
7. **دیتابیسِ محلیِ `hesabdari` دادهٔ واقعیِ کاربر است.** هرگز به‌عنوان محیطِ
   آزمایشی استفاده‌اش نکن. برای آزمایش، اسکیمای یک‌بارمصرف بساز (بخش ۶).
8. **هرگز دو اجرای `pytest` را همزمان نزن.** یک دیتابیسِ تست مشترک است؛ شکست‌ها
   واقعی به‌نظر می‌رسند ولی نیستند.
9. **`gh pr merge` و `ssh … deploy.sh production` را محیط مسدود می‌کند.** دورشان
   نزن؛ به کاربر بگو در رابط انجامش دهد.

---

## ۲. چیزهایی که خطا نمی‌دهند، فقط نتیجه‌ی غلط می‌دهند

اینها گرانترین اشتباه‌های تاریخِ پروژه‌اند. هر کدام یک‌بار واقعاً اتفاق افتاده.

| تله | قاعده |
|---|---|
| **RLS** | هر جدولِ مستأجرمحور `FORCE ROW LEVEL SECURITY` دارد — که **حتی مالکِ جدول** را هم مشمول می‌کند. در مهاجرت هیچ زمینه‌ی مستأجری نیست، پس هر `SELECT/UPDATE` بی‌صدا صفر ردیف می‌بیند. در مهاجرتِ **داده** حتماً `rls_disabled(conn, tables)` از `app/migration_utils.py`. بهتر از آن: **در مهاجرت اصلاً ردیف ننویس.** |
| **جدولِ تازه** | بعد از `create_table` باید `ENABLE` + `FORCE ROW LEVEL SECURITY` + `CREATE POLICY` بزنی، وگرنه جدول بینِ مستأجرها نشت می‌کند. بعدش با `pg_class.relforcerowsecurity` **واقعاً بسنجش**. |
| **شماره‌ی سند** | `next_document_number(db, doc_type)` از `app/services/numbering.py`. **SEQUENCE ممنوع** — روی rollback شماره می‌سوزاند و شکاف می‌افتد. |
| **صفحه‌بندی** | `MAX_LIMIT = 200`. `limit` بزرگ‌تر → ۴۲۲ و صفحه در UI بی‌صدا خالی می‌شود. |
| **انتخابِ حساب** | `get_account(db, cc.ROLE)` — هرگز با کد یا نام. کدِ حساب مالِ مشتری است و عوض می‌شود. |
| **قاعده‌ی نظیر** | هر منوی عملیاتی که رکورد می‌سازد باید در `OPS_LIST_MAP` یک ردیف داشته باشد (کلیدِ فهرست، یا `'state'`/`'view'`/`'none'`). ممیزِ R11 نبودش را خطا می‌دهد. |
| **ناوبری** | افزودنِ صفحه یعنی ویرایشِ **هر چهار** جا: `navModel.tsx` (PageKey + NAV_GROUPS/LIST_MENUS + PAGE_MODULE_KEY)، `moduleLists.tsx`، `Dashboard.tsx`، `HelpPage.tsx`. |

---

## ۳. قواعدِ طراحی که پروژه قبلاً تصمیم گرفته

**دوباره تصمیم نگیر — از اینها استفاده کن.**

- **«گزارش، نه گارد.»** خطای احتمالی را نشان بده، مسدود نکن — مگر آنکه واقعاً
  یکپارچگیِ داده باشد.
- **«هرگز دو نمای یک داده نساز.»** اگر جدول/صفحه‌ای برای این داده هست، همان را
  گسترش بده.
- **مشتق بهتر از ذخیره‌شده** — مگر وقتی مقدار در یک لحظه قفل شده (نرخِ ارز روی
  فاکتور، مبلغِ چک در لحظه‌ی دریافت).
- **پرچمِ تازه پیش‌فرضش «هیچ‌چیز عوض نمی‌شود» است.** `NULL` = پیش‌فرضِ سرویس =
  رفتارِ امروز.
- **تاریخ بازنویسی نمی‌شود.** حذف نکن؛ `voided_at` + سندِ معکوس
  (`voiding.reverse_journal_entry`).
- **آنچه موجودیت می‌داند را دوباره از کاربر نپرس.** (چکِ صادرشده از دسته‌چک
  حسابِ بانکی‌اش را می‌داند — هنگام وصول دوباره نپرس.)
- **مرزِ سیاست و یکپارچگی را تیز نگه دار.** «آیا اجازه هست؟» سیاست است و پرچم
  می‌گیرد؛ «آیا این عدد دروغ می‌شود؟» یکپارچگی است و همیشه سنجیده می‌شود.

### الگوهایی که سه فصلِ اخیر تکرار شده‌اند

```python
# ۱) ایندکسِ یکتای جزئی — تا ردیف‌های موجودِ خالی قید را نشکنند
Index("uq_x", "tenant_id", "col", unique=True,
      postgresql_where=text("col <> ''"))

# ۲) پرچمِ سیاست روی مستأجر
mode: Mapped[str | None] = mapped_column(String(20), nullable=True)  # NULL = پیش‌فرض
# سرویس: MODES/DEFAULT_MODE/LABELS/HINTS/EFFECTS + get_mode/set_mode
# روتر: {mode, options[{key,label,hint,effects,is_default}], is_explicit}
# رابط: کارت در PersonalizationPage.tsx

# ۳) شِمای ویرایشِ تماماً اختیاری
class XUpdateIn(BaseModel):
    name: str | None = None      # همه اختیاری
# سرویس: data.model_dump(exclude_unset=True)
# بدون این، ویرایشِ جزئی فیلدهای نفرستاده را بی‌صدا صفر/فعال می‌کند
```

---

## ۴. رویه‌ی یک «فصل»

کاربر یک تحلیلِ بلندِ فارسی از سپیدار می‌فرستد. مسیر این است:

### گام ۱ — تحلیلِ شکاف، **قبل از هر کد**
فصل تقریباً همیشه در بندِ آخرش می‌گوید «اول بررسی کن چه هست؛ زیرساختِ موازی
نساز». جدی بگیرش. سه دسته بساز:

1. **از قبل درست است** → دست نزن، ولی در PR بنویس که چه چیزهایی بررسی و رد شدند.
2. **شکافِ واقعی** → همین ساخته می‌شود.
3. **خودِ فصل موکولش کرده** → نساز، در `OPEN_DECISIONS.md` ثبت کن.

### گام ۲ — `OPEN_DECISIONS.md` را بخوان
اغلب فصلِ جدید تصمیمی را باز می‌کند که فصل‌های قبل عمداً پارک کرده بودند. اگر
ردیفی با «منتظرِ فصلِ X» هست و X همین فصل است، **حالا وقتش است**.

### گام ۳ — برنامه بنویس و تأیید بگیر
برنامه باید داشته باشد: زمینه (چرا) · شکافِ واقعی با شاهدِ کد · تصمیم‌ها و
**دلیلشان** · فایل‌هایی که عوض می‌شوند · آنچه ساخته **نمی‌شود** و چرا · راستی‌آزمایی.

### گام ۴ — بساز، به این ترتیب
```
شاخه feat/... →  مدل‌ها →  مهاجرت →  شِما →  سرویس →  روتر →  تست
              →  api.ts →  صفحه →  راهنما →  PROJECT_OVERVIEW §۱۰ + OPEN_DECISIONS
```

### گام ۵ — راستی‌آزمایی (بخش ۶). گام ۶ — PR (بخش ۷). گام ۷ — استقرار (بخش ۸).

---

## ۵. لایه‌های بک‌اند

```
app/models/x.py        ستون‌ها + __table_args__. منطق ندارد.
app/schemas/x.py       ورودی/خروجیِ pydantic. اعتبارسنجیِ شکلی.
app/services/x.py      کلِ منطق. HTTPException اینجا بالا می‌رود.
app/routers/x.py       فقط سیم‌کشی: مجوز، صفحه‌بندی، idempotent.
app/main.py            include_router
```

هر موجودیتِ بزرگ سرویسِ **خودش** را می‌گیرد (`cashboxes.py`، `bank_accounts.py`،
`card_terminals.py`، `checkbooks.py`، `receipts.py`). بلوکِ بزرگ داخلِ سرویسِ
مشترک نگه‌دار — منتقلش کن، **بدونِ alias** تا دو نام برای یک چیز نماند.

**سندِ حسابداری** همیشه از `common.make_journal_entry(db, date, desc, source_type,
user, lines)` می‌آید. `source_type` تازه باید در `entry_source.SOURCE_MODELS` و
`SOURCE_LABELS` ثبت شود.

**مجوزها:** `require_permission("module", "action")` یا تاپل برای «هر کدام».
مراقب باش: `checks_bank` فقط `view/create/update` دارد — `approve` و `delete`
ندارد، پس `require_permission("checks_bank", "approve")` همه جز مالک را بیرون
می‌گذارد. برای ابطالِ سند از `("accounting", "delete")` استفاده کن.

---

## ۶. راستی‌آزمایی — پیش از «تمام شد»

```bash
# بک‌اند (یک اجرا در یک زمان!)
cd backend && ./venv/Scripts/python.exe -m pytest -q

# فرانت‌اند
cd desktop && npx tsc -b --force && node scripts/audit-pages.mjs && npx oxlint src
```

`audit-pages.mjs` باید **۰ خطا** بدهد (هشدار اشکالی ندارد اگر مالِ فایلِ تو نباشد).

### مهاجرت را **واقعاً اجرا کن**

تست‌ها از `Base.metadata.create_all` می‌سازند، **نه از مهاجرت‌ها**. یعنی یک
مهاجرتِ شکسته از همه‌ی تست‌ها سبز رد می‌شود. پس:

```bash
export PATH="/c/Program Files/PostgreSQL/17/bin:$PATH" PGPASSWORD=<from backend/.env>
psql -h localhost -U cubita_app -d hesabdari -c "CREATE SCHEMA probe;"
export DATABASE_URL="postgresql+psycopg://cubita_app:<pw>@localhost:5432/hesabdari?options=-csearch_path%3Dprobe"
./venv/Scripts/python.exe -m alembic upgrade head      # از صفر
./venv/Scripts/python.exe -m alembic downgrade <prev>  # برگشت
./venv/Scripts/python.exe -m alembic upgrade head      # دوباره
# RLS جدول‌های تازه را بسنج:
psql … -c "SELECT relname, relrowsecurity, relforcerowsecurity FROM pg_class …"
psql -h localhost -U cubita_app -d hesabdari -c "DROP SCHEMA probe CASCADE;"
```

`search_path` روی اسکیمای جدا یعنی دادهٔ واقعیِ کاربر (در `public`) دست نمی‌خورد.
**بعدش تأیید کن که `public.alembic_version` عوض نشده.**

`alembic heads` باید **یک سر** بدهد.

### چشمی
```bash
CUBITA_EMAIL=… CUBITA_PASSWORD=… node scripts/verify-pages.mjs
CUBITA_EMAIL=… CUBITA_PASSWORD=… node scripts/verify-pages.mjs --width 390
```
چهار عدد باید صفر باشند: سرریزِ افقی، خطای کنسول، جدولِ بی‌کلاسِ کارتی، سلولِ
بی‌برچسب.

---

## ۷. کامیت و PR

**کامیت:** یک خطِ فارسی که *چه چیزی درست شد* را می‌گوید، نه چه فایلی عوض شد.
بدونِ پیشوندِ `feat:`. مثال‌های واقعی:

```
دسته‌چک هست، ولی برگ را دو بار می‌شد خرج کرد
کارت‌خوان: موجودیت هست، ولی به تراکنش‌هایش وصل نبود
```

پایانِ پیامِ کامیت:
```
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```

**بدنه‌ی PR** (فارسی) این ساختار را دارد:
1. جوابِ بندِ «آیا از قبل هست؟» — چه چیزهایی بررسی شد و دست نخورد
2. شکافِ واقعی، با تکه‌کدِ شاهد
3. تصمیم‌ها و دلیلشان (جدول اگر کمک می‌کند)
4. آنچه ساخته **نشد** و چرا
5. راستی‌آزمایی: تعدادِ تست، خروجیِ ممیز، اجرای واقعیِ مهاجرت
6. «چشمی» — چند بند که کاربر باید خودش ببیند

پایانِ بدنه:
```
🤖 Generated with [Claude Code](https://claude.com/claude-code)
```

**merge با کاربر است** (`gh pr merge` مسدود است). و یک تجربه‌ی تلخ:

> اگر PRها روی هم پشته شده‌اند، **شاخه‌ی PRِ پایینی باید قبل از merge کردنِ PRِ
> بالایی حذف شود**، وگرنه GitHub پایه‌ی PRِ بعدی را به master برنمی‌گرداند و کد
> روی شاخه‌ی اشتباه می‌نشیند.

---

## ۸. استقرار — **چهار گام، نه یکی**

سرور **مخزنِ git نیست**. `deploy.sh` از `/tmp` می‌خواند و اگر تازه نکنی، **کدِ
قدیمی را دوباره مستقر می‌کند و موفق هم گزارش می‌دهد**. این یک بار واقعاً افتاد.

**شکلِ بسته‌ی کد مهم است.** خطِ ۱۲۸ِ `deploy.sh` این است:

```bash
tar xzf "$CODE_TGZ" -C "$APP_DIR" app alembic alembic.ini scripts
```

یعنی آن چهار عضو باید در **ریشه‌ی** آرشیو باشند. اگر با `tar -czf … backend`
بسازی، همه‌چیز زیرِ `backend/` می‌نشیند و tar با `app: Not found in archive`
می‌شکند. این یک بار واقعاً افتاد و یک چرخه‌ی استقرار سوزاند.

```bash
# ۱) بسته‌ی کد — با ‎-C backend‎، نه ‎backend‎
tar --exclude=__pycache__ --exclude='*.pyc' --exclude='.pytest_cache' \
    -czf /tmp/cubita_code.tgz -C backend app alembic alembic.ini scripts
# ۲) بسته‌ی وب — محتویاتِ dist در ریشه
cd desktop && npm run build && tar -czf /tmp/cubita_web_production.tgz -C dist .
# ۳) انتقال
scp -i ~/.ssh/cubita_vps /tmp/cubita_code.tgz /tmp/cubita_web_production.tgz root@62.60.129.39:/tmp/
# ۴) استقرار (کاربر اجرا می‌کند — برای ایجنت مسدود است)
ssh -i ~/.ssh/cubita_vps root@62.60.129.39 '/opt/hesabdari/deploy.sh production'
```

**پیش از گامِ ۳ بسنج، نه بعدش:**

```bash
tar -tzf /tmp/cubita_code.tgz | awk -F/ '{print $1}' | sort -u
# باید دقیقاً بدهد:  alembic   alembic.ini   app   scripts
```

**اگر شبکه‌ات به سرور نمی‌رسد ولی گیت‌هاب کار می‌کند:** ممکن است چند مسیرِ
اینترنت داشته باشی و مسیرِ پیش‌فرض به آن میزبان نرسد. با `ipconfig` آدرسِ محلیِ
کارتی را که وصل می‌شود پیدا کن و اتصال را به همان ببند:
`ssh -b <local-ip> …` و `scp -o BindAddress=<local-ip> …`

**پیش از گام ۴ به آرش اطلاع بده.** بعدش بسنج: نسخه‌ی مهاجرت روی سرور، سلامتِ
سرویس، و اینکه فایلِ باندلِ سرو‌شده همان است که ساختی.

### «اطلاع بده» یعنی چه — پنج چیز، نه یک پیامِ «دارم مستقر می‌کنم»

دو deployِ همزمان وسطِ مهاجرت سرویس را می‌شکند، ولی خطرِ بزرگ‌ترْ چیزی است که
**پس از** استقرار بی‌خبر عوض می‌شود. پیشِ استقرار این پنج را بنویس؛ هرکدام که
خالی است صریح بگو «هیچ».

1. **شماره‌های مهاجرتی که گرفته‌ای، و اولین شماره‌ی آزاد.** این تنها موردی است
   که تا امروز واقعاً روی تولید شکسته (۰۱۰۹/۰۱۱۰). تخته‌ی ادعا کافی نیست —
   کسی که از آن نمی‌خواند هم باید بداند.
2. **هر رفتاری که روی مسیرِ مستقر عوض شده.** معیار ساده است: «کاربری که دیروز
   این دکمه را می‌زد، فردا همان نتیجه را می‌گیرد؟» اگر نه، اسمِ دکمه را بنویس.
3. **هر تغییرِ حسابداری** — حسابِ تازه، حسابی که عوض شده، سندی که جابه‌جا شده.
   این‌ها شکلِ صورت‌های مالی را عوض می‌کنند و حسابدار باید پیش از بستنِ ماه
   بداند، نه بعدش.
4. **هر قابلیتی که برداشته یا جابه‌جا شده.** کاربری که فرمِ همیشگی‌اش را پیدا
   نمی‌کند فکر می‌کند خراب شده.
5. **هر مهاجرتی که `downgrade`اش داده حذف می‌کند.** یعنی عملاً یک‌طرفه است و
   بازگشت از آن یعنی بازیابیِ پشتیبان. صریح بگو کدام‌ها.

جایشان [DEPLOY_NOTES.md](DEPLOY_NOTES.md) است: آنچه هنوز مستقر **نشده**. همین
پنج‌تا در `PROJECT_OVERVIEW.md` §۱۰ هم ثبت می‌شوند — ولی آن *تاریخچه* است، نه
*هشدار*. یادداشتِ استقرار پیش از deploy خوانده می‌شود و بعدش پاک؛ ردیفِ تاریخچه
برای همیشه می‌ماند.

---

## ۹. وقتی چند ایجنت همزمان کار می‌کنند

این پوشه ممکن است همزمان دستِ ایجنتِ دیگری باشد — گاهی Claude Code، گاهی Codex.
**یک درختِ کاری است، نه دو شاخه:** هیچ merge‌ای محافظت نمی‌کند و آخرین نویسنده
کلِ فایل را بی‌صدا می‌برد.

**تشخیصش:** اولِ کار `git status` بگیر. اگر فایل‌هایی تغییر کرده که تو نزده‌ای،
ایجنتِ دیگری فعال است.

- **اگر هر دو Claude Code هستید:** با `ListAgents` پیدایش کن و با `SendMessage`
  فایل‌ها را صریح تقسیم کنید — «اینها را برمی‌دارم، دست نزن» و وقتی تمام شد خبر بده.
- **اگر ایجنتِ دیگر Claude Code نیست** (مثلاً Codex): پیام‌رسانِ مستقیمی نیست.
  پس **از کاربر بخواه مرز را تعیین کند**، و بگو دقیقاً کدام فایل‌ها را برمی‌داری.
- **در هر دو حالت: قبل از نوشتن روی هر فایل، دوباره بخوانش.** ممکن است از آخرین
  باری که دیدی عوض شده باشد.
- **`pytest` و `alembic` را بدونِ هماهنگی اجرا نکن.**
- **زنجیره‌ی مهاجرت خطی و تک‌سر بماند.** اگر روی مهاجرتِ منتشرنشده‌ی دیگری
  می‌سازی، `down_revision` را به آن بزن — نه شاخه‌ی موازی.
- **مهاجرتِ منتشرنشده را درجا ویرایش کن، نه با یک مهاجرتِ وصله.** اگر هیچ
  دیتابیسی اجرایش نکرده (`alembic_version` را بسنج و `git log --all` روی فایل)،
  یک `0112` که جدولِ `0109` را چند ساعت بعد وصله کند فقط نویزِ دائمی است.
- **تقارنِ سندهای خواهر را حفظ کن.** اگر یکی تخفیف و اسناد مرتبط دارد و دیگری
  ندارد، کاربر باید یادش بماند کدام کدام است — و این همان چیزی است که «اساسِ
  نرم‌افزار» را خراب می‌کند.

---

## ۱۰. نوشتن

- **همه‌ی متنِ کاربر فارسی است.** ارقام با `toLocaleString('fa-IR')`، تاریخ‌ها
  جلالی از `desktop/src/lib/jalali`.
- **کامنت‌ها «چرا» را می‌گویند، نه «چه».** مخصوصاً *کدام تله را دور زدیم*. اگر
  کامنتی می‌نویسی که از روی کد خوانده می‌شود، ننویس.
- **مهاجرت‌ها docstring دارند** که می‌گوید چه چیزی نبود، چرا این شکل انتخاب شد، و
  چرا هیچ ردیفی نوشته نمی‌شود.
- **خطا باید بگوید چه شد و حالا چه کار کند.** «خطایی رخ داد» ممنوع.
- `dir="ltr"` برای متنِ لاتین؛ ولی placeholderِ فارسی روی فیلدِ `dir="ltr"` نگذار —
  راهنما را در `<span className="field-hint">` زیرِ فیلد بگذار.
- **گاردی که نمی‌تواند شلیک کند ننویس.** اگر ستونی را خودِ همان مهاجرت می‌سازد،
  پیش‌پروازِ «تکراری هست؟» کدِ مرده است و فقط خواننده‌ی بعدی را فریب می‌دهد.

---

## ۱۱. صداقت در گزارش

- تست شکست خورد؟ بگو، با خروجی.
- گامی را رد کردی؟ بگو.
- چشمی ندیدی؟ **نگو «تأیید شد».** بگو «فقط با `tsc` و تست سنجیده شد؛ خودِ رابط
  را ندیدم».
- اگر دستوری را محیط مسدود کرد، **دورش نزن** — به کاربر بگو.
