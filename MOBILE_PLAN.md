# نقشه‌ی اپِ موبایل — پنج فاز

اپِ اندروید (`mobile/`) نسخه‌ی v1 خود را دارد و روی کافه‌بازار است. این سند می‌گوید
از اینجا به بعد کجا می‌رویم و چرا به این ترتیب.

قواعدِ کدنویسی در [CLAUDE.md](CLAUDE.md)، نقشه‌ی کلی در
[PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md). کارهای شناخته‌شده‌ی موکول‌شده در
[FUTURE_WORK.md](FUTURE_WORK.md).

---

## تصمیمِ محصولی: یک اپ، چند نقش

اپ در پلنِ اولیه «مدیرِ همراه» بود — فقط برای صاحبِ کسب‌وکار، عمدتاً خواندنی. حالا
قرار است **هم مدیر و هم کارمندِ میدانی** را بپوشاند: ویزیتور، انباردار، مأمورِ حمل.

**این کارِ بک‌اندی ندارد.** RBAC از قبل کامل است: شش نقش در
`app/models/user.py` (`DEFAULT_ROLES`) و `/api/auth/me` هم `permissions` را
برمی‌گرداند.

| نقش | دسترسی |
|---|---|
| `owner` مدیر/مالک | `*` — همه‌چیز |
| `accountant` حسابدار | حسابداری، فاکتور، چک و بانک، دارایی |
| `salesperson` فروشنده | فاکتور (ساخت)، انبار (دیدن)، باشگاه مشتریان |
| `warehouse_keeper` انباردار | انبار (ساخت/ویرایش)، تولید |
| `delivery_agent` مأمور حمل | فقط بازار: دیدنِ سفارش + ثبتِ تحویل |
| `payroll_officer` مسئول حقوق | حقوق و دستمزد |

---

## نقطه‌ی شروع (۱۴۰۵/۰۶/۱۵) — و امروز

| معیار | آن‌وقت | حالا |
|---|---|---|
| کد | ~۴٬۵۰۰ خط، ۱۹ صفحه | ~۷٬۲۰۰ خط، ۲۲ صفحه (+ ۱٬۴۰۰ خط تست) |
| تست | **صفر** | ۱۲۲ تستِ موبایل (+ بک‌اند ۱۸۱۶) |
| گزارشِ خطا | **هیچ** | خودمیزبان، با صفِ روی دیسک |
| آفلاین | **هیچ** | کشِ خواندنی + صفِ نوشتن + پیش‌نویسِ انبارگردانی |
| CI | موبایل در `ci.yml` نیست | tsc + jest + ممیزِ ساختاری |
| نقش‌محوری | تب‌ها برای همه یکسان | هر نقش فقط آنچه واقعاً دارد |
| نقشِ بی‌محتوا | انباردار، مأمورِ حمل، مسئولِ حقوق | فقط مسئولِ حقوق |

---

## فاز ۱ — تورِ ایمنی ✅

**چرا اول:** اپ روی کافه‌بازار زنده است و اگر برای کاربری کرش کند، تنها راهِ
فهمیدنش نظرِ یک‌ستاره است. هر قابلیتی که روی کدِ بی‌تست و بی‌رصد اضافه شود ریسک
را ضرب می‌کند، نه جمع.

- [x] گزارشِ کرش (خودمیزبان) + `ErrorBoundary` به‌جای صفحه‌ی سفید
- [x] اندپوینتِ بک‌اندِ دریافتِ گزارش (مهاجرتِ ۰۱۰۰)
- [x] تست روی چیزهایی که بی‌صدا غلط می‌دهند: کلاینتِ API، رفرشِ توکن،
      قالبِ عدد/تاریخِ جلالی
- [x] افزودنِ موبایل به `.github/workflows/ci.yml`
- [x] ممیزِ ساده مثلِ `audit-pages.mjs` دسکتاپ (کدِ مرده را می‌گیرد)

**چرا خودمیزبان و نه Sentry:** حسابِ Sentry برای یک شرکتِ ایرانی قابلِ تعلیق
است، و آن‌وقت گزارشِ کرش **بی‌صدا** قطع می‌شود — همان حالتی که این فاز برای
فرار از آن است. ضمناً داده‌ی حسابداری نباید به سرویسِ ثالث برود. بک‌اند و VPS
از قبل هست، پس هزینه‌ی حاشیه‌ای کم است.

## فاز ۲ — نقش‌محوری ✅

`hasPermission` به ناوبری سیم‌کشی شود، و خانه‌ی متفاوت به‌ازای نقش (مالک KPI
ببیند، انباردار کارهای انبار).

**باید پیش از فازِ ۴ بیاید.** هر قابلیتِ میدانی که قبل از این اضافه شود، یک
بدهیِ نقش‌محوری هم اضافه می‌کند.

نقطه‌ی شروع نیمه‌کاره بود: `HomeScreen` از قبل کنش‌های فاکتور و خزانه را با
`hasPermission` گیت می‌کرد، ولی **نوارِ تب برای همه یکسان بود** — انباردار و
مأمورِ حمل «گزارش» و «اشخاص» را می‌دیدند و ۴۰۳ می‌گرفتند.

**انجام شد:** جدولِ مجوزها در `src/auth/access.ts` متمرکز شد (هر ردیف با ارجاع
به `require_permission`ِ همان روتر، نه حدس)، تب‌ها و کنش‌ها از همان‌جا گیت
می‌شوند، و نقشی که هیچ بخشی ندارد `NoAccessScreen` می‌بیند به‌جای تب‌های
۴۰۳‌دهنده. ۱۷ تست، پوشش‌دهنده‌ی هر ۷ نقشِ بک‌اند.

**آنچه این کار روشن کرد:** برای سه نقش (انباردار، مسئولِ حقوق، و مأمورِ حملِ
غیرِبازاری) اپِ موبایل **هیچ محتوایی ندارد**. این استدلالِ فازِ ۴ را مشخص
می‌کند: انبارگردانی و بارکد نه «قابلیتِ اضافه»، بلکه تنها دلیلِ وجودِ اپ برای
آن نقش‌هاست.

## فاز ۳ — آفلاین ✅

کشِ خواندنی + صفِ نوشتن. با تصمیمِ «اپِ میدانی» این از «خوب است» به **اجباری**
تبدیل شد: انباردارِ داخلِ انبار و ویزیتورِ داخلِ خیابان آنتن ندارند. دسکتاپ
الگویِ آزموده‌ی `outbox` را دارد.

**انجام شد — کشِ خواندنی، با صفر وابستگیِ تازه.** `dehydrate`/`hydrate` و
`onlineManager` در هسته‌ی react-query بودند و `expo-file-system` از قبل کار
می‌کرد؛ سه بسته‌ی تازه برای صد خط کد توجیه نداشت.

- کش روی دیسک می‌ماند و اپ فوراً با آن بالا می‌آید (پیش از این هر بازکردن =
  صفحه‌ی خالی تا وقتی شبکه جواب بدهد)
- سقفِ ۲۴ ساعت: عددِ هفته‌ی پیش که «امروزی» به‌نظر برسد، برای نرم‌افزارِ
  حسابداری از نبودِ عدد بدتر است
- بنرِ آفلاین، چون حالا اپ در حالتِ قطع هم عدد نشان می‌دهد
- کش هنگامِ خروج پاک می‌شود — روی گوشیِ مشترک وگرنه کاربرِ بعدی اعدادِ
  کسب‌وکارِ قبلی را می‌بیند
- تشخیصِ آفلاین از خطای واقعیِ کلاینتِ API می‌آید نه NetInfo: آنچه اهمیت دارد
  «آیا بک‌اندِ ما جواب می‌دهد» است، نه «آیا وای‌فای وصل است»

**صفِ نوشتن (outbox) هم انجام شد.** ثبتِ فاکتور و دریافت/پرداخت حالا آفلاین
کار می‌کنند و خودشان سرِ بازگشتِ شبکه می‌روند.

پیش‌نیازش یک تغییرِ بک‌اند بود: خزانه `Idempotency-Key` نمی‌پذیرفت. صفی که
پاسخ را نگیرد دوباره می‌فرستد — رفتارِ درستش همین است — ولی بدونِ آن کلید،
همان یک دریافت دو بار در دفتر می‌نشست، بی‌هیچ خطایی. زیرساختش از قبل بود
(`app/services/idempotency.py`)، فقط به خزانه وصل نشده بود.

## فاز ۴ — قابلیت‌های میدانی

جایی که موبایل از دسکتاپ **بهتر** است، نه کپی‌اش:

- [x] اسکنِ بارکد
- [x] انبارگردانی با گوشی در دست (به‌جای پشتِ میز)
- [x] ثبتِ سفارش در محلِ مشتری — اسکنِ بارکد در انتخابِ کالای فاکتور
- [ ] ثبتِ تحویل برای مأمورِ حمل

**کارِ بک‌اند: صفر.** `app/routers/stock_taking.py` با شش اندپوینت از قبل بود و
`warehouse_keeper` دقیقاً `inventory: [view, create, update]` دارد. کلِ فاز یک
اتصال بود، نه یک ساخت.

### سه چیزی که بی‌صدا غلط می‌دادند و بسته شدند

**۱. بازنویسیِ شمارشِ همکار.** `PUT /counts` در بک‌اند یک *patchِ جزئی* است: هر
`line_id` که بفرستی نوشته می‌شود، بقیه دست‌نخورده می‌مانند. انبارِ بزرگ را دو نفر
با دو گوشی می‌شمارند؛ اگر اپ کلِ فهرست را بفرستد — کارِ ساده و طبیعی — شمارشِ
نفرِ دوم با مقدارِ کهنه‌ی عکسِ اولیه پاک می‌شود. نه خطایی، نه هشداری. پس
`stock/countDraft.ts` **فقط** ردیف‌هایی را می‌فرستد که کاربر روی همین گوشی دست
زده، و تستش این را قفل می‌کند.

**۲. ثبتِ نهایی با شمارشِ روی گوشی.** «ثبت» سندِ تعدیلِ حسابداری می‌سازد. اگر
شمارش‌های ثبت‌نشده هنوز روی دیسک باشند، سند با اعدادِ قدیمی بسته می‌شود و کارِ یک
روزِ انباردار در دفتر اثری نمی‌گذارد. حالا ثبت تا وقتی چیزی ثبت‌نشده هست بسته
است و دلیلش را می‌گوید.

**۳. اسکنِ تکراری.** `onBarcodeScanned` مادامی که بارکد در کادر است ده‌ها بار در
ثانیه شلیک می‌کند؛ بدونِ دروازه، یک بار گرفتنِ گوشی جلوی جعبه یعنی «۳۰ عدد».
`scan/gate.ts` جدا و خالص نوشته شد تا تست‌پذیر باشد.

### تصمیم‌های دیگر

- **تطبیقِ بارکد محلی است**، نه با `GET /api/items/by-barcode`. انبارِ فلزی آنتن
  ندارد؛ اسکنی که برای هر کالا یک درخواست بزند دقیقاً جایی که لازم است کار
  نمی‌کند. فهرستِ کالاها از قبل در کشِ فازِ ۳ هست.
- **هر اسکن «۱+ از صفر» است، نه «۱+ روی مقدارِ فعلی»**: `counted_qty`ِ سرور با
  *موجودیِ سیستمی* پر شده، پس افزودن به آن یعنی «۱۲ تای سیستم + ۱ تایی که شمردم
  = ۱۳». شمارشِ فیزیکی همیشه از صفر است.
- **سه پیامِ متفاوت برای «پیدا نشد»**: بارکدِ ناشناس، کالای خارج از جلسه (یعنی
  بعد از عکس‌برداری ساخته شده)، و پیدا شد. ادغامشان یعنی کالایی بی‌سروصدا از
  انبارگردانی جا می‌ماند.
- **تاریخ‌ها با `jalaali-js`** — همان کتابخانه‌ی دسکتاپ، نه پیاده‌سازیِ جدا و نه
  `toLocaleDateString('fa-IR')` که به ICUِ خودِ گوشی وابسته است.
- `todayIso` و پاک‌سازیِ عدد که در دو صفحه کپی شده بودند به `lib/format.ts` رفتند
  و حالا ارقامِ عربی‌ـهندی (٠١٢) را هم می‌شناسند.

**یافته‌ی فازِ ۲ بسته شد:** انباردار دیگر دستِ خالی نیست. تنها نقشی که هنوز
هیچ‌چیز ندارد «مسئولِ حقوق و دستمزد» است.

## فاز ۵ — بلوغ

ویجتِ صفحه‌ی خانه، deep link کامل، دایجستِ زمان‌بندی‌شده‌ی هشدارها،
دسترس‌پذیری، بهینه‌سازیِ عملکرد.

---

## iOS — عمداً فازِ شماره‌دار نیست

تصمیمِ تجاریِ جداست: حسابِ توسعه‌دهنده‌ی اپل (~$۹۹ سالانه)، دسترسی به مک یا
EAS، و بازبینیِ App Store — و هر سه از ایران دردسر دارند. `bundleIdentifier`
در `app.json` هست ولی هرگز build نشده. هر جای این مسیر می‌تواند بنشیند، یا
اصلاً ننشیند.
