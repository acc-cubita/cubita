# پلنِ توسعه — «کوبیتا سازمانی» (Cubita Enterprise)

> **این سند چیست؟** نقشه‌ی راهِ نسخه‌ی سرور/کلاینتِ درون‌سازمانیِ کوبیتا.
> مرجعِ چندفازی؛ با هر فازِ تمام‌شده به‌روزرسانی شود. مکمل [PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md).
>
> **یک محصولِ جدا، از یک کدِ مشترک.** کوبیتا از این پس چهار محصول دارد: وب، دسکتاپِ
> ابری، موبایل، و **دسکتاپِ سازمانی**. سازمانی از دیدِ کاربر کاملاً جداست — نام، آیکون،
> نصاب، پوشه‌ی داده (`appId`ِ جدا)، کانالِ آپدیت؛ و روی یک سیستم **کنارِ** دسکتاپِ ابری
> نصب می‌شود بی‌تداخل. ولی از همان `desktop/` و `backend/`ِ همین مخزن ساخته می‌شود، با
> پرچمِ نسخه (`cloud` / `enterprise`) — نه فورک: هر باگی در دفترِ حسابداری یک‌بار درست
> می‌شود، نه دو بار.

---

## خلاصه‌ی اجرایی

کوبیتا امروز فقط ابری است: همه به `acc.cubita.ir` وصل‌اند و داده روی VPSِ ماست.
شرکت‌ها و سازمان‌های ایرانی الگوی دیگری می‌خواهند — همان الگوی جاافتاده‌ی نرم‌افزارهای
حسابداریِ داخلی: **یک سیستمِ سرور در شرکت، چند حسابدار به‌عنوانِ کلاینت روی شبکه‌ی
داخلی**، داده‌ای که از شرکت بیرون نمی‌رود، و فعال‌سازی با کدی که ما از `admin.cubita.ir`
صادر می‌کنیم.

## وضعیتِ مراحل

| مرحله | موضوع | وضعیت |
|---|---|---|
| **M0** | اسپایکِ امکان‌سنجیِ بسته‌بندی | 🟡 بخشِ محلی ✅ — بخشِ VM (ویندوزِ تمیز) مانده |
| **M1** | پرچمِ نسخه + آدرسِ سرورِ زمان‌اجرا | ✅ (جز جست‌وجوی خودکارِ `/24` — به M4 رفت) |
| **M2** | هسته‌ی مجوز (آفلاین‌محور) | ✅ — مانده: ساختِ کلیدِ امضای تولید روی ابر (یک دستور، پایین) |
| M3 | پنلِ ستاد + فعال‌سازیِ آنلاین | ⬜ |
| M4 | نصاب | ⬜ |
| M5 | آپدیت | ⬜ |
| M6 | مهاجرت و نگهداری | ⬜ |

**MVP = M1 + M2 + M4.** برای ده مشتریِ اول، M3 و M5 را می‌شود دستی اداره کرد.

---

## تصمیم‌های گرفته‌شده (۱۴۰۵/۰۷/۰۲ با مالک — دوباره بحث نشود)

| موضوع | تصمیم |
|---|---|
| فعال‌سازی | آنلاینِ یک‌باره؛ پس از آن برای همیشه آفلاین (+ مسیرِ آفلاینِ پشتیبان، پایین‌تر) |
| داده | کاملاً مستقل، بدونِ همگام‌سازی با ابر |
| کلاینت | فقط اپِ دسکتاپ روی شبکه‌ی داخلی — این نسخه اپِ وب ندارد |
| مجوز کنترل می‌کند | تعداد کاربر + تاریخِ انقضا + ماژول‌ها + گره به یک دستگاه |
| PostgreSQL | داخلِ همان یک نصاب، بدونِ نصبِ دستی |
| «تعداد کاربر» | تعدادِ حسابِ کاربری (نه همزمان) — همان `Tenant.max_users` |
| آپدیت | سرور از اینترنت می‌گیرد، کلاینت‌ها از سرور |
| پیش از فعال‌سازی | ۳۰ روز آزمایشی، بعد قفلِ *نوشتن* |
| نصاب | یک فایل؛ موقعِ نصب می‌پرسد سرور است یا کلاینت |
| نام | «کوبیتا سازمانی» / Cubita Enterprise |
| تعویضِ دستگاه | دکمه‌ی انتقال در `admin.cubita.ir` + ثبت در کارنامه‌ی ستاد |
| ماژول‌های حذف‌شده | «اتصال فروشگاه» (`integration`) و بازار/عمده‌فروشی (`tenant.kind`) |
| معوق | POS / فروش اقساطی / باشگاه مشتریان / مؤدیان — فعلاً می‌مانند، بعداً تصمیم |

---

## قیدهای کشف‌شده در کد (این‌ها طراحی را شکل دادند)

1. **PostgreSQL اجباری است.** `FORCE ROW LEVEL SECURITY` در `app/tenancy.py:104-125`،
   بایندِ نشست در `app/tenant_context.py:113-124`، تریگرهای append-only در
   `app/audit.py:43-74`، `JSONB` در ۴۹ جا، `UUID`ِ پستگرس در ۱۱۴ فایلِ مدل.
   SQLite گزینه نیست — on-prem یعنی حملِ Postgres.
2. **گلوگاهِ اجازه‌ی نوشتن یک نقطه است:** `app/deps.py:149-162`. داک‌استرینگش
   (`deps.py:120-122`) صریح می‌گوید چرا باید یک نقطه بماند. مجوز **همان‌جا** می‌نشیند.
3. **اصلِ «خواندن هرگز قفل نمی‌شود»** (`deps.py:132-133`): «دفتر مالی سند قانونی خودِ
   مشتری است و قفل کردنش پشت پرداخت، گروگان گرفتن چیزی است که مال ما نیست.» روی سرورِ
   خودِ مشتری این حتی واجب‌تر است — **انقضای مجوز یا آزمایشی نوشتن را می‌بندد، ولی
   خواندن و خروجی‌گرفتن (`/api/backup/export`) باز می‌ماند.** این عمداً از قفلِ کاملِ
   تریالِ ابری سخت‌گیری کمتری دارد: داده روی ماشینِ خودشان است.
4. **آدرسِ سرور در بیلد فریز می‌شود** (`desktop/src/api.ts:4-5`) ولی ماژول‌های Electron
   از قبل `apiBaseUrl` را پارامتری می‌گیرند (`sync.ts`، `authSession.ts`، `backup.ts`).
5. **خروجی/ورودیِ کاملِ یک مستأجر از قبل هست** (`app/routers/backup.py`) — مسیرِ
   مهاجرت از ابر به on-prem همین است.
6. **کارنامه‌ی ستاد اجباری است:** `tests/test_staff_audit_coverage.py:39-55` هر مسیرِ
   `/api/admin/*`ِ تغییردهنده را که `staff_audit.record(` نداشته باشد رد می‌کند.

---

## معماری — یک کد، دو نسخه

**پرچمِ نسخه.** `app/config.py`: `edition: str = "cloud"` با اعتبارسنجی در `_validate()`
(الگوی `KNOWN_ENVS`) + `api_bind`/`api_port` + `license_signing_key` (فقط ابر، هم‌رده‌ی
`secrets_key` — هرگز کامیت نمی‌شود). دسکتاپ: `VITE_EDITION=enterprise` در بیلد **و**
فیلدِ `edition` در `MeOut`، تا رابط به بیلد اعتماد نکند و سرور حقیقت را بگوید.

| لایه | cloud | enterprise |
|---|---|---|
| گیتِ نوشتن | تریال + اشتراک | تریال + **وضعیتِ مجوز** |
| `require_feature` (`deps.py:173`) | `is_trial` | `feature ∈ license.feat` |
| `require_module` (`deps.py:191`) | `allowed_modules()` | `allowed_modules() ∩ license.mods` |
| روترها (`main.py:128-194`) | همه | بدونِ `integration`، `shop`، `storefront`، `marketplace`، `billing`، `admin_*`، `devices` |
| `tenant.kind` | چند نوع | همیشه `standard` |

**ریفکتورِ کلیدی:** دو بلوکِ `deps.py:149-162` به
`app/services/entitlements.py::enforce(db, principal, actions)` منتقل می‌شوند با دو شاخه
بر پایه‌ی نسخه؛ `deps.py` فقط **یک** فراخوان نگه می‌دارد. یک تستِ ساختاری (هم‌سبکِ تستِ
کارنامه) اثبات می‌کند فهرستِ روترهای نسخه‌ی سازمانی هیچ مسیرِ `/api/shop`،
`/api/storefront`، `/api/admin` یا `/api/billing` ندارد.

## مجوز و فعال‌سازی

- **امضا:** Ed25519 با `cryptography` (از قبل غیرمستقیم در `requirements.txt` — صریح
  پین شود). قالب: `CUB1.<b64url(payload)>.<b64url(sig)>` روی JSONِ کانونیک.
- **payload:** `v, lic, iat, exp, org, seats, mods, feat, fp, grace, edition`.
- **کلیدها:** خصوصی فقط روی ابر؛ عمومی **ثابتِ داخلِ کد** در `app/licensing/keys.py`
  (فایلِ کنارِ باینری را می‌شود عوض کرد؛ فهرست بودنش چرخشِ کلید را ممکن می‌کند).
- **اثرانگشتِ ویندوز:** `MachineGuid`ِ رجیستری + `Win32_ComputerSystemProduct.UUID` +
  سریالِ ولومِ سیستم؛ تطبیقِ **۲ از ۳** کافی است تا تعویضِ یک دیسک نصب را خراب نکند.
  **هرگز MAC** — داک و VPN عوضش می‌کنند.
- **جریان:** سرور `{code, fp, components, install_id, org, version}` را به
  `POST /api/enterprise/activate` روی ابر می‌فرستد و توکنِ امضاشده می‌گیرد.
  **مسیرِ آفلاینِ پشتیبان:** «کدِ درخواست» به‌صورتِ متن/QR که ستاد در پنل وارد می‌کند و
  توکن را پس می‌دهد — سرورِ خیلی از شرکت‌ها اینترنت ندارد و بدونِ این، فروش دقیقاً به
  همان مشتری‌ها می‌خورد. امضا و منطق یکی است؛ فقط شبکه از وسط برداشته می‌شود.
- **ذخیره روی سرورِ مشتری:** هم `C:\ProgramData\Cubita\license.lic` هم یک ردیف در جدولِ
  `enterprise_license` در دیتابیسِ خودشان — پاک‌کردنِ فایل تریال را ریست نکند و ریستورِ
  دیتابیس فعال‌سازی را نبَرد. `last_seen_at`ِ یکنواخت، برای گرفتنِ عقب‌بردنِ ساعت.
- **راستی‌آزمایی یک‌بار در بوت** در `app/licensing/state.py` کش می‌شود — نه کوئری
  به‌ازای هر درخواست.
- **جدول‌های تازه‌ی ابر** (سراسری/بدونِ RLS — به `GLOBAL_TABLES` در `tenancy.py:31`):
  `enterprise_licenses`، `enterprise_installs`، `enterprise_license_transfers`.

## قابلیت‌های ابری که باید خنثی شوند

ایمیل (SMTP)، پیامک، زرین‌پال، پوشِ FCM، فروشگاه/بازار، کرونِ تریال (**هرگز جابِ حذفِ
خودکار روی داده‌ی مشتری اجرا نشود**)، و آپدیترِ فعلی. مؤدیان **می‌ماند** ولی به‌عنوانِ
`feat`ِ مجوزدار، و نصاب باید `SECRETS_KEY`ِ اختصاصیِ همان نصب بسازد.

**بازیابیِ رمزِ فراموش‌شده بدونِ اینترنت** — امروز هیچ مسیری ندارد (`routers/members.py`
فقط دعوت/نقش/وضعیت دارد). دو افزوده، هر دو ثبت در `app/audit.py`:
1. `POST /api/members/{id}/reset-password` مخصوصِ مالک (هم‌شکلِ `require_owner` در
   `backup.py:40`) با رمزِ موقتِ یک‌بارمصرف؛ رابط در `UserListPage.tsx`.
2. ابزارِ کنسولیِ `cubita-admin.exe reset-owner-password <email>` برای وقتی *مالک* فراموش
   کند — دسترسیِ فیزیکی به سرور تنها ریشه‌ی اعتمادِ قابلِ قبول در حالتِ آفلاین است.

## کارِ سمتِ کلاینت

✅ **ساخته شد در M1** — جزئیات در «نتایجِ M1» پایین. خلاصه: main نشانی را از
`userData/server-settings.json` می‌خواند و preload آن را **همزمان** (`sendSync`، نه
`additionalArguments` که بعد از تغییرِ نشانی کهنه می‌ماند) روی `window.cubitaConfig`
می‌گذارد؛ `api.ts` پیش از اولین fetch آن را دارد. جادوگرِ «اتصال به سرور» داخلِ خودِ اپ
است (پیش‌فرض *نامِ ماشین* نه IP). **مانده:** جست‌وجوی خودکار در `/24` — به M4 رفت،
چون بدونِ نصابِ واقعی و فایروال آزمودنی نیست.

## نصاب (سنگین‌ترین بخش)

- **Postgres 16**، بسته‌ی باینریِ EnterpriseDB، `initdb` موقعِ نصب،
  `C:\ProgramData\Cubita\pgdata`، پورت **5433**، `listen_addresses='localhost'`.
- **حیاتی:** اپ با نقشِ `cubita_app` (بدونِ `BYPASSRLS`) وصل شود
  (`scripts/setup_db_roles.sql`) — اتصال با سوپریوزر بی‌صدا کلِ RLS را خاموش می‌کند.
- **پایتون:** PyInstaller `--onedir` — ✅ **در M0 اثبات شد** (پایین).
- **سرویس‌ها:** WinSW — `CubitaPostgres` و `CubitaApi` (وابسته به اولی). API روی
  `0.0.0.0:8420`. فایروال فقط `private,domain` — **هرگز Public**.
- **NSIS:** صفحه‌ی «این رایانه سرور است یا کلاینت؟» بعد از صفحه‌ی مجوز؛ سرور elevation
  لازم دارد (امروز `perMachine: false`). کانفیگِ دومِ electron-builder با
  `appId: ir.ipnetcity.cubita.enterprise` → پوشه‌ی داده‌ی جدا. `npm run dist` باید همچنان
  اول `rebuild-native` بزند.
- **اعلامِ آدرس به ادمین:** صفحه‌ای که IPهای غیرداخلی + نامِ ماشین را با دکمه‌ی کپی
  نشان دهد و درباره‌ی IPِ DHCP هشدار بدهد.

## آپدیت

سرور `latest.yml`ِ کانالِ سازمانی را می‌گیرد، **امضای Ed25519 را می‌سنجد**، و از
`/updates/*` (با پشتیبانی از Range که electron-updater لازم دارد) به کلاینت‌ها می‌دهد.
کلاینت با `autoUpdater.setFeedURL({provider:'generic'})` در زمانِ اجرا به سرور اشاره
می‌کند و **پیش از نصب امضا را می‌سنجد** — روی HTTPِ LAN بدونِ این، هرکسی روی شبکه
می‌تواند کدِ دلخواه تزریق کند. آپدیتِ خودِ سرور **دستی و با تأییدِ اپراتور** است و
زنجیره‌ی `scripts/deploy.sh` را تکرار می‌کند: توقف → `pg_dump -Fc` → جابه‌جاییِ پوشه با
`app.prev` → `alembic upgrade head` → راه‌اندازی → `/api/health` → در هر خطا برگشت.

## پنلِ ستاد

`routers/admin_licenses.py` (صدور/ابطال/انتقال/صدورِ دوباره/فهرستِ نصب‌ها) + روترِ عمومیِ
`enterprise_activation.py`؛ ناحیه‌ی تازه‌ی `"licenses"` در `staff_roles.py:15-27`؛
اکشن‌های تازه در `services/staff_audit.py:23-53` و `staff_audit.record(` در هر مسیرِ
تغییردهنده؛ صفحه‌ی `admin/src/pages/LicensesPage.tsx` + `nav.tsx`/`App.tsx`/`PAGE_TITLES`
و کیسِ تازه در `admin/src/nav.test.ts` (قاعده‌ی A9).

---

## نتایجِ M0 (۱۴۰۵/۰۷/۰۲) — بخشِ محلی

**پرسش:** آیا بک‌اندِ فعلی، *بدونِ هیچ تغییری در کد*، با PyInstaller بسته‌بندی و اجرا
می‌شود؟ **پاسخ: بله.** این پرریسک‌ترین فرضِ کلِ پروژه بود و برقرار است؛ جایگزینِ
CPythonِ embedded لازم نشد.

### آنچه از داخلِ باینریِ فریزشده اثبات شد

| سنجش | نتیجه |
|---|---|
| ثبتِ روت‌ها | ✅ ۷۱۱ روت |
| bcrypt (رفت‌وبرگشتِ hash/verify) | ✅ |
| `psycopg[binary]` | ✅ DLLها خودکار پیدا شدند (`psycopg_binary.libs`) |
| `fpdf2` + `uharfbuzz` | ✅ |
| `alembic upgrade head` | ✅ همه‌ی ۱۸۱ مهاجرت در حالتِ فریز ایمپورت شدند (برای ساختنِ گراف لازم است) |
| ورودِ واقعی (DB + bcrypt + JWT + نوشتنِ رفرش) | ✅ HTTP 200 |
| `/api/auth/me` — **RLS و بایندِ نشست به مستأجر** | ✅ مستأجرِ درست، متنِ فارسی سالم |
| logout → باطل‌شدنِ رفرش | ✅ ۲۰۴ و سپس ۴۰۱ |
| حجم | **۸۴ مگابایت**، ۱۱۴۷ فایل (بزرگ‌ترین‌ها: Pillow ۱۲٫۸MB از طریقِ fpdf2، باندینگِ Rustِ `cryptography` ۹٫۴MB) |

### چهار تله که به آن‌ها خوردیم (هرکدام یک قاعده برای M4)

1. **گاردِ `config.py` موقعِ *بیلد* شلیک می‌کند، نه فقط موقعِ اجرا.** PyInstaller برای
   تحلیلِ وابستگی ماژول‌ها را ایمپورت می‌کند، پس ترکیبِ `ZARINPAL_SANDBOX=false` +
   `ENV=development` وسطِ بسته‌بندی `RuntimeError` می‌دهد. **پیامد برای M1:** وقتی
   اعتبارسنجیِ `edition` به `_validate()` اضافه می‌شود، محیطِ بیلدِ سازمانی هم باید از
   آن رد شود.
2. **مسیرهای `--add-data` نسبت به `--specpath` حل می‌شوند، نه cwd.** مسیرِ مطلق بدهید، یا
   بهتر: یک فایلِ `.spec`ِ کامیت‌شده در M4.
3. **`passlib` هندلرِ bcrypt را در زمانِ اجرا از رجیستری لود می‌کند** و PyInstaller
   نمی‌بیندش → `--collect-submodules passlib`.
4. **سه فایلِ دادهٔ زمان‌اجرا خودکار جمع نمی‌شوند** (PyInstaller فقط `.py` جمع می‌کند):
   `app/data/chart_templates.json` و `app/assets/fonts/Vazirmatn-{Regular,Bold}.ttf`.
   **این از آن خطاهای «بی‌صدا»ست:** فایلِ غیرِ‌`.py`ِ تازه‌ای که کسی زیرِ `app/` بگذارد
   بیلد را نمی‌شکند، فقط نسخه‌ی سازمانی را در زمانِ اجرا. **برای M4:** تستی که هر فایلِ
   غیرِ‌`.py`ِ زیرِ `app/` را که در `.spec` نیامده رد کند.

یافته‌ی جانبی: نقشِ دیتابیسِ توسعه اجازه‌ی `CREATE DATABASE` ندارد — اصلِ «حداقلِ
دسترسی» که طرحِ `cubita_app` رویش حساب کرده همین حالا هم برقرار است.

### دستورِ بیلدِ کارکرده (مرجعِ M4)

از `backend/`، با `ZARINPAL_SANDBOX=true` در محیط:

```bash
python -m PyInstaller --noconfirm --onedir --console --name cubita-server \
  --paths . \
  --add-data "<abs>/backend/alembic;alembic" \
  --add-data "<abs>/backend/alembic.ini;." \
  --add-data "<abs>/backend/app/data;app/data" \
  --add-data "<abs>/backend/app/assets;app/assets" \
  --collect-submodules app \
  --collect-submodules passlib \
  --collect-submodules uvicorn \
  entry.py
```

`entry.py` سه فرمان دارد: `serve` (uvicorn روی `app.main:app`)، `migrate`
(`alembic.command.upgrade` با `script_location`ِ **مطلق** از `sys._MEIPASS` — چون
`alembic.ini` نسبی است و cwdِ یک سرویسِ ویندوز قابلِ اتکا نیست)، و `selftest`.
در M0 اسپایک عمداً کامیت نشد (دورریختنی)؛ نسخه‌ی واقعیِ `entry.py` و `.spec` در M4 می‌آیند.

### آنچه هنوز باز است — بخشِ VM (باید روی ویندوزِ تمیز سنجیده شود)

- اجرا روی ویندوزی که **پایتون ندارد** (اینجا روی ماشینِ توسعه سنجیده شد).
- Postgres 16ِ پرتابل: `initdb`، و ساختِ نقش‌های `cubita_app`/`cubita_migrate`.
- سرویس‌های WinSW و ترتیبِ راه‌اندازیِ `CubitaPostgres` → `CubitaApi`.
- رفتارِ آنتی‌ویروس/SmartScreen با exeِ بدونِ امضا که پورت باز می‌کند.
- **آزمونِ حیاتیِ RLS روی VM:** با نقشِ `cubita_app`، یک `SELECT` روی جدولِ مستأجرمحور
  **بدونِ** `app.tenant_id` باید صفر ردیف بدهد. بدونِ این، جداسازی بی‌صدا رفته است.

---

## نتایجِ M1 (۱۴۰۵/۰۷/۰۲)

**بک‌اند**
- `EDITION=cloud|enterprise` در `config.py`، با اعتبارسنجی در `_validate()` و
  `settings.is_enterprise`. گاردِ زرین‌پال در نسخه‌ی سازمانی خاموش است (پولی جابه‌جا
  نمی‌کند) — تله‌ی ۱ِ M0 همین‌جا بسته شد.
- **`app/routing.py`**: همه‌ی روترها به ترتیب، به‌اضافه‌ی `CLOUD_ONLY` (ستاد، پرداخت،
  فروشگاه، بازار، اتصال فروشگاه، پوش) و `ENTERPRISE_ONLY`. `main.py` فقط
  `include_routers(app, settings.edition)` دارد. ابری ۷۱۱ مسیر (بی‌تغییر)، سازمانی ۶۱۳.
- **`services/entitlements.py::enforce`** — بلوکِ آزمایشی/اشتراکِ `deps.py` اینجا آمد و
  `require_permission` فقط یک فراخوان دارد. شاخه‌ی سازمانی (`_enforce_license`) عمداً
  خالی است تا M2؛ اشتراکِ ابری آنجا سنجیده **نمی‌شود**.
- **`/api/setup`** (فقط سازمانی): راه‌اندازیِ سرورِ تازه‌نصب — کسب‌وکار + مالک، بدونِ
  کدِ ایمیلی، فقط وقتی هیچ کسب‌وکاری نیست، زیرِ `pg_advisory_xact_lock`. کسب‌وکار
  `is_trial=False` و بدونِ اشتراک ساخته می‌شود (کرونِ حذفِ آزمایشی هرگز سراغش نمی‌آید).
- ثبت‌نامِ self-serve و بازیابیِ رمز با ایمیل/پیامک در سازمانی ۴۰۴ می‌دهند (`deps.cloud_only`).
- `/api/health` و `MeOut` فیلدِ `edition` دارند؛ `is_platform_admin` در سازمانی همیشه false.
- ماژول‌های محدود (تولید) در سازمانی بدونِ گرنت باز‌اند — سوپرادمینی نیست که گرنت
  کند؛ M2 این را به `license.mods` می‌سپارد. «اتصال فروشگاه» حذف است.
- CORS در سازمانی خودکار `file://` را می‌پذیرد (پایین توضیح داده شده).
- تست‌ها: `tests/test_editions.py` (۱۶ تست) — از جمله اینکه هیچ مسیرِ `/api/admin`،
  `/api/plans`، `/api/shop`، … در سازمانی سوار نیست و هر `APIRouter`ِ `app/routers` در
  `ALL_ROUTERS` ثبت شده.

**دسکتاپ**
- `CUBITA_EDITION=enterprise` در بیلدِ main فریز می‌شود (`vite.config.ts`)؛ رندرر نسخه
  را از preload می‌گیرد (`platform.isEnterprise`). وب همیشه ابری است.
- `electron/serverAddress.ts` (نرمال‌سازیِ نشانی: بی‌پورت → ۸۴۲۰، بی‌طرح → http، بدونِ
  مسیر/اعتبارنامه؛ با تست) و `electron/serverSettings.ts` (خواندن/ذخیره/آزمایش در main).
- IPC: `server:config` (همزمان)، `server:probe` (می‌سنجد طرف **کوبیتا سازمانی** است، نه
  هر وب‌سروری)، `server:save` — اگر سندِ ارسال‌نشده در صف باشد تغییرِ سرور را رد می‌کند
  (وگرنه سند به دفترِ شرکتِ دیگری می‌رفت)، و نشست/کشِ سرورِ قبلی را پاک می‌کند.
- صفحه‌ها: `ServerConnectScreen` (اتصال)، `EnterpriseSetupScreen` (راه‌اندازی)،
  و حالتِ سازمانیِ `LoginScreen` (نامِ محصول، نشانیِ سرور + «تغییرِ سرور»، بدونِ
  ثبت‌نام و بازیابیِ ایمیلی). نوارِ آزمایشی/اشتراک در سازمانی نیست.
- آپدیترِ ابری در سازمانی سیم‌کشی نمی‌شود (M5).
- `npm run dist:enterprise` (`scripts/dist-enterprise.mjs`): appId
  `ir.ipnetcity.cubita.enterprise`، نامِ «Cubita Enterprise»، خروجی در
  `release-enterprise/` و **بدونِ publish** — `publish-update` فقط `release/` را می‌خواند،
  پس نصابِ سازمانی هرگز روی فیدِ مشتریانِ ابری نمی‌رود.

**راستی‌آزمایی:** بسته‌ی واقعی (`--dir`) کنارِ بک‌اندِ سازمانی روی ۸۴۲۰: جادوگر → آزمایش →
ذخیره → بارگذاریِ دوباره → صفحه‌ی ورود با نشانیِ سرور → درخواستِ ورود به سرور رسید. سه
صفحه در ۱۴۴۰ و ۳۹۰ بدونِ سرریز و بدونِ خطای کنسول.

### سه چیز که فقط با بسته‌ی واقعی پیدا شد

1. **پوشه‌ی داده مشترک بود.** نامِ برنامه در Electron از `name`ِ package.json («desktop»)
   می‌آید، نه از appId — پس نسخه‌ی سازمانی در `%APPDATA%\desktop`ِ نسخه‌ی ابری نوشت و
   ذخیره‌ی نشانی، نشست و کشِ مرجعِ نصبِ ابریِ همان رایانه را پاک کرد (صفِ ارسال دست
   نخورد). رفع: `app.setPath('userData', …\Cubita Enterprise)` بالای `main.ts`، پیش از
   هر خواندنِ userData. **قاعده برای M4:** نصاب هم باید همین پوشه را بشناسد.
2. **`Origin: file://`.** اپِ بسته‌بندی‌شده صفحه را از دیسک بار می‌کند و CORSِ پیش‌فرض آن
   را نمی‌پذیرفت — یعنی هر درخواستِ رندرر بی‌صدا می‌افتاد، در حالی که «آزمایشِ اتصال»
   (که در main است) سبز بود. رفع در `allowed_origins_list`.
3. **برای آزمونِ خودکارِ بسته، `ELECTRON_RUN_AS_NODE` را بردار.** اگر در محیط باشد، exe
   مثلِ Node اجرا می‌شود و Playwright «Process failed to launch» می‌دهد. خودِ exe
   دیباگرِ Playwright را نمی‌پذیرد؛ با `--remote-debugging-port` و `connectOverCDP` وصل شو.

## نتایجِ M2 (۱۴۰۵/۰۷/۰۲) — هسته‌ی مجوز

**بک‌اند — `app/licensing/`**
- `token.py`: قالبِ `CUB1.<payload>.<sig>`، امضای Ed25519 روی **همان بایت‌هایی که base64
  شده‌اند** (نه JSONِ دوباره‌ساخته) — پس کانونیک‌سازی نمی‌تواند امضای درست را بشکند. شکستنِ
  خط در کپی‌وپیست بی‌اثر است.
- `keys.py`: کلیدهای عمومیِ مورد اعتماد، **ثابتِ داخلِ کد** (نه env یا فایل، که مشتری
  می‌توانست کلیدِ خودش را بگذارد). فهرست است تا کلید بچرخد.
- `fingerprint.py`: `MachineGuid` + UUIDِ SMBIOS + سریالِ ولومِ سیستم، هشِ نمک‌خورده،
  تطبیقِ ۲ از ۳؛ شناسه‌های ساختگیِ SMBIOS (همه‌صفر و …) نادیده گرفته می‌شوند. روی ویندوزِ
  توسعه هر سه جزء خوانده شدند.
- `state.py`: وضعیت **محاسبه** می‌شود، نه ذخیره: `trial` → `trial_expired`، `active` →
  `grace` → `expired`، به‌اضافه‌ی `clock` (ساعت بیش از یک روز عقب‌تر از `last_seen_at`)،
  `mismatch` (رایانه‌ی دیگر) و `invalid` (کلیدِ باطل‌شده). کشِ ۵ دقیقه‌ای در پردازه.
  ردیفِ `enterprise_license` (مهاجرتِ **۰۱۸۴**، سراسری) و فایلِ `license.lic` یکدیگر را
  ترمیم می‌کنند و زودترین `installed_at` برنده است.
- `cli.py`: `keygen` (کلیدِ خصوصی هرگز چاپ نمی‌شود، رونویسی نمی‌شود)، `issue` از روی کدِ
  درخواست، `inspect`.
- گلوگاه‌ها: `entitlements._enforce_license` فقط **نوشتن** را می‌بندد؛ `require_feature` از
  `feat` و `allowed_modules` از `mods` می‌خوانند (ماژولِ پایه را هیچ مجوزی نمی‌گیرد). نصبِ
  مجوز `max_users` را از `seats` می‌گذارد.
- روترِ `/api/license` (فقط سازمانی): وضعیت برای همه‌ی اعضا، `request` و نصب فقط مالک.
  `MeOut.license` برای نوارِ بالا.
- تست‌ها: `tests/test_licensing.py` (۲۹) — دست‌کاریِ payload، کلیدِ ناشناس، هر حالت، پاک‌کردنِ
  ردیف یا فایل (آزمایشی ریست نمی‌شود)، و **«آزمایشیِ منقضی: `POST` ۴۰۲، `GET` و
  `/api/backup/export` ۲۰۰»**.

**دسکتاپ:** صفحه‌ی «مجوز نرم‌افزار» (گروهِ تنظیمات، فقط سازمانی؛ وضعیت + دو گامِ کدِ
درخواست ← کدِ مجوز)، `LicenseBanner` (فقط وقتی کاری از کاربر برمی‌آید)، و نامِ محصول در
نوارِ عنوان/منو (`PRODUCT_NAME`). ۱۴۴۰ و ۳۹۰ بی‌سرریز.

### کاری که پیش از اولین فروش مانده (یک‌بار، روی سرورِ ابری)

```bash
cd /opt/hesabdari/backend
venv/bin/python -m app.licensing.cli keygen --out /opt/hesabdari/secrets/license-signing.pem
```

دو مقدارِ چاپ‌شده (`kid` و کلیدِ عمومی) به `TRUSTED_PUBLIC_KEYS` در
`backend/app/licensing/keys.py` اضافه و کامیت شوند. تا آن روز هیچ مجوزی معتبر نیست و هر نصب
در آزمایشی می‌ماند. کلیدِ خصوصی **کنارِ پشتیبانِ دیتابیس نه** — همان قاعده‌ی `SECRETS_KEY`.
صدور تا M3: `python -m app.licensing.cli issue --key … --request CUBREQ1.… --seats N --days 365`.

## راستی‌آزمایی (برای هر مرحله)

- **بک‌اند:** `pytest -q` کاملاً سبز؛ تستِ ساختاریِ روترهای نسخه‌ی سازمانی؛ تستِ واحد
  برای امضا/انقضا/اثرانگشتِ ۲ از ۳؛ و «انقضای مجوز نوشتن را می‌بندد ولی `GET`ها و
  `/api/backup/export` باز می‌مانند».
- **دسکتاپ:** `tsc -b --force`، `lint`، `audit-pages.mjs`، `vitest` — همه سبز؛ جادوگرِ
  اولین اجرا روی ۱۴۴۰ و ۳۹۰.
- **ادمین:** `audit-admin.mjs` و `nav.test.ts`.
- **سرتاسری (دستی، سه VM):** نصبِ سرور روی VM1 و کلاینت روی VM2 → فعال‌سازی با کدِ واقعی
  → ورودِ دو کاربرِ همزمان → ثبتِ سند از کلاینت و دیده‌شدن روی سرور → قطعِ کاملِ اینترنت
  و تکرار → عبور از انقضا و بررسیِ اینکه نوشتن بسته و خواندن/خروجی باز است → انتقالِ
  مجوز به VM3 از پنل و باطل‌شدنِ VM1.

## ریسک‌ها (صادقانه)

1. **این DRM نیست، بازدارندگی است.** Electronِ بدونِ امضا + پایتونِ بسته‌بندی‌شده قابلِ
   پچ‌اند. فعال‌سازی این را می‌خرد: مشتریِ صادق نمی‌تواند بی‌صدا روی پنج شعبه نصب کند و
   تخطی یک کارِ عمدیِ ردگیری‌شده می‌شود. فراتر از اثرانگشتِ ۲از۳، ذخیره‌ی دوگانه و چکِ
   ساعت، خرجِ obfuscation نکنیم.
2. **پشتیبان‌گیری مسئولیتِ مشتری می‌شود و انجامش نمی‌دهد.** از‌دست‌رفتنِ دفترِ حسابداری
   روی ماشینِ خودشان برای ما وجودی است — پشتیبانِ شبانه + نمایشِ پررنگِ کهنگی، اختیاری نیست.
3. **بارِ پشتیبانی ~۱۰ برابرِ ابری** — دکمه‌ی زیپِ عیب‌یابی و اتصالِ از راه دور باید
   **پیش از** اولین فروش آماده باشد.
4. **SmartScreen/آنتی‌ویروس:** نصابِ بدونِ امضا + سرویسی که پورت باز می‌کند = گواهیِ
   امضای کد دیگر اختیاری نیست.
5. **تله‌ی نسخه‌ی Postgres:** روی ۱۶ پین شود و داخلِ یک کانالِ آپدیت هرگز عوض نشود.
6. **RLS با یک مستأجر هم لازم است** — اگر نصاب هر دو نقشِ دیتابیس را نسازد، جداسازی
   بی‌صدا از بین می‌رود.
7. **HTTPِ ساده روی LAN:** توکن و بسته‌ی آپدیت بازند. امضای آپدیت از همین حالا اجباری؛
   گواهیِ self-signed با پینینگ در مرحله‌ی بعد.
