# کارهایی که در آینده باید انجام شوند

این فایل فهرستِ کارهای **شناخته‌شده ولی هنوز انجام‌نشده** است — چیزهایی که موقعِ ساختِ
یک قابلیت به آن‌ها برخوردیم و آگاهانه به بعد موکول کردیم.

**این فایل جای «ایده» نیست.** هر ردیف باید سه چیز داشته باشد: چه چیزی کم است، چرا
اهمیت دارد، و از کجا باید شروع کرد. اگر ردیفی این سه را ندارد، هنوز ایده است نه کار.

**قاعده:** وقتی کاری انجام شد، ردیفش از این‌جا **حذف** می‌شود و شرحش به بخشِ ۱۰ِ
[PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md) می‌رود. دو فهرستِ موازی نگه نمی‌داریم.

---

## طرف حساب و نشانی

### نمایشِ نشانی روی نقشه

**چه کم است:** جدولِ `contact_addresses` ستون‌های `latitude` و `longitude` را دارد و
فرمِ نشانی هم می‌گیردشان، ولی هیچ‌جا نقشه‌ای نشان داده نمی‌شود. عدد ذخیره می‌شود و
همان‌جا می‌ماند.

**چرا مهم است:** مختصات را برای **ارسال کالا** ذخیره می‌کنیم. مأمورِ ارسال با دو عددِ
اعشاری کاری نمی‌تواند بکند؛ چیزی که به کارش می‌آید یک پیوند یا نقشه‌ی قابلِ لمس است.
تا وقتی نمایش نداشته باشیم، پرکردنِ این دو فیلد هیچ فایده‌ای برای کاربر ندارد و در عمل
خالی می‌ماند.

**از کجا شروع کنیم:**
- کوچک‌ترین قدمِ مفید: دکمه‌ی «نمایش روی نقشه» کنارِ هر نشانی که پیوندِ
  `https://www.google.com/maps?q=<lat>,<lng>` (یا نشان/بلد، که در ایران بهتر جواب
  می‌دهند) را باز کند. صفرِ وابستگی، صفرِ کلید API.
- قدمِ بعدی: نقشه‌ی توکار برای **انتخابِ** مختصات — کاربر روی نقشه نقطه بگذارد و
  `latitude`/`longitude` خودشان پر شوند. تایپِ دستیِ مختصات کاری است که هیچ‌کس نمی‌کند.
- محدودیتِ واقعی: کاشیِ نقشه از اینترنت می‌آید و کوبیتا نسخه‌ی دسکتاپِ آفلاین هم دارد.
  پس نقشه باید **افزوده** باشد نه شرط: بی‌اینترنت، فیلدهای عددی سرِ جایشان بمانند.
- اگر نقشه‌ی توکار خواستیم، سرویسِ ایرانی (نشان/بلد) بر گوگل ترجیح دارد — هم دسترسی و
  هم دقتِ نشانیِ ایران.

**پیوستِ همین کار:** فیلترِ «کد مسیر» (زون) روی نقشه هم معنا دارد — دیدنِ مشتری‌های یک
زون روی یک نقشه، کارِ مسیربندی را از حدس درمی‌آورد.

### زون یک موجودیت شود، نه یک رشته

`contact_addresses.route_code` امروز فیلدِ متنی است چون هنوز موجودیتِ «زون» نداریم.
هرچه بیشتر بماند، بیشتر «زون ۱» و «زون۱» و «zone 1» ثبت می‌شود و گروه‌بندی از دست
می‌رود. وقتی زون ساخته شد **این باید کلیدِ خارجی شود**، نه اینکه فهرستِ متنیِ موازی
کنارش بماند.

### حذفِ طرف حساب

`DELETE /api/contacts/{id}` وجود ندارد (۴۰۵ برمی‌گرداند). گاردهایش باید مثلِ حذفِ حساب
باشد: طرف‌حسابی که فاکتور، سند یا مانده‌ی افتتاحیه دارد حذف نمی‌شود — غیرفعال می‌شود.

### دو فرمِ طرف حساب، یکی شود

«شرکت ← طرف حساب جدید» فرمِ کامل است و «فروش ← طرف حساب» یک فرمِ سادهٔ قدیمی با همان
داده. این دقیقاً همان «دو نمای یک داده» است که قاعده‌ی صفحه‌ها منع می‌کند. تا وقتی هر
دو هستند، فرمِ ساده باید **ویرایشِ جزئی** بفرستد (که حالا می‌فرستد) وگرنه فیلدهای فرمِ
کامل را پاک می‌کند.

---

## فروش و اعتبار

### سندِ حسابداریِ کارمزدِ واسطه

**نیمه‌ی اولش انجام شد** (مهاجرت ۰۰۹۷): فاکتورِ فروش حالا `broker_id` و
`broker_commission` دارد، کارمزد در لحظه‌ی ثبت از `contacts.commission_rate` قفل
می‌شود، فرم و ویزارد واسطه را می‌گیرند و فهرستِ فاکتور نشانش می‌دهد.

**آنچه مانده:** کارمزد هنوز فقط یک عدد روی فاکتور است و به دفتر نمی‌رود. برای
شناسایی‌اش به‌عنوان **بدهی به طرفِ بیرونی** اول باید یک تصمیمِ حسابداری گرفته شود
که فنی نیست: کارمزد در چه لحظه‌ای شناسایی می‌شود — هنگامِ فروش، یا هنگامِ وصولِ
پول از مشتری؟ اگر فاکتور بعداً باطل شود، بدهیِ کارمزد هم برمی‌گردد یا نه؟ تا این
روشن نشود، سندِ خودکار ساختن یعنی حدس‌زدن در دفتر.

**تله‌ای که باید از آن پرهیز کرد:** این با پورسانتِ **فروشنده** یکی نیست.
`CommissionRule`/`CommissionRun` در [sales_ops](backend/app/models/sales_ops.py) برای
*کاربرِ* سامانه است و خروجی‌اش پرداخت به کارمند؛ واسطه طرفِ بیرونی است و خروجی‌اش
حساب پرداختنی. دو محاسبه‌ی جدا، نه یکی با دو ورودی.

### مقادیرِ پیش‌فرضِ نوعِ فروش هنوز اثری ندارند

`sale_types` حالا به فاکتور می‌چسبد، ولی دو فیلدش هنوز فقط ذخیره می‌شوند:
`due_days` (مهلتِ تسویه) و `default_tax_rate` (نرخِ پیش‌فرضِ مالیات). docstringِ
خودِ مدل می‌گوید «همان چیزی است که کاربر با انتخابِ نوعِ فروش انتظار دارد خودکار پر
شود» — پس امروز همان الگوی آشناست: تنظیمی که ذخیره می‌شود و هیچ اثری ندارد.

**چه لازم است:** انتخابِ نوعِ فروش در فرم، نرخِ مالیات را پیشنهاد بدهد (نه اجبار
کند — کاربر باید بتواند بازنویسی‌اش کند)، و `due_days` سررسیدِ فاکتور را بسازد.

**چرا با سیم‌کشیِ فروشنده انجام نشد:** آن کار فقط اتصالِ ستون‌های مرده بود و هیچ
عددی را عوض نمی‌کرد. پرکردنِ خودکارِ نرخِ مالیات **مبلغِ فاکتور را عوض می‌کند** و
تصمیمِ جدایی است.

## مرکز هزینه

### تقسیمِ یک هزینه بین چند مرکز

امروز هر سند به **یک** مرکز برچسب می‌خورد. قبضِ برقِ کلِ شرکت اما در واقعیت بین
تولید و فروش و اداری تقسیم می‌شود، و کاربر مجبور است یا همه‌اش را به یک مرکز
بزند (که گزارش را غلط می‌کند) یا سند را دستی چند تکه کند.

**چه لازم است:** یک جدولِ تخصیص — هر ردیفِ سند بتواند چند سهم داشته باشد، با
درصد یا مبلغ، و جمعِ سهم‌ها دقیقاً برابرِ مبلغِ ردیف بماند (همان الگوی
`_allocate_discount` که باقی‌مانده را به بزرگ‌ترین کسر می‌دهد تا جمع سرِ سوزن
بخواند).

**چرا هنوز ساخته نشده:** روی موتورِ سند اثر می‌گذارد و گزارش‌های موجود همه از
`journal_lines.cost_center_id` می‌خوانند — یعنی یا آن ستون باید بماند و جدولِ
تخصیص کنارش بنشیند (دو منبعِ حقیقت، که قاعده‌ی «دو نمای یک داده» منعش می‌کند)،
یا همه‌ی گزارش‌ها به منبعِ تازه منتقل شوند. تصمیمِ معماری است، نه کارِ افزودنی.

## ابزار و آزمون

### راستی‌آزماییِ مرورگری دستی است — خودکار نیست

`playwright` حالا در `package.json` اعلام شده و `npm run verify-pages` کار می‌کند، پس
دیگر «اسکریپت اجرا نمی‌شود» درست نیست. **ولی در CI اجرا نمی‌شود**، و این عمدی است:
`verify-pages.mjs` یک بک‌اندِ زنده، یک دیتابیسِ داده‌دار و اعتبارنامه‌ی واقعی می‌خواهد
و هیچ‌کدام در CI نیستند. (به همین دلیل `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1` روی قدمِ
نصب نشسته — بدونش هر اجرا ~۴۰۰ مگابایت مرورگر می‌گیرد که هیچ قدمی مصرفش نمی‌کند.)

یعنی تأییدِ مرورگری هنوز به این بند است که کسی یادش بماند دستی بزند. بستنِ کاملش یک
سرویسِ بک‌اند و یک دیتابیسِ بذرخورده در CI می‌خواهد — کارِ کوچکی نیست و هنوز انجام
نشده.

### پوششِ حسابرسی بیرون از اپِ وب — نیمه‌بسته

`app.audit` با `@event.listens_for` خودش را روی هر نشستِ SQLAlchemy می‌نشاند، ولی
تا امروز هیچ‌جا صریح import نمی‌شد: در اپِ وب اتفاقی از راهِ `app.deps` می‌آمد.
هر اسکریپت یا cronی که `SessionLocal` می‌گرفت و آن زنجیره را نداشت، روی مدلِ
حسابرسی‌شده می‌نوشت و ردش **بی‌صدا** گم می‌شد. یک بار واقعاً اتفاق افتاد — لغوِ
یک جلسه‌ی انبارگردانی روی تولید، بدونِ هیچ ردی در `audit_log`.

`app/database.py` حالا صریح import‌اش می‌کند و `tests/test_audit_registration.py`
با یک زیرپروسه قفلش کرده. **ولی نیمه‌ی دیگر باز است:** رکوردی که از بیرونِ یک
درخواستِ HTTP ساخته شود `actor` ندارد و «سیستم» ثبت می‌شود. برای کارِ سرپرستی
درست است، ولی اگر روزی کارِ پس‌زمینه‌ای از طرفِ یک کاربرِ مشخص بنویسد، باید
`bind_session_actor` را صدا بزند — و امروز هیچ گاردی یادآوری‌اش نمی‌کند.

### مالکیتِ جدول‌ها در محیطِ محلی

`cubita_app` / `cubita_migrate` طبقِ `scripts/setup_db_roles.sql` جدا شده‌اند، ولی
مهاجرت‌هایی که با نقشِ اشتباه اجرا شوند جدولِ تازه را به مالکِ دیگری می‌دهند و
مهاجرتِ بعدی روی همان جدول با «must be owner» می‌شکند. یک بررسیِ ساده در `alembic/env.py`
که هشدار بدهد «داری با نقشی که مالک نیست مهاجرت می‌زنی» جلوی تکرارش را می‌گیرد.

---

## حقوق و دستمزد — بقیه‌ی منوهای عملیات

گامِ ۱ (جدول‌های مرجع) و گامِ ۲ (قراردادِ واقعی) **ساخته شدند** — شرحشان در بخشِ ۱۰ِ
[PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md). چیزی که از فرمِ سپیدار مانده این‌هاست.

### آنچه هنوز مانده

**«فایل الکترونیکی مالیات بر درآمد حقوق»** — قالبِ رسمیِ سازمانِ مالیاتی، که با
`tax-list.csv`ِ ساخته‌شده فرق دارد و نیازمندِ مشخصاتِ دقیقِ همان سامانه است.

**«لیست‌های حقوق»** — گزارشِ چاپیِ لیستِ حقوقِ دوره (امروز فقط فیشِ تکی چاپ می‌شود).

**مانده‌ی مرخصیِ استقرار به `LeaveRecord` وصل نیست.** `leave_balance_days` ذخیره
می‌شود ولی مانده‌ی مرخصی همچنان از صفرِ همان سال حساب می‌شود — کارمندی که ده روز
مرخصیِ نرفته از شرکتِ قبلی آورده، از دیدِ نرم‌افزار صفر دارد. (مالیات و بیمه‌ی
استقرار حالا در محاسبه اثر دارند؛ فقط این یکی مانده.)

### فهرستِ جدا برای جدول‌های مرجع

«محل خدمت جدید»، «شغل جدید»، «عوامل حقوق و مزایا» و «گروه مالیاتی و شعب» امروز
فرم و فهرست را در یک صفحه دارند و در `OPS_LIST_MAP` با `'view'` علامت خورده‌اند.
وقتی داده‌شان زیاد شد، هرکدام دفترِ خودش را با فیلتر و صفحه‌بندی می‌خواهد — همان
الگوی «مرکز هزینه» ↔ «مراکز هزینه».
