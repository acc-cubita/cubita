/*
 * پیکربندیِ فروشگاه — این فایل هنگامِ «ساخت و دانلودِ سایت» در پنلِ کوبیتا با
 * مقادیرِ واقعیِ همین کسب‌وکار بازنویسی می‌شود (apiBase/slug/key بیک می‌شوند).
 *
 * برای توسعه‌ی محلی، پیش‌فرض‌ها به بک‌اندِ لوکال اشاره می‌کنند. slug و key را از
 * پنلِ «فروشگاهِ کوبیتا» بردار و این‌جا بگذار تا سایت به همان حساب وصل شود.
 *
 * نکته‌ی امنیتی: key یک «کلیدِ منتشرشدنی» است؛ دیده‌شدنش در مرورگر بی‌خطر است و
 * فقط عملیاتِ عمومی (خواندنِ کاتالوگ + ثبتِ سفارش + شروعِ پرداخت) را مجاز می‌کند.
 */
window.SHOP = {
  // آدرسِ API کوبیتا. build: https://acc.cubita.ir — توسعه: http://localhost:8000
  apiBase: 'http://localhost:8000',
  // شناسه‌ی فروشگاه (Shop-Slug) و کلیدِ اتصال — از پنلِ کوبیتا
  slug: 'REPLACE_WITH_SHOP_SLUG',
  key: 'REPLACE_WITH_PUBLISHABLE_KEY',
}
