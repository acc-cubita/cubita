import { WifiOff } from 'lucide-react'

/**
 * نوارِ «آفلاین» — فقط در Electron، فقط وقتی نشست از رویِ کشِ محلی بازیابی شده
 * و تلاشِ رفرشِ خاموش با سرور به قطعیِ شبکه خورده (نه بطلانِ نشست).
 *
 * پیام عمداً کاربر را نگران نمی‌کند: می‌گوید با چه داده‌ای کار می‌کند، نه اینکه
 * چیزی خراب است. با وصل‌شدنِ دوباره، رفرشِ بعدی خودش این نوار را برمی‌دارد.
 */
export function OfflineBanner() {
  return (
    <div className="offline-banner" role="status">
      <WifiOff size={15} />
      <span>آفلاین — با آخرین اطلاعاتِ ذخیره‌شده روی این دستگاه کار می‌کنید.</span>
    </div>
  )
}
