# AGENTS.md — کوبیتا

نرم‌افزارِ حسابداریِ فارسی/راست‌به‌چپ. FastAPI + SQLAlchemy 2.0 + Alembic +
PostgreSQL، و React 19 + TypeScript + Vite. چندمستأجری با Row-Level Security.

**قبل از هر کاری این سه را بخوان:**

| فایل | چه می‌گوید |
|---|---|
| [AGENT_PLAYBOOK.md](AGENT_PLAYBOOK.md) | **رویه‌ی کار — مهم‌ترین فایل.** از تحلیلِ شکاف تا PR و استقرار |
| [CLAUDE.md](CLAUDE.md) | پروژه چیست و قیدهای سختش |
| [OPEN_DECISIONS.md](OPEN_DECISIONS.md) | تصمیم‌هایی که **عمداً** باز مانده‌اند — دوباره تصمیم نگیر |

---

## قیدهای سخت

1. **هرگز مستقیم روی `master` کامیت یا push نکن.** همیشه شاخه‌ی `feat/…` یا `fix/…`.
2. **قبل از ساختِ مهاجرتِ Alembic اول به کاربر بگو و تأیید بگیر.**
3. **قبل از هر تغییری بگو می‌خواهی چه کنی.** برنامه بده، تأیید بگیر، بعد بزن.
4. **هرگز `deploy.sh demo` را اجرا نکن** — درِ تریال را می‌شکند.
5. **پیش از استقرار روی production به کاربر و آرش اطلاع بده.**
6. **رازها کامیت نمی‌شوند.** `.env` نادیده گرفته شده؛ الگو در `*.env.example`.
7. **دیتابیسِ محلیِ `hesabdari` دادهٔ واقعیِ کاربر است** — محیطِ آزمایشی نیست.
8. **هرگز دو اجرای `pytest` را همزمان نزن** — یک دیتابیسِ تستِ مشترک است.

---

## دستورها

```bash
# بک‌اند (از backend/) — یک اجرا در یک زمان
./venv/Scripts/python.exe -m pytest -q
./venv/Scripts/python.exe -m alembic heads       # باید یک سر بدهد

# فرانت‌اند (از desktop/) — هر سه باید تمیز باشند
npx tsc -b --force
node scripts/audit-pages.mjs                     # باید ۰ خطا بدهد
npx oxlint src
```

---

## چهار چیزی که خطا نمی‌دهند، فقط نتیجه‌ی غلط می‌دهند

- **RLS.** هر جدولِ مستأجرمحور `FORCE ROW LEVEL SECURITY` دارد که **حتی مالکِ
  جدول** را مشمول می‌کند. مهاجرت‌ها بدونِ زمینه‌ی مستأجر اجرا می‌شوند، پس هر
  `SELECT/UPDATE` در مهاجرت بی‌صدا صفر ردیف می‌بیند. راه‌حل: در مهاجرت ردیف
  ننویس؛ اگر مجبوری، `rls_disabled(conn, tables)` از `app/migration_utils.py`.
  **جدولِ تازه** بعد از `create_table` باید `ENABLE` + `FORCE` + `CREATE POLICY`
  بگیرد، وگرنه بینِ مستأجرها نشت می‌کند.
- **شماره‌ی سند.** `next_document_number(db, doc_type)` — نه SEQUENCE (روی
  rollback شماره می‌سوزاند و شکاف می‌افتد).
- **صفحه‌بندی.** `MAX_LIMIT = 200`؛ بزرگ‌تر → ۴۲۲ و صفحه بی‌صدا خالی می‌شود.
- **انتخابِ حساب.** `get_account(db, cc.ROLE)` — هرگز با کد یا نامِ حساب.

---

## تست‌ها مهاجرت را نمی‌سنجند

`conftest` از `Base.metadata.create_all` می‌سازد، **نه از مهاجرت‌ها**. یعنی یک
مهاجرتِ شکسته از هر ۲۱۰۰+ تست سبز رد می‌شود.

پس هر مهاجرت را روی یک اسکیمای یک‌بارمصرف **واقعاً** اجرا کن — روشِ کامل در
بخشِ ۶ پلی‌بوک. خلاصه: `CREATE SCHEMA probe` → `DATABASE_URL` با
`?options=-csearch_path%3Dprobe` → `upgrade head` → `downgrade` → `upgrade` →
سنجشِ `relforcerowsecurity` → `DROP SCHEMA probe CASCADE`. بعدش تأیید کن
`public.alembic_version` عوض نشده.

---

## اگر ایجنتِ دیگری همزمان کار می‌کند

این پوشه ممکن است **همزمان دستِ یک ایجنتِ دیگر** باشد. یک درختِ کاری است، نه دو
شاخه: هیچ merge‌ای محافظت نمی‌کند و آخرین نویسنده کلِ فایل را بی‌صدا می‌برد.

چون ابزارِ پیام‌رسانیِ مستقیم بین ایجنت‌های متفاوت نیست:

1. **اولِ کار `git status` بگیر.** اگر فایل‌هایی تغییر کرده که تو نزده‌ای، یعنی
   ایجنتِ دیگری فعال است — به کاربر بگو و مرزِ کار را از او بپرس.
2. **قبل از نوشتن روی هر فایل، دوباره بخوانش.** ممکن است از آخرین باری که دیدی
   عوض شده باشد.
3. **از کاربر بخواه فایل‌ها را تقسیم کند** و بگو دقیقاً کدام‌ها را برمی‌داری.
4. **`pytest` و `alembic` را بدونِ هماهنگی اجرا نکن.**
5. **زنجیره‌ی مهاجرت خطی و تک‌سر بماند.** اگر روی مهاجرتِ منتشرنشده‌ی دیگری
   می‌سازی، `down_revision` را به آن بزن — نه شاخه‌ی موازی.
6. **مهاجرتِ منتشرنشده را درجا ویرایش کن، نه با یک مهاجرتِ وصله.**

---

## نوشتن

- **همه‌ی متنِ کاربر فارسی است.** ارقام با `toLocaleString('fa-IR')`، تاریخ‌ها
  جلالی از `desktop/src/lib/jalali`.
- کامنت‌ها **«چرا»** را می‌گویند، نه «چه» — مخصوصاً کدام تله دور زده شد.
- خطا باید بگوید چه شد و کاربر حالا چه کند. «خطایی رخ داد» ممنوع.
- پیامِ کامیت یک خطِ فارسی است که *چه چیزی درست شد* را می‌گوید، بدونِ پیشوندِ
  `feat:`.

## صداقت در گزارش

تست شکست خورد؟ با خروجی بگو. گامی را رد کردی؟ بگو. رابط را در مرورگر ندیدی؟
**نگو «تأیید شد»** — بگو فقط با تست و typecheck سنجیده شد.
