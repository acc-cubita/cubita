# کجاهای کوبیتا توسعه داده نشده

**تاریخ بررسی:** ۱۴۰۵/۰۶/۲۳ · **`master` روی `dbce0fa`** · تولید روی مهاجرتِ `0148`

این فایل **نتیجه‌ی یک بررسیِ مستقل** است، نه فهرستِ کارهای تصمیم‌گرفته‌شده.
نسبتش با دو فایلِ همسایه:

| فایل | چیست |
|---|---|
| [`FUTURE_WORK.md`](FUTURE_WORK.md) | کارهایی که **تصمیمشان گرفته شده** و نوبتشان نرسیده — ۱۲ مورد |
| [`OPEN_DECISIONS.md`](OPEN_DECISIONS.md) | دوراهی‌هایی که **تصمیمشان با مالکِ محصول** است — ۳۷ مورد |
| **`GAPS.md`** (همین) | چیزهایی که بررسی پیدا کرد و **هیچ‌کدامِ آن دو ثبتشان نکرده‌اند** |

جایگزینِ آن دو نمی‌شود. هرجا هم‌پوشانی هست ارجاع می‌دهد، نه تکرار.

> **درجه‌ی اطمینان:** هر مورد یا **تأییدشده** است (شاهدش اجرا و دیده شده) یا
> **مشکوک**. سه ادعا در بررسی **رد** شدند و در [بخش ۹](#۹-آنچه-شکاف-نیست) با
> دلیلشان آمده‌اند.

---

## ۰. خلاصه

سه چیز از بقیه بزرگ‌ترند:

1. **`desktop/` با ۷۹٬۱۱۲ خط، صفر تستِ خودکار دارد.** اپِ موبایل که یک‌هفتمِ
   اندازه‌ی آن است jest دارد و در CI اجرا می‌شود.
2. **۱۸۱ از ۶۱۱ مسیرِ API (۳۰٪) هرگز در هیچ تستی از HTTP صدا زده نمی‌شوند.** هشت
   روتر **کاملاً** بی‌پوشش‌اند.
3. **ده‌ها موجودیت ساخته می‌شوند و هرگز ویرایش یا حذف نمی‌شوند.** کارمند پس از
   ثبت حتی غلطِ املاییِ نامش هم اصلاح‌شدنی نیست.

و یک الگو که از همه‌ی این‌ها فراگیرتر است و **امضای توسعه‌نیافتگیِ این پروژه**
است: ستونی که فرم می‌گیردش، validator اعتبارسنجی‌اش می‌کند، قیدِ دیتابیس
محافظتش می‌کند — و هیچ‌کس نمی‌خوانَدش. بیست خوشه پیدا شد ([بخش ۳](#۳-الف-فیلد-و-تنظیماتِ-مرده)).

---

## ۱. اندازه‌ی امروز

| بخش | فایل | خط | فایلِ تست |
|---|---|---|---|
| `backend/` | ۶۰۵ | ۱۳۳٬۹۴۹ | ۱۷۹ |
| `desktop/` (وب + Electron) | ۲۲۲ | ۷۹٬۱۱۲ | **۰** |
| `mobile/` | ۸۹ | ۱۰٬۴۵۶ | ۱۲ |
| `website/` | ۳۲ | ۲٬۴۹۳ | ۰ |
| `storefront/` + `storefront-demo/` | ۴ | ۴۴۷ | ۰ |

بک‌اند: **۱۶۵** کلاسِ مدل · **۶۱۱** مسیر · **۹۸** سرویس · **۱۴۷** مهاجرت
دسکتاپ: **۵۹** فایلِ صفحه · **۱۴۰** کلیدِ صفحه

---

## ۲. زمینه‌ای که درجه‌بندی را عوض می‌کند

دادهٔ روی تولید هنوز کم است:

```
کارمند ۲ · حکم ۵ · سفارشِ تولید ۱ · تعدیلِ انبار ۱۱ · تراکنشِ بانکی ۲
دارایی ثابت ۲ · نرخِ ارز ۰ · فاکتور فروش ۱۶ · سند حسابداری ۶۲
```

پس **هیچ‌کدام از این شکاف‌ها امروز درد ندارند.** با اولین رشدِ واقعی خواهند
داشت. اولویتِ درست «پیش از رشد» است، نه «فوری» — و این تفاوت مهم است، چون
فهرستِ زیر بلند است و اگر همه‌اش «فوری» خوانده شود هیچ‌کدام انجام نمی‌شود.

---

## ۳. الف) فیلد و تنظیماتِ مرده

بزرگ‌ترین دسته. در همه‌ی این موارد فرم، validator، قیدِ دیتابیس و تایپِ
TypeScript **کامل**‌اند و فقط **مصرف‌کننده** غایب است.

### ۳٫۱ آن‌هایی که پول را غلط می‌کنند

| # | فیلد | شاهد | پیامد | اطمینان |
|---|---|---|---|---|
| ۱ | `Contact.discount_rate` | [`models/inventory.py:231`](backend/app/models/inventory.py#L231) — **صفر** ارجاع در `services/`+`routers/`، و **صفر** در `salesInvoiceDraft.ts` | کاربر تخفیفِ اختصاصیِ مشتری را می‌گذارد؛ فاکتور با نرخِ کامل صادر می‌شود | تأییدشده |
| ۲ | `PayrollDeploymentInfo.prior_service_days` و `.leave_balance_days` | [`models/payroll.py:1194-1195`](backend/app/models/payroll.py#L1194) — `calc_severance` و `calc_leave` در [`services/benefits.py`](backend/app/services/benefits.py) نمی‌خوانندشان | کارمندِ ده‌ساله‌ای که مهاجرت داده شده، **سنواتش کمتر از واقع** حساب می‌شود — بی‌صدا | تأییدشده |
| ۳ | `SalesInvoice.broker_commission` | [`models/invoices.py:123`](backend/app/models/invoices.py#L123) — فقط دو ارجاع، هر دو **نوشتن** | کارمزد محاسبه و نمایش داده می‌شود، هیچ بدهی‌ای به واسطه در دفتر ساخته نمی‌شود | تأییدشده |
| ۴ | ~~`FixedAsset.method`~~ | — | **این ادعا رد شد.** پایین‌تر | ‏**رد‌شده** |

> ### ادعای رد‌شده — `FixedAsset.method` (۱۴۰۵/۰۶/۲۴)
>
> نسخه‌ی اول نوشته بود «کاربر روشِ استهلاک را انتخاب می‌کند و هر سه گزینه یک عدد
> می‌دهند». **غلط بود.**
> [`DEPRECIATION_METHODS = ("straight_line",)`](backend/app/models/assets.py#L14)
> — فقط **یک** روش مجاز است، با قیدِ `CHECK` روی جدول، و رابط هیچ انتخابگری ندارد.
> `monthly_depreciation` دقیقاً همان خطِ مستقیم را پیاده می‌کند. ستون فیلدِ دروغین
> نیست؛ enumِ تک‌مقداری است برای روزی که روشِ دوم اضافه شود.
>
> این دومین ادعای این گزارش است که در آزمون رد شد (اولی نامِ کارمند بود، بخشِ ۴).
> هر دو در [`ACCOUNTING_KB_AUDIT.md`](ACCOUNTING_KB_AUDIT.md) §۶ هم ثبت‌اند.

> **مورد ۱ نمونه‌ی کاملِ این الگوست:** `discount_rate` و `commission_rate` در
> **همان** `model_validator` با هم اعتبارسنجی می‌شوند
> ([`schemas/inventory.py:222`](backend/app/schemas/inventory.py#L222)). یکی هشت
> جا خوانده می‌شود، دیگری هیچ‌جا. در بازبینی، فایل کاملاً سیم‌کشی‌شده به‌نظر می‌رسد.

### ۳٫۲ اسکلتِ گزارشی که ساخته نشد

این‌ها مستر-دیتایی هستند که برای یک **خروجی یا گزارشِ ساخته‌نشده** جمع می‌شوند:

- **بلوکِ ثبتِ قانونیِ شعبه‌ی بیمه/مالیات** — ۶ فیلد (`registration_code`،
  `workplace_name`، `workplace_address`، `employer_name`، `agreement_number`،
  `insurance_exempt_count`) در [`models/payroll.py:962-985`](backend/app/models/payroll.py#L962).
  هویتِ قانونیِ کارگاه برای فایلِ دیسکِت بیمه که وجود ندارد.
- **عکسِ شعبه روی فیش** — `insurance_branch_code/name` و `tax_branch_code/name`
  در [`models/payroll.py:362-368`](backend/app/models/payroll.py#L362). **تنها
  فیلدهایی که حتی نوشته هم نمی‌شوند** — نه اسکیما، نه سرویس، نه روتر. فیش‌های
  تاریخی قابلِ گروه‌بندی به تفکیکِ شعبه نیستند.
- **دموگرافیِ طرف حساب** — ۱۲ فیلد در `models/inventory.py` (`gender`،
  `marital_status`، `children_count`، `dependents_count`، `education_level`،
  `is_shareholder`، `share_percent`، …). تیزترینش `dependents_count` است که
  **ورودیِ معافیتِ مالیاتیِ حقوق** در ایران است و جمع‌آوری و دور ریخته می‌شود.
- `Contact.tax_ministry_class` — طبقه‌بندیِ وزارت اقتصاد که گزارشِ فصلی قرار بود
  بر اساسش تفکیک شود.
- **`SalesInvoice.statement_date`** — [`models/invoices.py:54`](backend/app/models/invoices.py#L54).
  کاربر «تاریخ صورتحساب» را جدا از تاریخ فاکتور وارد می‌کند **دقیقاً برای اینکه
  گزارشِ فصلی به آن کلید بخورد**؛ هر گزارشی `invoice_date` را می‌خواند.
- `Item.iran_code` · `barcode2` · `unit_weight` · `unit_volume` — بسته‌ی مؤدیان
  `iran_code` را نمی‌برد، و تخصیصِ کرایه در
  [`services/warehouse_receipts.py:495`](backend/app/services/warehouse_receipts.py#L495)
  بر مبنای ارزش/تعداد است نه وزن.
- `ContactAddress.route_code` و **۵ همزادش** (`route_title`، `route_title2`،
  `region_code`، `region_title`، `region_title2`) — آن پنج‌تا در هیچ اسکیما و
  هیچ رابطی نیستند؛ ستونِ خالصِ مرده.

### ۳٫۳ خواهرِ سالم کنارِ خواهرِ مرده

ارزان‌ترین برای اصلاح و سخت‌ترین برای دیدن در بازبینی:

| خوانده می‌شود | نمی‌شود |
|---|---|
| `supplementary_employee_factor_id` ([`services/payroll.py:711`](backend/app/services/payroll.py#L711)) | `supplementary_employer_factor_id` ([`models/payroll.py:512`](backend/app/models/payroll.py#L512)) |
| `tier_basis` (`services/crm.py:282`) | `tier_discount_auto` و `birthday_gift_points` — فقط مرورگر اعمالشان می‌کند |
| `commission_rate` (۸ ارجاع) | `discount_rate` (۰) |

پنلِ تنظیماتِ حقوق سه انتخابگرِ «کدام عامل این نقش را دارد» نشان می‌دهد؛ دو تا
روی معافیتِ مالیاتی اثر دارند و سومی هیچ.

### ۳٫۴ ذخیره‌شدنی، ولی دست‌نیافتنی از API

- **`PayrollTaxGroup.is_active`** — در `Out` هست، در `In` نیست، و روتر اصلاً
  `PATCH`/`DELETE` ندارد. گروهِ مالیاتی پس از ساخت **برای همیشه** غیرقابلِ
  ویرایش و همیشه فعال است.
- **`PriceList.effective_from`** — و این بدترینِ این دسته است، چون **فیلدِ
  تعیین‌کننده‌ی حلِ قیمت** است: [`services/pricing.py:154-171`](backend/app/services/pricing.py#L154)
  آخرین `effective_from` را می‌گیرد و هر نامزدِ دیگری را دور می‌ریزد. از مسیرِ
  `POST /price-lists` قابلِ تنظیم **نیست** (همیشه مهرِ امروز می‌خورد) ولی از
  `POST /price-announcements` هست. **یک جدول، دو مسیرِ نوشتن، یکی‌شان بی‌صدا
  ناتوان.**

> **همچنین در `FUTURE_WORK` ثبت شده‌اند** (این‌جا فقط ارجاع، نه تکرار):
> `sale_types.due_days` و `default_tax_rate` · `contact_addresses.latitude/longitude`
> · `payroll_factors.is_extraordinary` · `banking.cheque_print_format`
> («یک فیلد هست، موتور نیست»).

---

## ۴. ب) ساخته می‌شود، هرگز ویرایش یا حذف نمی‌شود

دامِ داده: هر خطای ورودی برای همیشه می‌ماند.

| موجودیت | مسیرهای موجود | آنچه کاربر نمی‌تواند |
|---|---|---|
| **کارمند** | `GET` + `POST` فقط ([`routers/payroll.py:99,104`](backend/app/routers/payroll.py#L99)) | نه `hire_date`، نه `termination_date`، نه غیرفعال‌کردنِ کسی که رفته. `is_active` روی مدل هست و **هیچ مسیری نمی‌نویسدش** — یعنی کارمندی که رفته، نمی‌رود. (نام و کدِ ملی **استثنا**ند؛ پایین‌تر) |
| **سفارشِ تولید** | `GET` + `POST` ([`routers/manufacturing.py:105,110`](backend/app/routers/manufacturing.py#L105)) | سند می‌زند و انبار حرکت می‌دهد، و **ابطال ندارد**. یک عددِ اشتباه هم موجودی و هم دفتر را دائمی خراب می‌کند. (BOM سی‌آر‌یو‌دیِ کامل دارد؛ سفارشی که از آن ساخته می‌شود هیچ.) |
| ~~**تعدیلِ انبار**~~ | **بسته** (PR #89، مهاجرتِ `0158`) | `POST /api/stock-adjustments/{id}/void` اضافه شد. در همین کار نقصِ دومی هم پیدا شد: حرکتِ انبارِ تعدیل هیچ‌وقت `source_id` نمی‌گرفت — تنها سندِ انباری که این کار را نمی‌کرد — و ۱۱ ردیفِ تولید در مهاجرت به سندشان گره خوردند. |
| حکمِ حقوق | `GET` + `POST` | حقوقِ پایه یا `effective_from`ِ اشتباه اصلاح‌شدنی نیست — و حکمِ جایگزین کمکی نمی‌کند وقتی تاریخِ **اولی** غلط باشد |
| تراکنشِ بانکی | `GET` + `POST` | در همان روتر، حسابِ بانکی و دسته‌چک CRUDِ کامل دارند؛ این یکی جا افتاده |
| نرخِ ارز | `GET` + `POST` | نرخِ غلط بی‌صدا وارد تسعیرِ ارز می‌شود. ارز خودش `POST`+`DELETE` دارد ولی `PATCH` ندارد |
| عملیاتِ چک | `GET` + `POST` | رویدادِ اشتباهِ چک برگشت‌پذیر نیست؛ تاریخچه‌ی وضعیتِ چک دائمی غلط می‌ماند |
| تسویه‌ی پایانِ کار · مرخصی · تراکنشِ باشگاه · باندل · قاعده‌ی پورسانت | `GET` + `POST` | باندل و قاعده‌ی پورسانت `is_active` دارند که هیچ مسیری نمی‌نویسدش — یعنی ویرایش **در نظر بوده** |

**و عملیاتِ یک‌طرفه‌ی بی‌بازگشت:** `POST /api/depreciation/run` ·
`/api/petty-cash/charge` و `/expense` — هیچ‌کدام مسیرِ برگشت ندارند.

> ### تصحیح — نامِ کارمند (۱۴۰۵/۰۶/۲۴)
>
> نسخه‌ی اول این‌جا نوشته بود «نه غلطِ املاییِ نام، نه کدِ ملیِ اشتباه». **غلط
> بود.** کارمند از روی طرف‌حساب ساخته می‌شود
> ([`payroll_contracts.py:195`](backend/app/services/payroll_contracts.py#L195))،
> `PATCH /api/contacts/{id}` وجود دارد، و
> [`employee_identities`](backend/app/services/payroll.py#L1145) عمداً **طرف‌حساب
> را ترجیح می‌دهد** — کپیِ روی `employees` فقط پشتیبانِ کارمندانِ میراثیِ
> بی‌طرف‌حساب است. پس فایلِ بانک، اظهارنامه و لیستِ بیمه **نامِ درست را می‌برند**،
> و `test_employee_identity.py` همین را تضمین می‌کند.
>
> **آنچه واقعاً از کپی می‌خواند، چهار جای نمایشی است** و نه خروجیِ قانونی:
> شرحِ سندِ سنوات و بازخریدِ مرخصی
> ([`benefits.py:313`](backend/app/services/benefits.py#L313)، [`:347`](backend/app/services/benefits.py#L347))،
> و `employee_name` در دو فهرست
> ([`routers/payroll.py:483`](backend/app/routers/payroll.py#L483)،
> [`payroll_contracts.py:356`](backend/app/services/payroll_contracts.py#L356)).
> یعنی نام را اصلاح می‌کنی و صفحه‌های حقوق و دستمزد هنوز قدیمی نشانش می‌دهند —
> ناهماهنگیِ واقعی، ولی کوچک و بی‌اثرِ مالی.
>
> ### و شکافِ بزرگ‌تری که همین‌جا پیدا شد
>
> `Employee.is_active` **تنها یک خواننده دارد**، و آن خواننده صدورِ فیشِ حقوقی
> است: [`payroll.py:849`](backend/app/services/payroll.py#L849) با
> `filter(Employee.is_active.is_(True))` تصمیم می‌گیرد چه کسی فیش بگیرد. هیچ
> مسیری این ستون را نمی‌نویسد.
>
> و تنها گاردِ دوم هم نمی‌گیردش:
> [`get_current_contract`](backend/app/services/payroll.py#L271) فقط
> `effective_from <= as_of` را می‌بیند و **نه `service_end_date` را نه
> `valid_until` را** — در حالی که docstringِ خودِ `SalaryContract` می‌گوید
> «`service_end_date` … از این‌جا به بعد کارکرد اصلاً محاسبه نمی‌شود».
>
> نتیجه: **کارمندی که رفته، هر دوره فیشِ کامل می‌گیرد** — با سندِ حسابداری،
> بیمه و مالیاتش. این دیگر نمایشی نیست.

---

## ۵. ج) قابلیتی که در ندارد

بک‌اند ساخته، رابط صدایش نمی‌زند.

**۱) تعویضِ کسب‌وکار — بزرگ‌ترین موردِ این دسته.**
[`routers/auth.py:214`](backend/app/routers/auth.py#L214) (`GET /api/auth/tenants`)
و [`:250`](backend/app/routers/auth.py#L250) (`POST /api/auth/switch-tenant`) هر
دو کامل و با دقت پیاده شده‌اند — `switch_tenant` حتی عضویت را دوباره تأیید
می‌کند تا جعلِ توکن ممکن نباشد. **صفر مصرف‌کننده در دسکتاپ، موبایل و وب‌سایت**
(تأییدِ مستقل).

docstring خودش بازار را نام می‌برد: «برای حسابدارِ مستقلی که دفترِ چند کسب‌وکار
را می‌برد لازم است — سهمِ بزرگی از بازار همین‌هاست.» امروز آن حسابدار باید خارج
و دوباره وارد شود، و هیچ‌جا نمی‌بیند عضوِ کدام کسب‌وکارهاست.

**بقیه‌ی مرده‌ها:**
- `GET /api/stock-batches/expiring` — **تنها کوئریِ اقدام‌سازِ بچ**. کلِ CRUDِ
  بچ به رابط وصل است جز همین یکی که می‌گوید کدام سری نزدیکِ انقضاست.
- `GET /api/client-errors` — موبایل گزارشِ خطا می‌فرستد (`POST` مصرف دارد) و
  **هیچ رابطی نمی‌تواند بخواندشان**. خطِ لوله‌ی فقط‌نوشتنی.
- `GET /api/accounting/overview` — قدیمی‌ترین سندِ موقت و تاریخِ آخرین بستن، یعنی
  دقیقاً دو عددی که docstring می‌گوید حسابدار قبل از هر کاری می‌خواهد.
- `GET /api/reports/cost-center` — و `/api/cost-centers/report` هم هست؛ **دو
  پیاده‌سازی، رابط به یکی می‌رسد**.
- **صفِ آفلاینِ چک** — `window.cubita.queueCheck` در Electron کامل پیاده شده و
  هیچ فراخوانی‌ای در renderer ندارد، پس صف همیشه خالی است. (ثبت‌شده در
  `OPEN_DECISIONS` ۲۸.)

**دو نمای هم‌پوشانِ یک طرف‌حساب — تصمیمِ ثبت‌شده، نه فراموشی.**
«صورت حساب طرف مقابل» (`contactstatement`) و «مرور جامع طرف حساب»
(`contactoverview`) هر دو گردشِ یک طرف‌حساب را نشان می‌دهند. اولی ضعیف‌تر است:
**کلِ دفتر را می‌کشد و در مرورگر فیلتر می‌کند** و راهِ ورود به سند ندارد.

قاعده‌ی پروژه («هرگز دو نمای یک داده نساز») می‌گوید باید یکی شوند. سرِ کارِ
فاکتور فروش (PR #101) این را دیدیم و **عمداً دست‌نخورده گذاشتیم** — چون ادغامشان
کارِ آن PR نبود و نصفه‌کاره‌کردنش بدتر از دو تا داشتنشان است. این‌جا ثبت می‌شود
تا در بازبینیِ بعدیِ ماژولِ فروش تصمیم گرفته شود، نه اینکه گم شود.

**کشِ محلیِ الکترون مستأجر‌محور نیست.** جدول‌های `accounts_cache`،
`warehouses_cache`، `items_cache`، `bank_accounts_cache` و **هر چهار صفِ خروجی**
هیچ ستونِ مستأجر ندارند.

پیامدش سرِ ساختِ «تعویضِ کسب‌وکار» پیدا شد: سندی که آفلاین برای کسب‌وکارِ الف در
صف نشسته، پس از تعویض **به ب ثبت می‌شود**. فعلاً با گارد بسته شده — تعویض تا
خالی‌نشدنِ صف ممکن نیست، و کشِ مرجع موقعِ تعویض پاک می‌شود.

راهِ اصولی‌اش افزودنِ `tenant_id` به این جدول‌هاست تا کار در چند کسب‌وکار
به‌صورتِ آفلاین هم ممکن شود.

**تنها صفحه‌ی ماکت:** «پیمانکاری»
([`ContractingPage.tsx:28`](desktop/src/pages/ContractingPage.tsx#L28)) — در
ناوبری هست، بک‌اند ندارد. به نفعش: ماکتِ صادقی است که چهار قابلیتِ برنامه‌ریزی‌شده
را نام می‌برد. **۱۰۶ کلیدِ ناوبریِ دیگر همه به کامپوننتِ واقعی می‌رسند.**

---

## ۶. د) پوششِ تست

**۱) دسکتاپ صفر.** نه اسکریپتِ `test`، نه ابزارِ تست در `devDependencies`، نه
یک فایلِ `*.test.*`. CI فقط `tsc` + `oxlint` + ممیزِ ساختاری + `build` می‌زند.

طعنه‌اش این است که موبایل jest دارد، و کامنتِ خودِ CI دلیلش را می‌گوید: «اپ روی
کافه‌بازار زنده است؛ تا پیش از این هیچ چیزی جز اجرای دستی نمی‌گفت چیزی شکسته یا
نه.» **همان استدلال برای دسکتاپ قوی‌تر است.** `api.ts` به‌تنهایی ۸٬۹۱۲ خط است.

**۲) ۱۸۱ از ۶۱۱ مسیر هرگز از HTTP صدا زده نمی‌شوند.** تست‌ها عمدتاً سطحِ سرویس‌اند
(۷۲ از ۱۷۹ فایل اصلاً کلاینتِ HTTP ندارند). برای آن ۱۸۱ مسیر، **گاردِ مجوز،
تجزیه‌ی پارامتر و شکلِ پاسخ هرگز آزموده نمی‌شوند**.

هشت روترِ **کاملاً** بی‌پوشش: `fiscal_year` · `stock_taking` · `assets` ·
`receipts` · `budgeting` · `calendar` · `cashbox` · `currencies` — از جمله
`/carry-forward`، `/close`، `/depreciation/run` و `/stock-counts/post`.

**۳) ~~مغایرت‌گیریِ بانکی هیچ پوششی ندارد~~ — بسته شد.**

این بند **یک ادعای غلط داشت**: `import_statement_lines` از ۲۳ شهریور نُه تست
داشت (`test_bank_statement_import.py`)، یعنی همان کامیتی که این فایل را نوشت.
چهار تابعِ دیگر واقعاً صفر بودند و حالا ۲۸ تست دارند
(`test_bank_reconciliation.py`): تطبیقِ خودکار، تطبیقِ دستی، بازکردنِ تطبیق و
خلاصه‌ی مغایرت.

سه رفتارِ کلیدی با شکستنِ عمدی اثبات شدند: گاردِ «یک تراکنش دوبار استفاده
نشود»، آزادکردنِ تراکنشِ قبلی در تطبیقِ دوباره، و مرزِ ±۳ روز.

آنچه باز می‌ماند: روترِ `api/bank-statement` هنوز اسموک‌تستِ HTTP ندارد (موردِ
۳ فهرستِ اولویت).

**۴) `factor_participation.py`** — پنج تابعی که **پایه‌ی بیمه و مالیاتِ حقوق** را
تعیین می‌کنند، بدونِ تست.

**۵) CRM بدترین نسبت:** ۲۷ مسیر / ۴۱۳ خطِ سرویس / ۱۳ تست — روی امتیازِ باشگاه که
یک بدهی است.

---

## ۷. ه‍) گزارش‌های غایب

گزارش‌های موجود قوی‌اند — تراز با انتخابِ ۲/۴/۶/۸ ستونی و سطحِ کل/معین/تفصیلی،
جریان وجوه نقد به **روشِ مستقیم** و خودتأییدشونده، و گزارش‌های کنترلی (مغایرتِ
ماهیت، تفصیلیِ جامانده) که غیرعادی کامل‌اند. آنچه نیست:

1. **صورت تغییرات در حقوق صاحبان سهام** — گرپِ پنج نامِ مختلف در کلِ
   `backend/app/` صفر نتیجه داد. **یکی از چهار صورتِ الزامیِ استانداردهای
   حسابداری ایران**؛ سه تای دیگر هست.
2. **تحلیلِ سمتِ خرید** — هفت اندپوینتِ `sales-review` و یک سرویسِ ۳۵ کیلوبایتی
   برای فروش؛ برای خرید هیچ.
3. **دفترِ دارایی ثابت** — `GET /api/depreciation` ردیفِ خام می‌دهد؛ نه دفترِ
   دارایی، نه جدولِ استهلاکِ هر دارایی، نه گردشِ استهلاکِ انباشته.
4. **مغایرت‌گیریِ بانکی به‌عنوان گزارش** — تابعش هست، در روترِ گزارش‌ها نیست.
5. **لیستِ حقوقِ دوره** — ثبت‌شده در `FUTURE_WORK`.

**و یک هشدار که باید دقیق گفته شود:** توزیعِ سطل‌های سالخوردگیِ مطالبات یک
**تقریبِ نمایشی** است. خودِ کد در
[`services/reports.py:1399`](backend/app/services/reports.py#L1399) صریح می‌گوید:
«این FIFO یک تقریبِ نمایشی است، نه تخصیصِ واقعی.»

**ولی این به معنی نبودِ تخصیص نیست** — همان docstring می‌گوید تخصیصِ *ثبت‌شده*
جای دیگری است: «تسویه حساب طرف مقابل» و گزارشِ اقلامِ باز
([`services/open_items.py`](backend/app/services/open_items.py)). جمعِ هر دو یکی
است؛ فقط توزیعشان می‌تواند فرق کند. پس شکافِ واقعی این است که **دو نما از یک
حقیقت هست و گزارشِ سالخوردگی نمای تقریبی‌تر را نشان می‌دهد** — گره‌خورده به
دوراهیِ باز ۲۰.

---

## ۸. و) کیفیتِ آنچه ساخته شده

**۱) ۱۵ از ۲۱ مسیرِ `DELETE` گاردِ ارجاع ندارند.**
الگوی درست در [`routers/accounts.py:693`](backend/app/routers/accounts.py#L693)
هست — گاردِ صریح + `SAVEPOINT` + `IntegrityError` ⇒ ۴۰۹ با پیامِ فارسی — و تکرار
نشده. نمونه‌ی تأییدشده:
[`DELETE /boms/{id}`](backend/app/routers/manufacturing.py#L92) فقط null-check
دارد، و `ProductionOrder.bom_id` تهی‌ناپذیر است ⇒ حذفِ BOMِ ارجاع‌شده یک **۵۰۰ِ
مبهم** می‌دهد نه پیامِ قابلِ فهم.

**۲) هیچ تستی نمی‌گوید «هر مسیر باید گارد داشته باشد».**
۶۱۱ مسیر، و تنها تستِ مسیرگردِ موجود محدود به `/api/audit` است. امروز مسیرها
گارد دارند (سنجیده شد)، ولی مسیرِ تازه‌ای که بی‌گارد اضافه شود **بی‌صدا از CI رد
می‌شود**.

**۳) `assert_within_fiscal_year` بی سال مالی بی‌صدا رد می‌شود.**
[`services/fiscal_year.py`](backend/app/services/fiscal_year.py) — اگر هیچ سال
مالی‌ای تعریف نشده باشد `return` می‌کند (سازگاری با حساب‌های قدیمی). چون
`FiscalPeriodCloseIn.closing_date` هیچ سقفی ندارد، **کسب‌وکارِ بدونِ سال مالی**
می‌تواند دوره را با تاریخی در آینده‌ی دور ببندد و چون بستن برگشت ندارد، ثبتِ سند
تا آن تاریخ مسدود می‌ماند. (روی تولید ۳ سال مالی تعریف شده، پس امروز در معرض نیست.)

**۴) `desktop/src/api.ts` ۸٬۹۱۲ خط در یک فایل**، بی‌تست.

**۵) صفر `TODO` در کلِ مخزن، ۴۹۲ بار «هنوز/فعلاً/بعداً».**
این پروژه کارِ ناتمام را با `TODO` علامت نمی‌زند — نثرِ فارسی می‌نویسد، و اغلب
با دلیل. پیامدِ عملی‌اش این است که **هر ابزارِ خودکارِ سنجشِ بدهیِ فنی روی این
مخزن «تمیز» گزارش می‌دهد و اشتباه می‌کند.** نمونه‌ها:
[`models/banking.py:310`](backend/app/models/banking.py#L310) «موتورِ چاپِ چک
هنوز وجود ندارد» · [`models/tenant.py:60`](backend/app/models/tenant.py#L60)
«جدول subscriptions وجود ندارد».

---

## ۹. آنچه شکاف نیست

تا دوباره شمرده نشود:

- **غیرفعال‌سازی به‌جای حذف** برای عضو و انبار — عمدی و مستند («سندِ بی‌صاحب»).
- **`/api/shop/*` بدونِ گاردِ کاربر** — عمداً عمومی، با هدرِ `X-Shop-Key`،
  و docstringش توضیح می‌دهد.
- **نبودِ جدولِ اشتراک** — محصول رایگان شده؛ کدِ پلن «مشکوک» است نه «ناقص».
- **موبایل زیرمجموعه‌ی عمدیِ دسکتاپ** (۴۲ صفحه در برابر ۱۴۰ کلید).
- **قالبِ کوچکِ فروشگاه** (۴۰۱ خط) — قالبی است که per-tenant بیک می‌شود.
- **نبودِ `GET /api/sales-invoices/{id}`** — لازم نیست و عمداً ساخته نشد:
  فهرست از قبل `selectinload(lines)` می‌کند و `SalesInvoiceOut.lines` را
  می‌دهد، پس نمایشِ جزئیاتِ فاکتور فراخوانِ دومی نمی‌خواهد (PR #101).

### سه ادعایی که در بررسی رد شدند

| ادعا | چرا غلط بود |
|---|---|
| «یک غلطِ تایپی در بستنِ دوره کلِ دفتر را قفل می‌کند» | زنجیره `close_period → issue_pnl_close → assert_period_open → assert_within_fiscal_year` است و تولید ۳ سال مالی دارد، پس تاریخِ پرت رد می‌شود. شکافِ واقعی باریک‌تر است و در ۸٫۳ آمده |
| «`admin_accounts.py` (۹ مسیر) هیچ گاردی ندارد» | `require_super_admin` دارد و docstringش هم می‌گوید |
| «لیستِ سیاه گاردِ فقط‌مرورگری است و دور زده می‌شود» | **عمداً هشدار است نه گارد.** [`BlacklistBanner.tsx`](desktop/src/components/BlacklistBanner.tsx) دلیلش را می‌دهد: اگر ثبت را می‌بست، کاربر تیک را برمی‌داشت و نشانه برای همیشه از بین می‌رفت. همان «گزارش، نه گارد» |

نکته‌ی **واقعیِ** باقی‌مانده‌ی موردِ سوم: آن هشدار فقط در دسکتاپ رندر می‌شود، پس
کاربرِ موبایل هیچ نشانه‌ای از لیستِ سیاه نمی‌بیند.

---

## ۱۰. اگر بخواهیم شروع کنیم

به ترتیبِ «ارزش تقسیم بر هزینه»:

| # | کار | چرا اول |
|---|---|---|
| ۱ | ~~**vitest + تستِ `api.ts` در دسکتاپ**~~ | **زیرساخت آماده شد** — `npm test` در CI، و ۴۶ تست روی سه نقطه‌ی پرهزینه: پروتکلِ کارتخوان، قاعده‌ی تخفیف، صفحه‌بندیِ `api.ts`. گسترشش به بقیه‌ی رابط باز است |
| ۲ | ~~**تستِ مغایرت‌گیریِ بانکی**~~ | **انجام شد** — ۲۸ تست روی چهار تابع. (پنجمی از قبل پوشش داشت؛ ادعای این فهرست دقیق نبود) |
| ۳ | **اسموک‌تستِ HTTP برای هشت روترِ بی‌پوشش** | ~۵۲ مسیر با چند صد خط تست |
| ۴ | ~~**ویرایش و غیرفعال‌سازیِ کارمند**~~ | **از پیش انجام شده بود** — کامیتِ `92a53c9` (۲۳ شهریور) `PATCH /api/employees/{id}` را با `hire_date`، `termination_date` و `is_active` اضافه کرد. این فهرست به‌روز نشده بود |
| ۵ | ~~**ابطالِ تعدیلِ انبار**~~ | **انجام شد** — PR #89 |
| ۶ | ~~**دَرِ رابط برای تعویضِ کسب‌وکار**~~ | **انجام شد** — انتخابگر در نوارِ بالا، با گاردِ صفِ آفلاین. کشِ محلی هنوز مستأجر‌محور نیست؛ ثبت‌شده در بخشِ ۵ |
| ۷ | ~~`Contact.discount_rate`~~ | **انجام شد** — روی تخفیفِ فاکتور نشست، با قاعده‌ی `max(نرخِ طرف‌حساب، درصدِ سطحِ باشگاه)` |

موارد ۴ تا ۷ هرکدام یک PRِ کوچک‌اند. ۱ تا ۳ زیرساختِ آزمون‌اند و بعد از آن‌ها
بقیه‌ی این فهرست خیلی امن‌تر انجام می‌شود.

---

## پانویس — دستورهای اندازه‌گیری

```bash
# اندازه‌ی هر بخش
find backend -name '*.py' -not -path '*/venv/*' -not -path '*/__pycache__/*' | xargs cat | wc -l

# شمارشِ مسیرها
grep -rho '@router\.\(get\|post\|patch\|put\|delete\)' backend/app/routers/*.py | wc -l

# فیلدِ مرده: آیا این ستون جایی خوانده می‌شود؟
grep -rn '\bdiscount_rate\b' backend/app/services/ backend/app/routers/ | wc -l

# TODO در برابر نثرِ فارسی
grep -rn 'TODO\|FIXME\|XXX\|HACK' backend/app/ desktop/src/ | wc -l
grep -rn 'هنوز\|فعلاً\|بعداً' backend/app/ desktop/src/ | wc -l

# DELETEهای بی‌گارد
grep -A15 '@router\.delete' backend/app/routers/*.py | grep -c 'IntegrityError'
```
