# پلنِ توسعه — «فروشگاهِ بومیِ کوبیتا» (Cubita Storefront)

> **این سند چیست؟** نقشه‌ی راهِ ساختِ سازنده‌ی فروشگاهِ اینترنتیِ یکپارچه با حسابداری.
> مرجعِ چندفازی؛ با هر فازِ تمام‌شده به‌روزرسانی شود. مکمل [PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md).
>
> **قاعده‌ی دوپلتفرمی:** پنلِ مدیریت روی **هر دو** پلتفرم (دسکتاپ + وب) و **موبایل ≤۷۶۰px**.

---

## خلاصه‌ی اجرایی

به‌جای «اتصال به سایتِ بیرونی» (مدلِ فعلی، شکننده: نگاشتِ دستی + sync)، فروشگاه یک
**سطحِ بومیِ خودِ برنامه** می‌شود:

- **کاتالوگ = همان `items`** (بدونِ نگاشت، بدونِ دیتابیسِ دوم).
- **سفارشِ سایت → مستقیم از `post_sales_invoice`** به حسابداری (موجودی یک منبعِ حقیقت).
- **قالب‌ها = پوسته‌ی متغیر** روی یک اسکیمای واحد (مثل تمِ شاپیفای).
- **پنلِ مدیریت داخلِ برنامه‌ی حسابداری**.
- **مارکت‌پلیسِ قالب:** گالریِ جذاب با پیش‌نمایشِ زنده + قیمت + خرید + فعال‌سازیِ خودکار.

مدلِ بیرونیِ فعلی (`storefront_integration.py`) **حذف نمی‌شود** (ipnetcity زنده است)؛
ماژول دو زبانه می‌شود: **«فروشگاهِ کوبیتا (بساز)»** جدید + **«اتصال به سایتِ موجود (وصل کن)»** فعلی.

---

## تصمیم‌های قطعی‌شده (۱۴۰۵/۰۵/۱۳)

| موضوع | تصمیم |
|---|---|
| **میزبانی** | **هاستِ جدای خودِ مستأجر** — سایتِ ساخته‌شده روی هاستِ خودِ کسب‌وکار آپلود می‌شود، نه روی زیرساختِ کوبیتا. کوبیتا = سازنده + مغزِ داده + پنلِ مدیریت. |
| **پایه‌ی قالب** | **بازاستفاده از فرانتِ فروشگاهِ ipnetcity** (`D:\Sample`) — SPAی وانیلا-جی‌اسِ کامل و زنده؛ پایه‌ی قالبِ «عمومی». Astro کنار رفت (چیزی که کار می‌کند از نو ساخته نمی‌شود). |
| **رندر/سئو** | **SPA + پیش‌رندرِ زمانِ بیلد** — منطقِ `seo_render.py` از «رندرِ لحظه‌ایِ ربات» به **مولدِ اسنپ‌شاتِ استاتیک** تبدیل می‌شود؛ HTMLهای سئویی (محصول/دسته + JSON-LD + sitemap) داخلِ ZIP بیک می‌شوند. موجودی/قیمت/سبد/تسویه سمتِ کلاینت از API. |
| **تشخیصِ مستأجر** | با **کلیدِ publishable** (`publishable_key` در سایتِ build‌شده)، نه Host header — چون هاست را ما کنترل نمی‌کنیم. |
| **پرداختِ خریدارِ نهایی** | با **زرین‌پالِ خودِ مستأجر** (نه کوبیتا). اطلاعاتِ درگاهِ مستأجر موقعِ راه‌اندازی گرفته می‌شود. **در MVP**. |
| **درآمدِ فروشِ افزونه** | مالِ **ادمینِ پلتفرم (کوبیتا)**، با زرین‌پالِ کوبیتا — همان `billing` موجود. |
| **اولین قالب** | **عمومی/چندمنظوره** (پایه‌ای که بقیه از آن مشتق شوند). |
| **تحویلِ سایت به هاست** | **دانلودِ ZIP + آموزشِ اتصال (خودآپلود)**. دموها روی VPS کوبیتا؛ پس از خرید، سایتِ استاتیک build و به‌صورت ZIP + راهنما به مستأجر داده می‌شود تا خودش روی هاستش بگذارد. کوبیتا هرگز کلیدِ هاستِ مستأجر را نمی‌گیرد. |
| **کلیدِ API سایت** | **کلیدِ منتشرشدنی (publishable)** داخلِ ZIP — چون در مرورگرِ خریدار دیده می‌شود. فقط: خواندنِ کاتالوگ + ثبتِ سفارشِ pending + شروعِ پرداخت. هرگز کاست/مدیریت. (الگوی publishable-keyِ استرایپ.) |

### دو جریانِ پول (جدا نگه داشته شود — منبعِ اصلیِ اشتباه)
1. **مستأجر → کوبیتا** (خریدِ افزونه‌ی فروشگاه): زرین‌پالِ **کوبیتا**، مسیرِ `billing` + `plans.prices`.
2. **خریدارِ نهایی → مستأجر** (خریدِ کالا): زرین‌پالِ **خودِ مستأجر** (`payment_gateways`). پولِ فروش به حسابِ کسب‌وکار می‌رود؛ کوبیتا واسطه‌ی پول نیست.

---

## معماری

```
[VPS کوبیتا]                              [هاستِ جدای مستأجر]
 دموی زنده‌ی همه‌ی قالب‌ها  ── گالری ──►   سایتِ استاتیکِ دانلودشده (Astro SSG)
 بک‌اندِ FastAPI                            + کلیدِ publishable + برندِ مستأجر
      ▲                                          │
      │  API عمومیِ /api/shop/*  ◄── fetch (CORS) ┘
      │  (احراز با کلیدِ publishable؛ سریالایزرِ باریکِ کاست‌پنهان)
      │
   items / stock_ledger        storefront_orders → پرداختِ زرین‌پالِ مستأجر
   (منبعِ حقیقتِ کاتالوگ)         → post_sales_invoice(انبار ONLINE)
```

- **دموها روی VPS کوبیتا**؛ سایتِ واقعیِ هر مستأجر روی **هاستِ خودش** (دانلودِ ZIP).
- سایتِ استاتیک با **کلیدِ publishable** به API وصل می‌شود؛ مستأجر از **کلید** تشخیص داده می‌شود، نه دامنه.
- **عوض‌کردنِ قالب/برند = build و دانلودِ دوباره**؛ ولی موجودی/قیمت/کاتالوگ **زنده** از API می‌آید، پس تغییرِ روزمره‌ی کالا نیازی به دانلودِ دوباره ندارد. فقط تغییرِ ساختار/قالب/برند دانلودِ دوباره می‌خواهد.
- **API عمومیِ باریک** = ستونِ امنیتی: هیچ کاست/سود/دیتای مستأجرِ دیگر بیرون نمی‌رود؛ CORS فقط از originِ مجازِ مستأجر.

---

## بازاستفاده از داراییِ موجود — فروشگاهِ ipnetcity (`D:\Sample`)

کدِ کاملِ فروشگاهِ ipnetcity در دسترس است و **پایه‌ی کارمان می‌شود** (کدِ خودِ کاربر است؛
`D:\Sample` یک کپیِ محلی برای ساختِ محصولِ تازه است).

**برمی‌داریم (شتاب‌دهنده):**
- **فرانتِ SPA** (CSS/صفحات/سبد/تسویه/تم روشن‌تیره/PWA/RTL) → پایه‌ی قالبِ «عمومی».
- **`seo_render.py`** (اسنپ‌شاتِ ربات + JSON-LD کامل + `sitemap.xml` + `llms.txt`) → به **پیش‌رندرِ زمانِ بیلد** تبدیل می‌شود.
- **کدِ پرداختِ زرین‌پال/زیبال/آی‌دی‌پی** (`/api/payments/request` + callbackها) + مدلِ چنددرگاهی → مبنای درگاهِ **خودِ مستأجر**.
- **شِماهای Pydantic** (Product/Order/OrderItem/Coupon/Review) → مرجعِ سریالایزرِ `/api/shop/*`.

**برنمی‌داریم:**
- **بک‌اندِ SQLite تک‌مستأجریِ آن** — دیتابیسِ دوم است؛ کاتالوگ/موجودی/سفارش باید همان بک‌اندِ حسابداری بماند.
- **پنلِ ادمینِ داخلِ سایت** (`js/pages/admin/*`) — مدیریت در برنامه‌ی حسابداریِ ماست.
- **ابزارهای دوربینی** (kit-builder، محاسبه‌گرِ هارد) — مختصِ ipnetcity؛ برای قالبِ عمومی حذف/اختیاری.

**گام‌های تطبیقِ فرانت:** `api.js` → به `/api/shop/*` با کلیدِ publishable · حذفِ ادمین و ابزارهای دوربینی ·
برند/رنگ/دسته پارامتری از `theme_config` (به‌جای هاردکدِ «آی‌پی‌نت») · `seo_render` → مولدِ اسنپ‌شاتِ استاتیک در خطِ بیلد.

---

## مدلِ داده (مهاجرت‌های تازه؛ head فعلی `0053` → `0054`+)

- **`storefront_themes`** *(سراسری)* — کاتالوگِ قالب: `key`, `name`, `description`, `category`, `preview_image`, `demo_url`, `is_active`.
- **`storefronts`** *(مستأجرمحور، RLS)* — یک ردیف به‌ازای کسب‌وکار: `theme_id`, `theme_config` (JSONB: رنگ/لوگو/بنر/هیرو/محصولِ ویژه)، `seo` (عنوان/توضیح)، `contact_block`، **`publishable_key`** (کلیدِ داخلِ سایت؛ قابلِ ابطال/بازتولید)، **`allowed_origin`** (دامنه‌ی هاستِ مستأجر برای CORS)، `last_built_at`.
- **`item_storefront`** *(مستأجرمحور، RLS)* — نمایشِ کالا: `item_id`, `is_listed`, `images` (JSONB)، `long_description`, `slug`, `sort`, `badge`. **بدونِ ردیف = کالا روی سایت نیست** (نمایش کاملاً اختیاری، `items` لاغر می‌ماند).
- **`storefront_categories`** *(مستأجرمحور)* — منوی دسته‌بندی (چون `items.category` رشته‌ی تخت است).
- **`storefront_orders` + `storefront_order_lines`** *(مستأجرمحور)* — انبارکِ سفارش: خریدار، اقلام، مبالغ، `payment_status`, `fulfillment_status`, `sales_invoice_id` (پس از پرداخت). **تا پرداخت تأیید نشود سندِ حسابداری نمی‌خورد.**
- **`storefront_customers`** *(مستأجرمحور)* — خریداران با ورودِ **OTPِ پیامکی** (زیرساختِ ملی‌پیامک آماده)؛ اختیاری به `contact` لینک.
- **`payment_gateways`** *(مستأجرمحور، راز مثل مؤدیان)* — درگاهِ **خودِ مستأجر** برای پولِ خریداران (زرین‌پال/زیبال/آی‌دی‌پی — مدلِ چنددرگاهیِ ipnetcity برداشته می‌شود). `merchant_id` راز؛ فقط `has_merchant` برمی‌گردد.

---

## چرخه‌ی سفارش (حلقه‌ی طلایی)

```
مرور کاتالوگ → سبد → تسویه (اطلاعات + زرین‌پالِ مستأجر) → storefront_orders (pending)
  → callbackِ موفقِ زرین‌پال → post_sales_invoice(انبار ONLINE, مشتری=contact)
  → کسرِ خودکارِ موجودی (یک‌بار) → فیدِ سفارش در برنامه
  → «تأیید/ارسال» → کسبِ امتیازِ باشگاه (موجود) + کارت‌حسابِ مشتری (موجود)
```

موجودی **یک‌بار** کم می‌شود (منبعِ حقیقت = حسابداری)؛ مشکلِ «دوبار کم‌شدن»ِ مدلِ بیرونی اصلاً نیست.

---

## فازبندی (هر فاز = مهاجرتِ DB + سرویس + UI؛ **نه** سواپِ فقط‌وب)

### فاز ۰ — پیش‌سازها (بک‌اند)
- [x] مهاجرت `0054`: `storefronts`, `item_storefront`, `storefront_categories`, `storefront_orders(+lines)`, `storefront_customers`, `payment_gateways` + RLS روی هر ۷. *(مدلِ `storefront_themes` سراسری در فازِ گالری اضافه می‌شود.)*
- [x] انبارِ `ONLINE` — از قبل در seed هست (`other_warehouse`/`ONLINE`).
- [x] **API عمومیِ کاتالوگ** (`/api/shop/{info,catalog,categories,product/{slug}}`) با سریالایزرِ باریکِ کاست‌پنهان (`ShopProductOut`).
- [x] احرازِ سطحِ عمومی با **کلیدِ publishable** (بوت‌استرَپ: مستأجر از `tenants.slug`، کلید داخلِ زمینه با `compare_digest`؛ منتشرنشده=۴۰۳).
- [x] **CORS برای `/api/shop/*`** — میدل‌ورِ `shop_public_cors` در main.py با `Allow-Origin: *` (سطحِ عمومیِ بدونِ کوکی؛ احراز فقط با کلیدِ publishable، پس origin مرزِ امنیتی نیست). preflightِ OPTIONS هم پاسخ داده می‌شود. `allowed_origin` برای هدفِ ریدایرکتِ callback می‌ماند.
- [ ] **کِشیدنِ ماژول‌های ipnetcity:** `seo_render.py` (تطبیق با items/تنظیماتِ ما) + کدِ پرداختِ زرین‌پال/زیبال/آی‌دی‌پی. *(اینکرمنتِ بعدی)*
- [x] تستِ ایزولاسیون: کاست لو نمی‌رود، لیست‌نشده پنهان، کلیدِ اشتباه/منتشرنشده رد، و **نشتِ کاتالوگِ بین دو مستأجرِ واقعی** (۸ تستِ تازه + پارامتریِ خودکارِ نشت روی ۷ جدول). کلِ سوییت: **۹۵۱ سبز**.

### فاز ۱ — MVP: حلقه‌ی کامل + یک قالبِ عمومیِ عالی
> **بک‌اندِ حلقه آماده است** (اینکرمنت‌های ۲–۳): API مدیریتِ `/api/storefront/*` (تنظیمات/کلید/انتشار/
> فهرستِ کالا/درگاه) + `POST /api/shop/orders` (قیمتِ سمتِ سرور) + `confirm-payment`→`post_sales_invoice`
> (کسرِ خودکارِ موجودی، idempotent) + فیدِ سفارش. مانده: **UIِ پنل، فرانتِ سایت، خطِ بیلد، و HTTPِ زرین‌پال**.
- [x] **قالبِ سایتِ عمومی** در `storefront/` — ⚠️ **بدونِ نامِ ipnetcity** (کاربر سایتِ دوربینِ زنده با همین نام دارد). به‌جای تطبیقِ verbatimِ کدِ دوربینی، یک قالبِ **عمومیِ تمیزِ بی‌برند** ساخته شد که زبانِ بصری را بازاستفاده می‌کند: وانیلا-جی‌اسِ استاتیک، `api.js`→`/api/shop/*` با کلیدِ publishable، برند/رنگ/نام پارامتری از `/api/shop/info`. صفحات: خانه/کاتالوگ+دسته+جست‌وجو، محصول، سبد (localStorage)، تسویه→`POST /orders`. Playwright روی خانه/محصول/سبد/تسویه (دسکتاپ+موبایل) تأیید شد.
- [ ] **دموی زنده روی VPS کوبیتا** (دیتای نمونه) برای نمایش در گالری.
- [x] **خطِ لوله‌ی build+بسته‌بندی:** `GET /api/storefront/site-bundle` قالبِ `storefront/` را می‌خواند، `config.js` را با apiBase/slug/key بیک می‌کند، و **ZIP** (+ فایلِ «آموزش-اتصال.txt») می‌سازد؛ دکمه‌ی **«ساخت و دانلودِ سایت»** در پنل + راهنمای گام‌به‌گام. `last_built_at` به‌روز می‌شود. تست: unzip → config با slug/key واقعی، بدونِ placeholder.
- [x] **پنلِ مدیریت در برنامه** — بک‌اندِ `/api/storefront/*` + **UIِ تبِ «فروشگاهِ کوبیتا»** (`NativeStorefrontPanel`): وضعیت/کلید/انتشار، ظاهر/سئو، فهرستِ کالا، درگاه، فیدِ سفارش. کنارِ «اتصال به سایتِ موجود» دوتب شد. دسکتاپ/وب/موبایلِ کارتی با Playwright تأیید شد.
- [x] تسویه با **زرین‌پالِ مستأجر** → callback → `post_sales_invoice` — `POST /orders/{id}/pay` (request v4، مبلغِ ریالی بر پایه‌ی واحدِ پولِ فروشگاه) + `GET /pay/callback?shop=<slug>` (verify → `confirm_payment`). سایت با `has_online_payment` تصمیم می‌گیرد به درگاه ببرد یا سفارشِ دستی؛ صفحه‌ی `order-result`. مسیرِ دستی/کارت‌به‌کارت هم‌چنان کار می‌کند. **زیبال/آی‌دی‌پی فعلاً موکول** (ساختار آماده).
- [x] **فیدِ سفارش** در برنامه (تأیید/ارسال) — بک‌اند: `GET /orders`, `confirm-payment`, `PUT .../fulfillment`.
- [ ] موبایلِ فروشگاه + موبایلِ پنل.

### فاز ۲ — مارکت‌پلیسِ قالب + بلوغِ خرید
- [ ] **گالریِ قالب**: کارت با پیش‌نمایشِ زنده (iframe دموی روی VPS) + قیمت + «انتخاب و خرید».
- [ ] خریدِ افزونه از **زرین‌پالِ کوبیتا** (`billing`/`plans.prices`) → فعال‌سازیِ خودکارِ پنل + دسترسیِ دانلود.
- [ ] ۲–۳ قالبِ دیگر (مشتق از عمومی).
- [ ] ورودِ OTPِ خریدار + لینک به باشگاهِ مشتریان.

### فاز ۳ — بلوغ
- [ ] راهنمای اتصالِ دامنه + SSLِ رایگان (Let's Encrypt) روی **هاستِ خودِ مستأجر**.
- [ ] **نسخه‌بندی و به‌روزرسانیِ سایت:** وقتی قالب را بهبود دادیم، پنل «نسخه‌ی جدید در دسترس است — دانلودِ دوباره» را نشان دهد.
- [ ] CMS سبک (درباره/تماس/بلاگ)، کدِ تخفیف، نقشه‌ی سایت/سئو.
- [ ] آنالیتیکسِ فروشگاه در داشبورد.

---

## امنیت (بحرانی)
- API عمومی سریالایزرِ **کاملاً جدا**؛ یک لغزش = لوِ بهای تمام‌شده یا مستأجرِ دیگر. تستِ ایزولاسیون واجب.
- `payment_gateways.merchant_id` و `admin_password`ها راز (الگوی مؤدیان: فقط `has_*` برمی‌گردد).
- سطحِ عمومی فقط `is_listed` و `stock>0` را می‌بیند؛ نه کالای غیرفعال/خدمت/کاستِ داخلی.

## ریسک‌ها
- **تطبیقِ فرانتِ ipnetcity:** برندزدایی/پارامتری‌کردن، حذفِ تمیزِ ادمین و ابزارهای دوربینی، و تبدیلِ `seo_render` به پیش‌رندرِ بیلد کارِ دقیق می‌خواهد (ولی از صفر ساختن نیست).
- **نسخه‌ی قدیمیِ دستِ مستأجر:** چون سایت روی هاستِ اوست، بهبودهای قالب خودکار نمی‌رسند → سازوکارِ «نسخه‌ی جدید، دانلودِ دوباره» لازم است.
- **پشتیبانیِ اتصال:** هاستِ مستأجر را کنترل نمی‌کنیم؛ آموزشِ اتصال باید بسیار روشن باشد (cPanel/دایرکت‌ادمین/FTP).
- **کلیدِ publishable لو نمی‌رود بلکه دیده می‌شود:** باید به‌گونه‌ای طراحی شود که دیده‌شدنش بی‌خطر باشد (rate-limit، فقط عملیاتِ عمومی، قابلِ ابطال).
- **حجم:** چند مهاجرت + سرویس + اپِ جدید؛ چند‌هفته‌ای، نه یک‌روزه.

---

## ضمیمه: قراردادِ API عمومیِ کاتالوگ (پیش‌نویس)
- `GET /api/shop/catalog` — کالاهای `is_listed`، فقط فیلدهای مجاز: `slug, name, sales_price, images, category, badge, in_stock`.
- `GET /api/shop/product/{slug}` — جزئیات (بدونِ کاست).
- `GET /api/shop/categories` — منوی دسته.
- `POST /api/shop/orders` — ثبتِ سفارشِ pending (بدونِ سند).
- `POST /api/shop/orders/{id}/pay` — شروعِ پرداختِ زرین‌پالِ مستأجر.
- `GET /api/shop/pay/callback` — تأیید → `post_sales_invoice`.
