# دوراهی‌ها و تناقض‌ها

جایی که جواب **مالِ ما نیست** — یا قاعده‌های خودِ پروژه دو طرف را می‌کِشند، یا
تصمیم، تصمیمِ کسب‌وکار است نه مهندس.

**این فایل [FUTURE_WORK.md](FUTURE_WORK.md) نیست.** آن‌جا کارِ *شناخته‌شده‌ی
موکول‌شده* است: می‌دانیم چه باید بشود و از کجا شروع کنیم، فقط هنوز نشده. این‌جا
کاری است که **نمی‌دانیم باید بشود یا نه** — و تا کسی تصمیم نگیرد، ساختنش حدس است.

**هر ردیف باید چهار چیز داشته باشد:** صورتِ دوراهی · بهای هر طرف · چه چیزی
تصمیم را عوض می‌کند · وضعیتِ امروز. ردیفی که این چهار را ندارد هنوز یک دوراهی
نیست، یک نگرانی است.

**قاعده:** وقتی تصمیمی گرفته شد، ردیفش از این‌جا **حذف** می‌شود و دلیلش به بخشِ ۱۰
[PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md) می‌رود. دو فهرستِ موازی نگه نمی‌داریم.

---

# الف) دوراهی‌هایی که تصمیمش با شماست


## ۱. اعتبارسنجی پیش از دائم‌کردنِ سند

**صورتِ مسئله:** سپیدار پیش از دائم‌کردن سند را می‌سنجد و مشکل‌دارها را رد می‌کند.
در کوبیتا این **تله است، نه گارد**.

**بهای ساختنش:** سندِ کوبیتا **ویرایشِ ردیف ندارد** — فقط `POST`،
`PATCH /sub-number` و `POST /void`. پس سندی که سرِ دائم‌شدن رد شود، نه می‌شود
درستش کرد نه دائمش؛ برای همیشه گیر می‌کند. تنها راهش ابطال و صدورِ دوباره است،
که برای سندی که ایرادِ واقعی *ندارد* مجازات است.

**خطرِ اصلی تفصیلی است:** سطحِ اجبارش تنظیمِ متغیرِ هر کسب‌وکار است. شرکتی که از
«شناور» به «اجباری» برود، همه‌ی اسنادِ موقتِ قدیمی‌اش یک‌شبه غیرقابلِ دائم‌شدن
می‌شوند — قاعده‌ای که در زمانِ ثبت وجود نداشت، بازگشتی اعمال شود.

**بهای نساختنش:** توازن از قبل در `JournalEntryIn` تضمین شده و سندِ خودکار با
ساختش متوازن است، پس چیزِ خطرناکی رد نمی‌شود. آنچه از دست می‌رود فقط «گزارشِ
سلامتِ اسنادِ موقت» است.

**چه چیزی تصمیم را عوض می‌کند:** اگر روزی سند **ویرایشِ ردیف** بگیرد، این دوراهی
از بین می‌رود و اعتبارسنجی بی‌ضرر می‌شود. تا آن روز، هر شکلی از این کار باید
**گزارش** باشد نه مانع.

**امروز:** ساخته نشده. ← ثبت در PROJECT_OVERVIEW §۱۰ (۱۴۰۵/۰۶/۱۶)

---

## ۲. سیاستِ «فاکتور مستقیماً دائم شود»

**صورتِ مسئله:** امروز همه‌ی اسناد — دستی و خودکار — **موقت** متولد می‌شوند.
سپیدار اجازه می‌دهد سندِ ماژول‌ها مستقیم دائم شود.

**بهای هر طرف:** موقت‌بودن یعنی هر سندِ فاکتور یک قدمِ دستیِ اضافه دارد؛ دائم‌شدنِ
خودکار یعنی سندی که کسی ندیده وارد سابقه‌ی رسمی می‌شود و فقط با ابطال برمی‌گردد.

**چه چیزی تصمیم را عوض می‌کند:** این خالصاً **رویه‌ی حسابداریِ شرکت** است. یک
کسب‌وکارِ تک‌نفره احتمالاً دائمِ خودکار می‌خواهد؛ جایی که حسابدار و مدیرِ مالی جدا
هستند، قطعاً نه.

**هشدارِ طراحی:** اگر ساخته شود باید **تنظیمِ مستأجر** باشد با پیش‌فرضِ «مثلِ
امروز». پرچمی که پیش‌فرضش چیزی را عوض کند، همان چیزی است که پروژه بارها ردش کرده.

**امروز:** ساخته نشده — «تنظیمِ مستأجرِ تازه پیش از اینکه کسی خواسته باشدش».

---

## ۳. مجوزِ جدا برای دائم‌کردن

**صورتِ مسئله:** امروز دائم‌کردن `require_permission("accounting", "update")`
است — همان مجوزِ ویرایش. یعنی هر کسی که سند می‌زند می‌تواند دائمش هم بکند.

**بهای ساختنش:** ماتریسِ مجوز، نقش‌های پیش‌فرض و صفحه‌ی دسترسی‌ها را لمس می‌کند.

**چه چیزی تصمیم را عوض می‌کند:** آیا در سازمانِ هدف، «حسابدار» و «مدیرِ مالی» دو
نفرند؟ اگر بله، این جداسازی معنا دارد؛ اگر نه، فقط یک ردیفِ اضافه در جدولِ مجوزهاست.

**امروز:** ساخته نشده — تصمیمِ سازمانی است، نه استنتاج از یک ویدیو.

---

## ۴. سود و زیانِ تسعیر: خالص یا ناخالص؟

**صورتِ مسئله:** سندِ تسعیر امروز **یک** ردیفِ خالص می‌زند: اگر مجموعِ اختلاف‌ها
مثبت شد «سود تسعیر ارز»، وگرنه «زیان تسعیر ارز».

**بهای وضعِ فعلی:** شرکتی که هم‌زمان روی دلار سود و روی یورو زیان داشته، در
صورتِ سود و زیان فقط تفاضل را می‌بیند. مبلغِ ناخالصِ هرکدام گم می‌شود.

**بهای تغییر:** دو ردیف به‌جای یک ردیف در هر سندِ تسعیر، و ناسازگاری با اسنادِ
تسعیرِ قبلی که خالص ثبت شده‌اند.

**چه چیزی تصمیم را عوض می‌کند:** رویه‌ی گزارشگریِ شرکت و نظرِ حسابرس. تفکیکِ
ناخالص با تفکیکِ تازه‌ی بُعدها (تفصیلی/مرکز) **بیشتر دیده می‌شود** تا قبل، چون
حالا ردیف‌های هم‌حساب با علامت‌های مخالف بیشترند.

**امروز:** خالص می‌ماند. این تنها ردیفِ این فایل است که در هیچ مرورِ ویدیویی
خواسته نشده بود — موقعِ کارِ تسعیر به آن برخوردم و بی‌تصمیم ماند.

---

## ۵. انتخابگرِ حسابِ سود/زیانِ تسعیر

**صورتِ مسئله:** سپیدار در فرمِ تسعیر «حساب معین مربوط به تسعیر» را از کاربر
می‌پرسد. کوبیتا حساب را با `system_role` (`fx_gain` / `fx_loss`) پیدا می‌کند.

**چرا امروز ساخته نشده:** کاربر می‌تواند **کد و نامِ همان حساب** را در چارت عوض
کند و ثبتِ خودکار همچنان پیدایش می‌کند. یک انتخابگرِ جدا همان کار را از راهِ دوم
انجام می‌دهد — «دو نمای یک داده».

**چه چیزی تصمیم را عوض می‌کند:** اگر کسب‌وکاری بخواهد **بیش از یک** حسابِ سود
تسعیر داشته باشد (مثلاً به تفکیکِ ارز)، آن‌وقت `system_role` کافی نیست و بحث
عوض می‌شود.

**امروز:** ساخته نشده.

---

## ۶. مقادیرِ پیش‌فرضِ نوعِ فروش (`due_days` و `default_tax_rate`)

**صورتِ مسئله:** docstringِ خودِ مدل می‌گوید این دو باید فرم را خودکار پر کنند،
ولی وصل نشده‌اند.

**چرا:** پرکردنِ نرخِ مالیات **مبلغِ فاکتور را عوض می‌کند** — و کاری که این ستون‌ها
را وصل کرد قرار بود هیچ عددی را تکان ندهد.

**دوراهیِ واقعی:** پیش‌فرض باید **پیشنهاد** باشد (کاربر ببیند و بتواند عوض کند) یا
**اعمال** (بی‌صدا در محاسبه بنشیند)؟ اولی امن است و دومی همان چیزی که کاربر از یک
«پیش‌فرض» انتظار دارد.

**امروز:** وصل نشده. ← [FUTURE_WORK.md](FUTURE_WORK.md)

---

## ۷. سندِ حسابداریِ کارمزدِ واسطه

**صورتِ مسئله:** `broker_commission` روی فاکتور ذخیره می‌شود ولی هیچ سندی نمی‌زند.

**دوراهی:** **لحظه‌ی شناسایی** — کارمزد وقتِ **فروش** بدهی می‌شود یا وقتِ
**وصولِ** فاکتور؟ و اگر فاکتور باطل شود، سندِ کارمزد چه می‌شود؟

**چه چیزی تصمیم را عوض می‌کند:** قراردادِ واقعیِ شرکت با واسطه‌ها. هر دو پاسخ در
حسابداری درست‌اند و انتخاب‌شان رویه است، نه فن.

**امروز:** ساخته نشده. ← [FUTURE_WORK.md](FUTURE_WORK.md)

---

## ۸. تقسیمِ یک هزینه بین چند مرکز

**صورتِ مسئله:** هر ردیفِ سند **یک** `cost_center_id` دارد. هزینه‌ای که باید بین
سه شعبه پخش شود، امروز یا باید دستی به سه ردیف شکسته شود یا به یک مرکز بچسبد.

**دوراهی:** تقسیم در **لحظه‌ی ثبت** انجام شود (سند سه ردیف می‌شود، دفتر خودش
گویاست) یا در **لحظه‌ی گزارش** (یک ردیف ثبت می‌شود و نسبت‌ها جدا نگه داشته
می‌شوند)؟ دومی انعطاف‌پذیرتر است ولی «مشتق بهتر از ذخیره» را وارونه می‌کند: نسبتِ
تخصیص چیزی است که در یک لحظه تصمیم گرفته شده، مثلِ `tax_amount`.

**امروز:** ساخته نشده. ← [FUTURE_WORK.md](FUTURE_WORK.md)

---

## ۹. «سند کل» به‌عنوان موجودیتِ ذخیره‌شده

**صورتِ مسئله:** سپیدار عملیاتِ مستقلِ «صدور سند کل» دارد: چند سند حسابداری در یک
بازه انتخاب می‌شوند و حاصلشان در سطحِ حسابِ کل، در یک سندِ شماره‌دارِ **ذخیره‌شده**
جمع می‌شود (تجمیعی یا روزانه).

**آنچه کوبیتا از قبل دارد — بیش از آنچه اول فکر کردم:** صفحه‌ی **«صدور سند کل»**
(`GeneralDocumentPage`) از مدت‌ها پیش هست و دقیقاً همین کار را می‌کند: گردشِ یک
بازه را در سطحِ حسابِ کل تجمیع می‌کند و در توضیحِ خودش می‌گوید «سندِ تازه‌ای ثبت
نمی‌کند؛ فقط اسنادِ موجود را تجمیع می‌کند». به‌علاوه: تجمیعِ سطحِ کل در «گزارش
ترازها»، دفترِ **کل** در «گزارش دفتر»، و دفترِ **روزنامه**ی قانونی از
`get_legal_book` — همه زنده و مشتق.

**یعنی مدلِ «مشتق» از قبل انتخاب و پیاده شده بود.** دوراهیِ باقی‌مانده باریک‌تر از
چیزی است که اول نوشتم: نه «بسازیم یا نه»، بلکه **«آیا برگه‌ی شماره‌دارِ ماندگار
هم لازم است؟»**

**بهای ساختنش:** محتوایش صددرصد مشتق است، و §۱۱ خودِ تحلیل هشدار می‌دهد که اگر
سند کل منبعِ دومِ حقیقت شود **ارقامِ گزارش‌ها دو برابر می‌شوند**. کوبیتا این
تصمیم را یک بار گرفته و شیپ کرده: دفترِ قانونی ذخیره نمی‌شود، ساخته می‌شود.

**بهای نساختنش:** دو چیز واقعاً مشتق‌شدنی نیستند — **«تا کجا صادر شده»** (§۷) و
**شماره‌ی سندِ کل**. هر دو واقعیتی درباره‌ی *کاغذِ بیرونِ سیستم*اند: اگر شرکتی
دفترِ پلمب‌شده می‌نویسد، باید بداند تا کجا نوشته.

**چه چیزی تصمیم را عوض می‌کند — چهار سؤال:**
1. مشتری‌های واقعی دفاترِ قانونی (روزنامه و کل) را نگه می‌دارند، یا فقط گزارشِ
   داخلی می‌خواهند؟ **این سؤالِ اصلی است؛ بقیه از آن درمی‌آیند.**
2. آیا رویه‌ی قانونیِ ایران برگه‌ی **شماره‌دارِ ماندگار** می‌خواهد یا چاپِ گزارش کافی است؟
3. تفاوتِ «تجمیعی» و «روزانه» در کوبیتا دقیقاً چه می‌شود — دو حالتِ صدور، یا فقط
   دو شکلِ گروه‌بندی در همان گزارش؟
4. اگر سندِ منبعی *پس از* صدور باطل شود، سندِ کل چه می‌شود؟ (در کوبیتا سند ویرایش
   نمی‌شود، فقط ابطال و معکوس — پس مجموعه‌ی منبع پایدار است و معکوس در دوره‌ی
   بعدی می‌نشیند. این خودش نیمی از جواب است.)

**قیدِ فنی، اگر روزی ساخته شد:** پیوندِ سند کل با اسنادِ منبع باید با **شناسه**
باشد نه شماره. `renumber_entries` شماره‌ها را عوض می‌کند و §۲۰ تحلیل دقیقاً همین
را هشدار داده: «شماره برای انسان است؛ هویتِ سند باید مستقل از شماره باشد».

**امروز:** نسخه‌ی **مشتق** ساخته شده («صدور سند کل» + «دفتر کل»). چیزی که ساخته
نشده فقط **ذخیره‌سازیِ** سندِ شماره‌دار است.
← PROJECT_OVERVIEW §۱۰ (۱۴۰۵/۰۶/۱۶)

---

## ۱۰. تسویه‌حسابِ پایانِ کار سند حسابداری نمی‌زند

**صورتِ مسئله:** `PayrollSettlement` ستونِ `journal_entry_id` دارد ولی هیچ‌جا پُر
نمی‌شود — تسویه‌حساب سنوات و بازخریدِ مرخصی و کسرِ وام را *حساب* می‌کند و
`net_amount` را ذخیره می‌کند، بدونِ هیچ اثرِ حسابداری.

**دو خوانشِ ممکن:** یا برگه‌ی محاسبه است و پرداختِ واقعی از مسیرِ خزانه سند
می‌خورد — که آن‌وقت ستون فقط آرزوست و بهتر است حذف شود؛ یا بدهیِ کارمندِ
خارج‌شده باید همان لحظه شناسایی شود و این یک **شکافِ واقعی** است.

**بهای هر طرف:** اگر شکاف باشد، بدهیِ واقعیِ شرکت تا لحظه‌ی پرداخت در ترازنامه
نیست. اگر عمدی باشد و سند بزنیم، پرداختِ خزانه همان مبلغ را دوباره ثبت می‌کند و
بدهی دوبار می‌نشیند.

**چه چیزی تصمیم را عوض می‌کند:** رویه‌ی امروزِ خودتان — تسویه‌حساب را در خزانه
دوباره ثبت می‌کنید یا نه؟

**امروز:** در `entry_source.MODELS_WITHOUT_ENTRIES` صریح معاف است، تا تستِ
کامل‌بودنِ رجیستری بینِ «عمداً» و «یادمان رفت» فرق بگذارد.

---

## ۱۱. اگر روزی صدورِ سند از عملیات جدا شود

**صورتِ مسئله:** امروز هر ماژول سندش را در همان تراکنش می‌زند، و همین باعث شد
«کارتابل صدور سند»ِ سپیدار موضوعیت نداشته باشد.

**اگر روزی جدا شود** (مثلاً تا حسابدار پیش از ثبت بازبینی کند)، این بندها یک‌شبه
معنا پیدا می‌کنند و باید **با هم** طراحی شوند: صفِ «منتظرِ صدور»، صدورِ گروهی،
گاردِ ایدمپوتنسی، و گزارشِ «چه چیزی سند نخورده».

**بهای جداکردن:** عملیاتی که سندش هنوز نخورده در گزارش‌ها **نامرئی** است — تراز
و دفتر از سند می‌آیند، نه از فاکتور. یعنی پنجره‌ای که در آن فاکتور ثبت شده ولی
اثرِ مالی‌اش دیده نمی‌شود. امروز این پنجره صفر است، و صفر بودنش خودش یک ویژگی است.

**امروز:** جدا نمی‌شود. این ردیف فقط هست تا دفعه‌ی بعد از صفر بحث نشود.

---

## ۱۲. گزارش فصلی: فایلِ رسمی لازم است یا ورودِ دستی کافی است؟

**صورتِ مسئله:** کوبیتا گزارشِ معاملاتِ فصلی (م۱۶۹) را می‌سازد و CSVِ فارسی‌سرصفحه
می‌دهد — برای خواندنِ آدم و اکسل، نه برای آپلودِ ماشینی.

**چرا این سؤالِ اصلی است:** تقریباً هر بندِ دیگرِ تحلیلِ فصلی به آن بند است —
لایه‌ی نگاشتِ ارز و طبقه‌بندیِ کالا و کشورِ مبدأ، حدِ نصاب، نسخه‌پذیریِ طبقه‌بندی،
و تاریخچه/snapshotِ خروجی. **بدونِ دیدنِ قالبِ مقصد، ساختنِ جدولِ نگاشت یعنی
حدس‌زدنِ ستون‌های چیزی که ندیده‌ایم.**

**اگر جواب «فایلِ رسمی» شد، سه قید از روزِ اول لازم‌اند:** طبقه‌بندیِ کالا باید
**نسخه‌دار** باشد (قواعد سالانه عوض می‌شوند و کدِ ثابت یعنی بازنویسیِ داده‌ی
تاریخی)، حدِ نصاب باید **قاعده‌ی دوره‌ای** باشد نه عددِ ثابت، و هر خروجی باید
سابقه داشته باشد تا معلوم باشد دقیقاً چه چیزی تولید شده.

**چه چیزی تصمیم را عوض می‌کند:** یک نمونه‌ی فایل یا مستندِ سازمانِ امور مالیاتی.

**امروز:** فقط اعتبارسنجیِ پیش از خروجی ساخته شد. ← PROJECT_OVERVIEW §۱۰ (۱۴۰۵/۰۶/۱۷)

---

## ۱۳. برگشت‌ها در گزارش فصلی: کسر یا بخشِ جدا؟

**صورتِ مسئله:** امروز برگشت از مبلغِ همان طرف‌حساب **کسر** می‌شود (فروش ۱۰۰ منهای
برگشت ۲۰ = ۸۰) و docstring دلیلش را دارد: «رقمِ سامانه دقیقاً چیزی باشد که در
دفاتر مانده». بندِ ۶ تحلیل می‌گوید باید جدا دیده شوند.

**بهای هر طرف:** کسر، رقمِ خالص را درست می‌دهد ولی منشأش را پنهان می‌کند —
معامله‌ای که ۱۰۰ بوده و ۲۰ برگشته، از معامله‌ای که از اول ۸۰ بوده قابلِ تشخیص
نیست. تفکیک، شفاف‌تر است ولی اگر سامانه رقمِ خالص بخواهد، ستونِ اضافه سردرگمی است.

**چه چیزی تصمیم را عوض می‌کند:** حسابدارتان، یا فرمِ واقعیِ سامانه. این یک واقعیتِ
بیرونی است نه انتخابِ طراحی.

**امروز:** دست‌نخورده. عوض‌کردنش ارقامِ گزارش را تکان می‌دهد و کارِ اعتبارسنجی
قرار نبود هیچ عددی را تکان دهد.

---

## ۱۴. مالیات در سطحِ ردیفِ فاکتور

**صورتِ مسئله:** مالیات امروز **یک نرخ روی سربرگ** است:
`tax_amount = compute_tax(total_amount, tax_rate)`. از وقتی «معاف» وجود دارد،
فاکتوری که ردیفِ معاف و مشمول را با هم دارد روی ردیفِ معاف هم مالیات می‌گیرد.

**بهای ساختنش:** `tax_rate`/`tax_amount`ِ سربرگ به ردیفِ سندِ حسابداری، ارسالِ
مؤدیان، برگشت‌ها، صندوق و چاپِ فاکتور گره خورده‌اند. بردنِ مالیات به ردیف یعنی
بازنویسیِ همه‌ی آن مسیرها، و اسنادِ گذشته که سربرگی محاسبه شده‌اند باید همچنان
درست خوانده شوند.

**بهای نساختنش:** کاربری که فاکتورِ ترکیبی می‌زند، مالیاتِ اضافه می‌گیرد. امروز
این فاکتورها در گزارشِ ارزش افزوده **فهرست می‌شوند** ولی جلویشان گرفته نمی‌شود.

**چه چیزی تصمیم را عوض می‌کند:** اینکه فاکتورِ ترکیبی در کارِ واقعیِ شما چقدر
پیش می‌آید. اگر نادر باشد، فهرستِ هشدار کافی است؛ اگر رایج باشد، بازنویسی می‌ارزد.

**امروز:** فقط گزارش می‌شود. ← PROJECT_OVERVIEW §۱۰ (۱۴۰۵/۰۶/۱۷)

---

## ۱۵. هویتِ مالیاتیِ شرکت داخلِ ماژولِ مؤدیان است

**صورتِ مسئله:** `economic_code` و `national_id`ِ خودِ کسب‌وکار در جدولِ
**تنظیماتِ مؤدیان** ذخیره می‌شوند، نه در اطلاعاتِ پایه‌ی شرکت.

**چرا مهم است:** هر گزارشِ قانونیِ بعدی (ارزش افزوده، فصلی، هر چیزِ دیگر) یا باید
دستش را داخلِ ماژولِ مؤدیان ببرد، یا نسخه‌ی خودش را نگه دارد — و آن‌وقت یک شرکت
چند هویتِ مالیاتیِ ناهماهنگ پیدا می‌کند.

**بهای جابه‌جایی:** مهاجرت به‌علاوه‌ی دست‌زدن به ماژولی که با سامانه‌ی واقعی حرف
می‌زند. تا وقتی مصرف‌کننده‌ی دومی وجود ندارد، سودش صفر و ریسکش واقعی است.

**چه چیزی تصمیم را عوض می‌کند:** اولین گزارشِ قانونی‌ای که به این اطلاعات نیاز
پیدا کند. آن روز باید **پیش از** نوشتنش جابه‌جا شود، نه بعدش.

**امروز:** دست‌نخورده.

---

## ۱۶. مؤدیان: وضعیتِ داخلی و وضعیتِ استعلام یک ستون‌اند

**صورتِ مسئله:** `MoadianSubmission.status` هم می‌گوید «ارسال شد» و هم نتیجه‌ی
استعلام را نگه می‌دارد. `inquire_status` روی همان می‌نویسد.

**بهای وضعِ فعلی:** «ارسال شده ولی هنوز استعلام نشده» از «ارسال شده و تأیید شده»
قابلِ تفکیک نیست مگر از روی `reference_number`. §۲۹ تحلیل می‌گوید این دو مفهومِ
جدا هستند.

**بهای تغییر:** مهاجرت، بازنویسیِ `inquire_status` و فهرست، و سازگاری با
ردیف‌های موجود.

**چه چیزی تصمیم را عوض می‌کند:** اینکه در کارِ واقعی به این ابهام بخورید. تا
امروز نخورده‌ایم.

**امروز:** یک ستون.

---

## ۱۷. مؤدیان: تشخیصِ تغییرِ فاکتور پس از ارسال

**صورتِ مسئله:** `request_payload` **دقیقاً همان چیزی است که ارسال شد** (§۳۴
برقرار). ولی هیچ‌چیز آن را با وضعیتِ *امروزِ* فاکتور مقایسه نمی‌کند.

**یعنی نیمی از کار هست:** snapshot داریم، مقایسه نداریم. اگر فاکتوری پس از ارسال
تغییر کند (نامِ کالا، طرف حساب)، کسی خبردار نمی‌شود.

**بهای ساختنش:** مقایسه‌ی بسته‌ی ذخیره‌شده با بسته‌ای که *الان* ساخته می‌شود — و
تصمیم اینکه کدام تفاوت‌ها مهم‌اند (نامِ کالا؟ مبلغ؟ فقط مبلغ؟).

**چه چیزی تصمیم را عوض می‌کند:** قواعدِ اصلاحِ صورتحساب در سامانه. اگر اصلاح فقط
با صورتحسابِ برگشتی ممکن باشد، هشدار کافی است؛ اگر نه، باید چرخه‌ی اصلاح ساخته شود.

**امروز:** ساخته نشده. snapshot هست و آماده‌ی مقایسه.

---

## ۱۸. مقصدِ بستنِ سود و زیان: «جاری» یا «انباشته»؟

**صورتِ مسئله:** تحلیلِ سپیدار (§۱۲) صریح می‌گوید این دو را یکی نگیرید — بستنِ
درآمد و هزینه باید به **سود و زیان جاری** برود و انتقالِ آن به **انباشته**
فرآیندِ جداگانه‌ای است که معمولاً پس از تصویبِ مجمع انجام می‌شود. کوبیتا یک‌قدمی
می‌بندد: مستقیم به `RETAINED_EARNINGS`.

**بهای ساختنش:** کار *فنی* کم است — مهاجرت نمی‌خواهد، چون
`get_or_create_account` نقشِ تازه را موقعِ اولین استفاده می‌سازد (همان کاری که
برای «حساب اختتامیه» می‌کند). بهای واقعی جای دیگری است: کوبیتا «سود و زیان
جاری» را از قبل **دارد، به‌صورتِ مشتق** — `get_balance_sheet` آن را از صورتِ سود و
زیان حساب می‌کند و در `current_period_profit` نشان می‌دهد. با افزودنِ حسابِ ۳۱۰۳،
ترازنامه این عدد را پیش از بستن از راهِ مشتق و پس از بستن از راهِ مانده‌ی حساب
نشان می‌دهد — **دو نمای یک داده**. و بدونِ عملیاتِ انتقال، پول در «جاری» معلق
می‌ماند و سالِ بعد سودِ پارسال را «جاری» نشان می‌دهد.

**بهای نساختنش:** «سود و زیان جاری» در چارتِ حساب دیده نمی‌شود، و حسابدارِ ایرانی
که انتظارِ این سطرِ استاندارد را دارد آن را پیدا نمی‌کند. سود بلافاصله به انباشته
می‌رود، حتی پیش از آنکه مجمع تصمیم بگیرد.

**چه چیزی تصمیم را عوض می‌کند:** اینکه دفاترِ شما را چه کسی می‌خواند. اگر
حسابرس یا مجمع سطرِ «سود و زیان جاری» را جدا می‌خواهد، ساختنش می‌ارزد و آن‌وقت
باید `current_period_profit`ِ مشتق هم برداشته شود تا دو نما نشود.

**اگر روزی بازش کردیم، بسته‌ی کامل این است:** نقشِ `CURRENT_PROFIT_LOSS` (کدِ
پیش‌فرض ۳۱۰۳) · `issue_pnl_close` به آن ببندد · عملیاتِ تازه‌ی «انتقال جاری به
انباشته» · حذفِ `current_period_profit`ِ مشتق از ترازنامه · هشدار (نه گارد) در
اختتامیه اگر «جاری» مانده دارد.

**امروز:** یک‌قدمی به سود و زیان انباشته. ← PROJECT_OVERVIEW §۱۰ (۱۴۰۵/۰۶/۱۸)

---

## ۱۹. ثبتِ سند روی حسابِ گروه: گارد بگذاریم یا فقط گزارش؟

**صورتِ مسئله:** `routers/journal.py` هیچ گاردی روی `is_group` ندارد، پس سندِ
دستی می‌تواند مستقیم روی یک سرفصل بنشیند. `get_balances` اما فقط
`is_group = False` را می‌آورد — یعنی آن مبلغ **از «گزارش ترازها» غیب می‌شود** در
حالی که خودِ دفتر متوازن می‌ماند. نتیجه: جمعِ ستون‌های تراز با هم نمی‌خوانند و
هیچ پیامی نمی‌گوید چرا.

**چرا امروز فقط گزارش شد:** قاعده‌ی «گزارش، نه گارد» که `get_nature_violations`
گذاشت. اضافه کردنِ گاردِ ۴۰۹ روی مسیرِ ثبت **رفتارِ موجود را عوض می‌کند** و
داده‌ی مستقر ممکن است از قبل چنین ردیف‌هایی داشته باشد؛ آن‌وقت کاربر سندی را که
سال‌ها ثبت شده نمی‌تواند ویرایش کند. بررسیِ یکپارچگی این ردیف‌ها را پیدا می‌کند
بدونِ آنکه چیزی بشکند.

**فرقش با «خلافِ ماهیت» اما این است که این یکی هیچ‌وقت درست نیست.** اضافه‌برداشتِ
بانکی یک رویدادِ واقعی است؛ نشستنِ سند روی سرفصل هیچ معنای حسابداری ندارد و
همیشه خطای ورودِ داده است. پس اینجا دلیلِ «گاردِ سخت» قوی‌تر از موردهای دیگر است.

**بسته‌ی کامل اگر روزی بازش کردیم:** گارد در `routers/journal.py` (و هر جای
دیگری که `account_id` از کاربر می‌گیرد) · یک بار اجرای بررسیِ یکپارچگی روی
داده‌ی مستقر پیش از فعال‌کردن · عملیاتی برای بردنِ ردیف‌های موجود به حسابِ درست
(`اصلاح طبقه‌بندی مانده` از قبل هست).

**امروز:** فقط گزارش. ← PROJECT_OVERVIEW §۱۰ (۱۴۰۵/۰۶/۱۸)

---

## ۲۰. تخصیصِ رسید به فاکتور (Allocation)

**صورتِ مسئله:** امروز «تسویه حساب طرف مقابل» کلِ مانده‌ی یک طرف‌حساب را یکجا
می‌بندد و **هیچ رابطه‌ای بینِ رسید و فاکتور ثبت نمی‌کند**. یعنی نمی‌شود گفت آن
۸۰ میلیونی که مشتری داد، بابتِ کدام فاکتور بود. تحلیلِ خزانه (§۲۲ §۲۳) این را
«یکی از نقاط مهم طراحی» می‌خواند و رابطه را ذاتاً چندبه‌چند می‌داند: یک رسید چند
فاکتور را می‌بندد و یک فاکتور با چند رسید بسته می‌شود.

**بهای ساختنش:** جدولِ تخصیص + مهاجرت، و بازنویسیِ مسیرِ رسید و پرداخت. ولی هزینه‌ی
اصلی فنی نیست — **تصمیم‌های طراحی‌ای است که هنوز جوابشان را نداریم:** تخصیصِ
جزئی چطور نمایش داده شود؟ باقی‌مانده «علی‌الحساب» می‌شود یا معلق؟ تخصیصِ خودکارِ
FIFO یا انتخابِ دستی؟ با ابطالِ رسید، تخصیص‌هایش چه می‌شوند؟ گزارشِ سنی
(`get_aging`) که امروز از مانده‌ی کل می‌آید، باید از تخصیص بیاید یا نه؟

**بهای نساختنش:** حسابدار نمی‌تواند بگوید کدام فاکتور تسویه شده و کدام نه — فقط
مانده‌ی کلِ طرف‌حساب را می‌بیند. برای کسب‌وکاری با فاکتورهای متعدد و پرداخت‌های
جزئی، این واقعاً کم است.

**چه چیزی تصمیم را عوض می‌کند:** فصل‌های اختصاصیِ «رسید دریافت» و «اعلامیه
پرداخت» که کاربر قرار است بفرستد. خودِ فصلِ نقشه صریح می‌گوید
«Do NOT invent detailed field-level implementation for each operation yet».

**امروز:** تسویه‌ی کلِ مانده، بدونِ تخصیص. منتظرِ فصلِ اختصاصی.

---

## ۲۱. رسیدِ چندابزاری

**صورتِ مسئله:** `TreasuryTransaction` یک ستونِ `method` دارد، پس یک رسید = یک
ابزار. تحلیل (§۴) می‌گوید فرض نکنیم یک دریافت یک روش دارد: «۴۰ نقد + ۳۰ چک +
۳۰ انتقال بانکی» می‌تواند یک رویدادِ دریافت باشد.

**بهای ساختنش:** تبدیلِ رسید به سربرگ + ردیف، یعنی مهاجرت و بازنویسیِ مسیری که
**مستقر است** — و صندوق و کارتخوان و «تسویه حساب طرف مقابل» همگی از همین مسیر
می‌روند. متادیتای کارتخوان (`reference_no` و یکتاییِ RRN) هم روی سربرگ نشسته و
باید به ردیف منتقل شود.

**بهای نساختنش:** کاربر باید یک دریافتِ ترکیبی را چند رسیدِ جدا ثبت کند. از نظر
حسابداری درست است ولی رویدادِ واقعی را تکه‌تکه نشان می‌دهد.

**چه چیزی تصمیم را عوض می‌کند:** اینکه دریافتِ ترکیبی در کارِ واقعیِ شما چقدر
پیش می‌آید. خودِ تحلیل هم محتاط است و می‌گوید «may **potentially** be settled by
multiple instruments».

**امروز:** یک رسید، یک ابزار. منتظرِ فصلِ «رسید دریافت».

---

## ۲۲. سیاستِ مانده‌ی منفیِ صندوق

**صورتِ مسئله:** صندوق می‌تواند مانده‌ی منفی پیدا کند — یعنی بیش از آنچه داشته
پرداخت شده. از نظر فیزیکی ناممکن است، پس معمولاً یعنی سندی جا افتاده یا تاریخی
اشتباه خورده. امروز کوبیتا هیچ واکنشی ندارد.

**بهای ساختنش:** یک تنظیمِ سه‌حالته روی مستأجر (مجاز / هشدار / ممنوع)، مثلِ
`tafsili_enforcement` که از قبل هست. کارِ فنی کم است.

**بهای نساختنش:** خطای ورودِ داده بی‌صدا می‌ماند تا وقتی کسی مانده را ببیند.

**چه چیزی تصمیم را عوض می‌کند:** خودِ فصلِ صندوق صریح می‌گوید از روی آن قانونِ
Hard Block نسازید و این را تصمیمِ محصولیِ کوبیتا می‌داند. پس سؤال این است که در
کارِ واقعیِ شما صندوقِ منفی چقدر پیش می‌آید و آیا مسدودکردنش کمک است یا مزاحمت.
تجربه‌ی «حساب‌های خلافِ ماهیت» می‌گوید گزارش‌کردن معمولاً کافی است.

**امروز:** هیچ کنترلی نیست. مانده‌ی منفی در فهرستِ صندوق‌ها دیده می‌شود.

---

## ۲۲. جدولِ مرجعِ بانک و شعبه

**صورتِ مسئله:** فصلِ «تعریف و مدیریت حساب بانکی» (§۳) می‌گوید بانک و شعبه بهتر
است متنِ آزاد نباشند و ساختارِ `Bank → Branch → Bank Account` منطقی‌تر است، چون
دسته‌چک و چکِ پرداختنی و صورت‌حساب و مغایرت و کارت‌خوان همه بعداً به اطلاعاتِ بانک
نیاز پیدا می‌کنند.

**بهای ساختنش:** دو جدولِ تازه با RLS، فهرست و CRUD و مهاجرتِ داده‌ی `bank_name`ِ
موجود به رکوردهای مرجع — و بعد نگهداریِ فهرستی از بانک‌های ایران که هر چند سال
عوض می‌شود (ادغامِ بانک‌ها، تغییرِ نام).

**چه چیزی تصمیم را عوض می‌کند:** اولین قابلیتی که واقعاً به بانک *به‌عنوان
موجودیت* نیاز داشته باشد — مثلاً قالبِ چاپِ چک به تفکیکِ بانک، یا اعتبارسنجیِ شبا
بر اساسِ کدِ بانک. تا آن روز `bank_name` و `branch_name` دو متن‌اند و کسی از
روزِ اول شکایتی نکرده.

**امروز:** دو فیلدِ متن روی خودِ حساب. ← PROJECT_OVERVIEW §۱۰ (۱۴۰۵/۰۶/۱۹)

---

## ۲۳. `BankTransaction` دفترِ کاملِ حساب نیست

**صورتِ مسئله:** ردیفِ `bank_transactions` فقط در سه جا ساخته می‌شود —
واریز/برداشتِ دستی، وصولِ چک، و کارمزدِ تسویه‌ی کارت‌خوان. **رسید و پرداختِ بانکی
هیچ ردیفی نمی‌سازند.** یعنی «مغایرت بانکی» گردشِ کوبیتا را با صورت‌حسابِ بانک
مقایسه می‌کند در حالی که سمتِ کوبیتا از پیش ناقص است: واریزِ یک مشتری که با
«رسید دریافت» ثبت شده، در فهرستِ تطبیق پیدا نمی‌شود.

**چرا الان درست نشد:** مانده‌ی حساب از **دفتر** می‌آید نه از این جدول، پس عددها
درست‌اند و این نقص روی هیچ گزارشِ مالی اثر ندارد. ولی تطبیق را عملاً دستی
می‌کند. درستش دو راه دارد: یا رسید/پرداختِ بانکی هم `BankTransaction` بسازند
(یعنی رفتارِ مغایرت و شمارشِ ردیف‌ها برای داده‌ی مستقر عوض شود)، یا مغایرت
مستقیم از دفتر بخواند (یعنی بازنویسیِ `auto_match_statement`). هر دو تصمیمِ
مستقلی‌اند که باید با فصلِ «مغایرت بانکی» گرفته شوند.

**چه چیزی تصمیم را عوض می‌کند:** اولین کاربری که بگوید تطبیقِ خودکار واریزهایش
را پیدا نمی‌کند.

**امروز:** ثبت‌شده، دست‌نخورده. ← PROJECT_OVERVIEW §۱۰ (۱۴۰۵/۰۶/۱۹)

---

## ۲۴. وجوه در راهِ کارت‌خوان — و تفصیلیِ دستگاه، که **یک تصمیم‌اند**

**صورتِ مسئله:** رسیدِ کارتی از همان لحظه معینِ بانک را بدهکار می‌کند، ولی PSP
چند روز بعد واریز می‌کند. یعنی مانده‌ی بانک در آن فاصله بیش‌ازواقع است و مغایرتِ
بانکیِ همان روز دقیقاً به اندازه‌ی جمعِ تسویه‌نشده اختلاف نشان می‌دهد. فصلِ
کارت‌خوان (§۲۸) حسابِ واسطِ «وجوه در راه» را پیشنهاد می‌کند: رسید به واسط بدهکار،
و تسویه آن را به بانک منتقل کند.

**چرا این و «تفصیلیِ دستگاه» (§۱۰ §۱۱) یک تصمیم‌اند:** `journal_lines` تنها **یک**
`analytic_id` دارد و از مهاجرتِ ۰۱۰۶ آن را حسابِ بانکی گرفته. تا وقتی ردیفِ کارتی
روی معینِ بانک است، جای دومی برای تفصیلیِ دستگاه نیست. تفصیلیِ کارت‌خوان فقط با
حسابِ واسط ممکن است — پس نمی‌شود یکی را ساخت و دیگری را نه.

**بهای ساختنش:** فنی کم است (یک نقشِ حساب + سندِ تسویه). بهای واقعی این است که
**رفتارِ حسابداریِ مستقر را عوض می‌کند**: مانده‌ی بانکِ کاربر ناگهان پایین می‌آید
بدونِ آنکه تراکنشی رخ داده باشد. این باید تغییرِ اعلام‌شده باشد، نه اثرِ جانبیِ
فصلی که اسمش «تعریف دستگاه» است. پرچمِ اختیاری هم جواب نیست: دو مسیرِ حسابداریِ
موازی برای همیشه می‌ماند، و پرچم‌های کوبیتا دامنه‌ی *گزارش* را عوض می‌کنند نه
اینکه سند به کدام حساب بخورد.

**چه چیزی تصمیم را عوض می‌کند:** فصلِ «تسویه کارت‌خوان». خودِ فصلِ حاضر هم در §۱۴
می‌گوید فرمولِ نهایی را بدونِ آن Hard-code نکنید.

**امروز:** رسیدِ کارتی مستقیم به بانک؛ «تسویه‌نشده» با `settled_at` دیده می‌شود و
موجودیِ دستگاه از همان مشتق می‌شود. ← PROJECT_OVERVIEW §۱۰ (۱۴۰۵/۰۶/۱۹)

---

# ب) تناقض‌های خودِ طراحی

این‌ها اشتباه نیستند — دو قاعده‌ی درستِ پروژه‌اند که در نقطه‌ای به هم می‌رسند.
نوشته می‌شوند تا دفعه‌ی بعد کسی فکر نکند یکی‌شان فراموش شده.

## ۱. «گزارش، نه گارد» در برابر حالتِ «اجباری»ِ تفصیلی

قاعده‌ی پروژه این است که انتخابِ کاربر تعیین می‌کند چه چیزی *مسدود* شود، نه چه
چیزی *دیده* شود — و ماهیتِ حساب دقیقاً همین‌طور عمل می‌کند: خلافِ ماهیت گزارش
می‌شود، مسدود نمی‌شود.

ولی `tafsili_enforcement = "strict"` **یک گاردِ تمام‌عیار است** و ثبت را متوقف
می‌کند.

**چطور امروز حل شده:** گارد بودنش *انتخابِ کسب‌وکار* است نه پیش‌فرضِ ما (پیش‌فرض
`hybrid` است)، و گزارشِ `find_missing_tafsili` **در هر سه حالت** کار می‌کند.

**نقطه‌ی دردناکش — بازگشتی بودن:** تنظیمی که امروز عوض شود، داده‌ی *دیروز* را
نامعتبر می‌کند. دو بار همین یک چیز باعثِ استثنا شده:
- سندِ **برگشتی** هرگز مسدود نمی‌شود (ابعادش کپیِ اصل است)
- سندِ **تسعیرِ ارز** هم، با همان استدلال (`DIMENSION_COPYING_SOURCES`)

**سؤالِ باز:** آیا این الگو باید یک قاعده‌ی عمومی شود — «هر منبعی که ابعادش را از
داده‌ی موجود کپی می‌کند مسدود نمی‌شود» — یا هر بار جداگانه تصمیم گرفته شود؟ امروز
فهرستِ صریح است، که یعنی منبعِ سومی که فردا اضافه شود، همین تله را دوباره می‌خورد
تا کسی به یادش بیاورد.

---

## ۲. «مشتق بهتر از ذخیره» در برابر عددی که در یک لحظه قفل شده

قاعده روشن است: چیزی را که می‌شود حساب کرد، ذخیره نکن.

**استثنا هم روشن است:** مقداری که در یک لحظه *تثبیت* شده، داده است نه مشتق —
`tax_amount` کنارِ `tax_rate`، `broker_commission`، `fx_rate`ِ ردیف.

**مرزِ لغزنده:** هر بار باید پرسید «اگر فردا نرخ عوض شود، این عدد باید عوض شود یا
نه؟». اگر نه، ذخیره‌اش کن. تسعیرِ ارز کلِ ماجرا را در یک جمله دارد: **مقدارِ ارز
تغییر نمی‌کند، ارزشِ ریالی‌اش می‌کند.**

---

## ۳. «هرگز دو نمای یک داده نساز» در برابر جفتِ عملیات/فهرست

قاعده‌ی صفحه‌ها می‌گوید هر عملیاتِ رکوردساز باید یک **فهرستِ نظیر** داشته باشد —
یعنی همان داده در دو صفحه دیده می‌شود.

**چرا تناقض نیست:** مرزشان کارکردی است (ثبت در برابر مرور) و در `OPS_LIST_MAP`
ماشین‌خوان ثبت شده، با سه استثنای صریح.

**جایی که واقعاً باید مراقب بود:** وقتی صفحه‌ی عملیات ته خودش فهرستی دارد که
صفحه‌ی فهرست هم همان را نشان می‌دهد. آن‌وقت باید **یکی شوند**، نه اینکه هر دو
بمانند و کاربر حدس بزند کدام را باز کند.

---

## ۴. تست‌ها از مدل‌ها ساخته می‌شوند، نه از مهاجرت‌ها

`conftest.py` با `Base.metadata.create_all` جدول می‌سازد. یعنی **هیچ تستی
مهاجرت‌ها را اجرا نمی‌کند** — نه RLS، نه تریگرِ فقط‌افزودنیِ حسابرسی (هر دو در
conftest دستی اعمال می‌شوند چون `create_all` نمی‌سازدشان).

**پیامدش:** یک زنجیره‌ی مهاجرتِ شکسته می‌تواند با **سوییتِ کاملاً سبز** وارد
production شود. هر مهاجرت باید جداگانه روی یک اسکیمای یک‌بارمصرف اجرا شود.

**سؤالِ باز:** آیا ارزشش را دارد که سوییت یک بار هم از مسیرِ `alembic upgrade
head` بالا بیاید (کندتر ولی واقعی)، یا بررسیِ دستیِ هر مهاجرت کافی است؟

---

## ۵. سپیدار به‌عنوان مرجع، در برابر «اساسِ نرم‌افزار حفظ شود»

قرارِ کار این است که قابلیت‌ها از سپیدار الگو بگیرند و هم‌زمان بنیانِ کوبیتا
دست‌نخورده بماند. این دو گاهی هم‌جهت نیستند، و الگوی تکرارشونده‌اش این است:

**بعضی فیلدهای سپیدار قابلیت نیستند، جبرانِ معماری‌اند.** «کد تفصیلی» روی مرکز
هزینه بهترین نمونه است: سپیدار بُعدِ جدا برای مرکز ندارد و با سطحِ تفصیلی
شبیه‌سازی‌اش می‌کند؛ کوبیتا هر دو را در `journal_lines` جدا دارد. کپی‌کردنِ آن
فیلد یعنی وارد کردنِ ضعفِ سپیدار به جایی که ضعف ندارد.

**قاعده‌ای که از این درآمده و تا امروز جواب داده:** پیش از ساختنِ هر فیلدِ سپیدار
بپرس **«این چه مشکلی را حل می‌کند و آیا کوبیتا آن مشکل را دارد؟»** اگر جواب «چون
سپیدار دارد» بود، هنوز دلیل پیدا نشده.

---

# ج) دوراهی‌های زنده‌ی همین حالا

| دوراهی | وضعیت |
|---|---|
| استقرارِ پنج کارِ قبلی و کارِ تسعیر **با هم** بروند یا **جدا**؟ با هم = یک قطعیِ سرویس و یک هماهنگی با آرش؛ جدا = ریسکِ کوچک‌ترِ هر استقرار | منتظرِ تصمیم |
| شاخه‌ی `fix/fx-revaluation-dimensions` (کامیتِ `9ff4a1b`) push و PR شود؟ | منتظرِ تصمیم |
