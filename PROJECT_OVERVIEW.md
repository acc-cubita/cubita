# کوبیتا (Cubita) — نقشه‌ی کامل پروژه

> **این فایل چیست؟** مرجع جامعِ «جزء‌به‌جزء» کل پروژه. هدفش این است که اگر تاریخچه‌ی
> گفتگو پاک شد، با خواندن همین یک فایل بتوان کل پروژه را دوباره فهمید و ادامه داد.
>
> **قاعده‌ی نگه‌داری (مهم):** با **هر ارتقا/تغییر مهم**، بخش مربوطه و بخش
> «تاریخچه‌ی ارتقاها» در انتهای همین فایل به‌روزرسانی شود. این فایل باید همیشه با
> وضعیت واقعی کد یکی باشد.
>
> **قاعده‌ی دو‌پلتفرمی (مهم):** محصول روی **دو پلتفرم** عرضه می‌شود — اپ دسکتاپ و اپ
> آنلاین/وب — و هر دو از **یک کدپایه‌ی مشترک** (`desktop/`) ساخته می‌شوند. هر
> فیچر/ارتقا باید روی **هر دو** پلتفرم اعمال و آزموده شود.
>
> **این‌جا فقط کارهای *انجام‌شده* ثبت می‌شوند.** کارهای شناخته‌شده‌ی هنوز
> انجام‌نشده در [FUTURE_WORK.md](FUTURE_WORK.md) می‌آیند؛ وقتی انجام شدند از آن‌جا
> حذف و به بخشِ ۱۰ِ همین فایل منتقل می‌شوند.

---

## ۱. محصول در یک نگاه

کوبیتا یک **نرم‌افزار حسابداری دوطرفه‌ی فارسی/شمسی** برای کسب‌وکارهای ایرانی است.
ماژول‌ها: فروش، خرید، اشخاص (مشتری/تأمین‌کننده)، انبار، حسابداری (دفتر کل/سند)،
چک و بانک، حقوق و دستمزد، گزارش‌ها، اتصال به فروشگاه آنلاین، و مدیریت کاربران.

هر کسب‌وکار یک **مستأجر (tenant) کاملاً ایزوله** است؛ داده‌ی مشتری‌ها با
Row-Level Security در سطح پایگاه‌داده از هم جدا می‌ماند.

## ۲. دو پلتفرم (اصل parity)

| پلتفرم | چیست | از کجا ساخته می‌شود |
|---|---|---|
| **اپ دسکتاپ** | Electron، قابلیت کار **آفلاین** (SQLite محلی + صف outbox و همگام‌سازی) | `desktop/` (حالت Electron) |
| **اپ آنلاین/وب** | همان اپ در مرورگر، مستقیم و زنده از API | `desktop/` (حالت مرورگر) |

تفکیک دو حالت با `isElectron` در [desktop/src/platform.ts](desktop/src/platform.ts):
`window.cubita` فقط در Electron تزریق می‌شود. در حالت وب، همان کامپوننت‌ها به‌جای
صف آفلاین، مستقیم API را صدا می‌زنند (توابع `*Live`/`*Direct` در
[desktop/src/api.ts](desktop/src/api.ts)).

## ۳. ساختار مخزن (monorepo)

```
D:\hesabdari\
├── backend/     FastAPI + PostgreSQL + SQLAlchemy + Alembic (هسته‌ی API و منطق)
├── desktop/     React + TS + Vite؛ هم Electron (دسکتاپ) هم وب (اپ آنلاین)
├── website/     React + TS + Vite؛ سایت معرفی/فروش (cubita.ir)
├── docker-compose.yml   فقط Postgres برای توسعه‌ی محلی
└── PROJECT_OVERVIEW.md  همین فایل
```
> نکته: `.claude/` و حافظه‌ی دستیار جدا از مخزن است؛ نقشه‌ی VPS و روال استقرار در
> حافظه‌ی پروژه ذخیره شده (فایل‌های `vps-deployment`, `local-dev-run`, ...).

---

## ۴. Backend — `backend/`

**استک:** FastAPI، SQLAlchemy (ORM)، Alembic (مهاجرت)، PostgreSQL، JWT دستی،
`psycopg`. نقطه‌ی ورود: [backend/app/main.py](backend/app/main.py) (شیء `app`،
سلامت روی `/api/health`، docs فقط در غیرproduction روی `/api/docs`).

**ساختار:** `app/routers/` (لایه‌ی HTTP) · `app/services/` (منطق کسب‌وکار) ·
`app/models/` (جدول‌های ORM) · `app/schemas/` (Pydantic ورودی/خروجی) ·
`app/security.py` (هش رمز، JWT) · `app/deps.py` (احراز هویت/زمینه‌ی مستأجر) ·
`app/config.py` (تنظیمات + گاردهای fail-closed) · `app/tenant_context.py` +
`app/tenancy.py` (چسباندن مستأجر به تراکنش) · `app/observability.py` (لاگ/
request-id) · `app/seed.py` (داده‌ی اولیه).

### ۴.۱ روترها و ماژول‌ها (نقشه‌ی فیچر)

| روتر | prefix | کارکرد |
|---|---|---|
| `auth` | `/api/auth` | ورود، ثبت‌نام self-serve، switch-tenant، فراموشی/بازیابی رمز، پذیرش دعوت، تغییر رمز |
| `members` | `/api/members` | کاربران کسب‌وکار، دعوت، نقش، فعال/غیرفعال، سقف صندلی |
| `accounts` | `/api/accounts` | چارت حساب (درختی) |
| `journal` | `/api/journal-entries` | سند حسابداری دوطرفه |
| `inventory` | — | `/api/items`، `/api/warehouses`، `/api/stock`، `/api/stock-adjustments` |
| `invoices` | — | `/api/sales-invoices`، `/api/purchase-invoices` (+ابطال/چاپ؛ `?kind=goods\|service` — فاکتور خرید خدمات با کسورات) |
| `purchase_deductions` | — | `/api/purchase-deduction-types` (انواعِ کسرِ خرید خدمات: مالیات تکلیفی و بیمه — نرخ، مبنا، حساب) |
| `sales_ops` | `/api/sales-ops` | عملیاتِ فروش (نوعِ فروش، تخفیف و عامل، بسته، پورسانت، گمرک، اعلامیه‌ی قیمت) + **اعلامیه‌ی بدهکار/بستانکار** `/notes` (فهرستِ فیلتردار و keyset، جزئیات، `duplicate-draft`، ابطال با تاریخ؛ مجوزِ `invoices` یا `accounting`، ابطال `accounting.delete`) |
| `quotations` | — | `/api/sales-quotations` (پیش‌فاکتور + تبدیل به فاکتور) |
| `returns` | — | `/api/sales-returns`، `/api/purchase-returns` |
| `transfers` | — | `/api/stock-transfers` (انتقال بین انبار — ثبتِ idempotent، ابطال، چاپِ مجوز) |
| `warehouse_issues` | — | `/api/warehouse-issues` (فهرستِ خروج‌ها و انتقال‌ها، خروجِ مستقیم، جزئیات، زمینه‌ی فاکتور، چاپِ مجوز) |
| `issue_returns` | — | `/api/warehouse-issue-returns` (فهرستِ برگشت‌ها، پنجره‌ی «مبنا» با باقیمانده، ثبتِ idempotent، جزئیات، ابطال، چاپ با فی و مبلغ) |
| `inventory_valuation` | — | `/api/inventory-valuation` (پیش‌نمایشِ قیمت‌گذاری بی‌نوشتن با توکنِ دفتر، ثبتِ idempotentِ اجرا با یک سندِ اصلاحی، فهرست و جزئیات، ابطالِ آخرین اجرا؛ دیدن `inventory`/`accounting`، ثبت `accounting.create`، ابطال `accounting.delete`) |
| `reports` | `/api/reports` | تراز آزمایشی، سود و زیان، ترازنامه، دفتر کل |
| `banking` | — | `/api/bank-accounts`، `/api/checks`، `/api/bank-transactions`، صورت‌حساب/تطبیق بانکی، `/api/petty-cash` (تنخواه) |
| `payroll` | — | `/api/employees`، قرارداد حقوق، دوره‌ی حقوق، کارکرد، فیش، خروجی لیست بیمه CSV |
| `period_close` | — | `/api/fiscal-period-closes` (بستن دوره‌ی مالی) |
| `integration` | `/api/integration` | همگام‌سازی با فروشگاه آنلاین (ipnetcity.ir): import سفارش، push کالا |
| `billing` | — | پلن‌ها و خرید اشتراک از سایت تجاری (زرین‌پال)، `/api/admin/purchases` |
| `treasury` | `/api/treasury` | دریافت/پرداخت وجه به اشخاص + **پرداختِ کارتی** (`/card-payment`: رسیدِ بانکی از کارتخوان، idempotent روی RRN) |
| `pos_terminals` | `/api/pos-terminals` | CRUD دستگاه‌های کارتخوان (نام/روشِ اتصال/host:port/حسابِ تسویه) — پیکربندی سرور-ساید |
| `audit` | `/api/audit` | دفتر ردِ حسابرسی (فقط‌افزودنی) |
| `backup` | `/api/backup` | پشتیبان‌گیری/بازیابیِ کاملِ کسب‌وکار — `export` (JSONِ همه‌ی جدول‌های مستأجر، عمومی با تکیه بر RLS)، `import` (جایگزینیِ اتمیک)؛ فقط مالک |
| `subscription` | `/api/subscription` | وضعیت اشتراک کسب‌وکار (`can_write`, `days_left`, ...) |
| `calendar` | `/api/calendar-events` | تقویم و یادآوری — CRUD رویداد (list با بازه‌ی `from`/`to`، toggle-done، حذف) |
| `onboarding` | `/api` | ورودِ گروهیِ کالا/اشخاص (`/import/items`، `/import/contacts`) و مانده‌های اول دوره (`/opening-balances` + status). نامِ روتر تاریخی است؛ صفحه‌هایش زیرِ انبار/اشخاص/حسابداری‌اند |
| `installments` | `/api/installment-plans` | فروش اقساطی — قرارداد + زمان‌بندیِ اقساط، پرداختِ قسط (دریافتِ خودکارِ خزانه)، لغو |
| `contracting` | `/api/contracting` | پیمانکاری، هر پنج فاز — پیمان: ثبت، فهرست (فیلترِ وضعیت/طرف‌حساب)، تغییرِ وضعیت با گذارِ محدود؛ متممِ پیمان: ثبت (تغییرِ مبلغِ الحاقی + تاریخِ پایانِ تازه)، فهرست؛ صورت‌وضعیتِ دریافتی: ثبت (کسرِ سپرده/پیش‌پرداخت خودکار از رویِ پیمان + سایرِ کسورات)، فهرست؛ تسویه‌حسابِ پیمان: ثبت (جمعِ خودکارِ صورت‌وضعیت‌ها + سندِ حسابداریِ واقعی)، فهرست، ابطال؛ همه (فیلترِ `contract_id`)؛ فقط تسویه‌حساب سند می‌زند |

### ۴.۲ چند‌مستأجری + RLS (ستون امنیتی اصلی)

- هر جدولِ مستأجرمحور `tenant_id` دارد (`TenantMixin`). مستأجر جاری با
  `SET LOCAL`/`set_config(app.current_tenant, …, true)` روی **همان تراکنش** ست
  می‌شود — جزئیات و دلایل در [backend/app/tenant_context.py](backend/app/tenant_context.py).
- رویداد `before_flush` خودکار `tenant_id` را روی هر ردیف جدید مهر می‌زند؛ پس
  سرویس‌ها لازم نیست دستی بدهند.
- در پایگاه‌داده **FORCE ROW LEVEL SECURITY** روی ~۳۰+ جدول فعال است (لایه‌ی
  پشتیبان حتی اگر کد اشتباه کند). اسکریپت استقرار تعداد جدول‌های RLS‌دار را چک
  می‌کند (`>= 30`).
- زمینه از **Session** خوانده می‌شود نه فقط ContextVar، چون FastAPI اندپوینت‌های
  sync را در threadpool اجرا می‌کند.

### ۴.۳ RBAC (نقش‌ها و مجوز)

نقش‌های پیش‌فرض در [backend/app/models/user.py](backend/app/models/user.py):
`owner` (مدیر/مالک، مجوز `*`)، `accountant` (حسابدار)، `salesperson` (فروشنده/
صندوق‌دار)، `warehouse_keeper` (انباردار)، `payroll_officer` (حقوق)، `demo`
(فقط مشاهده — عمداً بدون wildcard). نقش روی **عضویت** (Membership) می‌نشیند نه
روی User، چون یک نفر می‌تواند در دو کسب‌وکار نقش متفاوت داشته باشد. بررسی مجوز
هم در سرور و هم در UI (`can()` در api.ts) انجام می‌شود؛ سرور مرجع است.

**ادمین پلتفرم:** جدا از RBAC مستأجر، با `PLATFORM_ADMIN_EMAILS` — فقط برای
کنترل‌پنل فروش خودِ کوبیتا (`/api/admin/purchases`). fail-closed (خالی = بسته).

### ۴.۴ نکات مهم دیگر

- **JWT:** توکن به یک مستأجر مشخص گره می‌خورد؛ `token_version` روی User با هر
  تغییر رمز یکی زیاد می‌شود و همه‌ی نشست‌های قبلی را باطل می‌کند.
- **Idempotency:** عملیاتِ سندساز `Idempotency-Key` می‌گیرند تا دوبار کلیک/retry
  دو سند نسازد (migration 0020).
- **اشتراک/Billing:** خرید پلن از سایت تجاری با **زرین‌پال**. `config.py` گاردِ
  fail-closed دارد: `ZARINPAL_SANDBOX=false` + `ENV≠production` = بالا نمی‌آید
  (چون یعنی با پول واقعی در محیط توسعه).
- **گاردهای config:** ENV نامعتبر، JWT ضعیف/کوتاه هنگام کار با پول واقعی → بالا
  نمی‌آید.

### ۴.۵ تاریخچه‌ی مهاجرت‌ها (سیر تکامل فیچرها)

`0001` پایه · `0002` انبار · `0003` بانک · `0004` حقوق · `0005` اتصال فروشگاه ·
`0006` مغایرت انبار · `0007` پیش‌فاکتور · `0008` برگشت فروش/خرید · `0009` انتقال
انبار · `0010` خطوط صورت‌حساب بانکی · `0011` بستن دوره · `0012` billing ·
`0013` خزانه/treasury · `0014` حذف wildcard نقش دمو · `0015` **چند‌مستأجری** ·
`0016` system_role حساب · `0017` حذف یکتاهای سراسری کهنه · `0018` توکن‌های
احراز + صندلی · `0019` اسناد قابل‌ابطال · `0020` کلید idempotency · `0021` دفتر
حسابرسی · `0022` اشتراک‌ها · `0023` **تقویم و یادآوری** (جدول `calendar_events`
مستأجرمحور + RLS) · `0024` **مالیات بر ارزش افزوده روی فاکتور** · `0025` مالیاتِ
برگشت‌ها · `0026` **دارایی‌های ثابت و استهلاک** · `0027` **بودجه‌بندی** (جدول
`budget_lines` مستأجرمحور + RLS) · `0028` **مراکز هزینه/پروژه** (جدول `cost_centers`
+ ستونِ `cost_center_id` روی `journal_lines`/`sales_invoices`/`purchase_invoices`) ·
`0029` **سامانه مؤدیان** (`moadian_settings` شاملِ کلید خصوصیِ امضا + `moadian_submissions`،
هر دو با RLS) · `0030` **تخفیف روی فاکتور** (`discount` روی ردیف‌ها و `total_discount`
روی سرِ فاکتورِ فروش و خرید) · `0031` **سقف اعتبار مشتری** (`credit_limit` روی
`contacts`؛ صفر = بدون سقف) · `0032` **انبارگردانی** (جدول‌های `stock_count_sessions`
و `stock_count_lines` مستأجرمحور + RLS) · `0033` **اسناد تکرارشونده** (جدول‌های
`recurring_journal_entries` و `recurring_journal_lines` مستأجرمحور + RLS) · `0034`
**چندارزی** (جدول‌های `currencies` و `exchange_rates` مستأجرمحور + RLS، و ستون‌های
`currency_code`/`exchange_rate` روی فاکتورها) · `0035` **تکمیل حقوق (عیدی/سنوات/مرخصی)**
(ستون‌های `min_base_wage`/`annual_leave_days` روی `payroll_settings` + جدول‌های
`leave_records` و `benefit_runs` مستأجرمحور + RLS) · `0036` **اتصالِ فروشگاهِ پرمستأجر**
(جدولِ `storefront_settings` + RLS؛ تنظیماتِ اتصال از `.env`ِ سراسری به سطحِ هر کسب‌وکار) ·
`0037` **باشگاه مشتریان / CRM** (جدول‌های `crm_leads`, `crm_activities`,
`crm_loyalty_transactions` مستأجرمحور + RLS؛ سرنخ/قیف، پیگیری و امتیازِ وفاداری) ·
`0038` **بارکدِ کالا برای صندوقِ فروشگاهی (POS)** (ستونِ `barcode` روی `items` + ایندکس؛
POS از همان مسیرِ فاکتورِ فروش استفاده می‌کند، جدولِ تازه ندارد) · `0039` **تولید و بهای
تمام‌شده** (جدول‌های `boms`, `bom_lines`, `production_orders`, `production_order_lines` +
شمارنده‌ی `production_order` با بک‌فیل) · `0040` **انبار پیشرفته** (جدول‌های `price_lists`,
`price_list_items`, `stock_batches` — لیستِ قیمت و ردیابیِ بچ/تاریخِ انقضا) · `0041`
**عمیق‌سازی** (`contacts.default_price_list_id` + جدولِ `loyalty_settings` — لیستِ قیمتِ
پیش‌فرضِ مشتری و کسبِ خودکارِ امتیاز هنگامِ فروش) · `0042` **هویتِ مالیاتیِ طرف حساب**
(ستون‌های `entity_type`/`national_id`/`economic_code`/`postal_code` روی `contacts` — برای
گزارشِ معاملاتِ فصلیِ ماده ۱۶۹؛ بدونِ جدولِ تازه) · `0043` **فروش اقساطی** (جدول‌های
`installment_plans` و `installments` مستأجرمحور + RLS + شمارنده‌ی `installment_plan` با بک‌فیل) ·
`0044` **نامِ مشتریِ دستی روی پیش‌فاکتور** (ستونِ `customer_name` روی `sales_quotations`؛ بدونِ جدولِ تازه) ·
`0045` **تخفیفِ کلِ فاکتور و گِرد کردنِ فروش** (ستون‌های `invoice_discount` و `rounding` روی `sales_invoices`؛
تخفیفِ کل هنگام ثبت در ردیف‌ها تسهیم می‌شود، رند به حسابِ نقشِ تازه‌ی `sales_rounding` می‌رود؛ بدونِ جدولِ تازه) ·
`0046` **تخفیفِ کلِ فاکتور خرید** (ستونِ `invoice_discount` روی `purchase_invoices`؛ همان تسهیم — اثر بر
ارزش‌گذاریِ موجودی و اعتبارِ مالیاتی؛ رند برای خرید عمداً نیست چون فاکتور عیناً از سندِ تأمین‌کننده ثبت می‌شود) ·
`0047` **آخرین ورودِ کاربر** (ستونِ nullableِ `last_login_at` روی `users`؛ در login/reset-password/accept-invite
پر می‌شود؛ برای «آخرین فعالیت» در پنلِ مدیریت؛ بدونِ جدولِ تازه) ·
`0048` **نقطه‌ی سفارشِ مجدد** (ستونِ `reorder_point` روی `items`، server_default=0؛ برای هشدارِ «نیازمندِ
سفارش»؛ بدونِ جدولِ تازه) ·
`0049` **فعال/غیرفعالِ حساب** (ستونِ `is_active` روی `accounts`، server_default=true؛ برای بایگانیِ حسابِ
دارای‌سند؛ بدونِ جدولِ تازه) · `0050` **فعال‌سازیِ شماره‌ی موبایل** (ستون‌های `users.phone_verified_at` و
`auth_tokens.attempts` برای کدِ پیامکی) · `0051` **نسخه‌ی آزمایشیِ ۱۴روزه** (ستون‌های `tenants.is_trial`/
`trial_started_at`/`trial_reminder_sent_at`) · `0052` **تأیید ایمیل هنگام ثبت‌نام** (جدولِ سراسریِ
`email_verification_codes` — کدِ ۶رقمی پیش از ساختِ حساب؛ کلیدش ایمیل است نه user_id، پس در `auth_tokens`
نمی‌گنجید — و ستونِ `users.email_verified_at`؛ verify-before-create) · `0053` **قیمت‌گذاریِ چنددوره‌ای**
(ستونِ `plans.prices` JSONBِ نگاشتِ دوره→قیمت + `purchases.billing_period`؛ ماهانه/شش‌ماهه/سالانه) ·
`0054` **فروشگاهِ بومیِ کوبیتا — جدول‌های پایه** (۷ جدولِ مستأجرمحور + RLS: `storefronts`, `item_storefront`,
`storefront_categories`, `storefront_customers`, `storefront_orders`, `storefront_order_lines`, `payment_gateways`؛
شروعِ فازِ ۰ سازنده‌ی فروشگاه — نقشه در STOREFRONT_PLAN.md؛ مدلِ اتصالِ بیرونیِ `storefront_settings`/0036 دست‌نخورده).
`0055`–`0057` **بازاریابیِ پیشرفته/وفاداری** · `0058`–`0061` **بازارِ B2B عمده‌فروشی** (۷ جدولِ سراسری + `Tenant.kind`) ·
`0062` **کمیسیونِ بازار** · `0063` **محدودیتِ سفارش + تقسیمِ نقد/اعتباری** · `0064` **کارتخوان/پرداختِ کارتی**
(۶ ستونِ متادیتای کارت روی `treasury_transactions` — `paid_via`/`reference_no(RRN)`/`trace_no`/`card_mask`/`terminal_no`/`psp`
+ ایندکسِ یکتای جزئیِ `(tenant_id, reference_no)`؛ جدولِ تازه‌ی `pos_terminals` مستأجرمحور + RLS؛ و `contacts.is_system`
برای طرف‌حسابِ سیستمیِ «فروشِ کارتیِ گذری»). ·
`0065` **گفتگوی بازار** (جدولِ سراسریِ `marketplace_messages` + دو ستونِ `*_last_read_at` روی `marketplace_connections`؛ چتِ متنیِ دوطرفه به‌ازای هر اتصالِ approved). ·
`0066` **گفتگوی زیرِ هر سفارش** (`marketplace_messages.order_id` + `connection_id` تهی‌پذیر + CheckConstraintِ «دقیقاً یک رشته»؛ دو ستونِ `*_last_read_at` روی `marketplace_orders`؛ رشته‌ی گفتگوی جدا برای هر سفارش).
`0067` **رفرش‌توکنِ نشست** (جدولِ سراسریِ `refresh_tokens`) · `0068` **توکنِ دستگاهِ Push/FCM** (جدولِ سراسریِ `device_tokens`) ·
`0069` **بارکدِ یکتای کالا** (ایندکسِ جزئیِ `uq_items_tenant_barcode` روی `(tenant_id, barcode)` where barcode not null؛ با پاک‌سازیِ تکراری‌های موجود) ·
`0070` **جداسازیِ بارِ ورودی + سریالِ کارتن** (ستون‌های `received_qty`/`unit_cost`/`source_type`/`source_id` روی `stock_batches`؛ جدولِ `stock_batch_serials` مستأجرمحور + RLS؛ ستونِ `stock_adjustments.batch_id`) ·
`0071` **زونِ مشتری + مرجوعیِ بازار + قیمتِ مصرف/تاریخِ بچ** (جدول‌های سراسریِ `marketplace_zones`/`marketplace_returns`/`marketplace_return_lines`؛ `marketplace_connections.zone_id`؛ `marketplace_settings.{return_policy,return_window_days,next_return_number}`؛ `marketplace_listings.consumer_price`؛ `stock_batches.{consumer_price,production_date}`) ·
`0072` **نقشِ «مامور حمل/انتقال» + گردشِ کارِ تحویل** (بک‌فیلِ نقشِ سراسریِ `delivery_agent` برای همه‌ی مستأجرها با `rls_disabled`؛ `marketplace_settings.require_delivery`؛ `marketplace_orders.{delivered_at,delivered_by_name}`).
`0073` **شخصی‌سازیِ پنل بر اساسِ صنف/شرکت** (`tenants.{industry,enabled_modules,granted_modules}`؛ بک‌فیل: حساب‌های دارای BOM/سفارشِ تولید گرنتِ `manufacturing` می‌گیرند تا دسترسی‌شان قطع نشود، با `rls_disabled` روی `boms`/`production_orders`).
`0074` **سال‌های مالی** (جدولِ `fiscal_years` مستأجرمحور؛ بازه، وضعیت، سالِ فعال) ·
`0075` **دسترسیِ ماژول‌به‌ماژولِ عضو** (`memberships.permissions` — نگاشتِ ماژول→کنش‌ها؛ NULL = پیروی از نقش) ·
`0076` **قاعده‌ی کدینگِ چارت** (`tenants.account_code_widths`: رقمِ افزوده در هر سطح؛ NULL = پیش‌فرضِ سرویس، پس هیچ کدِ موجودی نامعتبر نمی‌شود) ·
`0077` **«اتصال فروشگاه» ماژولِ محدود شد** (بک‌فیل: هر مستأجری که نشانه‌ی واقعیِ استفاده دارد — سایتِ منتشرشده/دامنه/build، کالای لیست‌شده، سفارشِ فروشگاهی، یا اتصالِ بیرونیِ تنظیم‌شده — گرنتِ `integration` می‌گیرد؛ با `rls_disabled` روی `storefront_settings`/`storefronts`/`item_storefront`/`storefront_orders`. `publishable_key` عمداً نشانه نیست چون با اولین بازکردنِ صفحه ساخته می‌شود).
`0078` **موجودیت‌های «شرکت»** (`contact_groups`/`geo_locations`/`related_persons` + دو ستونِ *اختیاریِ* `contacts.{group_id,geo_location_id}`؛ بدونِ بک‌فیل، پس هیچ طرف‌حسابِ موجودی نامعتبر نمی‌شود. `geo_locations.parent_id` عمداً `RESTRICT` و `related_persons.contact_id` عمداً `CASCADE`) ·
`0079` **گزارش‌ساز** (`saved_reports`: نام، منبع، `config` از نوعِ JSONB — فقط *تعریفِ* گزارش ذخیره می‌شود نه نتیجه‌اش).
(نسخه‌ی فعلی head = `0079`.)

---

## ۵. اپ دسکتاپ/وب — `desktop/`

**استک:** React 19 + TypeScript + Vite؛ Electron 42 (+ `vite-plugin-electron`)؛
`better-sqlite3` (DB محلی)؛ `electron-updater` (به‌روزرسانی خودکار)؛
`lucide-react`، `vazirmatn`. نسخه‌ی فعلی اپ: `1.1.0`.

**ورودی UI:** [desktop/src/App.tsx](desktop/src/App.tsx) → ورود/ثبت رمز →
`Dashboard`. ناوبری در [desktop/src/components/Sidebar.tsx](desktop/src/components/Sidebar.tsx):
داشبورد، فروش، خرید، اشخاص، انبار، حسابداری، چک و بانک، حقوق و دستمزد، اتصال
فروشگاه، گزارش‌ها، **تقویم و یادآوری** + (کاربران، راهنما) + (فقط ادمین پلتفرم:
خریدهای سایت تجاری). صفحه‌ی تقویم ([desktop/src/pages/CalendarPage.tsx](desktop/src/pages/CalendarPage.tsx))
تب «تقویم ماهانه» (شبکه‌ی شمسی با chipهای رنگیِ دسته) و تب «فهرست رویدادها» دارد؛
از `desktop/src/lib/jalali.ts` و `JalaliDatePicker` استفاده می‌کند و مثل بقیه‌ی
ماژول‌ها مسیرِ مستقیمِ API را می‌رود (روی هر دو پلتفرم یکسان).
صفحات در `desktop/src/pages/`، اجزای فرم/جدول در `desktop/src/components/`.

### ۵.۱ معماری آفلاین (فقط Electron)

- **Main process** [desktop/electron/main.ts](desktop/electron/main.ts): پنجره‌ی
  بدون فریم (نوار عنوان سفارشی)، منوی راست‌کلیک فارسی، IPC، auto-update.
- **DB محلی** `electron/db.ts` + **همگام‌سازی** `electron/sync.ts`: جدول‌های
  **cache** (accounts, warehouses, items, bank_accounts) و **outbox**
  (journal_entries, sales_invoices, purchase_invoices, checks).
- **جریان:** `pullAll` داده‌ی پایه را کش می‌کند؛ کاربر آفلاین سند/فاکتور می‌سازد
  که در outbox صف می‌شود؛ `pushOutbox` هنگام اتصال آن‌ها را به سرور می‌فرستد
  (با `server_id`/`server_number`/`sync_error`). قرارداد نوع‌ها در
  [desktop/src/electron.d.ts](desktop/src/electron.d.ts) (پل `window.cubita`).
- **نشستِ ورود** `electron/authSession.ts` (+ جدولِ `session` در `db.ts`):
  بارِ اولِ یوزر/پسورد کافی است — رفرش‌توکنِ ۶۰روزه‌ی چرخشی (همانی که
  `services/refresh.py` برای موبایل ساخته بود) روی دیسک ذخیره می‌شود و اپ از
  آن پس خودش وارد می‌ماند، آنلاین یا آفلاین. بازیابیِ بدو اجرا یک رفرشِ خاموش
  می‌زند: موفق → توکنِ تازه؛ **قطعیِ شبکه → نشستِ ذخیره‌شده دست‌نخورده برمی‌گردد**
  (هرگز پاک نمی‌شود)؛ فقط ۴۰۱ِ صریحِ سرور (رمز عوض‌شده/رفرشِ باطل) نشست را
  می‌بندد. جزئیات و دامنه‌ی این تصمیم: §۱۰، ردیفِ «ورودِ آفلاینِ اپِ دسکتاپ».
- **API_BASE_URL:** بسته‌شده → `https://acc.cubita.ir`؛ توسعه → `http://localhost:8000`
  (قابل override با `CUBITA_API_URL`). در renderer هم fallback مشابه در
  [desktop/src/api.ts](desktop/src/api.ts) (`VITE_API_URL` یا localhost:8000).
- **به‌روزرسانی:** `electron-updater` از `https://acc.cubita.ir/updates/`
  (تنظیمات در `desktop/package.json` بخش `build.publish`).

### ۵.۲ حالت وب (اپ آنلاین)

وقتی `isElectron` نادرست است، همان کامپوننت‌ها مسیر «مستقیم/زنده» را می‌روند:
`fetch*Live` / `create*Direct` در api.ts. یعنی **همان UI** بدون Electron هم کار
می‌کند و همان بک‌اند را صدا می‌زند.

---

## ۶. سایت معرفی — `website/`

React + Vite، صفحه‌ی فرود بازاریابی برای **cubita.ir** (RTL، فارسی، تیره).
اجزا در `website/src/components/` (Hero، Features، Pricing، FAQ، ...)، صفحات
حقوقی و نتیجه‌ی پرداخت در `website/src/pages/`. پس‌زمینه‌ی ساده‌ی آرام:
[website/src/components/SoftBackground.tsx](website/src/components/SoftBackground.tsx)
(هاله‌ی آبیِ کم‌رنگ روی رنگ تخت). فرم خرید مستقیم به `acc.cubita.ir` وصل می‌شود
(CSP سختگیرانه؛ بدون inline style/script). متغیرها: `VITE_API_BASE_URL`,
`VITE_DEMO_URL`.

---

## ۷. زیرساخت، دامنه‌ها و استقرار

**VPS:** `62.60.129.39` (root، رمز). سایت‌ها با nginx:

| دامنه | مسیر روی سرور | چیست |
|---|---|---|
| `cubita.ir` | `/opt/cubita-marketing/dist` | سایت معرفی (`website/`) |
| `acc.cubita.ir` | `/opt/hesabdari/web` + بک‌اند `:8001` | اپ اصلی (production) — **فقط دفترِ مشتری** |
| `admin.cubita.ir` | `/opt/cubita-admin/web` + پراکسیِ `/api/` به `:8001` | پنلِ مدیریتِ پلتفرم (`admin/`) |
| `demo.cubita.ir` | `/opt/cubita-demo/web` + بک‌اند `:8002` | نسخه‌ی دمو |

- **استقرار بک‌اند/اپ:** اسکریپت روی سرور [backend/scripts/deploy.sh](backend/scripts/deploy.sh)
  `{production|demo}` (پشتیبان‌گیری، توقف سرویس، استقرار کد+وب، مهاجرت، راستی‌آزمایی،
  برگشت خودکار).
- **استقرار اپِ ستاد:** [backend/scripts/deploy-admin-web.sh](backend/scripts/deploy-admin-web.sh) —
  فقط فایلِ ایستا (بی‌سرویس، بی‌دیتابیس، بی‌مهاجرت)، با گاردِ آدرسِ باندل **پیش از**
  لمسِ ریشه‌ی زنده و سه کاوش پس از آن. `web.old` نقطه‌ی برگشت است.
  vhostِ مرجع: [deploy/nginx/admin.cubita.ir.conf](deploy/nginx/admin.cubita.ir.conf).
  **هم‌مبدأ** است: باندل با `VITE_API_URL=https://admin.cubita.ir` ساخته می‌شود و
  nginx مسیرِ `/api/` را به همان `:8001` می‌فرستد — پس نه CORS لازم است، نه
  گاردِ ضدِجابه‌جاییِ `deploy.sh` کور می‌شود.
- **استقرار سایت معرفی:** build محلی `website/` → آرشیو → آپلود `/tmp` → جابه‌جایی
  داخل `/opt/cubita-marketing` (`dist.old` نقطه‌ی برگشت). جزئیات کامل + روش SSH با
  رمز در حافظه‌ی پروژه (`vps-deployment`).
- **فیدِ به‌روزرسانی:** `/opt/hesabdari/updates/` از `acc.cubita.ir/updates/` سرو می‌شود (nginx: `root /opt/hesabdari`).
  دسکتاپ = `latest.yml` + `Cubita-Setup*.exe`؛ **اندروید = `updates/android/latest.json` + `cubita-vN.apk`** (هر دو `latest.*`
  با قاعده‌ی no-cacheِ اختصاصی). آپلود با `scp … root@62.60.129.39:/opt/hesabdari/updates[/android]/`.
- **گواهیِ TLS — ⚠️ فقط DNS-01، چون HTTP-01 روی این سرور کار نمی‌کند.**
  سرور در تهران است (Hostiran، AS59441) و **پورت ۸۰ از خارجِ ایران timeout
  می‌دهد** در حالی که ۴۴۳ باز است. یعنی Let's Encrypt هرگز نمی‌تواند چالشِ
  HTTP-01 را بردارد. این را نه DNS خراب می‌کرد نه `ufw`/`fail2ban`/`ipset` —
  همه سالم بودند؛ با یک fetcherِ ثالثِ مستقل هم تأیید شد.
  **نشانه‌ی خرابی خاموش است:** `certbot` با `Timeout during connect (likely
  firewall problem)` شکست می‌خورد و تنها اثرش این است که چند هفته بعد سایت
  منقضی می‌شود. در ۱۴۰۵/۰۶/۳۰ هر چهار گواهیِ کوبیتا با ۲۳ روز اعتبارِ باقی‌مانده
  در همین وضعیت بودند و `acc.cubita.ir` از صدورش **هرگز** تمدید نشده بود.
  راهِ درست: `--dns-cloudflare` با اعتبارنامه‌ی `/etc/letsencrypt/cloudflare.ini`
  (توکنِ `Zone:DNS:Edit` روی زونِ `cubita.ir`، **بدونِ TTL و بدونِ فیلترِ IP** —
  دلیلش در [vhostِ مرجع](deploy/nginx/admin.cubita.ir.conf)).
  و **هوکِ `renewal-hooks/deploy/reload-nginx.sh` اجباری است**: با رفتنِ
  `installer = nginx` دیگر کسی بعد از تمدید ری‌لود نمی‌کند و nginx گواهیِ قدیمی
  را از حافظه سرو می‌کند.
- **CI:** `.github/workflows/ci.yml` فقط تست/بیلد، **بدون استقرار خودکار**.
- **پرداخت:** زرین‌پال با **مرچنتِ اختصاصیِ کوبیتا** (`50f3a131-…`) — روی prod در `/opt/hesabdari/.env`
  (`ZARINPAL_MERCHANT_ID`، `SANDBOX=false`، callback روی acc.cubita.ir، ریدایرکت روی cubita.ir). زنده و بارگذاری‌شده.
  مانده فقط یک تراکنشِ واقعیِ end-to-end برای اطمینانِ نهایی است (بند ۲ — [[commercialization-roadmap]]).
- **اتصال فروشگاه (پرمستأجر — `0036`):** تنظیماتِ اتصال (آدرس/ایمیل/رمز/cutover/فعال)
  در جدولِ `storefront_settings` به‌ازای هر کسب‌وکار است، نه `.env`ِ سراسری — پس هر کاربر
  فروشگاهِ خودش را وصل می‌کند. رمز راز است (RLS، فقط `has_password` برمی‌گردد؛ الگوی مؤدیان).
  حسابداری منبعِ حقیقتِ موجودی است: push موجودی را **مطلق set** می‌کند (سفارش‌های آینده
  دوبار کم نمی‌شوند)، و `cutover_order_id` سفارش‌های قدیمیِ پیش از اتصال را کنار می‌گذارد
  (سایت موقعِ ثبتِ سفارش خودش `stock -= qty` می‌کند). راه‌اندازیِ یک کسب‌وکار: کالاها را از
  سایت bootstrap کن (SKU `IPN-{id}`، از پیش نگاشت)، موجودیِ اولیه = موجودیِ فعلیِ سایت
  (بهای صفر)، سپس sync. UI: `IntegrationPanel` (فرم تنظیمات + جدول نگاشت + دکمه‌ی sync).
  کاربرِ ipnetcity.ir روی production راه‌اندازی و راستی‌آزمایی شد (۷۲ کالا، بدونِ آسیب به سایت).

## ۸. توسعه‌ی محلی

- Postgres محلی روی `:5432` (DB `hesabdari`)؛ Docker نصب نیست (compose فقط اگر
  خواستی). venv بک‌اند: `backend/venv/Scripts/python.exe`.
- **گاردِ sandbox:** `backend/.env` مقدار `ZARINPAL_SANDBOX=false` دارد که با
  `ENV=development` تعارض دارد؛ موقع اجرای محلی با متغیر محیطی `ZARINPAL_SANDBOX=true`
  override کن (فایل را تغییر نده).
- مهاجرت: `ZARINPAL_SANDBOX=true venv/Scripts/python.exe -m alembic upgrade head`
- ساخت حساب واقعی: `... -m app.seed <email> <password>` (ایمیل باید دامنه‌ی معتبر
  باشد؛ `.local`/`.test` را `EmailStr` رد می‌کند → 422).
- اجرای بک‌اند: `... -m uvicorn app.main:app --host 127.0.0.1 --port 8000`
- اجرای اپ: در `desktop/` → `npm run dev` (Vite :5173 + پنجره‌ی Electron؛ به
  localhost:8000 وصل می‌شود).
- **حساب تست محلی:** هر توسعه‌دهنده حسابِ خودش را با `app.seed` می‌سازد
  (راهنما در [CONTRIBUTING.md](CONTRIBUTING.md)). رمز اینجا نوشته نمی‌شود —
  مخزن مشترک است و تاریخچه‌ی گیت پاک نمی‌شود.

## ۹. وضعیت فعلی کار (git)

- **گیت یکدست شد (۱۴۰۵/۰۵/۰۳):** `master` با کلِ خطِ توسعه هم‌تراز است (۵۹+ کامیت،
  سرِ فعلی `4c42dd9`). working tree تمیز.
  **ریموت‌ها:** `origin` روی GitHub (کارِ مشترک، شاخه + Pull Request) و `vps`
  روی مخزنِ bareِ سرور (مسیرِ استقرار). گردشِ کارِ تیمی در [CLAUDE.md](CLAUDE.md).
- **prod = demo = repo روی مهاجرت `0046`** — مستقر و زنده (ارتقای فروش سپس خرید — بخشِ ۱۰).
  راستی‌آزماییِ عمومی: `alembic_version=0046`، health ok، RLS روی ۶۶ جدول، اندپوینت‌های تازه‌ی فروش و خرید
  (`/api/{sales,purchase}-invoices/summary`، `/returnable`، `/{sales,purchase}-returns/{id}/print`) روی هر دو دامنه ۴۰۱.
  **۸۲۳ تستِ بک‌اند سبز.** موبایل: ردیف‌های فاکتور روی `≤۶۴۰px` کارتِ برچسب‌دار می‌شوند ([[mobile-view-rules]]).
  - **رفعِ باگِ استقرار:** خطِ `ls -t backups/*.dump | head -1` در `deploy.sh` با انباشته‌شدنِ ده‌ها
    بکاپ زیرِ `pipefail` سیگنالِ SIGPIPE می‌گرفت و استقرار را «شکست‌خورده» اعلام می‌کرد؛ با `|| true`
    امن شد (هم در مخزن، هم روی `/opt/hesabdari/deploy.sh`ِ زنده).
- **۴ ماژولِ درخواستیِ کاربر (۱۴۰۵/۰۵/۰۷) — همه ✅:** (۱) CRM (۲) POS+بارکد (۳) تولید/BOM
  (۴) انبارِ پیشرفته (لیستِ قیمت + بچ/انقضا). سپس **عمیق‌سازی (`0041`):** لیستِ قیمتِ پیش‌فرضِ
  مشتری (اعمالِ خودکار در فاکتورِ فروش و صندوق) + کسبِ خودکارِ امتیازِ وفاداری هنگامِ فروش.
  همه مستقر روی prod و demo.
- **جمع‌بندیِ تکمیل (۱۴۰۵/۰۵/۰۳، به‌روز ۱۴۰۵/۰۵/۱۵):** موتور حسابداری کامل است. مواردِ باقی‌مانده تا لانچِ
  عمومی همگی بیرونی/تصمیمی‌اند: **مرچنتِ اختصاصیِ زرین‌پالِ کوبیتا روی prod ست و زنده شد** (`ZARINPAL_MERCHANT_ID=
  50f3a131-…`؛ فقط یک تراکنشِ واقعیِ end-to-end برای اطمینان مانده)، اعتبارنامه‌ی مؤدیان + تطبیق با اسپکِ جاری،
  پیامکِ بازیابیِ رمز + الگوهای مشتری، نمادِ اعتماد (اینماد)، و تصمیمِ ساختِ فایلِ نصبیِ دسکتاپ. کدِ ایمیل (SMTP)
  و اجرای پرداخت روی production فعال‌اند. فهرستِ کاملِ کارهای تجاری‌سازی: [[commercialization-roadmap]].

---

## ۱۰. تاریخچه‌ی ارتقاها (با هر تغییر مهم اینجا یک ردیف اضافه کن)

- **۱۴۰۵/۰۷/۰۲ (2026-09-24) — سربرگ و نوارِ پایینِ سند، ستون‌به‌ستون با گریدِ اکسلی + باگِ ناپدیدشدنِ ستونِ «حساب» (بدونِ مهاجرت):**

  آرش خواست بالا و پایینِ فرمِ سند با گریدِ اکسلی «هماهنگ و از دیدِ کاربر ستونی و منظم» باشد.
  - **یک جدول در سه تکه.** سربرگ (`.jh-bar`) و نوارِ پایین (`.jf-foot`) زبانِ گرید را گرفتند: هر فیلد/جمع یک
    «خانه» با سرستونِ خاکستریِ گرید (برچسب)، خطوطِ ظریفِ جدول میانِ خانه‌ها، کادرهای بی‌قاب و تمام‌خانه با قابِ
    accentِ خانه‌ی فعال؛ «موقت/دائم» دو نیمه‌ی یک خانه. «افزودن ردیف» ادامه‌ی قابِ گرید است (سطرِ «ردیفِ تازه»)،
    نه دکمه‌ی خط‌چینِ جدا. توکن‌های خط و سرستون (`--xl-line`، `--xl-head`) یک‌جا برای هر سه.
  - **هم‌خطیِ ستونی** (`lib/alignToGrid.ts`، `useAlignToGrid`): هر دو شبکه‌ی پنج‌خانه‌ای‌اند با مرزهای همان
    ستون‌های گرید — ردیف+حساب | شرح ردیف | بدهکار | بستانکار | آیکون‌ها. سربرگ: «شرح سند» روی دو خانه‌ی اول،
    «تاریخ» روی بدهکار، «وضعیت» روی بستانکار، «شماره فرعی» روی آیکون‌ها. پایین: «ثبت سند» وسطِ خانه‌ی اول،
    «وضعیتِ توازن» زیرِ شرح، و **هر جمع دقیقاً زیرِ ستونِ خودش** (`subgrid`). عرض‌ها از سرستون‌های گرید سنجیده
    می‌شوند (ResizeObserver) و با کشیدنِ ستون یا آمدنِ مرکز هزینه جابه‌جا می‌شوند؛ روی `style` نوشته می‌شوند،
    نه state. گریدی که افقی می‌لغزد → نسبت‌های پیش‌فرض. سنجیده شد: اختلافِ لبه‌ها ۰px در ۱۴۴۰/۱۲۰۰/۱۰۰۰.
    موبایل چیدمانِ دوستونه‌ی خودش را دارد. حالتِ ساده گرید ندارد و نوارش هم‌خط نمی‌شود.
  - **باگ: ستونِ «حساب» در اپِ ویندوز صفر شد** و فلشِ کادرش روی شماره‌ی ردیف افتاد. علت: درصدهای ذخیره‌شده
    نسبت به عرضِ جدول‌اند ولی ستون‌های ثابت پیکسلی؛ سهم‌هایی که در پنجره‌ای جا می‌شدند، در پنجره‌ی باریک‌تر
    (یا ذخیره‌ی نسخه‌های آزمایشیِ پیش از «جا در قاب» — شرح ۷۲٪) همه‌ی جا را می‌گرفتند. حالا `fitShares` در
    **زمانِ نمایش** ستون‌های درصدی را به یک نسبت کوچک می‌کند تا کشسان کمینه‌اش (۱۶۰px) را داشته باشد؛ ذخیره‌ی
    کاربر دست نمی‌خورد. ذخیره‌ای که برای ستونِ ثابت یا کشسان هم سهم دارد (قالبِ نسخه‌های آزمایشی) کنار می‌رود.

- **۱۴۰۵/۰۷/۰۳ (2026-09-25) — جدولِ اکسل‌مانند: گریدِ ثبتِ سند و فهرستِ «اسناد حسابداری» (بدونِ مهاجرت؛ سه پارامترِ تازه‌ی API):**

  آرش جدولی به حس‌وحالِ اکسل و نرم‌افزارهای مالیِ کلاسیک خواست؛ تصمیمِ او: «هر دو» جدول.
  - **ظاهرِ مشترک (`.xl-grid`):** سرستونِ خاکستری، خطوطِ ظریفِ افقی و عمودی، hoverِ ملایم، ستونِ شماره‌ی
    ردیف مثلِ سرستونِ ردیفِ اکسل، اعدادِ راست‌چین و tabular. همه از توکن‌های پوسته — تیره هم درست.
  - **عرضِ ستون، «جا در قاب»** (`lib/useColumnWidths.ts`، `ColResizer`): جدول **همیشه هم‌عرضِ قاب** است
    و کشیدن هرگز اسکرولِ افقی یا فضای سفید نمی‌سازد. کشیدنِ لبه‌ی میانِ دو ستون فقط همان دو را عوض
    می‌کند (یکی پهن‌تر، هم‌سایه به همان اندازه باریک‌تر؛ `dragBoundary`، کمینه ۴۸px). شماره‌ی ردیف و
    ستونِ آیکون‌های کنش **ثابت‌اند** (خواسته‌ی آرش) و لبه‌ی کنارشان کشیدنی نیست؛ «حساب» (فهرست: «شرح»)
    کشسان است و باقی را می‌گیرد، پس با تغییرِ پنجره هم جدول پر می‌ماند. بقیه درصدی ذخیره می‌شوند (روی
    دستگاه؛ دوبار کلیک پیش‌فرض). کفِ گرید ۷۲۰px (قبلاً ۹۰۰ — در ۱۲۰۰px با کارت‌های کناری اسکرول می‌ساخت).
    مسیرِ رسیدن به این: جدول = جمعِ ستون‌ها فضای سفید می‌گذاشت؛ پرکردنش با «حساب» ستون‌های میانی را
    جابه‌جا می‌کرد («همه به‌هم می‌ریزد»). دستگیره تمامش درونِ سرستونِ خودش است؛ عنوانِ باریک سه‌نقطه.
  - **انتخابِ ردیف** (`lib/rowSelection.ts`): کلیک / Ctrl / Shift روی شماره، و نوارِ وضعیتِ اکسلی
    (`SelectionBar`): تعداد و جمع.
  - **گریدِ ثبتِ سند:** خانه‌ها بی‌قاب و تمام‌خانه، خانه‌ی فعال قابِ accent؛ جمعِ بدهکار/بستانکارِ
    انتخاب‌شده‌ها و **حذفِ دسته‌ای** (دکمه یا Ctrl+Delete؛ `ops.removeRows`، کفِ دو ردیف با ردیفِ خالی)؛
    Esc لغو. **جست‌وجوی سریع** سرِ کارت (Ctrl+F، `ops.findLines`): حساب، شرح یا مبلغ (رقمِ فارسی و
    جداکننده هم)؛ یافته‌ها برجسته، بقیه کم‌رنگ — **پنهان نمی‌شوند** تا ناوبریِ شاخص‌محورِ گرید سالم بماند؛
    Enter/Shift+Enter بینشان، Esc به همان ردیف.
  - **فهرستِ اسناد:** ردیفِ فیلتر زیرِ سرستون‌ها — شماره (`entry_from=entry_to`)، عطف، فرعی، شرح، منشأ،
    وضعیت — **همه سمتِ سرور** (قاعده‌ی §۷ِ صفحه‌ها)؛ API سه پارامترِ ستونیِ تازه گرفت: `atf` (دقیق)، `sub`
    و `desc` (جزئی) روی `/api/journal-entries` و `/summary`، با «و» میانِ هم. رنگِ ملایمِ وضعیت: موقت
    ته‌رنگ و نوارِ کهربایی، دائم نوارِ سبز، باطل نوارِ خاکستری. «وضعیت»ِ نوارِ بالا حالا فقط موبایل
    (`xl-narrow-only`) — روی دسکتاپ فیلترِ سرستون است.
  - تست: pytest ۲ تستِ تازه (۳۹۷۲)؛ vitest ۵۴۸/۵۴۸ (`rowSelection`، `findLines`/`removeRows`،
    `api.journalQuery`، `JournalGrid.select`)؛ E2E سبز؛ مرورگر ۱۴۴۰/۳۹۰، روشن و تیره.

- **۱۴۰۵/۰۷/۰۳ (2026-09-25) — کارت‌های «عملیات» و «فهرست»: جابه‌جاییِ منوها با فلشِ بالا/پایین (بدونِ مهاجرت):**

  آرش خواست کاربر ترتیبِ منوهای این دو کارت را خودش بچیند.
  - هر ردیف (`MenuItem` در `ModulePanels.tsx`) با hover یا فوکوس دو فلشِ کوچکِ ↑/↓ روی لبه‌اش نشان
    می‌دهد؛ **Alt+↑/↓** روی خودِ منو همان کار را می‌کند و فوکوس روی منو می‌ماند. فلش‌ها `tabIndex=-1`اند
    تا ایستگاه‌های Tab سه برابر نشوند. دستگاهِ بی‌hover همیشه می‌بیندشان.
  - هر فهرستِ منو «دامنه»ی خودش را دارد (`ops:<گروه>`، `list:<گروه>`، `pages:<گروه>`،
    `sec-ops:<صفحه>`، `sec-list:<صفحه>`) و جابه‌جایی فقط داخلِ همان است.
  - ترتیب **روی دستگاه** (`localStorage`: `cubita.menuOrder`) — همان تصمیمِ میان‌برها و جمع‌بودنِ
    کارت‌ها. `lib/menuOrder.ts`: منوی تازه (نسخه‌ی بعد، ماژولِ تازه‌روشن) ته فهرست می‌آید و ناپدید
    نمی‌شود؛ کلیدِ ماژولِ خاموش با جابه‌جایی پاک نمی‌شود.
  - «ترتیبِ پیش‌فرض» ته هر کارت، فقط وقتی ترتیب عوض شده — بیرون از بدنه‌ی اسکرول‌خور تا گم نشود.
  - تست: `menuOrder.test.ts` (۷)، `ModulePanels.order.test.tsx` (۴). vitest ۵۳۰/۵۳۰.

- **۱۴۰۵/۰۷/۰۳ (2026-09-25) — ثبتِ سند: فوترِ نهایی، پنجره‌ی میان‌برها، عرضِ ستون‌ها، جست‌وجوی درجای حساب (بدونِ مهاجرت):**

  دورِ سومِ همان صفحه، به فهرستِ ایرادهای نهاییِ آرش.
  - **نوارِ پایین:** راست دکمه‌ی «ثبت سند (Ctrl+S)» — متنِ دکمه، نه چیپِ قاب‌دار که دکمه‌ی دوم
    به‌نظر می‌آمد؛ چپ جمعِ بدهکار/بستانکار و «وضعیتِ توازن: متوازن / نامتوازن» (با مبلغِ اختلاف).
  - **راهنمای میان‌برها از زیرِ گرید رفت:** دکمه‌ی «میان‌برها» سرِ کارت یا Ctrl+/ یک پنجره
    (`.modal-card`ِ موجود) باز می‌کند؛ Esc می‌بندد و فوکوس به همان خانه برمی‌گردد. Ctrl+S از داخلِ
    پنجره سند نمی‌فرستد.
  - **نوارِ «رفتن به ردیف … n ردیف» برداشته شد** (شمارش همان نشانِ «ردیف معتبر» است). Ctrl+G کادرش
    را شناور روی سرستون باز می‌کند؛ Enter می‌پرد، Esc برمی‌گرداند.
  - **عرضِ ستون‌ها:** `table-layout: fixed` + `<colgroup>` با درصد — حساب باقی را می‌گیرد، مبلغ‌ها
    ۱۶٪، شرح ۱۱٪، مرکز ۱۴٪؛ شماره و کنش‌ها ثابت. قاعده‌ی عمومیِ `.ef-form td > input {min-width:120px}`
    کادرِ شرح را از خانه‌اش بیرون می‌زد و به بدهکار می‌چسباند؛ در گرید خنثی شد. همه‌ی کادرها حالا
    دقیقاً ۸px از لبه‌ی خانه‌اند (سنجیده در ۱۲۸۰/۱۴۴۰/۱۹۲۰).
  - **خانه‌ی حساب کادرِ جست‌وجوی درجاست (`AccountCombo`، تازه):** تایپِ کد یا نام در **همان خانه**
    فهرست را زیرش فیلتر می‌کند (`matchRank`)، ↑/↓، **Enter انتخاب می‌کند و مستقیم به بدهکار می‌رود**،
    Tab پس از تایپ هم برمی‌دارد، Esc/بیرون‌رفتن حسابِ قبلی را برمی‌گرداند. جلو رفتن بعد از انتخاب
    در اثرِ گرید و با وضعیتِ **تازه** است (حسابِ تازه ممکن است ستونِ تفصیلیِ اجباری بیاورد).
    `SearchSelect` برای تفصیلی/مرکز و بقیه‌ی برنامه دست نخورد.
  - **مسیرِ Enter حالا از شرحِ ردیف هم می‌پرد** (مثلِ مرکزِ هزینه از §۱۵): حساب → [ارز] → [تفصیلی]
    → بدهکار → بستانکار. شرح با Tab/←/→/موس. E2E به‌روز شد و ستون‌ها را از خودِ صفحه می‌خواند
    (با `E2E_LATE_FRAMES_MS=300` هم سبز).
  - **خانه‌های جمع و توازن اندازه‌ی ثابت دارند** (۱۶۸/۱۶۸/۲۰۰ × ۶۶؛ موبایل ستونِ برابر × ۶۰) و با تایپ
    نمی‌لرزند: عددی که جا نشود به نسبت کوچک می‌شود (`lib/useFitText.ts`: `scrollWidth`/`clientWidth` →
    `--fit`، کفِ خوانایی ۰٫۵۵). سنجیده از ۰ تا ۱۵ رقم در ۱۴۴۰ و ۳۹۰: اندازه‌ی خانه‌ها و نوار ثابت.
  - تست: `JournalGrid.account.test.tsx` (۱۰)، `useFitText.test.ts` (۴)، به‌روزرسانیِ enter/memory/footer.
    vitest ۵۱۹/۵۱۹.

- **۱۴۰۵/۰۷/۰۳ (2026-09-25) — ثبتِ سند: نوارِ پایانیِ شناور، راهنمای تاشو، کنش‌های ردیف روی hover (بدونِ مهاجرت):**

  دنباله‌ی ردیفِ پایین. آرش خواست پایینِ صفحه از راهنما خالی و از جمع‌ها پُر شود، و ستون‌ها خلوت شوند.
  - **نوارِ پایانی (`JournalFooter`، جای `ActionBar`ِ عمومی، هر دو حالت):**
    - عددها درشت (`--fs-xl`)؛ اختلاف یک «چیپ» خاکستری/سبز/قرمز؛ رنگِ توازن روی لبه‌ی بالای **کلِ نوار**؛
    - دکمه‌ی ثبت ۴۸ پیکسلی و برجسته، با «Ctrl+S» کنارش (فقط حالتِ حسابدار؛ `aria-keyshortcuts`).
    - **باگِ چسبیدن رفع شد:** نوارِ چسبنده در paddingِ پایینِ `.app-content` (۲۸px) می‌ایستاد و ردیف‌ها
      از زیرش پیدا بودند. حالا `bottom: -28px` است و به لبه‌ی پنجره می‌چسبد؛ `scroll-state` گوشه‌های
      پایین را وقتِ چسبیدن صاف می‌کند. **فرم‌های دیگری که `ActionBar` دارند هنوز همان فاصله را دارند.**
  - **Ctrl+S از هر جای فرمِ حسابدار** (قبلاً فقط داخلِ خانه‌های گرید؛ در سربرگ مرورگر «ذخیره‌ی صفحه» باز
    می‌کرد). گرید همان کلید را زودتر می‌گیرد؛ فرم با `defaultPrevented` دوباره نمی‌فرستد — تستی که
    POSTها را می‌شمارد با برداشتنِ این شرط قرمز می‌شود.
  - **راهنمای میان‌برها (`ShortcutGuide`)** جای نوارِ ده‌تاییِ همیشه‌باز: یک خطِ کم‌رنگ با سه کلیدِ اصلی؛
    با کلیک یا **Ctrl+/** چهار دسته (حرکت، ورود و انتخاب، ردیف‌ها، سند) باز می‌شود و در سندِ بلند به دید
    می‌آید. باز/بسته در `localStorage` (`cubita.journal.shortcutsOpen`). در موبایل پنهان.
  - **کنش‌های ردیفِ گرید** (تکرار، کپی از قبلی، حذف) فقط روی ردیفِ زیرِ ماوس یا ردیفِ فوکوس‌دار دیده
    می‌شوند (`opacity`، تا ستون‌ها نپرند)، بی‌قاب و ۲۸ پیکسلی. دستگاهِ بی‌hover همیشه می‌بیندشان. جدولِ
    حالتِ ساده (یک دکمه‌ی حذف) دست نخورد.
  - تست: `JournalEntryForm.footer.test.tsx` (۶). vitest ۵۰۰/۵۰۰؛ مرورگر در ۱۴۴۰ و ۳۹۰، روشن و تیره.

- **۱۴۰۵/۰۷/۰۳ (2026-09-25) — ثبتِ سند: ارگونومیِ صفحه‌کلیدمحور در حالتِ حسابدار (بدونِ مهاجرت):**

  آرش خواست صفحه‌ی سند به استانداردِ نرم‌افزارهای مالیِ حرفه‌ای برسد. گرید از UI-01 بیشترِ
  پایه را داشت (Enter، ↑/↓، Ctrl+Enter/S/D/G، باقی‌مانده با Enter). آنچه اضافه شد:
  - **سربرگِ فشرده (`JournalHeaderBar`):**
    - شرح، تاریخ، وضعیت و شماره‌ی فرعی در **یک ردیف**، با برچسبِ ریز و کنترلِ ۳۶ پیکسلی
      (همان `--ef-h`ِ فرم‌کیت، فقط این‌جا کوچک‌تر).
    - مرکز هزینه، تفصیلیِ سایر و ارز در ردیفِ دومِ کوچک، فقط اگر تعریف شده باشند.
    - سربرگ و گرید حالا **یک کارت‌اند**. ارتفاعِ سربرگ از حدودِ ۳۰۰ به حدودِ ۱۵۰ پیکسل رسید.
    - «موقت/دائم» دو دکمه‌ی کنارِ هم است (radiogroup با ←/→)، و «دائم» رنگِ خودش را دارد.
  - **نوارِ جمع و توازن (`BalanceSummary`)** در نوارِ چسبنده‌ی پایین، در هر دو حالت:
    - سه خانه: جمعِ بدهکار، جمعِ بستانکار، و اختلاف با اینکه کدام طرف بیشتر است؛
    - رنگ: سبز متوازن، قرمز نامتوازن، خاکستری خالی؛
    - `aria-live` روی اختلاف.
  - **کلیدهای تازه‌ی گرید:**
    - **Tab/Shift+Tab** خانه‌به‌خانه، بی سه دکمه‌ی کنشِ ردیف. در انتهای ردیفِ آخرِ پر، ردیفِ
      تازه می‌سازد. اگر ردیفِ آخر خالی است، به «ثبت سند» می‌رود تا کاربر در گرید گیر نکند.
    - **←/→** با قاعده‌ی Excel: خانه‌ی تازه‌رسیده (مقدارِ کاملاً انتخاب‌شده) یا خالی یعنی حرکت،
      و ← در راست‌به‌چپ خانه‌ی بعد است. وقتی مکان‌نما داخلِ متن است، پیکان مالِ فیلد است تا
      لبه‌ی متن (`arrowLeavesField`). این همان تعارضی است که §۱۲ به‌خاطرش چپ/راست را کنار
      گذاشته بود.
    - **F2** حالتِ ویرایش است.
    - **F4** فهرستِ حساب‌های همین ردیف را باز می‌کند. از سربرگ، اولین ردیفِ بی‌حساب را.
    - **Alt+↓** انتخاب‌گرِ همین خانه را باز می‌کند.
  - **راهنمای میان‌برها** همیشه زیرِ گرید دیده می‌شود (به‌جای `title`ِ پنهانِ «میان‌برها»). در
    موبایل پنهان است. `ShortcutsPage` هم پنج ردیفِ تازه گرفت.
  - **جست‌وجوی هوشمندِ `SearchSelect`** (همه‌جا، نه فقط حساب): نتیجه‌ها رتبه می‌گیرند
    (`matchRank`). اول برچسبی که با عبارت شروع می‌شود (کد: «۱۱» ← ۱۱۰۱)، بعد ابتدای یک
    واژه (نام: «بان» ← بانک)، بعد بقیه. پیش‌تر ترتیبِ اصلی می‌ماند و با سقفِ ۸۰ نمایش،
    حسابِ درست ممکن بود بیرون بیفتد.
  - **میان‌برِ ثبت همان Ctrl+S ماند** و Ctrl+Enter همان «ردیفِ تازه». هر دو از UI-01 جا
    افتاده‌اند و E2E رویشان است. عوض‌کردنِ Ctrl+Enter عادتِ کاربرانِ فعلی را می‌شکست.
  - **تست:** ۹ تستِ jsdomِ تازه (`JournalGrid.keys.test.tsx`)، ۱۱ تستِ ناوبری، ۴ تستِ رتبه.
    vitest ۴۹۴ سبز. تستِ حالت‌ها به نوارِ تازه به‌روز شد.
    - `jsdom` در `package.json` بود ولی روی این رایانه نصب نبود. پس تست‌های jsdomِ گرید پیش‌تر
      این‌جا اجرا نمی‌شدند و شمارشِ vitestِ گزارش‌های قبلیِ من ناقص بود.
  - **در مرورگر، فقط با کیبورد، سنجیده شد (۱۴۴۰ و ۳۹۰):** جست‌وجوی کد، Tab به ردیفِ بعد، ←، F4،
    نوارِ قرمز و سبز. سرریزِ افقی صفر، بی خطای کنسول.

- **۱۴۰۵/۰۷/۰۳ (2026-09-25) — «کوبیتا سازمانی» مستقر و منتشر شد (۱٫۷٫۰):**

  - **production:** `deploy.sh production` از `e8fbd88` (مهاجرتِ ۰۱۸۴ ← ۰۱۸۵، M3 تا M6، و
    #۱۹۱/#۱۹۵ که از استقرارِ قبلی مانده بودند).
    - کلیدِ امضای `edea436f` فعال است.
    - `admin.cubita.ir` صفحه‌ی مجوزها را دارد.
    - demo با `rsync` تازه شد.
  - **کانالِ امضاشده‌ی سازمانی زنده است:** `acc.cubita.ir/updates/enterprise/`.
    - نصابِ ۱۹۷MB به‌همراهِ `latest.yml` و امضایش.
    - از بیرون سنجیده شد: امضا پذیرفته می‌شود و نسخه‌ی دست‌کاری‌شده رد می‌شود.
  - **دو رفعِ کوچک:**
    - `build_server_bundle.py` دیگر با خروجیِ هدایت‌شده روی پیامِ فارسی نمی‌افتد.
    - مهاجرتِ ۰۱۵۹ روی دیتابیسِ خالی دیگر «*** … بررسی کنید ***» چاپ نمی‌کند. این پیام در
      پنجره‌ی نصابِ سرورِ تازه جلوی مشتری می‌آمد. رفتارِ مهاجرت عوض نشده، فقط شرطِ پیام.
  - **مانده:**
    - چک‌لیستِ VM (بندهای ۱ تا ۱۱ در ENTERPRISE_PLAN.md)؛
    - گواهیِ امضای کد (SmartScreen)؛
    - #۱۹۶ (باگِ #۱۹۱) در این استقرار نیست.

- **۱۴۰۵/۰۷/۰۲ (2026-09-24) — وضعیتِ ذخیره‌شده‌ی صفحه‌ها به‌ازای کسب‌وکار:**
  در بازبینیِ UI-02 (#191) پیدا شد. «مرور حساب‌ها» وضعیتش را با کلیدِ **سراسریِ**
  `cubita.accountBrowser.v1` در `sessionStorage` نگه می‌داشت: حسابِ انتخاب‌شده، گره‌های باز،
  بازه و فیلترهای مرکز هزینه و تفصیلی. تعویضِ کسب‌وکار (`TenantSwitcher`) فقط
  `window.location.reload()` می‌زند و `sessionStorage` بعد از بارِ دوباره می‌ماند. پس بعد از
  تعویض، شناسه‌ی مرکز هزینه‌ی کسب‌وکارِ قبلی به سرور می‌رفت و **درخت بی‌صدا خالی می‌آمد**.
  حافظه‌ی شرحِ سندِ UI-01 (`cubita.journal.descriptions`) هم همین ایراد را داشت و شرح‌های
  کسب‌وکارِ دیگر را پیشنهاد می‌داد.
  - `lib/tenantScope.ts`: `tenantKey(base)` کلید را `base:{tenant_id}` می‌کند. بی کسب‌وکار
    (پیش از ورود) `null` است و چیزی نه خوانده می‌شود نه نوشته.
  - `App` کسب‌وکارِ جاری را **هنگامِ رندر** از `me.tenant_id` می‌گذارد، نه در افکت، چون
    صفحه‌ی فرزند وضعیتش را در مقدارِ اولیه‌ی `useState`ِ همان رندر می‌خوانَد. این راه هم در وب
    کار می‌کند و هم در Electron. خواندنِ `tid` از توکن در Electron شدنی نبود، چون آن‌جا توکن
    در `localStorage` نیست.
  - تست‌ها: برگشت‌نیامدنِ وضعیت و شرح‌ها بعد از تعویضِ کسب‌وکار، و برگشتنشان با برگشت به
    کسب‌وکارِ اول. آزمونِ جهشی (کلیدِ بی‌کسب‌وکار) ۳ تست را قرمز می‌کند.

- **۱۴۰۵/۰۷/۰۲ (2026-09-24) — «کوبیتا سازمانی» M6: کدِ دعوت/بازنشانی، انتقال از ابر، پشتیبانِ شبانه، زیپِ عیب‌یابی (بدونِ مهاجرت):**

  **کشف:** سرورِ سازمانی ایمیل ندارد، پس دعوتِ کارمند به هیچ‌جا نمی‌رسید و «فراموشیِ رمز»
  راهی نداشت. حالا:
  - دعوت و بازنشانی در سازمانی یک **کدِ ۱۶ نویسه‌ای** می‌دهند. همان `auth_tokens` است، کوتاه و
    قابلِ‌تایپ (`tokens.canonical`).
  - مالک کدِ بازنشانیِ کارمند را از «کاربران» می‌سازد (`POST /api/members/{id}/reset-code`،
    فقط سازمانی، در دفترِ حسابرسی ثبت می‌شود).
  - کارمند در صفحه‌ی ورود «کدِ دعوت یا بازنشانی دارم» می‌زند (`POST /api/auth/redeem-code`).
  - مالکِ رمزفراموش‌کرده روی خودِ سرور `cubita-server reset-owner-password` را اجرا می‌کند.

  مالک هرگز رمزِ کسی را نمی‌بیند. در ابر هیچ‌کدام از این مسیرها نیست (۴۰۴) و رفتارِ دعوتِ
  ایمیلی دست نخورده است.

  **انتقال از ابر:** خروجیِ `/api/backup/export` (هر دو نسخه) حالا `users` و `tenant_settings`
  هم دارد، **بی hashِ رمز**. ورودیِ سازمانی کاربرانِ غایب را با همان شناسه می‌سازد، اعضا را
  **غیرفعال** می‌آورد (صندلی نمی‌گیرند)، و مالکِ هم‌ایمیل را به حسابِ محلی نگاشت می‌کند
  (۱۰۱ ستونِ FK به `users` بازنویسی می‌شوند). فایلِ قدیمی با پیامِ روشن رد می‌شود.
  **تا بک‌اندِ ابری مستقر نشود، خروجیِ ابری این بخش‌ها را ندارد.**

  **نگهداری (`app/onprem/maintenance.py`، روترِ `enterprise_maintenance`):**
  - `BackupScheduler` داخلِ `cubita-server serve` هر ۲۴ ساعت `pg_dump` می‌گیرد و ۱۴ تای آخر را
    نگه می‌دارد.
  - برای مالک نوارِ قرمز می‌آید، اگر آخرین پشتیبان بیش از ۴۸ ساعت پیش بوده، هیچ پشتیبانی
    نیست، یا آخرین تلاش شکست خورده.
  - کارتِ «پشتیبان و عیب‌یابیِ سرور» زیپِ عیب‌یابی را هم می‌دهد، بی `.env` و رازها و با
    پاک‌کردنِ رمز و JWT از لاگ‌ها. همین از خطِ فرمان: `cubita-server backup` و
    `cubita-server diagnostics`.
  - صفحه‌ی «مجوز و به‌روزرسانی» حالا «سرور و مجوز» نام دارد.

  ۱۹ تستِ تازه، به‌اضافه‌ی تستِ یکپارچه با Postgres 17ِ واقعی: کدِ مالک روی کسب‌وکارِ واقعی، و
  دامپِ شبانه‌ای که `pg_restore` می‌خواند. رابط در ۱۴۴۰ و ۳۹۰ سنجیده شد. جزئیات در
  ENTERPRISE_PLAN.md، «نتایجِ M6».

  **کلیدِ امضای تولید ساخته شد:** `kid` `edea436f`، روی سرورِ ابری در
  `/opt/hesabdari/secrets/license-signing.pem` (فقط کاربرِ سرویس). کلیدِ عمومی در `keys.py` و
  `updateKeys.ts` است. `LICENSE_SIGNING_KEY_FILE` در `.env`ِ production است و با اولین
  استقرار فعال می‌شود. از این به بعد مجوزِ صادرشده از پنلِ ستاد معتبر است.

- **۱۴۰۵/۰۷/۰۲ (2026-09-24) — دفتر روزنامه: چهار باگ (قدمِ مستقلِ UI-03):**
  ممیزیِ UI-03 چهار ایراد در تبِ «دفتر روزنامه»ِ «گزارش دفتر» پیدا کرد.
  1. **سقفِ بی‌صدای ۲۰۰ سند.** `DaybookCard` با `limit: 200` می‌گرفت و بقیه را نمی‌آورد. فهرست
     از جدید به قدیم است، پس در بازه‌ی یک‌ساله **ماه‌های اولِ سال** دیده نمی‌شدند و هیچ هشداری
     هم نبود.
  2. **جمعِ جعلی.** «جمعِ گردشِ بازه» جمعِ همان ۲۰۰ سند بود، یعنی همان چیزی که §۳۶ِ UI-03 منع
     کرده.
  3. **فیلترها وصل نبودند.** نوارِ فیلترِ مشترک روی سه تبِ دیگر بود ولی روی روزنامه نه، و
     `GET /api/journal-entries` هم شماره‌ی سند، مرکز هزینه و تفصیلی را نمی‌شناخت.
  4. **برچسبِ دروغ.** جست‌وجو «نام یا کدِ حساب» می‌گفت، ولی سرور روی شماره، عطف، شماره‌ی فرعی
     و شرحِ سند می‌گردد. جست‌وجوی حساب بی‌صدا چیزی پیدا نمی‌کرد.

  **سرور (بدونِ مهاجرت):**
  - `list_entries` حالا همان `report_filters`ِ دفتر و تراز را می‌گیرد و از همان
    `apply_report_filters` رد می‌شود، نه نسخه‌ی دومی از آن. نام‌های پارامتر همان‌اند
    (`date_from`، `entry_from`، `status`، `cost_center_id`، `analytic_id`،
    `include_system_entries`، …)، پس فراخوان‌های قبلی دست نمی‌خورند.
  - سند وقتی می‌آید که **دست‌کم یکی** از ردیف‌هایش از فیلتر رد شود، و کامل نشان داده می‌شود.
  - `GET /api/journal-entries/summary` تازه است: شمارِ سندها و ردیف‌ها و جمعِ بدهکار و
    بستانکارِ **کلِ دامنه**. با فیلترِ مرکز هزینه یا تفصیلی فقط ردیف‌های منطبق شمرده می‌شوند،
    یعنی همان عددی که `get_balances` با همان فیلتر می‌دهد. یک تست همین تطبیق را قفل می‌کند.
    مسیرش پیش از `/{entry_id}` تعریف شده، وگرنه «summary» شناسه‌ی سند خوانده می‌شد.
  - اسنادِ باطل: ابطال سندِ معکوس ثبت می‌کند و هر دو سند در فهرست و جمع می‌مانند، همان
    سیاستِ دفتر و تراز. وضعیتِ تازه‌ای ساخته نشد.

  **رابط:**
  - روزنامه ۵۰ سند ۵۰ سند با کرسرِ سرور می‌آید («نمایشِ ۵۰ از ۱٬۲۳۴ سند» + «۵۰ سندِ بعدی»).
  - جمع‌ها از `/summary` است و برچسب با فیلترِ ردیفی «بدهکارِ ردیف‌های منطبق» می‌شود.
  - نوارِ فیلترِ مشترک به روزنامه وصل شد، جست‌وجو «شماره یا شرحِ سند» شد و با ۳۰۰ms مکث
    می‌رود.
  - خروجیِ CSV کلِ دامنه را صفحه‌به‌صفحه می‌گیرد، نه فقط صفحه‌های دیده‌شده.
  - **باگی که فقط مرورگرِ واقعی گرفت:** «سندهای بعدی» ← فیلترِ دیگر ← برگشت. کلیدِ دامنه‌ی
    مقداری دوباره همان می‌شد و صفحه‌ی دومِ کهنه کنارِ صفحه‌ی اولِ دامنه‌ی قبلی می‌نشست:
    سندِ تکراری و هشدارِ کلیدِ تکراریِ React. صفحه‌های بعدی حالا به **همین بارِ** دامنه
    گره خورده‌اند (`visit`)، نه به مقدارش. تستی دارد که روی منطقِ قبلی شکست می‌خورد.

  **راستی‌آزمایی:**
  - ۱۱ تستِ سرور (`test_journal_list_filters.py`) و ۹ تستِ کامپوننت (`Daybook.test.tsx`).
  - مرورگرِ واقعی روی پایگاه‌داده‌ی دورریختنی با ۶۰ سندِ ثبت‌شده از API:
    - ۵۰ از ۶۰، و «سندهای بعدی» ← ۶۰ از ۶۰.
    - مرکز هزینه: ۲۰ سند، ۲۰ ردیفِ منطبق، ۵۹۰٬۰۰۰.
    - سند ۱۰ تا ۱۹: ۱۰ سند.
    - جست‌وجو: ۱۱ سند.
    - سرریزِ افقی در ۱۴۴۰ و ۳۹۰ صفر و خطای کنسول صفر.
  - **باقی‌مانده:** نوارِ فیلترِ مشترک در ۳۹۰px بلند است، همان‌طور که روی سه تبِ دیگر بود.
    بقیه‌ی UI-03 (محیطِ یکپارچه، رفتن از تراز به دفتر و سند، حفظِ وضعیت با Back) روی
    UI-02 (#191) بنا می‌شود.

- **۱۴۰۵/۰۷/۰۲ (2026-09-24) — UI-02: مرور حسابِ حرفه‌ای (بی‌مهاجرت):**

  «مرور حساب‌ها» از نمای سطح‌به‌سطح (یک سطح + مسیر، جمعِ سرفصل با `reduce` در مرورگر)
  به **کاوشگرِ دوستونه** رسید: درختِ کدینگ سمتِ راست، خلاصه و گردشِ حساب سمتِ چپ، سند در
  کشو — بی‌ترکِ صفحه. **بک‌اند:** اندپوینتِ تازه‌ی `GET /api/accounting/balance-tree`
  (`get_balance_tree`): همه‌ی گره‌ها با مانده‌ی اول دوره، گردش و مانده‌ی پایانِ **تجمیعی** —
  جمع در سرور از همان هسته‌ی `get_balances` (پس برگ = تراز)، ردیفِ مستقیمِ روی سرفصل هم
  شمرده و پرچم می‌شود تا با پایانِ دفترِ همان گره بخواند، و هشدارِ ماهیت فقط روی برگ.
  `general-ledger/{id}` پارامترهای **اختیاریِ** `limit/offset` گرفت (سقف `MAX_LIMIT`):
  مانده‌ی ابتدای برش را پایگاه‌داده حساب می‌کند و `closing_balance`، `period_debit/credit`،
  `fx_totals` همیشه مالِ کلِ دوره‌اند؛ بی‌`limit` پاسخ مثلِ پیش است (فقط فیلدِ اضافه).
  مانده‌ی اول دوره‌ی دفتر هم حالا با `GROUP BY` جمع می‌شود نه با خواندنِ همه‌ی ردیف‌ها.
  ردیفِ دفتر تفصیلی و مرکزِ هزینه‌اش را هم دارد. **رابط:** `lib/accountTree.ts` (خالص،
  تست‌شده): ردیف‌های پیدا فقط از گره‌های باز، جست‌وجوی کد/نام با ی/ک عربی و ارقامِ فارسی،
  مسیر و «نمایش در درخت»، کیبوردِ RTL (← باز/فرزند، → بستن/والد، Enter گردش، Esc سطحِ
  بالا، `/` جست‌وجو؛ در گردش ↑↓، PgUp/PgDn صفحه‌ی سرور، Enter سند). گره‌های باز، حساب،
  بازه، فیلترها، اسکرول و صفحه‌ی گردش در `sessionStorage` می‌مانند. انتخابِ سال مالی،
  «فقط دارای گردش»، «مخفی‌کردنِ صفر»، دسترسیِ سریع (ثبت سند، دفتر کل، تراز، کدینگ).
  تست: ۱۶ تستِ بک‌اند (`test_account_browser.py`)، ۱۷ واحد + ۱۰ کامپوننت (از جمله ۱۰۱۰ حساب)،
  و E2Eِ کیبوردیِ `e2e/account-browser.e2e.mjs` در CI که با `E2E_LATE_FRAMES_MS` ۰/۳۰۰/۶۰۰ هم
  قبول می‌شود: فوکوسِ درخت پس از جست‌وجو با `useLayoutEffect` می‌آید نه `requestAnimationFrame`،
  و اسکریپت پیش از هر تایپ منتظرِ فوکوسِ واقعی می‌ماند. باقی‌مانده‌ها در FUTURE_WORK.

- **۱۴۰۵/۰۷/۰۲ (2026-09-24) — «کوبیتا سازمانی» M5: آپدیتِ امضاشده، سرور اول و کلاینت‌ها از سرور (بدونِ مهاجرت):**

  **قاعده: کلاینت هرگز از سرورش جلو نمی‌زند.** ترتیب این است:
  1. سرور نسخه‌ی تازه را از `acc.cubita.ir/updates/enterprise/` می‌گیرد
     (`app/onprem/updates.py`). امضای Ed25519ِ `latest.yml` (با پیشوندِ زمینه‌ی
     `cubita-update-v1`) و sha512ِ نصاب را می‌سنجد.
  2. مالک روی خودِ سرور نصب می‌کند. `provision` پیش از مهاجرت `pg_dump -Fc` می‌گیرد و بی‌آن
     مهاجرت نمی‌کند.
  3. `/updates/*` فقط **نسخه‌ی خودِ سرور** را به کلاینت‌ها می‌دهد.
  4. کلاینت (`electron/updateVerify.ts`) پیش از نصب امضا و sha512ِ فایلِ دانلودشده را خودش
     دوباره می‌سنجد.

  نسخه‌ی سرور = نسخه‌ی دسکتاپ (`app/version.py` و `version.txt`ِ بسته). انتشار:
  `npm run publish-enterprise` (همان `publish-update.mjs` با `--enterprise`؛ امضا روی سرورِ ابری
  با `cli sign-update`). رابط: کارتِ «به‌روزرسانیِ سرور» در «مجوز و به‌روزرسانی».

  زنجیره‌ی واقعی با نصابِ ۱۹۷MB روی همین رایانه سنجیده شد، از جمله ردِ فایلِ دست‌کاری‌شده.
  **کلیدِ آپدیتِ کلاینت (`updateKeys.ts`) باید با `keys.py` یکی بماند**؛ تست دارد.

- **۱۴۰۵/۰۷/۰۲ (2026-09-24) — CI: کامیت‌های `master` دیگر با مرجِ بعدی لغو نمی‌شوند:**
  مرج‌های #۱۷۴، #۱۷۵ و #۱۷۶ روی `master` با «✗ ۲/۴» ماندند و آرش پرسید چرا خراب شده‌اند.
  هیچ‌کدام خراب نبودند. PRِ هر سه سبز بود و مرجِ #۱۷۷، که همه‌شان را دارد، هم کامل سبز
  شد. ولی `concurrency` با `cancel-in-progress: true` روی `ci-${{ github.ref }}` بود. هر
  مرجی که کمتر از دو دقیقه بعد می‌آمد، `backend` و `frontend`ِ قبلی را وسطِ کار لغو
  می‌کرد. GitHub برای لغو و شکست یک ✗ نشان می‌دهد. #۱۷۸ و یکی از دو اجرای #۱۸۳ هم همین
  را خوردند.
  - گروه روی `master` حالا به‌ازای `github.sha` است و روی PRها همان `github.ref` مانده.
    پوشِ تازه روی شاخه‌ی PR هنوز اجرای قبلی را لغو می‌کند، چون آن‌جا فقط آخرین پوش مهم است.
  - `cancel-in-progress: false` کافی نبود. گروهِ concurrency فقط **یک** اجرای در انتظار
    نگه می‌دارد، و سومین مرجِ پشتِ‌هم همان اجرای در انتظار را لغو می‌کند.
  - هزینه ندارد: مخزن عمومی است و دقیقه‌ی Actions رایگان است. مرج‌های پشتِ‌هم حالا
    موازی اجرا می‌شوند.
  - اجراهای لغوشده‌ی قدیمی، بعد از مرجِ همین تغییر، **یکی‌یکی** دوباره اجرا می‌شوند. rerun با `ci.yml`ِ همان کامیتِ
    قدیمی اجرا می‌شود و گروهِ قدیمی را دارد، پس rerunهای هم‌زمان دوباره یکدیگر را لغو
    می‌کردند.
- **۱۴۰۵/۰۷/۰۲ (2026-09-24) — «کوبیتا سازمانی» M4: نصابِ سرور/کلاینت (بدونِ مهاجرت):**

  منطقِ نصب در **`backend/app/onprem/`** است (پایتونِ تست‌پذیر) و NSIS فقط `cubita-server.exe`
  را صدا می‌زند:
  - **`provision.py`**: PostgreSQLِ همراه (initdb، localhost، پورتِ ۵۴۳۳)، دو نقشِ
    `cubita_migrate`/`cubita_app` با `ALTER DEFAULT PRIVILEGES FOR ROLE`، و `.env` با رازهایی
    که فقط یک بار ساخته می‌شوند. بعد مهاجرت‌ها، و در پایان سنجشِ RLS که نصبِ خراب را متوقف
    می‌کند.
  - **`services.py`**: دو سرویسِ ویندوز (pg_ctl register و WinSW) با NetworkService، ACL با
    SID، و فایروالِ فقط private/domain. حذف هرگز داده را پاک نمی‌کند.

  بسته‌بندی در **`backend/packaging/`**:
  - `cubita-server.spec` هر فایلِ غیرِ `.py`ِ `app/` را خودکار جمع می‌کند.
  - `build_server_bundle.py` اجزا را کنارِ هم می‌چیند: exe، PostgreSQL 17 و WinSW با هشِ پین‌شده.

  دسکتاپ:
  - `build/installer-enterprise.nsh` صفحه‌ی نقش را دارد و آپدیت/حذفِ امن را انجام می‌دهد.
  - `dist:enterprise` حالا per-machine است و بسته‌ی سرور را همراه دارد.
  - جست‌وجوی خودکارِ سرور روی `/24`ِ شبکه‌های خصوصی.

  با Postgresِ واقعی روی همین رایانه سنجیده شد: راه‌اندازی از صفر در ۳۰ ثانیه، **۱۶۰ جدول با
  RLSِ کامل**، و روی دیتابیسِ پُر برنامه بدونِ زمینه‌ی مستأجر صفر ردیف می‌بیند. **`backend/dist/`
  و `backend/build/` حالا gitignore شده‌اند.** مانده: چک‌لیستِ VM در «نتایجِ M4»ِ
  `ENTERPRISE_PLAN.md`.

- **۱۴۰۵/۰۷/۰۲ (2026-09-24) — «کوبیتا سازمانی» M3: پنلِ ستادِ مجوزها و فعال‌سازیِ آنلاین (مهاجرتِ ۰۱۸۵):**

  **ابر:**
  - مهاجرتِ **۰۱۸۵** دو جدولِ سراسری می‌سازد: `enterprise_licenses` (دفترِ مجوزهای فروخته‌شده؛
    کدِ فعال‌سازی فقط هش ذخیره می‌شود) و `enterprise_license_events` (تاریخچه).
  - تنها مسیرِ صدور `services/enterprise_licenses.bind_and_issue` است، مشترک میانِ فعال‌سازیِ
    آنلاین (`POST /api/enterprise/activate`، عمومی، ۱۰ تلاش در ساعت) و صدورِ آفلاینِ ستاد.
    «هر مجوز یک دستگاه» در هر دو یکسان اجرا می‌شود و دستگاهِ دوم فقط با «انتقال» در پنل
    فعال می‌شود.
  - ستاد (`/api/admin/licenses`): ساخت، ویرایش، صدورِ آفلاین، انتقال، ابطال و کدِ تازه، همه در
    کارنامه‌ی ستاد. حوزه‌ی تازه‌ی نقش‌ها `licenses` است و پشتیبانی می‌تواند صدورِ آفلاین کند.
  - `LICENSE_SIGNING_KEY_FILE` در `.env`ِ ابر؛ اگر نباشد صدور ۵۰۳ می‌دهد.

  **سرورِ سازمانی:** `POST /api/license/activate` با `LICENSE_SERVER_URL` به ابر وصل می‌شود.
  صفحه‌ی «مجوز نرم‌افزار» حالا «فعال‌سازیِ آنلاین» دارد.

  **ستاد (`admin/`):** صفحه‌ی «مجوزهای سازمانی».

  سرتاسری روی یک رایانه آزموده شد (دو بک‌اند + رابطِ دسکتاپ + پنلِ ستاد). یک باگ هم رفع شد:
  `cli keygen` روی کنسولِ غیر UTF-8ِ ویندوز پس از نوشتنِ کلید می‌افتاد.

- **۱۴۰۵/۰۷/۰۲ (2026-09-24) — قرمزیِ CI روی مرجِ M2 (`8adc75e`) از E2E بود، نه از کدِ سازمانی:**

  آرش — آن قرمز («گزینه‌ی 1102 — بانک پیدا نشد») تقصیرِ کدِ تو نبود. E2Eِ ثبتِ سند
  (`desktop/e2e`) پیش از رسیدنِ فوکوس به کادرِ جست‌وجوی انتخاب‌گر تایپ می‌کرد؛ SearchSelect
  فوکوس را یک فریم بعد می‌دهد، و روی رانرِ کُند کلیدها گم می‌شدند. رفع شد: صبر برای
  فوکوس و سنجشِ کدِ تایپ‌شده. `E2E_LATE_FRAMES_MS=300` همان شکست را قطعی بازتولید می‌کند
  (پیش ✗✗✗، پس ✓✓✓). در همین بررسی سنجیده شد که کارهای UI-01 چیزی از M1/M2 نشکسته:
  اپ در `EDITION=enterprise` بالا می‌آید، handlerِ خطای ردیفی و مسیرِ سند در هر دو
  نسخه هستند، و `test_editions` و `test_licensing` سبزند.

- **۱۴۰۵/۰۷/۰۲ (2026-09-24) — خطای ردیفیِ سند raise می‌شود، نه return؛ دسکتاپ ۱.۷.۰ (بدونِ مهاجرت):**

  #183 به خطای ۴۰۰ِ ثبتِ سند `line_errors` (اندیسِ ساختاری) افزود و `detail` را دست
  نزد — درست و سازگار. ولی خطا را با `return JSONResponse` برمی‌گرداند. `get_db` فقط
  وقتی rollback می‌کند که استثنا از روتر بیرون برود؛ پاسخِ برگشتی یعنی **commitِ همان
  تراکنش**. پیش از این گاردها چیزی نوشته نمی‌شود، پس ضرری نزده بود — ولی «خطا =
  rollback» قاعده‌ی کلِ پروژه است و نباید به ترتیبِ خط‌ها وابسته بماند.

  حالا `JournalLineInputError` (زیرکلاسِ `HTTPException`) raise می‌شود و
  `journal_line_error_handler` (ثبت در `main.py`) همان پاسخِ `{detail, line_errors}` را
  می‌سازد — کلاینت هیچ تفاوتی نمی‌بیند. تستِ تازه خودِ تابعِ روتر را صدا می‌زند و
  raise را می‌خواهد؛ با نسخه‌ی return قرمز است.

  **نسخه‌ی دسکتاپ ۱.۶.۱ ← ۱.۷.۰:** از ۱.۶.۰ به بعد ویژگیِ تازه آمده (سوییچِ حالت،
  حافظه‌ی شرح، رفتن به ردیف، مرکزِ هزینه‌ی ردیفی — #178)، و قاعده‌ی پروژه ویژگی را
  minor می‌داند. نصبیِ ۱.۶.۱ که محلی ساخته شده بود هرگز منتشر نشد.


- **۱۴۰۵/۰۷/۰۲ (2026-09-24) — «کوبیتا سازمانی» M2: هسته‌ی مجوز (مهاجرتِ ۰۱۸۴):**

  بسته‌ی تازه‌ی **`backend/app/licensing/`**: توکنِ امضاشده‌ی Ed25519 (`CUB1.…`)، کلیدهای
  عمومیِ ثابت در کد (`keys.py`)، اثرانگشتِ سه‌جزئیِ ویندوز با تطبیقِ ۲ از ۳، محاسبه‌ی وضعیت
  (آزمایشیِ ۳۰روزه، فعال، مهلت، منقضی، ساعتِ عقب‌رفته، رایانه‌ی دیگر) و ابزارِ خطِ فرمانِ
  صدور. مهاجرتِ **۰۱۸۴** جدولِ تک‌ردیفیِ سراسریِ `enterprise_license` را می‌سازد (در ابر خالی
  می‌ماند). مجوز در همان گلوگاهِ `entitlements.enforce` می‌نشیند و **فقط نوشتن** را می‌بندد —
  حتی آزمایشیِ منقضیِ سازمانی، برخلافِ ابر، دفتر و پشتیبان‌گیری را باز می‌گذارد. `seats`
  سقفِ `max_users` را می‌گذارد، `mods` ماژول‌ها و `feat` قابلیت‌های پولی را. دسکتاپ: صفحه‌ی
  «مجوز نرم‌افزار» و نوارِ مجوز (فقط سازمانی). **ابری بی‌تغییر** (کلِ سوییت ۳۸۶۹ سبز).
  **مانده پیش از فروش:** یک‌بار `cli keygen` روی سرورِ ابری و کامیتِ کلیدِ عمومی — دستورش در
  «نتایجِ M2»ِ `ENTERPRISE_PLAN.md`.

- **۱۴۰۵/۰۷/۰۲ (2026-09-24) — «کوبیتا سازمانی» M1: پرچمِ نسخه + نشانیِ سرورِ زمان‌اجرا (بدونِ مهاجرت):**

  **بک‌اند:** `EDITION=cloud|enterprise` در `config.py`. سوارکردنِ روترها به **`app/routing.py`**
  رفت — `ALL_ROUTERS` به ترتیب، به‌اضافه‌ی `CLOUD_ONLY` (ستاد، پرداخت، فروشگاه، بازار، اتصال
  فروشگاه، پوش) و `ENTERPRISE_ONLY`؛ `main.py` فقط `include_routers(app, settings.edition)`
  دارد. **روترِ تازه باید در `ALL_ROUTERS` ثبت شود** (و اگر فقط ابری است، در `CLOUD_ONLY`) —
  `tests/test_editions.py` هر دو را می‌گیرد. قفلِ آزمایشی/اشتراکِ `require_permission` بی‌تغییرِ
  رفتار به **`services/entitlements.py::enforce`** منتقل شد تا مجوزِ M2 در همان گلوگاه بنشیند.
  `SignupIn` حالا از `BusinessOwnerIn` ارث می‌برد (همان فیلدها؛ ترتیبِ شِما عوض شد). تازه‌ها
  فقط در سازمانی: `/api/setup` (راه‌اندازیِ سرورِ تازه‌نصب، یک‌بار، زیرِ قفلِ تراکنشی)، ۴۰۴ برای
  ثبت‌نام و بازیابیِ ایمیلی/پیامکی (`deps.cloud_only`)، باز بودنِ ماژولِ محدود بدونِ گرنت، و
  پذیرشِ خودکارِ `Origin: file://`. `/api/health` و `MeOut` فیلدِ `edition` گرفتند. **ابری
  رفتارش عوض نشد** (۷۱۱ روت، کلِ سوییت سبز).

  **دسکتاپ:** `CUBITA_EDITION=enterprise` در main فریز می‌شود؛ نشانیِ سرور از
  `userData/server-settings.json`، همزمان از preload به `window.cubitaConfig` و `api.ts`.
  صفحه‌های `ServerConnectScreen` و `EnterpriseSetupScreen` + حالتِ سازمانیِ `LoginScreen`.
  `npm run dist:enterprise` نصابِ جدا می‌سازد (appId و پوشه‌ی داده‌ی جدا، `release-enterprise/`،
  بدونِ publish). در بیلدِ ابری هیچ‌کدام فعال نیست. آزمونِ بسته‌ی واقعی دو باگ را پیش از
  ورود گرفت: پوشه‌ی داده‌ی مشترک با نسخه‌ی ابری (نامِ برنامه از `name`ِ package.json می‌آید،
  نه appId) و CORSِ `file://`. جزئیات و مانده‌ها: «نتایجِ M1» در `ENTERPRISE_PLAN.md`.

- **۱۴۰۵/۰۷/۰۲ (2026-09-24) — آغازِ «کوبیتا سازمانی» (نسخه‌ی سرور/کلاینتِ درون‌سازمانی)؛ M0 بخشِ محلی (بدونِ مهاجرت، بدونِ تغییرِ کد):**

  تصمیمِ مالک: نسخه‌ی تازه‌ای برای شرکت‌ها و سازمان‌ها — یک سیستمِ سرور در شرکت، بقیه‌ی
  حسابدارها کلاینت روی شبکه‌ی داخلی، داده کاملاً مستقل از ابر، و فعال‌سازی با کدی که از
  `admin.cubita.ir` صادر می‌شود (تعداد کاربر + انقضا + ماژول‌ها + گره به دستگاه). **در
  همین مخزن** و با **همین کد** — پرچمِ `edition` (`cloud`/`enterprise`)، نه یک فورک.
  نقشه‌ی کامل، همه‌ی تصمیم‌ها و مراحل: **`ENTERPRISE_PLAN.md`**.

  دو قیدِ کشف‌شده که کلِ طراحی را شکل داد: (۱) **PostgreSQL اجباری است** — RLS، JSONB،
  تریگرهای plpgsql و UUIDِ پستگرس یعنی SQLite گزینه نیست و نصابِ سرور باید Postgres را با
  خودش ببرد؛ (۲) مجوز در **همان گلوگاهِ واحدِ** `deps.py:149-162` می‌نشیند و اصلِ «خواندن
  هرگز قفل نمی‌شود» (`deps.py:132-133`) را حفظ می‌کند — انقضای مجوز نوشتن را می‌بندد،
  خواندن و خروجی‌گرفتن را نه.

  **M0 (امکان‌سنجیِ بسته‌بندی) — بخشِ محلی موفق:** بک‌اندِ فعلی *بدونِ هیچ تغییری در کد* با
  PyInstaller `--onedir` بسته‌بندی شد (۸۴MB) و از داخلِ باینری: ۷۱۱ روت، bcrypt، psycopg،
  fpdf2، **همه‌ی ۱۸۱ مهاجرت**، ورودِ واقعی، و `/api/auth/me` با **RLS و بایندِ مستأجر**
  کار کردند. چهار تله پیدا و رفع شد — مهم‌ترینش اینکه گاردِ `config.py` موقعِ *بیلد* شلیک
  می‌کند (PyInstaller ماژول‌ها را ایمپورت می‌کند)، و سه فایلِ داده (`chart_templates.json`
  + دو فونتِ وزیرمتن) که PyInstaller خودکار جمع نمی‌کند و نبودشان فقط در زمانِ اجرا لو
  می‌رود. اسپایک عمداً کامیت نشد؛ دستورِ کارکرده و جزئیات در `ENTERPRISE_PLAN.md`.
  **باز:** بخشِ VM (ویندوزِ بی‌پایتون، Postgresِ پرتابل، سرویس‌های WinSW، آنتی‌ویروس).

  ضمناً تاریخِ شمسیِ سه ردیفِ ۲۰۲۶-۰۹-۲۲ پایین‌تر اصلاح شد (`۱۴۰۵/۰۷/۰۱` → `۱۴۰۵/۰۶/۳۱`؛
  با `jalaali-js`ِ خودِ پروژه راستی‌آزمایی شد) — یک روز جلو نوشته شده بودند و ترتیبشان را
  نسبت به ردیف‌های ۰۹-۲۳ به‌هم می‌زدند.

- **۱۴۰۵/۰۷/۰۱ (2026-09-23) — تکمیلِ UI-01 پیش از UI-02 (بدونِ مهاجرت):**

  گذرِ تکمیل بر پایه‌ی گزارشِ «چه مانده»؛ هر مورد کامیتِ اتمیِ خودش را دارد.

  * **شماره‌ی ردیف در خطا = شماره‌ی گرید.** پیامِ تفصیلی ردیف را میانِ ردیف‌های پُر
    می‌شمرد؛ با یک ردیفِ خالیِ وسط، «ردیف ۲» برای ردیفِ سوم. `postedRowNumbers` نگاشتِ
    payload ← گرید را می‌دهد و خطای ۴۲۲ِ سرور (`loc: ["body","lines",i,…]`) هم با آن
    به ردیفِ درست می‌رسد. مبلغِ بی‌حساب و حسابِ بی‌مبلغ دیگر بی‌صدا دور ریخته نمی‌شوند.
  * **`ApiError`:** `detail`ِ ۴۲۲ آرایه است و هشت جای `api.ts` آن را خام به `new Error`
    می‌دادند — کاربر «[object Object]» می‌دید، از جمله در ورود و ثبت‌نام.
  * **Enter روی مبلغِ خالی** باقی‌مانده را می‌نشاند **و** جلو می‌رود (کامنت و کد یکی شدند).
  * **فوکوسِ خودکارِ گرید** بعد از کلیک روی نوارِ بالا هم می‌رسد؛ قبلاً هر فوکوسی بیرون
    از گرید را محترم می‌شمرد، از جمله همان دکمه‌ی نوار. E2E پیدایش کرد.
  * **سوییچِ سریعِ حالت** در منوی کاربر و کشوی موبایل — همان ترجیحِ پروفایل.
  * **حالتِ ساده‌ی ثبتِ سند:** فقط شرح و تاریخ؛ بقیه پشتِ «گزینه‌های بیشتر»
    (`MoreOptions` در FormKit). داده‌ی پُر پنهان نمی‌شود.
  * **حافظه‌ی شرح** (همین سند + همین جلسه، فقط پیشنهاد) و **رفتن به ردیف** (Ctrl+G).
  * **مرکزِ هزینه‌ی ردیفی — حالتِ A:** ستون از ۰۰۲۸ در `journal_lines` بود و مدل، خروجی،
    گزارش‌ها، ابطال و چاپ همه از آن می‌خواندند؛ فقط `JournalLineIn` راهی نداشت.
    ردیف بر سند مقدم است (قاعده‌ی تفصیلی)؛ کلاینتِ قدیمی تفاوتی نمی‌بیند. Enter از
    ستونش می‌پرد (§۱۵). **مهاجرت ساخته نشد.**
  * **E2E در CI** (`desktop/e2e`، همان `playwright`ِ موجود): ورود ← حالتِ حسابدار از منو
    ← «حسابداری» ← دو ردیف فقط با کیبورد ← باقی‌مانده ← Ctrl+S ← سندِ متوازن با شماره‌ی
    سرور. خودش هم سنجیده شد: با دو خرابیِ عمدی دقیقاً در گامِ درست قرمز شد.
  * **پیامِ فاکتور فروش** از پاسخِ سرور ساخته می‌شود: سیاستِ «خودکار» (پیش‌فرض) سند و
    خروج را از قبل زده و «دومرحله‌ای» نه — پیامِ قبلی همیشه دومی را می‌گفت.

  **کارایی — ۳۰۰ ردیف، jsdom، میانه‌ی ۵ اجرا:** سوارشدن ۵۸۷ ← ۶۰۴ms، هر کلید ۸٫۶ ← ۹٫۷ms،
  Enter ۲۸٫۱ ← ۳۲٫۳ms. سنجه‌ی رندر (`JournalGrid.perf.test.tsx`) تازه است؛ قبلاً فقط
  منطقِ خالص سنجیده می‌شد.

  **مانده:** نمای خواندنیِ سند (`EntryCard`) تفصیلی و مرکزِ هزینه‌ی ردیف را نشان
  نمی‌دهد (از قبل هم نمی‌داد؛ چاپ و گزارش‌ها نشان می‌دهند). خطاهای تفصیلی و پیگیریِ
  سرور حساب را با نام می‌گویند نه اندیس — تغییرِ قراردادِ خطا پیگیریِ جداست.

- **۱۴۰۵/۰۷/۰۱ (2026-09-23) — «همه‌ی گزارش‌ها» (Reports Workspace): قدمِ چهارمِ فازِ دومِ UI-01 (بدونِ مهاجرت):**

  گزارش‌ها در دست‌کم پنج گروهِ منو پخش بودند و صفحه‌ی «گزارش‌ها» خودش می‌نوشت که تراز و
  دفتر «منتقل شده‌اند». سرِ همان صفحه حالا فهرستِ یک‌جای ۳۲ گزارشِ برنامه آمده، در هفت
  دسته (`lib/reportCatalog.ts`)، با صافیِ متنی و صفحه‌کلید (Enter اولین نتیجه، پیکان‌ها،
  Home/End، Escape). حسابدار با «دفاتر و ترازها» شروع می‌کند، ساده با «صورت‌های مالی».

  **هیچ گزارشی ساخته نشد** (§۵۶). برچسب، آیکن و گیتِ دسترسی از `buildLaunchers` می‌آیند —
  همان کاتالوگِ کارت‌ها — پس گزارشی که کاربر نمی‌بیند این‌جا هم نمی‌آید. سه جفتِ هم‌داده
  («کاردکس کالا»ی انبار، «صورت حساب طرف مقابل»، «سنین مطالبات»ِ اشخاص) عمداً بیرون ماندند
  تا دو ردیفِ هم‌نام کاربر را به حدس وادار نکند.

  **دوازده تبِ صفحه پیوندِ مستقیم گرفتند، بی `MODULE_SECTIONS`.** `Reports` حالا
  `section`ِ `NavSectionContext` را می‌خواند؛ `navigate('reports', 'balance-sheet')`
  ترازنامه را باز می‌کند. ثبت در `MODULE_SECTIONS` کارتِ «عملیات»، کشوی موبایل و قواعدِ
  R11/R14ِ ممیز را هم درگیر می‌کرد، بی سودی برای این قدم. فهرستِ تب‌ها از `Reports.tsx`
  به `reportCatalog.ts` رفت تا تب و فهرست یکی بمانند. `selectTab` روی mount هم صدا زده
  می‌شود: کاردکسی که مستقیم باز می‌شد بی آن فهرستِ کالا نمی‌گرفت.

  **تله‌ی پوسته:** `:root[data-structure='guided'] .page section` هر `section`ِ درونِ صفحه
  را کارتِ شناور می‌کند. دسته‌ها اول `section` بودند و فهرست روی ۳۹۰ به ۱۲۰۱px رسید؛ با
  `div role="group"` شد ۷۷۳ و روی ۱۴۴۰ از ۵۲۸ شد ۴۶۰.

  راهنمای «گزارش‌ها» هم اصلاح شد: هنوز «تراز آزمایشی» و «دفتر کل» را جزوِ همین صفحه
  می‌شمرد.

- **۱۴۰۵/۰۷/۰۱ (2026-09-23) — فرم‌های حرفه‌ای: قدمِ سومِ فازِ دومِ UI-01 (بدونِ مهاجرت):**

  نُه فرم دو نسخه دارند — ویزاردِ مرحله‌ای و فرمِ کلاسیکِ فشرده — و هر دو از یک
  پیش‌نویس می‌خوانند: فاکتور فروش، پیش‌فاکتور، فاکتور برگشتی، فاکتور و برگشتِ خرید،
  تعدیل دستی، انتقال بین انبارها، کارت دارایی و لیستینگِ پخش. انتخاب بینِ آن‌ها با
  `theme.content` بود و هر سه تم «مرحله‌ای»اند، پس **فرم‌های کلاسیک از هیچ‌جا باز
  نمی‌شدند** — همان قاطی‌شدنِ حالت و تم که §۳۲ منع کرده بود. فرم‌ها کهنه هم نشده بودند:
  هم‌پای ویزارد به‌روز می‌شدند (فرمِ فروش ۱۳ کامیت از ۱۴۰۵/۰۵/۲۳).

  حالا یک هوکِ مرکزی تصمیم می‌گیرد (`useGuidedForms` در `experienceMode.ts`، §۳۳):
  حسابدار فرمِ فشرده، ساده ویزارد. **هیچ فرمِ تازه‌ای ساخته نشد** (§۵۶). داشبوردِ
  `overview` عمداً روی تم ماند: صفحه است نه فرم، و کارت‌هایش از قبل با حالت عوض می‌شوند.

  در مرورگر روی پایگاه‌داده‌ی دورریختنی: هر نُه فرم در هر دو حالت و هر دو عرض (۱۴۴۰ و
  ۳۹۰) سرریز ۰، خطای کنسول ۰؛ و سه فرمِ کلاسیک واقعاً ثبت شدند — فاکتور خرید (با سندش)،
  تعدیل دستی (+۲۰) و فاکتور فروش (سند و خروجِ ۳ واحد، مانده ۱۷).

  چون همه‌ی کاربرانِ تولید حسابدارند (مهاجرتِ ۰۱۸۳)، **همه از ویزارد به فرمِ فشرده
  می‌روند**؛ کسی که ویزارد را می‌خواهد «حالت ساده» را در پروفایل می‌زند. این تصمیمِ
  صریحِ مالک بود.

- **۱۴۰۵/۰۷/۰۱ (2026-09-23) — منوی حالتِ حسابدار: قدمِ دومِ فازِ دومِ UI-01 (بدونِ مهاجرت):**

  «حسابداری» در `NAV_GROUPS` هشتمین گروه است، چون آن فهرست به ترتیبِ گردشِ کارِ
  کسب‌وکار چیده شده. در حالتِ حسابدار «حسابداری» و «دریافت و پرداخت» بلافاصله بعد از
  داشبورد می‌آیند (`MODE_GROUP_ORDER`) و کلیک روی «حسابداری» به‌جای «درختواره حساب‌ها»
  سند را باز می‌کند (`MODE_GROUP_LANDING`). حالتِ ساده همان ترتیبِ قبلی را دارد.

  **فقط ترتیب، نه محتوا** (§۵۲): `buildNav` همچنان تنها جایی است که تصمیم می‌گیرد چه
  دیده شود؛ `orderNavGroups` بعد از آن فقط جابه‌جا می‌کند. تستِ `navModel.test.ts` برای
  هر ترکیبِ ماژول، نوعِ حساب و نقش می‌سنجد که هر دو حالت دقیقاً همان صفحه‌ها را نشان
  دهند. مقصدِ کلیک هم اگر در این کسب‌وکار دیده نشود به پیش‌فرض برمی‌گردد.

  فقط نوار (و کشوی موبایلش) و سایدبار ترتیبِ تازه را می‌گیرند. کارتِ «عملیات»، پالتِ
  فرمان و انتخاب‌گرِ کارت عمداً ترتیبِ ثابت را می‌خوانند: `groupOf` برای صفحه‌ای که در دو
  گروه است («اعلامیه بدهکار بستانکار») اولین گروهِ دارنده را برمی‌دارد. جابه‌جاییِ امروز
  ترتیبِ آن دو گروه را عوض نمی‌کند، ولی اگر روزی یکی از آن‌ها جلو بیاید، کارتِ کناری
  با عوض‌شدنِ حالت عوض می‌شد — بی هیچ خطایی.

  یادداشتِ «حالت کار» در پروفایل هم اصلاح شد: تا امروز فقط گریدِ سند را نام می‌برد و
  نه کارت‌های داشبوردِ #173 را، پس کاربر نمی‌دانست عوض‌کردنِ حالت چه چیزهایی را عوض می‌کند.

- **۱۴۰۵/۰۷/۰۱ (2026-09-23) — داشبوردِ هر حالت: قدمِ اولِ فازِ دومِ UI-01 (بدونِ مهاجرت):**

  §۶۱ی تسکِ UI-01 فازِ دوم را پنج مورد و «تدریجی» تعریف کرده بود. قدمِ اول دو موردِ
  اولش با هم است — داشبوردِ حسابدار و داشبوردِ کسب‌وکارِ ساده — با همان محتوایی که
  §۵۳ نوشته بود.

  **حالت فقط *پیش‌فرضِ* کارت‌ها را تعیین می‌کند** (`MODE_DEFAULT_CARDS` در
  `lib/launchers.ts`). کسی که کارت‌هایش را خودش چیده (`dashboard_cards` پُر) چیزی از
  دست نمی‌دهد؛ روی تولید ۱۱ عضویت از ۱۲ هنوز انتخابی نکرده بودند و همین‌ها داشبوردِ
  تازه را می‌بینند. عوض‌کردنِ حالت کارت‌ها را همان لحظه عوض می‌کند، بی درخواستِ شبکه.
  نوارِ میان‌برِ زیرِ منو از همان منبع می‌خواند و خودکار همراه می‌شود.

  **هیچ صفحه‌ی تازه‌ای ساخته نشد** (§۵۶): هر مورد به صفحه‌ای موجود رسید. سه نگاشت در
  کد سنجیده شد چون از نام پیدا نبود — «دفتر روزنامه» ← `ledgerreport` (نه `ebooks` که
  دفاترِ تجارتِ الکترونیک است)، «سود» ← `reports` (تب‌هایش پیوندِ مستقیم ندارند ولی
  پیش‌فرضش سود و زیان است)، و «بدهکاران» و «طلبکاران» ← **یک** کارتِ `contacts/aging`
  چون آن صفحه هر دو را با یک کلید نشان می‌دهد. پس حالتِ ساده هفت کارت دارد نه هشت.

  حالت ≠ مجوز حفظ ماند: `resolveCards` هر کارتی را که کاربر دسترسی ندارد کنار می‌گذارد.
  بک‌اند و مهاجرت ندارد. `DEFAULT_CARD_IDS`ِ ثابت حذف شد؛ `TASK_LAUNCHERS` برای توضیحِ
  کارت‌ها و پالتِ فرمان سرِ جایش ماند.

- **۱۴۰۵/۰۷/۰۱ (2026-09-23) — حسابِ سندخورده هم زیرحساب می‌گیرد (بدونِ مهاجرت):**

  کاربر «صندوق» را می‌خواست به صندوق‌های جدا تقسیم کند و خطا گرفت: «این حساب سندِ
  مستقیم دارد و دیگر نمی‌تواند زیرحساب بگیرد». آن گارد از مدخلِ ۱۴۰۵/۰۶/۱۳ آمده بود
  («به شرطِ نداشتنِ سندِ مستقیم») و **برداشته شد**.

  **ترسِ گارد سنجیده شد و واقعی نبود.** دلیلش این بود که «مانده دو منبع پیدا می‌کند
  و هر گزارشی باید حدس بزند کدام را جمع بزند». ولی:
  - درختِ چارت (`AccountTreePanel`) مانده‌ی گره را «خودش + جمعِ فرزندان» حساب می‌کند
    و کامنتش همین حالت را صریحاً پیش‌بینی کرده بود.
  - گزارش‌ها (`_leaf_account_totals` و همتاهایش) روی `is_group` صافی می‌گذارند، نه روی
    «فرزند ندارد». والدِ زیرحساب‌دار `is_group=False` می‌ماند و ردیف‌هایش در تراز
    می‌آیند؛ و هر ردیفِ سند مالِ یک حساب است، پس نه جا می‌افتد نه دوبار شمرده می‌شود.
    تستِ `test_trial_balance_keeps_a_posted_parents_own_lines` همین را واقعاً می‌سنجد.

  **ردیف‌های قدیمی عمداً منتقل نمی‌شوند.** جابه‌جاکردنشان `account_id`ِ سندهای **دائم**
  را بازنویسی می‌کرد، و دائم در این برنامه یعنی امضاشده — حتی شماره‌ی فرعی‌اش قفل
  است. برای بردنِ مانده به زیرحساب، صفحه‌ی موجودِ «اصلاح طبقه‌بندی مانده» همین را با
  یک سندِ روشنِ تاریخ‌دار انجام می‌دهد و اسنادِ قدیمی دست نمی‌خورند.

  **ثبتِ سند روی والد هم بسته نشد.** صندوق و بانک حسابِ سیستمی‌اند و رسید و فاکتور
  خودکار رویشان می‌نشینند؛ بستنشان همه‌ی آن ثبت‌ها را می‌شکست — همان دامی که
  تفصیلیِ اجباری را به سندِ دستی محدود کرد.

- **۱۴۰۵/۰۶/۳۱ (2026-09-22) — نوارِ میان‌برِ افقی زیرِ TopNav (QuickAccessBar):**

  همان کارت‌هایی که کاربر در داشبورد («شروعِ کارِ تازه») برای خودش چیده بود، تا
  امروز فقط از خودِ داشبورد در دسترس بود — کاربری که وسطِ کارِ یک صفحه‌ی دیگر بود
  باید اول به داشبورد برمی‌گشت. حالا همان کارت‌ها به‌صورتِ یک ردیفِ افقیِ کوتاه و
  مینیمال (چیپ‌های آیکون‌دار) درست زیرِ TopNav هم می‌نشینند — روی هر صفحه‌ای،
  چون TopNav خودِ اپ است نه بخشی از داشبورد. کارتِ فعال (منطبق با صفحه/بخشِ
  جاری) با رنگِ تأکید مشخص می‌شود.

  منبعِ داده و منطقِ ذخیره یکی ماند تا انتخابِ کاربر همه‌جا هم‌گام بماند: منطقِ
  مشترکِ ذخیره/بازنشانی که قبلاً فقط داخلِ `LauncherBoard.tsx` بود، به
  `lib/useDashboardCards.ts` منتقل شد و هر دو کامپوننت (کارتِ داشبورد و نوارِ
  تازه) همان را صدا می‌زنند. نوار وقتی کاربر هنوز کارتی انتخاب نکرده چیزی رندر
  نمی‌کند (بی‌فایده بودنِ یک نوارِ خالی زیرِ هر صفحه)؛ دکمه‌ی مدیریتِ کوچک در
  انتهای نوار همان انتخاب‌گرِ `LauncherPicker` را باز می‌کند، پس از هر جای
  برنامه هم می‌شود کارت‌ها را دوباره چید. در عرضِ موبایل فقط ناحیه‌ی چیپ‌ها
  اسکرولِ افقی می‌گیرد، نه کلِ صفحه (بدونِ سرریز، با Playwright روی ۳۹۰px
  راستی‌آزمایی شد).

  فایل‌ها: `desktop/src/components/QuickAccessBar.tsx` (تازه)،
  `desktop/src/lib/useDashboardCards.ts` (تازه، استخراج‌شده از
  `LauncherBoard.tsx`)، `desktop/src/components/Dashboard.tsx` (سیم‌کشی زیرِ
  TopNav)، `desktop/src/App.css` (`.quick-bar*`).

- **۱۴۰۵/۰۶/۳۱ (2026-09-22) — حادثه: انتشارِ ۱.۴.۷ با کرشِ بدو‌اجرا برای همه، رفعِ
  ریشه‌ای و نسخه‌ی ۱.۴.۸:**

  بلافاصله بعد از انتشارِ ۱.۴.۷ (حاویِ ورودِ آفلاین، ردیفِ بالا)، اپ برای هر
  کاربری که آپدیت می‌گرفت **اصلاً بالا نمی‌آمد** — نه فقط این نسخه، هیچ صفحه‌ای.
  علت: `better-sqlite3` (ماژولِ بومیِ SQLiteِ محلی) موقعِ `npm run dist` با ABIِ
  Node.jsِ سیستم (`NODE_MODULE_VERSION 137`) بسته‌بندی شده بود نه با ABIِ Electron
  (`146`) — چون قدمِ `npm run rebuild-native` که `desktop/README.md` از قبل بابتِ
  دقیقاً همین خطر توصیه کرده بود، قبل از بیلد اجرا نشد. `initLocalDb()` همین اول
  کارِ main process (پیش از ساختِ پنجره) صدا زده می‌شود، پس کرش قبل از رسیدن به
  هر UI یا حتی چکِ auto-update اتفاق می‌افتد — یعنی نصبِ خراب **نمی‌توانست از
  راهِ auto-update خودش را ترمیم کند**؛ کاربرِ گرفتار باید دستی نصاب را دوباره
  نصب می‌کرد. لاگِ `%APPDATA%/Cubita/startup-debug.log` (از قبل برای دیباگِ
  استارتاپ در main.ts بود) دقیقاً همین خطا را ثبت کرده بود و تشخیص را سریع کرد.

  **رفعِ ریشه‌ای:** به‌جای تکیه به یادآوریِ README، `npm run dist`/`npm run pack`
  حالا خودشان `rebuild-native` را قبل از `electron-builder` صدا می‌زنند
  (`desktop/package.json`) — این کلاس خطا دیگر به یادِ توسعه‌دهنده وابسته نیست.
  نسخه‌ی ۱.۴.۸ با این بیلدِ درست منتشر شد.

- **۱۴۰۵/۰۶/۳۱ (2026-09-22) — ورودِ آفلاینِ اپِ دسکتاپ: بارِ اولِ یوزر/پسورد، از آن
  پس خودش وارد می‌ماند (چند روز/هفته، مناطقِ کم‌اینترنت):**

  اپِ دسکتاپ ادعای آفلاین‌بودن داشت (کشِ SQLiteِ محلی + outboxِ اسناد) ولی درِ
  ورودی این وعده را نقض می‌کرد — نه از رویِ طراحیِ عمدی، بلکه چون هرگز ساخته
  نشده بود: توکن در Electron فقط یک متغیرِ حافظه‌ی `main.ts` بود (`authToken`،
  با هر بازکردنِ اپ صفر می‌شد) و `loadStoredToken()` برای Electron **همیشه
  `null`** برمی‌گرداند — با هر ری‌استارت، صفحه‌ی ورود می‌آمد، آنلاین یا
  آفلاین فرقی نداشت. عوارضِ جانبیِ کشف‌شده‌ی همین ریشه: سوییچِ کسب‌وکار روی
  دسکتاپ هم همیشه به صفحه‌ی ورود برمی‌گشت، چون `window.location.reload()` بعد
  از سوییچ دوباره همان مسیرِ شکسته را می‌زد.

  **کشفِ تعیین‌کننده:** بک‌اند از قبل زیرساختِ کامل داشت. `services/refresh.py`
  یک رفرش‌توکنِ ۶۰روزه‌ی چرخشی (گره‌خورده به `token_version` و عضویتِ فعال) برای
  «همیشه‌واردمانده»ی موبایل صادر می‌کرد و `/api/auth/login` همین امروز هم آن را
  برمی‌گرداند — دسکتاپ فقط دورش می‌ریخت (`schemas/auth.py` صریح می‌گفت «وب/دسکتاپ
  نادیده می‌گیرند»). و `mobile/src/api/client.ts` الگوی کلاینتِ کامل را داشت:
  رفرشِ تک‌پروازه، تفکیکِ خطای شبکه از خطای احراز. کارِ اصلی پورت‌کردنِ همین الگو
  به Electron بود، نه اختراعِ چیزِ تازه — **صفر مهاجرت، صفر مدلِ تازه**.

  **قاعده‌ی طلایی که کل کار حولش ساخته شد:** خطای شبکه هرگز نشست را باطل
  نمی‌کند؛ فقط ۴۰۱ِ صریحِ سرور این کار را می‌کند. `electron/authSession.ts`
  (تازه) این را در `restoreSession` پیاده می‌کند: رفرشِ خاموشِ بدو اجرا اگر
  `fetch` throw کند (قطعیِ شبکه) یا سرور ۵xxِ گذرا بدهد، نشستِ ذخیره‌شده
  دست‌نخورده برمی‌گردد با `offline:true`؛ فقط ۴۰۱ِ صریح نشست را پاک می‌کند.
  رمزِ خام هرگز ذخیره نمی‌شود — فقط همین توکنِ رفرش، در جدولِ تازه‌ی `session`
  داخلِ همان SQLiteِ محلیِ موجود.

  **سه جای دیگر هم باید رفرش را تازه نگه دارند، وگرنه نشستِ آفلاین بی‌صدا
  می‌شکست:** `switch-tenant` رفرشِ قبلی را باطل و یکی برای مستأجرِ مقصد صادر
  می‌کند (وگرنه بعد از سوییچ + یک بازیابیِ آفلاین، اپ به کسب‌وکارِ لحظه‌ی ورود
  برمی‌گشت)؛ `signup` هم رفرش می‌دهد (اولین ورودِ یوزر/پسورد همان ثبت‌نام است)؛
  و `change-password` هم (چون `set_password` نسلِ توکن را جلو می‌برد و
  `refresh_svc.rotate` رفرشِ نسلِ قدیم را رد می‌کند — بدونِ این، کاربری که رمزش
  را از داخلِ اپ عوض می‌کرد، از نشستِ آفلاینش هم بیرون می‌افتاد).

  نوارِ «آفلاین» (`OfflineBanner.tsx`) فقط وقتی دیده می‌شود که نشست از کش آمده و
  رفرشِ خاموش به قطعیِ شبکه خورده — نه هر بار.

  **راستی‌آزمایی:** بک‌اند با تست‌های تازه در الگوی موجودِ
  `test_refresh_token.py`/`test_signup_and_switching.py`/`test_password_reset.py`
  (۳۸۰۷ تست، سبز). فرانت: ۱۱ تستِ واحدِ `authSession.test.ts` با `fetch`ِ mock
  و `db.js`ِ mock (سه مسیر: آنلاین، قطعیِ شبکه، ۴۰۱)، به‌علاوه‌ی
  `test-auth-session.ts` — اسکریپتِ آزمونِ واقعی با Electron/SQLite/بک‌اندِ
  واقعی، هم‌الگو با `test-sync.ts` موجود؛ **در محیطِ sandboxِ این نشست اجرا
  نشد** (خودِ Electron هم اینجا بالا نمی‌آید — با `test-sync.ts`ِ دست‌نخورده هم
  بازتولید شد، پس محدودیتِ محیط است نه رگرسیون) و باید یک‌بار روی ماشینِ واقعی
  اجرا شود. `tsc -b`، `oxlint`، و ۱۶ قاعده‌ی `audit-pages.mjs` صفر خطا؛ ۲۲۳
  تستِ vitest سبز.

- **۱۴۰۵/۰۶/۳۰ (2026-09-21) — زیرساختِ `admin.cubita.ir` + نجاتِ TLSِ کلِ سرور:**

  زیردامنه بالا آمد: رکوردِ `A` در Cloudflare، `/opt/cubita-admin/web`، vhost با
  پراکسیِ `/api/` به `:8001`، زونِ نرخِ `adminlogin` در `conf.d`، و گواهیِ
  لتس‌انکریپت. `nginx -t` سالم و هر سه سایتِ دیگر دست‌نخورده.

  **ولی گواهی با HTTP-01 گرفته نشد و همین یک مشکلِ بزرگ‌تر را لو داد.** سرور در
  تهران است (Hostiran، AS59441) و **پورت ۸۰ از خارجِ ایران timeout می‌دهد** در
  حالی که ۴۴۳ باز است. پس Let's Encrypt هرگز نمی‌تواند چالشِ ACME را بردارد.
  مقصر نه DNS بود، نه `ufw`/`fail2ban`/`ipset` — همه سالم بودند و یک fetcherِ
  ثالثِ مستقل هم همان timeout را بازتولید کرد. (نکته‌ی جانبی: IPِ **خروجیِ**
  سرور `95.217.210.65` است نه `62.60.129.39` — از یک تونل می‌رود؛ به همین دلیل
  روی توکنِ Cloudflare **نباید** فیلترِ IP گذاشت.)

  خرابی خاموش بود: تمدید هر شب بی‌سروصدا شکست می‌خورد و تنها نشانه‌اش این بود که
  سایت هفته‌ها بعد منقضی شود. **هر چهار گواهیِ کوبیتا ۲۳ روز اعتبار داشتند و
  `acc.cubita.ir` از صدورش در ۱۷ ژوئیه هرگز تمدید نشده بود** (فقط `cert1.pem` در
  archive). همه به **DNS-01 روی Cloudflare** کوچ کردند و حالا تا ۲۰ دسامبر
  معتبرند. به‌علاوه‌ی هوکِ `renewal-hooks/deploy/reload-nginx.sh`: با رفتنِ
  `installer = nginx` دیگر کسی بعد از تمدید ری‌لود نمی‌کرد، یعنی تمدیدِ «موفق»
  باز هم گواهیِ قدیمی را از حافظه سرو می‌کرد. جزئیات در §۷ و
  [vhostِ مرجع](deploy/nginx/admin.cubita.ir.conf).

  **دو ایرادِ واقعی در vhostِ مرجع** که فقط با نصبِ واقعی پیدا شدند: `http2 on;`
  روی nginx ۱.۱۸ـِ سرور خطای نحو می‌دهد (از ۱.۲۵.۱ به بعد است)؛ و هدرهای امنیتی
  فقط در سطحِ `server` بودند، در حالی که nginx وقتی یک سطح `add_header`ِ خودش را
  دارد هدرهای بالاتر را **به ارث نمی‌برد** — یعنی `index.html` و `assets/` بدونِ
  `X-Frame-Options` می‌ماندند. هر دو اصلاح شد و مرجع حالا دستور‌به‌دستور با فایلِ
  زنده یکی است.

  **آزمونِ مهاجرت پیش از استقرار:** `hesabdari` روی همان سرور کپی شد و `0180`/`0181`
  روی PG۱۴ـِ واقعی با دادهٔ واقعی اجرا شدند. حذفِ مستأجر **پیش از** مهاجرت شکست
  خورد (`assurance_findings_tenant_id_fkey`) و **پس از** آن موفق شد؛ `NO ACTION`
  از ۴۶ به ۰ رسید و ۱۶۲ جدولِ FORCE RLS دست‌نخورده ماند. کپی حذف شد.

- **۱۴۰۵/۰۶/۳۰ (2026-09-21) — کوچِ مدیریتِ پلتفرم به `admin.cubita.ir`: هویتِ
  ستادِ مستقل، ردِ کارهای ستاد، و پاک‌سازیِ اپِ مشتری (مهاجرت‌های `0180` و `0181`):**

  تا امروز «سوپرادمین» یک حالتِ ویژه **درونِ** اپِ مشتری بود: همان باندل، همان
  توکن، و یک allowlistِ ایمیل در `.env`. مهم‌تر اینکه `get_principal` عضویتِ فعال
  در یک کسب‌وکار را اجباری می‌کرد، پس حسابِ مدیریتی **مجبور** بود یک کسب‌وکارِ
  واقعی داشته باشد. حالا سه چیزِ جدا شدند: `acc.cubita.ir` فقط دفترِ مشتری،
  `admin.cubita.ir` فقط مدیریت، و حسابِ `acc.cubita@gmail.com` یک اکانتِ معمولی.

  **هویتِ ستاد روی `platform_admins` نشست** — جدولی که از مهاجرتِ `0015` وجود
  داشت و هیچ‌جا کوئری نمی‌شد. اعتبارنامه عمداً روی `users` ماند (یک مسیرِ هش، یک
  سازوکارِ ابطال با `token_version`)؛ `platform_admins` فقط اختیار را نگه می‌دارد.
  توکنِ ستاد با ادعای `typ` از توکنِ مستأجری جدا می‌شود و **هیچ `tid` ندارد**.

  **نداشتنِ مستأجر خودِ سازوکارِ ایمنی است.** سیاستِ RLS بدونِ `app.tenant_id`
  هیچ ردیفی رد نمی‌کند، پس نشستِ ستاد نمی‌تواند تصادفی دفترِ کسی را بخواند و هر
  کارِ میان‌مستأجری باید `tenant_scope`ِ صریح و grep‌شدنی باشد. مدلِ قبلی برعکس
  بود: `_client_scope` در `finally` مستأجرِ ادمین را برمی‌گرداند، یعنی درخواست
  **با یک مستأجرِ بسته تمام می‌شد** و `tenant_scope`ِ فراموش‌شده بی‌صدا در دفترِ
  خودِ ادمین می‌نوشت. آن تابع حذف شد.

  **`staff_audit_log`** (سراسری، فقط‌افزودنی): تا دیروز حذفِ برگشت‌ناپذیرِ یک
  مشتری، تعلیق و بازنشانیِ رمزِ مالک **صفر رد** می‌گذاشتند، چون `audit_log`
  مستأجرمحور است و هیچ مدلِ پلتفرمی‌ای در `audited_models()` نیست. ستونِ
  `tenant_id` عمداً FK ندارد — کلیدِ خارجی ردِ حذف را همراهِ خودِ حذف cascade
  می‌کرد، و آن مهم‌ترین ردیفِ جدول است.

  **دو باگِ واقعیِ production که همین کار بیرون کشید:**

  - **حذفِ اکانت اصلاً کار نمی‌کرد.** `TenantMixin` از روزِ اول `ondelete=CASCADE`
    داشت، پس اسکیمای `create_all`ِ تست‌ها درست بود؛ ولی مهاجرت‌ها **۴۷ جدول** را
    با `NO ACTION` ساخته بودند و `provision_tenant` برای هر کسب‌وکارِ تازه واحدِ
    اندازه‌گیری می‌سازد — یعنی عملاً هیچ اکانتی قابلِ حذف نبود. هیچ تستی نمی‌دیدش
    چون `test_migration_drift` فقط ایندکس‌های یکتا را مقایسه می‌کرد. مهاجرت
    `0181` اصلاحش کرد و دو تستِ انحرافِ تازه قفلش کردند.
  - **`purge_tenant` کاربرانِ بی‌عضویت را یتیم می‌شمرد.** هویتِ ستاد عمداً
    بی‌عضویت است، پس اولین حذفِ اکانت روی `platform_admins_user_id_fkey` می‌شکست.

  **اپِ `admin/` پروژه‌ی npmِ مستقل است، نه ورودیِ دومِ `desktop/`** — تا کدِ
  مدیریتی ساختاراً نتواند وارد باندلِ مشتری شود، باندلِ نصب‌کننده‌ی Electron آن را
  با خودش نبرد، و ممیزِ صفحه‌ها صادق بماند. چهار فایلِ پایه کپی شده و
  `check-shared.mjs` بایت‌به‌بایت می‌سنجدشان تا واگرایی **پرصدا** باشد.
  شش صفحه، از جمله دو تا که پیش‌تر اصلاً وجود نداشتند: «گزارش خطاها» (اندپوینتش
  بود، صفحه‌اش نه) و «کاربران ستاد».

  **قاعده‌ی ضدِقفل:** همیشه باید دستِ‌کم یک `owner`ِ فعال بماند. بازیابیِ رمزِ ستاد
  در فازِ ۱ خودکار نیست، پس بدونِ این گارد یک اشتباهِ ساده پنل را برای همیشه
  می‌بست. به همین دلیل پیش از معمولی‌کردنِ حسابِ `acc.cubita@gmail.com` باید **دو**
  مالکِ ستادی ساخته شود.

  **پلِ `LEGACY_ADMIN_ALLOWLIST`** تا باندلِ مستقرِ acc در فاصله‌ی دو استقرار
  نشکند؛ خاموش‌کردنش یک ویرایشِ `.env` و ری‌استارت است، نه یک استقرار — یعنی
  راهِ برگشتِ ده‌ثانیه‌ای. `is_super_admin`/`is_platform_admin` هم عمداً در
  `/api/auth/me` دست‌نخورده ماندند و در PRِ پاک‌سازی برداشته می‌شوند.

  **یک باگِ UI که فقط با بازکردنِ واقعیِ صفحه پیدا شد:** `formatJalali` روی
  datetime می‌شکست (`split('-')` چهار تکه می‌داد و روز `NaN` می‌شد). اپِ مشتری
  فقط ستونِ `date` می‌فرستاد پس هرگز نگزیده بود؛ ستونِ «انقضا»ی اپِ ستاد رشته‌ی
  خامِ ISO نشان می‌داد. در فایلِ مشترک اصلاح شد.

- **۱۴۰۵/۰۶/۲۹ (2026-09-20) — ردیابیِ بار (بچ/LOT): مانده از دفترِ انبار، رزرو،
  کاتالوگ، سود و مرجوعیِ بارمحور (PR #۱۴۶ و #۱۴۷، مهاجرت‌های `0171` تا `0176`):**

  `stock_batches.qty` ادعا می‌کرد «مانده‌ی سالمِ این بار» است، ولی شمردنِ
  نویسنده‌هایش نشان داد **فروش، خروجِ انبار و انتقال اصلاً کمش نمی‌کردند** — فقط
  تعدیلِ دستی، برگشت از خرید و ابطال. یعنی بارِ ۱۰۰۰تایی که تمامش فروخته شده بود
  هنوز ۱۰۰۰ نشان می‌داد، و «دفترِ ثبتِ بچ» عملاً از اولین فروش به بعد دروغ می‌گفت.

  راهِ حل یک جدولِ `BatchMovement` **نبود**: `stock_ledger` خودش دفترِ حرکت است و
  جدولِ دوم یعنی دو نمای یک داده که باید تا ابد با هم بخوانند. به‌جایش یک ستونِ
  هویت (`batch_id`) به همان دفتر اضافه شد و مانده مثلِ موجودیِ کالا `SUM(qty)` شد.
  کنارش `source_line_id` آمد، چون ردیفی که بینِ دو بار تقسیم می‌شود دو ردیفِ دفتر
  می‌سازد و `source_id` فقط سند را می‌شناسد نه ردیف را.

  **`batch_id` فقط هویت است.** بهای بار هرگز در `unit_cost` نمی‌نشیند: میانگینِ
  موزون سراسریِ کالاست و `valuation.py` بچ نمی‌شناسد. نوشتنِ بهای بار در آن ستون
  بازپخشِ ارزش‌گذاری را بی‌صدا خراب می‌کرد، پس عمداً به `_COLUMNS` اضافه نشد.

  **هیچ مهاجرتِ داده‌ای انجام نشد، و این عمدی است.** خروجی‌های گذشته هیچ‌وقت بچ
  نداشته‌اند و حدس‌زدنشان یعنی جعلِ تاریخچه؛ مشتق‌کردنِ بارهای قدیمی هم بدترش
  می‌کرد، چون بارِ بدونِ ورودِ برچسب‌خورده به **صفر** مشتق می‌شود — دروغی با
  اعتمادبه‌نفس، جایی که ستونِ قدیمی دست‌کم یک راستِ کهنه بود. پس API صریح می‌گوید
  عدد از کجا آمده (`qty_source = ledger | legacy`)، ستونِ قدیمی برای همان‌ها زنده
  و نگهداری‌شده می‌ماند، و یک **گزارشِ مغایرت** می‌گوید کدام بار هنوز تخمینی است و
  چرا. «گزارش، نه گارد».

  پرکردنِ خودکارِ مهاجرت فقط جایی است که ابهام صفر است: سندی که برای آن
  `(source_id, item_id)` دقیقاً یک ردیفِ دفتر و یک بار دارد. بازسازیِ ترتیبِ
  `P{n}-{idx}` از روی `seq` وسوسه‌انگیز بود و رد شد — امروز دو حلقه اتفاقی
  هم‌ترتیب‌اند و تکیه به این تصادف، غلطی می‌ساخت که بعداً قابلِ تشخیص نبود.

  بیشترین ارزش به‌ازای یک خط: `voiding._compensating_moves` حالا برچسب را با
  حرکتِ قرینه می‌برد. بی آن، هر ابطال موجودیِ کالا را درست برمی‌گرداند و مانده‌ی
  بار را سرِ جایش رها می‌کرد — بی هیچ خطایی.

  **۰۱۷۲ — ردیابی، وضعیت و موقعیت.** `items.is_batch_tracked` (پیش‌فرض خاموش)،
  `minimum_sellable_shelf_life_days`، جدولِ `warehouse_locations`، و وضعیتِ بار
  در **سه ستونِ عمود بر هم** (`qc_status`/`hold_status`/`is_closed`) به‌جای یک
  ستونِ دوازده‌حالته. آن ۱۲ وضعیتِ خواسته‌شده سه واقعیتِ مستقل را در یک ستون
  فشرده می‌کردند — باری می‌تواند هم‌زمان منتظرِ QC **و** فراخوان‌شده باشد — و
  بقیه‌شان (`near_expiry`، `depleted`، `fully_reserved`) از عدد و تاریخ مشتق‌اند.
  API هر ۱۲ تا را می‌دهد؛ پایگاه‌داده هیچ‌کدام را نگه نمی‌دارد.

  گاردِ روشن‌کردنِ پرچم **کمّی** است نه تاریخی، و این عمداً با قاعده‌ی
  `is_serial_tracked` فرق دارد: سریال به دنیایی اضافه شد که هیچ سریالی نداشت،
  ولی بچ برای هر خریدی که تا امروز ثبت شده از قبل ساخته می‌شده. «گردش دارد →
  رد» یعنی این قابلیت دقیقاً برای کالاهایی که مهم‌اند غیرقابلِ دسترس شود. پس
  معیار این است که موجودیِ **بی‌برچسب** صفر باشد، و راهِ رسیدن به آن «انتسابِ
  موجودی به بار» است: جفتِ حرکتِ جمع‌صفری که موجودی و میانگین را دست‌نخورده
  می‌گذارد (`batch_opening` در `AVERAGE_INFLOWS` ثبت شد تا این خاصیت اعلانی
  باشد، نه وابسته به درست‌بودنِ عددی که ثبت‌کننده حساب کرده).

  FEFO سمتِ سرور تخصیص می‌دهد، پس هیچ فراخوانِ موجودی نشکست؛ نقضش (§۱۱) سرتاسر
  باز است — از اسکیما تا فرمِ خروج. و انتقالِ بینِ انبار بارِ آینه در مقصد
  می‌سازد (`parent_batch_id`): در ۳۶ بندِ درخواست حرفی از انتقال نبود، ولی
  `StockBatch` به `(کالا، انبار)` بسته است و بی این، کالا آن‌طرفِ مرز بی‌بچ
  ظاهر می‌شد.

  **۰۱۷۳ — رزرو.** `stock_reservations`، دفترِ علامت‌دار به‌جای ستونِ وضعیت، چون
  لغوِ **جزئیِ** سفارش با `status` بیان نمی‌شود. کالامحور با `batch_id`ِ
  تهی‌پذیر، چون §۷ می‌گوید مشتری لازم نیست بار را انتخاب کند — و رزروِ بی‌بار
  دقیقاً همان حالت است.

  دو چیز حینِ کار اصلاح شد و هر دو از جنسِ «قید همان کاری را می‌کرد که نباید»:
  ایندکسِ یکتا بی `COALESCE` روی حالتِ رایج (هر دو ستونِ تهی) اصلاً نمی‌بست —
  همان درسِ `uq_price_list_items_context` — و اگر همه‌ی رویدادها را می‌بست،
  لغوِ جزئیِ دوم را رد می‌کرد. حالا فقط روی `event = 'reserve'` است.

  §۹ «بلاک در سطحِ Database» خواسته بود و صادقانه بخشی از آن شدنی نیست: Postgres
  ادعای تجمعیِ بین‌ردیفی اعلانی ندارد. آنچه تحویل شد سریال‌سازیِ تراکنشی
  (`FOR UPDATE` روی ردیفِ **کالا**، نه بار — به همان دلیلِ `lock_items`) به‌علاوه‌ی
  تکرارناپذیریِ اعلانی است. تستِ همزمانی با دو تراکنشِ واقعی و Barrier می‌سنجدش.

  **۰۱۷۴ — تخصیصِ بار به کاتالوگ (§۵ §۶ §۷).** بارِ ۱۰۰۰تایی در انبار لزوماً یعنی
  ۱۰۰۰ تا برای فروشِ عمده نیست؛ مدیرِ پخش ممکن است بخواهد ۴۰۰ تایش را عرضه کند و
  بقیه را برای مشتریِ قدیمی نگه دارد. تا امروز چنین مرزی نبود: هرچه در انبار بود
  در کاتالوگ قابلِ سفارش بود.

  `marketplace_catalog_allocations` **برخلافِ بقیه‌ی `marketplace_*` مستأجرمحور
  است.** آن‌ها سراسری‌اند چون داده‌شان ذاتاً میان‌مستأجری است — کاتالوگی که مستأجرِ
  دیگر می‌بیند. این یکی **موجودی** است: می‌گوید از انبارِ *من* چه‌قدر عرضه می‌شود، و
  موجودی هرگز سراسری نبوده. پس `listing_id` بی کلیدِ خارجی می‌ماند، همان‌طور که
  `MarketplaceOrder.distributor_sales_invoice_id` از سمتِ مقابل بی‌FK به دفترِ
  مستأجر اشاره می‌کند.

  از سه عددِ §۶ فقط `allocated` ستون دارد؛ `reserved` و `remaining` مشتق‌اند.
  دو محاسبه‌ای که به‌راحتی غلط می‌شدند: **سقف هم باید سفارش‌های ثبت‌شده را کم کند**
  (موجودیِ آزاد رزروها را کم کرده ولی سقف عددِ ثابت است — ۴۰۰ تخصیص با ۱۰۰ سفارش
  یعنی ۳۰۰)، و **واحدِ کسر واحدِ کالاست نه واحدِ لیستینگ**، پس رزرو به تفکیکِ
  (لیستینگ، کالا) خوانده می‌شود؛ جمع‌زدنِ «تعدادِ پک» با «تعدادِ شامپو» عددی می‌داد
  که هیچ‌چیز نیست.

  §۱۶ («بارِ فراخوان‌شده از کاتالوگ خارج شود») بی یک خطِ اضافه برآورده شد: تخصیص به
  «قابلِ فروشِ خودِ بار» محدود است و بارِ فراخوان‌شده `sellable = 0` دارد.

  **سه باگِ زنده‌ی بی‌ربط به بچ که در مسیر پیدا شدند و بسته شدند.** هیچ‌کدام از این
  کار نیامده بودند؛ همه از قبل در تولید بودند:

  1. **`place_order` موجودی را اصلاً نمی‌سنجید.** اتصال، صنف، کف و سقفِ تعداد و
     سقفِ روزانه را می‌سنجید و انبارِ پخش‌کننده را **هرگز**. کمبود تازه موقعِ *تأیید*
     بیرون می‌زد — فروشگاه سفارشی می‌داد که فکر می‌کرد ثبت شده و روزِ بعد رد می‌شد.
     حالا ثبتِ سفارش قفل می‌زند، موجودیِ آزاد را می‌سنجد و رزرو می‌کند؛ تأیید ادعا
     را **مصرف** می‌کند و رد **آزادش**. تستی که ادعا می‌کرد «سفارش ثبت می‌شود و فقط
     تأیید شکست می‌خورد» عوض شد — رفتارِ باگ‌دار را قانونی کرده بود.
  2. **`tenant_scope` زمینه‌ی مستأجر را نشت می‌داد.** در `finally` فقط ContextVar
     برمی‌گشت؛ `db.info[SESSION_KEY]` و متغیرِ `app.tenant_id`ِ **خودِ Postgres** —
     که سیاستِ RLS رویش می‌نشیند — روی مستأجرِ **داخلی** جا می‌ماندند. نگزیده بود
     چون هیچ فراخوانی بعد از بلوک کارِ مستأجرمحور نمی‌کرد، ولی `place_order` دقیقاً
     همین را می‌خواست. با سه تست بازتولید شد (خروج، تودرتو، مسیرِ خطا) — هر سه قرمز
     — بعد رفع شد.
  3. **`GET /api/reports/kardex/{id}` برای هر کالایی که یک بار تعدیل خورده بود با
     ۵۰۰ می‌شکست.** `valuation.document_numbers` کورکورانه `model.number` می‌خواند و
     `StockAdjustment` — که از مهاجرتِ ۰۱۵۸ در `_SOURCE_MODELS` است — چنین ستونی
     ندارد. با درخواستِ واقعی بازتولید شد؛ سندِ بی‌شماره حالا برچسبِ بی‌شماره
     می‌گیرد.

  **۰۱۷۵ — قیمتِ مصرف‌کننده و سود (§۱۷ تا §۲۷).** سه قیمتِ **جدا**، نه یکی: «چاپی»
  آنچه واقعاً روی بسته نوشته شده، «پیشنهادی» نظرِ تولیدکننده، «حداکثر» سقفِ مجاز.
  هر سه تهی‌پذیر و نه «صفر = نامشخص»، چون §۳۰ می‌گوید فیلدِ بی‌مقدار اصلاً نباید
  نمایش داده شود و صفر یک مقدارِ معتبرِ دیگر است. `suggested_retail_price` روی
  **بار** ستونِ تازه نگرفت: `consumer_price` از مهاجرتِ ۰۰۷۰ دقیقاً همان تعریف را
  دارد و افزودنش یعنی دو نمای یک داده.

  **اشانتیون ردیفِ صفرقیمت نشد.** وسوسه‌ی طبیعی این بود که کارتنِ رایگان ردیفی با
  قیمتِ صفر شود — و غلط است: میانگینِ موزون از دفتر بازپخش می‌شود و ردیفِ صفرقیمت
  آن را پایین می‌کشد بی آنکه جایی نوشته باشد چرا. راهِ درست همان §۲۵ است: تعداد ۱۱
  با تخفیفی برابرِ بهای ۱ واحد، و آن‌وقت `post_purchase_invoice` **بی هیچ تغییری**
  همان `total_paid / 11` را حساب می‌کند. `bonus_qty` فقط یادداشتِ گزارشی است.

  سود و مارک‌آپ **زنده** خواسته شده بود (§۲۶) و درخواست به سرور به‌ازای هر کلید یعنی
  تأخیر، پس منطق دو جا هست: `app/margins.py` و `desktop/src/lib/margins.ts`. دو
  نسخه یعنی دو جا برای واگرایی، پس **هیچ‌کدام صاحبِ حقیقت نیست**: هر دو یک پرونده‌ی
  نمونه‌ی مشترک می‌خوانند و ۳۰ موردِ یکسان را می‌سنجند. پرونده سمتِ دسکتاپ نشست چون
  تایپ‌اسکریپت اجازه‌ی خواندن از بیرونِ `src` را نمی‌دهد ولی پایتون محدودیتی ندارد.
  درصدها با یک رقمِ اعشار و «نیم به بالا» سنجیده می‌شوند: پایتون `Decimal` دارد و
  جاوااسکریپت `float64`، مبلغِ صحیح در هر دو دقیق است ولی **درصد** در رقم‌های آخر
  فرق می‌کند — پس آنچه سنجیده می‌شود عددِ نمایشی است، نه خامِ محاسبه.

  **۰۱۷۶ — مرجوعیِ بارمحور، جایگزینی و جمع‌آوری (§۱۲ §۱۳ §۱۴).** «برگشت به همان
  باری که از آن رفته بود» بی هیچ ورودیِ تازه‌ای جواب دارد، چون خروج به‌ازای هر بار
  یک حرکتِ جدا می‌نویسد و `source_line_id` را هم می‌نشاند؛ کاربر لازم نیست چیزی
  انتخاب کند.

  «وارد موجودی قابل فروش نشود» **مکانیزمِ تازه نگرفت**: ادعای `blocked`ِ دفترِ رزرو
  دقیقاً همین است — از `available` کم می‌شود بی آنکه `physical` تکان بخورد. ستونِ
  `blocked_qty` یعنی دو کم‌شونده با دو مکانیزمِ مختلف که روزی با هم اختلاف پیدا
  می‌کنند. (۵۰ خرید، ۲۰ خروج، ۸ برگشتِ معیوب → فیزیکی ۳۸، مسدود ۸، قابلِ فروش ۳۰.)
  پیش‌فرضِ `return_condition` عمداً `sellable` است: هیچ مرجوعیِ ثبت‌شده‌ای یک‌شبه
  مسدود نمی‌شود.

  **جایگزینی جدول می‌خواهد، مرجوعی نه.** حرکتِ دفتر می‌گوید «چه شد» ولی نمی‌گوید
  **چرا** بارِ اول کنار گذاشته شد — و همان «چرا» است که ماهِ بعد کسی دنبالش می‌گردد.
  از هیچ داده‌ی موجودی مشتق نمی‌شود، پس `batch_substitutions` ستونِ دلیلِ اجباری
  دارد؛ خودِ جابه‌جایی جمعِ صفر است. برگه‌ی جمع‌آوری برعکس، **داده‌ی تازه‌ای نخواست**:
  بار و انقضا و محلِ قرارگیری همه از قبل بودند.

  **آنچه عمداً انجام نشد: بهای بچ‌محور (FIFO).** درخواست حرفی از بهای بار نزده بود؛
  بچ **مقدار** را ردیابی می‌کند نه قیمت. عوض‌کردنش یعنی بازنویسیِ `valuation.py` و
  هر گزارشِ مالیِ گذشته.
- **۱۴۰۵/۰۶/۲۹ (2026-09-20) — ماژولِ «حسابرسی»، فازِ ۱
  (شاخه‌ی `feat/assurance-module`، مهاجرت‌های `0177`–`0179`):**

  حسابرسی به‌صورتِ «نرم‌افزار + خدمت»: کاربر از داخلِ ماژول درخواست می‌دهد، ما
  تأیید می‌کنیم، و آن‌وقت زیرمنوها باز می‌شوند تا حسابرس دفتر را ببیند.
  * **کلیدِ ریشه `assurance` است نه `audit`** — `audit` از قبل مالِ *ردِ تغییرات*
    است (`app/audit.py`، `routers/audit.py`، ماژولِ مجوزِ `audit`). برچسبِ آن ماژولِ
    مجوز هم از «گزارش حسابرسی» به «ردِ تغییرات» عوض شد تا دو ردیفِ هم‌نام در
    ماتریسِ مجوزها نماند.
  * **گیت با ماژولِ «مشتق»، نه با گرنت.** `assurance_work` در هیچ ستونی ذخیره
    نمی‌شود و از خودِ قراردادِ باز مشتق می‌شود (`services/assurance_access.py`).
    دلیل: `set_grants` کلِ فهرست را جایگزین می‌کند و «مدیریت اکانت‌ها» آن فهرست
    را از یک آرایه‌ی ثابتِ کلاینت می‌سازد — پس اولین باری که سوپرادمین ماژولِ
    دیگری را برای همان حساب جابه‌جا کند، گرنتِ حسابرسی **بی‌صدا** پاک می‌شد.
    `allowed_modules`/`enabled_modules` پارامترِ کلیدواژه‌ایِ `derived` گرفتند که
    فراموش‌کردنش در **بسته‌شدن** می‌شکند نه در بازشدن؛ `require_module` خودش
    حسابش می‌کند تا گیتِ سرور و منو نتوانند اختلاف پیدا کنند.
  * **دسترسیِ حسابرس = عضویتِ موقتِ فقط‌خواندنی** با نقشِ تازه‌ی `auditor` و ورود
    از همان سوییچرِ موجود. RLS دست نخورد، چون تا امروز هیچ راهی برای دیدنِ دفترِ
    یک مستأجر توسطِ غیرِعضو وجود نداشت و ساختنش پرریسک‌ترین کارِ ممکن بود.
    ستونِ عمومیِ `memberships.expires_at` (NULL = بی‌انقضا) در `get_principal`
    سنجیده می‌شود — **صفر کوئریِ اضافه**، چون ردیفِ عضویت از قبل بارگذاری شده.
    بستنِ پرونده عضویت را `disabled` می‌کند. حسابرس صندلیِ پولیِ مشتری را هم مصرف
    نمی‌کند و مالکِ مشتری نمی‌تواند دامنه‌اش را پهن کند (ولی **می‌تواند**
    غیرفعالش کند — دفتر مالِ اوست).
  * **یک موتور، دو مخاطب.** دوازده بررسیِ تازه به `services/integrity.py` اضافه
    شد، نه به موتورِ دوم: `_check` پارامترِ `family` گرفت و `run_integrity_check`
    پارامترهای `families` و `row_limit`، هر دو با پیش‌فرضِ **دقیقاً رفتارِ امروز**.
    صفحه‌ی «بررسی یکپارچگی» همان نُه بررسی را با همان ترتیب می‌گیرد و
    `test_ledger_family_output_is_unchanged` قفلش می‌کند.
  * **نمره هرگز بدونِ توضیحش نشان داده نمی‌شود.** جدولِ «بررسی · شدت · تعداد ·
    امتیازِ ازدست‌رفته · از» خودِ نمره است. وزن‌ها در `assurance_runs.summary`
    **منجمد** می‌شوند تا تنظیمِ بعدی، کارنامه‌ی پارسالِ مشتری را بازنویسی نکند.
  * **هیچ‌چیز مداوم محاسبه نمی‌شود** — تصمیمِ صریحِ مالکِ محصول. سه نویسنده‌ی
    `assurance_runs`: تأییدِ قرارداد، اجرای دستی (با خنک‌کننده‌ی پنج‌دقیقه‌ای) و
    اجرای ستادی. نه کرونی، نه هوکی. اگر اجرای هنگامِ تأیید خطا بدهد، **تأیید
    پابرجا می‌ماند**: دفترِ خرابِ مشتری نباید جلوی دسترسیِ حسابرس را بگیرد.
  * **قرارداد جدولِ سراسری است** (`assurance_engagements` در `GLOBAL_TABLES`) چون
    کارتابلِ ستاد باید درخواست‌های همه‌ی کسب‌وکارها را کنارِ هم ببیند — ساختاراً
    همان `subscriptions`. بهایش: جداسازی در روتر، با **یک** تابعِ دسترسی و یک
    تستِ نشتیِ الزامی. اجرا و یافته‌ها زیرِ RLS ماندند، پس هر تغییرِ ستادی درونِ
    `tenant_scope(client)` اجرا می‌شود — وگرنه سیاستِ `WITH CHECK`ِ `audit_log`
    ردِ تغییر را بی‌صدا از دست می‌داد.
  * **یک نشتیِ خاموشِ عمومی هم بسته شد:** `menuEntryVisible` صفحه‌های *فهرست* را
    اصلاً گیت نمی‌کرد. تا امروز بی‌خطر بود چون خاموش‌شدنِ ماژول همه‌ی صفحه‌های
    منویش را می‌بُرد؛ حسابرسی این فرض را شکست (صفحه‌ی «درخواست» همیشه دیده
    می‌شود). حالا دفتر با همان ماژولی گیت می‌شود که صفحه‌های منویِ هم‌ماژولش —
    تغییری که فقط **می‌بندد**، هیچ‌جا باز نمی‌کند.
  * راستی‌آزمایی: ۳۵۴۶ تستِ بک‌اند (۸۰ تستِ تازه)، ۱۶۱ تستِ فرانت، مهاجرت
    رفت‌وبرگشت روی دیتابیسِ **دارای ردیف**، و مرورگر روی ۱۴۴۰ و ۳۹۰ — پیش از
    تأیید ۴۰۳ و منوی پنهان، پس از تأیید نمره و ۱۱۸ یافته و تاریخچه، صفر سرریز.

- **۱۴۰۵/۰۶/۲۹ (2026-09-20) — کارت‌های داشبورد را کاربر می‌چیند
  (شاخه‌ی `feat/dashboard-launchers`، مهاجرت `0170`):**

  باکسِ «شروعِ کارِ تازه» شش کارتِ **ثابت** داشت (`TASK_LAUNCHERS`) — همان شش‌تا
  برای پیمانکاری که هر روز صورت‌وضعیت می‌زند و برای فروشگاهی که فقط فاکتور می‌بُرد.
  حالا دکمه‌ی «انتخابِ کارت‌ها» درختِ ماژول‌ها و زیرمنوهایشان را باز می‌کند، کاربر
  تیک می‌زند و همان‌ها کارت می‌شوند.
  * **کاتالوگ از منابعِ موجود ساخته می‌شود، نه فهرستِ تازه** (`lib/launchers.ts`):
    `NAV_GROUPS` (گیت‌شده با `buildNav`) + `MODULE_SECTIONS` + `OPS_MENUS` +
    `LIST_MENUS` — دقیقاً همان چهار منبعی که دو کارتِ کنارِ سایدبار را می‌سازند. پس
    منویی که فردا اضافه شود، بی‌هیچ کاری در انتخاب‌گر هم هست. هم «عملیات» و هم
    «فهرست» قابلِ انتخاب‌اند (۴۴ دفتر، با فیلترِ نوع).
  * **ذخیره روی حساب، نه روی مرورگر:** `memberships.dashboard_cards` (JSONB).
    سطحِ *عضویت* است نه کاربر، چون کارت‌ها به ماژول‌های همان کسب‌وکار اشاره می‌کنند؛
    حسابداری که در دو شرکت عضو است، در شرکتِ بی‌انبار کارتِ «رسید انبار» نمی‌بیند.
    نتیجه: همان چیدمان روی وب، ویندوز و هر دستگاهِ دیگر.
  * **`NULL` و `[]` یکی نیستند.** `NULL` یعنی «هنوز انتخاب نکرده» → پیش‌فرض‌ها؛
    `[]` یعنی «عمداً خالی». اگر ستون `server_default='[]'` می‌گرفت، هیچ‌کس
    نمی‌توانست باکس را خالی نگه دارد. به همین دلیل «بازگرداندن به پیش‌فرض» یک
    `DELETE` است که ستون را دوباره `NULL` می‌کند، نه ذخیره‌ی کپیِ منجمدی از
    پیش‌فرضِ امروز — وگرنه کارتِ پیش‌فرضِ فردا هرگز به کاربرِ قدیمی نمی‌رسید.
  * **سرور معنا را نمی‌سنجد، شکل را می‌سنجد.** فهرستِ صفحه‌ها فقط در فرانت است و
    نسخه‌ی دومش روی سرور دیر یا زود واگرا می‌شد؛ پس روتر فقط الگوی
    `page` یا `page/section`، سقفِ ۲۴ کارت و یکتایی را می‌سنجد و شناسه‌ی ناشناخته
    هنگامِ نمایش کنار می‌رود (ماژولی که خاموش شود کارتِ مرده جا نمی‌گذارد).
  * **یک باگِ قدیمیِ خاموش هم رفت:** لانچرِ «دریافت و پرداخت» به `contacts/treasury`
    می‌رفت — تبی که وجود ندارد — پس هم کارت و هم فرمانِ کامندپالت بی‌صدا به تبِ اولِ
    «اشخاص» می‌افتادند. حالا «رسید دریافت» است، و تستِ
    `هر پیش‌فرض در کاتالوگ هست` نگذارد دوباره تکرار شود.
  * **هر دو داشبورد یک کامپوننت‌اند** (`LauncherBoard`): «راهنما» و کلاسیک. نکته‌ی
    قابلِ ذکر: هر سه تمِ فعلی `content: 'guided'`اند، پس داشبوردِ کلاسیک امروز به
    کسی نشان داده نمی‌شود — سیم‌کشی شد تا اگر تمِ کلاسیکی برگشت، عقب نماند.
  * راستی‌آزمایی: ۳۴۶۶ تستِ بک‌اند، ۱۵۴ تستِ فرانت (۱۳ تستِ تازه‌ی کاتالوگ)،
    مهاجرت رفت‌وبرگشت، و مرورگر روی ۱۴۴۰ و ۳۹۰ — ذخیره، `refresh`، بازگرداندن به
    پیش‌فرض، صفر سرریز، صفر خطای کنسول.

- **۱۴۰۵/۰۶/۲۹ (2026-09-20) — سقفِ health در `deploy.sh` از ۳۰ به ۱۲۰ ثانیه
  (شاخه‌ی `fix/deploy-health-timeout`):**

  استقرارِ ماژولِ «شرکت» شکست خورد و خودش برگشت، در حالی که هیچ اشکالی نداشت:
  بالاآمدنِ اپ روی همین ماشین حدودِ **۳۵ ثانیه** طول می‌کشد (ایمپورتِ SQLAlchemy و
  ساختِ متادیتا) ولی `deploy.sh` فقط ۳۰ ثانیه صبر می‌کرد. یعنی گاردِ سلامت، خودش
  منبعِ خرابی بود: سرویسِ سالم را ناسالم می‌خواند، کد را برمی‌گرداند و استقرار را
  دو برابر طولانی می‌کرد. سقف ۱۲۰ ثانیه شد و لاگ هم می‌گوید در چند ثانیه بالا آمد.

- **۱۴۰۵/۰۶/۲۹ (2026-09-20) — تاکسونومیِ جامع: از ۹۴ صنف به ۸۰۲ صنف
  (شاخه‌ی `feat/master-taxonomy`):**

  فهرستِ ۹۴تاییِ همان روز برای «بستنی‌فروشی» و «لوازم یدکی» خوب بود، ولی خیلی از
  کسب‌وکارهای واقعی خودشان را در آن پیدا نمی‌کردند و «سایر» می‌زدند — و هرچه
  `other` بیشتر انتخاب شود، هدف‌گیریِ پخش‌کننده بی‌اثرتر می‌شود.
  * **۶۵ گروه، ۸۰۲ صنف.** «فهرستِ همه‌ی مشاغلِ دنیا» نیست (چنین چیزی انتها ندارد)؛
    فهرستِ *عملیاتیِ بازار ایران* است.
  * **کاملاً Additive.** هر ۹۴ کلیدِ منتشرشده سرِ جایش ماند، با همان برچسب. تنها
    چیزی که عوض شد گروه‌بندی بود — و کلیدِ گروه هیچ‌جا ذخیره نمی‌شود (تنها مصرفش
    `key` propِ ری‌اکت روی `<optgroup>` است)، پس جابه‌جاییِ «بستنی‌فروشی» از «مواد
    غذایی» به «نان و شیرینی» هیچ داده‌ای را نشکست.
  * **`test_legacy_keys_survive_every_taxonomy_change`** فهرستِ صریحِ آن ۹۴ کلید را
    در خودِ تست قفل می‌کند. سه چیز آزاد است — جابه‌جایی بینِ گروه‌ها، اصلاحِ برچسب،
    افزودنِ ردیف. آنچه آزاد نیست، *برداشتنِ* یک کلید است: صنفِ اعلام‌شده‌ی یک
    کسب‌وکارِ واقعی به رشته‌ی ناشناخته تبدیل می‌شود و از هدف‌گیری می‌افتد، **بی‌آنکه
    خطایی بدهد**. تستِ دوم می‌گوید هرکدام باید برچسبِ فارسی هم داشته باشد.
  * **سه برچسبِ تکراری حذف شد** (اولین ظهورِ هر برچسب ماند): «کفش ایمنی»
    (`safety_footwear` رفت)، «تعمیر ساعت» (`watch_service` رفت) و «دوربین
    مداربسته» (`security_camera` رفت، `cctv` ماند). دو ردیفِ هم‌نام در انتخابگر
    برای کاربر تفکیک‌ناپذیرند.
  * **تاکسونومی از خودِ فایلِ منبع تولید شد**، نه دستی — «مو‌به‌مو» این‌طور یک
    ادعا نیست، خروجی از همان منبع می‌آید. اسکریپتش فایل را پارس می‌کند، قاعده‌ی
    حذفِ برچسبِ تکراری را می‌زند و بلوکِ `TRADE_GROUPS` را می‌نویسد.
  * **انتخابگر بازنویسی شد**، چون ۸۰۱ چک‌باکس در یک جعبه‌ی اسکرول عملاً غیرقابلِ
    استفاده است: فیلدِ جست‌وجو با همان `textMatches` (نیم‌فاصله و یِ عربی)،
    گروه‌ها پیش‌فرض جمع، و شمارنده‌ی «۳ از ۲۰» روی سرِ هر گروه. دو ریزه‌کاری که در
    تست قفل شدند: شمارنده از **کلِ گروه** است نه از نتایجِ جست‌وجو (وگرنه کاربر فکر
    می‌کند انتخاب‌هایش پاک شده)، و جست‌وجو گروهِ دارای نتیجه را **باز** می‌کند
    (نتیجه‌ای که داخلِ گروهِ بسته بماند یعنی جست‌وجو کار نمی‌کند).
  * **بدونِ مهاجرت.** `tenants.trade` از قبل `String(40)` است و بلندترین کلیدِ تازه
    ۳۰ کاراکتر (`industrial_parts_manufacturing`)؛ دو ستونِ دیگر JSONB اند.
  * پاسخِ `/api/trades` از ~۶ به **~۵۸ کیلوبایت** رسید؛ `useTrades` کشِ سطحِ ماژول
    دارد، پس یک بار در هر نشست.

- **۱۴۰۵/۰۶/۲۹ (2026-09-20) — ماژولِ «شرکت» به سبکِ فرمِ سازمانی
  (شاخه‌ی `feat/company-forms-redesign`):**

  ادامه‌ی همان کارِ «حقوق و دستمزد» و «حسابداری»، این بار روی ۱۶ صفحه‌ی کارت‌های
  «عملیات» و «فهرست»ِ شرکت. منطق و API دست نخوردند.
  * **صفحه‌ها:** طرف حساب جدید، تراکنش شریک، فروش اقساطی، مرکز هزینه، محل‌های
    جغرافیایی، گروه جدید، عملیات اول دوره، عملیات پایان سال، یادآوری پایان سال،
    تقویم و یادآوری، گزارش‌ساز، فعالیت‌های روز، گزارش استفاده — و فهرست‌ها:
    طرف حساب‌ها، تراکنش‌های شریک، افراد مرتبط، قراردادهای اقساطی، همه اقساط،
    مراکز هزینه، گروه‌ها، رویدادهای تقویم، گزارش‌های پویا.
  * **بومِ `ef-form` روی صفحه‌های بی‌`OpsPage` هم آمد:** این صفحه‌ها `.page.panels`ِ
    خودشان را دارند، پس بوم یک `<div className="ef-form">` دورِ کارت‌هاست.
  * **دو گریدِ قدیمی (`cmp-form` و `invoice-form`) و تبِ قدیمی (`cc-tabs`) زیرِ بوم
    ظاهرِ تازه گرفتند** — به‌جای بازنویسیِ ده‌ها فیلدِ فرمِ طرف‌حساب. قاعده‌ها همه زیرِ
    `.ef-form`اند، پس همان فرم‌ها در ماژول‌های بازطراحی‌نشده دست‌نخورده‌اند.
  * **فرم‌هایی که کامل بازنویسی شدند:** گروه، محلِ جغرافیایی، افراد مرتبط، تراکنش
    شریک، رویدادِ تقویم، فرمِ قراردادِ اقساطی و کارتِ ذخیره‌ی گزارش‌ساز — با
    `FormGrid`/`FormField`، واحدِ «ریال»/«٪» درونِ فیلد و نوارِ چسبنده.
  * **ثبتِ ناقص** پیام می‌دهد و مکان‌نما را روی اولین فیلدِ خالی می‌برد (به‌جای دکمه‌ی
    خاموش) — در هر هفت فرم.
  * **جدول‌ها `ef-table` شدند**، نشان‌های `badge` به `status-badge` رنگی، و کنشِ ردیف
    به دکمه‌ی آیکونیِ `RowAction`.
  * **راستی‌آزمایی:** هر ۲۲ صفحه‌ی ماژول در ۱۴۴۰ و صفحه‌های اصلی در ۳۹۰ — همه‌ی
    کنترل‌ها ۴۰px، سرریز ۰، خطای کنسول ۰.

- **۱۴۰۵/۰۶/۲۹ (2026-09-20) — اصناف: تفکیکِ صنفِ کسب‌وکار + هدف‌گیریِ صنف در بازارِ پخش
  (شاخه‌ی `feat/trades-taxonomy`):**

  کاربر خواست اصناف (لوازم یدکی‌فروشی، ابزارفروشی، بستنی‌فروشی، …) در پروژه باشند و
  دسته‌بندی شوند، موقعِ ثبت‌نام انتخاب شوند، و **شرکت‌های پخش اعلام کنند به کدام
  اصناف جنس می‌دهند**.
  * **چرا `industry` کافی نبود.** آن ستون هم «صنف» نامیده می‌شد ولی کارِ دیگری
    می‌کرد: پنج گزینه‌ی درشت که **قالبِ ماژول‌های پنل** را انتخاب می‌کنند، و فقط
    سوپرادمین می‌توانست عوضش کند. حالا دو مفهوم صریحاً جدا شده‌اند و در UI هم
    «قالب» از «صنف» جدا نامیده می‌شود.
  * **تاکسونومی** در `backend/app/services/trades.py`: **۹۴ صنف در ۱۲ گروه**، ثابت
    در کد. چرا ثابت و نه جدولِ per-tenant: این یک برچسبِ محلی نیست، **کلیدِ تطبیق
    بینِ دو مستأجر** است؛ اگر هر کسب‌وکار صنفِ خودش را می‌نوشت، «لوازم یدکی» و
    «یدکی فروشی» دو چیز می‌شدند و هدف‌گیری بی‌صدا از کار می‌افتاد.
  * **`GET /api/trades` عمومی است** (بدونِ توکن) چون صفحه‌ی ثبت‌نام هم فهرست را
    می‌خواهد. عمداً از سرور می‌آید نه از ثابتی در فرانت: پنج صنفِ درشت امروز در
    `SignupScreen.tsx` دستی تکرار شده‌اند، و برای ~۹۰ ردیف این یعنی دو فهرستی که
    دیر یا زود از هم دور می‌شوند.
  * **`tenants.trade`** (مهاجرتِ `0168`) عمداً nullable است و backfill ندارد.
    `NULL` یعنی «هنوز اعلام نکرده»، که با «هیچ‌کدام» فرق دارد — و همین تفاوت است
    که بازار را برای حساب‌های موجود دست‌نخورده نگه می‌دارد.
  * **`marketplace_settings.target_trades`** — فهرستِ خالی یعنی «بدونِ محدودیت»، پس
    هیچ پخش‌کننده‌ی فعالی با این ارتقا از بازار غیب نمی‌شود. تبِ تنظیمات به‌جایش
    هشدار می‌دهد تا قابلیت ندیده نماند.
  * **قاعده‌ی تطبیق** در `distributor_matches_trade` جمع شده و چهار حالتش تست دارد.
    دو تای بحرانی: «فروشگاهِ بی‌صنف همه را می‌بیند» و «پخش‌کننده‌ی بی‌هدف پنهان
    نمی‌شود» — این دوتا هستند که با یک بی‌دقتی، بازار را برای حساب‌های موجود خاموش
    می‌کنند.
  * **فیلترِ سخت دلیلش را می‌گوید.** چون پخش‌کننده‌ی نامرتبط *پنهان* می‌شود (نه
    پایین‌تر)، تبِ «پخش‌کننده‌ها» می‌نویسد «بر اساسِ صنفِ شما (X) فیلتر شده» — وگرنه
    فروشگاه یک صفحه‌ی کم‌پشت می‌بیند و نتیجه می‌گیرد بازار خالی است.
  * **اعلامِ صنف دستِ خودِ مالک است** (`PUT /api/modules/trade`, کارتِ تازه در
    «شخصی‌سازیِ پنل»). بدونِ آن هیچ حسابِ موجودی نمی‌توانست صنفش را بگوید و
    هدف‌گیری فقط برای حساب‌های تازه کار می‌کرد.
  * **اصنافِ اضافه‌ی یک قلمِ کاتالوگ** (`marketplace_listings.extra_trades`، مهاجرتِ
    `0169`). هدف‌گیریِ بالا دانه‌ی کلِ کسب‌وکار را دارد: یا همه‌ی کاتالوگ برای یک
    صنف باز است یا هیچ‌کدام. تولیدیِ پوشاکی که «لباس کار» هم می‌سازد، برای رساندنش
    به یدکی‌فروشی مجبور بود یدکی را به اصنافِ کلی‌اش اضافه کند — و آن‌وقت
    یدکی‌فروش کلِ پیراهن و مانتو را هم می‌دید.
    * **اضافه می‌کند، جایگزین نمی‌کند** (`listing_visible_to_trade`): مخاطبِ هر قلم
      = اصنافِ کلی ∪ اصنافِ اضافه‌ی قلم. پس تغییرِ بعدیِ اصنافِ کلی خودکار روی همه‌ی
      اقلام اثر می‌گذارد و تله‌ی «چرا کار نکرد» ساخته نمی‌شود. قیدش: پخش‌کننده‌ی
      بدونِ صنفِ کلی نمی‌تواند یک قلم را محدود کند — و **فرم همان‌جا می‌گویدش**،
      چون انتخابِ بی‌اثرِ بی‌صدا بدترین حالت است.
    * **کشف هم باید بداند.** بدونِ تغییرِ `list_distributors_for_retailer`،
      یدکی‌فروش هرگز آن تولیدی را پیدا نمی‌کرد و «لباس کار» برایش وجود نداشت. حالا
      پخش‌کننده‌ای که حتی یک قلمِ منتشرشده‌اش به صنفِ فروشگاه می‌رسد هم می‌آید، و
      کارتش می‌گوید «n قلم از m قلم برای صنفِ شما» تا فروشگاه پیش از اتصال بداند
      کاتالوگِ باریکی در انتظارش است.
    * **فیلتر گارد است نه تزئین:** `place_order` هم همان قاعده را می‌زند، وگرنه با
      `listing_id`ِ حدس‌زده می‌شد قلمی را سفارش داد که دیده نمی‌شود.
    * `TradePicker` مشترک شد (تنظیمات + فرمِ لیستینگ + ویزارد) و منطقِ انتخابش در
      `lib/tradeSelection.ts` تست می‌شود: «کلِ گروه» در حالتِ **نیمه‌انتخاب** باید
      گسترش بدهد نه پاک کند.

- **۱۴۰۵/۰۶/۲۸ (2026-09-19) — ماژولِ «حسابداری» به سبکِ فرمِ سازمانی
  (شاخه‌ی `feat/accounting-forms-redesign`):**

  همان الگوی «حقوق و دستمزد» روی هر ۲۹ صفحه‌ی کارت‌های «عملیات» و «فهرست»ِ حسابداری.
  منطق، API و محاسبه‌ها دست نخوردند.
  * **`OpsPage` پارامترِ `canvas` گرفت:** بومِ `ef-form` با کارت‌های سایه‌دار، نوارِ بازه و
    ارقامِ سرِ صفحه در کارتِ بالا، و `ActionBar`ِ چسبیده. ماژول‌هایی که هنوز بازطراحی
    نشده‌اند (فروش، خزانه، انبار) `canvas` ندارند و دست‌نخورده‌اند — قاعده‌های تازه‌ی CSS
    هم همه زیرِ `.ef-form`اند.
  * **دکمه‌ی اصلی از سرِ کارت به نوارِ پایین رفت** و وضعیتِ زنده کنارش می‌نشیند: جمعِ
    بدهکار/بستانکار، سود/زیانِ دوره، تعدادِ سندِ انتخاب‌شده. دکمه دیگر خاموش نمی‌ماند؛
    اگر چیزی انتخاب نشده باشد پیام می‌دهد.
  * **جدول‌ها `ef-table` شدند** (سرستونِ خاکستری، قابِ گرد) و کنش‌های ردیف `RowAction`.
    جست‌وجوها `SearchField`، شمارها `CountBadge`.
  * **ویزاردِ «سند حسابداری» حذف شد** (`JournalEntryWizard`): فرمِ تازه دو کارت دارد —
    سربرگ و ردیف‌ها — با «افزودن ردیف» زیرِ جدول و توازن در نوارِ پایین. پیامِ
    `journalEntryDraft` حالا `{text, kind}` است.
  * **رفعِ متنِ انگلیسی در ستونِ «منشأ»:** ‏`warehouse_receipt`، `asset_disposal`،
    `asset_improvement`، `production_plan`، `owner_transaction`، `pos_settlement`،
    `contract_settlement`، `payment_withdrawal` و `payment_void` برچسبِ فارسی گرفتند.
    `production_order` هم بعد از تفکیکِ سفارش/سند «سند تولید» شد.
  * **FormKit:** `InputAffix` (واحد درونِ فیلد)، `Switch`، `CountBadge`، `RowAction`،
    `AddRowButton`، `FormGrid cols`، `FormField optional`، `SectionCard badge`.
  * **راستی‌آزمایی:** هر ۲۹ صفحه در ۱۴۴۰ و ۳۹۰ — ارتفاعِ همه‌ی کنترل‌ها ۴۰px، سرریز ۰،
    خطای کنسول ۰.

- **۱۴۰۵/۰۶/۲۸ (2026-09-19) — ۳۹۳ واحد شمارش + جست‌وجو در هر فهرستِ انتخاب + جست‌وجویی
  که در عرض‌های کوچک پنهان نمی‌شود (شاخه‌ی `feat/units-and-searchable-selects`):**

  سه کار با یک ریشه: کاربر باید بتواند چیزی را که می‌خواهد **پیدا** کند.
  * **واحدهای شمارش ۱۹ → ۳۹۳** (`STANDARD_UNITS` در `backend/app/seed.py`، مهاجرتِ
    `0167`). علتِ اینکه کسب‌وکارها فقط دو-سه واحد داشتند در `0118` نوشته شده بود:
    واحدها **از خودِ داده** ساخته می‌شدند و فهرستِ استاندارد فقط به کسب‌وکارِ *بی‌کالا*
    می‌رسید — یعنی هر کسی که یک کالا داشت، آن ۱۹تا را هم هرگز ندید. مهاجرتِ تازه
    فهرست را به هر کسب‌وکاری می‌دهد که نداردش (`NOT EXISTS`)، و `downgrade` فقط
    واحدهای بی‌استفاده را برمی‌دارد. «بار» و «بازدید» در فهرستِ کاربر دوبار بودند و
    چون نام در هر کسب‌وکار یکتاست، یک ردیف ماندند.
  * **`SearchSelect`** — جایگزینِ درجای `<select>`: همان `value`/`onChange`/`<option>`.
    **۳۵۶ فهرست در ۹۳ فایل** تبدیل شد. زیرِ ۸ گزینه خودش `<select>`ِ بومی رندر می‌کند،
    پس «وضعیت: همه/باز/بسته» کادرِ بی‌معنی نمی‌گیرد و روی موبایل چرخِ بومی می‌ماند.
    تطبیق از `textMatches` می‌آید، پس «کا»→«کارتن»، «كارتن» با کافِ عربی و «متر مربع»
    با نیم‌فاصله هر سه کار می‌کنند. قاعده‌ی ممیزِ **R15** جلوی `<select>`ِ خامِ بعدی را می‌گیرد.
  * **جست‌وجو در عرض‌های کوچک پنهان نمی‌شود.** پیش از این دو جا برش می‌داشت
    (`topnav--tight` و `@media (max-width: 760px)`)، و چون کشوی همبرگری کادرِ جست‌وجو
    ندارد و `Ctrl+K` روی موبایل صفحه‌کلیدی ندارد که زده شود، **زیرِ ۷۶۰px بیرون از
    داشبورد هیچ راهی به جست‌وجو نبود**. حالا نوار یک کنترل دارد که فقط *کوچک* می‌شود:
    قرصِ «جست‌وجو… Ctrl+K» ← ذره‌بینِ ۳۴px. هر دو همان کامندپالتی را باز می‌کنند که
    `Ctrl+K` باز می‌کند، پس کادرِ تایپِ قبلیِ نوار (که فقط منوها را می‌گشت) حذف شد و
    نتیجه در هر عرضی یکی است. `open`ِ پالت به `Dashboard` بالا رفت.
  * **نردبانِ نوار در `desktop/src/lib/topnavFit.ts`** جدا و سنجیدنی شد. چهار چیدمان
    به ترتیبِ مطلوبیت آزموده می‌شوند و «پنهان» در نوعِ خروجی **وجود ندارد**. دو خطای
    اندازه‌گیری هم همان‌جا درست شد: همبرگر *جایگزینِ* منوست نه اضافه بر آن (شمردنِ
    همیشگی‌اش نوار را در عرض‌های میانی بی‌دلیل به کشو می‌فرستاد، و نشمردنش روی ۳۲۰px
    ۴۴px سرریز می‌داد)، و زیرِ ۱۰۲۴px که مدیاکوئری خودش منو را برمی‌دارد، سنجش آن را
    از `display: none` می‌فهمد به‌جای اینکه عرضِ صفر را «جا شد» بخواند.
  * **پله‌ی تازه‌ی `@media (max-width: 1200px)`** برای نوار (فونت/فاصله‌ی ریزتر): ۱۱۴px
    آزاد می‌کند، پس ماژول‌ها دیرتر به کشو می‌روند — همان «فونت کوچک شود، نه اینکه چیزی
    پنهان شود» که کاربر خواست.
  * **`placePopover`** (`desktop/src/lib/popover.ts`) — `ItemPicker` و `SearchSelect` دو
    رونوشتِ کلمه‌به‌کلمه از حسابِ جاگذاری داشتند که برای ارتفاعِ فهرست کفِ ۱۴۰px
    می‌گذاشت حتی وقتی زیرِ فیلد جا نبود؛ چون پاپ‌آور `fixed` است، آن قسمت نه دیده
    می‌شد و نه اسکرول به آن می‌رسید. حالا یک پیاده‌سازی: اگر پایین جا نباشد **بالای**
    فیلد باز می‌شود (با `bottom` نه `top`، تا به ارتفاعِ رندرشده نیاز نباشد) و در
    پنجره‌ی خیلی کوتاه به ویوپورت می‌چسبد.
  * **راستی‌آزمایی:** ۲۳ تستِ خالصِ تازه (نردبان + جاگذاری) کنارِ ۷ تستِ مهاجرت و ۱۱
    تستِ `SearchSelect`. و با مرورگر: نوار در ۱۵ عرض از ۱۹۲۰ تا ۳۲۰ — جست‌وجو در همه
    هست، سرریزِ افقی همه‌جا صفر؛ و پاپ‌آورِ فهرست در ۴ ویوپورت × ۳ جای فیلد، همیشه
    داخلِ صفحه با کادرِ جست‌وجوی فوکوس‌دار.

- **۱۴۰۵/۰۶/۲۸ (2026-09-19) — «جداول مالیات» بازطراحی شد + جدولِ فهرستِ سازمانی (`ef-table`)
  (شاخه‌ی `feat/tax-tables-redesign`):**

  کاربر برای این صفحه دستورِ جزءبه‌جزء داد. منطق و API دست نخورد.
  * **فرم:** گریدِ دوستونه (`FormGrid cols={2}`: عنوان | گروه، تاریخ اجرا | نوع محاسبه).
    «عنوان دوم» حالا فیلدِ تمام‌عرضِ «توضیح» با برچسبِ «اختیاری» است
    (`FormField optional`) و زیرِ عنوان در فهرست دیده می‌شود.
  * **پله‌ها:** جدولِ `ef-table` با واحد درونِ فیلد (`InputAffix`: «ریال»، «٪»). «از مبلغ»
    ورودی نیست، چون مدل «سقفِ تجمعی» است و مرزِ پایین مشتق است. به‌جایش جعبه‌ی
    فقط‌خواندنیِ هم‌شکل با مرزِ خط‌چین دارد.
    بی‌سقفیِ پله‌ی آخر کلیدِ «بدون سقف (تا بی‌نهایت)» شد (`Switch`) و دیگر نمادِ ∞ تایپ نمی‌شود.
    «افزودن پله» (`AddRowButton`) زیرِ آخرین ردیف است. پله‌ی تازه بی‌سقف می‌شود و
    فوکوس روی سقفِ پله‌ی قبلی می‌رود. سقفِ خالی و پله‌ی آخرِ سقف‌دار با پیام و فوکوس
    گرفته می‌شوند.
  * **فهرست:** عنوان با شمار (`SectionCard badge` + `CountBadge`: «۳ جدول ثبت‌شده»).
    جدول سرستونِ خاکستریِ ملایم و قابِ گرد دارد. «پله‌ها» چیپ است («۴ پله»).
    ویرایش و حذف دکمه‌ی آیکونی‌اند (`RowAction`) و در کارتِ موبایل متن هم دارند.
    حذفِ جدولِ «در استفاده» غیرفعال است و دلیلش روی hover دیده می‌شود. ردیفِ در حالِ ویرایش رنگ می‌گیرد.
  * **وضعیت:** «جاری» (سبز)، «بایگانی» (خاکستری) و «آینده». در کلاینت و با همان قاعده‌ی
    `resolve_tax_table` حساب می‌شود: تازه‌ترین جدولِ رسیده در هر «گروه + نوع» جاری است.
    `status-badge.tone-info` تازه است.
  * **جدولِ `ef-table` عمومی است** و برای فهرست‌های ماژول‌های بعدی ساخته شده.
    قاعده‌هایش فقط بالای ۷۶۰px‌اند و زیرِ آن `cards-on-mobile` حاکم است. اختصاصیتش
    (۰،۳،۲) از `:root[data-structure='guided'] .page th` بالاتر است.
    **تله:** `NumberInput` خودش `dir="ltr"` است، پس `padding-inline-end` واحد را از سمتِ
    اشتباه جا باز می‌کرد و عدد ۴۸px از لبه فاصله می‌گرفت. قاعده‌ی جدا
    `.ef-affix > input[dir='ltr']` همین را درست می‌کند.
    سقفِ پله دیگر `allowDecimal` ندارد، چون ریالِ صحیح است و
    `toAmount` موقعِ بارگذاری «…٫۰۰»ِ سرور را برمی‌دارد.

- **۱۴۰۵/۰۶/۲۸ (2026-09-19) — همه‌ی زیرمنوهای «حقوق و دستمزد» به سبکِ «قرارداد جدید»
  (شاخه‌ی `feat/payroll-forms-redesign`):**

  کاربر خواست بازطراحیِ «قرارداد جدید» روی همه‌ی صفحه‌های کارت‌های «عملیات» و «فهرست»ِ
  این ماژول بیاید. پانزده صفحه: محل خدمت، شغل، عوامل، گروه مالیاتی و شعب، جداول مالیات،
  نوع وام، وام پرسنلی، تسویه حساب، اطلاعات استقرار، کارکرد و صدور فیش، تنظیمات حقوق،
  پرسنل و احکام، مزایا، قراردادها، مرور حقوق. منطق و API دست نخورد.
  * **الگوی مشترک:** بومِ `ef-form` با کارت‌های جدا، `FormGrid`ِ حداکثر سه‌ستونه، همه‌ی
    کنترل‌ها ۴۰px (پیش‌تر ۳۲ و ۳۶ قاطی)، راهنماهای ثابت ← «؟» (`tip` روی `FormField` و حالا
    روی خودِ `SectionCard`)، و `ActionBar`ِ چسبیده با دکمه‌ی اصلی. صفحه‌ی «فرم + فهرست»
    `<form>`ِ خودش را درونِ بوم دارد تا نوارِ چسبنده فقط تا پایانِ همان فرم بچسبد و روی
    جدولِ زیرش نیفتد.
  * **ثبتِ ناقص** دیگر دکمه را خاموش نمی‌کند: پیام در نوار + فوکوس روی اولین فیلدِ خالی
    (`components/form/firstMissing.ts`).
  * **FormKit** این‌ها را گرفت: `FormStatus` (پیامِ نوار)، `InlineCreate` (از فرمِ قرارداد
    بیرون آمد؛ «+»ِ نوعِ وام در «وام پرسنلی» هم از آن است)، `ListToolbar` و `SearchField`
    برای نوارِ فیلترِ فهرست‌ها.
  * **ساختار:** «گروه مالیاتی و شعب» دو تب شد (هر تب فرم + فهرستِ خودش). «تنظیمات حقوق»
    کارتِ سال/نرخ‌ها + کارتِ جزئیات با چهار تب؛ خطای پلکان کاربر را به تبِ پلکان می‌برد.
    «کارکرد و صدور فیش» هر سه قدم را روی یک صفحه دارد (دوره ← کارکرد و عوامل ← فیش‌ها) و
    صدور در نوارِ پایین است. «مزایا»: صدورِ عیدی کارِ اصلیِ نوار، ثبتِ مرخصی کارتِ خودش.
  * **ویزاردهای «پرسنل» و «کارکرد» حذف شدند** (`EmployeeWizard`، `PayrollRunWizard`): در
    پوسته‌ی «راهنما» فقط همین دو صفحه‌ی ماژول ویزارد بودند و بقیه فرم؛ حالا هر دو پوسته
    یک فرم دارند. `TaskFlow` برای ماژول‌های دیگر دست‌نخورده است.
  * **رفعِ خطاهای کوچکِ هم‌مسیر:** ذخیره‌ی کارکرد خطای سرور را بی‌صدا می‌بلعید؛ سال و
    شماره‌ی سند در پیام‌ها و دکمه‌ها رقمِ لاتین بود؛ درصدِ ۷ در ویرایشِ جدولِ مالیات
    ۷٫۰۰۰۰۰۰۰۰۰۰۰۰۰۰۱ نشان داده می‌شد. **تله:** سقفِ پله حالا `NumberInput` است؛ بدونِ
    `allowDecimal` نقطه‌ی «…٫۰۰»ِ سرور حذف و عدد صد برابر می‌شد — برای همین هم
    `allowDecimal` دارد و هم موقعِ بارگذاری `String(Number(v))` می‌شود.

- **۱۴۰۵/۰۶/۲۸ (2026-09-19) — «قرارداد جدید» بازطراحی شد + اجزای فرمِ سازمانی (`FormKit`)
  (شاخه‌ی `feat/contract-form-redesign`):**

  کاربر فرمِ «مشخصات قرارداد» را با استانداردِ داشبوردهای سازمانی خواست. منطقِ فرم،
  اعتبارسنجی و API دست نخورد؛ فقط رابط.
  * **`components/form/FormKit.tsx` (تازه):** `FormField` (برچسب + ستاره + «؟»)، `InfoTip`،
    `FormGrid`، `SelectWithAdd`، `FormTabs` (نقش‌های ARIA؛ در راست‌به‌چپ «چپ» = تبِ بعدی)،
    `TabHead` و `ActionBar`. سبک‌ها زیرِ `ef-*` در `App.css`. هر فرمِ بلندِ دیگری
    می‌تواند همین را بگیرد.
  * **گرید:** حداکثر سه‌ستونه با `minmax(max(220px, (100% − 2gap)/3), 1fr)` — بدونِ
    media query. container query امتحان نشد چون روی کارت stacking context می‌سازد و
    تقویمِ بازشده‌ی کارتِ بالا زیرِ کارتِ پایینی می‌رفت.
  * **یک ارتفاع:** همه‌ی ورودی‌ها، فهرست‌ها، دکمه‌ی تاریخ و دکمه‌ی «+» ۴۰px (پیش‌تر ۳۲ و ۳۶
    قاطی بود). آیکونِ تقویم در همه‌ی تاریخ‌ها در انتهای فیلد.
  * **راهنماها:** هفت متنِ ثابتِ زیرِ فیلدها و دو متنِ بالای تب‌ها به «؟» کنارِ برچسب رفتند.
    برچسب با `htmlFor` به کنترل وصل است نه با پیچیدن: `<label>`ی که دکمه‌ی «؟» هم دارد اولین
    کنترلِ درونش را هدف می‌گیرد. `JalaliDatePicker` برای همین `id` گرفت.
  * **کارمند (`payroll/EmployeePicker.tsx`):** جست‌وجو و انتخاب یک combobox شد (قبلاً دو
    فیلد). «کارمندی پیدا نشد» فقط بعد از جست‌وجوی ناموفق دیده می‌شود.
  * **دکمه‌های ساخت:** «محل خدمت/شغل تازه بسازید» زیرِ فهرست‌ها به «+» کنارِ فهرست رفت؛
    «ساخت عوامل پیش‌فرض» گوشه‌ی سرِ تب است و حالتِ خالی یک کادرِ نقطه‌چین.
  * **نوارِ عملیاتِ چسبیده به پایین:** «ثبت قرارداد» (اصلی) و «فهرست قراردادها» (ثانویه).
    نتیجه‌ی ثبت یا خطا همان‌جا می‌آید، وگرنه جمعِ ماهانه‌ی حقوق و کسورات. ثبتِ بی‌کارمند
    پیام می‌دهد و فوکوس را به فیلدِ کارمند می‌برد، به‌جای دکمه‌ی خاموشِ بی‌توضیح.
  * **تله:** `button:hover` سراسری (0,2,1) رنگِ تبِ فعال را زیرِ موس زرد می‌کرد؛ قاعده‌ی
    تبِ فعال با `.ef-tabs` جلویش نوشته شد.

- **۱۴۰۵/۰۶/۲۷ (2026-09-18) — سایتِ معرفی: رنگِ برند آبیِ نیلی، بدونِ سبز
  (شاخه‌ی `feat/website-indigo`):**

  کاربر رنگِ برند را آبیِ نیلی اعلام کرد و سبز را نخواست. همه‌چیز از توکن‌های `.cc-root`
  در `concept.css` می‌آید، پس ساختارِ صفحه دست نخورد.
  * **پالت:** اصلی `#4F46E5` (دکمه‌ها، هایلایت، آیکونِ فعال)؛ لهجه‌ی فیروزه‌ایِ `#06B6D4`
    فقط برای نشان‌ها (تخفیفِ دوره، برچسبِ بنر، نقطه‌ی نشانِ بنر)؛ زمینه‌ی بنر و دعوتِ
    پایانی `#1E1B4B` تا `#312E81`؛ بخش‌های روشن `#F8FAFC`؛ متن و خط‌ها از طیفِ slate.
    پاصفحه `#0F172A`. فاویکن و نشانِ برند نیلی شدند.
  * **سرصفحه:** فاصله‌ی عمودیِ ۱۶px به‌جای ارتفاعِ ثابت؛ «۱۴ روز رایگان» دکمه‌ی اصلی.
  * **هیرو:** H1 با وزنِ ۸۰۰ و فاصله‌ی ۱۶px. ستون‌ها مساوی شدند (`1fr 1fr`) و چهار
    کارتِ کنارِ متن هم‌ارتفاع‌اند. `Vazirmatn-UI-ExtraBold.woff2` از همان نسخه‌ی ۳۳٫۰٫۳
    اضافه شد (فایلِ Boldِ همان بسته با فایلِ موجود بایت‌به‌بایت یکی است)؛ بدونِ آن مرورگر
    ۸۰۰ را جعلی می‌ساخت.
  * **دکمه‌ها:** padding برابر با ۱۲×۲۸px و line-height ۱٫۵. با line-height ۱ جعبه‌ی متنِ
    وزیرمتن از گلیف‌ها کوچک‌تر بود و متن نسبت به آیکون جابه‌جا می‌نشست.
  * **کارت‌ها:** padding ۲۴px، گردیِ ۱۲px، سایه‌ی `0 10px 30px rgba(15,23,42,.05)` روی همه‌ی
    کارت‌ها (امکانات، پلن، صنایع، سوالات، صفحه‌های حقوقی).
  * دعوتِ پایانی گرادیانِ برعکسِ بنر دارد: تیره‌ترین سمت چپ است، جایی که دکمه‌های
    نیلی می‌نشینند، تا از زمینه جدا بمانند.

- **۱۴۰۵/۰۶/۲۷ (2026-09-18) — کارتِ «فهرست» دوباره منوی کاملِ فهرستِ هر ماژول است
  (شاخه‌ی `fix/restore-list-menus`):**

  #۱۲۴ کارتِ «فهرست» را به یکی‌دوتا ردیفِ «مالِ همین عملیات» (`listsForOps`) محدود کرده
  بود. بعد از استقرار، کاربر گفت «تمام زیرمنوهای فهرست که برای ماژول‌ها تعریف کرده بودیم
  حذف شده»: دارایی ثابت از پنج ردیف یکی نشان می‌داد، تولید یکی از پنج، تامین‌کنندگان و
  انبار یکی از دوازده، و دریافت و پرداخت و سامانه‌ی مؤدیان اصلاً کارت نداشتند.
  * `ModulePanels` دوباره منوی گروه (`LIST_MENUS`) را نشان می‌دهد، و برای ماژولِ
    تب‌داری که منوی گروه ندارد تب‌های `kind: 'list'` را. فیلترِ `menuEntryVisible` روی آن
    می‌ماند.
  * بقیه‌ی #۱۲۴ دست نخورد: نُه صفحه‌ای که به ستونِ عملیات رفتند همان‌جا ماندند، `listsForOps`
    و آزمون‌هایش و R14 سرِ جایشان‌اند. پنهان‌شدنِ کارت وقتی هیچ فهرستی نیست هم ماند.

- **۱۴۰۵/۰۶/۲۷ (2026-09-18) — سایتِ معرفی (cubita.ir): طراحیِ اداری، روشن و کارت‌محور
  (شاخه‌ی `feat/website-corporate-redesign`):**

  کاربر سایت را «ساده، اداری و شیک» خواست، با مشخصاتِ روشن: سبزِ `#00A859`، زمینه‌ی
  خاکستریِ `#F8F9FA` برای بخش‌ها، کارت با گردیِ ۸ تا ۱۲px و سایه‌ی نرم، و مقیاسِ قلمِ
  H1 ۳۰ / H2 ۲۴ / H3 ۱۸ / متن ۱۴ با فاصله‌ی خطِ ۱٫۸. صفحه‌ی اصلی (`ConceptLanding`) از
  تیره‌ی طلایی به همین طراحی رفت؛ محتوا و ترتیبِ بخش‌ها همان ماند.
  * **بنرِ تمام‌عرض زیرِ منو** (خواستِ کاربر): دعوتِ کوتاه به شروعِ رایگان و دانلود،
    با نشانِ برند به‌جای تصویر. بعد از آن هیروی دوستونه: متن + چهار کارتِ «واقعیت».
  * **خوشه‌ی کارت‌های شناورِ هیرو (`HeroCluster`) از صفحه‌ی اصلی حذف شد**، به خواستِ
    کاربر. فایلش ماند چون `/classic` از آن استفاده می‌کند. `HeroVisual.tsx` که از قبل
    استفاده نمی‌شد و سبک‌هایش با این بازنویسی رفت، حذف شد.
  * **`concept/SiteChrome.tsx`:** سرصفحه و پاصفحه‌ی مشترک. صفحه‌های «شرایط استفاده»،
    «حریم خصوصی» و «نتیجه‌ی پرداخت» هم همین را گرفتند؛ پیش‌تر سرصفحه‌ی تیره‌ی
    `/classic` را داشتند. پاصفحه پنج‌ستونه شد (برند، محصول، دانلود، پشتیبانی، قوانین).
  * **قلم:** IRANSansX تجاری است و در مخزن نیست؛ فعلاً «وزیرمتن» با همان مقیاس. با
    فایل‌های مجوزدار فقط ‎@font-face‎ و `--cc-font` عوض می‌شوند.
  * **تله‌ی `App.css`:** قاعده‌ی سراسریِ `section { padding: 80px 0 }` برای `/classic`
    روی هر `section`ِ بی‌padding می‌نشیند؛ بنر `padding: 0` صریح دارد. پایه‌ی
    `a { color: inherit }` با `:where` نوشته شد تا رنگِ دکمه‌های لینکی را نپوشاند.
  * فاویکن سبز شد و `theme-color` سفید. `og-image.jpg` هنوز تیره‌ی طلایی است.

- **۱۴۰۵/۰۶/۲۷ (2026-09-18) — «ورود گروهی اشخاص» به ماژولِ «تنظیمات» رفت
  (شاخه‌ی `feat/contact-import-settings`):**

  به خواستِ کاربر، ورودِ گروهیِ اشخاص از تبِ صفحه‌ی «اشخاص» بیرون آمد و صفحه‌ی مستقلِ
  `contactimport` در گروهِ «تنظیمات» شد، بعد از «روش‌های شماره‌گذاری»، کنارِ بقیه‌ی
  کارهای راه‌اندازی. پنل همان `BulkImportPanel` است و هیچ API‌ای عوض نشد.
  * `PAGE_MODULE_KEY.contactimport = 'contacts'`: صفحه در «تنظیمات» است ولی داده‌اش
    طرف‌حساب است، پس کسب‌وکارِ بی‌ماژولِ اشخاص نمی‌بیندش.
  * `OPS_LIST_MAP.contactimport = 'contactlist'`: اشخاصِ واردشده همان «طرف حساب‌ها» هستند.
  * تبِ `import` از `ContactsPage` و `MODULE_SECTIONS.contacts` حذف شد. «ورود گروهی
    کالا» تبِ «انبار» ماند.
  * **`menuEntryVisible` (navModel):** ورودی‌های منوی گروه (`LIST_MENUS`/`OPS_MENUS`)
    ثابت‌اند و فیلتر نمی‌شدند. پنج ردیفِ بازارِ عمده‌فروشی که #۱۲۲ به فهرستِ «تنظیمات»
    داد («سفارش‌های پخش»، «مرجوعی‌های پخش»، «کمیسیون پخش»، «سفارش‌های من»، «مرجوعی‌های
    من») برای هر کسب‌وکاری دیده می‌شدند و برای کسب‌وکارِ عادی صفحه‌ی خالی باز می‌کردند.
    حالا ورودی‌ای که صفحه‌اش ردیفِ منوی اصلی دارد ولی در منوی همین کسب‌وکار نیست (ماژولِ
    خاموش، نوعِ دیگرِ کسب‌وکار) در `ModulePanels`، کشوی موبایل و کلیکِ گروه در نوارِ بالا
    نمی‌آید. صفحه‌های فهرست (که ردیفِ منوی اصلی ندارند) دست نخوردند.

- **۱۴۰۵/۰۶/۲۷ (2026-09-18) — منوهای «تامین‌کنندگان و انبار»: کار‌به‌کار، نه صفحه‌به‌صفحه
  (شاخه‌ی `feat/supply-warehouse-menus`):**

  این گروه دو صفحه‌ی تب‌دار است («خرید» ۵ تب و «انبار» ۱۸ تب) به‌علاوه‌ی «اعلامیه بدهکار
  بستانکار». کاربر منو را بر اساسِ کار خواست: شش عملیات و شش فهرستِ مشخص. کارهای منوهای
  قدیمی را هم خودمان جا دادیم. هیچ API‌ای عوض نشد؛ فرم‌ها و دفترها همان کامپوننت‌های قبلی‌اند.
  * **`OPS_MENUS` (moduleLists):** منوی «عملیات»ِ یک گروه، مستقل از `NAV_GROUPS`. هر
    ورودی یک صفحه است یا تبی از آن (`section`). `ListMenuItem` هم `section` گرفت.
    `NAV_GROUPS` دست نخورد، چون ماژول‌ها، مجوزها، جست‌وجو و «شخصی‌سازیِ پنل» با صفحه کار
    می‌کنند. `ModulePanels`، کشوی موبایل و کلیکِ گروه در نوارِ بالا (روی اولین کار، «رسید
    انبار») از آن می‌خوانند. `menuEntryActive` فعال‌بودنِ ورودی را با صفحه و تب می‌سنجد.
  * **عملیات (۲۰):** رسید انبار · حواله انبار · رسید/حواله انتقال بین انبارها · تگ
    انبارگردانی · ثبت مغایرت انبارگردانی · قیمت‌گذاری اسناد انبار. سپس از منوهای قدیمی:
    فاکتور خرید · فاکتور خرید خدمات · برگشت از خرید · برگشت خروج انبار · تعدیل دستی ·
    قیمت‌گذاری ورودی‌ها · اعلامیه بدهکار بستانکار · کالاها · انبارها · واحدها · گروه و
    مشخصات · لیست قیمت · انواع کسورات · ورود گروهی کالا. تعریف‌ها (کالا، انبار، واحد،
    …) فرم و جدولشان یک کار است و در «عملیات» ماندند.
  * **فهرست (۱۲):** فهرست رسیدها و حواله‌های انبار · کاردکس کالا · مرور انبار / موجودی
    کالا · فهرست تامین‌کنندگان · گزارش نقطه سفارش · فهرست انبارگردانی‌ها. سپس: فاکتورهای
    خرید · فاکتورهای خرید خدمات · برگشت‌های خروج انبار · اعلامیه‌های بدهکار و بستانکار ·
    جستجوی سریال · بچ و انقضا.
  * **جداکردنِ فرم از دفتر.** `view: 'form' | 'ledger'` روی `WarehouseReceiptsTab`،
    `WarehouseIssuesTab`، `IssueReturnsTab` و `ServicePurchaseTab`. پیش‌فرض همان هر دو
    است، پس بقیه‌ی مصرف‌کننده‌ها دست نخوردند. تبِ تازه‌ی `documents` دفترِ رسیدها و
    حواله‌ها را کنارِ هم می‌گذارد؛ انتقال‌ها همان حواله‌های نوعِ «انتقال»اند، پس دفترِ
    جدای تبِ انتقال حذف شد. «رونوشت» از دفترِ فاکتورهای خرید و خدمات پیش‌نویس را در
    صفحه می‌گذارد و به تبِ فرم می‌رود.
  * **انبارگردانی سه نما شد:** `StockCountPanel` با `mode` = `tags` (ساختِ جلسه +
    چاپِ برگه) / `variance` (شمارش و ثبتِ نهایی) / `list`. کلیدِ `count` برای «ثبت مغایرت»
    ماند تا پیوندِ «انبارگردانی» از داشبوردِ شرکت همان‌جا بیاید. «ثبت مغایرت» بی‌انتخاب
    با تازه‌ترین جلسه‌ی باز باز می‌شود.
  * **«فهرست تامین‌کنندگان» = `supplierlist`:** همان `ContactListPage` با
    `presetRole="supplier"`، نه جدولِ دوم.
  * **صفحه‌ی مشترکِ دو گروه در گروهِ فعلی می‌ماند.** `groupOf` در `ModulePanels` گروهی
    را که کاربر در آن بود ترجیح می‌دهد. «اعلامیه بدهکار بستانکار» از منوی انبار دیگر
    ستون‌ها را به منوی فروش عوض نمی‌کند.
  * `MODULE_SECTIONS.inventory` دو تبِ «واحدها» و «گروه و مشخصات» را نداشت (ناهم‌خوان
    با `<Tabs>`)؛ درست شد.
  * `verify-pages.mjs` با `groupMenuTargets()` هر ۳۲ ورودیِ این گروه را جداگانه می‌سنجد.
    پیش از این، صفحه‌ی «انبار» با زیررشته‌ی «رسید انبار» پیدا می‌شد که تبِ صفحه‌ی خرید
    است، پس خودِ انبار سنجیده نمی‌شد.

- **۱۴۰۵/۰۶/۲۶ (2026-09-17) — منوهای «تولید»: عملیات و فهرست از هم جدا شدند، و گروهِ
  «سفارش کار» حذف شد (شاخه‌ی `fix/manufacturing-menus`):**

  همان الگوی ردیفِ «دارایی ثابت» در پایین، این بار برای تولید. هیچ API‌ای عوض نشد.
  * **عملیات:** فرمول‌های ساخت · سفارش تولید · تحویل مواد · رسید محصول · محاسبه قیمت
    تمام‌شده. **فهرست** (`kind: 'list'`): فهرست فرمول‌های ساخته‌شده · سفارشات تولید ·
    انحراف مصرف مواد · کاردکس تولید · گزارش قیمت تمام‌شده.
  * **دو تبِ فهرستیِ تازه، با جدول‌های موجود.** `bom-list` همان جدولِ فرمول‌هاست که کنارِ
    فرمِ «فرمول‌های ساخت» بود (ویرایش، فعال/غیرفعال، حذف). `order-list` همان جدولِ
    سفارش‌هاست که کنارِ فرمِ «سفارش تولید» بود (تغییرِ وضعیت). ویرایشِ فرمول از فهرست،
    فرمول را در `bomToEdit`ِ صفحه می‌گذارد و به تبِ `boms` می‌رود. `BomsTab` هنگامِ
    mount آن را در فرم بار می‌کند.
  * **سرتیترِ گروهِ ناوبری از «سفارش کار» به «تولید» تغییر کرد.** در کشوی موبایل
    «تولید» زیرِ «سفارش کار» بود. حالا گروهِ تک‌ماژولیِ هم‌نام است و بخش‌ها مستقیم زیرِ
    سرتیترِ «تولید» می‌آیند. نوارِ بالای دسکتاپ از قبل «تولید» نشان می‌داد.

- **۱۴۰۵/۰۶/۲۶ (2026-09-17) — منوهای «دارایی ثابت»: عملیات و فهرست از هم جدا شدند
  (شاخه‌ی `fix/fixedassets-menus`):**

  بازچینیِ منو، نه بازنویسیِ صفحه. هیچ فرم، جدول یا API‌ای عوض نشد.
  * **ردیفِ تکراریِ نامِ ماژول حذف شد.** «ریشه»ای که ردیفِ پایینی (`fix/nav-hierarchy`)
    به ماژول‌های تک‌صفحه‌ایِ تب‌دار داده بود، در ستونِ «عملیات» یک زیرمنوی «دارایی
    ثابت» زیرِ خودِ «دارایی ثابت» می‌ساخت. کاربر این را اشتباه دانست. برای تولید،
    مؤدیان و اتصال فروشگاه هم برداشته شد. در کشوی موبایل هم گروهی که فقط یک ماژولِ
    هم‌نام دارد (دارایی ثابت، سامانه مؤدیان، اتصال فروشگاه) دیگر ردیفِ ماژول نمی‌گیرد
    و بخش‌ها مستقیم زیرِ سرتیترند. «سفارش کار ← تولید» چون نامشان فرق دارد دست نخورد.
  * **`SectionDef.kind: 'list'`:** بخشی که دفتر است نه کار. `ModulePanels` آن را در
    ستونِ «فهرست» می‌گذارد، به‌جای ردیف‌های سریعِ `ListPanel`. کشوی موبایل هم آن را
    زیرِ تیترِ «فهرست» می‌آورد. هنوز یک تبِ همان صفحه است، پس `<Tabs>` و `setSection`
    دست نخوردند. `MODULE_LISTS.fixedassets` می‌ماند، چون سازنده‌ی گزارش منبعش را از
    آن‌جا می‌خوانَد.
  * **ترتیبِ خواسته‌ی کاربر.** عملیات: کارت دارایی · تحویل و استقرار · محاسبه استهلاک ·
    صدور سند استهلاک · تغییر روش یا عمر مفید · جابه‌جایی دارایی · خروج دارایی ·
    تعمیرات اساسی. فهرست: فهرست دارایی‌ها · فهرست محاسبات استهلاک · گزارش اسناد
    استهلاک · جابه‌جایی‌ها و تحویل‌ها · خروج و فروش دارایی.
  * **«فهرست دارایی‌ها» تبِ تازه‌ی `registry` است.** همان جدولِ `FixedAssetsList` که
    کنارِ فرمِ «کارت دارایی» بود، جابه‌جا شده و کشوی کارتِ کامل هم با آن آمده. دکمه‌ی
    ویرایشِ ردیف درفت را پر می‌کند و به تبِ «کارت دارایی» می‌رود (`onEdit`).
  * `verify-pages.mjs` گروهِ بی‌ردیفِ ماژول را می‌شناسد.

- **۱۴۰۵/۰۶/۲۶ (2026-09-17) — سلسله‌مراتبِ منو: بخش‌های ماژول در کشوی موبایل، و
  بازچینیِ ستونِ «عملیات/فهرست» (شاخه‌ی `fix/nav-hierarchy`):**

  **کشوی موبایل (`TopNav`) — سطحِ سوم اضافه شد.** «تولید» در کشو فقط یک لینک بود و
  بخش‌هایش (فرمول‌های ساخت، سفارش تولید، …) هیچ‌جای کشو نبودند؛ روی موبایل تنها راهِ
  رسیدن به آن‌ها نوارِ تبِ داخلِ صفحه بود. حالا ماژولِ تب‌دار با ضربه **باز** می‌شود
  و بخش‌هایش با خطِّ راهنما زیرش تورفته می‌آیند؛ ماژولِ فعال هنگامِ بازشدنِ کشو
  خودکار باز است و بخشِ فعال هایلایت دارد (از `useNavSection`).
  * همه‌ی سطح‌ها (گروه، ماژول، بخش، فهرست، خروج) از یک کامپوننتِ `MobileRow` می‌سازند:
    آیکون و متن راست، فلش چپ، `justify-content: space-between`، ستونِ آیکونِ ثابتِ
    ۲۰px — حتی برای گروهِ بی‌آیکون («میزکار»)، که پیش از این متنش را به جای آیکون
    می‌چسباند. هر ۱۴ سرتیتر حالا دقیقاً هم‌ترازند.
  * **کادرِ زردِ گزینه‌ی فعال حذف شد.** ته‌رنگ از `--topnav-active` می‌آمد و در
    پوسته‌ی Tipalti طلایی روی سرمه‌ای یک کادرِ قهوه‌ایِ گل‌آلود با آیکون و متنِ زرد
    می‌ساخت. حالا ته‌رنگ از رنگِ **متن** است (خنثی)، آیکون هم‌رنگِ متن، و تنها
    نشانه‌ی رنگی نوارِ نازکِ ۳px است — که برای بخشِ فعال روی خودِ خطِّ راهنما می‌نشیند.
  * تیترهای داخلیِ «عملیات»/«فهرست» (۱۱px، کم‌رنگ) فقط در گروهی که هر دو را دارد.
  * عرض `width: 80vw; max-width: 320px`؛ پس‌زمینه‌ی نیمه‌شفاف با کلیک می‌بندد؛
    ورودِ کشو و محوشدنِ پس‌زمینه انیمیشن دارند (با `prefers-reduced-motion` خاموش).

  **ستونِ «عملیات/فهرست» (`ModulePanels`، بالای ۱۰۲۴px):**
  * **ماژول‌های تک‌صفحه‌ایِ تب‌دار ریشه گرفتند** (تولید، دارایی ثابت، مؤدیان، اتصال
    فروشگاه): بخش‌ها تخت و بی‌ریشه بودند و چیزی نبود که «فرمول‌های ساخت» نسبت به آن
    تورفتگی داشته باشد. حالا ردیفِ ماژول ریشه است و بخش‌ها در `.mod-sub` زیرش،
    هم‌شکلِ ماژول‌های چندصفحه‌ای. قاعده‌ی «ردیفِ هم‌نامِ گروه تکراری است» برای
    گروهِ چندصفحه‌ای دست نخورد.
  * **صفحه‌ی والد دیگر هم‌شکلِ زیرمنوی فعال برجسته نمی‌شود** (`mod-op--parent`:
    پررنگ، بی‌ته‌رنگ) — هایلایت فقط مالِ بخشِ انتخاب‌شده است.
  * تیترِ «عملیات»/«فهرست» از ۱۲px/`--text-h` (پررنگ‌تر از خودِ لینک‌ها، ۱۳٫۸ در
    برابرِ ۹٫۵) به ۱۱px/`--text-muted` رفت — همچنان بالای ۴٫۵:۱ در هر سه پوسته.
  * **سه باگ:** (۱) زیرمنوی فعال در پوسته‌ی روشن متنِ `--accent-text` (سفید) روی
    ته‌رنگِ تقریباً سفید داشت — **کنتراستِ ۱٫۱۶:۱**؛ توکنِ تازه‌ی `--accent-on-tint`
    (تیره: accent-hover، روشن: accent-hover، Tipalti: سرمه‌ای) آن را به ۵٫۳/۶٫۱/۱۴٫۵
    رساند. (۲) `.mod-op:hover` از `--surface-3` می‌خواند که **هیچ‌جا تعریف نشده** —
    hover پس‌زمینه نداشت و فقط قاب و متنِ accent از قاعده‌ی سراسریِ
    `button:hover:not(:disabled)` می‌گرفت، یعنی شبیهِ فعال بود. (۳) آیکونِ تیتر و
    آیکونِ ردیف‌ها ۸px جابه‌جا بودند؛ حالا ستونِ ۱۷px مشترک دارند و خطِّ راهنمای
    زیرمنو دقیقاً زیرِ مرکزِ آیکونِ والد است.
  * `--surface-3` جاهای دیگری هم بی‌تعریف مصرف شده (نشانِ سالِ مالیِ بسته و …) —
    خارج از این تغییر، دست نخورد.

  `verify-pages.mjs` با ساختارِ تازه‌ی کشو به‌روز شد: گروه را باز می‌کند، و برای
  ماژولِ تب‌دار پس از بازکردن، بخشِ اول را می‌زند.

- **۱۴۰۵/۰۶/۲۵ (2026-09-16) — بازسازیِ UI/UX کشوی منوی موبایل
  (شاخه‌ی `fix/mobile-menu-ux`):**

  کشوی موبایل (`.topnav-mobile`، تنها منوی موبایل چون هر سه پوسته
  `shell: 'topnav'` هستند) **همه‌ی ۱۴ گروه را باز و تخت** نشان می‌داد. با احتسابِ
  منوهای «فهرست»ِ هر گروه یعنی ۱۶۵ ردیف روی یک صفحه‌ی ۳۹۰px — بیش از سه صفحه
  اسکرول برای پیداکردنِ یک ماژول.

  * **آکاردئون:** هر گروه باز/بسته می‌شود، فقط یکی هم‌زمان، و گروهی که صفحه‌ی فعال
    در آن است هنگامِ باز شدنِ کشو خودکار باز می‌شود. گروهِ بسته‌ای که صفحه‌ی فعال را
    دارد یک نقطه‌ی رنگی می‌گیرد. ارتفاعِ محتوا از ~۲۳۰۰px به ~۸۴۰px رسید.
  * **سلسله‌مراتب:** سرتیتر ۱۵px/۷۰۰ در برابرِ آیتم ۱۳px/۵۰۰؛ زیرمنوها ۲۳px تورفته
    با خطِّ راهنمای عمودی، و ردیف‌های «فهرست» یک پله بیشتر (۳۶px) و سبک‌تر.
  * **فاصله‌گذاری:** `padding: 10px 12px` و `line-height: 1.6` روی آیتم‌ها.
  * **دو باگِ واقعی که پیدا شد و حل شد:**
    1. `.topnav-mobile-item.active` و `:hover` **هر دو** `--topnav-hover` بودند —
       یعنی انگشتِ روی یک آیتم همان‌قدر برجسته می‌شد که صفحه‌ی بازِ فعلی. بدتر:
       قاعده‌ی سراسریِ `button:hover:not(:disabled)` (ویژگیِ ۰،۲،۱) رنگِ متن را
       accent می‌کرد و از قاعده‌های ۰،۲،۰ی کشو بالاتر می‌نشست، پس hover **دقیقاً**
       هم‌رنگِ فعال می‌شد. حالت‌های تعاملی زیرِ `.topnav-mobile` تودار شدند تا
       ببرند، و فعال ته‌رنگِ accent + نوارِ عمودیِ ۳px گرفت.
    2. سربرگِ کشو `flex-shrink` پیش‌فرض داشت و کلِ کشو یک ظرفِ `overflow-y: auto`
       بود، پس با فهرستِ بلند فشرده می‌شد و **دکمه‌ی بستن روی نامِ برند می‌نشست**.
       حالا سربرگ/بدنه/فوتر سه ناحیه‌ی مستقل‌اند و فقط بدنه می‌لغزد. دکمه‌ی X هدفِ
       لمسیِ ۴۴px گرفت (پیش از این فقط آیکونِ ۱۸px) و سربرگ `safe-area-inset` را
       رعایت می‌کند.
  * **کنتراست:** خطوطِ جداکننده `rgba(255,255,255,…)` بودند و در پوسته‌ی روشن
    (نوارِ `#f8f8f8`) **نامرئی**؛ با توکن‌های `--topnav-line`/`--topnav-line-soft`
    که از رنگِ متن مشتق می‌شوند جایگزین شدند. `--topnav-active` روی زمینه‌ی
    `#181818` فقط ۳٫۹۲:۱ می‌داد (زیرِ AA)، پس توکنِ `--topnav-active-text` اضافه شد
    که در پوسته‌ی تیره روشن‌تر است و در دو پوسته‌ی دیگر همان تأکید را نگه می‌دارد.
    هر پنج رنگِ متنِ کشو در **هر سه پوسته** بالای ۴٫۵:۱ سنجیده شد.
  * «خروج» از آکاردئون بیرون آمد و در فوترِ ثابت نشست — کاری که باید یک ضربه باشد.

  **نکته‌ی پیاده‌سازی:** پروژه Tailwind ندارد، پس درخواستِ کلاس‌های Tailwind با
  همان سیستمِ توکن/CSS خودِ پروژه پیاده شد (`space-y` → `gap`، `pl-6` →
  `padding-inline-start`، `bg-indigo-600/20` → `color-mix` روی توکنِ accent).

  **راستی‌آزمایی:** رفتارِ آکاردئون، تفکیکِ hover از active، کارکردِ دکمه‌ی X،
  تورفتگی و کنتراست همه روی ۳۹۰px و در هر سه پوسته اندازه‌گیری شدند؛ سرریزِ افقی ۰،
  خطای کنسول ۰.

- **۱۴۰۵/۰۶/۲۵ (2026-09-16) — دارایی ثابت، فازِ ۳ و ۴: تعمیراتِ اساسی، تغییرِ
  برآورد، تفکیکِ محاسبه از صدورِ سند، و کارتِ داراییِ کامل
  (مهاجرت `0166`، شاخه‌ی `feat/asset-disposal`):**

  با این دو فاز، ماژول به همان ساختاری رسید که کاربر خواسته بود: **هفت عملیات و
  پنج فهرست**، در دوازده تب.

  **مهم‌ترین تغییرِ فنی — استهلاک حالا آینده‌نگر است.** فرمولِ قدیم «(بها − اسقاط) ÷
  عمرِ اولیه» بود و با هر تغییرِ بعدی غلط می‌شد: تمدیدِ عمر از ۱۲ به ۲۴ ماه پس از ۶
  دوره، مبلغِ ماهانه را غلط می‌داد و دارایی سرِ جایِ بی‌ربطی تمام می‌شد. حالا
  «ماندهٔ استهلاک‌پذیر ÷ ماه‌های باقیمانده» است — همان برخوردی که استاندارد برای
  «تغییر در برآوردِ حسابداری» می‌خواهد. شمارشِ دوره‌ها از خودِ
  `depreciation_entries` با یک `GROUP BY` می‌آید، نه از شمارنده‌ای روی دارایی که
  می‌توانست از آن جدا بیفتد. برای داراییِ دست‌نخورده نتیجه دقیقاً همان قبل است.

  * **`asset_improvements`** — مخارجِ سرمایه‌ایِ پس از تحصیل. سند: بدهکارِ داراییِ
    ثابت، بستانکارِ حسابِ تأمین — همان اتفاقِ خرید. مرزش با «هزینه‌ی تعمیرات» همان
    مرزِ استاندارد است و در فرم نوشته شده: تعمیرِ نگه‌دارنده هزینه‌ی دوره است و
    اینجا نمی‌آید. اختیاراً عمرِ مفید را هم تمدید می‌کند.
  * **`asset_estimate_changes`** — ردِ حسابرسیِ تغییرِ روش/عمر/ارزشِ اسقاط، **بی‌سند**
    و آینده‌نگر. گاردها: عمرِ تازه کمتر از دوره‌های ثبت‌شده نباشد، و مبنای تازه از
    استهلاکِ انباشته کمتر نشود.
  * **روشِ دومِ استهلاک: `declining_balance`** (ماندهٔ نزولی، نرخِ مضاعف). بدونِ آن،
    «تغییرِ روش» صفحه‌ای بود با یک گزینه. `ck_fixed_assets_method` باز شد.
  * **تفکیکِ محاسبه از صدورِ سند.** تبِ قدیمیِ «استهلاک دوره» هم‌زمان سه کار می‌کرد
    (محاسبه، ثبت، نمایشِ فهرست) و اشتباهِ تاریخ یعنی سندی که باید ابطال شود. حالا
    `POST /api/depreciation/preview` چیزی نمی‌نویسد و از همان
    `_pending_depreciation` می‌خواند که ثبت هم از آن می‌خواند — پس پیش‌نمایش
    **دقیقاً** همان چیزی است که ثبت می‌شود، نه تخمینی موازی.
  * **دو گزارشِ استهلاک با دو دانه‌بندی:** «فهرست محاسبات» سطحِ ردیف (دارایی×دوره) و
    «گزارش اسناد» سطحِ سند (`GROUP BY` در پایگاه‌داده، نه جمع‌زدن در پایتون).
  * **کارتِ داراییِ کامل** — کشوی `AssetCardDrawer` با کلیک روی نامِ هر دارایی:
    مشخصات، خروج، تحویل‌ها، مخارج، تغییرهای برآورد و استهلاک‌ها. نمای دومِ همان
    داده نیست؛ برشِ یک دارایی است که در آن تب‌ها پنج فیلترِ جدا می‌خواست.

  **راستی‌آزمایی:** ۳۳۰۸ تست سبز (۱۷ تستِ تازه)، رفت‌وبرگشتِ مهاجرت، و سناریوی
  زنده‌ی استهلاک→تعمیرات→تغییرِ روش→گزارش‌ها→کارتِ دارایی روی ۱۴۴۰ و ۳۹۰ با سرریزِ
  ۰ و کنسولِ ۰. عددها در دفتر بررسی شدند: پس از ۳۰۰٬۰۰۰ مخارج و تمدیدِ ۶ ماه،
  دوره‌ی بعد ۱٬۴۰۰٬۰۰۰÷۱۷ = ۸۲٬۳۵۳؛ با روشِ نزولی ۱٬۴۰۰٬۰۰۰×۲÷۱۸ = ۱۵۵٬۵۵۶.

- **۱۴۰۵/۰۶/۲۵ (2026-09-16) — دارایی ثابت، فازِ ۲: خروجِ دارایی با سندِ واقعی
  (مهاجرت `0165`، شاخه‌ی `feat/asset-disposal`):**

  **این فاز یک باگِ حسابداری را می‌بندد، نه فقط قابلیتی را اضافه کند.** تا امروز
  `dispose_asset` فقط `is_disposed` را روشن می‌کرد: دارایی از چرخه‌ی استهلاک بیرون
  می‌آمد ولی **بهای تمام‌شده و استهلاکِ انباشته‌اش تا ابد در ترازنامه می‌ماندند**.
  یعنی ترازنامه دارایی‌ای را نشان می‌داد که دیگر وجود نداشت، و سود/زیانِ واگذاری
  هیچ‌جا شناسایی نمی‌شد.

  * جدولِ تازه‌ی `asset_disposals` با سه نوعِ خروج (`sale` / `scrap` / `donation`)،
    مبلغِ دریافتی، حسابِ دریافت، خریدار، و **عکسِ اعدادِ لحظه‌ی خروج**
    (`cost_at_disposal` / `accumulated_at_disposal` / `book_value` / `gain_loss`).
    عکس‌برداری عمدی است: بدونِ آن، گزارشِ خروج با هر ویرایشِ بعدیِ دارایی بی‌صدا
    عوض می‌شد و دیگر با سندی که واقعاً خورده نمی‌خواند.
  * سندِ خروج: **بدهکار** استهلاکِ انباشته + حسابِ دریافت، **بستانکار** داراییِ
    ثابت، و مابه‌التفاوت روی دو نقشِ تازه‌ی چارت — `asset_disposal_gain` (۴۱۱۲) و
    `asset_disposal_loss` (۵۱۲۴). دو حساب و نه یکی، به همان استدلالِ
    `FX_GAIN`/`FX_LOSS`: واگذاریِ پرسود و واگذاریِ زیان‌ده نباید در یک عددِ خالص
    گم شوند. کدها ۴۱۱۲/۵۱۲۴ گرفته شدند چون ۴۱۱۰–۴۱۱۱ و ۵۱۲۰–۵۱۲۳ مالِ قالب‌های
    صنفی‌اند (تله‌ی آشنای ۴۱۰۴/۵۱۰۷).
  * اسقاط و اهدا حالتِ خاصِ حسابداری نیستند، فقط بی‌عوض‌اند — کلِ ارزشِ دفتری زیان
    می‌شود. اسکیما مبلغِ دریافتی روی این دو را **رد می‌کند**، وگرنه سودِ فروش زیرِ
    عنوانِ «اسقاط» گم می‌شد.
  * قیدِ یکتای `(tenant_id, asset_id)`: یک دارایی دو بار خارج نمی‌شود.
  * مجوزِ نقطه‌ی خروج از `update` به **`approve`** رفت — حالا سندِ مالی می‌زند.

  **رابط:** دو تبِ تازه (مجموعاً هفت): «خروج دارایی» با پیش‌نمایشِ زنده‌ی سود/زیان
  پیش از ثبت، و «خروج و فروش دارایی» به‌عنوان گزارش با فیلترِ نوع/بازه و چهار
  کارتِ جمع. دکمه‌ی «واگذاری» در کارتِ دارایی دیگر یک `confirm()` تکی نیست —
  به تبِ خروج می‌برد با همان دارایی انتخاب‌شده، چون سند بدونِ نوع و مبلغ و حساب
  زده نمی‌شود. ورودیِ راهنمای «دارایی ثابت» (که اصلاً وجود نداشت) نوشته شد.
  ضمناً کارتِ کناریِ `MODULE_LISTS.fixedassets` روی `purchase_date`/`purchase_cost`
  می‌خواند که هیچ‌وقت روی `FixedAssetRecord` نبودند — تاریخ و مبلغِ خالی نشان
  می‌داد؛ به `acquired_date`/`cost` اصلاح شد.

  **راستی‌آزمایی:** ۳۲۸۳ تست سبز (۹ تستِ تازه)، رفت‌وبرگشتِ مهاجرت، و سناریوی
  زنده‌ی فروش→گزارش روی ۱۴۴۰ و ۳۹۰ با سرریزِ ۰ و کنسولِ ۰؛ سندِ تولیدشده در دفتر
  راستی‌آزمایی شد (بدهکار صندوق ۱٬۵۰۰٬۰۰۰ / بستانکار دارایی ۱٬۲۰۰٬۰۰۰ + سود ۳۰۰٬۰۰۰).

  **باقیمانده‌ی دارایی ثابت:** فازِ ۳ (تعمیرات اساسی + تغییر روش/عمر مفید) و فازِ ۴
  (کارتِ داراییِ کامل + تفکیکِ محاسبه از صدورِ سندِ استهلاک + گزارشِ اسنادِ استهلاک).

- **۱۴۰۵/۰۶/۲۵ (2026-09-16) — دارایی ثابت، فازِ ۱: تحویل/استقرار و جابه‌جایی
  (مهاجرت `0163`، شاخه‌ی `feat/asset-assignments`):**

  کاربر ساختارِ کاملِ موردِ انتظارش از «دارایی ثابت» را داد (هفت عملیات، پنج
  فهرست)؛ ماژولِ موجود فقط چهار موردش را داشت و **یک صفحه‌ی تخت** بود، نه ماژولِ
  تب‌دار. این فاز پایه را می‌گذارد: تا امروز دارایی فقط «چه چیزی و چند» را
  می‌دانست — نه معلوم بود دستِ کیست، نه کجاست، نه تا حالا بینِ چه کسانی گشته.

  * `fixed_assets` سه ستونِ وضعیتِ امروز گرفت (جمعدار، محلِ استقرار، مرکزِ هزینه)
    — **جدولِ دارای ردیفِ واقعی + FKِ درون‌خطی → زیرِ `rls_disabled`** (باگِ PG14).
  * جدولِ تازه‌ی `asset_assignments`: هر تحویل و هر جابه‌جایی، با **مبدأ و مقصد
    روی خودِ ردیف**. بدونِ `from_*` تاریخچه فقط زنجیره‌ای از مقصدهاست و «از چه
    کسی به چه کسی» باید از ردیفِ قبلی حدس زده شود — که با ثبتِ خارج از ترتیبِ
    تاریخ غلط از آب درمی‌آید.
  * **هیچ اثرِ حسابداری ندارد** — جابه‌جایی بینِ جمعداران مالکیت را عوض نمی‌کند.
  * فیلدِ خالی یعنی «دست نزن»: جابه‌جاییِ محل، جمعدار را پاک نمی‌کند.

  **رابط:** ماژول از یک صفحه‌ی تخت به **پنج تب** رفت (کارت دارایی · تحویل و
  استقرار · جابه‌جایی · جابه‌جایی‌ها و تحویل‌ها · استهلاک دوره). ویزاردِ پوسته‌ی
  «راهنما» حذف نشد بلکه **داخلِ تبِ اولش نشست** و درفتش مشترک شد — پیش از این دو
  درفتِ جدا داشتند و فهرست بعدِ ثبتِ ویزارد به‌روز نمی‌شد.

  **راستی‌آزمایی:** ۵ تستِ تازه (کلِ سوییت ۳۲۴۵ سبز) شاملِ نگه‌داشتنِ دو سرِ
  جابه‌جایی، پاک‌نشدنِ جمعدار در جابه‌جاییِ محل، ردِ جابه‌جاییِ داراییِ واگذارشده،
  و ردِ تاریخِ پیش از تحصیل. مهاجرت round-trip تمیز؛ گردشِ کامل زنده در مرورگر
  (تحویل → جابه‌جایی → تاریخچه) با صفر خطای کنسول، هر پنج تب در ۱۴۴۰px و ۳۹۰px
  بدونِ سرریز.

  **باقی‌مانده‌ی این ماژول:** خروجِ دارایی (فروش/اسقاط/اهدا) — که **امروز هیچ
  اثرِ حسابداری ندارد و دارایی را برای همیشه با بهای کامل در ترازنامه نگه
  می‌دارد**، تعمیراتِ اساسی، تغییرِ روش/عمرِ مفید، و تفکیکِ «محاسبه» از «صدورِ
  سندِ» استهلاک.

- **۱۴۰۵/۰۶/۲۵ (2026-09-16) — تولید، فازِ ۴: سه گزارشِ خواسته‌شده (مهاجرت `0162`،
  شاخه‌ی `feat/production-reports`):**

  بخشِ «فهرست»ِ ساختارِ خواسته‌شده کامل شد: **انحرافِ مصرفِ مواد**، **کاردکسِ خطِ
  تولید** و **گزارشِ قیمتِ تمام‌شده** (دو مورد دیگرِ آن فهرست — فرمول‌ها و
  سفارش‌ها — از قبل داخلِ تبِ خودشان بودند).

  **هر سه مشتق‌اند، نه ذخیره‌شده** ([`services/production_reports.py`](backend/app/services/production_reports.py)):
  از همان حواله‌ها و رسیدهای واقعیِ `0161` خوانده می‌شوند. جدولِ گزارشیِ موازی
  ساخته نشد، چون هر جدولِ موازی روزی از سندِ اصلی واگرا می‌شود.

  * **انحرافِ مصرف:** مبنای استاندارد **مقدارِ تولیدشده** است نه مقدارِ برنامه —
    وگرنه هر سفارشِ نیمه‌تمام یک «صرفه‌جوییِ» دروغین نشان می‌داد. جزئی که در فرمول
    نیست ولی مصرف شده هم در گزارش می‌آید، نه اینکه بی‌صدا بیفتد.
  * **کاردکس:** ورودِ خط (حواله) و خروجِ خط (رسید) در **دو ستونِ جدا**، نه یک
    ستونِ علامت‌دار که در گزارشِ فارسی خوانده نمی‌شود.
  * **قیمتِ تمام‌شده:** مهاجرتِ `0162` دو ستونِ `labor_cost_applied` و
    `overhead_cost_applied` را به `production_plans` اضافه کرد، چون `0161`
    دستمزد/سربار را فقط به سندِ حسابداری می‌برد و هیچ‌جا به سفارش گره نمی‌زد —
    یعنی تفکیکِ سه‌جزئی ممکن نبود.

  **سندِ باطل‌شده در هیچ گزارشی شمرده نمی‌شود** (`voided_at IS NULL`) — تستِ
  اختصاصی دارد، چون انحرافی که از حواله‌ی باطل ساخته شود انحرافِ دروغ است.

  **راستی‌آزمایی:** ۶ تستِ تازه (کلِ سوییت ۳۲۳۵ سبز) شاملِ انحرافِ صفر در مصرفِ
  مطابقِ فرمول، شکافِ سفارشِ نیمه‌تمام، تفکیکِ سه‌جزئیِ بها، فیلترِ کالا در
  کاردکس، و خروجِ حواله‌ی باطل از هر دو گزارش. `tsc`/`audit-pages` صفر خطا؛
  هر سه تب زنده روی دادهٔ واقعی با صفر خطای کنسول و صفر سرریز.

- **۱۴۰۵/۰۶/۲۵ (2026-09-16) — تولید، فازِ ۳: تحویلِ مواد، رسیدِ محصول و محاسبهٔ
  قیمتِ تمام‌شده — سه عملیاتِ واقعی به‌جای یک سندِ اتمی (مهاجرت `0161`، شاخه‌ی
  `feat/production-material-flow`):**

  کاربر ساختارِ کاملِ موردِ انتظارش از ماژولِ تولید را داد (پنج عملیات، پنج
  فهرست) و معلوم شد آن‌چه تا فازِ ۲ ساخته شده بود فقط دو تای اول را دارد.
  «سند تولید»ِ قدیمی هر سه کارِ مصرفِ مواد، تولیدِ محصول و جذبِ سربار را در **یک**
  سندِ اتمی انجام می‌داد — یعنی نه حواله‌ی واقعیِ انبار در کار بود، نه رسیدِ
  واقعی، و نه گامِ مستقلی برای دستمزد/سربار که بعداً معلوم می‌شود.

  **تصمیمِ کلیدی — موتورِ سوم ساخته نشد.** هر دو سندِ تازه روی موتورهای موجودِ
  انبار سوارند، نه یک مسیرِ موازی:
  * `WarehouseIssue` با نوعِ **تازه‌ی** `production` («تحویل به تولید»). طرفِ
    بدهکارش مثلِ «فروش» خودکار است — «کالای در جریان ساخت»، نه انتخابِ کاربر.
  * `WarehouseReceipt` با نوعِ `production` که **از قبل وجود داشت و بی‌استفاده
    بود** — حتی حسابداریِ درستش (`_direct_credit_account` → کالای در جریان
    ساخت) از قبل نوشته شده بود و فقط هیچ‌کس صدایش نمی‌زد.
  * دو ستونِ تازه‌ی `production_plan_id` (روی هر دو جدولِ **دارای ردیفِ واقعی**،
    پس زیرِ `rls_disabled` — همان باگِ PG14) و `production_plans.material_cost_issued`.

  **گردشِ تازه:** سفارش (برنامه) → «شروع» → تحویلِ مواد (حواله‌ی واقعی، بدهکار
  WIP) → رسیدِ محصول (رسیدِ واقعی، بستانکار WIP؛ نرخِ واحد = موادِ تحویل‌شده ÷
  تعدادِ برنامه) → محاسبهٔ قیمتِ تمام‌شده (دستمزد+سربار ÷ مقدارِ دریافت‌شده روی
  میانگینِ موزون می‌نشیند، با سندِ خودکار). هر سه گام **جزئی‌پذیر** و
  idempotent‌اند؛ محاسبهٔ بها قابلِ‌تکرار است چون هزینه‌ی تولید یک‌جا معلوم نمی‌شود.

  **رابط:** ماژول از سه تب به **پنج تب** رفت (فرمول‌های ساخت · سفارش تولید ·
  تحویل مواد · رسید محصول · محاسبه قیمت تمام‌شده) — دقیقاً پنج عملیاتِ خواسته‌شده.
  هشدارِ «جلوگیری از خروجِ مواد» (فازِ ۲) به تبِ «تحویل مواد» منتقل شد، یعنی
  همان‌جایی که مواد واقعاً از انبار بیرون می‌رود.

  **بازنشستگیِ سندِ تولیدِ قدیمی:** تبِ «سند تولید» و `ProductionCostDrawer` حذف
  شدند تا دو مسیرِ موازی برای یک کار نماند (قاعده‌ی «دو نمای یک داده نساز»).
  موتورِ `post_production_order` و جدولش **دست‌نخورده و تست‌شده** باقی ماند —
  دیتای تاریخی جایی نرفت و از دفتر و گزارش‌های حسابداری خواندنی است.

  **راستی‌آزمایی:** ۸ تستِ تازه (کلِ سوییت ۳۲۲۹ سبز) شاملِ حواله‌ی واقعی با
  نوع/پیوندِ درست، رسیدِ جزئیِ دوباره با همان نرخ، گاردِ وضعیتِ پیش‌نویس، گاردِ
  مقدارِ بیش از باقی‌مانده، و ایدمپوتنسیِ محاسبهٔ بها. مهاجرت round-trip تمیز.
  گردشِ کامل زنده در مرورگر اجرا شد (سفارش → شروع → تحویل → رسید → محاسبه) با
  صفر خطای کنسول؛ هر پنج تب در ۱۴۴۰px و ۳۹۰px بدونِ سرریز.

  **هنوز نیست (بخشِ «فهرست»ِ خواسته‌ی کاربر):** گزارشِ انحرافِ مصرفِ مواد،
  کاردکسِ موادِ در جریانِ ساخت، و گزارشِ تفصیلیِ قیمتِ تمام‌شده. زیرساختِ داده‌شان
  با همین فاز ساخته شد (`production_plan_id` روی حواله/رسید).

  **مستقر شد (همان روز):** PR #96 مرج و با `deploy.sh production` رفت —
  نسخه‌ی نهایی `0161`، health سالم، ۱۵۱ جدولِ FORCE RLS دست‌نخورده. تأیید از
  راهِ دور: هر دو ستونِ `production_plan_id` روی `warehouse_issues`/
  `warehouse_receipts` موجودند و باندلِ زنده برچسب‌های «تحویل مواد» و
  «محاسبه قیمت تمام‌شده» را دارد.

- **۱۴۰۵/۰۶/۲۵ (2026-09-16) — بک‌فیلِ ماژولِ «پیمانکاری» (مهاجرت `0160`، شاخه‌ی
  `fix/backfill-contracting-module`):**

  کاربر گزارش داد پیمانکاری را در پنلِ `acc.cubita@gmail.com` نمی‌بیند. ریشه:
  `contracting` در `OPTIONAL_MODULES` هست، ولی مستأجرهایی که پیش از وجودِ این
  ماژول پنلشان را شخصی‌سازی کرده‌اند (`tenants.enabled_modules` دیگر `NULL`
  نیست) هرگز عضوِ تازه‌ی این لیست را نمی‌بینند — همان شکافی که `0073` برای
  `manufacturing` با گرنتِ خودکار بست، این‌بار برای یک ماژولِ **غیرمحدود** که
  فقط باید در `enabled_modules` باشد. تأیید از راهِ دور روی پایگاه‌دادهٔ
  production نشان داد: همین حسابِ گزارش‌دهنده هم `enabled_modules`ِ شخصی‌سازی‌شده
  داشت (بدونِ `contracting`) — یعنی این احتمالاً روی هر مستأجرِ قدیمی‌ترِ دیگری
  هم بود. مهاجرت برای هر مستأجرِ با `enabled_modules` غیرِ‌`NULL` که هنوز
  `contracting` ندارد، آن را اضافه می‌کند؛ `downgrade` عمداً no-op است (تشخیصِ
  اینکه کدام مستأجر پیش از مهاجرت خودش داشته ممکن نیست).

- **۱۴۰۵/۰۶/۲۵ (2026-09-16) — تولید، فازِ ۲: هشدارِ «جلوگیری از خروجِ مواد»
  (شاخه‌ی `feat/manufacturing-material-outflow-warning`، **بدونِ مهاجرت**):**

  فازِ ۱ سفارش (برنامه) را از سند (اجرا) جدا کرد؛ ولی هیچ‌چیز جلوی این را
  نمی‌گرفت که یک سندِ تولیدِ بی‌ربط موادی را مصرف کند که سفارش‌های بازِ *دیگر*
  رویش حساب کرده‌اند. کاربر تصمیم گرفت: **فقط هشدار، نه قفل** (مطابقِ گرایشِ
  مستندِ پروژه «گزارش، نه گارد» — قفلِ سختِ موجودیِ منفیِ *این* سند از فازِ ۱
  دست‌نخورده ماند).

  **پیاده‌سازی کاملاً سمتِ کلاینت است — صفر تغییرِ بک‌اند/مهاجرت.** تبِ «سند
  تولید» با دادهٔ از قبل واکشی‌شده (`plans`، `boms` با `lines`) به‌علاوهٔ یک
  فراخوانیِ تازهٔ `/api/stock`ِ **موجود** (`fetchStockLevels`) محاسبه می‌کند:
  برای هر جزءِ فرمول، چقدر آن را سفارش‌های بازِ *دیگر* (وضعیتِ
  `started`/`in_progress`/`stopped` — پیش‌نویس هنوز قطعی نشده، رزرو نمی‌کند؛
  تمام/لغوشده دیگر چیزی نمی‌خواهد) در همان انبار لازم دارند (خطِ فرمول ×
  باقی‌ماندهٔ سفارش ÷ بازده)، و اگر موجودیِ باقی‌مانده بعدِ این سند از آن
  کمتر شود، بنری (الگوی [BlacklistBanner](desktop/src/components/BlacklistBanner.tsx):
  `credit-banner credit-banner--warn`) نامِ جزء و عددها را نشان می‌دهد. سفارشِ
  خودِ سند (اگر از «سفارشِ تولیدِ مرتبط» انتخاب شده) از این حساب کنار گذاشته
  می‌شود — مصرف برای خودِ آن سفارش طبیعی است.

  **راستی‌آزمایی:** `tsc -b`/`audit-pages`/`oxlint` صفر خطا. سه سناریو با
  Playwright زنده روی دادهٔ واقعی (فرمول با یک جزء، ۱۰ واحد به‌ازای هر محصول؛
  یک سفارشِ بازِ `started` با ۶۰ واحد رزرو؛ موجودیِ جزء ۷۷): تولیدِ بی‌ربطِ
  ۲ واحد (نیاز ۲۰) → بنر (باقی ۵۷ < رزروِ ۶۰) ✓؛ تولیدِ ۱ واحد (نیاز ۱۰) →
  بدونِ بنر (باقی ۶۷ ≥ ۶۰) ✓؛ اتصال به همان سفارش با تعدادِ باقی‌ماندهٔ ۶ →
  بدونِ بنر (رزروِ خودش حساب نمی‌شود) ✓. هر سه در ۱۴۴۰px و ۳۹۰px (کشوی موبایل
  + نوارِ تب)، صفر خطای کنسول، صفر سرریز.

  **مستقر شد (۱۴۰۵/۰۶/۲۵، همان روز):** PR #93 روی master مرج شد؛ `deploy.sh
  production` اجرا شد (بدونِ مهاجرتِ تازه — نسخه‌ی نهایی `0159`، مطابقِ
  انتظار). تأیید: `https://acc.cubita.ir/api/health` سالم، ۱۵۱ جدولِ FORCE RLS
  دست‌نخورده، و باندلِ زنده حاویِ متنِ بنرِ تازه («رزروِ سفارش…») است —
  از راهِ دور با `curl` روی خودِ فایلِ جاوااسکریپتِ سرو‌شده چک شد.

- **۱۴۰۵/۰۶/۲۴ (2026-09-15) — استقرارِ تولید: `0149` → `0157`، هفت مهاجرت
  (بزرگ‌ترین استقرارِ تاریخِ پروژه):**

  آنچه رفت: `0150` مرجعِ بانکیِ ردیفِ صورت‌حساب · `0152`..`0155` پیمانکاری در چهار
  فاز · `0156` تفکیکِ سفارشِ تولید از سندِ اجرا · `0157` صندوقِ تنخواه.

  **تأییدِ پس از استقرار (از راهِ دور):** `alembic_version` = `0157` · سرویس
  `hesabdari-backend` فعال روی ۸۰۰۱ · `https://acc.cubita.ir` و `/api/health` هر دو
  ۲۰۰ · لاگِ بک‌اند بدونِ هیچ خطا · هر شش جدولِ تازه (`contracting_contracts`،
  `contracting_amendments`، `contracting_statements`، `contracting_settlements`،
  `production_plans`، `petty_cash_funds`) و هر چهار ستونِ تازه موجود.

  **`rls_disabled` بارِ دوم روی PostgreSQL 14.24 آزمونِ واقعی داد.** backfillِ `0156`
  **۷ شمارنده — یکی به‌ازای هر مستأجر** ساخت؛ اگر پوشش کار نمی‌کرد آن عدد صفر می‌شد و
  مهاجرت بی‌صدا «موفق» گزارش می‌داد. (backfillِ تنخواه درست صفر صندوق ساخت، چون تولید
  هیچ تراکنشِ تنخواهی نداشت — سازگار، نه شکست.)

  **یک باگ پیش از استقرار گرفته شد:** `0157` ستونی با کلیدِ خارجیِ درون‌خطی به
  `petty_cash_transactions` (جدولِ دارای ردیف) اضافه می‌کرد **بدونِ** `rls_disabled`.
  استدلالِ «ستون تازه است و همه‌اش NULL، پس چیزی برای اعتبارسنجی نیست» کافی نبود:
  `0156` دقیقاً همین حالت را داشت و کامنتش خطای واقعیِ PG14 را نام می‌برد
  (`invalid input syntax for type uuid: ""`). چون `0157` **آخرین** مهاجرتِ صف بود،
  شکستش یعنی شش مهاجرتِ کامیت‌شده با کدِ برگشته.

  **حادثه‌ی «دو سرِ زنجیره»:** تنخواه اول `0151` بود و پیمانکاری هم `0152` را پشتِ
  `0150` گذاشت — دو سر روی یک ریشه. اصلاحش (#81) تاریخچه را بازنویسی کرد و merge‌ی
  PR #76 را انداخت، پس صندوقِ تنخواه و «هدف حرکت» از `master` غایب شدند بی‌آنکه کسی
  متوجه شود (GitHub همچنان «merged» نشانشان می‌داد). با PR #87 و شماره‌ی `0157`
  برگشتند. **درس: شماره‌ی مهاجرت را از سرِ فعلیِ `master` بگیر، نه از آخرین چیزی که
  خودت دیده‌ای.**

- **۱۴۰۵/۰۶/۲۴ (2026-09-15) — تولید، فازِ ۱: تفکیکِ سفارش (برنامه) از سند (اجرا)
  (مهاجرت `0156`، پشتِ `0155`؛ شاخه‌ی `feat/manufacturing-plan-document-split`):**

  تا امروز «سفارشِ تولید» بلافاصله مواد را مصرف و محصول را تولید می‌کرد — هیچ
  مرحله‌ی «برنامه‌ریزی، بعد اجرا»یی نبود. این فاز آن را به دو مفهومِ جدا می‌شکند،
  دقیقاً طبقِ ساختاری که کاربر خواسته بود.

  **ساخته‌شده:**
  * `production_plans` — جدولِ تازه برای «سفارش تولید»: فقط برنامه (فرمول، انبار،
    تاریخِ برنامه، مقدار)، **بدونِ** ستونِ بها یا اثرِ انبار. وضعیت با گذارِ محدود
    (`draft→started→in_progress→{stopped,finished}`، `cancelled` فقط از `draft`؛
    همان الگوی پیمانکاری).
  * `ProductionOrder` (جدولِ موجودِ `production_orders`) **دست‌نخورده به‌عنوانِ
    سندِ تولید (اجرا) ماند** — نامش عمداً عوض نشد تا دیتای موجود دست‌نخورده
    بماند؛ فقط `production_plan_id`ِ اختیاری گرفت تا اگر از رویِ یک سفارش اجرا شد
    ردش بماند. ثبتِ سندِ تولیدِ متصل، `qty_produced`ِ سفارشِ مبنا را هم به‌روز
    می‌کند.
  * برچسب‌های نمایشی عوض شدند تا با معنایِ تازه بخوانند: «سفارش تولید» حالا یعنی
    برنامه (`production_plan`)، «سند تولید» یعنی اجرا (`production_order` —
    قبلاً همین برچسب را داشت).
  * `GET /api/production-orders` و `GET /api/production-plans` هر دو
    `Page`/`PageParams` گرفتند (اولی تا امروز صفحه‌بندیِ سمتِ سرور نداشت)؛ هر دو
    POST زیرِ `idempotent()` رفتند (تا امروز نداشت — دوکلیک یعنی مصرفِ دوبرابرِ مواد).
  * **رابط:** سه تب — «فرمول‌های ساخت» (دست‌نخورده)، «سفارش تولید» (تازه، فقط
    برنامه‌ریزی)، «سند تولید» (بازنویسیِ نیمه‌ی اجراییِ تبِ قدیمیِ «تولید» + یک
    `<select>` تازه‌ی اختیاریِ «سفارشِ تولیدِ مرتبط» که فرمول/انبار/مقدارِ
    باقیمانده را از سفارشِ بازِ انتخاب‌شده پر می‌کند).

  **عمداً ساخته نشد:** «جلوگیری از خروج مواد» (فازِ ۲ — به مفهومِ «چقدر برای
  سفارش‌های بازِ این فاز رزرو شده» نیاز دارد که همین فاز زیرساختش را ساخت)؛
  ابطالِ سندِ تولید هنوز مسیرِ مشترکِ `voiding.py` را نگرفته — اسکوپِ این فاز فقط
  تفکیکِ سفارش/سند بود، نه رفعِ هر شکافِ دیگرِ ماژول.

  **تصمیمِ نوشته‌شده:** موتورِ ثبتِ سندِ تولید (`post_production_order`) عمداً
  به `WarehouseIssue`/`WarehouseReceipt` سیم‌کشی نشد و همان مسیرِ ثابت‌شده‌ی
  `StockLedger` مستقیم را نگه داشت — بازنویسیِ آن به‌سمتِ حواله/رسیدِ واقعی
  ریسکِ بالایی به یک موتورِ حسابداریِ تست‌شده اضافه می‌کرد؛ زیرِ فشارِ زمان،
  تفکیکِ سفارش/سند (که مستقیماً خواسته شده بود) بر بازآراییِ موتورِ ثبت اولویت
  گرفت. اگر لازم شد، این یک فازِ جداست.

  **راستی‌آزمایی:** `test_manufacturing.py` حالا ۱۱ تست (۶ تازه، شاملِ
  اثرنداشتنِ سفارش روی موجودی، به‌روزرسانیِ `qty_produced`، تولیدِ بی‌برنامه)؛
  کلِ سوییتِ بک‌اند ۳۱۵۵ سبز. مهاجرت `upgrade→downgrade→upgrade` روی اسکیمای
  یک‌بارمصرفِ ایزوله؛ سناریوی اصلیِ بحرانی هم جداگانه تست شد — یک ردیفِ واقعی
  در `production_orders` کاشته شد (دقیقاً همان چیزی که باگِ PG14ِ اعتبارسنجیِ
  FK را لازم می‌کند)، بعد مهاجرت روی همان دیتا اجرا شد و بدونِ خطا رد شد؛ ستونِ
  تازه برای ردیفِ قدیمی `NULL` ماند، RLS دست‌نخورده. `tsc`/`audit-pages`/`oxlint`
  صفر خطا؛ هر سه تب با ۰ خطای کنسول و ۰ سرریز در ۱۴۴۰px و ۳۹۰px.

- **۱۴۰۵/۰۶/۲۳ (2026-09-14) — پیمانکاری، فازِ ۱: پیمان (مهاجرت `0152`، پشتِ `0150`؛
  شاخه‌ی `feat/contracting`):**

  *(۱۴۰۵/۰۶/۲۴: شماره‌ی این مهاجرت اول `0151` بود؛ چون هم‌زمان شاخه‌ی #76 هم از
  `0149` منشعب شد و `0150` گرفت و هر دو روی master مرج شدند، `alembic heads` دو
  سر داد. هیچ‌کدام روی production نبود، پس زنجیره‌ی پیمانکاری پشتِ `0150`
  شماره‌گذاریِ مجدد شد — `fix/contracting-migration-renumber`.)*

  تا امروز «پیمانکاری» فقط یک صفحه‌ی «به‌زودی» با چهار کارتِ خالی بود — صفر بک‌اند.
  این فاز اولین حلقه‌ی زنجیره را می‌سازد؛ متمم/صورت‌وضعیت/تسویه‌حساب فازهای بعدند.

  **ساخته‌شده:**
  * `contracting_contracts` (FORCE RLS) و شمارنده‌ی `contract`. کارفرما همان موجودیتِ
    `Contact` است؛ شماره‌ی داخلی + یک فیلدِ آزادِ «شماره‌ی قراردادِ کارفرما» جدا می‌مانند.
  * **پیمان:** ثبتِ کارفرما، موضوع، مبلغِ کل، بازه، درصدِ سپرده/پیش‌پرداخت (فعلاً فقط
    اطلاعاتی) و مرکزِ هزینه‌ی اختیاری (`COST_CENTER_KINDS` از قبل `"contract"` داشت).
    ثبتش **هیچ سندِ حسابداری نمی‌زند** — مثلِ `CostCenter`/`Bom`، نه فاکتور؛ برای همین
    `VoidableMixin` ندارد و لغو از راهِ `status="cancelled"` است.
  * **تغییرِ وضعیت:** گذارِ محدود — `draft→{active,cancelled}`، `active→{suspended,
    terminated,completed}`، `suspended→{active,terminated}`؛ بقیه پایانی‌اند. گذارِ
    نامجاز ۴۰۹ می‌گیرد.
  * **رابط:** الگوی «فروش» (تخت، نه تب‌دار) — چون پنج عملیاتِ این ماژول پنج نوعِ رکوردِ
    واقعاً متفاوت‌اند که زنجیره‌ی سند می‌سازند. عملیات «پیمان» ↔ فهرستِ «پیمان‌ها»
    (الگوی مرجع: `installments` ↔ `installmentplans`). گروهِ ناوبریِ «پیمانکاری» از
    قبل بود؛ فقط آیتمِ جای‌نگه‌دارش با سه صفحه‌ی واقعی جایگزین شد.
  * ثبت در `services/modules.py` (`OPTIONAL_MODULES`، قالبِ صنفیِ «عمومی») — تا امروز
    این نامِ ماژول اصلاً گیت نمی‌شد.

  **عمداً ساخته نشد:** متمم پیمان، صورت وضعیتِ دریافتی، تسویه‌حسابِ پیمان و فهرست‌های
  نظیرشان — هرکدام فازِ جداست. اثرِ حسابداریِ سپرده/پیش‌پرداخت هم به همان فازها موکول شد.

  **راستی‌آزمایی:** `test_contracting.py` (۸ تست) + کلِ سوییت ۳۰۹۰ سبز؛ مهاجرت
  `upgrade→downgrade→upgrade` روی اسکیمای یک‌بارمصرف با RLS تأییدشده؛ `tsc`/
  `audit-pages`/`oxlint` صفر خطا (R11 هر دو صفحه را پوشش داد)؛ روی همان اسکیمای
  probe با کاربرِ واقعی: ثبتِ پیمان → تغییرِ وضعیت → دیدنش در فهرست، هر سه با ۰
  خطای کنسول و ۰ سرریز در ۱۴۴۰px و ۳۹۰px.

- **۱۴۰۵/۰۶/۲۴ (2026-09-15) — پیمانکاری، فازِ ۲: متمم پیمان (مهاجرت `0153`، پشتِ
  `0152`؛ شاخه‌ی `feat/contracting-amendment`، پشتِ `feat/contracting`):**

  *(شماره‌ی این مهاجرت اول `0152` بود؛ با رفعِ دوسرِ مهاجرتِ master (§قبل) به
  `0153` جابه‌جا شد.)*

  ادامه‌ی زنجیره‌ی فازِ ۱. تصمیمِ بازِ #۲ (چطور «مبلغِ روزِ پیمان» محاسبه شود) اینجا
  بسته شد: **الحاقی، نه بازنویسی**.

  **ساخته‌شده:**
  * `contracting_amendments` (FORCE RLS) و شمارنده‌ی مستقلِ `contract_amendment` —
    شماره‌ی متمم از شماره‌ی پیمانِ مبنا جداست (سراسری، مثلِ بقیه‌ی اسناد، نه
    شماره‌گذاریِ به‌ازای هر پیمان).
  * **متمم پیمان:** انتخابِ پیمان، تاریخ، موضوع (اجباری)، تغییرِ مبلغ (می‌تواند
    منفی باشد — کاهشِ مبلغ) و تاریخِ پایانِ تازه (اختیاری). `Contract.total_amount`
    **دست‌نخورده می‌ماند** — مبلغِ روز از جمعِ آن با همه‌ی متمم‌هایش مشتق می‌شود؛ اگر
    تاریخِ پایانِ تازه داده شود، `Contract.end_date` همان‌جا به‌روز می‌شود (فیلدِ
    زمان‌بندی است، نه مقدارِ جمع‌شونده — نیازی به اشتقاق ندارد). پیمانی که به
    وضعیتِ پایانی رسیده (`terminated`/`completed`/`cancelled`) متممِ تازه نمی‌پذیرد،
    ۴۰۹.
  * متمم ابطال یا تغییرِ وضعیتِ خودش را ندارد — فقط رکوردِ الحاقی است (اگر لازم شد
    خنثی‌کردنِ یک متمم، آن هم یک متممِ دیگر با `amount_delta` وارونه است).
  * **رابط:** همان الگوی «فروش» — عملیات «متمم پیمان» ↔ فهرستِ «متمم‌های پیمان»؛
    فهرستِ متمم فیلترِ `contract_id` هم دارد (استفاده‌ی داخلی/آینده).

  **عمداً ساخته نشد:** صورت وضعیتِ دریافتی، تسویه‌حسابِ پیمان و فهرست‌های نظیرشان —
  هرکدام فازِ جداست.

  **راستی‌آزمایی:** `test_contracting.py` حالا ۱۶ تست (۸ تازه) + کلِ سوییت ۳۱۰۳
  سبز؛ مهاجرت `upgrade→downgrade→upgrade` با کلِ زنجیره از پایه روی اسکیمای
  یک‌بارمصرفِ ایزوله (بدونِ `search_path` مشترک با `public` — یک بارِ اول با
  اشتباه به `alembic_version`ِ دیتابیسِ واقعی نوشت و همان‌جا برگردانده شد)؛ `tsc`/
  `audit-pages`/`oxlint` صفر خطا (R11 هر دو صفحه‌ی تازه را پوشش داد)؛ روی همان
  اسکیمای probe با کاربرِ واقعی: پیمان → متمم (تغییرِ مبلغِ منفی، تاریخِ پایانِ تازه)
  → دیدنش در فهرست → رد شدنِ متمم روی پیمانِ فسخ‌شده (۴۰۹)، و هر دو صفحه‌ی تازه با
  ۰ خطای کنسول و ۰ سرریز در ۱۴۴۰px و ۳۹۰px.

- **۱۴۰۵/۰۶/۲۴ (2026-09-15) — پیمانکاری، فازِ ۳: صورت وضعیت دریافتی (مهاجرت `0154`،
  پشتِ `0153`؛ شاخه‌ی `feat/contracting-statement`، پشتِ `feat/contracting-amendment`):**

  *(شماره‌ی این مهاجرت اول `0153` بود؛ با رفعِ دوسرِ مهاجرتِ master (§قبل) به
  `0154` جابه‌جا شد.)*

  تصمیمِ بازِ فازِ ۱ («صورت‌وضعیت چه اثرِ حسابداری‌ای دارد؟») از کاربر پرسیده شد —
  جوابِ صریح: **بدونِ سند، فقط رکورد** (مثلِ پیمان/متمم). اثرِ مالیِ واقعی به فازِ
  تسویه‌حساب (فازِ ۴) موکول شد.

  **ساخته‌شده:**
  * `contracting_statements` (FORCE RLS) و شمارنده‌ی مستقلِ `contract_statement`.
  * **صورت وضعیت دریافتی:** ثبتِ مبلغِ ناخالصِ کارکرد + سایرِ کسورات (دستی، مثلِ
    بیمه/جریمه). کسرِ سپرده و کسرِ پیش‌پرداخت **خودکار** از رویِ درصدهای همان پیمان
    محاسبه می‌شود و **Snapshot** روی خودِ صورت‌وضعیت می‌نشیند — تغییرِ بعدیِ درصدِ
    پیمان صورت‌وضعیت‌های قبلی را عوض نمی‌کند (همان الگوی Snapshotِ فاکتورهای دیگر).
    مبلغِ خالصِ قابلِ‌پرداخت هم ذخیره می‌شود. پیمانِ به‌وضعیتِ‌پایانی‌رسیده
    صورت‌وضعیتِ تازه نمی‌پذیرد (۴۰۹، همان قاعده‌ی متمم).
  * **رابط:** همان الگوی «فروش» — عملیات «صورت وضعیت دریافتی» ↔ فهرستِ «صورت
    وضعیت‌های دریافتی» (کسرِ سپرده/پیش‌پرداخت/سایر و خالص، هرکدام ستونِ خودش).

  **عمداً ساخته نشد:** تسویه‌حسابِ پیمان و فهرستش — فازِ بعد.

  **راستی‌آزمایی:** `test_contracting.py` حالا ۲۳ تست (۷ تازه) + کلِ سوییت ۳۱۱۵
  سبز (یک باگِ Decimal/float در محاسبه‌ی کسورات همین‌جا پیدا و رفع شد — `retention_
  percent`/`advance_percent` از دیتابیس `Decimal` برمی‌گردند نه `float`)؛ مهاجرت
  `upgrade→downgrade→upgrade` با کلِ زنجیره از پایه روی اسکیمای یک‌بارمصرفِ ایزوله؛
  `tsc`/`audit-pages`/`oxlint` صفر خطا؛ روی همان اسکیمای probe با کاربرِ واقعی:
  پیمان (سپرده=۱۰٪، پیش‌پرداخت=۲۰٪) → صورت‌وضعیت (ناخالص=۱۰۰م، سایرِ کسورات=۵م) →
  کسرِ سپرده=۱۰م، کسرِ پیش‌پرداخت=۲۰م، خالص=۶۵م — عددها با محاسبه‌ی دستی یکی بودند؛
  ردِ صورت‌وضعیت روی پیمانِ فسخ‌شده (۴۰۹)؛ هر دو صفحه‌ی تازه با ۰ خطای کنسول و ۰
  سرریز در ۱۴۴۰px و ۳۹۰px (جدولِ ۸ستونیِ فهرست هم بدونِ سرریز جا شد).

- **۱۴۰۵/۰۶/۲۴ (2026-09-15) — پیمانکاری، فازِ ۴ (آخرین فاز): تسویه‌حساب پیمان
  (مهاجرت `0155`، پشتِ `0154`؛ شاخه‌ی `feat/contracting-settlement`، پشتِ
  `feat/contracting-statement`):**

  *(شماره‌ی این مهاجرت اول `0154` بود؛ با رفعِ دوسرِ مهاجرتِ master (§قبل) به
  `0155` جابه‌جا شد.)*

  تصمیمِ بازِ فازِ ۱ («تسویه‌حسابِ نهایی چه اثری دارد؟») هم از کاربر پرسیده شد —
  اینجا جوابْ برعکسِ سه فازِ قبل بود: **سندِ حسابداریِ واقعی می‌زند.** تنها سندِ
  این ماژول که در دفتر می‌نشیند.

  **ساخته‌شده:**
  * `contracting_settlements` — تنها جدولِ این ماژول با `VoidableMixin` و
    `journal_entry_id` (FORCE RLS) + شمارنده‌ی مستقلِ `contract_settlement`.
  * **تسویه حساب پیمان:** کاربر فقط پیمان/تاریخ/توضیحات می‌دهد؛ همه‌ی مبالغ از
    جمعِ همه‌ی `ContractStatement`های آن پیمان خودکار محاسبه می‌شود (Snapshot،
    نه ارجاعِ زنده). سندِ متوازن: ناخالصِ کارکرد → بستانکارِ **درآمدِ پیمانکاری**
    (نقشِ تازه)؛ خالص → بدهکارِ **حساب‌های دریافتنی** (همان نقشِ مشترکِ همه‌ی
    فاکتورها، با `analytic_id`ِ کارفرما)؛ سپرده → بدهکارِ **سپرده‌ی حسن انجامِ کار
    نزدِ کارفرما** (نقشِ تازه، دارایی — «سپرده آزاد شد، حالا قابلِ‌وصول است»)؛
    پیش‌پرداخت → بدهکارِ **پیش‌دریافتِ پیمان** (نقشِ تازه، بدهی — کاهشِ همان بدهی
    با کارکردِ واقعی)؛ سایرِ کسورات → بدهکارِ **سایرِ کسوراتِ صورت‌وضعیت** (نقشِ
    تازه، هزینه). چهار نقشِ تازه هم در `get_or_create_account` (کسب‌وکارهای
    موجود) هم در `seed.py CHART_OF_ACCOUNTS` (مستأجرِ تازه) نشستند — دومی را
    تستِ محافظِ `test_every_posting_role_resolves_after_provisioning` اجبار کرد؛
    کدهایشان (۱۱۱۶، ۲۱۱۴، ۴۱۰۹، ۵۱۱۹) عمداً بازه‌ی رزروشده‌ی قالبِ صنفیِ
    «پیمانکاری» (۱۱۵۰–۱۱۵۲، ۲۱۰۶–۲۱۱۱، ۴۱۳۰) را دور زدند — همان دلیلِ
    `WITHHOLDING_TAX_PAYABLE`/`CONTRACT_INSURANCE_PAYABLE`.
  * هر پیمان فقط یک تسویه‌حسابِ فعال می‌گیرد (۴۰۹ روی دومی)؛ بدونِ صورت‌وضعیت
    هم تسویه رد می‌شود (۴۰۰). **ابطال** از همان مسیرِ مشترکِ `voiding.py`
    (`reverse_journal_entry`، الگوی `void_note`) — سندِ معکوس، نه حذف؛ پس از
    ابطال پیمان دوباره قابلِ‌تسویه می‌شود.
  * `entry_source.SOURCE_MODELS['contract_settlement'] = ContractSettlement` —
    تستِ محافظِ کامل‌بودنِ رجیستری همین را می‌خواست.
  * **رابط:** «تسویه حساب پیمان» (عملیات، پیش‌نمایشِ سمتِ کلاینت از صورت‌وضعیت‌های
    پیمانِ انتخاب‌شده) ↔ فهرستِ «تسویه‌حساب‌های پیمان» (کارتِ آمار + دکمه‌ی ابطال
    با `window.prompt` برای دلیل، همان الگوی `voidSalesReturn`).

  **ماژولِ پیمانکاری همین‌جا کامل شد** — هر پنج عملیات و هر چهار فهرست، طبقِ
  طرحِ کاربر.

  **راستی‌آزمایی:** `test_contracting.py` حالا ۳۱ تست (۸ تازه، شاملِ تعادلِ
  سند و بازگشتِ سندِ ابطال) + کلِ سوییت ۳۱۲۸ سبز؛ دو تستِ محافظِ سراسری
  (`test_every_posting_role_resolves_after_provisioning`،
  `test_every_model_with_a_journal_link_is_registered`) اول شکستند و همین‌جا
  به رجیستری‌های درست وصل شدند — دقیقاً همان چیزی که برای گرفتنشان ساخته
  شده بودند. مهاجرت `upgrade→downgrade→upgrade` با کلِ زنجیره از پایه روی
  اسکیمای یک‌بارمصرفِ ایزوله؛ `tsc`/`audit-pages`/`oxlint` صفر خطا؛ روی همان
  اسکیمای probe با کاربرِ واقعی: دو صورت‌وضعیت (ناخالص=۱٬۰۰۰٬۰۰۰+۵۰۰٬۰۰۰) →
  تسویه‌حساب (ناخالص=۱٬۵۰۰٬۰۰۰، سپرده=۱۵۰٬۰۰۰، پیش‌پرداخت=۳۰۰٬۰۰۰،
  کسورات=۲۰٬۰۰۰، خالص=۱٬۰۳۰٬۰۰۰) → سندِ حسابداری متوازن (بدهکار=بستانکار=
  ۱٬۵۰۰٬۰۰۰)؛ دومین تسویه رد شد (۴۰۹)؛ ابطال → سندِ معکوس ساخته شد → پیمان
  دوباره تسویه شد. هر دو صفحه‌ی تازه با ۰ خطای کنسول و ۰ سرریز در ۱۴۴۰px و
  ۳۹۰px.

- **۱۴۰۵/۰۶/۲۳ (2026-09-14) — استقرارِ تولید تا `0149`:** یک مهاجرت رفت (`0149`) — دو
  جدولِ تازه‌ی «قیمت‌گذاری اسناد انبار» (FORCE RLS، بی کلیدِ خارجی به جدولِ دارای ردیف،
  بی `UPDATE`). بی‌حادثه بالا رفت؛ پس از استقرار از راهِ دور تأیید شد: `alembic current`
  روی سرور `0149`، سرویس `active`، و `/health`، `/api/health`، `/` هر سه ۲۰۰.

- **۱۴۰۵/۰۶/۲۳ (2026-09-14) — قیمت‌گذاری اسناد انبار، نوبتِ دوم: اجرای قیمت‌گذاری با پیش‌نمایش،
  سندِ اصلاحی و ابطال (مهاجرت `0149`، پشتِ `0148`؛ شاخه‌ی `feat/inventory-valuation-run`):**

  **ساخته‌شده:**
  * `inventory_valuation_runs` و `inventory_valuation_adjustments` (FORCE RLS) و شمارنده‌ی
    `inventory_valuation`. دفترِ موجودی دست نمی‌خورد؛ «بهای فعالِ» هر حرکت = آخرین اصلاحِ
    باطل‌نشده، و بازپخش، کاردکس، ارزشِ موجودی و تطبیق همه همان را می‌خوانند.
  * **پیش‌نمایش** (`services/valuation_runs.calculate`) چیزی نمی‌نویسد: حرکاتِ منقضی با بهای قبل و
    بعد، سندِ اصلاحی، موجودیِ منفیِ تاریخی (که ثبت را می‌بندد)، حرکاتِ اصلاح‌نشدنی با علت، و توکنِ دفتر.
  * **ثبت:** قفلِ کالاهای دامنه، محاسبه‌ی دوباره و ردِ توکنِ کهنه (۴۰۹)؛ یک سند با تاریخِ «تا تاریخ» که
    هر اصلاح را روی معینِ موجودیِ انبار و **همان حسابی که حرکتِ اصلی خورد** می‌زند — حسابِ ردیفِ
    خروج یا برگشتِ خروج با مرکز هزینه‌اش، مغایرتِ انبار برای تعدیل و انبارگردانی، بهای تمام‌شده برای
    فروشِ قدیمی؛ دو سرِ انتقال طرفِ مقابل ندارند و هم را می‌پوشانند. اجرای دوباره چیزی نمی‌یابد (۴۰۰).
  * **برگشتِ خروج بهایش را از خروجِ مبدأ می‌گیرد** — در بازپخش هم. اصلاحِ خروج برگشتش را هم اصلاح
    می‌کند و فقط بهای واحدهای بیرون‌مانده به سود و زیان می‌رسد.
  * **ابطال:** فقط آخرین اجرای باطل‌نشده؛ سندِ معکوس و برگشتِ بهای فعال. سندی که بهایش اصلاح شده تا
    اجرایش باطل نشود ابطال‌پذیر نیست (گاردِ مشترکِ ابطال‌ها، حالا روی خروجِ انبار هم).
  * **رابط:** تبِ «قیمت‌گذاری اسناد» در انبار — دامنه (تا تاریخ، از تاریخِ اختیاری، انبار، کالا)،
    پیش‌نمایش، ثبت و فهرستِ اجراها با ابطال؛ کاردکس نشانِ «اصلاح‌شده» گرفت.

  **کنارِ «قیمت‌گذاری ورودی‌ها»** (`production_pricing`، فصلِ دیگرِ همین روز)، نه جای آن: آن ورودیِ
  **بی‌فی** را قیمت می‌زند؛ این بهای حرکاتی را که از قبل بها دارند و کهنه شده‌اند اصلاح می‌کند — از
  جمله خروج‌های پس از همان ورودیِ تازه‌قیمت‌شده. دو نقطه‌ی اتصال: توکنِ دفتر جمعِ ارزش را هم دارد
  (قیمت‌گذاریِ ورودی ردیفِ تازه نمی‌سازد و `seq` تکان نمی‌خورد)، و `valuation.recompute` پارامترِ
  `voided`ِ آن ماژول را همچنان می‌پذیرد. دیدنِ پیش‌نمایش و اجراها همان مجوزِ بهای گزارش‌های انبار است
  (`accounting.view`)، نه مجوزِ انبار.

  **عمداً ساخته نشد:** اصلاحِ مصرفِ تولید (بهای محصول را هم جابه‌جا می‌کند؛ در پیش‌نمایش با علت
  می‌آید)، اجرای خودکار یا زمان‌بندی‌شده، ابطالِ یک کالا از یک اجرا. معینِ موجودیِ سندِ اصلاحی از
  نگاشتِ **امروزِ** انبار است.

  تست: `test_inventory_valuation_run.py`.

- **۱۴۰۵/۰۶/۲۳ (2026-09-14) — استقرارِ تولید تا `0148`، و باگی که خودِ استقرار لو داد:**
  پنج مهاجرت پشتِ هم رفتند (`0144`..`0148`) و هر پنج بی‌حادثه: انبارگردانیِ کور و عکسِ
  لحظه‌ی شمارش · ابعادِ گزارشِ انبار و رویدادهای سریال · پروفایلِ حسابداریِ نوعِ فروش ·
  ورودیِ دوره‌ایِ عواملِ حقوق · رمزگذاریِ کلیدِ امضای مؤدیان در حالتِ سکون.

  **۱. آن بدهیِ PG-14 از ردیفِ بالا بالاخره آزمونِ واقعی داد — و الگو جواب داد.**
  ورودیِ استقرارِ قبلی نوشته بود که این ماشین کلاسِ باگِ «اعتبارسنجیِ کلیدِ خارجی زیرِ
  RLS» را **ساختاراً نمی‌تواند** بگیرد، چون ۱۷ آن اسکن را حذف می‌کند. `0146` دو کلیدِ
  خارجی به `sale_types` اضافه می‌کرد — جدولی که روی تولید ردیف دارد — و داخلِ
  `rls_disabled` با **هر دو** جدولِ طرفِ قید پیچیده شده بود. روی PostgreSQL 14.24 بدونِ
  خطا رد شد. **این اولین اثباتِ عملیِ آن الگو روی تولید است**؛ تا امروز فقط استدلال
  داشتیم و یک شکستِ تاریخی. اختلافِ نسخه همچنان بدهیِ باز است، ولی الگو دیگر فرضیه نیست.

  **۲. ردِ حسابرسیِ کلِ سامانه به یک importِ اتفاقی بند بود.** پس از استقرار، یک جلسه‌ی
  انبارگردانیِ بازمانده از مرداد با یک اسکریپتِ سرور لغو شد. جلسه درست لغو شد ولی
  `audit_log` **هیچ ردی نگرفت** — در حالی که `StockCountSession` از `0144` حسابرسی می‌شود.

  علت: `app.audit` خودش را با `@event.listens_for(Session, "before_flush")` روی هر نشستِ
  SQLAlchemy می‌نشاند، یعنی ثبتش **عارضه‌ی جانبیِ import** است — و هیچ‌جای کد صریح
  import‌اش نمی‌کرد. در کلِ مخزن شش فایل import‌اش می‌کنند و آنکه اپِ وب را نجات می‌داد
  `app/deps.py` بود، چون هر router به آن وابسته است. `app/main.py` اصلاً نمی‌کند.

  یعنی هر اسکریپت، cron یا کارِ پس‌زمینه‌ای بیرونِ آن زنجیره روی مدلِ حسابرسی‌شده
  می‌نوشت و ردش **بی‌صدا** گم می‌شد؛ و یک بازآراییِ ساده‌ی `deps.py` می‌توانست حسابرسی
  را در کلِ سامانه خاموش کند. `app/database.py` حالا صریح import‌اش می‌کند — انتهای
  فایل، چون `app.audit` به `app.models.audit` وابسته است و آن به `Base`ِ همین فایل.

  **درسِ ماندگار — گاردی که در همان پروسه اجرا شود گاردِ تقلبی است.** تستِ این باگ
  عمداً در **زیرپروسه** است: در یک اجرای pytest، `app.audit` را ده‌ها فایلِ دیگر از قبل
  import کرده‌اند، پس هر تستِ درون‌پروسه‌ای همیشه سبز می‌شود. آزموده شد، نه فرض: با
  برگرداندنِ اصلاح، تستِ زیرپروسه‌ای شکست و **هر دو تستِ درون‌پروسه‌ای سبز ماندند**.
  هر جا درستیِ چیزی به **وضعیتِ بارگذاریِ ماژول** بستگی دارد، تنها آزمونِ معتبر یک
  پروسه‌ی تازه است.

  **و نشانه‌ی این باگ نبودِ یک ردیف بود، نه وجودِ یک خطا** — برای همین چهار ماه دیده
  نشد. چیزی که هرگز خطا نمی‌دهد فقط وقتی پیدا می‌شود که کسی انتظارِ یک ردیفِ مشخص را
  داشته باشد و نبیندش.

- **۱۴۰۵/۰۶/۲۲ (2026-09-13) — استقرارِ تولید تا `0143`، و دو باگی که همین استقرار رو کرد:**
  تولید نُه مهاجرت عقب بود (`0134`) و به `0143` رسید. مسیرش دو چیز را آشکار کرد که هیچ‌کدام
  در فصل‌ها نبودند.

  **۱. نقشِ `sales_rounding` روی حسابِ درآمدِ فروشِ آنلاین نشسته بود (مهاجرت `0143`).**
  `DEFAULT_CODE_BY_ROLE[SALES_ROUNDING]` کدِ `4102` بود و `4102` در `seed.py`
  «فروش کالا - آنلاین» است. چون `provision_tenant` نقش را از روی همین نگاشت مهر می‌زند،
  در چارتِ **هر** کسب‌وکاری اختلافِ گِردکردنِ فاکتور مستقیم داخلِ درآمدِ فروشِ آنلاین می‌رفت —
  دقیقاً همان چیزی که `SALES_RETURN`/`4107` برای پرهیز از آن ساخته شده بود. نقش به حسابِ
  اختصاصیِ `4108` رفت و مهاجرت چارت‌های موجود را جابه‌جا کرد؛ **ردیف‌های سندِ گذشته
  بازنویسی نشدند** (روی تولید یک ردیف، ۹٬۹۲۳ ریال، سرِ جایش ماند). هیچ تستی نمی‌دیدش چون
  همه‌ی تست‌های رند حساب را *با نقشش* پیدا می‌کنند و نقش همیشه یک حسابی برمی‌گرداند —
  هیچ‌کس نپرسیده بود آن حساب *چه* حسابی است. حالا `test_default_role_codes_are_unique`
  یکتاییِ `ROLE_BY_DEFAULT_CODE` را هم می‌سنجد: دو نقشِ هم‌کد باعث می‌شود یکی بی‌صدا از
  وارونه بیفتد و هرگز مهر نخورد — همان سکوتی که این باگ را ساخت.

  **۲. افزودنِ کلیدِ خارجی زیرِ RLS استقرار را انداخت.** اولین تلاشِ استقرار وسطِ `0139` با
  `invalid input syntax for type uuid: ""` شکست خورد و برگشتِ خودکار تمیز انجام شد.
  مقصر خودِ `ADD FOREIGN KEY` نبود، **اعتبارسنجیِ** آن بود: Postgres پس از افزودنِ قید یک
  اسکنِ راستی‌آزمایی می‌زند که — برخلافِ اعتبارسنجیِ `CHECK` و ساختِ ایندکسِ یکتا که اسکنِ
  مستقیمِ heap‌اند — یک **کوئریِ واقعی** است و مشمولِ RLS. سیاست
  `current_setting('app.tenant_id', true)::uuid` را ارزیابی می‌کند، در مهاجرت آن متغیر تهی
  است، و `''::uuid` کلِ مهاجرت را می‌اندازد. هر هشت نقطه‌ی افزودنِ کلیدِ خارجی روی جدولِ
  موجود در `0136`..`0142` همین شکل را داشت؛ همه داخلِ `rls_disabled` رفتند، با **هر دو**
  جدولِ طرفِ قید. (`create_table` مستثناست: جدولِ تازه ردیف ندارد، پس اسکنی اجرا نمی‌شود.)

  **درسِ ماندگار — چرا هیچ راستی‌آزمایی‌ای این را نگرفت، و هر دو دلیل هنوز برقرارند:**
  * روی جدولِ **خالی** هرگز رخ نمی‌دهد. رِهرسالی که داده را *بعد* از مهاجرت می‌کارد، این
    طبقه از باگ را **ساختاراً** نمی‌بیند. داده باید *پیش* از مهاجرت کاشته شود.
  * **تولید روی PostgreSQL 14.24 است و ماشینِ توسعه روی 17.11.** روی ۱۷ برنامه‌ریز آن
    اسکنِ داخلی را حذف می‌کند و مهاجرت سبز می‌شود — حتی با داده‌ی کاشته‌شده هم بازتولید
    نشد. یعنی این ماشین این کلاس از باگ را **نمی‌تواند** بگیرد، و تنها گاردِ در دسترس یک
    بازرسِ نحوی است. اختلافِ نسخه‌ی Postgres بینِ توسعه و تولید یک بدهیِ باز است.

  شرحِ کاملِ سازوکار در [`app/migration_utils.py`](backend/app/migration_utils.py) نوشته شد،
  کنارِ کاربردِ اولِ همان ابزار.

- **۱۴۰۵/۰۶/۲۲ (2026-09-13) — قیمت‌گذاری اسناد انبار، نوبتِ اول: یک موتورِ ارزش‌گذاری، ترتیبِ
  زمانی، گاردِ خطِ زمان، گزارشِ ریالی و تطبیق با دفتر (بدونِ مهاجرت؛ شاخه‌ی
  `feat/inventory-valuation`، پشتِ #52):** فصلِ «قیمت‌گذاری اسناد انبار» شکاف‌سنجی و نوبتِ اولش ساخته شد.

  **آنچه بود و ماند:** ارزش‌گذاریِ دائمی (هر سندِ انبار همان لحظه بها و سندش را می‌زند)؛ میانگینِ
  موزونِ متحرک، یکی برای هر کالا در کلِ شرکت؛ قفلِ ردیفِ کالا؛ ابطال با حرکتِ جبرانی و سندِ معکوس؛
  جدایی درآمدِ فاکتور از بهای خروج.

  **شکاف‌هایی که بسته شد:**
  * **`services/valuation.py` — یک تعریف از میانگین:** بازپخشِ دفترِ موجودی به ترتیبِ (تاریخِ سند،
    `seq`) با چهار قاعده‌ی حرکت (ورود با بها/با میانگین، خروج با میانگین/با بها). بازمحاسبه‌ی پس از
    ابطال، بهای سندِ پیش‌تاریخ، کاردکس، ارزشِ موجودی و تطبیق همه از همین می‌خوانند؛
    `recompute_average_cost` و `_voided_sources`ِ `voiding.py` فقط صدایش می‌زنند.
  * **ترتیب `seq` بود، نه تاریخ:** خروج، انتقال، تعدیل و مصرفِ تولیدِ پیش‌تاریخ میانگینِ امروز را
    می‌گرفتند. حالا `cost_for_posting` میانگینِ همان روز را می‌دهد و ثبتِ پیش‌تاریخ میانگینِ کالا را از
    بازپخش می‌سازد.
  * **گاردِ موجودیِ منفی فقط امروز را می‌دید:** `settle_posting` (پس از هر سندِ پیش‌تاریخ) و
    `guard_void` (ابطالِ هر ورود) خطِ زمانِ هر کالا در هر انبار را می‌سنجند؛ فقط منفی‌ای که همین تغییر
    ساخته ۴۰۹ می‌گیرد و گذشته‌ی منفیِ قدیمی چیزی را قفل نمی‌کند. همگام‌سازیِ فروشِ آفلاین معاف است.
  * **دو مسیرِ ناهمخوان:** برگشت از خرید در ثبت با بهای خودش بیرون می‌رفت ولی بازپخش بی‌اثرش
    می‌گرفت؛ ابطالِ خروج و انتقال اصلاً بازمحاسبه نمی‌کرد. هر دو یکی شدند. تستِ ساختاری هر سرویسی را
    که `StockLedger` می‌نویسد و از `valuation` نمی‌گذرد می‌گیرد.
  * **ویرایشِ دستیِ `average_cost`** برای کالای دارای موجودی ۴۰۹ است (سندی نمی‌زد و ارزشِ انبار را
    از دفتر جدا می‌کرد).
  * **ارزشِ «تا تاریخ» مقدارِ آن روز × میانگینِ امروز بود:** گزارشِ ارزشِ موجودی حالا مانده‌ی
    اول، ورود، خروج و پایان را به تعداد و ریال از بازپخش می‌دهد (`date_from` اختیاری، `book_value`،
    `stale_from`)، و در گزارش‌ها بازه‌ای شد.
  * **کاردکس** به ترتیبِ تاریخ، با بها، مبلغ، ارزشِ مانده، میانگین، شماره‌ی سند و نشانِ «باطل» و
    «منقضی». مؤلفه‌ی مشترکِ `KardexTable` جای سه جدولِ تکراری (تبِ انبار، کشو، گزارش‌ها) نشست.
  * **بررسی یکپارچگی** دو هشدارِ تازه گرفت: «ارزشِ انبار در برابرِ دفتر» (هر معینِ موجودی در برابرِ
    انبارهای نگاشته‌اش، با تفکیکِ سهمِ منقضی) و «ارزش‌گذاریِ منقضی» (از کِی، چند حرکت، چه‌قدر، و
    علت — سندِ پیش‌تاریخ یا ابطال — از خودِ دفتر)، با میان‌بر به کاردکس.

  **تصمیمِ متفاوت با شکاف‌سنجی:** «منقضی» ذخیره نمی‌شود، مشتق می‌شود. علامتِ ذخیره‌شده فقط اسنادِ
  از امروز را می‌دید؛ این تعریف دفترِ گذشته را هم می‌بیند، مهاجرت نمی‌خواهد و با اصلاحِ ارزش‌گذاری
  خودش پاک می‌شود.

  **عمداً ساخته نشد (نوبتِ دوم):** فرآیندِ قیمت‌گذاری با پیش‌نمایش و سندِ اصلاحی — سندِ حسابداریِ
  حرکاتِ منقضی تا آن‌وقت دست نمی‌خورد؛ FIFO، بهای بچ/سریال، هزینه‌ی خریدِ پس از رسید، انتقال به
  سالِ بعد. تست: `test_inventory_valuation.py`.

- **۱۴۰۵/۰۶/۲۲ (2026-09-13) — اعلامیه‌ی بدهکار/بستانکار، نوبتِ دوم: ارز، گاردِ حساب، ابطالِ مشترک،
  رونوشت، دسترسی از خرید (مهاجرت `0142` — نخست `0136`، پشتِ `0141`):** فصلِ «اعلامیه بدهکار و بستانکار» با
  مشخصاتِ کامل‌تر دوباره شکاف‌سنجی شد.

  **آنچه از `0127` درست بود و دست نخورد:** سندِ مشترکِ ردیفی (هر ردیف جفتِ بدهکار/بستانکار با
  یک مبلغ)، هویتِ یکتای طرف حساب، حلِ معین از نقش، ثبت = سند در همان تراکنش (پس سندِ دوم
  برای یک اعلامیه از ساختار ناممکن است و حالتِ «ذخیره‌شده ولی سندنخورده» عمداً ساخته نشد)،
  مانده و کارتِ حساب از دفتر (پس اثرِ دوبرابر روی طرف حساب ممکن نیست)، و مرزِ «نه خزانه، نه
  انبار، نه مالیات، نه تسویه‌ی خودکار».

  **شکاف‌ها — چهارتا باگ یا خطرِ واقعی:**
  * **ارز ذخیره می‌شد ولی اعمال نه.** اعلامیه‌ی ۱۰۰ دلاری در دفتر ۱۰۰ ریال می‌نشست. حالا مبلغِ
    ردیف به ارزِ سند است و `base_amount = مبلغ × نرخ` ردیف‌به‌ردیف گرد (همان قراردادِ رسید و
    اعلامیه پرداخت) کنارش ذخیره می‌شود؛ جمعِ دفتر دقیقاً جمعِ همین ستون است. مبلغِ اعشاری رد
    می‌شود به‌جای گردشدنِ بی‌صدا در ستونِ `Numeric(18,0)`. فرم ارز و نرخ گرفت.
  * **ابطالِ همزمان دو سندِ معکوس می‌زد** — ردیف قفل نمی‌شد.
  * **ابطال مسیرِ مشترک را نمی‌رفت:** معکوس `reverses_entry_id` نداشت (دفتر روزنامه جفتِ اصلی
    و معکوس را نمی‌دید)، همیشه امروز تاریخ می‌خورد، و «چه کسی و چرا» روی اعلامیه نمی‌ماند.
    حالا `reverse_journal_entry` با قفل، تاریخِ پیش‌فرضِ اعلامیه (قابلِ تغییر به دوره‌ی باز) و
    `voided_by_id`/`void_reason`.
  * **سرویس هر معینی را می‌پذیرفت** — فقط فرم محدود بود: اعلامیه‌ای از راهِ API صندوق را
    بستانکار می‌کرد بی‌آنکه تراکنشِ خزانه‌ای باشد. حالا سمتِ دارای طرف حساب فقط دریافتنی یا
    پرداختنی (وگرنه تعدیل از کارتِ حسابِ همان شخص غایب می‌شد) و سمتِ بی‌طرف‌حساب نه آن دو و
    نه حساب‌های خزانه، انبار و مالیات — **با نقش یا با نگاشتِ `gl_account_id`ِ بانک، صندوق و
    انبار**. فرم همان فهرست را از `GET /notes/accounts?scope=other` می‌گیرد.

  **و یک یافته‌ی جانبی:** روتر مجوزِ ماژولِ `sales` می‌خواست که **در رجیستریِ مجوزها نیست** —
  یعنی جز مالک هیچ‌کس، حتی حسابدار، نمی‌توانست اعلامیه بزند. `require_permission` حالا چند
  ماژولِ جایگزین می‌پذیرد؛ اعلامیه با `invoices` یا `accounting` صادر و دیده می‌شود و ابطالش
  `accounting.delete` می‌خواهد (همان ابطالِ برگشت‌ها). **بقیه‌ی عملیاتِ `sales-ops` هنوز
  پشتِ همان ماژولِ ناموجودند** — دست نخوردند و باید جداگانه تصمیم گرفته شوند.

  **قابلیت‌های تازه:** «رونوشت» (`GET /notes/{id}/duplicate-draft` — چیزی نمی‌نویسد، پس تکرارش
  چیزی نمی‌سازد؛ شماره، تاریخ، سند و کلید کپی نمی‌شوند) و «اصلاح» = ابطال + فرمِ پیش‌پر،
  نه ویرایشِ سند · دفترِ اعلامیه‌ها با **فیلترِ سمتِ سرور** و keyset (تا امروز همه را یک‌جا
  می‌کشید)، ستونِ سندِ حسابداری، ارز و معادلِ ریالی، وضعیتِ ابطال با سندِ معکوس، ردیف‌های
  بازشونده و پنلِ ابطال با تاریخ · `GET /notes/{id}` · کارتِ حساب شماره و تاریخِ سندِ هر ردیف
  و `source_id` را می‌دهد (برای همه‌ی انواعِ سند) · فرم: نوعِ هر سمت (مشتری، تأمین‌کننده، حساب)
  و پنلِ «اطلاعات حساب» · **همان صفحه** در گروهِ «تامین‌کنندگان و انبار» هم آمد و برای
  کسب‌وکارِ فقط-خرید هم دیده می‌شود (`PAGE_MODULE_KEY` آرایه پذیرفت؛ فهرست‌های تختِ
  ناوبری با `uniqueNavItems` تکرار را حذف می‌کنند) · برچسبِ `credit_debit_note` و
  `void_*` در دفتر روزنامه و کارتِ حسابِ موبایل.

  **عمداً ساخته نشد:** حالتِ پیش‌نویس/سندنخورده، ویرایشِ درجای اعلامیه‌ی ثبت‌شده، ستونِ
  ردیابیِ ردیف‌به‌ردیف (از ترتیبِ ردیف‌ها مشتق است: ردیفِ k ← ردیف‌های `2k-1` و `2k`ِ سند،
  با تست)، ثبتِ `fx_amount` روی ردیف‌های دفتر (رسید و فاکتور هم نمی‌کنند؛ تسعیر باید یکسان
  بماند)، چاپِ اختصاصیِ اعلامیه.

  **راستی‌آزمایی:** ۱۶ تستِ تازه در `test_counterparty_notice.py` (ارز و گرد کردن، اعشار، ارزِ
  ناشناخته، دو قاعده‌ی گاردِ حساب شاملِ نگاشتِ انبار، مسیرِ ابطال و تاریخش، فهرست و صفحه‌بندی،
  رونوشت، ترتیبِ ردیف‌ها، ردیابیِ کارتِ حساب، مجوزِ سه نقش) — کلِ سوییت **۲۷۴۹ سبز**. `tsc`
  صفر خطا، `audit-pages` صفر خطا (۱۴ هشدارِ قبلی). پایگاه دادهٔ توسعه `0135 → 0136`. مرورگر در
  ۱۴۴۰ و ۳۹۰ از راهِ خودِ رابط: بازکردن از گروهِ خرید، اعلامیه‌ی دوردیفی (تأمین‌کننده ← مشتری،
  و «تخفیفات» بی‌طرف‌حساب ← مشتری) با پنلِ اطلاعاتِ درست، دفتر با شماره‌ی سند، ردیف‌های
  باز، رونوشت با نوعِ سمتِ بازسازی‌شده، ابطال با پنل؛ جست‌وجوی نوار صفحه‌ی مشترک را یک
  بار نشان داد. سرریز، جدولِ بی‌کارت، سلولِ بی‌برچسب و خطای کنسول صفر. اعلامیه‌ی آزمایشی
  باطل شد.

- **۱۴۰۵/۰۶/۲۲ (2026-09-13) — فاکتور خرید خدمات: هزینه یکی، بدهی سه‌تا (مهاجرت `0141` — نخست `0135`؛ پس از ادغامِ master که `0136`–`0140`ِ حقوق و شعبه را دارد جابه‌جا شد):**
  فصلِ «فاکتور خرید خدمات».

  **شکاف‌سنجی پیش از ساخت.** خدمت از قبل موجودی نمی‌ساخت و به حسابِ هزینه‌ی خودش
  می‌نشست، مرکزِ هزینه به سند می‌رسید و تسویه از روی دفتر حساب می‌شد. ولی:
  * **کسورات اصلاً نبود.** بستانکارِ تأمین‌کننده همیشه «خالص + ارزش افزوده» بود؛ تنها
    نقشِ مشابه `2103` مالِ بیمه و مالیاتِ **کارکنان** است.
  * فاکتور خرید خدمات سندِ جدا نبود: یک سری شماره، یک عنوانِ چاپی، یک فهرست.
  * **مانده دروغ می‌گفت:** فهرست `settled_amount=0` و مانده = جمعِ فاکتور را ثابت
    نوشته بود (توضیحِ کد هنوز منتظرِ موتورِ تسویه‌ای بود که از `0114` هست)، و میان‌برِ
    «اعلامیه پرداخت» همان جمع را پیشنهاد می‌داد.
  * **باگِ حسابِ تاریخی:** حسابِ هزینه روی ردیف نبود و برگشت از خرید آن را از نگاشتِ
    **امروزِ** خدمت می‌گرفت — با عوض‌شدنِ معینِ خدمت، برگشت حسابی را بستانکار می‌کرد که
    فاکتور هرگز بدهکارش نکرده بود.
  * فاکتوری که فقط خدمت داشت برای همیشه «تحویل‌نشده» بود و برای ردیفِ خدمت رسیدِ انبارِ
    بی‌حرکت ساخته می‌شد.

  **چه ساخته شد:**
  * **نوعِ سند روی همان جدول** (`purchase_invoices.kind`) با سریِ شماره‌ی
    `service_purchase_invoice` و یکتایی روی (مستأجر، نوع، شماره). تسویه، اعلامیه‌ی
    پرداخت، ابطال، رونوشت و ردیابیِ سند بی‌کپی کار می‌کنند. فاکتور خرید خدمات فقط خدمت
    می‌پذیرد، انبار و رسید ندارد. فاکتور خرید کالا همچنان ردیفِ خدمت می‌پذیرد، بی‌کسر.
  * **«انواع کسورات»** (`purchase_deduction_types`): عنوان، ماهیت (مالیات تکلیفی/بیمه)،
    مبنا (خالص پیش از مالیات و عوارض / ناخالص)، نرخِ پیش‌فرض، حساب. **هیچ درصدی از پیش
    ساخته نمی‌شود.** نوعِ استفاده‌شده حذف نمی‌شود و ماهیتش عوض نمی‌شود.
  * **کسوراتِ هر فاکتور** (`purchase_invoice_deductions`): Snapshotِ نوع، مبنا، نرخ،
    مبلغ و حساب. مبلغ از مبنا × نرخ پیشنهاد می‌شود و کاربر می‌تواند عوضش کند. جمعِ
    کسورات از جمعِ فاکتور بیشتر نمی‌شود.
  * **سند:** هزینه کلِ مبلغ بدهکار؛ هر کسر یک بستانکارِ مستقل؛ تأمین‌کننده مابقی. دو
    نقشِ تازه با ساختِ تنبل: `withholding_tax_payable` (`2112`) و
    `contract_insurance_payable` (`2113`). مثالِ ویدیو دقیقاً همان سندِ چهارردیفی است.
  * **حسابِ هزینه روی ردیف** (`purchase_invoice_lines.expense_account_id`) با override
    فقط در فاکتور خرید خدمات؛ سند و برگشت از همین می‌خوانند. ردیف‌های پیش از `0141`
    `NULL`اند و همان رفتارِ قبل را دارند.
  * **مانده‌ی واقعی:** فهرست تسویه‌شده را از تخصیص‌های موتورِ تسویه و مانده را از
    «جمعِ فاکتور − کسورات» می‌گیرد؛ میان‌برِ پرداخت مانده را پیشنهاد می‌دهد و جمعِ فاکتور
    را فقط مرجع نشان می‌دهد. کرسرِ فهرست `id` را کلیدِ سوم گرفت (دو سری شماره در یک روز).
  * **چاپ/PDF «فاکتور خرید خدمات»** با کدِ خدمت، «جمع فاکتور»، ردیفِ هر کسر (−) و
    «خالص فاکتور»؛ مبلغ به حروف همان خالص.
  * **رابط:** دو تبِ تازه در ماژولِ خرید — «فاکتور خرید خدمات» (فرمِ خدمت با معین،
    مقدار و واحد، کسورات، جمع‌های تفکیکی، و دفتر با ستون‌های مالیات تکلیفی، بیمه، خالصِ
    پرداختنی و سند) و «انواع کسورات». تبِ «فاکتور خرید» فقط کالا را فهرست می‌کند.

  **عمداً ساخته نشد:** برگشت از فاکتوری که کسورات دارد (۴۰۹ — قاعده‌ی تسهیمِ کسورات در
  برگشتِ جزئی تعریف نشده؛ اصلاح = ابطال و ثبتِ دوباره)؛ «سپرده‌ی بیمه»ی قابلِ استرداد
  به پیمانکار (فصل نخواست؛ جدولِ انواع اجازه‌ی افزودنش را می‌دهد)؛ تاریخِ اثرِ نرخ؛
  مرکزِ هزینه‌ی ردیفی؛ ورود با واحدِ فرعی.

  **راستی‌آزمایی:** ۲۱ تستِ تازه (`test_service_purchase_invoice.py`) — سندِ چهارردیفیِ
  مثالِ ویدیو، ارزش افزوده بیرونِ مبنای کسر، مبنای خالص/ناخالص، مبلغِ دستی و Snapshotِ
  نرخ، سریِ شماره، برگشت با حسابِ تاریخی (هر دو نوعِ فاکتور)، برگشتِ کسوردار ۴۰۹، قلمِ
  بازِ تأمین‌کننده = خالص، ابطالِ همه‌ی بدهی‌ها، ردِ رسید، idempotency، چاپ/PDF و CRUDِ
  انواعِ کسر. محافظِ `test_chart_renumbering` نبودِ دو نقشِ تازه در چارتِ پایه را گرفت؛
  به `seed.py` اضافه شدند. کلِ سوییت ۲۷۳۳ سبز. `0134 → 0135` روی پایگاه دادهٔ توسعه.
  مرورگر در ۱۴۴۰ و ۳۹۰ از راهِ خودِ رابط (ساختِ دو نوعِ کسر، ثبتِ مثالِ ویدیو، دفتر،
  جزئیات، میان‌برِ پرداخت با مبلغِ ۸۰٬۳۳۳٬۰۰۰): سرریز/جدولِ بی‌کارت/سلولِ بی‌برچسب/خطای
  کنسول صفر.
- **۱۴۰۵/۰۶/۲۲ (2026-09-13) — «فعال» روی عامل نوشته می‌شد و هیچ‌کس
  نمی‌خوانْدَش (مهاجرت `0140`):**
  - **سه ستونِ مرده روی `payroll_factors`.** `is_active`، `is_extraordinary` و
    `kind` هیچ مصرف‌کننده‌ای نداشتند. یعنی عاملی که کاربر غیرفعال کرده بود
    **همچنان به حکمِ تازه اضافه می‌شد و در فیش می‌آمد**. حالا `is_active` گاردِ
    واقعی دارد: عاملِ غیرفعال به حکمِ تازه نمی‌رود، ولی حکم‌های موجود و فیش‌های
    صادرشده دست نمی‌خورند — «دیگر انتخاب نشو»، نه «از گذشته پاک شو».
  - **و `PATCH` اصلاً جزئی کار نمی‌کرد.** اسکیمای کامل می‌گرفت، پس «فقط این
    عامل را غیرفعال کن» ۴۲۲ می‌داد و عملاً هیچ مسیری برای غیرفعال‌کردن نبود.
    رابط هم هیچ‌وقت صدایش نمی‌زد. حالا `PayrollFactorPatch` و ویرایشِ عامل در
    صفحه هست.
  - **طبقه‌ی عاملِ در استفاده قفل شد.** بردنِ یک عامل از «مزایا» به «کسورات»
    علامتِ مبلغش را برمی‌گرداند؛ روی عاملی که در حکم یا فیش نشسته یعنی معنیِ
    داده‌ی گذشته بی‌صدا عوض شود. تغییرِ نام همچنان آزاد است — هویت `factor_id`
    است نه نام.
  - **اولویتِ نمایش.** ترتیبِ ردیف‌های فیش تا امروز در کد ثابت بود. **فقط
    نمایشی است:** هیچ محاسبه‌ای از آن نمی‌خوانَد، و چون `PayslipLine.seq` لحظه‌ی
    صدور منجمد می‌شود، تغییرش فیشِ گذشته را تکان نمی‌دهد.
  - **پروفایلِ حسابداریِ عامل.** سند یک ردیفِ «هزینه حقوق» برای کلِ دوره می‌زد و
    همه‌ی مزایا در یک عدد گم می‌شدند. حالا هر عامل می‌تواند حساب و بُعدِ
    تفصیلیِ خودش را داشته باشد (مرکز هزینه / طرف مقابل، از همان زیرساختِ
    مرکزی). **تقدم صریح است:** عاملِ حساب‌دار سهمش به حسابِ خودش، عاملِ بی‌حساب
    به حسابِ عمومی — پس تا وقتی کسی حسابی نگذارد سند بایت‌به‌بایت همان می‌ماند.
    دو سمت مستقل‌اند: مزایا سمتِ هزینه، کسور سمتِ پرداختنی.
  - **آنچه عمداً ساخته نشد:** طبقه‌ی «کارکرد» — کوبیتا کارکرد را در ستون‌های
    `Attendance` دارد و ساختنِ عاملِ کارکرد یعنی **دو کاتالوگ برای یک چیز**،
    دقیقاً همان چیزی که فصل خطرناک‌ترین تله می‌نامدش. همچنین طبقه‌ی «تعهدات
    کارفرما» (موتوری ندارد)، پیکربندیِ فرمول، `is_system`، و تغییرِ رفتارِ حذف.
  - **۲۸۵۴ تست سبز** (۱۸ تازه). مهاجرت روی اسکیمای یک‌بارمصرف رفت‌وبرگشت شد؛
    دیتابیسِ توسعه روی `0098` دست نخورد.

- **۱۴۰۵/۰۶/۲۲ (2026-09-13) — پارامترهای قانونیِ حقوق از سورس‌کد به تنظیمات
  آمدند، و «مشمولِ بیمه» از یک پرچم به یک ماتریس (مهاجرت `0138` و `0139`):**
  - **یک باگِ توازن، با پروب اندازه‌گیری شد.** ردیف‌های *کسورات*ِ قرارداد خالص را
    کم می‌کردند ولی هیچ ردیفِ بستانکاری در سند نداشتند. با یک کسورِ ۵٬۰۰۰٬۰۰۰:
    بدهکار ۳۶۹٬۰۰۰٬۰۰۰ در برابر بستانکار ۳۶۴٬۰۰۰٬۰۰۰ — اختلاف دقیقاً همان کسور،
    بی هیچ خطایی، و تراز آزمایشی بی‌صدا به‌هم می‌خورد. حالا نقشِ
    `payroll_deductions_payable` ردیفش را می‌گیرد.
  - **پنج عددِ قانونی از کد بیرون آمدند:** ساعتِ ماهانه (۱۹۴)، ضریبِ اضافه‌کار
    (۱٫۴)، ضریبِ مبنای عیدی (۲)، روزهای سنوات در سال (۳۰) و مبنای روزِ کاری (۳۰).
    هر تغییرِ مصوبه تا امروز یک استقرار می‌خواست.
  - **و سه چیزی که اصلاً نبودند:** سقفِ روزانه‌ی بیمه (نرخ روی کلِ ناخالص می‌خورد،
    پس حقوقِ بالای سقف بیمه‌ی بیش از واقع می‌داد)، نرخِ بیمه‌ی بیکاری و مشاغل
    سخت، و ضریبِ معافیتِ بیمه از مالیات. به‌علاوه‌ی سیاستِ «مالیاتِ منفی محاسبه
    نشود» و رندِ خالصِ پرداختی با حسابِ `payroll_rounding`ِ مستقل.
  - **«مشمولِ بیمه» یک پرچمِ واحد نیست.** `payroll_factor_participations` به‌ازای
    هر `(عامل، هدف)` ضریب می‌گیرد، برای پنج هدفی که کوبیتا **امروز موتورشان را
    دارد**: مبنای بیمه، مالیات، عیدی، سنوات، بازخریدِ مرخصی. یک قاعده در
    `services/factor_participation.py`، نه چهار موتورِ موازی.
  - **مبنای عیدی/سنوات/مرخصی دیگر «فقط حقوق پایه» نیست** — همان چیزی که فصلِ
    مرجع «اشتباهِ خطرناک» می‌نامد. مبنا از ردیف‌های قرارداد ساخته می‌شود.
  - **پیش‌فرض = رفتارِ دیروز، و روی داده‌ی واقعی سنجیده شد.** جدولِ مشارکت
    **استثنا** نگه می‌دارد: نبودِ ردیف یعنی «همه شریک‌اند» برای بیمه/مالیات و
    «فقط حقوق پایه» برای سه مبنای مزایا — رمزگذاریِ دقیقِ فرمولِ قبلی. روی اسکیمای
    یک‌بارمصرف، ردیفِ تنظیماتِ پیش از مهاجرت دقیقاً ثابت‌های قبلی را گرفت
    (۰ / ۰ / ۲ / ۳۰ / ۳۰ / ۱۹۴ / ۱٫۴ / ۱) و صفر ردیفِ مشارکت ساخته شد.
  - **و شش پرچمِ مرده روی خودِ حکم زنده شدند.** `salary_contracts` از قبل
    `is_insured`، `is_hard_job`، `exempt_employee_insurance`،
    `exempt_employer_insurance`/`employer_exempt_percent`،
    `exempt_unemployment_insurance` و `housing_loan_exempt_amount` را داشت و
    **هیچ‌کدام خوانده نمی‌شدند**. پروب: حکمی با «مشمول بیمه = خیر» و «معاف از
    بیمه سهم کارمند» باز هم ۲۱٬۰۰۰٬۰۰۰ کسر می‌کرد. معنیِ هرکدام از برچسبِ خودِ
    فرم گرفته شد؛ پیش‌فرضِ حکم دقیقاً همان نرخ‌های تنظیمات را می‌دهد.
  - **آنچه ساخته نشد:** بیمه‌ی تکمیلی/درمان به‌عنوان موتور، تقویم کاری، گردشِ
    تأیید، و نسخه‌بندیِ جداگانه‌ی تنظیماتِ حسابداری — هیچ‌کدام شاهد یا موتور
    ندارند.
  - **۲۸۲۸ تست سبز** (۳۷ تازه). هیچ تستی ویرایش نشد؛ تنها شکستِ مسیر
    (`payroll_rounding` در چارتِ مستأجرِ تازه نبود) با افزودنِ حساب به seed بسته شد.
    مهاجرت‌ها روی اسکیمای یک‌بارمصرف با داده رفت‌وبرگشت شدند؛ دیتابیسِ توسعه روی
    `0098` دست نخورد.

- **۱۴۰۵/۰۶/۲۲ (2026-09-13) — جدولِ مالیات یک بُعد داشت — سال — و قاعده‌ی
  واقعی سه بُعد دارد (مهاجرت `0137`):**
  - **یک باگ، با پروب ثابت شد.** موتور فرض می‌کرد پلکان صعودی است ولی هیچ‌کس
    این را نمی‌سنجید. با پله‌های ۱۰۰@۱۰٪ · ۲۰۰@۲۰٪ · ∞@۳۰٪ و مبنای ۳۰۰، فهرستِ
    مرتب ۶۰ می‌داد و فهرستِ **نامرتب ۱۰۰** — ۶۷٪ اضافه، بی هیچ خطایی، و اسکیما
    فهرستِ نامرتب را می‌پذیرفت. حالا ورودی رد می‌شود و موتور هم دفاعی مرتب
    می‌کند (برای داده‌ای که از قبل ذخیره شده).
  - **سه بُعد به‌جای یک سال.** `payroll_settings.tax_brackets` یک `JSONB` یکتا
    روی `(مستأجر، سال)` بود. حالا `tax_tables` + `tax_table_brackets` با
    `(تاریخِ اجرا، گروهِ مالیاتی، نوعِ محاسبه)`، و ایندکسِ یکتا روی همان سه‌تا تا
    انتخابِ جدول هیچ‌وقت مبهم نشود. جدول از روی **عنوان** پیدا نمی‌شود.
  - **و ضربی که بازنشسته شد.** «مناطق محروم» = پلکانِ عادی × درصدِ گروه ÷ ۱۰۰،
    یعنی نرخِ مؤثر از **نامِ گروه** ساخته می‌شد. حالا هر گروه جدولِ خودش را
    دارد. مهاجرت ضرب را **یک‌بار در داده منجمد می‌کند** (پیش‌فرض ۱۰/۲۰، محروم
    ۵/۱۰، معاف ۰/۰) پس **عددِ هیچ فیشی عوض نمی‌شود** — ولی از فردا نرخ در جدول
    است و ویرایش‌شدنی.
  - **یافته‌ی مهاجرت: RLS انتقال را بی‌صدا خنثی می‌کرد.** پروب نشان داد نقشِ
    `cubita_app` `BYPASSRLS` ندارد و `payroll_settings` هم `FORCE RLS` دارد، پس
    `SELECT`ِ بی‌زمینه **صفر ردیف** می‌داد بی هیچ خطایی. نسخه‌ی اولِ انتقال
    دقیقاً همین بود و روی تولید هیچ جدولی نمی‌ساخت. حالا مستأجربه‌مستأجر با
    `app.tenant_id`ِ ست‌شده اجرا می‌شود.
  - **ردیابیِ قاعده.** `payslips.tax_table_id` — «مالیاتِ مرداد از کدام جدول
    آمد؟» جواب دارد، و `GET /api/payslips/{id}/tax-breakdown` تفکیکِ پله‌به‌پله
    («مبلغ جزء» و «مبلغ کل») را **مشتق** می‌کند، نه ذخیره.
  - **عیدی جدولِ خودش را گرفت.** تا امروز عیدی اصلاً مالیات نمی‌خورد. حالا
    جدولِ `eidi` را می‌خواند و اگر تعریف شده باشد سندِ عیدی سه‌خطی می‌شود.
    جدولِ عیدی **خودکار ساخته نمی‌شود** — مهاجرت نباید بی‌خبر مالیات کسر کند.
  - **مدلِ «سقفِ تجمعی» عمداً حفظ شد** به‌جای «از/تا»ی مرجع: با آن، شکاف و
    همپوشانیِ پله‌ها ساختاراً ناممکن‌اند و «از مبلغ» از سقفِ ردیفِ قبل مشتق
    می‌شود، نه ستونِ دوم. سقفِ نامحدود `NULL` است، نه ۹۹٬۹۹۹٬۹۹۹٬۹۹۹.
  - **آنچه ساخته نشد:** فهرستِ بسته‌ی «نوع محاسبه» فراتر از دو مقدارِ دیده‌شده،
    تاریخِ پایانِ اعتبار (از جدولِ بعدی مشتق می‌شود)، ارجاع به شعبه روی جدول،
    گردشِ تأیید، و ماشین‌حسابِ پیش‌نمایش — هیچ‌کدام شاهد ندارند.
  - **۲۷۸۶ تست سبز** (۳۰ تازه). مهاجرت روی اسکیمای یک‌بارمصرف با **داده‌ی واقعی**
    رفت‌وبرگشت شد و انتقالِ سه‌گانه تأیید شد؛ دیتابیسِ توسعه دست نخورد.

- **۱۴۰۵/۰۶/۲۲ (2026-09-13) — شعبه‌ی بیمه و مالیات یک اسم بود، نه یک هویت
  (مهاجرت `0136`):** شکاف‌سنجیِ فصلِ «تعریف شعب بیمه و مالیات» نشان داد اسکلت از
  قبل درست است — *یک* مِستر برای هر دو (`kind`)، نه دو زیرسیستمِ موازی؛ نوع با
  شناسه‌ی پایدار و برچسبِ جدا؛ و تخصیصِ **نقش‌محور** روی حکم (`tax_branch_id` و
  `insurance_branch_id` جدا، نه یک `branch_id` عمومی). و «نفراتِ معاف» از چیزی که
  فصل می‌خواست بهتر بود: به‌جای یک عددِ شعبه‌ای، معافیت‌ها کارمند‌به‌کارمند روی خودِ
  حکم نشسته‌اند. سه شکاف بسته شد:
  - **شعبه به هیچ هویتِ حسابداری وصل نبود.** فقط `code` و `name` داشت، در حالی که
    بدهیِ بیمه و مالیاتِ تکلیفی واقعاً **به همان سازمان پرداخت می‌شود** و پرداخت
    طرفِ حساب می‌خواهد. حالا `contact_id` دارد: شعبه ← طرف حساب ← تفصیلی. پیوند
    عمداً به *طرف حساب* است و نه به تفصیلی — کدِ تفصیلی رشته‌ای است که کاربر
    عوضش می‌کند، و پیوندِ متنی همان چیزی است که این فصل دارد از بین می‌برد.
  - **انتخابِ شعبه هیچ اثری نداشت.** `tax_branch_id` و `insurance_branch_id`
    سال‌ها روی حکم می‌نشستند و **هیچ‌جا خوانده نمی‌شدند** — نه در محاسبه، نه در
    خروجی. حالا فایلِ بیمه و فایلِ مالیات ستونِ شعبه دارند، و شعبه از حکمِ **همان
    دوره** می‌آید نه از آخرین حکم: کارمندی که ماهِ پیش منتقل شده، در فایلِ ماهِ
    پیش زیرِ شعبه‌ی قبلی می‌آید. حکمی که شعبه ندارد ستونِ **خالی** می‌دهد، نه یک
    شعبه‌ی حدسی.
  - **تغییرِ شعبه ردِ حسابرسی نداشت** — با اینکه عوض‌کردنِ طرف حسابِ یک شعبه یعنی
    بدهیِ ده‌ها حکم جای دیگری برود، و هیچ سندی این را نشان نمی‌دهد.
  - `PATCH /api/insurance-tax-branches/{id}` اضافه شد؛ بی آن، `contact_id` ستونی
    می‌شد که فقط شعبه‌های *تازه* پرش می‌کردند و هر شعبه‌ای که امروز روی حکم‌ها
    نشسته تا ابد بی‌هویت می‌ماند. حذف عمداً نیست — `is_active` همان کار را بدونِ
    بی‌مرجع‌کردنِ حکم‌های گذشته می‌کند.
  - **آنچه عمداً ساخته نشد:** «نحوه محاسبه مالیات» (تعدیل ماهانه/سالانه/بدون
    تعدیل) — فصل خودش می‌گوید فهرستِ کاملش قطعی نیست و موتور فقط یک روش را
    می‌فهمد؛ ستونی که کاربر پرش کند و هیچ اثری نداشته باشد از نبودنش بدتر است.
    و بیمه‌ی تکمیلی روی همین مِستر — قواعدِ محاسبه‌اش در فصل «نامعلوم» است.
  - **و بعد فصلِ «تعریف شعب بیمه» همان مهاجرت را کامل کرد.** فهرستِ شعب در
    سپیدار هر سه نوع را کنارِ هم نشان می‌دهد و ستون‌های ثبتِ کارگاه را هم — یعنی
    شواهدی که فصلِ قبل نداشت:
    - **نوعِ سوم:** کرکره سه گزینه دارد، نه دو. `BRANCH_KINDS` شد
      `("insurance", "tax", "supplementary")`. (`supplementary_branch`ِ متنیِ
      روی حکم فعلاً می‌ماند؛ مهاجرتش وقتی است که معلوم شود چه محاسبه‌ای می‌کند.)
    - **هسته‌ی مشترکِ ثبت:** کد شرکت/شماره پرونده، نام و نشانیِ کارگاه، نام
      کارفرما، شماره پیمان، نفراتِ معاف، مرکز هزینه. `registration_code` عمداً
      عمومی نام‌گذاری شد و نه `tax_file_number` — معنایش با نوعِ شعبه عوض
      می‌شود، و هیچ اعتبارسنجیِ قالبی رویش نیست چون هیچ‌جا اثبات نشده.
    - **قیدِ نوع‌محور در خودِ پایگاه داده:** ردیفِ مالیاتی «تعدیل ماهانه» دارد و
      ردیفِ تأمین اجتماعی همان ستون را **خالی**. یعنی وجودِ میدان در فرمِ مشترک
      ≠ کاربردش برای هر نوع، و این باید سمتِ سرور فهمیده شود نه با غیرفعال‌کردنِ
      یک ورودی در مرورگر:
      `CHECK (tax_calculation_method = '' OR kind = 'tax')`. مقادیر هم به همان
      سه‌تای دیده‌شده بسته‌اند، مثلِ `FREIGHT_BASES`ِ `0131`. **و هنوز هیچ
      محاسبه‌ای نمی‌خواندش** — داده‌ی ثبتِ قانونی است نه سوییچِ محاسبه.
    - **نوع، هویت است:** شعبه‌ای که روی حکمی نشسته دیگر نوعش عوض نمی‌شود.
    - **و مهم‌ترینش — عکسِ شعبه روی فیش.** فایلِ بیمه شعبه را از مِسترِ *جاری*
      می‌خواند، پس ثبتِ تازه‌ی کارگاه بازتولیدِ فایلِ پارسال را بی‌صدا عوض
      می‌کرد. حالا فیش شعبه‌ی مؤثرِ لحظه‌ی صدور را عکس می‌گیرد، همان‌طور که
      `payslip_lines.factor_name` می‌گیرد — و این **عکسِ** تصمیمِ هویتِ کارمند
      است، به دلیلی روشن: اصلاحِ نامِ یک آدم غلطِ تایپی را درست می‌کند و باید به
      فایل برسد؛ ثبتِ تازه‌ی کارگاه رویدادِ واقعیِ تازه‌ای است و نباید گذشته را
      بازنویسی کند. فیشِ بی‌عکس (پیش از ۰۱۳۶) به حلِ زنده برمی‌گردد.
    - **و باز هم آنچه ساخته نشد:** نرخِ بیمه روی شعبه (این مِستر ثبت است نه
      نرخ)، «کدام کارمندان معاف‌اند» از روی عددِ سرصفحه، قاعده‌ی ثبتیِ مرکز
      هزینه، یکی‌کردنِ کارگاه با محل خدمت، و کلیدِ خارجی از شماره‌ی پیمان به حکم.
  - **۲۷۴۶ تست سبز** (۳۱ تازه). مهاجرت روی اسکیمای یک‌بارمصرف رفت‌وبرگشت شد
    (upgrade → downgrade → upgrade)، با `FORCE RLS` و دقیقاً یک سیاست؛ دیتابیسِ
    توسعه دست نخورد.

- **۱۴۰۵/۰۶/۲۲ (2026-09-13) — کارمند هویتِ دوم نبود، ولی دو حقیقت می‌ساخت
  (بدونِ مهاجرت):** شکاف‌سنجیِ فصلِ «تعریف کارمند» نشان داد معماریِ کوبیتا از قبل
  درست است — `Contact.is_employee` تیکِ نقش، `Contact.employee_id` پیوند، و
  واقعیت‌های *شخص* (جنسیت، تأهل، فرزند/تحتِ تکفلِ جدا، تحصیلات، تاریخِ تأهلِ جدا
  از تاریخِ ازدواج، افرادِ مرتبطِ جدا از تحتِ تکفل) همه روی طرف حساب. ولی دو جا
  همان اصل نقض می‌شد:
  - **نام و کدِ ملی دو حقیقت داشتند.** `Employee` این‌ها را هنگامِ استخدام کپی
    می‌کرد و هرگز همگام نمی‌شد. و چون **هر سه خروجیِ قانونی** (مالیات، بیمه،
    دیسکتِ پرداختِ بانک) از همان کپی می‌خواندند، اصلاحِ نام یا کدِ ملی هیچ‌وقت به
    فایلِ بانک و اظهارنامه نمی‌رسید. با پروبِ واقعی سنجیده شد: طرف حساب
    «محمدیِ تازه / ۰۹۱۲۹۹۹۹۹۹۹» و کارمند «محمدی / ۰۹۱۲۰۰۰۰۰۰۱».
  - **`POST /api/employees` هویتِ یتیم می‌ساخت** — کارمندی بدونِ هیچ طرف حسابی،
    یعنی همان رکوردِ تکراری که مِسترِ مشترک برای جلوگیری از آن هست.
  - **اصلاح، بدونِ شکستنِ هیچ فراخوانی.** `employee_identities` هویت را از طرف
    حسابِ متصل می‌خواند و ستون‌های `Employee` **پشتیبان** می‌شوند، نه حقیقتِ دوم
    — برای کارمندانِ میراثیِ بی‌طرف‌حساب. و اندپوینت هر دو شکل را می‌پذیرد: با
    `contact_id`، یا با نام و کدِ ملی که آن‌وقت **اول دنبالِ طرف‌حسابِ موجود با
    همان کدِ ملی می‌گردد** و فقط اگر نبود می‌سازدش. جست‌وجو با نام انجام نمی‌شود؛
    دو «محمد رضایی» یک نفر نیستند.
  - دو مسیرِ موازیِ ساختِ کارمند (فرمِ کارمند، و استخدامِ ضمنیِ هنگامِ ثبتِ حکم) به
    یک درِ ورودی رسیدند.
  - **مرزی که حفظ شد:** هویت از طرف حسابِ امروز می‌آید، ولی **اعدادِ فیش** همان
    snapshotِ لحظه‌ی صدور می‌مانند — تستی این را قفل می‌کند.
  - **۲۷۱۵ تست سبز** (۱۳ تازه). هیچ تستِ موجودی نشکست، چون شکلِ قدیمیِ API
    دست‌نخورده ماند.

- **۱۴۰۵/۰۶/۲۲ (2026-09-13) — استقرارِ `0129` → `0134` روی تولید:** PR #45 (رسید انبار،
  برگشت رسید انبار، خروج انبار، برگشت خروج انبار) — سه شاخه‌ی زنجیره‌ای در یک PR به
  master، merge از راهِ API گیت‌هاب، بسته‌ها با `pack.sh`.

  **پیش از استقرار (فقط‌خواندنی):** تولید روی `0128` بود و استقرارِ دیگری در جریان نبود.
  `stock_ledger` ۶۷ ردیف و `warehouse_issue_lines` صفر — بازنویسیِ `0131`/`0133`
  ناچیز بود. **هیچ‌یک از دو باگِ برگشتِ فروش روی داده‌ی تولید اثر نکرده بود:** صفر
  برگشتِ معتبر روی فاکتوری که همه‌ی خروج‌هایش باطل‌اند، صفر مستأجرِ «دومرحله‌ای». صفر
  انبار با `gl_account_id` — پس هیچ انتقالِ گذشته‌ای بی‌سند نمانده.

  **استقرار:** `deploy.sh production` مهاجرت‌ها را پشتِ سرِ هم اجرا کرد و نسخه‌ی نهایی
  `0134` شد؛ health سالم، ۱۳۵ جدول با FORCE RLS، پشتیبانِ پس از استقرار با RLSِ فعال سالم.
  برگشتِ کد: `rollback_code_20260913_104952.tgz`؛ پشتیبانِ پیش از استقرار:
  `hesabdari_20260913_104953.dump`.

  **پس از استقرار:** `/api/warehouse-issue-returns` و `/basis` بدونِ توکن ۴۰۱ (نه ۴۰۴)؛
  باندلِ سرو‌شده (`index-wL8BpCSS.js`) همان باندلِ ساخته‌شده؛ شمارنده‌ی
  `warehouse_issue_return` برای هر ۷ مستأجر؛ تنها برگشتِ فروشِ موجود `inline` ماند.
  وبِ درِ تریال (`/opt/cubita-demo/web`) با `rsync` از تولید تازه شد (پشتیبان
  `web.bak-20260913_105129`) — نه با `deploy.sh demo`.

- **۱۴۰۵/۰۶/۲۱ (2026-09-12) — فاکتور برگشتی خودش کالا را به انبار برمی‌گرداند
  (مهاجرت `0134`):** فصلِ «برگشت خروج انبار».

  **شکاف‌سنجی پیش از ساخت.** سندِ «برگشت خروج انبار» وجود نداشت. `post_sales_return`
  هم سندِ تجاری بود و هم برگشتِ فیزیکی: حرکتِ مثبتِ `sales_return` و «موجودی / بهای
  تمام‌شده» را خودش می‌زد. ساختنِ سندِ برگشتِ خروج کنارِ همین رفتار یعنی موجودی دو
  بار زیاد شود. کالای مصرفیِ برگشتی فقط با «تعدیل موجودی» برمی‌گشت — که هزینه‌ی مصرف
  را برنمی‌گرداند و «مغایرت انبار» را بستانکار می‌کرد.

  **دو باگِ زنده، هر دو با تستِ موقت پیش از ساخت ثابت شد:**
  * **موجودیِ شبح.** ۲۰ خرید، ۱۰ فروشِ خودکار، ابطالِ خروجِ فاکتور (فاکتور معتبر ماند)،
    برگشتِ ۵ → موجودی **۲۵** از ۲۰ عددِ خریده‌شده، و ۵٬۰۰۰ ریال COGS که از قبل برگشته
    بود دوباره برگشت. برگشت کالا را به انبارِ سربرگِ فاکتور می‌داد و نمی‌پرسید خروجی
    هنوز معتبر است یا نه.
  * **معینِ اشتباه در برگشتِ دومرحله‌ای.** کالا به انبارِ دارای معینِ موجودیِ خودش
    برمی‌گشت ولی سند `inventory_account_id(invoice.warehouse_id=None)` یعنی حسابِ
    پیش‌فرض را بدهکار می‌کرد.

  **چه ساخته شد:**
  * **سندِ `WarehouseIssueReturn`** با سه نوع (فروش، مصرف، سایر — بدونِ انتقال)، انبار،
    **تحویل‌دهنده** (نه تحویل‌گیرنده)، شماره‌ی مستقل (`warehouse_issue_return`).
    **هر ردیف اجباراً به ردیفِ خروجی که برمی‌گرداند گره می‌خورد**؛ در فروش به ردیفِ
    فاکتور برگشتی هم. باقیمانده‌ها مشتق‌اند: خروج − برگشت‌های معتبر، و فاکتور برگشتی −
    برگشت‌های انبارِ معتبر. قفل روی سربرگ و ردیف‌های خروج و ردیف‌های فاکتور برگشتی.
  * **بها و حساب از خروجِ مبدأ:** بها = بهای همان ردیفِ خروج (قاعده‌ی موجودِ کوبیتا
    برای برگشت، نه اختراعِ تازه). سند: معینِ انبارِ مقصد بدهکار / **همان حسابی که
    خروج بدهکار کرده بود** بستانکار، با مرکزِ هزینه‌ی خروج. برگشتِ مصرف هزینه را
    برمی‌گرداند، نه درآمد.
  * **مالکیت:** `sales_returns.stock_mode`. در سیاستِ «خودکار» فاکتور برگشتی فقط سندِ
    تجاری‌اش را می‌زند و برگشتِ خروج (`origin=sales_return`) را خودش می‌سازد — برای هر
    انبارِ خروج یکی. در «دومرحله‌ای» فقط تجاری است و برگشتِ فیزیکی بعداً، جزئی و
    چندباره. برگشت‌های پیش از `0134` و **فاکتورهای پیش از `0125`** (خروج ندارند؛ آن
    مهاجرت backfill نداشت) مسیرِ قدیمیِ `inline` را می‌روند. سقفِ برگشتِ تجاریِ هر ردیف
    = آنچه خروج‌های معتبر بیرون فرستاده‌اند (همین باگِ اول را می‌بندد).
  * **خروجِ فروشِ بی‌فاکتور** مستقیم برمی‌گردد و فاکتورِ بعدی مقدارِ **خالص** را می‌گیرد
    (زمینه‌ی فاکتور، گاردِ مقدار و `total_cost`). خروجِ فاکتورشده فقط از مسیرِ فاکتور
    برگشتی برمی‌گردد.
  * **ابطال:** حرکتِ جبرانی، سندِ معکوس، بازمحاسبه‌ی میانگین (`_voided_sources` هم
    می‌شناسدش). ابطالِ خروجی که برگشتِ معتبر دارد ۴۰۹؛ ابطالِ فاکتور برگشتی برگشتِ
    خودکارش را هم باطل می‌کند ولی اگر کاربر دستی برگشتِ انبار زده ۴۰۹؛ برگشتِ خودکار
    مستقیم باطل نمی‌شود؛ برگشتِ مستقیمی که خروجش بعداً فاکتور شده ۴۰۹.
  * **چاپ** «برگشت خروج انبار» (استاندارد/A5) با **فی و مبلغِ بها** — `render_issue_permit`
    ستونِ مبلغِ اختیاری گرفت؛ مجوزِ خروج همچنان بی‌مبلغ است.
  * **رابط:** تبِ «برگشت خروج انبار» در ماژولِ انبار (فرمِ «مبنا» با مقدار/برگشت‌خورده/
    باقیمانده، دفتر با فیلترِ نوع، جزئیات با خروجِ مبدأ و معین). «فاکتورهای برگشتی» ستونِ
    «برگشت به انبار» (برنگشته/بخشی/کامل) و میان‌برِ «ثبت برگشت به انبار» گرفت.

  **عمداً ساخته نشد:** ردیابی/سریال/بچ (خروج هویتی ثبت نمی‌کند که برگردد)، توزین (در
  کوبیتا نیست)، وضعیتِ «در انتظارِ قیمت‌گذاری»، صدور/حذفِ سندِ حسابداری جدا از ثبت،
  ویرایشِ برگشتِ ثبت‌شده (ابطال و ثبتِ دوباره)، انتخابِ واحدِ فرعی در فرم.

  **راستی‌آزمایی:** ۲۹ تستِ تازه (`test_issue_return_chapter.py`)، سه تستِ
  `test_return_warehouse.py` که رفتارِ قدیمی (حرکتِ `sales_return`) را قفل کرده بودند به
  مالکیتِ تازه بازنویسی شدند؛ کلِ سوییت ۲۷۰۲ سبز. `0133 → 0134 → 0133 → 0134` روی
  پایگاه دادهٔ توسعه: شمارنده برای هر ۷ مستأجر، برگشتِ موجود `inline`، FORCE RLS روی دو
  جدولِ تازه. مرورگر در ۱۴۴۰ و ۳۹۰: سرریز/جدولِ بی‌کارت/سلولِ بی‌برچسب/خطای کنسول صفر.

- **۱۴۰۵/۰۶/۲۱ (2026-09-12) — خروجِ انبار فقط زیرِ فاکتورِ فروش ساخته می‌شد
  (مهاجرت `0133`):** فصلِ «خروج انبار».

  **شکاف‌سنجی پیش از ساخت.** `WarehouseIssue` از فصلِ جداسازیِ فاکتور هست و تنها
  مالکِ کسرِ موجودی و COGSِ فروش است — فاکتور خودش دفترِ انبار نمی‌نویسد، پس «دو بار
  کسرِ موجودی» امروز رخ نمی‌داد. ولی `sales_invoice_id` و `sales_invoice_line_id`
  اجباری بودند: مصرف و «سایر» جز تعدیلِ دستی راهی نداشتند، فروشی که تحویلش جلوتر از
  فاکتور است ثبت‌شدنی نبود، و مسیرِ «خروج ← فاکتور» وجود نداشت.

  **یک موتور، سه نوع.** خروجِ مستقیم و خروجِ فاکتوری از یک هسته (`_post_issue`)
  می‌گذرند: قفلِ کالا پیش از خواندن، سنجشِ جمعیِ ردیف‌های یک کالا، حرکتِ منفی، سندِ
  بها با معینِ همان انبار. تنها چیزی که با نوع عوض می‌شود طرفِ بدهکار است: فروش ←
  COGS؛ مصرف و سایر ← معینی که کاربر روی سربرگ یا ردیف انتخاب می‌کند. نقشِ حسابداریِ
  تازه ساخته نشد؛ حسابِ گروه، غیرفعال، موجودی، و حساب‌هایی که موتورِ دیگری مانده‌شان
  را می‌سازد (طرف‌حساب، نقد و بانک، چک، مالیات، درآمد، پایانِ سال) رد می‌شوند. همراهش:
  تحویل‌گیرنده (اجباری در فروش)، مرجعِ پیش‌فاکتور، مرکزِ هزینه، مقدارِ فرعیِ نمایشی از
  همان `units.to_primary`، شماره‌ی ردیف و بهای چهاررقمی.

  **خروج ← فاکتور فروش.** «صدور فاکتور فروش» از ردیفِ خروج فقط زمینه می‌برد
  (`/invoice-context` + پیش‌پرکردنِ همان فرمِ فاکتور). ثبت با
  `source_warehouse_issue_id` هیچ حرکتِ انباری نمی‌سازد — حتی در سیاستِ «خودکار» — و
  ردیف‌ها را به همان خروج وصل می‌کند. کالا و مقدار باید همان خروج بماند؛ کالای دیگر،
  خروجِ «مصرف» و فاکتورِ دوم برای یک خروج رد می‌شوند (گاردِ دوم روی قفلِ ردیف است، نه
  فقط کلیدِ تکرار). ابطالِ چنین فاکتوری خروج را **جدا** می‌کند، نه باطل: کالا واقعاً
  رفته است. برای همین ستونِ `origin` (`invoice` / `direct`) لازم شد.

  **انتقال از موتورِ خودش.** نوعِ «انتقال بین انبار» در فرمِ خروج به
  `post_stock_transfer` واگذار می‌شود و فهرستِ خروج‌ها انتقال را از جدولِ خودش
  می‌خواند (keyset روی `UNION ALL`). همان‌جا پنج شکافِ انتقال بسته شد: قفل نداشت (دو
  انتقالِ هم‌زمان موجودی را منفی می‌کردند)، ردیف‌های یک کالا را جمع نمی‌زد، دوره‌ی
  بسته و انبارهای مجازِ کالا را نمی‌سنجید، کلیدِ تکرار و ابطال نداشت، و میانِ دو
  انبار با دو معینِ موجودیِ متفاوت سند نمی‌زد. ابطال هر دو حرکت را برمی‌گرداند و اگر
  کالا از مقصد رفته باشد ۴۰۹ است. تبِ «انتقال» زیرِ ویزارد همان دفترِ خروج‌ها را با
  نوعِ انتقال نشان می‌دهد؛ `TransfersList` حذف شد تا از یک داده دو نما نماند.

  **و سه نقصِ دیگر:** برگشتِ فروش COGS را با میانگینِ روزِ *فاکتور* برمی‌گرداند در
  حالی که خروج با میانگینِ روزِ *خروج* سند زده بود — در سیاستِ دومرحله‌ای برگشتِ کامل
  COGS را صفر نمی‌کرد (نمونه‌ی تست: ۵٬۰۰۰ به‌جای ۱۲٬۵۰۰)؛ ابطالِ خروجی که فاکتورش
  برگشتِ فعال دارد کالا را دو بار به انبار برمی‌گرداند (حالا ۴۰۹)؛ و تعدیلِ دستی قفلِ
  کالا نداشت.

  **چاپ.** «مجوز خروج انبار» (`render_issue_permit`) بی‌مبلغ، با مقدار و واحدِ اصلی
  و فرعی و جمع، در دو قالبِ استاندارد و A5 روی همان سند؛ انتقال با انبارِ مقصد.

  **عمداً ساخته نشد:** برگشتِ خروج (فصلِ خودش)؛ خروجِ سریال/بچ‌محور (`StockBatch`
  موتورِ مصرف نیست)؛ توزین (حوزه‌اش در کوبیتا نیست)؛ وضعیتِ «در انتظارِ قیمت‌گذاری»
  (کوبیتا گردشِ قیمت‌گذاری ندارد و بها همیشه فوری است)؛ انتخابِ واحدِ فرعی در فرم
  (کشِ کالا شناسه‌ی واحد ندارد — مقدارِ فرعی از سرور می‌آید و در جزئیات و چاپ دیده
  می‌شود)؛ «کد تحویل‌گیرنده» روی چاپ (طرف‌حساب در کوبیتا کد ندارد؛ شناسه‌ی ملی و
  کدِ اقتصادی می‌آید).

  **راستی‌آزمایی:** ۳۸ تستِ تازه · کلِ سوییت ۲۶۷۲ سبز · `tsc` و ممیزِ صفحه‌ها بی‌خطا ·
  backfillِ `0133` روی داده‌ی واقعیِ محلی با `downgrade → upgrade` سنجیده شد
  (تحویل‌گیرنده = مشتریِ فاکتور، ردیف‌های ۱ و ۲، معین = COGS، FORCE RLS برقرار) ·
  سنجشِ مرورگری در ۱۴۴۰ و ۳۹۰ روی تبِ خروج، فرم با هر چهار نوع و جدولِ موجودیِ
  پیش/پس، جزئیاتِ ردیف، تبِ انتقال و فاکتورِ پیش‌پرشده از خروج: سرریز ۰، جدولِ
  بی‌کارت ۰، سلولِ بی‌برچسب ۰، خطای کنسول ۰. اجرای اولِ سنجش یک مرحله را FAIL گزارش
  کرد که خطای خودِ اسکریپت بود (متنِ label گزینه‌های select را هم دارد)؛ اسکرین‌شات
  فرم را نشان می‌داد، اسکریپت اصلاح و دوباره اجرا شد.

  **شماره‌ی مهاجرت، بارِ سوم:** پیش از این فصل `0128_payslip_lines` روی master نشست و
  چهار مهاجرتِ شاخه‌ی رسید به `0129`–`0132` رفتند (merge `620be90`). `0133` روی
  شاخه‌ای ادعا شده که خودش هنوز merge نشده — همان ریسکی که سه بار گران تمام شد.

- **۱۴۰۵/۰۶/۲۱ (2026-09-12) — رسیدِ انبار و برگشتش هیچ راهی به رابط نداشتند؛
  چاپ و میان‌برِ پرداخت هم ساخته شد (بدونِ مهاجرت):** بستنِ دو فصلِ «رسید انبار»
  (§۳۸–§۴۲) و «برگشت رسید انبار».

  **رابطِ کاربری.** چهار گامِ قبلی فقط بک‌اند بودند: رسید فقط از دلِ ردیفِ فاکتور
  ساخته می‌شد و رسیدِ مستقیم، حمل، برگشتِ رسید و فهرستِ رسیدها هیچ راهی به صفحه
  نداشتند. حالا ماژولِ خرید تبِ «رسید انبار» دارد
  (`desktop/src/components/WarehouseReceiptsTab.tsx`): فرمِ رسیدِ مستقیم با پنجره‌ی
  حمل (مبلغ، مالیات، عوارض، حمل‌کننده، مبنای تسهیم)، دفترِ رسیدها با فیلترِ
  سمتِ سرور و ستون‌های کالا/حمل/مالیات/خالص و «سند» جدا از «وضعیت» (§۴۴)، ردیفِ
  جزئیات با «فی» و «فی تمام‌شده» کنارِ هم، و پنلِ برگشت با «باقیمانده»ی مشتق و
  «فیِ توافقی». رابط هیچ عددِ مالی‌ای حساب نمی‌کند — سهمِ حمل، فیِ تمام‌شده و
  خالص از سرور می‌آیند (§۲۳ §۲۷). فهرستِ برگشت از خرید ستون‌های «مبدأ»، «نوع»،
  «خالص» و «خالص توافقی» گرفت.

  **چاپ (§۴۱ §۴۲).** `render_warehouse_document` برگه‌ی رسید و برگشتِ رسید را از
  همان سند می‌سازد، نه از مدلِ مالیِ دوم: انبار، تحویل‌دهنده/تحویل‌گیرنده، ستون‌های
  حمل و «مالیات و عوارض»، و برای برگشت «خالص توافقی» کنارِ «خالص». چاپِ برگشتی که
  به رسیدِ مستقیم لنگر زده پیش از این فرض می‌کرد فاکتوری هست.

  **میان‌برِ اعلامیه پرداخت (§۳۸–§۴۰).** `GET /api/warehouse-receipts/{id}/payment-context`
  فقط زمینه منتقل می‌کند؛ اعلامیه همان سندِ مستقلِ خزانه می‌ماند و «رسید انبار»
  به‌عنوان سندِ مرتبط پذیرفته شد. **مبلغِ پیشنهادی عمداً «خالصِ رسید» نیست:** حمل
  بدهیِ حمل‌کننده است (و بی‌حمل‌کننده نقد پرداخت شده)؛ پیشنهادِ خالص یعنی کرایه
  به تأمین‌کننده هم پرداخت شود. «جمع مبلغ رسید انبار» فقط برای دیدن زیرِ فرم
  می‌آید. رسیدِ نقدی (بی‌تحویل‌دهنده) و رسیدِ باطل زمینه‌ی پرداخت ندارند.

  **ثبتِ رسیدِ مستقیم idempotent شد** — تکرارِ شبکه‌ای رسیدِ دوم، ورودِ دوباره‌ی
  کالا و سندِ دوم نمی‌سازد.

  **راستی‌آزمایی:** ۱۲ تستِ تازه · کلِ سوییت ۲۵۵۷ سبز · `tsc`، `oxlint` و ممیزِ
  صفحه‌ها بی‌خطا · سنجشِ مرورگری در ۱۴۴۰ و ۳۹۰ روی تب، ردیفِ جزئیات، پنلِ برگشت و
  صفحه‌ی اعلامیه‌ی پیش‌پرشده: سرریز ۰، جدولِ بی‌کارت ۰، سلولِ بی‌برچسب ۰، خطای
  کنسول ۰. `scripts/verify-pages.mjs` هدفِ «رسید انبار» را هم گرفت.

  **دو دامِ این کار برای هر که بعد می‌آید:** `RECEIPT_TYPE_LABELS` در `api.ts` مالِ
  «رسید دریافت»ِ خزانه است، نه رسیدِ انبار — برچسب‌های انبار
  `WAREHOUSE_RECEIPT_TYPE_LABELS` است و `tsc` تداخلشان را نمی‌گیرد. و اعلامیه
  پرداخت «شرح» و «شرح دوم» را اجباری می‌خواهد.

- **۱۴۰۵/۰۶/۲۱ (2026-09-12) — برگشت، کالا را در انبارِ اشتباه دنبال می‌کرد
  (مهاجرت `0132`):** فصلِ «برگشت رسید انبار».

  **نقصِ زنده‌ای که ثابت شد:** از مهاجرتِ `0130` فاکتورِ خرید `warehouse_id`
  ندارد و کالا با رسید وارد می‌شود. ولی `post_purchase_return` موجودی را هنوز
  در `invoice.warehouse_id` می‌جست — خریدِ ۱۰۰تایی که رسیدش به انبار خورده
  بود، موقعِ برگشت می‌گفت «موجودی کافی نیست (موجود: ۰)». یعنی روی گردشِ رسیدِ
  انبار، برگشت از خرید **اصلاً کار نمی‌کرد**.

  **لنگرِ دوم، نه جدولِ دوم.** فصل می‌پرسد آیا `WarehouseReceiptReturn` بسازیم و
  خودش محدودش می‌کند («نباید کورکورانه Entity دوم ساخته شود»؛ «Keep commercial
  and physical return distinguishable even if Cubita intentionally combines
  them in one workflow»). `PurchaseReturn` از قبل هر دو نیمه را انجام می‌داد،
  پس جدولِ دوم یعنی دو سند یک بدهی را برگردانند. حالا همان سند می‌تواند به
  **ردیفِ رسید** لنگر بزند:

      PurchaseInvoiceLine   ──┐
                              ├──→  PurchaseReturnLine
      WarehouseReceiptLine  ──┘

  همراهش: «تحویل‌گیرنده» جدا از «تحویل‌دهنده» · چهار نوعِ برگشت (بدونِ «موجودی
  اول دوره»، چون فرمِ برگشت ندارَدش) · پنجره‌ی «مبنا» با باقیمانده‌ی **مشتق** ·
  برگشتِ جزئیِ چندباره · دو سقفِ جدا (ظرفیتِ مبدأ و موجودیِ واقعی) · خروج از
  همان بارِ ورودی (`StockBatch` تا امروز اصلاً لمس نمی‌شد) · بهای **تمام‌شده**
  از انبار خارج می‌شود نه فی، پس سهمِ حمل روی موجودیِ صفر جا نمی‌ماند · طرفِ
  مقابل از سندِ مبدأ می‌آید نه از قالبِ ثابت · گاردِ ابطالِ رسیدی که برگشت
  خورده.

  **و دو مبلغی که یکی نیستند:** «ارزشِ دفتری» و «مبلغ مرجوعی توافقی» هر دو
  ذخیره می‌شوند. فصل می‌گوید حسابِ اختلاف را اختراع نکنیم، پس نکردیم: اگر
  برابر نباشند ثبت **رد** می‌شود با پیامِ روشن. تصمیمش در `OPEN_DECISIONS.md`.

  **۲۱ تستِ تازه · کلِ سوییت ۲۵۴۵ سبز.** رابطِ کاربری هنوز ساخته نشده.

- **۱۴۰۵/۰۶/۲۱ (2026-09-12) — فصلِ «رسید انبار»، سه گام (مهاجرت‌های `0129`
  `0130` `0131`):**

  > **چرا ۰۱۲۹ و نه ۰۱۲۳:** این سه مهاجرت اول `0123`–`0125` بودند و شماره‌ها
  > روی تخته‌ی ادعا برداشته شده بود — ولی آن ادعا فقط روی شاخه‌ی merge‌نشده‌ی
  > خودم بود، پس همکار هرگز ندیدش و همان سه شماره را برداشت. شاخه‌ی او تمام
  > شده بود (ادعاهایش آزاد)، پس **من** عقب رفتم، نه او. درس: ادعای شماره‌ی
  > مهاجرت باید روی `master` بنشیند، نه روی شاخه.
  >
  > **بارِ دوم به `0128`:** همکار `0126` و `0127` را **روی master** ادعا و
  > merge کرده بود — دقیقاً به روشِ درست. پس دوباره من عقب رفتم.
  >
  > **و بارِ سوم به `0129`:** این شاخه هنوز merge نشده بود که `0128` (شرحِ
  > فیشِ حقوق) روی master نشست. هر سه بار علت یکی است: شاخه‌ای که merge نشده
  > شماره‌ای را نگه نمی‌دارد، هرچه هم روی تخته‌ی ادعایش نوشته باشد.

  **۱) رسید بدونِ فاکتور ممکن نبود** (`0129`). `purchase_invoice_id` اجباری
  بود، یعنی خریدی که فاکتورش بعداً می‌آید — یا اصلاً نمی‌آید — هیچ راهی برای
  ورودِ کالا نداشت. پیش از ساخت، سیاستِ ثبت سنجیده شد چون فصل هشدار می‌دهد
  فاکتور و رسید نباید یک بدهی را دو بار ثبت کنند: بدهی را فقط فاکتور می‌شناسد
  و آن دست نخورد. ولی رسیدِ **مستقیم** فاکتوری ندارد که بدهی را شناخته باشد،
  پس خودش منشأِ مالی شد. همراهش: پنج نوعِ رسید، تفکیکِ تحویل‌دهنده/حمل‌کننده/
  واسطِ حمل، ارز و نرخ، و فهرستِ مستقلِ رسیدها.

  **۲) کالایی که نرسیده بود، موجودیِ انبار شمرده می‌شد** (`0130`). فاکتورِ
  بی‌انبار همان لحظه «موجودی کالا» را بدهکار می‌کرد: دفتر ۵۰٬۰۰۰٬۰۰۰ و گزارشِ
  انبار صفر. حالا حسابِ واسطِ «کالای در راه» (نقشِ `goods_in_transit`، کد
  پیش‌فرض ۱۱۱۴) بینشان می‌نشیند و رسید کالا را به انبار منتقل می‌کند —
  طبقه‌بندیِ دوباره، نه شناساییِ تازه، پس بدهی دست نمی‌خورد.
  `purchase_invoices.goods_in_transit` سیاستِ ثبتِ همان سند را قفل می‌کند تا
  فاکتورهای موجود موجودیِ دفتری را دو برابر نکنند. ابطالِ رسید هم حالا سند را
  برمی‌گرداند.

  **۳) هزینه‌ی حمل جایی برای نشستن نداشت** (`0131`). کرایه یا ثبت نمی‌شد یا
  هزینه‌ی دوره می‌شد؛ هر دو یعنی موجودی ارزان‌تر از واقع ارزش‌گذاری شود و
  سودِ فروشِ بعدی بیش از واقع. حالا رسید بلوکِ حمل دارد (مبلغ، مالیات، عوارض،
  مبنای تسهیم) و `services/freight.py` تسهیم را انجام می‌دهد — سرویسِ دامنه
  نه کدِ فرم. «فی» و «فی تمام‌شده» از هم جدا ماندند، و کالا/حمل/مالیات/عوارض
  مستقل و قابلِ ردیابی: مبلغ و عوارض به بهای ورود می‌روند، مالیات به اعتبارِ
  مالیاتی. خالصِ رسید ذخیره نمی‌شود، از اجزایش مشتق است.

  **و یک واگرایی که فقط راستی‌آزماییِ زنده پیدایش کرد:** بهای تمام‌شده نتیجه‌ی
  یک تقسیم است، ولی `items.average_cost` و `stock_ledger.unit_cost` ریالِ
  صحیح بودند. روی نمونه‌ی واقعی دفتر ۱٬۴۵۰٬۰۰۰ می‌گفت و گزارشِ انبار
  ۱٬۴۵۰٬۰۵۰. نقص از پیش بود (هر فاکتورِ خریدی که خالصش بر تعداد بخش‌پذیر نباشد
  همین را می‌ساخت) ولی تسهیمِ حمل عادی‌اش می‌کند، پس ستون‌های بهای تمام‌شده به
  `Numeric(18, 4)` رفتند و گزارشِ موجودی به ریالِ صحیح گِرد می‌شود.

  **۵۷ تستِ تازه · کلِ سوییت ۲۴۹۸ سبز.** رابطِ کاربریِ این فصل هنوز ساخته
  نشده — فقط بک‌اند.

- **۱۴۰۵/۰۶/۲۱ (2026-09-12) — استقرار روی prod: نسخه ۰۱۲۵ ← ۰۱۲۸، و سه شکستِ
  استقرار که همه از *شکلِ بسته* آمدند:** سه مهاجرت با هم رفتند (`0126` سیاستِ
  صدورِ فاکتور، `0127` اعلامیه‌ی دوسویه، `0128` تفکیکِ فیش). **درستیِ داده روی
  خودِ prod سنجیده شد**:
  - **هر ۱۵ فاکتورِ زنده مقابلِ دفتر گذاشته شد.** ۱۲ فاکتور دقیقاً می‌خوانند
    (اختلافِ صفر). ۳ فاکتورِ دیگر گردشِ دریافتنی ندارند و بررسی نشان داد هر سه
    **نقدی**اند و سندشان «صندوق» را بدهکار می‌کند. یعنی صفربودنِ دریافتنی درست
    است — و کدِ تازه همین را می‌گوید، درحالی‌که کدِ قدیمی فروشِ نقدی را بدهیِ
    مشتری نشان می‌داد. **یک اشتباهِ دومِ بی‌سروصدا که همین استقرار بست.**
  - باندلِ سرو‌شده همان فایلی است که ساخته شد (`index-CItFMUX_.js`) · دو جدولِ
    تازه `FORCE ROW LEVEL SECURITY` و دقیقاً یک سیاست دارند · `alembic current`
    روی `0128 (head)` · صفر خطای سرویس در بیست دقیقه‌ی اول.

  **و سه شکستِ استقرار، هر سه از شکلِ بسته نه محتوایش، و هر سه فقط بعد از رسیدن
  به سرور دیده شدند:**
  1. `git archive HEAD backend` پیشوندِ `backend/` می‌گذاشت ولی `deploy.sh`
     خطِ ۱۲۸ `app alembic alembic.ini scripts` را از **ریشه** می‌خواهد.
  2. اسکریپت‌های پوسته در گیت `100644` بودند، پس `tar` بی‌بیتِ اجرا بازشان
     می‌کرد. **این یکی *بعد* از اجرای موفقِ هر سه مهاجرت شکست** — برگشت کد را
     برگرداند ولی دیتابیس روی `0128` ماند. بی‌خطر بود چون مهاجرت‌ها افزودنی
     بودند، ولی شانس بود نه طراحی. و دلیلش این بود که گامِ صفر
     `$APP_DIR/backup.sh` را می‌سنجید درحالی‌که خطِ ۱۹۹ مسیرِ **دیگری** را
     hard-code کرده بود — بررسیِ پیش‌پروازی که مسیرِ اجرانشده را تأیید می‌کرد.
  3. `git archive` روی ویندوز پایان‌خط‌ها را به **CRLF** تبدیل می‌کند هرچند blob
     در مخزن LF است: آرشیوِ **زیردرخت** `.gitattributes`ِ ریشه را نمی‌بیند، پس
     `eol=lf` اعمال نمی‌شود. نتیجه: `/usr/bin/env: 'bash\r': No such file`.

  **درسِ ثبت‌شده:** `backend/scripts/pack.sh` حالا بسته را می‌سازد **و
  می‌آزماید** — همان استخراجی که سرور می‌زند، LF و نحوِ هر اسکریپت، وجودِ
  `scripts/backup.sh` در آرشیو، و نبودِ فایلِ راز. هر شکستی که آن‌جا دیده شود،
  شکستی است که پیش از توقفِ سرویس دیده شده. دو فراخوانیِ پشتیبان هم با
  `bash <file>` انجام می‌شوند، چون مودِ فایل از مخزن تا آرشیو تا دیسک سه حلقه
  دارد و هر کدام می‌تواند خرابش کند.

- **۱۴۰۵/۰۶/۲۱ (2026-09-12) — فیشِ حقوقی عددش درست بود ولی نمی‌گفت از چه ساخته
  شده (مهاجرت `0128`):** شکاف‌سنجیِ فصلِ «آشنایی با حقوق و دستمزد» نتیجه‌ای برعکسِ
  سه فصلِ قبل داد: **ماژول از قبل تقریباً همان چیزی است که فصل می‌خواهد** — هویتِ
  شخص از استخدام جدا (`Contact.employee_id`، «دو پیوند نه دو کپی»)، تنظیمات
  سال‌به‌سال و محاسبه‌ای که `period.year` را می‌خواند نه امسال را، ماندهٔ مرخصی و
  وام **مشتق**، و سه خروجیِ قانونی که همه از خودِ فیش می‌خوانند نه محاسبه‌ی دوباره.
  سه چیز کم بود:
  - **تفکیکِ فیش.** `allowances_total` یک عدد بود و هر عاملی که `housing`/`food`
    نبود اول در `other_allowance` جمع می‌شد و بعد در آن یک عدد. پس «این مزایا از
    چه ساخته شد؟» جواب نداشت — و بازخواندنش از قرارداد هم جواب نمی‌دهد چون
    قرارداد می‌تواند عوض شده باشد. جدولِ `payslip_lines` همان تفکیک را در لحظه‌ی
    صدور ذخیره می‌کند، با **نامِ عاملِ همان لحظه** و منشأ (حکم/کارکرد/تنظیمات/وام).
    گاردِ `assert_breakdown_matches` جمعِ ردیف‌ها را مقابلِ خالص می‌گذارد و اگر
    نخواند صدور را متوقف می‌کند — چون تفکیک و محاسبه دو کدند و دو کد جدا می‌افتند.
  - **حسابرسیِ تنظیماتِ حقوق.** تا امروز فقط `Payslip` حسابرسی می‌شد.
    `PayrollSettings` (یعنی پلکانِ مالیات)، `PayrollFactor`، `PayrollTaxGroup` و
    `SalaryContract` اضافه شدند — عوض‌کردنِ یک پله، مالیاتِ *همه‌ی* کارکنان را
    عوض می‌کند. ۱۷ → ۲۱ مدلِ حسابرسی‌شده.
  - **یکتاسازیِ فرمانِ صدور.** داده از قبل هم خراب نمی‌شد (قیدِ یکتای
    `(employee, period)` و گاردِ «دوره نهایی شده») ولی اجرای دوم **۴۰۰** می‌گرفت و
    کاربر نمی‌فهمید حقوق صادر شده یا نه. حالا همان فهرستِ اول را پس می‌دهد.
  - **و یک باگ در کدِ خودِ همین کار که تست گرفتش:** عاملِ «حقوق پایه» دسته‌اش
    `benefit` است (همه‌ی عوامل پیش‌فرض همین‌اند) پس در ردیف‌های مزایا هم می‌آمد و
    پایه دو بار شمرده می‌شد؛ جمع درست می‌ماند چون «سایر مزایا» منفی می‌شد.
  - **پنج چیز عمداً ساخته نشد** چون منبع ندارند و فصل هم «حدس نزن» گفته: قالبِ
    سال‌محورِ فایلِ مالیات/بیمه، «فرایند پیشرفته»، موتورِ فرمولِ عوامل، فرمولِ
    تسویه، و تخصیصِ هزینه از شغل/محلِ خدمت.
  - **۲۵۴۴ تست سبز** (۱۱ تازه).

- **۱۴۰۵/۰۶/۲۱ (2026-09-12) — مرور فروش: شش نما به‌جای یک تب، و دو باگ
  (بدونِ مهاجرت):**
  - **باگِ زنده که اول بسته شد:** مهاجرتِ `0125` ستونِ `warehouse_id` فاکتور
    فروش را nullable کرد و سیاستِ «دومرحله‌ای» فاکتورِ بی‌انبار را دست‌یافتنی
    کرد؛ ولی `post_sales_return` همان ستون را در `stock_ledger.warehouse_id`
    می‌نوشت که `NOT NULL` است. نتیجه: **خطای ۵۰۰ مدیریت‌نشده** روی برگشتِ چنین
    فاکتوری (با پروبِ واقعی سنجیده شد). حالا کالای برگشتی به همان انباری
    برمی‌گردد که از آن خارج شده — از `WarehouseIssueLine` — و اگر چیزی خارج
    نشده باشد با پیامِ روشن رد می‌شود. **مسیرِ فاکتورهای انبار‌دار دست‌نخورده.**
  - **سرویسِ `sales_review.py`** با چهار جریانِ واقعیتِ جدا که هر کدام **پیش از**
    اتصال تجمیع می‌شوند. این تنها راهِ جلوگیری از ضرب‌شدنِ سطرهاست: فاکتورِ
    سه‌ردیفی با پیوستِ خام سه برابر می‌شد، و ردیفی که از دو انبار رفته دو برابر.
  - **شش نما**: کالا، مشتری، اسنادِ فروش، اقلامِ فروش، انبار، پیش‌فاکتور —
    به‌علاوه‌ی **فاکتورهای ابطالی** که در هیچ نمای دیگری شمرده نمی‌شوند ولی
    تاریخشان پاک نمی‌شود.
  - **عددی که تا امروز هیچ‌جا دیده نمی‌شد:** «فروخته و نرفته». از `0125` که
    فاکتور و خروجِ انبار دو سند شدند، این اختلاف وجود داشت و هیچ گزارشی
    نشانش نمی‌داد.
  - «فیِ متوسط» **وزنی و تاریخی** است (مبلغ ÷ مقدار) نه میانگینِ سادهٔ فی‌ها و نه
    قیمتِ اعلامیه‌ی امروز؛ و موجودی از دفترِ موجودی می‌آید نه از «فروش منهای برگشت».
  - **پنج چیز عمداً ساخته نشد** چون کوبیتا منبعشان را ندارد و فصل هم «حدس نزن»
    گفته: موجودیِ قابلِ فروش (`is_sellable` پرچمِ بولی است نه مقدار)، برگشتِ
    فیزیکیِ خروجِ انبار (چنین سندی نیست)، پورسانتِ هر فاکتور (`CommissionRunLine`
    تجمیعیِ فروشنده است)، درخواستِ فروش، و ردیابی به‌عنوان بُعدِ ردیفِ فروش.
  - صفحه‌ی قبلی **یک تب** داشت و کلِ فاکتورها را به مرورگر می‌کشید.
  - **۲۵۲۸ تست سبز** (۲۰ تازه — ۱۳ برای دانه‌بندی و ریاضی، ۷ برای انبارِ برگشت).

- **۱۴۰۵/۰۶/۲۱ (2026-09-12) — مرور جامع طرف حساب، و سه موتورِ مانده که دو‌تایشان
  با دفتر نمی‌خواندند (بدونِ مهاجرت):** فصلِ «مرور جامع» یک گزارش می‌خواست؛ آنچه
  پیدا شد یک باگِ زنده بود.
  - **سه جا مانده‌ی طرف حساب را حساب می‌کردند، با سه فرمول.** از مهاجرتِ `0125`
    که «اضافات»، «عوارض» و «رند» به فاکتور اضافه شدند، `credit.customer_outstanding`
    (بنرِ سقفِ اعتبار) هر پنج جزء را می‌شمرد ولی `reports.contact_balance` و
    `get_contact_statement` فقط `total + tax`. فاکتوری با ۱۰٬۰۰۰ کالا و ۵٬۰۰۰
    اضافات/عوارض در دفتر **۱۵٬۰۰۰** مطالبات می‌ساخت و کارتِ حساب **۱۰٬۰۰۰**
    می‌گفت — با یک پروبِ واقعی سنجیده شد، نه از روی خواندنِ کد.
  - **حالا یک موتور است و آن `open_items` است**، که اثرِ هر سند را از
    `debit - credit`ِ واقعیِ همان سند در دفتر می‌گیرد. پس ستونِ تازه‌ی فردا
    نمی‌تواند گزارش را بی‌صدا از دفتر جدا کند.
  - **سرویسِ تازه‌ی `counterparty.py`** با سه دانه‌بندیِ جدا (نقش‌ها / رویدادها /
    اقلام) و سه اندپوینت جدا — تا رابط مجبور نشود اقلام را جمع بزند و یک مبلغ
    چند بار شمرده شود. مانده **نقش‌محور** است: طلبِ ما و بدهیِ ما دو عددِ جدا
    می‌مانند و «مانده کل» فقط مشتق است، نه تهاتر.
  - **و یک باگِ دوم که همین مسیر بیرون آورد:** رویدادِ **واخواست و استردادِ چک**
    `contact_id` نداشت، پس احیای مطالبات به هیچ طرف حسابی نسبت داده نمی‌شد —
    در نمای اقلامِ باز، چکِ برگشت‌خورده همچنان «پرداختِ مشتری» به نظر می‌رسید و
    می‌شد بابتش فاکتور تسویه کرد.
  - صفحه‌ی «مرور جامع طرف حساب» بازنویسی شد: تا امروز **فقط فاکتور فروش** را
    نشان می‌داد و برای فیلترکردن کلِ فاکتورها را به مرورگر می‌کشید.
  - شرطِ طرف‌حسابِ اعلامیه در `open_items` **سمت‌آگاه** شد: پیش از این تهاترِ
    «تأمین‌کننده → مشتری» سمتِ آن یکی را به پای این یکی می‌نوشت.
  - کلیدهای کارتِ حساب به `source_type`ِ **قانونی** رسیدند (`check_in`/`check_out`
    → `check`، `receipt` → `treasury_receipt`) — همان کلیدهایی که drill-down با
    آن‌ها کار می‌کند. کلیدهای قبلی ساختگی و به هیچ سندی وصل نبودند.
  - **۲۵۰۸ تست سبز.**

- **۱۴۰۵/۰۶/۲۱ (2026-09-12) — اعلامیه‌ی بدهکار/بستانکار دو سمت داشت، ولی فقط
  یکی‌اش را می‌پرسید (مهاجرت `0127`):** فصلِ «اعلامیه بدهکار و بستانکار» شکاف‌سنجی
  شد. نتیجه: هویتِ مشترکِ طرف حساب (`Contact.type`)، حلِ حساب با `system_role`،
  و مشتق‌کردنِ سمت از اثرِ واقعیِ دفتر در `open_items` — همه از قبل درست بودند.
  ولی **خودِ سند** با یک فرضِ نانوشته ساخته شده بود: یک طرف حساب، یک مبلغ، و
  سمتِ دوم همیشه **حسابِ فروش**.
  - **تعدیلِ مانده، درآمد می‌ساخت.** هر اعلامیه‌ی بدهکار فروش را باد می‌کرد و هر
    بستانکار کمش می‌کرد — سندی که هیچ فروشی در آن نبود، شکلِ صورتِ سود و زیان
    را عوض می‌کرد.
  - **سمتِ تأمین‌کننده اصلاً وجود نداشت.** تهاترِ «بدهیِ ما به یکی در برابرِ طلبِ
    ما از دیگری» ناممکن بود، و برای طرف‌حسابی که تأمین‌کننده است سند روی معینِ
    *دریافتنی* می‌نشست.
  - **و در کارتِ حساب دیده نمی‌شد.** `reports.py` صفر بار این سند را نام می‌برد:
    دفتر تکان می‌خورد و صورت‌حساب خبر نداشت. `contact_balance` هم نمی‌دیدش، پس
    اعلامیه‌ی بستانکار سقفِ اعتبارِ مشتری را آزاد نمی‌کرد.
  - حالا هر ردیف یک **جفتِ کامل** است (سمتِ بدهکار و بستانکار، هرکدام با طرف
    حساب و معینِ خودش) و ذاتاً تراز؛ حسابِ هر سمت از **نقشِ** طرف حساب حل می‌شود
    و قفل نیست؛ تفصیلیِ هر سمت روی ردیفِ سند می‌نشیند؛ و سند در کارتِ حسابِ
    **هر دو** طرف دیده می‌شود.
  - **مرزها دست‌نخورده ماندند:** نه خزانه، نه انبار، نه مالیات — و **نه تسویه‌ی
    قلمِ باز**. کم‌شدنِ ۲۰ میلیون از مانده نمی‌گوید کدام فاکتور پرداخت شده؛ آن
    تخصیصِ صریح در `settlements` است.
  - چون در هر شش مستأجرِ تولید **صفر اعلامیه** ثبت شده بود، مهاجرت هیچ backfillی
    ندارد و هیچ ردیفی نمی‌نویسد. `kind`/`contact_id` حذف نشدند، فقط nullable —
    ستونِ مرده بی‌آزار است، حذفِ ستون یک‌طرفه.

- **۱۴۰۵/۰۶/۲۱ (2026-09-12) — «ثبت فاکتور» چه‌قدر کار انجام دهد، انتخابِ خودِ
  کسب‌وکار شد (مهاجرت `0126`):** مهاجرتِ `0125` فاکتور فروش را از خروجِ انبار جدا
  کرد و «ثبت فاکتور» دیگر سند و کسرِ موجودی نمی‌زد. جداسازی درست است و برنگشت —
  ولی برای مغازه‌ای که فاکتور و تحویل یک لحظه‌اند، دو دکمه‌ی اضافه یعنی دو
  فراموشیِ ممکن: فاکتوری که در دفتر نیست، و کالایی که از انبار کم نشده.
  - پرچمِ `tenants.sales_invoice_posting` با همان الگوی «کنترل شماره چک»:
    `immediate` (پیش‌فرض) و `staged`. کارتش در «تنظیمات ← شخصی‌سازی».
  - **پیش‌فرض `immediate` است، نه رفتارِ تازه** — یعنی همان چیزی که کاربران
    سال‌ها داشته‌اند. مسیر در هر دو حالت یکی است؛ فقط خودکاربودنِ دو گام عوض
    می‌شود.
  - **و یک بن‌بست که با این پیش‌فرض ساخته می‌شد و بسته شد:** روتر برای ابطالِ
    فاکتور می‌گفت «اول خروجِ انبار را باطل کنید». در حالتِ خودکار آن خروج را
    *سیستم* ساخته و کاربر سندی نمی‌بیند که آگاهانه باطلش کند — همان شکلِ بن‌بستی
    که فصلِ «فاکتور برگشتی» یکی‌اش را بست. گارد حالا فقط در حالتِ دومرحله‌ای
    اعمال می‌شود؛ `void_sales_invoice` خودش خروج‌های فعال را آبشاری باطل می‌کند.
  - مرزِ سیاست و یکپارچگی سرِ جایش ماند: **در هیچ حالتی نمی‌شود بیش از موجودی
    فروخت** — تستِ پارامتریِ هر دو حالت این را قفل می‌کند.

- **۱۴۰۵/۰۶/۲۱ (2026-09-12) — استقرار روی prod: نسخه ۰۱۲۰ ← ۰۱۲۵.** پنج
  مهاجرت با هم رفتند: فصلِ «فاکتور برگشتی» (`0121`)، «اعلامیه قیمت» (`0122`)، و
  سه فصلِ کدکس — Snapshotِ طرفینِ خرید، پیش‌فاکتورِ منبع، و جداسازیِ فاکتور فروش
  از خروجِ انبار (`0123`–`0125`). **درستیِ داده روی خودِ prod سنجیده شد**، نه فقط
  محلی:
  - **هر ۶ مستأجر شمارنده‌ی `warehouse_issue` گرفتند** — پرخطرترین نقطه‌ی این
    استقرار، چون `next_document_number` نبودنِ ردیف را با ۵۰۰ جواب می‌دهد و
    اولین خروجِ انبارِ هر مستأجر می‌شکست.
  - هیچ قاعده‌ی قیمتی گم نشد و هیچ قاعده‌ی بی‌هدفی ساخته نشد · هیچ فاکتورِ
    فروشی بی‌انبار نماند (با اینکه ستون nullable شد) · هیچ بیش‌برگشتی در داده‌ی
    موجود نیست.
  - **تنها ردیفِ برگشتِ پیش از `0121`** (فاکتور ۲: ۱۰ از ۴۰) درست خوانده می‌شود؛
    `_drain_legacy` مقدارش را از استخرِ همان کالا کم می‌کند، پس ماندهٔ قابلِ
    برگشت بی‌صدا بالا نپرید — دقیقاً همان چیزی که نبودِ backfill می‌توانست بشکند.
  - **هر فاکتورِ مالیات‌دار، جمعِ `tax_amount_snapshot`ِ ردیف‌هایش با سربرگ
    می‌خواند** — گاردِ همان رگرسیونی که در ادغامِ کارِ کدکس نزدیک بود برگردد.
  - هر سه جدولِ تازه `FORCE ROW LEVEL SECURITY` و دقیقاً یک سیاست دارند.
  - `/api/health` سالم، `acc.cubita.ir` ۲۰۰، و **باندلِ سرو‌شده همان فایلی است
    که ساخته شد** (`index-CejmxuXJ.js`).

  حساب‌های ۴۱۰۷/۴۱۹۸/۴۱۹۹ هنوز ساخته نشده‌اند و این درست است: با
  `get_or_create_account` تنبل‌اند و با اولین سندِ مربوطه می‌آیند.

- **۱۴۰۵/۰۶/۲۱ (2026-09-12) — دوازده صفحه‌ی فهرستِ فروش از هیچ عرضی باز
  نمی‌شدند، و اسکریپتی که باید می‌گرفتش «سالم» می‌گفت:** با اجرای واقعیِ
  `verify-pages.mjs` (که تا امروز هرگز اجرا نشده بود) دو چیز بیرون آمد.
  - **کلیدِ غلط در `moduleLists.tsx`.** `LIST_MENUS` و `LIST_PAGE_GROUP` گروهِ
    فروش را با کلیدِ «فروش» می‌شناختند، ولی هر دو مصرف‌کننده — کارتِ «فهرست»ِ
    `ModulePanels` و کشوی موبایلِ `TopNav` — با `group.heading` می‌خوانندشان، که
    «مشتریان و فروش» است. نتیجه: **هر دوازده صفحه‌ی فهرستِ فروش** — از
    «فاکتورهای فروش» تا «اعلامیه‌های قیمت» و «فاکتورهای برگشتی» — نه در ۱۴۴۰ و
    نه در ۳۹۰ از رابط قابلِ باز کردن نبودند. تایپ‌اسکریپت نمی‌دیدش (کلید
    `string` است) و ممیزِ ایستا هم نه، چون ثبتشان در `OPS_LIST_MAP` درست بود.
  - **و دو «سبزِ دروغین» در خودِ `verify-pages.mjs`:** صفحه‌ای که باز نمی‌شد
    `[SKIP]` می‌گرفت و در شمارشِ شکست نمی‌آمد، و در مسیرِ دسکتاپ اگر منو پیدا
    نمی‌شد اسکریپت بی‌کلیک ادامه می‌داد و **صفحه‌ی قبلی** را می‌سنجید. یعنی
    اجرایی که ۳۲ صفحه از ۳۴ را باز نکرده بود «همه‌ی صفحه‌ها سالم» گزارش می‌داد.
    حالا هر دو شکست‌اند، و سه صفحه‌ی تازه‌ی این فصل‌ها هم به فهرستِ هدف اضافه شد.

- **۱۴۰۵/۰۶/۲۱ (2026-09-12) — فرودِ کارِ کدکس روی master، و چهار چیزی که در
  ادغام بی‌صدا می‌شکستند (مهاجرت‌های `0123`–`0125`):** پنج فصلِ کدکس (رسیدِ انبار،
  اعلامیه پرداخت، Snapshotهای فاکتور خرید، پیش‌فاکتورِ منبع، و جداسازیِ فاکتور
  فروش از خروجِ انبار) روی شاخه‌ای مانده بودند که **۱۸ کامیت عقب** بود و هیچ‌وقت
  push نشده بود. کیفیتش خوب بود — روی شاخه‌ی خودش ۲۲۳۸ تست سبز، RLS روی
  جدول‌های تازه، الگوهای خانه رعایت‌شده — ولی کهنگی چهار چیز را با خودش می‌آورد:
  - **سه شناسه‌ی مهاجرتِ تکراری.** `0114`/`0115`/`0116` دقیقاً همان‌هایی بودند که
    master برای تسویه‌ی طرف مقابل، ردیابی چک و انبار گرفته بود. به `0123`–`0125`
    شماره‌گذاری دوباره شدند. `git` هیچ تعارضی نشان نمی‌داد.
  - **ستونِ دوبار ساخته‌شده.** `0125` دوباره `tax_rate_snapshot` و
    `tax_amount_snapshot` را روی `sales_invoice_lines` می‌ساخت، در حالی که `0117`
    از قبل ساخته بودشان — روی دیتابیسِ مستقر با `DuplicateColumn` می‌شکست. نه
    `git` دیدش، نه تست‌هایی که schema را با `create_all` می‌سازند؛ فقط
    `test_migration_drift` که مهاجرت را واقعاً از صفر اجرا می‌کند.
  - **انحرافِ میانگینِ بهای تمام‌شده.** حالا که `WarehouseIssue` مالکِ حرکتِ
    انبارِ فروش است، نبودنش در `voiding._voided_sources` یعنی حرکتِ خروجِ
    باطل‌شده در بازپخش شمرده می‌شود. اثرش در لحظه دیده نمی‌شد و با **ابطالِ هر
    سندِ دیگری** در همان کسب‌وکار ظاهر می‌شد: میانگینِ ۱۵۰۰ به ۲۰۰۰ می‌پرید.
    یک ردیف در آن tuple، به‌علاوه‌ی `test_warehouse_issue_void_cost.py`.
  - **سه گاردِ master که برمی‌گشتند:** مالیاتِ ردیف‌به‌ردیف با `effective_tax_rate`
    (نسخه‌ی کدکس مالیات را روی جمعِ فاکتور می‌گرفت و تسهیم می‌کرد — یعنی قلمِ
    **معاف** دوباره مالیات می‌خورد و با مالیات به مؤدیان اظهار می‌شد)، معینِ
    **همان انبار** در سندِ COGS (§۹ فصلِ انبار)، و گاردِ تفصیلی روی سندِ خروج.
    به‌علاوه `cancel_unposted_sales_invoice` — مسیرِ تازه‌ی لغوِ فاکتورِ سندنشده —
    گاردِ «برگشتِ فعال» را نداشت، پس فاکتوری با برگشتِ زنده لغو می‌شد و سندِ
    برگشت بی‌پدر می‌ماند.

- **۱۴۰۵/۰۶/۲۱ (2026-09-12) — اعلامیه قیمت یک ماتریس بود که هیچ راهی برای
  پرکردنش نبود (مهاجرت `0122`):** فصلِ «اعلامیه قیمت». نیمی از فصل از قبل ساخته
  شده بود (فصلِ «تعریف کالا و خدمت»): `price_list_items` چهار بُعدِ زمینه داشت،
  `pricing.resolve()` قطعی بود، و `assert_within_policy` سمتِ دامنه روی مسیرِ
  تنهای `post_sales_invoice` می‌نشست — پس §۹۶ و §۹۷ از قبل برآورده بودند. ولی:
  - **سه موتورِ قیمت، سه جواب (§۵۴ §۹۲).** فرمِ فاکتور
    ([salesInvoiceDraft.ts](desktop/src/lib/salesInvoiceDraft.ts)) و صندوق
    ([PosPage.tsx](desktop/src/pages/PosPage.tsx)) هرکدام نگاشتِ محلیِ
    `item_id → price` از `contact.default_price_list_id` می‌ساختند — ستونی که
    `resolve()` **اصلاً نگاهش نمی‌کند** — و `effective_from`، `is_active` و هر
    چهار بُعد را نادیده می‌گرفتند. یعنی فرم یک قیمت پر می‌کرد و سرور با قاعده‌ی
    دیگری اعتبارش را می‌سنجید؛ اولین اعلامیه‌ای با حدِ واقعی، قیمتی را رد می‌کرد
    که خودِ فرم پر کرده بود. حالا هر دو از `GET /api/sales-ops/pricing/resolve`
    می‌آیند. **این تغییرِ رفتار روی مسیرِ مستقر است.**
  - **یک مسیر که ماتریس را پاک می‌کرد.** `PUT /api/price-lists/{id}/items` اول
    `pl.items.clear()` می‌زد، و `PriceListsPanel` ردیف‌ها را در
    `Record<item_id, price>` فرو می‌ریخت و با زمینه‌ی **خالی** پس می‌فرستاد. یک
    بار «ذخیره» در تبِ «انبار ← لیست قیمت» هر قیمتِ عمده/خرده/واحد/گروهِ مشتری و
    هر حدِ تغییرِ نرخِ آن اعلامیه را برای همیشه می‌برد — بی‌صدا، بی‌خطا، و
    بی‌ردِ حسابرسی. اندپوینت حالا **upsert بر کلیدِ زمینه** است (حذف مسیرِ صریحِ
    خودش را دارد) و آن تب **خواندنی** شد.
  - **ماتریس از هیچ‌جا قابلِ ورود نبود.** هم صفحه‌ی «اعلامیه قیمت» و هم پنلِ
    انبار فقط `{item_id, price}` می‌فرستادند، پس «عادی ۱۰٬۰۰۰ / خرده ۱۲٬۰۰۰ /
    عمده ۸٬۰۰۰» ساختنی نبود. صفحه حالا گریدِ کامل است: هدف (کالا یا **گروهِ
    فروشِ کالا**)، نوعِ فروش، واحد، گروهِ مشتری، ارز، فی، درصدِ اضافات، امکانِ
    تغییرِ فی، امکانِ تغییرِ تخفیف، و درصدِ کاهش/افزایشِ مجاز.
  - **امکانِ تغییرِ تخفیف پرچمِ جدا شد (§۲۵)** — تا امروز فقط `allow_rate_change`
    بود، و قفل‌کردنِ نرخ ناخواسته تخفیف را هم قفل می‌کرد (یا برعکس).
  - **«تغییر فی»ِ گروهی با یکتاسازی (§۴۲–§۴۸ §۹۴).** شش حالتِ دیالوگِ مرجع، با
    قفلِ ردیف‌ها، ثبتِ قبل/بعد در حسابرسی، و `idempotent(...)` — چون «۲۰٪ اضافه
    کن» جابه‌جاکننده است نه نشاننده و تکرارِ درخواست ۱۰۰ را به ۱۴۴ می‌برد.
    «رقمِ اعشار»ِ دیالوگ به **دقتِ رند** ترجمه شد، چون `price` عددِ صحیحِ ریالی است.
  - **ردِ قیمت روی ردیفِ فاکتور (§۹ §۵۹ §۹۱).** `declared_unit_price` و
    `price_rule_id` — snapshot، به همان دلیلِ `tax_rate_snapshot`. «کاربر قیمت را
    دست‌کاری کرده» از همین دو **مشتق** می‌شود؛ پرچمِ `price_changed` ساخته نشد.
  - **تغییرِ نوعِ فروش می‌پرسد (§۷ §۸ §۹۵)** و بی‌صدا قیمتِ ردیف‌ها را بازنویسی
    نمی‌کند. پرسش در هوکِ مشترک است، پس فرم و ویزارد یک رفتار دارند.
  - **اعلامیه‌ی قیمت وارد `audited_models()` شد (§۴۰ §۸۵)** — تنها تصمیمِ مالیِ
    شرکت که تغییرش هیچ ردی نمی‌گذاشت. و حذفِ اعلامیه‌ی قاعده‌دار به
    **غیرفعال‌کردن** تبدیل شد (§۸۳): فاکتورِ پارسال باید بتواند بگوید نرخش از
    کدام قاعده آمد.
  - **انتخابِ برنده قطعی شد (§۳۵ §۳۷ §۹۲).** `max()` روی هم‌رتبه‌ها ترتیبِ
    دیتابیس را برنده می‌کرد — دو کوئری، دو قیمت. حالا مرتب‌سازیِ کاملاً مرتب،
    و پرچمِ `ambiguous` به کاربر می‌گوید پیکربندی‌اش مبهم است.
  - **عمداً ساخته نشد:** بُعدِ «ردیابی» (§۱۷ §۱۸ — کوبیتا مِسترِ ردیابی ندارد و
    ساختنش همان زیرسیستمِ موازیِ ممنوع بود)، نسخه‌بندیِ بومیِ قاعده (§۸۸ —
    «اعلامیه‌ی تازه کنارِ قبلی» خودش هست)، قیمتِ اختصاصیِ یک مشتری (§۱۶)، تغییرِ
    سیاستِ نبودِ تطابق (§۵۸)، سیاستِ کهنگیِ پیش‌نویس (§۸۷)، ورود/خروجِ اکسل (§۸۱)
    و چاپ (§۸۲)، و گره‌زدنِ «درصدِ اضافات» به اضافاتِ فاکتور (§۲۳ — ذخیره و نمایش
    می‌شود، اعمال نه).

- **۱۴۰۵/۰۶/۲۱ (2026-09-12) — برگشت نه ردیفِ مبدأش را می‌شناخت، نه می‌شد پسش
  گرفت (مهاجرت `0121`):** فصلِ «فاکتور برگشتی». بخشِ بزرگی از فصل از قبل درست
  بود — سندِ مستقل با شماره و سندِ حسابداریِ خودش، ماندهٔ قابلِ برگشتِ **مشتق**،
  ردِ بیش‌برگشت، قیمت از فروشِ اصلی نه قیمتِ امروز، و ثبتِ `sales_return` در
  `open_items` به‌عنوان سندِ تسویه‌پذیر. چهار چیز نبود:
  - **تخصیصِ سطحِ ردیف.** `sales_return_lines` فقط `item_id` داشت، پس فاکتوری با
    دو ردیفِ یک کالا به دو قیمت هنگامِ برگشت **میانگین** می‌گرفت — مشتری مبلغی پس
    می‌گرفت که هرگز نپرداخته بود. حالا `sales_invoice_line_id` دارد (و قرینه‌اش
    در خرید). ورودیِ کالا-محورِ `marketplace` و موبایل نشکست: سرویس خودش آن را
    روی ردیف‌های همان کالا پخش می‌کند، پس هر ردیفِ تازه هویتِ مبدأ می‌گیرد.
  - **ابطال.** `SalesReturn` نه `voided_at` داشت نه حذف — و
    `voiding._guard_no_active_returns` هنگامِ ابطالِ فاکتور می‌گفت «اول سندِ
    برگشت را برگردانید»، کاری که **هیچ راهی برایش نبود**. یعنی یک برگشتِ
    اشتباهی فاکتورش را برای همیشه از ابطال قفل می‌کرد. حالا هر دو سربرگ
    ابطال‌پذیرند و از همان `voiding._apply_void` رد می‌شوند.
  - **حسابِ مخصوصِ برگشت (۴۱۰۷).** سند `SALES_REVENUE` را بدهکار می‌کرد، پس
    فروشِ ناخالص و برگشتی در دفتر از هم جدا نمی‌شدند و «نرخِ برگشت» درنمی‌آمد.
    **این یک تغییرِ رفتارِ حسابداری روی مسیرِ مستقر است**؛ سندهای تاریخی
    بازنویسی نشدند. ۴۱۰۴ عمداً گرفته نشد (قالب‌های صنفی آن را «تخفیفات **و**
    برگشت از فروش» برده‌اند و `get_or_create_account` کدِ `V`دار می‌ساخت).
  - **علتِ برگشت.** جدولِ `sales_return_reasons` با RLS، و ستونِ nullable روی
    ردیف. متنِ آزاد نشد چون «خرابی» و «خراب بود» هرگز با هم جمع نمی‌شدند.
    غیرفعال‌کردن هست، حذف نیست — سندِ تاریخی علتش را نگه می‌دارد.
  - **و هفت جا که با ابطال بی‌صدا می‌شکستند:** `open_items.SETTLEABLE` (هر دو
    نوع برگشت)، `voiding._voided_sources` (وگرنه حرکتِ انبارِ برگشتِ باطل‌شده در
    `recompute_average_cost` می‌ماند و میانگینِ بها منحرف می‌شد)،
    `voiding._guard_no_active_returns`، `credit.customer_outstanding`، و چهار
    مسیر در `reports.py` (اظهارنامه‌ی مالیاتی، گزارشِ فصلی، رَوَندِ ماهانه،
    ماندهٔ طرف‌حساب، صورت‌حساب و گزارشِ سنی). پارامترِ `voidable` در
    `get_vat_report` حذف شد — دقیقاً به‌خاطر نبودِ ابطال روی برگشت ساخته شده بود.
  - همچنین: قفلِ ردیف و سربرگ پیش از اعتبارسنجی (دو برگشتِ هم‌زمان دیگر مجموعاً
    از مانده رد نمی‌شوند)، `Idempotency-Key` روی هر دو مسیرِ ثبت، و وضعیت‌های
    مشتقِ فهرست (پرداخت‌شده/ماندهٔ برگشت) بدونِ هیچ شمارنده‌ی ذخیره‌شده.
  - **عمداً ساخته نشد:** برگشتِ خروج انبار (§۳۰–§۳۴) و حذفِ COGS از سندِ تجاری
    (§۵۲/§۵۳) و تسهیمِ اضافات (§۱۷–§۲۲) و حساب‌های نوع فروش (§۱۰۵) —
    زیرساختشان (`WarehouseIssue`، ستون‌های `addition`/`duty_amount`، ستون‌های
    حساب روی `sale_types`) روی `master` نیست.

- **۱۴۰۵/۰۶/۲۰ (2026-09-11) — استقرار روی prod: نسخه ۰۱۱۵ ← ۰۱۲۰.** دو فصلِ
  «تعریف و مدیریت انبار» و «تعریف کالا و خدمت» با هم رفتند (`0116`–`0120`).
  مهاجرت‌ها بی‌خطا اجرا شدند و **درستیِ داده روی خودِ prod سنجیده شد**، نه فقط
  روی محلی: هر ۲۱ کالا `purchase_vat_status`شان برابرِ `vat_status` ماند (پس
  هیچ کالایی یک‌شبه در خرید مشمول نشد) · هر ۲۱ کالا واحدِ اصلی گرفتند · هر ۳
  کالای دسته‌دار به گروه پیوند خوردند · و **هر ۶ فاکتورِ مالیات‌دار، جمعِ
  `tax_amount_snapshot`ِ ردیف‌هایش دقیقاً با `tax_amount`ِ سربرگ خواند** — که
  پرخطرترین پرکردنِ این استقرار بود. ۴۷ واحدِ سنجش برای ۶ مستأجر و ۳ گروه از
  خودِ داده ساخته شدند. چهار نقطه‌ی تازه (`/api/units`، `/api/item-groups`،
  `/api/item-attributes`، `/api/stock/over`) بدونِ توکن ۴۰۱ می‌دهند نه ۴۰۴، و
  هر ۹ جدولِ لمس‌شده `FORCE ROW LEVEL SECURITY` دارند. وبِ demo با
  `rsync -a --delete` تازه شد (نه `deploy.sh demo`) و درِ تریال ۲۰۰ ماند.

- **۱۴۰۵/۰۶/۲۰ (2026-09-11) — خریدِ خدمت به «موجودی کالا» می‌نشست و قلمِ معاف
  مالیات می‌خورد (مهاجرت‌های `0117`–`0120`):** فصلِ «تعریف کالا و خدمت». اصلِ
  اصلی‌اش از قبل رعایت شده بود — کوبیتا **یک** جدولِ `items` دارد و خرید، فروش،
  انبار، صندوق، تولید، بازار و مؤدیان همه از همان می‌خوانند؛ کاتالوگِ موازی
  وجود ندارد. ولی چهار گامِ کامل‌کردن لازم بود.

  **گامِ ۱ — دو نقصِ پول‌ساز که تراز را به‌هم نمی‌زدند (`0117`).**
  `post_purchase_invoice` کلِ مبلغِ فاکتور را بدهکارِ «موجودی کالا» می‌کرد، چه
  کالا بود چه خدمت. خریدِ ده ساعت مشاوره یعنی **۵۰ میلیون دارایی در ترازنامه که
  وجود نداشت و هرگز خارج نمی‌شد**، و هزینه‌ای که هیچ‌وقت به سود و زیان نمی‌رسید؛
  گزارشِ انبار صفر می‌گفت چون از `stock_ledger` مشتق می‌شود. سند متوازن بود و
  تراز آزمایشی صفر می‌شد، پس هیچ نگهبانی خبر نمی‌داد. حالا نقشِ تازه‌ی
  `service_expense` (کد ۵۱۱۷) و معینِ اختیاریِ هر کالا، با تفکیکِ ردیف‌به‌ردیف در
  خرید و برگشت از خرید. **نقصِ دوم:** مالیات روی *جمعِ* فاکتور حساب می‌شد، پس
  قلمِ **معاف** هم مالیات می‌خورد — `vat_status` ذخیره و در گزارش شمرده می‌شد
  ولی در محاسبه اثری نداشت، یعنی گزارش می‌گفت «فروشِ معاف: X» و همان فاکتور روی
  X مالیات بسته بود. بسته‌ی مؤدیان هم نرخِ سربرگ را به هر ردیف می‌زد و قلمِ معاف
  را **با مالیات به سازمان اظهار می‌کرد**. حالا مالیات ردیف‌به‌ردیف است، ردیفِ
  فروش هم snapshotِ نرخ و مبلغ دارد (قرینه‌ی ردیفِ خرید که از قبل داشت)، و برگشت
  نرخِ همان ردیفِ اصلی را برمی‌گرداند. شناسنامه‌ی کالا هم کامل شد: عنوان دوم،
  ایران‌کد، بارکد دوبعدی، «قابل فروش» جدا از «فعال» (و اعمالش در فروش)، پرچمِ
  ثبتِ سریالی با گاردِ تغییر پس از گردش، نرخِ مالیات/عوارضِ کالا، و وضعیتِ
  مالیاتیِ خرید مستقل از فروش. کد، دسته و واحد از این پس قابلِ اصلاح‌اند.

  **گامِ ۲ — واحدِ سنجش یک رشته‌ی آزاد بود (`0118`).** «کیلوگرم» و «كيلوگرم» (با
  کاف و یای عربی) دو واحدِ متفاوت می‌شدند و نگاشتِ کدِ واحدِ مؤدیان — که روی همان
  نوشتار کلید می‌خورد — برای هر املا جدا لازم بود. حالا `units_of_measure` با
  RLS، **ساخته‌شده از خودِ داده**: هر نوشتاری که امروز روی کالاهای یک کسب‌وکار
  هست همان می‌شود یک واحد، پس هیچ کالایی واحدش عوض نمی‌شود و هیچ نگاشتی
  نمی‌شکند. واحدِ فرعی و نسبتِ تبدیل هم آمد، با تفکیکِ نسبتِ ثابت از متغیر.
  `units.to_primary` **تنها** پیاده‌سازیِ تبدیل در کلِ کوبیتاست (§۲۱ صریحاً
  می‌گوید هر فرم نباید خودش تبدیل کند) و نسبتِ متغیر را **رد می‌کند** نه اینکه
  عددی از خودش دربیاورد — §۲۲ ورودش در تراکنش را به فصل‌های بعد موکول می‌کند.
  `Item.unit` ماند ولی از این پس *پرتوِ* نامِ واحدِ اصلی است، نه منبعِ حقیقت.

  **گامِ ۳ — کالا نمی‌دانست در کدام انبارها جا دارد (`0119`).** رابطه‌ی کالا و
  انبار وجود نداشت و کنترلِ موجودی فقط نقطه‌ی سفارش بود. حالا `item_warehouses`
  (چندبه‌چند) با انبارِ پیش‌فرض، و حداقل/حداکثر با امکانِ override برای هر انبار.
  سه تصمیمِ عمدی: **فهرستِ خالی یعنی «همه‌ی انبارها»** نه «هیچ انباری» (یعنی
  رفتارِ امروز، پس هیچ کالای موجودی محدود نمی‌شود) · **انبارِ پیش‌فرض مالکیت
  نیست** و کالا به آن قفل نمی‌شود (§۳۲) · **حذفِ رابطه هیچ حرکتِ انباری یا
  کاردکسی را پاک نمی‌کند** و کالایی که در انباری موجودی دارد همان‌جا قابلِ
  عملیات می‌ماند، وگرنه موجودی در انباری گیر می‌افتاد که نه واردش می‌شود نه
  خارج (§۳۳). مهاجرت عمداً **هیچ ردیفی نمی‌سازد**: استنتاجِ فهرست از حرکاتِ
  گذشته یک حدس است نه یک واقعیت. حداقل و حداکثر هر دو داده‌ی برنامه‌ریزی‌اند نه
  سدِ تراکنش (§۲۶) — «نیازمندِ سفارش» حالا حداقل را هم می‌بیند و می‌گوید کدام
  آستانه ردیف را آورده، و `/api/stock/over` مازاد را گزارش می‌کند.

  **گامِ ۴ — گروه یک متن بود، مشخصه‌ای نبود، و قیمت فقط یک عدد داشت (`0120`).**
  `Item.category` متنِ آزاد بود، پس «لوازم خانگی» و «لوازم‌خانگی» دو گروه
  می‌شدند؛ حالا `item_groups` (ساخته‌شده از خودِ داده، مثلِ واحدها) و `category`
  پرتوِ نامِ گروه. برای «رنگ» و «سایز» جایی نبود — و §۳۶ صریح است که برای هر
  مشخصه نباید ستونِ تازه‌ای روی `items` بنشیند، پس الگو *تعریفِ مشخصه* ←
  *مقدارِ کالا* است (`item_attributes` + `item_attribute_values`). و
  `price_list_items` برای هر کالا در هر لیست **یک** قیمت داشت؛ حالا ابعادِ نوعِ
  فروش، واحد، گروهِ مشتری و ارز را دارد (همه از داده‌ی موجود، نه تعریفِ موازی —
  §۳۹ §۴۰) و مشخص‌ترین قاعده می‌برد. یکتایی با `COALESCE` است چون در Postgres
  دو `NULL` در ایندکسِ یکتا برابر شمرده نمی‌شوند. کنترلِ تغییرِ نرخ (§۴۲) هم
  آمد و در فاکتور فروش اعمال می‌شود؛ پیش‌فرض **بی‌حد** است پس هیچ فروشی یک‌شبه
  مسدود نمی‌شود.

  **چه چیزی عمداً ساخته نشد:** نوعِ «دارایی» روی `Item` — فصل خودش می‌گوید از
  این قسمت workflowِ دارایی اختراع نکن، و کوبیتا از قبل ماژولِ کاملِ داراییِ
  ثابت دارد؛ افزودنِ `type=ASSET` دقیقاً همان کاتالوگِ موازی می‌شد که فصل منعش
  می‌کند. همچنین موتورِ بارکدِ دوم و تعریفِ موازیِ نوعِ فروش/ارز — هر سه از قبل
  هستند و بازاستفاده شدند.

  **دو چیزی که آزمونِ زنده لو داد:** ساختِ کالا با کدِ تکراری **۵۰۰ خام**
  می‌داد (گاردِ کد فقط روی ویرایش بود، نه ساخت) — حالا ۴۰۹ با پیامِ روشن. و
  افزودنِ کلیدِ خارجی به جدولی که *مرجعش* FORCE RLS دارد همان اسکنِ اعتبارسنجیِ
  مهاجرتِ ۰۱۰۹ را راه می‌اندازد، این بار از سمتِ مقابل. بک‌اند **۲۳۹۲ تست سبز**
  (۸۰ تای تازه)؛ `tsc` · `lint` · `audit-pages` بی‌خطا. روی بک‌اندِ محلی با
  داده‌ی واقعی هم سنجیده شد: خریدِ خدمت «موجودی کالا» را صفر و «هزینه خرید
  خدمات» را ۵۰٬۰۰۰٬۰۰۰ تکان داد؛ فاکتورِ مختلط با نرخ ۱۰٪ فقط ۱۰۰٬۰۰۰ مالیات
  گرفت (نه ۲۰۰٬۰۰۰)؛ و نگاشتِ خدمت به «صندوق» و فروشِ کالای غیرقابل‌فروش هر دو
  ۴۰۰ دادند. داده‌ی آزمون بعد از سنجش باطل و غیرفعال شد و دفتر پاک برگشت.

- **۱۴۰۵/۰۶/۲۰ (2026-09-11) — انبار سه ستون داشت: کد، نام، فعال
  (مهاجرت `0116`):** مهم‌ترین اصلِ فصلِ انبار از قبل رعایت شده بود — `Warehouse`
  **هیچ ستونِ موجودی ندارد** و مانده همیشه از `stock_ledger` می‌آید؛ آن دست
  نخورد و یک تست حالا ساختارش را قفل می‌کند. ولی جز آن سه ستون چیزی نداشت:
  **(۱) عنوانِ دوم، مسئول، تلفن و آدرس** نبود، با آن‌که طرف‌حساب و بانک و صندوق
  همه دارند. **(۲) معینِ انبار نبود** — همه‌ی انبارها (مرکزی، مواد اولیه،
  ضایعات) به *یک* حسابِ `inventory` می‌نشستند و در دفتر از هم تشخیص‌دادنی
  نبودند؛ حالا هر انبار می‌تواند معینِ خودش را داشته باشد و `NULL` یعنی همان
  حسابِ پیش‌فرض، پس **هیچ مستأجری رفتارش عوض نمی‌شود** (§۱۳). نگاشت در **لحظه‌ی
  ثبت** حل می‌شود، پس تغییرِ بعدی‌اش سندهای گذشته را بازنویسی نمی‌کند (§۳۴) —
  و یک تست همین را می‌سنجد. **(۳) `is_active` فقط در یک مسیر سنجیده می‌شد**
  (رسیدِ انبارِ خرید)؛ فاکتور فروش، فاکتور خرید، تعدیل، انتقال و انبارگردانی
  همگی انبارِ غیرفعال را می‌پذیرفتند — یعنی «غیرفعال» یک برچسبِ نیمه‌کاره بود نه
  یک قاعده. **(۴) انبارِ دارای موجودی بی‌هیچ هشداری غیرفعال می‌شد** (§۱۷)؛ حالا
  مسدود می‌شود با پیامی که تعداد و راهِ خروج را می‌گوید. **یک تله که آزمونِ زنده
  لو داد:** اولین بار که نگاشت را روی داده‌ی واقعی امتحان کردم، توانستم انبار را
  به حسابِ **«صندوق»** وصل کنم — یعنی موتورِ خزانه و موتورِ انبار روی یک حساب با
  دو معنی بنویسند و ماندهٔ صندوق بی‌صدا با بهای کالا قاطی شود، بی‌آنکه ترازی
  به‌هم بخورد. حالا حسابی که `system_role`ِ دیگری دارد رد می‌شود — قاعده‌ای که از
  خودِ سازوکارِ نقشِ کوبیتا می‌آید، نه از حدسِ سلسله‌مراتبِ چارت (فصل صریحاً
  hard-code کردنِ کد و نامِ حساب را منع می‌کند). بک‌اند ۲۲۸۷ تست سبز؛ `tsc` ·
  `lint` · `audit-pages` بی‌خطا. روی بک‌اندِ محلی با داده‌ی واقعی هم سنجیده شد:
  گاردِ غیرفعال‌سازی «۲ قلم کالا با موجودیِ غیرصفر» را درست شمرد و ۴۰۹ داد.

- **۱۴۰۵/۰۶/۲۰ (2026-09-11) — تایم‌لاینِ چک می‌گفت چه شد، ولی نه کجا ثبت شد
  (مهاجرت `0115`):** مهاجرتِ ۰۱۱۰ تاریخچه‌ی چک را ساخت و هر گذر بانک و صندوق و
  طرف‌حساب و **سندِ حسابداری**ش را نگه می‌داشت. یک چیز جا مانده بود: **سندِ
  عملیاتی**. یعنی تایم‌لاین می‌گفت «دریافت، ۱۴۰۴/۰۱/۰۱» ولی نه این‌که در کدام
  *رسید* ثبت شده — و سؤالِ «این چک با کدام رسید آمد؟ با کدام اعلامیه خرج شد؟»،
  که اولین سؤالِ هر حسابرسی است، هیچ جوابی نداشت. `operation_no` بود ولی
  شماره‌ی **عملیاتِ چک** است؛ رسید و اعلامیه عملیاتِ چک نیستند. حالا
  `check_events.source_type`/`source_id` سندِ عملیاتی را نگه می‌دارند و
  drill-downِ §۱۴ کار می‌کند. **جستجو هم جستجو نبود:** `/api/checks` هیچ فیلتری
  نداشت و کلاینت همه‌ی چک‌ها را می‌گرفت و در مرورگر غربال می‌کرد — پس «جستجو روی
  کدِ صیادی» اصلاً ممکن نبود، چون آن ستون در غربالِ کلاینت نبود. حالا فیلترها
  روی سرورند: شماره، صیادی، پشت‌نمره، صاحبِ چک، بانک، طرف حساب، نوع، وضعیت،
  **بازه‌ی سررسید** (§۹) و **بازه‌ی مبلغ** (§۱۰). افزون بر این: **«الان کجاست؟»**
  (§۲۰ §۲۱) از وضعیت و پیوندهای واقعی **مشتق** می‌شود نه ذخیره — و یک تفکیکِ
  ظریف را درست می‌گیرد: گیرنده‌ی چکِ خرج‌شده روی *رویدادِ خرج* نشسته، نه
  `check.contact_id` که هنوز دهنده است؛ **KPIهای شمارش و مبلغِ هر وضعیت** (§۳۴)
  از همان کوئریِ جستجو؛ و صفحه‌ی «جستجوی چک» به **کارگاهِ پرونده‌ی چک** تبدیل شد:
  انتخابِ ردیف، مشخصاتِ برگ، مسیرِ گام‌به‌گام با سندِ هر گام، و کنش‌های مجاز که
  کاربر را به همان عملیاتِ استاندارد **می‌برند** — صفحه‌ی جستجو خودش هیچ گذری
  ثبت نمی‌کند (§۲۸). **دو شکافِ جانبی که حین ساخت پیدا شدند:** (۱)
  `create_check` — مسیرِ مستقیمِ ثبتِ چک — `owner_name`، `branch_name`،
  `branch_code`، `account_number` و `description2` را کپی **نمی‌کرد** با آن‌که
  `new_check_row` (مسیرِ رسید/اعلامیه) می‌کرد؛ پس چکِ ثبت‌شده‌ی مستقیم صاحب و
  شعبه‌اش را بی‌صدا گم می‌کرد و جستجوی «صاحبِ چک» هرگز پیدایش نمی‌کرد. (۲) گذرِ
  تکی هیچ شماره‌ی عملیاتی نمی‌گرفت، پس رویدادش به «در کدام عملیات؟» جوابی نداشت؛
  حالا **هر رویداد یک هویت دارد** — یا سندِ منبع، یا شماره‌ی عملیاتِ خودش.
  backfillِ مهاجرت فقط چیزی را وصل می‌کند که از یک کلیدِ خارجیِ واقعی مشتق
  می‌شود (`receive`→`receipt_id`، `issue`/`endorse`→`payment_id`)؛ بقیه `NULL`
  می‌مانند، چون حدس‌زدن همان جعلِ تاریخچه است که ۰۱۱۰ هم ردش کرد.
  **مستقر شد روی prod همان روز** (نسخه ۰۱۱۴ ← ۰۱۱۵). پیش از استقرار اثرِ
  مهاجرت سنجیده شد: prod **صفر رویدادِ چک** دارد و هر دو چکش `cleared` است، پس
  backfill هیچ ردیفی را لمس نکرد و مهاجرت عملاً فقط اسکیما بود — `journal_lines`
  همان ۱۶۴ ردیف ماند. پس از استقرار: ستون‌های `source_type`/`source_id` روی
  `check_events` نشستند؛ `/api/checks`، `/api/checks/summary` و
  `/api/check-operations` هر سه **۴۰۱** (وجود دارند و پشتِ احرازند، نه ۴۰۴)؛
  چک‌سامِ `check_search.py` و `check_ops.py` روی سرور با محلی یکی، و باندلِ وب
  بایت‌به‌بایت همان بیلدِ محلی (`5357eb78cae13545`). پشتیبانِ پیش از استقرار
  `hesabdari_20260911_173732.dump` و برگشتِ کد
  `rollback_code_20260911_173732.tgz`. وبِ demo با `rsync` هم‌گام شد (پشتیبان:
  `web.bak-20260911_173858`)؛ طبقِ قاعده **هرگز** `deploy.sh demo` اجرا نشد.
  بک‌اند ۲۲۷۱ تست سبز؛ `tsc` · `lint` · `audit-pages` بی‌خطا. روی بک‌اندِ محلی با
  داده‌ی واقعی هم سنجیده شد: رسیدِ دارای چک ساخته شد، با کدِ صیادی پیدا شد،
  تایم‌لاینش «دریافت → رسید دریافت ۱» را نشان داد، و پس از خرج‌کردن موقعیتش به
  «خرج‌شده به …» با نامِ **گیرنده** تغییر کرد.

- **۱۴۰۵/۰۶/۲۰ (2026-09-11) — «پرداخت» و «تسویه» یکی گرفته شده بودند؛ حالا
  فاکتور می‌داند چقدرش و با کدام رسید تسویه شده (مهاجرت `0114`):** رسیدِ دریافت
  می‌گفت پول جابه‌جا شد، ولی **هیچ‌جا نمی‌گفت بابتِ کدام فاکتور**. نه
  `receipt.invoice_id` بود نه جدولِ واسط — یعنی سؤالِ «این ۱۰۰ میلیون بابتِ کدام
  دو فاکتور بود؟» اصلاً قابلِ ثبت نبود. تنها عددی که شبیهِ «پرداخت‌شده» ساخته
  می‌شد در گزارشِ سنیِ مطالبات بود و آن‌جا هم **در لحظه‌ی گزارش با FIFO حدس زده
  می‌شد**: جمعِ دریافت‌های شخص روی قدیمی‌ترین فاکتورهایش ریخته می‌شد — حدسی که
  ذخیره نمی‌شد، دیده نمی‌شد و کاربر نمی‌توانست تصحیحش کند. بدتر: صفحه‌ی «تسویه
  حساب طرف مقابل» که از قبل در منو بود **اصلاً تسویه نبود** — یک دکمه بود که
  برای کلِ ماندهٔ شخص یک *رسیدِ دریافتِ تازه* می‌ساخت؛ نامش تسویه بود و کارش ثبتِ
  پولِ تازه. حالا `settlements` + `settlement_allocations` رابطه را نگه می‌دارند:
  چند قلمِ بدهکار در برابرِ چند قلمِ بستانکارِ یک طرف حساب، روی یک **معینِ مشخص**
  (§۵)، با شماره‌ی بی‌شکافِ خودش. **مهم‌ترین تصمیم: تسویه سندِ حسابداری نمی‌زند**
  (§۲۲ §۲۳) — اثرِ مالی را فاکتور و رسید قبلاً زده‌اند و سندِ دوم فقط ۱۰۰ بدهکار/
  ۱۰۰ بستانکارِ خنثی به دفتر اضافه می‌کند؛ تستِ سرشناسِ این فصل همین را قفل
  می‌کند. **سمتِ هر قلم از دفتر می‌آید نه از نامِ فرم** (§۸): `services/open_items.py`
  اثرِ واقعیِ سندِ هر منبع را روی معینِ انتخاب‌شده می‌خواند (بدهکار منهای
  بستانکار)، پس فاکتورِ فروش بدهکار و فاکتورِ خرید بستانکار درمی‌آید بی‌آنکه جایی
  hard-code شود — و **جمعِ اقلامِ باز دقیقاً برابرِ ماندهٔ همان معین می‌ماند**،
  چون هر دو از یک ردیف‌های دفتر مشتق می‌شوند (§۲۶ §۴۴). هشت نوعِ منبع در
  رجیستریِ `SETTLEABLE` ثبت شدند (فاکتورِ فروش/خرید، برگشتی‌ها، رسید، اعلامیه‌ی
  پرداخت، اعلامیه‌ی بدهکار/بستانکار، و **رویدادِ چک** — چون دریافتِ چک دریافتنی را
  بستانکار و واخواستش دوباره بدهکار می‌کند و این دو دو قلمِ جدا هستند).
  «تسویه‌شده» و «مانده‌ی قابلِ تسویه» **ذخیره نمی‌شوند** و از تخصیص‌ها جمع زده
  می‌شوند (§۴۳)، پس تسویه‌ی جزئی (§۱۵)، پیش‌دریافتِ تخصیص‌نیافته (§۲۸ §۲۹) و
  برگشتِ تسویه (§۳۷) همگی بدونِ ستونِ اضافه کار می‌کنند. کنترل‌ها: دو سمت باید
  برابر باشند و پیامِ خطا **هر سه عدد** را می‌گوید (§۱۹ §۲۱)، مبلغ از مانده بیشتر
  نمی‌شود (§۱۴)، قلمِ طرف حسابِ دیگر رد می‌شود (§۴)، دریافتنی و پرداختنیِ یک شرکت
  از هم جدا می‌مانند (§۳۰ §۳۱)، و اقلامِ چندارزی **صریحاً رد** می‌شوند چون این
  فصل قاعده‌ی تسویه‌ی بینِ دو ارز را تعریف نمی‌کند (§۳۲). افزون بر این:
  **ابطالِ سندی که تخصیص خورده بسته شد** (§۴۱ — چون تسویه سندی نمی‌زند، هیچ
  ترازی از این خرابی خبر نمی‌داد)، بررسیِ **«تخصیصِ بیش از مانده»** به «بررسی
  یکپارچگی» اضافه شد (§۳۹ §۴۰)، `Settlement` در `audit.audited_models()` ثبت شد،
  و دفترِ تازه‌ی **«تسویه‌های طرف مقابل»** با drill-downِ اقلام ساخته شد.
  «تخصیصِ خودکار» هست ولی **فقط با فشردنِ دکمه** و قابلِ ویرایش (§۴۷ §۴۸).
  **یک کشفِ داده‌ی واقعی حینِ ساخت:** ادعای اولیه این بود که «جمعِ اقلامِ باز
  دقیقاً برابرِ ماندهٔ معین است»، و روی پایگاهِ محلی **صادق نبود** — ۲,۰۰۰,۰۰۰
  ریال اختلاف. علتش یک چکِ ثبت‌شده پیش از مهاجرتِ ۰۱۱۰ بود که رویدادی ندارد
  (`Check` ستونِ `journal_entry_id` ندارد و تاریخچه هم عمداً backfill نشد)، پس
  گردشش در دفتر هست ولی هیچ راهی از دفتر به آن نیست. همین برای **سندِ دستی روی
  دریافتنی** و **ماندهٔ اول دوره** هم صدق می‌کند. حذفِ خاموشش یعنی بخشی از
  ماندهٔ مشتری در «اقلامِ باز» گم شود، پس حالا `unattributed` تفاوت را گزارش
  می‌کند، صفحه هم زیرِ خلاصه می‌گویدش، و بررسیِ تازه‌ی **«گردشِ بدونِ سندِ قابلِ
  تسویه»** در «بررسی یکپارچگی» نشانش می‌دهد.
  مهاجرت عمداً **فقط اسکیماست**: تخصیصِ گذشته backfill نمی‌شود، چون همان FIFOیی
  که گزارشِ سنی می‌زند حدس است نه حقیقت و ثبتش به‌عنوانِ سابقه جعلِ تاریخچه بود —
  همان استدلالِ ۰۱۱۰.
  **مستقر شد روی prod همان روز** با `deploy.sh production` — و چون سه فصلِ همکار
  هنوز مستقر نشده بودند، زنجیره‌ی **۰۱۱۰ ← ۰۱۱۴** یک‌جا رفت (رسید دریافت،
  اعلامیه‌ی پرداخت، رسیدِ انبارِ خرید، تسویه). با کاربر هماهنگ شد که همه با هم
  بروند، چون مهاجرتِ تسویه روی ۰۱۱۳ سوار است و جدا‌شدنی نبود. نتیجه:
  `journal_lines` پیش و پس از استقرار همان **۱۶۴ ردیف** ماند — هیچ‌کدام از چهار
  مهاجرت داده‌ای ننوشت جز شمارنده‌ها؛ `document_counters` از ۸۴ به **۱۰۸** رفت،
  یعنی دقیقاً ۶ مستأجر × ۴ نوعِ سندِ تازه، که خودش تأییدِ زنده‌ی همان اصلاحِ
  شمارنده است. جدول‌های `FORCE RLS` از ۱۱۳ به ۱۲۲. پس از استقرار:
  `/api/settlements`، `/api/settlements/accounts`، `/api/receipts` و
  `/api/payments` هر چهار **۴۰۱** (وجود دارند و پشتِ احرازند، نه ۴۰۴)؛
  `settlements`، `settlement_allocations`، `receipts` و `payments` هر چهار با
  `RLS + FORCE`؛ چک‌سامِ `open_items.py`، `settlements.py` و `receipts.py` روی
  سرور با محلی یکی؛ و باندلِ وبِ سرو‌شده بایت‌به‌بایت همان بیلدِ محلی
  (`052681207ce3db58`). پشتیبانِ پیش از استقرار
  `hesabdari_20260911_152853.dump` و برگشتِ کد
  `rollback_code_20260911_152853.tgz`. وبِ demo با `rsync` هم‌گام شد (پشتیبان:
  `web.bak-20260911_153047`)؛ طبقِ قاعده **هرگز** `deploy.sh demo` اجرا نشد. بک‌اند ۲۲۵۳ تست سبز؛ `tsc` · `lint` · `audit-pages` بی‌خطا.
  **ادغام با «رسید دریافت» یک تلهٔ واقعی داشت:** آن فصل رسید را سربرگ کرد و
  اجزایش (نقد، حواله، کارت‌خوان، چک) **یک `journal_entry_id` مشترک** گرفتند. اگر
  لنگرِ قلمِ تسویه روی جزء می‌ماند، رسیدی با دو جزء دو بار کلِ بستانکارِ دریافتنی
  را می‌آورد — دو برابرِ واقعیت و بی‌صدا، چون هیچ ترازی به‌هم نمی‌خورد. حالا لنگر
  `Receipt`/`Payment` است و ردیفِ خزانه‌ی **سربرگ‌دار** از فهرست کنار می‌رود؛
  ردیفِ بی‌سربرگ (پیش از ۰۱۱۱، یا اثرِ جانبیِ قسط و بازارگاه) می‌ماند چون واقعاً
  سندِ مستقل است. یک تستِ رگرسیون همین را قفل می‌کند.
  **و یک شکافِ استقرار که همان‌جا پیدا شد:** مهاجرت‌های ۰۱۱۱ تا ۰۱۱۳ سه نوعِ
  سندِ تازه (`receipt`, `payment`, `warehouse_receipt`) به `DOC_TYPES` افزودند
  ولی ردیفِ `document_counters` مستأجرهای **موجود** را نساختند. چون
  `next_document_number` نبودنِ ردیف را عمداً ۵۰۰ می‌دهد، اولین رسیدی که روی
  تولید ثبت می‌شد می‌شکست؛ تست هم نمی‌گرفتش، چون هر تست مستأجرِ تازه می‌سازد و
  `seed.py` روی `DOC_TYPES` حلقه می‌زند. با فراخوانیِ واقعیِ
  `next_document_number` روی یک مستأجرِ موجود اثبات شد و مهاجرتِ ۰۱۱۴ حالا
  **هر نوعِ کم‌بود** را می‌سازد، نه فقط `settlement`.
- **۱۴۰۵/۰۶/۲۰ (2026-09-11) — فاکتور فروش تجاری و خروج مستقل انبار (مهاجرت `0116`):**
  - ثبت فاکتور فروش فقط حقیقت تجاری را می‌سازد؛ صدور سند فروش و خروج انبار دو اقدام
    مستقل با وضعیت‌های مشتقِ حسابداری، تحویل و تسویه‌اند. شرط نقدی هم وصول خودکار
    نمی‌سازد و وصول فقط از تخصیص رسیدهای معتبر مشتق می‌شود.
  - سند فروش درآمد ناخالص، تخفیف بدهکار، اضافات، مالیات/عوارض و دریافتنی مشتری را
    جدا ثبت می‌کند و هیچ ردیف موجودی یا COGS ندارد. صدور دوباره idempotent است.
  - `WarehouseIssue` خروج جزئی را با قفل ردیف، کنترل مانده/موجودی، Snapshot کالا،
    کاردکس و سند COGS مستقل ثبت می‌کند؛ ابطال آن حرکت و سند جبرانی می‌سازد.
  - Snapshot مشتری/فروشنده و کالا چاپ تاریخی را حفظ می‌کند. فرم و فهرست دسکتاپ
    شرایط تسویه، اضافات/عوارض، سه وضعیت مستقل و عملیات صدور سند/خروج را نشان می‌دهند.
  - فروش فوری فروشگاه بومی، اتصال فروشگاه و بازار عمده‌فروشی هنوز در یک تراکنش
    نهایی می‌شود، اما سه سند جدا و قابل ردیابی می‌سازد.
  - مهاجرت روی اسکیمای یک‌بارمصرف مسیر `۰→۰۱۱۶→۰۱۱۵→۰۱۱۶` را طی کرد؛ هر دو جدول
    خروج `RLS + FORCE` دارند و `public.alembic_version` برابر `0098` باقی ماند.
  - کل مجموعهٔ بک‌اند **۲۲۳۸ تست سبز**؛ TypeScript و ممیز صفحه‌ها بدون خطا و
    oxlint بدون خطا (فقط هشدارهای قدیمی) تمام شدند.

- **۱۴۰۵/۰۶/۲۰ (2026-09-11) — پیش‌فاکتور به‌عنوان منبع تجاری مستقل (مهاجرت `0115`):**
  - رابطه‌ی ۱:۱ قدیمی کنار گذاشته شد: هر ردیف پیش‌فاکتور می‌تواند در چند فاکتور
    جزئی مصرف شود و مقدار فاکتورشده/مانده فقط از ردیف‌های فاکتور معتبر مشتق می‌شود.
  - تبدیل با قفل ردیف و `Idempotency-Key` از بیش‌تبدیل و ساخت فاکتور تکراری محافظت
    می‌شود. فاکتور حاصل Source Link سربرگ و ردیف دارد، ولی موجودی و COGS را حرکت
    نمی‌دهد؛ خروج فیزیکی مسیر مستقل و تصمیم باز §۳۱ است.
  - Snapshot مشتری و کالا، خاتمه/بازگشایی مستقل از انقضا، تکثیر با شماره و هویت
    تازه، و گارد ویرایش پس از ساخت Child اضافه شد. چاپ تاریخی از Snapshot می‌خواند.
  - مهاجرت روی اسکیمای یک‌بارمصرف مسیر `۰→۰۱۱۵→۰۱۱۴→۰۱۱۵` را واقعاً طی کرد و
    `public.alembic_version` پیش و پس از آزمون همان `0098` ماند.
  - مجموعهٔ کامل بک‌اند با **۲۲۲۴ تست سبز** تمام شد؛ TypeScript و ممیزی صفحه‌ها
    نیز بدون خطا بودند.

- **۱۴۰۵/۰۶/۲۰ (2026-09-11) — رسید دریافتِ چندابزاری (مهاجرت `0111`):**
  - **شکافِ واقعی:** `TreasuryTransaction` هرچه داشت در سطحِ **ابزار** بود
    (`cashbox_id`، `bank_account_id`، `pos_terminal_id`، `reference_no`،
    `settled_at`) و هرچه نداشت در سطحِ **سند** (شماره، نوعِ دریافت، ارز، تخفیف).
    یعنی آن جدول از اول «یک جزء» بود، نه یک رسید — و چون سربرگی نبود،
    `CheckConstraint` روی `method` عملاً می‌گفت: یک رسید، یک ابزار. این همان
    چیزی است که `OPEN_DECISIONS §۲۱` عمداً پارکش کرده بود.
  - **شکلِ انتخاب‌شده: سربرگ بالای اجزا، نه ردیف زیرِ خزانه.** جدولِ تازه‌ی
    `receipts` ساخته شد و `treasury_transactions.receipt_id` و
    `checks.receipt_id` به آن اشاره می‌کنند. راهِ دیگر — جدولِ ردیف زیرِ خودِ
    خزانه — رد شد چون آن هفت ستونِ سطحِ‌ابزار در **نُه** جای دیگر مستقیم خوانده
    می‌شوند (تسویه‌ی کارت‌خوان، گاردهای «در حالِ استفاده»، موجودیِ دستگاه) و هر
    کدام که جا می‌ماند یک عددِ غلطِ بی‌صدا می‌شد.
  - **چک همان `Check` ماند** — چرخه‌ی عمرِ مستقل دارد و `contact_balance` جداگانه
    می‌شماردش؛ اگر جزءِ خزانه می‌شد، هر چکِ دریافتی **دو بار** از مانده کم می‌کرد.
  - **یک رسید، یک سند:** به‌ازای هر جزء یک ردیفِ بدهکار با حسابِ ماهیتِ خودش
    (صندوق، بانک، چک‌های دریافتنی، حسابِ تسویه‌ی دستگاه) و یک ردیفِ بستانکار روی
    حسابِ طرفِ مقابل. اجزا همان `journal_entry_id` را به اشتراک می‌گذارند.
  - **نوعِ دریافت تفسیرِ حسابداری را عوض می‌کند** (پنج نوعِ سپیدار). این یک اشکالِ
    واقعی را هم بست: تا امروز «دریافت از تأمین‌کننده» هم حساب‌های **دریافتنی** را
    بستانکار می‌کرد. پیش‌فرضِ `customer` رفتارِ امروز است، پس داده‌ی مستقر عوض
    نمی‌شود.
  - **هویتِ چک:** `sayad_id` (جدا از شماره‌ی چک)، `back_number`، شعبه و کدش،
    شماره‌حساب، صاحبِ چک، شرحِ دوم — با ایندکسِ یکتای **جزئی** روی کد صیادی، چون
    آن کد در سطحِ کشور یکتاست و تکراری‌اش خطای ورودِ داده است.
  - **ابطال، برای اولین بار:** تا امروز رسیدِ اشتباه **هیچ راهِ اصلاحی نداشت** —
    نه حذف، نه ابطال، و `void_journal_entry` سندِ غیر‌دستی را رد می‌کند. حالا
    `voided_at` روی رسید و اجزا و چک‌ها می‌نشیند و
    `voiding.reverse_journal_entry`ِ موجود سندِ معکوس می‌زند؛ گاردها: چکِ
    واگذارشده و کارتِ تسویه‌شده ۴۰۹ می‌گیرند. **و چهار گزارش فیلترِ ابطال گرفتند**
    (`contact_balance`، کارت حساب، گزارش سنی، سقفِ اعتبار) — بدونِ آن، رسیدِ باطل
    همچنان از مانده کم می‌کرد.
  - **قرینه‌ی اعلامیه پرداخت:** چهار مفهومِ مالیِ مستقل (مبلغ، تخفیف، جمعِ تسویه،
    معادلِ ارزِ پایه)، `establishment`، اسنادِ مرتبط، و `updated_by_id`. **کارمزد
    عمداً اضافه نشد** — معادلِ سمتِ دریافت، کارمزدِ وصولِ چکِ واگذاری است که مفهومِ
    دیگری است.
  - **چاپ از همان رکورد** (`render_receipt`) روی همان موتورِ HTMLِ فاکتور و سند؛
    «مبلغ به حروف» مشتق است و ذخیره نمی‌شود. **و اعلامیه پرداخت هم چاپ گرفت** —
    تا امروز تنها سندِ مالیِ کوبیتا بود که چاپ نمی‌شد.
  - **راس‌گیری** یک سرویسِ محاسباتیِ محض است و هیچ ردیفی نمی‌نویسد.
  - **`Idempotency-Key` بالاخره از رابط می‌رود.** زیرساختش از قبل بود ولی هیچ
    مسیرِ خزانه‌ای کلید نمی‌فرستاد.
  - **۲۱۵۶ تستِ بک‌اند سبز** · ممیزِ صفحه‌ها ۰ خطا · `tsc` و `oxlint` تمیز ·
    زنجیره‌ی مهاجرت **واقعاً** اجرا شد (۰→۰۱۱۱، برگشت به ۰۱۰۸، دوباره ۰۱۱۱) روی
    اسکیمای یک‌بارمصرف، با سنجشِ `relforcerowsecurity` روی جدول‌های تازه.

- **۱۴۰۵/۰۶/۲۰ (2026-09-11) — هویت تاریخی و Trace فاکتور خرید (مهاجرت `0114`):**
  - هویت تأمین‌کننده و خریدار برای فاکتورهای تازه در دو Snapshot از نوع JSONB
    قفل می‌شود؛ چاپ HTML/PDF از همان داده می‌خواند، پس تغییر بعدی نام، شناسه، تلفن
    یا نشانی طرف حساب گذشته را بازنویسی نمی‌کند. فاکتورهای قدیمی با Snapshot خالی
    برای سازگاری از Master فعلی fallback می‌گیرند و مهاجرت تاریخ ساختگی backfill
    نمی‌کند.
  - فهرست خرید مسیر مستقیم به سند حسابداری و شمارش اعلامیه‌های پرداخت فعال دارد؛
    شمارش صرفاً Trace است و در وضعیت تسویه دخالت نمی‌کند. ابطال فاکتور تا ابطال
    رسید انبار یا اعلامیه پرداخت فعال مسدود می‌شود و سپس همان مسیر سند معکوس را دارد.
  - تبدیل واحد و تقسیط خرید ساخته نشدند: موتور اول باید میان همه دامنه‌ها مشترک
    باشد و موتور اقساط فعلی مخصوص دریافت فروش است. مرز هر دو در
    `OPEN_DECISIONS §۲۹–§۳۰` ثبت شد؛ Allocation نیز طبق §۲۰ باز ماند.
  - مهاجرت روی اسکیمای یک‌بارمصرف مسیر `۰→۰۱۱۴→۰۱۱۳→۰۱۱۴` را واقعاً طی کرد؛
    ستون‌ها حاضر، `FORCE RLS` برقرار و `public.alembic_version` پیش و پس از آزمون
    همان `0098` بود.
  - رونوشت فاکتور خرید از یک endpoint بدون نوشتن می‌آید: شماره‌ها، تاریخ، رسیدها،
    پرداخت‌ها و سند حسابداری کپی نمی‌شوند و سهم‌های تسهیم‌شده‌ی تخفیف/اضافات دوباره
    روی سربرگ اعمال نمی‌شوند. سه سناریوی رفتاری Snapshot چاپ، گارد وابستگی پرداخت
    و رونوشت امن را می‌بندند؛ کل سوییت بک‌اند **۲۲۲۱ تست سبز**، TypeScript بدون
    خطا، ممیز صفحات ۰ خطا/۱۴ هشدار قدیمی و `oxlint` بدون خطا با هشدارهای قدیمی است.

- **۱۴۰۵/۰۶/۲۰ (2026-09-11) — فاکتور خرید و رسید مستقل انبار (مهاجرت `0113`):**
  - `PurchaseInvoice` سند تجاری/بدهی است و در گردش جدید بدون `warehouse_id` ثبت می‌شود؛ بنابراین ثبت آن به‌تنهایی موجودی فیزیکی را تغییر نمی‌دهد. ورودی قدیمی دارای انبار برای سازگاری کلاینت‌های قبلی حفظ شده است.
  - `WarehouseReceipt` و `WarehouseReceiptLine` شماره، تاریخ، انبار و ردیف مستقل دارند؛ چند رسید جزئی برای یک فاکتور مجاز است و مجموع تحویل معتبر نمی‌تواند از مقدار ردیف خرید بیشتر شود.
  - `inventory_status` از رسیدهای معتبر مشتق می‌شود. `financial_status` تا ساختِ
    موتور رسمی Settlement/Allocation تسویه‌نشده می‌ماند؛ صرفِ Reference یک
    `PaymentRelatedDocument` دیگر فاکتور را تسویه‌شده نشان نمی‌دهد.
  - شناسنامه، نام، واحد، نرخ/مبلغ مالیات، اضافات و عوارض روی ردیف خرید snapshot می‌شوند تا تغییر Item Master تاریخ را بازنویسی نکند.
  - UI فاکتور خرید تأمین‌کننده را اجباری می‌گیرد، شماره فاکتور تأمین‌کننده را از شماره داخلی جدا نگه می‌دارد، آخرین بهای خرید را نشان می‌دهد، و صدور رسید انبار/اعلامیه پرداخت را از همان فهرست آغاز می‌کند.
  - سابقه‌ی همه‌ی رسیدهای جزئی در جزئیات فاکتور دیده می‌شود و کاربرِ دارای مجوز
    ابطالِ حسابداری می‌تواند رسید اشتباه را از همان‌جا باطل کند. ابطال، حرکت
    جبرانیِ کاردکس می‌سازد، مانده‌ی سالمِ `StockBatch` را صفر می‌کند و مقدارِ
    قابل‌دریافت فاکتور را دوباره باز می‌کند؛ `received_qty` بچ برای رد تاریخی
    حفظ می‌شود.
  - صدور رسید `Idempotency-Key` دارد؛ retry شبکه نه رسید دوم می‌سازد و نه موجودی
    را دوبار بالا می‌برد. **۲۲۱۷ تست بک‌اند سبز**؛ `tsc` و ممیز صفحه‌ها بدون خطا
    (۱۴ هشدار قدیمی) و `oxlint` بدون خطا با هشدارهای قدیمی.
  - زنجیره‌ی مهاجرت روی اسکیمای یک‌بارمصرف واقعاً اجرا شد:
    `۰→۰۱۱۳→۰۱۱۲→۰۱۱۳`. هر دو جدول رسید انبار `RLS + FORCE` دارند و نسخه‌ی
    `public.alembic_version` پیش و پس از آزمون همان `0098` ماند.
  - **هشدارِ بازگشت:** `downgrade`ِ این مهاجرت روی دیتابیسِ دارای داده **شکست
    می‌خورد** — `warehouse_id` را دوباره `NOT NULL` می‌کند، در حالی که کلِ هدفِ
    مهاجرت اجازه‌ی `NULL` بودنش بود. روی اسکیمای خالی رد می‌شود و همان هم آزموده
    شد؛ ولی به‌محضِ ثبتِ اولین فاکتورِ خریدِ بی‌انبار، این مسیر دیگر راهِ فرار
    نیست. مهاجرتِ «فقط رو به جلو» است.

- **۱۴۰۵/۰۶/۱۹ (2026-09-10) — اعلامیه‌ی پرداخت چندابزاری (مهاجرت `0112`):**
  مدل تک‌ردیفیِ قدیمی (`TreasuryTransaction.method = cash|bank`) برای سازگاری حفظ
  شد، اما سربرگ مستقل `payments` بالای اجزای واقعی خزانه قرار گرفت: نقد از صندوق
  موجود، برداشت بانکی با اصل و کارمزدِ جدا، چک پرداختنی از حساب/دسته‌چک موجود، و
  خرج‌کردنِ **همان** چک دریافتنی با رخداد قابل‌ردگیری
  (`payment_cheque_transfers`)؛ در حالت آخر چک دوم ساخته نمی‌شود. هر اعلامیه یک
  سند چندردیفیِ متوازن دارد، حساب‌ها از `system_role` یا انتخاب صریحِ حسابِ قابل
  ثبت می‌آیند، ارز و نرخ از موتور چندارزی موجود اعتبارسنجی می‌شوند، تخفیف جریان
  نقدی نیست، و مبلغ پایه/پرداخت/تسویه/کارمزد مستقل مانده‌اند. برداشت واقعی در
  `bank_transactions` ثبت می‌شود تا مغایرت بانکی آن را ببیند؛ شماره‌ی برگ نیز از
  گارد دسته‌چک می‌گذرد. مسیر `POST /api/payments` با Idempotency-Key، فهرست و جزئیات،
  چک‌های واجد شرایط و ابطال کنترل‌شده اضافه شد. UI مشترک دسکتاپ/وب فرم چندابزاری،
  نرخ روز، جمع‌های توضیح‌پذیر و فهرست مستقل «اعلامیه‌های پرداخت» دارد و جدول‌ها در
  موبایل کارت می‌شوند. هفت تست رفتاری چرخه‌ی چک، سند متوازن، تفکیک کارمزد،
  idempotency، ابطال و مرز Reference/Allocation را می‌بندند.
  - **انطباق با مرز فصل:** سند مرتبط در Payment فقط Reference است و مبلغ تخصیص
    غیرصفر رد می‌شود؛ تصمیم FIFO/تخصیص جزئی/اثر ابطال همچنان در
    `OPEN_DECISIONS §۲۰` مانده است. ابطال از مجوز `accounting/delete` استفاده
    می‌کند و فهرست اعلامیه مسیر مستقیم به سند حسابداری دارد.
- **۱۴۰۵/۰۶/۲۰ (2026-09-11) — شماره‌گذاریِ دوباره‌ی سه مهاجرت، پس از تصادم با `master`:**
  سه فصلِ «رسید دریافت»، «اعلامیه پرداخت» و «رسید انبار» روی شاخه‌ای ساخته شدند
  که هم‌زمان آرش `0109` (تسویه‌ی کارت‌خوان) و `0110` (چرخه‌ی عمرِ چک) را گرفت و
  **روی تولید برد**. پس شماره‌های ما جابه‌جا شدند: `0109→0111`، `0110→0112`،
  `0111→0113`، و زنجیره روی `0110`ی آرش بسته شد.
  - **و یک تصادمِ ستون هم بود که استقرار را می‌شکست:** مهاجرتِ `0110` از قبل
    `checks.sayad_id` و `checks.back_number` را ساخته بود. مهاجرتِ ما دوباره
    می‌ساختشان. حذف شدند و طول‌های آرش مرجع ماندند — **ولی ایندکسِ یکتای جزئیِ
    صیادی مالِ ماست و ماند**: آن مهاجرت ستون را داد و قید را نه، پس یک کدِ صیادی
    می‌توانست روی دو چک بنشیند.
  - **بلوکِ چک از `services/banking.py` به `services/check_ops.py` رفت** (کارِ
    آرش) و افزوده‌های ما (`assert_sayad_free`، `new_check_row`) هم با آن منتقل
    شدند. `banking.py` حالا فقط گردشِ بانکی و تنخواه و صورت‌حساب است.

- **۱۴۰۵/۰۶/۱۹ (2026-09-10) — چک وضعیت داشت ولی نه تاریخچه، نه سندِ واگذاری، و
  نه راهِ نقد کردن (مهاجرت `0110`):** سه شکافِ جدا که همه از یک ریشه می‌آمدند —
  چرخه‌ی عمرِ چک فقط یک ستونِ `status` بود. **(۱)** گذرِ `in_hand → deposited`
  هیچ سندی نمی‌زد و فقط `bank_account_id` را می‌نشاند، پس مبلغِ چک تا لحظه‌ی وصول
  روی «چک‌های دریافتنی» می‌ماند و سؤالِ «چقدر چک در جریانِ وصول داریم؟» از تراز
  جواب نداشت؛ حالا حسابِ تازه‌ی **«چک‌های واگذارشده به بانک»**
  (`checks_in_collection`، کد ۱۱۱۳) طبقه‌بندیِ دوباره می‌گیرد و وصول/واخواست از
  همان بستانکار می‌شوند. **(۲)** هیچ تاریخچه‌ای نبود و `Check` **در رجیستریِ
  `audit.audited_models()` هم نبود** — موجودیتی با غنی‌ترین چرخه‌ی عمرِ خزانه،
  تنها موجودیتی بود که هیچ ردی نمی‌گذاشت؛ حالا `check_events` هر گذر را با
  عملیات، بانک/صندوق/طرف‌حساب، سند و کاربر نگه می‌دارد و **فقط‌افزودنی** است
  (همان گاردِ `audit_log`، که برای این کار پارامتری شد). **(۳)** «نقد کردن»
  وجود نداشت و کاربر مجبور بود «وصول» بزند — که پول را به بانکی می‌برد که هرگز
  چیزی نگرفته بود؛ حالا وضعیتِ `cashed` پول را به صندوق می‌برد. افزون بر این:
  **استردادِ چکِ پرداختنی** باز شد (§۳۱) و تله‌ای که زیرش بود بسته شد — ردیف‌های
  `returned` سمتِ *دریافتنی* hard-code شده بودند، پس بازکردنِ آن گذر سندِ غلط
  می‌زد بی‌آنکه چیزی خطا بدهد. **عملیاتِ گروهی** با نتیجه‌ی جدا برای هر چک
  (§۴۴ §۴۵)، **idempotency** روی همه‌ی گذرها (§۴۸)، **کد صیادی و پشت‌نمره**
  (§۸ §۲۸)، پیامِ خطای فارسی که می‌گوید از این وضعیت چه ممکن است (§۴۳)، و دفترِ
  تازه‌ی **«عملیات چک»** به‌عنوان نمای دومِ دامنه (§۴۰ §۴۱). سندِ چک در
  `entry_source` از `BankTransaction` به `CheckEvent` منتقل شد: آن یکی فقط برای
  *وصول* جواب می‌داد و صدور و واگذاری و واخواست را خالی برمی‌گرداند. منطقِ چک از
  `services/banking.py` به `services/check_ops.py` منتقل شد و اندپوینت‌هایش به
  روترِ خودش. دو تستِ موجود که رفتارِ قبلی را تثبیت کرده بودند بازنویسی شدند.
  بک‌اند ۲۱۳۷ تست سبز؛ `tsc` · `lint` · `audit-pages` بی‌خطا.
  **مستقر شد روی prod همان روز** با `deploy.sh production` (نسخه ۰۱۰۹ ← ۰۱۱۰).
  پیش از استقرار اثرِ مهاجرت سنجیده شد: prod دو چک دارد و هر دو `cleared`، پس
  **هیچ چکِ `deposited`ای نبود** و طبقه‌بندیِ دوباره صفر ردیف را لمس کرد —
  `journal_lines` پیش و پس از استقرار همان ۱۶۴ ردیف ماند و هیچ حسابِ واسطی هم
  ساخته نشد. پس از استقرار: `check_events` با `RLS + FORCE` و تریگرِ
  `check_events_no_update_delete`، شش ردیفِ شمارنده، قیدِ وضعیت شاملِ `cashed`،
  `/api/checks` و `/api/check-operations` هر دو **۴۰۱** (وجود دارند و پشتِ
  احرازند، نه ۴۰۴)، و چک‌سامِ `check_ops.py` و باندلِ وب روی سرور با بیلدِ محلی
  یکی. وبِ demo با `rsync` هم‌گام شد (پشتیبان: `web.bak-20260911_100604`).
  طبقِ قاعده **هرگز** `deploy.sh demo` اجرا نشد.

- **۱۴۰۵/۰۶/۱۹ (2026-09-10) — نیمی از حجمِ APKِ موبایل کدِ نیتیوی بود که روی
  هیچ گوشی‌ای اجرا نمی‌شد:** APKِ انتشار ۱۱۸.۵ مگابایت بود و هر چهار معماری را
  داشت؛ `x86` و `x86_64` روی هم **۵۴.۶ مگابایت** — فقط برای امولاتور. حالا
  افزونه‌ی `withArmOnlyAbis` کلیدِ `reactNativeArchitectures` را روی
  `armeabi-v7a,arm64-v8a` می‌گذارد و خروجی **۶۳.۵ مگابایت** شد. **نکته‌ی
  ماندگارش شکستِ تلاشِ اول است:** نسخه‌ی نخست بلوکِ `ndk { abiFilters = … }` در
  `defaultConfig` می‌گذاشت — بلوک درست تولید می‌شد، بیلد سبز بود، و APK بی‌تغییر
  ۱۱۸.۵ مگابایت می‌ماند، چون افزونه‌ی گریدلِ React Native معماری‌ها را از
  `gradle.properties` می‌خواند و *بعد* از `defaultConfig` رویش می‌نویسد.
  پیکربندی درست به‌نظر می‌رسید و چیزی خطا نمی‌داد؛ **تنها راهِ کشفش اندازه‌گرفتنِ
  خودِ artifact بود، نه خواندنِ پیکربندی.** آزمونِ امولاتور با پرچمِ خودِ RN
  (`-PreactNativeArchitectures=x86_64`) باز می‌ماند.

- **۱۴۰۵/۰۶/۱۹ (2026-09-10) — پولِ کارت‌خوان پیش از رسیدن، روی بانک می‌نشست
  (مهاجرت `0109`):** رسیدِ کارتی از لحظه‌ی کارت‌کشیدن **معینِ بانک** را بدهکار
  می‌کرد. ولی شبکه‌ی پرداخت چند روز بعد و منهای کارمزد واریز می‌کند، پس مانده‌ی
  بانک — که از دفتر می‌آید — **دقیقاً به اندازه‌ی پولِ تسویه‌نشده باد کرده بود**،
  و چون برای واریزِ واقعیِ PSP هیچ `BankTransaction`ی ساخته نمی‌شد،
  **مغایرت‌گیریِ بانکی هرگز نمی‌توانست آن خطِ صورت‌حساب را تطبیق دهد** (تنها ردیفِ
  سیستمی یک «منهای کارمزد» بود). حالا دو مرحله جداست: کارت‌کشی روی حسابِ تازه‌ی
  **وجوهِ در راهِ کارت‌خوان** (`pos_clearing`، کد ۱۱۱۲ — ۱۱۰۸ در قالبِ چارتِ صنفی
  «پیش‌پرداخت‌ها» بود) با **تفصیلیِ خودِ دستگاه** می‌نشیند، و تسویه آن را با سندِ
  «بانک بدهکار / وجوهِ در راه بستانکار» به بانک می‌برد، به‌همراه یک ردیفِ بانکیِ
  مثبتِ خالص برای تطبیق. تسویه از یک *عملیات* به یک **سند** تبدیل شد
  (`pos_settlements`): شماره‌ی بی‌شکافِ خودش، «تسویه تا تاریخ» جدا از تاریخِ
  واریز، فهرست با drill-down، پیش‌نمایشِ رسیدهای منبع پیش از ثبت، ابطال با سندِ
  معکوس، و idempotency روی ثبت. ستونِ `treasury_transactions.settlement_id`
  شکافِ ردیابی را بست — تا امروز تنها حلقه `settlement_txn_id` بود که **فقط وقتی
  کارمزد بزرگ‌تر از صفر بود** پر می‌شد، یعنی تسویه‌ی بی‌کارمزد (حالتِ رایج) هیچ
  ردی نمی‌گذاشت. مهاجرت برخلافِ ۰۱۰۵/۰۱۰۶/۰۱۰۷ **داده می‌نویسد** و باید بنویسد:
  رسیدهای تسویه‌نشده‌ی موجود از معینِ بانک به وجوهِ در راه طبقه‌بندیِ دوباره
  می‌شوند، وگرنه تسویه‌ی آینده‌شان حسابی را بستانکار می‌کرد که هرگز بدهکار نشده
  بود. دو تستِ همکار که رفتارِ قبلی را تثبیت کرده بودند بازنویسی شدند (یکی‌شان
  کنارِ ادعایش نوشته بود «عددها برابرند ولی معناشان یکی نیست» — همان برابری خودش
  نشانه‌ی اشکال بود). بک‌اند ۲۱۰۸ تست سبز؛ `tsc` و `audit-pages` بی‌خطا.
  **مستقر شد روی prod همان روز** با `deploy.sh production` (نسخه ۰۱۰۸ ← ۰۱۰۹).
  پیش از استقرار، اثرِ مهاجرت روی داده‌ی واقعی سنجیده شد: `paid_via IS NOT NULL`
  روی prod **صفر ردیف** داشت، یعنی بخشِ طبقه‌بندیِ دوباره هیچ ردیفی را لمس نکرد و
  هیچ حسابِ واسطی هم ساخته نشد — مهاجرت عملاً فقط اسکیما بود. پس از استقرار:
  `journal_lines` همان ۱۶۴ ردیف، `pos_settlements` با `RLS + FORCE`، شش ردیفِ
  شمارنده برای شش مستأجر، `/api/pos-settlements` و `/pending` هر دو **۴۰۱** (یعنی
  وجود دارند و پشتِ احرازند، نه ۴۰۴)، و چک‌سامِ باندلِ وب و `pos_settlements.py`
  روی سرور با بیلدِ محلی یکی. وبِ demo — که درِ تریال است و به همین بک‌اند وصل
  می‌شود — با `rsync` از باندلِ prod هم‌گام شد (پشتیبان:
  `web.bak-20260911_090727`). طبقِ قاعده **هرگز** `deploy.sh demo` اجرا نشد.

- **۱۴۰۵/۰۶/۱۹ (2026-09-10) — دسته‌چک برگش را کنترل می‌کند (مهاجرت `0108`):**
  `checkbooks` از مهاجرتِ ۰۰۸۴ بود و `checks.checkbook_id` هم — ولی **هیچ‌جا
  سنجیده نمی‌شد**: `create_check` مقدارِ `checkbook_id` را می‌گرفت و بی‌هیچ
  اعتبارسنجی‌ای می‌نشاند. یعنی دو چک با شماره‌ی یکسان از یک دسته هر دو ثبت
  می‌شدند، و شماره‌ی بیرونِ بازه هم پذیرفته می‌شد — که بدتر بود، چون «برگِ مانده»
  مشتق است و آن ردیفِ بی‌ربط هم از آن کم می‌کرد؛ شمارنده دروغ می‌گفت. حالا دو
  لایه دارد: سنجشِ سرویس با پیامِ فارسیِ روشن، و **ایندکسِ جزئیِ یکتای**
  `uq_checks_tenant_book_number` روی `(tenant_id, checkbook_id, number)` که
  تضمینِ واقعی است. برگ‌ها عمداً materialize **نمی‌شوند** — یک دسته‌ی پنجاه‌برگی
  پنجاه ردیف نمی‌سازد؛ وضعیتِ هر برگ از چک‌های وصل‌شده مشتق می‌شود، و همین قاعده‌ی
  «برگِ باطل‌شده آزاد نمی‌شود» را هم می‌سازد: ردیفِ چک می‌ماند پس شماره‌اش تا ابد
  گرفته است. **چکِ پرداختنی حالا حسابِ بانکی‌اش را از دسته می‌گیرد**؛ تا امروز تا
  لحظه‌ی وصول `NULL` می‌ماند و آن‌وقت دوباره از کاربر پرسیده می‌شد، با آنکه دسته از
  روزِ اول می‌دانستش — و وصول از حسابِ دیگری حالا رد می‌شود نه اینکه بی‌صدا ثبت
  شود. سیاستِ `cheque_number_control` روی `tenants` اضافه شد (`off` پیش‌فرض =
  رفتارِ امروز، `book` = چکِ پرداختنی حتماً از دسته)، ولی **بازه و تکراری‌نبودن
  پرچم نیستند**: اگر دسته‌ای انتخاب شود در هر دو حالت سنجیده می‌شوند، چون آن
  یکپارچگیِ داده است نه سیاست. سه چیزِ دیگر: هم‌پوشانیِ بازه‌ی دو دسته‌ی یک حساب
  رد می‌شود (وگرنه یک شماره از دو دسته صادرشدنی بود و ایندکسِ یکتا دور می‌خورد)؛
  **ویرایشِ دسته اضافه شد** (تا امروز فقط باز/بستن و حذف بود، پس غلطِ تایپی تا ابد
  می‌ماند) با قفلِ ساختاری پس از خرج‌شدنِ اولین برگ؛ و `checkbooks
  .cheque_print_format` که خالی یعنی «از حسابِ بانکی ارث ببر» — موتورِ چاپِ چک
  ساخته نشد، فقط ترتیبِ خواندن تثبیت شد. بلوکِ دسته‌چک از `banking.py` به
  `services/checkbooks.py` منتقل شد، مثلِ `cashboxes.py` و `bank_accounts.py` و
  `card_terminals.py`.

- **۱۴۰۵/۰۶/۱۹ (2026-09-10) — کارت‌خوان به تراکنش‌هایش وصل شد (مهاجرت `0107`):**
  اول یک ابهام رفع شد: `pos_terminals` در کوبیتا **خالصاً کارت‌خوانِ بانکی است**،
  نه صندوقِ فروش — `psp`، `transport`، `host`، `port`، `com_port` و درایورهای
  TCP/سریالِ الکترون همه به همان‌اند. «صندوق فروشگاهی» جدول ندارد و از مسیرِ
  فاکتورِ فروش می‌رود؛ رابطه‌شان یک‌طرفه و درست است. پس ادغامی رخ نداده بود.
  اشکالِ واقعی جای دیگری بود: **موجودیت به تراکنش‌هایش وصل نبود.**
  `treasury_transactions` هیچ `pos_terminal_id` نداشت و خودِ `pos_terminals`
  ستونِ `terminal_no` نداشت — تنها حلقه‌ی اتصال رشته‌ای بود که کاربر در تسویه
  دستی تایپ می‌کرد و با `==` مقایسه می‌شد؛ یک فاصله‌ی اضافی یعنی صفر نتیجه بدونِ
  هیچ خطایی. حالا کلیدِ خارجی هست، شماره روی خودِ دستگاه می‌نشیند (یکتا وقتی پر
  باشد، با ایندکسِ جزئی تا دستگاه‌های بی‌شماره‌ی موجود نشکنند)، و تسویه دستگاه را
  **از فهرست** انتخاب می‌کند. رسیدِ کارتی هم حسابِ تسویه را از خودِ دستگاه
  می‌گیرد (§۴)؛ اگر کلاینت حسابِ ناهمخوان بفرستد رد می‌شود، نه اینکه بی‌صدا یکی
  ترجیح داده شود. «موجودیِ دستگاه» اضافه شد و **مشتق** است: جمعِ رسیدهای کارتیِ
  تسویه‌نشده — عمداً از خزانه، نه از دفتر، چون ناخالص از لحظه‌ی رسید روی بانک
  نشسته و خواندنش از دفتر همان پول را دو بار می‌شمرد. ارزِ دستگاه باید با ارزِ
  حسابِ تسویه بخواند (§۹، رد می‌شود نه هشدار). سه اشکالِ بی‌صدا هم بسته شد:
  PATCH شِمای کامل می‌گرفت و پیش‌فرضِ `is_active=True` را می‌نشاند، پس **ویرایشِ
  نامِ یک دستگاهِ غیرفعال بی‌صدا فعالش می‌کرد** (و اصلاً دکمه‌ی غیرفعال‌سازی در
  رابط نبود)؛ `settled_at` و `cashbox_id` در `_to_out` جا افتاده بودند و همیشه
  `null` برمی‌گشتند؛ و اگر درایور RRN برنمی‌گرداند، رسید ۴۲۲ می‌خورد **بعد از
  اینکه کارتِ مشتری کشیده شده بود** — حالا پیش از تماس با سرور پیامِ روشن داده
  می‌شود. حذفِ دستگاهِ استفاده‌شده هم مسدود شد (§۲۱)؛ تا امروز هیچ سنجشی نداشت
  چون کلیدِ خارجی وجود نداشت و پایگاه‌داده هم جلویش را نمی‌گرفت.

- **۱۴۰۵/۰۶/۱۹ (2026-09-10) — موبایل فازِ ۶: صفِ آفلاین ساخته شده بود ولی به هیچ صفحه‌ای وصل نبود (بدونِ مهاجرت):**
  فازِ ۳ صفِ نوشتنِ آفلاین را ساخت — ۲۰۰ خط، با تست، با کلیدِ idempotency — و
  `MOBILE_PLAN.md` نوشت «ثبتِ فاکتور و دریافت/پرداخت حالا آفلاین کار می‌کنند».
  **این ادعا در اپِ منتشرشده درست نبود.**
  - **`enqueue` هیچ صدازننده‌ای نداشت.** `NewInvoiceScreen` و `TreasuryScreen`
    مستقیم `apiPost` می‌زدند. یعنی ویزیتورِ بی‌آنتن دقیقاً مثلِ قبل کارش را از
    دست می‌داد، و `OutboxBadge` که در `App.tsx` رندر می‌شد هرگز چیزی نشان
    نمی‌داد چون صف همیشه خالی بود.
  - **و ممیز چرا نگرفت:** الگوی `R2` فقط `export const|function|class` را
    می‌دید. `enqueue`, `discard`, `clearOutbox` و `discardDraft` همه
    `export async function` بودند — نامرئی. یک `(?:async )?` اختیاری در regex،
    و چهار موردِ پنهان بیرون آمد.
  - **`drain()` بی‌توکن صدا زده می‌شد.** صف `fetch`ِ خام می‌زد و توکن را
    پارامتری می‌گرفت، ولی `App.tsx` سرِ بازگشتِ شبکه `drain()` را بدونِ آرگومان
    صدا می‌زد. یعنی حتی اگر صف وصل بود، هر تلاشِ خودکار ۴۰۱ می‌گرفت و کار تا ابد
    می‌ماند. حالا از `apiPost`ِ مشترک رد می‌شود: توکن، رفرشِ ۴۰۱، و `detail`ِ
    فارسیِ خطای سرور — هر سه رایگان.
  - **بن‌بستِ موردِ ردشده باز شد.** پیش از این همه‌ی ردها یک جمله‌ی یکسان
    می‌گرفتند، معلوم نبود کدام سند بود، و `discard` را هیچ صفحه‌ای صدا نمی‌زد —
    پس مورد تا ابد می‌ماند و نوار **همیشه** قرمز بود. صفحه‌ی «صفِ ارسال» حالا
    مبلغ و تاریخ و **پیامِ خودِ سرور** را نشان می‌دهد، با تلاشِ دوباره و حذف.
  - **ردِ سرور سرِ ثبت پرتاب می‌شود، نه پارک.** کاربری که همین حالا دکمه را زده
    جلوی گوشی ایستاده و می‌تواند اشکال را درست کند؛ پارک‌کردن یعنی فرم بسته شود
    و کار به فهرستی برود که باید بعداً کشفش کند. پارک فقط برای `drain`ِ پس‌زمینه.
  - **تایپِ بدنه برنگشت — برگردانده شد.** انتقال به `enqueue(kind, body: unknown)`
    کنترلِ نوعِ بدنه را از بین برد؛ یک نامِ فیلدِ اشتباه دیگر کامپایل را قرمز
    نمی‌کرد. با `OutboxBodies`/`OutboxResponses` جنریک شد و همان لحظه هشت جای
    تست را که بدنه‌ی واقعی نداشتند قرمز کرد.
  - **ثبتِ دریافت از خودِ صفحه‌ی شخص.** تنها کنشِ آن صفحه اشتراکِ PDF بود؛
    ویزیتور مانده را می‌دید و باید برمی‌گشت به خانه و همان مشتری را دوباره پیدا
    می‌کرد. نوعِ کنش از **علامتِ مانده** می‌آید نه از فرض.
  - **یک ادعای غلطِ خودم:** گفتم `PushGate` هم وصل نیست؛ نبود — در
    `RootNavigator` خطِ ۷۸ رندر می‌شود. در `App.tsx` دنبالش گشته بودم.
  - راستی‌آزمایی: **۱۶۴ تستِ موبایل** (۱۹ تای صف)، tsc پاک، ممیز بدونِ خطا.
    درستیِ تست‌ها با شکستنِ عمدیِ کلیدِ idempotency سنجیده شد — دو تستِ درست قرمز
    شدند.
  - **هشدارِ انتشار:** این صف روی `Idempotency-Key` تکیه دارد و آن کدِ بک‌اند
    هنوز روی production نرفته. تا پیش از استقرار نباید منتشر شود، وگرنه یک قطعیِ
    شبکه می‌تواند دو سند ثبت کند.

- **۱۴۰۵/۰۶/۱۹ (2026-09-10) — حساب بانکی هم تفصیلی گرفت؛ مانده‌اش تا امروز محاسبه‌شدنی نبود (مهاجرت `0106`):**
  `bank_accounts` از مهاجرتِ ۰۰۰۳ ساخته شد و **هیچ ALTERی نخورده بود**. شش ستون
  داشت، و مهم‌تر از هر ستونِ نبوده این بود که `analytic_id` نداشت: همه‌ی حساب‌های
  بانکی روی همان معینِ «بانک» می‌نشستند و در دفتر هیچ چیزی نبود که بگوید کدام
  ریال مالِ کدام بانک است. یعنی «مانده‌ی بانک سامان» نه سخت، بلکه **غیرقابلِ
  محاسبه** بود؛ و «کارتِ حساب» هم بی‌صدا گردشِ *همه‌ی* بانک‌ها را نشان می‌داد.
  حالا جفتِ `(gl_account_id, analytic_id)` هویتِ حسابداریِ حساب است: هر پنج نقطه‌ای
  که به پولِ بانک سند می‌زند (رسید، پرداخت، وصولِ چک، واریز/برداشتِ دستی، کارمزدِ
  کارت‌خوان) همان جفت را روی ردیف می‌گذارد و `bank_accounts.balance()` با همان
  جفت می‌خواند. **هیچ ستونِ مانده‌ای ساخته نشد** — و یک تست ثابت می‌کند جمعِ
  حساب‌ها با مانده‌ی معین در دفتر یکی است. `NULL` معنا دارد («تفکیک‌نشده») پس
  ردیف‌های مستقر دست نخوردند و backfill لازم نشد؛ فقط *یک* حساب می‌تواند
  تفکیک‌نشده بماند. به‌علاوه: عنوان دوم، شعبه، نوع حساب، شماره کارت، ارز، تاریخ
  افتتاح، نام صاحب حساب، فرمت چاپ چک، و **مبلغ بلوکه** که ذخیره می‌شود ولی هرگز
  به دفتر نمی‌خورد — «قابل استفاده» از آن مشتق می‌شود. صفحه‌ی «فهرست حساب‌های
  بانکی» حذف و در صفحه‌ی تعریف ادغام شد (دو نمای یک داده بود). سه اشکالِ بی‌صدا
  هم بسته شد: `GET /api/bank-transactions` پارامترِ `bank_account_id` را تعریف
  نکرده بود و بی‌صدا دور می‌ریخت (صافیِ صفحه‌ی «مرور عملیات بانکی» بی‌اثر بود)،
  تسویه‌ی خودکارِ بازارگاه حسابِ غیرفعال را هم برمی‌داشت، و مسیرِ پولِ بانکی
  `is_active` را اصلاً نمی‌سنجید. و گاردِ §۳۱ به **صندوق هم** اضافه شد: تفصیلیِ
  موجودیتِ باسابقه عوض نمی‌شود، چون مانده را بی‌صدا به ردیف‌های دیگری می‌بُرد.

- **۱۴۰۵/۰۶/۱۸ (2026-09-09) — صندوق یک موجودیت شد؛ تا امروز یک صافی بود (مهاجرت `0105`):**
  مرورِ «تعریف و مدیریت صندوق»ی سپیدار. برخلافِ سه فصلِ قبل که ممیزی نشان داد
  تقریباً همه‌چیز هست، این‌جا `grep -rn "cashbox" backend/app/` **صفر نتیجه** داد.
  - **آنچه بود:** تنها تعریفِ «صندوق» در کلِ بک‌اند یک
    `if method == "cash": return get_account(cc.CASH)` بود، و در رابط یک
    `filter(t => t.method === 'cash')`. یعنی صندوق *همان* حسابِ معین بود — دقیقاً
    یکی‌گرفتنی که §۷ منع می‌کند — و **بیش از یک صندوق ممکن نبود**. هیچ‌کدام از
    هشت موردِ چک‌لیستِ §۲۷ (عنوان دوم، تفصیلی، ارز، نرخ، موجودیِ سالانه، تاریخِ
    افتتاح، فعال/غیرفعال، مانده‌ی مشتق) جایی برای نشستن نداشت.
  - **الگویش از قبل در همان ماژول بود:** `BankAccount` و `PosTerminal` هر دو
    موجودیتِ عملیاتی‌اند که با یک کلید به حسابداری وصل می‌شوند. صندوق تنها چیزی
    بود که این الگو را نگرفت — پس این تعمیم بود، نه اختراع.
  - **مانده ستون ندارد و عمدی است.** مانده‌ی صندوق از **دفتر** مشتق می‌شود:
    مانده‌ی `(حسابِ صندوق، تفصیلیِ همان صندوق)`. دو سود دارد که هیچ‌کدام تصادفی
    نیستند — هر مسیری که به نقد دست بزند شمرده می‌شود (نه فقط رسید و پرداخت)، و
    **صندوق نمی‌تواند با تراز واگرا شود**، چون یک عددند. یک تست همین را قفل
    می‌کند: جمعِ صندوق‌ها = مانده‌ی حسابِ صندوق در دفتر.
  - **موجودیِ اولیه هم جدول ندارد** (§۱۲): موجودیِ اولِ سالِ N همان مانده‌ی مشتق
    در آستانه‌ی شروعِ آن سال است. جدولِ «سال مالی | موجودی» یعنی دو منبع برای یک
    پول — همان چیزی که §۱۳ و خودِ پروژه منعش می‌کنند. کوبیتا از قبل مانده‌ی اول
    دوره را با سند می‌زند و افتتاحیه‌ی هر سال تفصیلی را منتقل می‌کند.
  - **بدونِ backfill.** صندوقِ پیش‌فرض `analytic_id = NULL` دارد و مانده‌اش
    ردیف‌های بی‌تفصیلیِ حسابِ صندوق است — یعنی دقیقاً داده‌ی امروز. پس مهاجرت هیچ
    ردیفی نمی‌نویسد (قاعده‌ی RLS) و هیچ داده‌ی مستقری دست نمی‌خورد.
  - گاردها: عملیات پیش از `opening_date` رد می‌شود (§۱۴)، صندوقِ غیرفعال انتخاب
    نمی‌شود ولی سوابقش می‌ماند (§۱۶)، و صندوقِ استفاده‌شده حذف نمی‌شود بلکه
    غیرفعال می‌شود (§۱۷ — الگوی `delete_checkbook`).
  - §۲۱ رعایت شد: **جمعِ کلِ صندوق‌ها بینِ ارزها نمایش داده نمی‌شود** — جمع فقط
    درونِ هر ارز.
  - ۱۸ تستِ تازه در `test_cashbox.py`.

- **۱۴۰۵/۰۶/۱۸ (2026-09-09) — خزانه: خرج کردنِ چک راهِ رفت داشت و راهِ برگشت نه (بدونِ مهاجرت):**
  مرورِ «دریافت و پرداخت»ی سپیدار یک فصلِ **نقشه** بود، نه دستورِ ساخت. ممیزی نشان
  داد **هر ۲۷ مفهومِ §۲۷ از قبل هست** — از جمله هر هفت موردی که §۳۲ «شاید لازم به
  توسعه» می‌نامد: کارت‌خوان و تسویه‌اش، دسته‌چک، تنخواه‌دار و صورت هزینه‌اش،
  صورت‌حساب بانکی، مرور عملیات بانکی، و ماشینِ وضعیتِ چک. هجده عملیاتِ منو کامل‌اند.
  - **شکافِ واقعی، دو لبه‌ی گمشده در گرافِ چک بود.** `RECEIVABLE_TRANSITIONS`
    کلیدی برای `endorsed` نداشت، یعنی چکی که خرج شده بود **برای همیشه قفل**
    می‌ماند و «برگشت از خرج کردن» (§۱۷) ناممکن بود. `deposited` هم راهِ بازگشت
    نداشت — با آن‌که کامنتِ خودِ جدول و متنِ صفحه‌ی «استرداد چک» هر دو وعده‌اش را
    می‌دادند («چکی که به بانک واگذار شده اول باید برگردد»). رابط چیزی را می‌گفت که
    بک‌اند نمی‌پذیرفت.
  - **و بدتر: خرج کردنِ چک هیچ رابطی نداشت.** بک‌اند `in_hand → endorsed` را با
    سندِ درست می‌پذیرفت، ولی هیچ صفحه‌ای این کنش را نمی‌فرستاد — `endorsed` فقط
    یک برچسب و یک رنگ بود. یعنی مسیرِ سومِ §۱۶ عملاً در دسترس نبود.
  - حالا هر سه کنش هست: **خرج کردن**، **برگشت از خرج**، **بازگشت از بانک**؛
    به‌علاوه‌ی بخشِ «خرج‌شده‌ها» که تا امروز هیچ‌جا دیده نمی‌شد.
  - **قیدی که تست‌ها نگه می‌دارند:** رفت‌وبرگشت اثرِ خالص ندارد. سندِ «برگشت از
    خرج» دقیقاً معکوسِ سندِ خرج است و مانده‌ی «اسناد دریافتنی» و «حساب‌های
    پرداختنی» به نقطه‌ی اول برمی‌گردد. «بازگشت از بانک» **هیچ سندی نمی‌زند**، چون
    خودِ واگذاری هم نزده بود.
  - **مهاجرت لازم نبود:** `in_hand` از قبل در `CHECK_STATUSES` بود؛ این‌ها لبه‌اند
    نه حالتِ تازه.
  - §۲۲/§۲۳ (تخصیصِ رسید به فاکتور) و §۴ (رسیدِ چندابزاری) عمداً ساخته نشدند و در
    `OPEN_DECISIONS.md` ثبت شدند — خودِ فصل می‌گوید جزئیات را به فصل‌های اختصاصی
    واگذار کنید.
  - ۱۱ تستِ تازه در `test_check_lifecycle.py`.

- **۱۴۰۵/۰۶/۱۸ (2026-09-09) — بررسیِ یکپارچگی و نماهای ذخیره‌شده (بدونِ مهاجرت):**
  دو بندِ باقی‌مانده‌ی فصلِ گزارش‌ها. §۲۷ سرویسِ تازه‌ی `services/integrity.py` است و
  §۳۵ **هیچ کدِ بک‌اندی ندارد** — `SavedReport` از قبل با CRUDِ کامل روی
  `/api/company/reports` وجود داشت و ساختنِ جدولِ دوم برای همان کار، همان «دو
  نمای یک داده»ای بود که پروژه منعش می‌کند.
  - **پنج بررسی، و هر پنج‌تا چیزی را می‌بینند که هیچ گاردِ دیگری نمی‌بیند.**
    اعتبارسنجیِ توازن در `schemas/accounting.py` است، یعنی فقط سرِ راهِ سندِ
    *دستی*؛ سرویس‌ها ردیف را مستقیم می‌سازند و از آن رد نمی‌شوند. پس سندِ
    نامتوازنی که یک سرویس بسازد امروز هیچ‌جا صدا نمی‌کند.
  - **بی‌صداترین یافته: ردیفِ سند روی حسابِ گروه.** `get_balances` فقط
    `is_group = False` را می‌آورد و `routers/journal.py` هیچ گاردی روی `is_group`
    ندارد — پس مبلغی که روی یک سرفصل بنشیند **از تراز غیب می‌شود** در حالی که
    خودِ دفتر متوازن می‌ماند. تراز ناتراز می‌شود و هیچ‌جا نمی‌گوید چرا. طبقِ
    «گزارش، نه گارد» ثبتش مسدود *نشد*؛ حالا فقط دیده می‌شود.
  - **`trial_vs_ledger` عمداً دو پیاده‌سازیِ متفاوت را مقایسه می‌کند:** تجمیعِ
    `GROUP BY`ِ `get_balances` (که بازه را دو تکه می‌کند) در برابر جمعِ
    ردیف‌به‌ردیفِ `get_general_ledger` در پایتون. هزینه‌اش یک خواندنِ دفتر به‌ازای
    هر حسابِ دارای گردش است و پذیرفته شد: مقایسه‌ای که خودش را دوباره پیاده کند،
    چیزی را که باید بسنجد نمی‌سنجد.
  - **میان‌بُرِ دفترِ برگ.** `get_general_ledger` برای حسابِ سطحِ آخر دیگر کلِ چارت
    را برای پیمودنِ درخت نمی‌خوانَد. سودش فقط برای این گزارش نیست — «کارتِ حساب»
    همان مسیر را باز می‌کند و تقریباً همیشه روی حسابِ برگ است.
  - **نمای ذخیره‌شده، بازه را به‌صورتِ پیش‌فرض نگه می‌دارد نه تاریخِ حل‌شده.** نمایی
    که «امسال» را ذخیره کند سالِ بعد هم درست است؛ نمایی که تاریخِ ثابت ذخیره کند
    فردا دروغ می‌گوید — همان دلیلی که `SavedReport` تعریف را ذخیره می‌کند نه نتیجه.
    ردیف‌ها با پیشوندِ `view:` از ردیف‌های «گزارش‌ساز» جدا می‌مانند.

- **۱۴۰۵/۰۶/۱۸ (2026-09-09) — گزارش‌ها: هسته یکی بود، ولی فقط تاریخ می‌فهمید (بدونِ مهاجرت):**
  مرورِ «گزارش‌های سیستم حسابداری»ی سپیدار ۳۶ بند داشت و بیشترش از قبل برآورده بود.
  §۲۵ («سه موتورِ مالیِ جدا نساز») از روزِ اول رعایت شده: `get_balances` همان هسته‌ی
  مشترک است و شش عددی می‌دهد که تراز دو/چهار/شش/هشت‌ستونی نمایش‌های مختلفِ آن‌اند.
  `JournalLine` هم ارز و پیگیری و تفصیلی و مرکز هزینه را از قبل داشت.
  - **ولی هسته فقط `date_from`/`date_to` می‌گرفت.** محدوده‌ی شماره‌ی سند (§۸)،
    ورود/عدمِ ورودِ افتتاحیه و اختتامیه (§۹)، موقت/دائم (§۳۰)، منشأ (§۳۱)، مرکز
    هزینه (§۳۲) و تفصیلی (§۲۹) در **هیچ‌کدام** از سه گزارش نبودند — با آن‌که
    `routers/journal.py` همه‌شان را برای فهرستِ اسناد داشت. حالا `ReportFilters`ِ
    مشترک به `get_balances` و `get_general_ledger` هر دو می‌رود، پس اضافه‌شدنِ یک
    فیلتر هر سه گزارش را با هم کامل می‌کند — همان سودی که §۲۵ وعده داده بود.
  - **§۲۶ حالا تست دارد.** مهم‌ترین تستِ این کار مقایسه‌ی دو مسیرِ *مستقل* است:
    تجمیعِ `get_balances` در برابر جمعِ ردیف‌به‌ردیفِ `get_general_ledger`. اگر روزی
    واگرا شوند، همان‌جا قرمز می‌شود.
  - **هیچ عددی دیگر بن‌بست نیست** (§۶ §۱۱ §۲۸). زنجیره: تراز/مرور حساب ← کارتِ
    حساب ← خودِ سند. دو حلقه‌ی اولش از قبل ساخته شده بود (`AccountLedgerDrawer`)
    و فقط وصل نبود؛ حلقه‌ی آخر `GET /api/journal-entries/{id}` بود که **وجود
    نداشت** — تنها راهِ دیدنِ یک سند، یافتنش در فهرست بود.
  - **`EntryCard` بیرون کشیده شد.** «یک سند با ردیف‌هایش» فقط داخلِ `DaybookCard`
    وجود داشت؛ برای درایو نسخه‌ی دوم لازم می‌شد که یعنی دو نمای یک داده. حالا هر
    دو از یک قطعه می‌خوانند.
  - **ارز و پیگیری در دفتر دیده می‌شوند** (§۱۲ §۱۳ §۲۱) — از قبل روی ردیفِ سند
    بودند و هیچ گزارشی نشانشان نمی‌داد. جمعِ ارزی به تفکیکِ ارز می‌آید، چون دلار و
    یورو جمع‌شدنی نیستند. ستون‌ها فقط وقتی ظاهر می‌شوند که ردیفی مقدار داشته باشد.
  - **دفترِ تفصیلی اضافه شد** (§۲۰): تبِ چهارم، بدونِ حسابِ اجباری — گردشِ یک تفصیلی
    در همه‌ی حساب‌ها.
  - **«بی‌گردش» از «مانده صفر» جدا شد** (§۱۸). این دو از نظر حسابداری یکی نیستند و
    تا امروز اولی اصلاً در گزارش نمی‌آمد. `has_activity` روی هر ردیف، و فیلترِ
    نوعِ مانده (§۱۷) با پنج حالت.
  - **پیش‌فرضِ هر فیلترِ تازه «چیزی عوض نمی‌شود» است** و یک تست همین را قفل می‌کند.
  - ۲۱ تستِ تازه در `test_report_filters.py`.

- **۱۴۰۵/۰۶/۱۸ (2026-09-09) — بستنِ سود و زیان: ابعادِ ردیف، و جداشدنِ بستن از قفل (بدونِ مهاجرت):**
  مرورِ «بستن حساب‌های سود و زیانی»ی سپیدار بیست‌ونه بند داشت. کوبیتا این قابلیت را
  **داشت** — ولی نه جایی که اسمش را دارد: `close_period` همان بستنِ سود و زیان است.
  بیشترِ بندها از قبل برآورده بودند (مبلغ از گردش می‌آید نه از دستِ کاربر، جهت از
  ماهیتِ حساب، مقصد با `system_role` نه با کد، جدایی از سندِ اختتامیه، و «دستیار
  پایان سال»). سه چیز می‌لنگید.
  - **ابعادِ ردیف حذف می‌شدند — همان اشکال، بارِ چهارم.** مانده از
    `_leaf_account_totals` می‌آمد که فقط روی `Account.id` گروه می‌کند، پس ردیفِ
    سندِ بستن نه تفصیلی داشت نه مرکز هزینه. در `hybrid` حساب صفر می‌شد و
    «فروش داخلی / شرکت آلفا» مانده‌دار می‌ماند — و چون درآمد و هزینه به سالِ بعد
    منتقل نمی‌شوند، آن مانده **هیچ‌وقت** بسته نمی‌شد. در `strict` بدتر:
    `make_journal_entry` سندِ بی‌تفصیلی را رد می‌کرد و **بستنِ دوره اصلاً انجام
    نمی‌شد**. حالا `pnl_balances` روی هر سه بُعد گروه می‌کند و `period_close` در
    `DIMENSION_COPYING_SOURCES` نشسته.
  - **نمونه‌ی پنجمِ همان اشکال در `fiscal_year.carry_forward` بود** و همان‌جا درست
    شد: به‌جای تراز آزمایشی از `_permanent_balances`ِ بُعدآگاه می‌خواند.
  - **بستن و قفلِ دوره یک دکمه بودند.** یک کلیک هم سند را می‌زد، هم دوره را قفل
    می‌کرد، هم اسنادِ موقتِ بازه را دائم می‌کرد — و قفل **برگشت ندارد** (روتر نه
    حذف دارد نه بازگشایی). حالا دو گام است: `POST /api/accounting/pnl-close` فقط
    سند را می‌زند (موقت، چرخه‌ی عمرِ عادی) و قفل گامِ دومِ صریح است.
  - **یک پیاده‌سازی، دو ورودی.** `close_period` دیگر ردیف نمی‌سازد؛
    `accounting_ops.issue_pnl_close` را صدا می‌زند و اگر سندِ بستن از قبل باشد،
    سندِ دوم **نمی‌زند** و سود/زیان را از خودِ همان سند می‌خواند. کامنتِ روتر که
    می‌گفت «دو پیاده‌سازیِ موازیِ یک سند نباشد» سرِ جایش است.
  - **§۲۰ و §۲۱ گاردِ تازه نخواستند.** چون پیش‌نمایش همیشه از خودِ دفتر می‌خواند،
    سندِ بستنِ قبلی داخلِ بازه دیده می‌شود و مانده را صفر می‌کند: بستنِ دوباره‌ی
    بی‌فعالیت خودبه‌خود رد می‌شود، و بستن پس از فعالیتِ تازه فقط **همان تفاوت** را
    می‌بندد.
  - **مقصد عمداً «سود و زیان انباشته» ماند** (§۱۲) — دلیلش در `OPEN_DECISIONS.md`.
  - **پیش‌نمایش کامل شد** (§۲۵ §۲۷): تفصیلی و مرکز هزینه‌ی هر ردیف، حسابِ مقصد،
    جمعِ بدهکار و بستانکار، و اختلاف که تا صفر نشود دکمه‌ی صدور فعال نیست.
  - بیست تستِ تازه در `test_pnl_close_dimensions.py`؛ ۵۸ تستِ موجودِ بستن و سال
    مالی **بدونِ تغییر** سبز ماندند.

- **۱۴۰۵/۰۶/۱۸ (2026-09-09) — ویجتِ صفحه‌ی خانه‌ی اندروید (بدونِ مهاجرت، بدونِ تغییرِ بک‌اند):**
  «نبضِ کسب‌وکار» روی صفحه‌ی خانه — فروشِ ۳۰ روز، سود/زیانِ دوره و شمارِ هشدارها.
  تنها بخشی از محصول که **بدونِ باز کردنِ اپ** کار می‌کند.
  - **ویجت به API وصل نیست، و این تصمیم است نه میان‌بر.** راهِ بدیهی این بود که
    کوتلین خودش درخواست بزند؛ سه هزینه داشت: توکنِ JWT از `expo-secure-store` به
    نیتیو می‌رفت، منطقِ رفرشِ ۴۰۱ و آفلاین در کوتلین تکرار می‌شد، و از همه بدتر
    **یک عدد دو پیاده‌سازیِ مستقل پیدا می‌کرد** که با هم واگرا می‌شوند. به‌جایش اپ
    یک فایلِ کوچک می‌نویسد و ویجت همان را می‌خواند.
  - **مسیر حدس زده نشد.** روی دستگاه بررسی شد که `Paths.document`ِ
    expo-file-system دقیقاً `context.filesDir` است
    (`/data/data/ir.cubita.app/files/` — همان‌جا که `query-cache.json`ِ فازِ ۳
    می‌نشیند)، پس کوتلین بدونِ هیچ پلِ نیتیوی می‌خواندش.
  - **کهنگی پنهان نمی‌ماند.** ویجت به‌اندازه‌ی آخرین باز شدنِ اپ تازه است،
    به‌علاوه‌ی بیدارباشِ نیم‌ساعته‌ی اندروید (`updatePeriodMillis` کمینه‌ی خودِ
    سیستم است — عددِ کوچک‌تر بی‌صدا به همین گرد می‌شود). پس زمانِ عکس روی خودِ
    ویجت نوشته می‌شود: «به‌روزرسانی: امروز ۱۰:۳۰». همان قاعده‌ی سقفِ ۲۴ ساعتِ کش.
  - **نبودِ داده «—» است نه «۰».** ویجتی که پیش از اولین ورود «۰ ریال» بدهد دارد
    دروغ می‌گوید: کاربر می‌فهمد فروش صفر بوده، نه اینکه هنوز داده‌ای نیست.
  - **تاگلِ «نمایشِ مبالغ در ویجت».** ویجت روی صفحه‌ی خانه است و **قفلِ بیومتریکِ
    اپ را دور می‌زند**. ولی گره‌زدنش به آن قفل هم جواب نمی‌داد، چون قفل خودکار
    است (هر گوشیِ اثرانگشت‌دار) و ویجت روی تقریباً هر گوشیِ امروزی بی‌عدد می‌شد.
    هیچ‌کدام را نمی‌شد به‌جای کاربر تصمیم گرفت؛ تاگلِ صریح در «بیشتر»، پیش‌فرض
    روشن، با توضیحِ «ویجتِ صفحه‌ی خانه قفلِ اپ را ندارد» زیرش.
  - **عکس سرِ خروج پاک می‌شود**، مثلِ کشِ آفلاین و پیش‌نویسِ انبارگردانی — وگرنه
    گوشیِ دستِ همکار، گردشِ مالیِ کسب‌وکارِ قبلی را جایی نگه می‌دارد که هیچ قفلی
    ندارد.
  - **مبالغ رشته می‌مانند.** مبلغِ ریالی از `Number.MAX_SAFE_INTEGER` رد می‌شود و
    تبدیل به عدد رقمِ آخر را بی‌صدا گرد می‌کند — روی ویجتی که کسی با چیزی
    مقایسه‌اش نمی‌کند، این خطا هرگز دیده نمی‌شد. یک تست قفلش می‌کند.
  - **و یک باگ که خودِ «اصلاح» ساختش.** برای ته‌نشین‌کردنِ زمانِ به‌روزرسانی یک
    `<View>`ِ فاصله‌گیر اضافه شد و **کلِ ویجت از کار افتاد** — روی صفحه‌ی خانه
    کارتِ سفیدِ «Can't load widget». علتش این است که `RemoteViews` یک layout
    معمولی نیست: لانچر آن را در پروسه‌ی خودش باد می‌کند و فقط کلاس‌هایی را
    می‌پذیرد که `@RemoteView` دارند؛ `<View>`ِ ساده در آن فهرست نیست. حالتِ شکست
    بی‌رحم است — **نه استثنا، نه کرش، نه ردِ پشته**، فقط یک خطِ هشدارِ
    `W AppWidgetHostView: Error inflating RemoteViews` در logcat. tsc، jest و
    gradle همه سبز می‌مانند و APKِ خرابِ امضاشده ساخته می‌شود. تنها راهِ فهمیدنش
    نگاه‌کردن به صفحه‌ی خانه بود. اصلاح: وزن روی خودِ سطرِ هشدار به‌جای
    فاصله‌گیرِ جدا (صفر view اضافه)، و **قاعده‌ی تازه‌ی `R5` در
    `audit-mobile.mjs`** که فهرستِ مجاز را می‌سنجد — با برگرداندنِ عمدیِ باگ
    آزموده شد و خطِ درست را با خروجِ ۱ گرفت.
  - **بقای `prebuild`:** منبعِ حقیقت `mobile/widget/` است (بیرونِ `android/`) و
    `plugins/withHomeWidget.js` هر بار سورسِ کوتلین، منابع و `<receiver>` را
    تزریق می‌کند — همان درسی که سرِ کلیدِ امضا (`restore-signing.mjs`) و مانیفستِ
    دوربین (`withOptionalCamera.js`) گرفته شد. روی یک چرخه‌ی واقعیِ prebuild
    راستی‌آزمایی شد.

- **۱۴۰۵/۰۶/۱۷ (2026-09-08) — مقایسه‌ی آمارِ موبایل با وب روی داده‌ی واقعی؛ عددها درست بودند، دو چیزِ دیگر نه (بدونِ مهاجرت):**
  با حسابِ سوپرادمین روی production هر سه را کنارِ هم گذاشتیم: پاسخِ خامِ API،
  وب‌اپ، و اپِ موبایلِ منتشرشده.
  - **دقتِ عددها تأیید شد.** فروشِ کل، سود ناخالص، حاشیه، تعداد فاکتور، میانگین،
    فروشِ ۳۰ روز، جمعِ درآمد و هزینه، سودِ خالص، و ارزشِ موجودی — همه مو‌به‌مو با
    API یکی بودند. منطقی هم هست: هر دو پلتفرم **دقیقاً یک اندپوینت** را می‌خوانند
    (`/api/sales-invoices/summary` و `/api/reports/dashboard`)، پس چیزی که می‌توانست
    غلط باشد نگاشتِ فیلد به برچسب بود، نه خودِ عدد.
  - **ولی صفحه‌ی خانه‌ی موبایل مهم‌ترین عدد را نداشت.** وب در خانه‌اش
    «سود/زیان دوره جاری» را نشان می‌دهد (`GuidedDashboard`، از
    `income_statement.net_profit`)؛ موبایل فقط «سود ناخالص» داشت. روی همین حسابِ
    واقعی یعنی گوشی **۵۹٬۰۰۰٬۰۰۰ سبز** نشان می‌داد و وب در همان لحظه
    **۳۲۶٬۵۰۰٬۰۰۰− قرمز** — هر دو درست، چون ناخالص فقط فروش منهای بهای تمام‌شده
    است و حقوق و اجاره و استهلاک در آن نیستند. برای اپی که کارش «نبضِ کسب‌وکار در
    حرکت» است، نبودنِ عددی که می‌گوید کسب‌وکار سود می‌دهد یا زیان، شکافِ واقعی بود.
    حالا کارتِ تمام‌عرضِ «سود/زیانِ دوره» اضافه شده (سبز/قرمز بر پایه‌ی علامت،
    با توضیحِ «درآمد منهای همه‌ی هزینه‌ها»). مجوزش همان `accounting: view`ِ تبِ
    خانه است، پس گیتِ جداگانه نمی‌خواهد.
  - **برچسبِ نمودارِ ماهانه دو غلط داشت.** `jy % 100` سالِ ۱۴۰۵ را «۵» می‌نوشت، و
    با نامِ کوتاهِ ماه می‌شد «شهر ۵» — که فارسی‌زبان آن را «شهریورِ پنجم» می‌خواند نه
    «شهریور ۱۴۰۵». حالا نامِ کامل و سالِ کامل، همان قالبِ تولتیپِ دسکتاپ.
  - **و پنجره‌ی زمانی گفته نمی‌شد.** کارتِ «فروشِ کل» در همان صفحه `get_sales_summary`
    است — **بدونِ فیلترِ تاریخ، با مالیات** (۱۹۰٬۵۷۵٬۰۰۰) — ولی نمودار
    `get_sales_dashboard` است: **فقط ۱۲ ماهِ اخیر، بدونِ مالیات** (۱۵۸٬۲۵۰٬۰۰۰).
    دو اختلافِ مستقل روی هم سوار بودند و هیچ‌کدام برچسب نداشت. دسکتاپ از قبل
    می‌نوشت «روند فروش و خرید (۱۲ ماه اخیر)»؛ حالا موبایل هم می‌نویسد، و عدد را از
    `data.length` می‌گیرد نه ثابتِ ۱۲.
  - **نامِ ماه‌ها یک منبع شد** (`faMonth` در `lib/format.ts`، همان فهرستِ دسکتاپ) و
    محورِ نمودار کوتاه‌شده‌ی همان است — تا دو فهرست واگرا نشوند. ۴ تستِ تازه.
  - **یک یافته‌ی حسابداری که ربطی به موبایل ندارد و روی وب هم هست:** فروشِ خالصِ
    کلِ فاکتورهای باطل‌نشده ۱۷۳٬۲۵۰٬۰۰۰ است ولی درآمد در سود و زیان ۱۵۸٬۲۵۰٬۰۰۰ —
    یعنی ۱۵٬۰۰۰٬۰۰۰ فروش در دفترِ درآمد ننشسته. علتش بررسی نشده.

- **۱۴۰۵/۰۶/۱۷ (2026-09-08) — موبایل فازِ ۵: اندازه‌ی فونتِ سیستم، شش نقصی که هیچ تستی نمی‌گرفت (بدونِ مهاجرت):**
  اندروید اجازه می‌دهد کاربر فونت را تا **دو برابر** بزرگ کند، و کاربرِ این اپ —
  صاحبِ کسب‌وکارِ میان‌سال — واقعاً این کار را می‌کند. اپ تا امروز این را کاملاً
  نادیده می‌گرفت: صفر `accessibilityLabel` روی ۶۰ کنترل، صفر سقفِ بزرگ‌نمایی، و
  ارتفاعِ **ثابت** روی کادرهایی که متن داشتند. tsc، ۱۵۰ تستِ jest و ممیزِ ساختاری
  هیچ‌کدام صفحه را نمی‌بینند، پس هیچ‌کدام چیزی نگفتند. با اجرای واقعی روی امولاتور
  در `font_scale = 2.0` شش نقص بیرون آمد:
  - **فیلدِ شمارشِ انبارگردانی** (`width: 84, height: 46`) عددِ خودش را از هر دو سو
    می‌برید — به‌جای «۴۶» تکه‌ای ناخوانا. جدی‌ترین مورد، چون **سندِ تعدیلِ
    حسابداری** از روی همین فیلد ساخته می‌شود و انباردار عددی می‌دید که خواندنش
    ممکن نبود.
  - **فیلدِ جست‌وجو** (`height: 46`) متن را از پایین قیچی می‌کرد.
  - **چیپ‌های فیلتر:** `Segmented` هر گزینه را `flex: 1` می‌دهد و «مغایرت‌دار» یک
    واژه‌ی بی‌فاصله است — نمی‌شکند، پس از کادر بیرون می‌زد.
  - **برچسبِ نوارِ تب** «انبارگر…» و «اشخا…» می‌شد.
  - **هدف‌های لمسیِ ۲۸ و ۳۶** زیرِ کفِ ۴۸dpِ اندروید بودند — روی دکمه‌ی بستنِ پیامِ
    خطا و بستنِ اسکنر.
  - **`﷼`** لیگاتورِ تک‌گلیفی است و در اندازه‌ی بزرگ حروفش روی هم می‌افتاد؛ صفحه‌ی
    خانه از قبل «ریال» می‌نوشت. حالا همه‌جا یکی است.
  - **سقف گذاشته شد نه خاموشی.** `font.maxScale` فقط سه جا اعمال می‌شود که هندسه‌شان
    ثابت است (نشانِ عدد، چیپ، برچسبِ تب). در ۱٫۳ — پرکاربردترین تنظیم — هیچ‌چیز
    محدود نمی‌شود؛ بقیه‌ی اپ آزادانه با کاربر بزرگ می‌شود چون `height`های ثابت به
    `minHeight` تبدیل شدند.
  - **قاعده‌ی R4 ممیزِ موبایل:** «`height: <عدد>` روی استایلی که متن دارد». کادرِ
    مربعیِ آیکون استثناست. با برگرداندنِ عمدیِ باگ راستی‌آزمایی شد که واقعاً قرمز
    می‌شود — قاعده‌ای که هرگز نگیرد ارزشی ندارد.
  - **دسترس‌پذیری:** دوازده کنترلِ فقط‌آیکون برچسب و نقش گرفتند. پیش از این با
    TalkBack اپ فهرستی از دکمه‌های بی‌نام بود.
  - **deep link عمداً ساخته نشد.** `prefixes` از M0 شاملِ `https://acc.cubita.ir`
    بود ولی مانیفست هیچ فیلترِ http/https ندارد — روی دستگاه سنجیده شد: کروم باز
    می‌شود نه اپ. ولی در کلِ مخزن هیچ‌کس چنین لینکی نمی‌سازد و Push هم از `linking`
    رد نمی‌شود. افزودنِ فیلتر **پس‌رفت** بود: `acc.cubita.ir` دامنه‌ی وب‌اپ است و اپ
    همه‌ی لینک‌هایش را می‌قاپید، از جمله مسیرهایی که موبایل ندارد.
  - راستی‌آزمایی: ۱۵۰ تستِ موبایل، tsc پاک، ممیز بدونِ خطا، و **مقایسه‌ی چشمیِ
    قبل/بعد روی امولاتور** در هر دو مقیاسِ ۱٫۰ و ۲٫۰.

- **۱۴۰۵/۰۶/۱۷ (2026-09-08) — دو شاخه هم‌زمان مهاجرتِ `0100` ساختند؛ Alembic خطا نمی‌داد (شماره‌گذاریِ مجدد به `0104`):**
  شاخه‌ی موبایل و شاخه‌ی حسابداری هر دو از `0099` منشعب شده بودند و هر کدام
  مهاجرتی به نامِ `0100` ساخت — یکی `client_errors` و دیگری `journal_line_seq`.
  - **این خطا نمی‌دهد، و همین بدترین بخشش است.** Alembic فقط
    `UserWarning: Revision 0100 is present more than once` می‌دهد، یکی از دو تا را
    بی‌صدا دور می‌ریزد (آنکه دیرتر بارگذاری می‌شود برنده است، یعنی صرفاً ترتیبِ
    الفباییِ نامِ فایل) و بعد **دو head** گزارش می‌کند. روی حالتِ merge‌شده امتحان
    شد: `heads = ['0100', '0103']` و `walk_revisions` با «۰۱۰۳ با ۰۱۰۰ همپوشانی
    دارد» می‌ایستاد.
  - **پیامدِ عملی:** جدولِ `client_errors` ساخته نمی‌شد و اندپوینتِ گزارشِ کرشِ
    موبایل روی سرور خطا می‌داد، بی‌آنکه استقرار چیزی گفته باشد — و از آن طرف
    `alembic upgrade head` به‌خاطرِ دو سر اصلاً جلو نمی‌رفت.
  - **راهِ حل:** مهاجرتِ موبایل به `0104` با والدِ `0103` منتقل شد (روی production
    هرگز اجرا نشده بود، پس شماره‌اش آزاد بود). حالا `heads = ['0104']`، پیمایشِ
    `base→head` سالم با ۱۰۴ مهاجرت، و بدونِ هیچ هشداری.
  - **دلیلِ ریشه‌ای همان قاعده‌ی «مهاجرتِ همزمان»ِ CLAUDE.md است:** پیش از ساختِ
    مهاجرت باید `alembic heads` دیده شود یا به نفرِ دیگر گفته شود. چون Alembic
    خودش نمی‌گیرد، این قاعده تنها محافظِ موجود است.
  - ادغامِ `origin/master` (۴۵ کامیت، ۱۸ PR) در شاخه‌ی موبایل. تنها تعارضِ متنی
    همین بخشِ تاریخچه بود؛ `ci.yml` و `models/__init__.py` خودکار حل شدند.
    راستی‌آزمایی پس از ادغام: **۱۹۱۴ تستِ بک‌اند**، **۱۵۰ تستِ موبایل**، tsc پاک،
    و هر دو ممیزِ ساختاری بدونِ خطا.

- **۱۴۰۵/۰۶/۱۷ (2026-09-08) — «اصلاح طبقه‌بندی» دو چیزِ متفاوت بود و یکی‌شان نبود (بدونِ مهاجرت):**
  مرورِ «اصلاح و طبقه‌بندی حساب‌ها»ی سپیدار بیست‌وهشت بند داشت. کوبیتا صفحه‌ای
  **با همین نام** داشت — ولی کاری که می‌کرد آن چیزی نبود که تحلیل می‌خواست، و
  دقیقاً همان چیزی بود که §۱۶ منع می‌کند.
  - **آنچه بود:** `reclassify_accounts` **`parent_id` و `type`ِ خودِ حساب** را عوض
    می‌کند — حساب را در درختواره جابه‌جا می‌کند. گاردهایش درست‌اند، ولی این
    عملیاتی است که *نحوه‌ی گزارشِ* تاریخ را بازنویسی می‌کند: حسابی که امروز زیرِ
    سرفصلِ دیگری برود، گزارشِ **پارسال** هم از امروز طورِ دیگری دیده می‌شود.
  - **آنچه نبود:** اصلاحِ *مانده*. یعنی «۱۸ میلیونِ اجاره پرداختنی / شخص A باید
    در سایر پرداختنی / شرکت B می‌بود» — با یک سندِ متوازنِ تاریخ‌دار، بی‌آنکه یک
    سند از گذشته دست بخورد.
  - **و خطرِ واقعی همین هم‌نامی بود:** کاربری که دنبالِ «این مانده در حسابِ اشتباه
    است» می‌گشت، صفحه‌ای پیدا می‌کرد به همین نام که کارِ دیگری می‌کرد. عملیاتِ
    ساختاری حالا **«جابه‌جایی حساب در درختواره»** نام دارد، با هشداری در توضیحش
    که می‌گوید مانده را جابه‌جا نمی‌کند و گزارشِ گذشته را عوض می‌کند.
  - **جهتِ سند از مانده می‌آید، نه از فرض** (§۱۲): مانده‌ی بدهکار یعنی مبدأ
    بستانکار می‌شود؛ مانده‌ی بستانکار یعنی برعکس. هیچ‌جا «مبدأ بدهکار / مقصد
    بستانکار» ثابت نوشته نشده و یک تست همین را قفل می‌کند.
  - **فقط همان تفصیلی منتقل می‌شود** (§۱۳) — حسابی که ده‌ها مشتری دارد و یکی‌شان
    اشتباه طبقه‌بندی شده، بقیه‌اش دست‌نخورده می‌ماند. این قیدِ اصلیِ تست‌هاست.
  - **یک جفت ردیف به‌ازای هر مبدأ**، نه یک ردیفِ تجمیعیِ مقصد، تا از روی خودِ سند
    معلوم باشد کدام مبلغ از کجا آمد (§۲۷).
  - **`system_role` هشدار می‌دهد، مسدود نمی‌کند** (§۲۴ و §۱۵). عملیاتِ ساختاری این
    حساب‌ها را کامل می‌بندد و آن‌جا درست است؛ ولی *بردنِ مانده* از حسابِ نقش‌دار
    مشروع است. آنچه کاربر باید بداند این است که **ثبت‌های خودکارِ آینده همچنان به
    همان حساب می‌آیند** — و پیش‌نمایش همین را می‌گوید.
  - **گاردِ تفصیلی این‌جا گارد است نه تله**، برخلافِ تسعیر و اختتامیه: آن‌جا ابعاد
    از داده‌ی موجود کپی می‌شوند، این‌جا کاربر خودش مقصد را انتخاب می‌کند. پس
    `reclassification` عمداً در `DIMENSION_COPYING_SOURCES` **نیست**.
  - **گزارشِ §۲۸ ساخته نشد چون از قبل هست:** سند `source_type="reclassification"`
    می‌گیرد و فهرستِ اسناد با فیلترِ منشأ همان گزارش است. جدولِ تازه یعنی «دو نمای
    یک داده».
  - ۱۱ تستِ تازه، از جمله رگرسیونِ §۳ (اسنادِ تاریخی بایت‌به‌بایت دست‌نخورده).

- **۱۴۰۵/۰۶/۱۷ (2026-09-08) — واحدِ سنجشِ مؤدیان ثابت بود؛ هر چیزی «عدد» اظهار می‌شد (مهاجرتِ `0103`):**
  مرورِ «سامانه مؤدیان»ِ سپیدار سی‌وهشت بند داشت و §۳۶ می‌گفت ماژولِ موجود را
  بررسی کن نه اینکه موازی بسازی. بررسی شد و **ماژول به‌مراتب کامل‌تر از فرضِ
  تحلیل بود**: امضای JWS/JWE، `generate_tax_id` با رقمِ کنترلیِ Verhoeff، تستِ
  ارتباط، ارسالِ گروهی با نتیجه‌ی per-item، استعلام، و — مهم‌تر — §۳۴ و §۳۵ که
  تحلیل به‌عنوان کمبود می‌شمردشان از قبل بودند: `request_payload` دقیقاً همان
  چیزی است که ارسال شد، و هر تلاش **یک ردیفِ جدا** است نه بازنویسیِ آخری.
  - **ولی یک خط دادهٔ نادرست می‌فرستاد:** `"mu": "164"` در `build_invoice_packet`
    ثابت بود. یعنی هر ردیفِ هر صورتحساب — کیلوگرم، متر، لیتر، کارتن — به‌عنوان
    «عدد» به سازمانِ امور مالیاتی اظهار می‌شد. فروشِ ۵۰ کیلوگرم می‌شد ۵۰ عدد.
    این «قابلیتِ نداشته» نبود؛ **اظهارِ نادرست بود**.
  - **جدولِ `moadian_unit_maps`** نگاشتِ `(نوشتارِ واحد → کد)` را نگه می‌دارد.
    روی *کالا* نرفت چون صد کالای «کیلوگرم» باید صد بار جدا تنظیم می‌شدند و اولین
    اشتباهِ تایپی یک کدِ متفاوت به سامانه می‌فرستاد.
  - **کدها حدس زده نشدند.** جدولِ رسمی ده‌ها ردیف دارد و کدِ اشتباه از نفرستادن
    بدتر است؛ پس تنها پایه `BUILTIN_UNIT_CODES = {"عدد": "164"}` در کد است —
    همان چیزی که تا امروز برای *همه* می‌رفت — و بقیه را کاربر وارد می‌کند.
    **هیچ ردیفی seed نشد**، چون درجِ داده داخلِ مهاجرت روی جدولِ RLS ممنوع است.
  - **واحدِ نگاشت‌نشده ارسال را می‌بندد**، با پیامی که واحد و کالا و محلِ اصلاح را
    می‌گوید. تصمیمِ صریحِ کاربر بود: دادهٔ نادرست به سامانه نرود.
  - **`_assert_unit_codes` پیش از `_next_serial`** صدا زده می‌شود تا سریالِ مالیاتی
    بابتِ بسته‌ای که ساخته نمی‌شود هدر نرود — همان الگوی `_assert_stuff_ids`. یک
    تست همین را قفل می‌کند.
  - **گاردِ دومِ تازه:** خریداری که شماره اقتصادی دارد ولی کد/شناسه‌ی ملی ندارد،
    صورتحسابِ «نوعِ اول» می‌گرفت و `bid` خالی ارسال می‌شد. حالا مسدود می‌شود.
  - `readiness` واحدهای نگاشت‌نشده را **یک‌جا** فهرست می‌کند تا کاربر همه را با هم
    درست کند، نه فاکتور به فاکتور. مبنایش کالاهای فعال است نه فاکتورها.
  - **چند بند عمداً نیامد و دلیلشان یکی است:** «مشارکت مدنی» و «اتباع غیرایرانی»
    (§۸) کدهای رسمیِ خودشان را دارند و **مطمئنشان نبودم** — همان دلیلی که کدهای
    واحد را حدس نزدم. فروشِ صادراتی، پیمانکاری و دارایی ثابت هرکدام الگوی جداگانه
    می‌خواهند. جداییِ وضعیتِ داخلی و بیرونی (§۲۹) و تشخیصِ تغییرِ منبع پس از ارسال
    (§۳۳) در `OPEN_DECISIONS.md` ثبت شدند.
  - ۹ تستِ تازه، از جمله رگرسیونِ «عدد بدونِ هیچ نگاشتی همان ۱۶۴ می‌ماند» — ۵۵
    تستِ موجودِ مؤدیان دست‌نخورده سبز ماندند.

- **۱۴۰۵/۰۶/۱۷ (2026-09-08) — «معاف» از «نرخِ صفر» جدا شد (مهاجرتِ `0102`):**
  مرورِ «مالیات بر ارزش افزوده»ِ سپیدار سی بند داشت و کوبیتا چند بندِ سختش را از
  قبل درست انجام داده بود — §۱۹ (نرخ **و** مبلغِ مالیات هر دو در لحظه‌ی معامله
  ذخیره می‌شوند)، §۲۷ (مالیات درآمد نیست)، §۷ (`Item.is_service`)، و برگشت‌ها که
  هم جدا گزارش می‌شوند هم از پایه کسر.
  - **ولی اولین سؤالِ §۱ جوابی نداشت:** «چقدر فروشِ معاف داشته‌ایم؟». نه کالا
    وضعیتِ مالیاتی داشت، نه گزارش پایه را تفکیک می‌کرد. تنها چیزِ موجود
    `tax_rate` روی سربرگِ فاکتور بود — و `tax_rate = 0` **مبهم** است: سالِ بعد
    کسی نمی‌تواند بگوید کالا واقعاً معاف بوده یا فقط آن فاکتور بی‌مالیات صادر شده.
    دقیقاً هشدارِ §۲۰.
  - **سه ستون، دو نقشِ متفاوت.** `items.vat_status` **تنظیم** است (جایی که کاربر
    تعیین می‌کند) و `sales_invoice_lines.vat_status` / `purchase_invoice_lines.vat_status`
    **واقعیتِ قفل‌شده در لحظه‌ی معامله**اند. جداییِ این دو عمدی است و همان الگوی
    `tax_rate`/`tax_amount` را دارد: اگر گزارش از وضعیتِ *امروزِ* کالا می‌خواند،
    کالایی که امسال معاف شده فروشِ **پارسال** را هم معاف نشان می‌داد. قانون عوض
    می‌شود؛ تاریخ نه. تستِ `test_reclassifying_an_item_does_not_rewrite_past_sales`
    همین را قفل می‌کند.
  - **`is_service` عمداً قفل نمی‌شود** و از خودِ کالا خوانده می‌شود: کالا/خدمت
    بودن واقعیتی فیزیکی است، نه طبقه‌بندیِ قانونی که با قانون عوض شود.
  - **تفکیک از ردیف‌ها جمع می‌شود نه سربرگ**، و دقیق است چون تخفیفِ سطحِ فاکتور از
    قبل بینِ ردیف‌ها تسهیم شده. `test_the_four_buckets_sum_to_the_total` این
    برابری را قفل می‌کند — اگر روزی آن رفتار عوض شود، اولین چیزی است که قرمز می‌شود.
  - **فاکتورِ ترکیبی گزارش می‌شود، نه مسدود.** از لحظه‌ای که «معاف» وجود دارد،
    فاکتوری که ردیفِ معاف و مشمول را با هم دارد و نرخِ سربرگش غیرصفر است روی ردیفِ
    معاف هم مالیات گرفته — چون `compute_tax(total_amount, tax_rate)` روی کلِ فاکتور
    است. فاکتورِ ثبت‌شده ویرایش نمی‌شود و مسدودکردنِ گزارش هیچ‌چیز را درست نمی‌کند؛
    آنچه کمک می‌کند این است که حسابدار بداند. **گزارش، نه گارد.**
  - **دو برچسبِ نادرستِ رابط اصلاح شد:** «فروشِ مشمول» و «خریدِ مشمول» در جدولِ
    ریزِ محاسبه در واقع جمعِ *کل* بودند نه بخشِ مشمول.
  - **§۶ (مالیات در سطحِ ردیف) عمداً ساخته نشد.** این کار *طبقه‌بندی* را به ردیف
    برد که پیش‌نیازِ آن است؛ بردنِ خودِ مالیات، ردیفِ سند و مؤدیان و برگشت‌ها و صندوق
    را لمس می‌کند و باید تصمیمِ جدا باشد. تا آن روز، فاکتورِ ترکیبی گزارش می‌شود.
  - **یافته‌ی جانبی:** هویتِ مالیاتیِ شرکت (`economic_code`، `national_id`) داخلِ
    تنظیماتِ **مؤدیان** زندگی می‌کند — همان الگویی که §۲۲ منع می‌کند. ثبت شد.
  - ۸ تستِ تازه. **مهاجرت هیچ `UPDATE`ی ندارد** — پیش‌فرضِ DDL در کاتالوگ می‌نشیند.

- **۱۴۰۵/۰۶/۱۷ (2026-09-08) — آمادگیِ گزارش فصلی: کدام ردیف سرِ آپلود رد می‌شود (بدونِ مهاجرت):**
  مرورِ «خرید و فروش فصلی»ِ سپیدار سی بند داشت و §۲۹ می‌گفت اول کدِ موجود را ببین.
  **کوبیتا این گزارش را از قبل داشت** — `get_seasonal_report` با انتخابِ سال و فصل،
  تفکیکِ طرف حساب، کنارگذاشتنِ فاکتورِ باطل، ردیفِ تجمیعیِ «معاملاتِ خرد» و خروجیِ
  CSV. حتی §۱۰ («یک شخص می‌تواند هم مشتری باشد هم تأمین‌کننده») از قبل حل بود:
  `CONTACT_TYPES` مقدارِ `both` دارد و `is_broker` پرچمِ مستقل است، با کامنتی که
  توضیح می‌دهد چرا مقدارِ چهارمِ `type` نشد.
  - **شکاف، اعتبارسنجی بود (§۱۹ تا §۲۱).** ردیف‌ها `national_id` و `economic_code`
    و `postal_code` را برمی‌گرداندند و جدول جای خالی را `—` نشان می‌داد — ولی
    هیچ‌چیز نمی‌گفت این یک **مشکل** است. نه شمارشی، نه هشداری، نه راهی برای
    پیداکردنشان بینِ صدها ردیف. حسابدار بعد از آپلود در سامانه می‌فهمید.
  - **بدتر از نبودن، بدشکل بودن:** کامنتِ `Contact.national_id` می‌گوید حقیقی ۱۰
    رقم و حقوقی ۱۱ رقم، ولی هیچ‌جا سنجیده نمی‌شد. کدِ ۱۰رقمی روی شخصِ حقوقی *پُر*
    به‌نظر می‌رسد و سرِ آپلود رد می‌شود — همان چیزی که پیدا کردنش سخت‌ترین است.
  - `_seasonal_issues` **کدِ ماشین‌خوان** می‌دهد نه متنِ فارسی (برچسب کنارِ
    `ENTITY_LABEL` در رابط می‌نشیند)، هر بخش `ready_count`/`incomplete_count`
    می‌گیرد، و صفحه تیکِ «فقط ناقص‌ها» دارد. **گزارش است نه گارد** — خروجی هرگز
    مسدود نمی‌شود.
  - **ردیفِ «معاملاتِ خرد» عمداً هیچ‌وقت ایراد نمی‌گیرد** و کامنتش این را می‌گوید:
    طرف‌حساب نیست، سطلِ فروشِ نقدی است و هویتی ندارد که کم داشته باشد.
  - ترتیبِ ردیف‌ها **عوض نشد**: مبلغِ بزرگ‌تر همچنان بالاست. بالاکشیدنِ ناقص‌ها
    ترتیبی را می‌شکست که حسابدار برای خواندنِ گزارش می‌خواهد؛ پیداکردنشان کارِ
    فیلتر است.
  - **بقیه‌ی تحلیل عمداً ساخته نشد و دلیلش یکی است:** لایه‌ی نگاشت (ارز، طبقه‌بندیِ
    کالا، کشورِ مبدأ)، حدِ نصاب، نسخه‌پذیریِ طبقه‌بندی، و تاریخچه/snapshotِ خروجی
    همگی به **قالبِ فایلِ رسمی** بند‌ند، و کاربر هنوز نمی‌داند آن را لازم دارد یا
    ورودِ دستی کافی است. خرید وارداتی و صورت‌وضعیت و پیمانکار جزء هم موجودیت‌های
    عملیاتیِ تازه‌اند، نه لایه‌ی گزارش. هر دو سؤال در `OPEN_DECISIONS.md` ثبت شد.
  - ۸ تستِ تازه، از جمله دو رگرسیون: قاعده‌ی طول وارونه اعمال نشود، و **هیچ عددی
    تکان نخورَد**.

- **۱۴۰۵/۰۶/۱۶ (2026-09-07) — تفصیلی دیگر در گذر از سال گم نمی‌شود (بدونِ مهاجرت):**
  مرورِ «سند افتتاحیه و اختتامیه»ِ سپیدار بیست‌ودو بند داشت و کوبیتا بیشترش را از
  قبل داشت — و یکی‌اش را **بهتر از سپیدار**: افتتاحیه *معکوسِ سندِ اختتامیه* است نه
  یک محاسبه‌ی تازه، پس مغایرتِ بندهای ۱۵ و ۱۶ ساختاراً ناممکن است. دو شکاف ماند.
  - **بُعدها در اختتامیه/افتتاحیه دور ریخته می‌شدند.** `_permanent_balances` فقط
    `group_by(Account.id)` می‌کرد، پس «دریافتنیِ تجاری» با سه مشتری یک ردیف می‌شد و
    سالِ جدید با حسابی باز می‌شد که صد میلیون مانده دارد و **دفترِ تفصیلی‌اش خالی
    است**. همان باگی که چند ساعت پیش در `fx_revaluation_preview` پیدا شد — سومین
    جای همان الگو. و مثلِ تسعیر، در حالتِ `strict` کلِ اختتامیه رد می‌شد.
    گروه‌بندی به هر سه بُعد رفت و افتتاحیه خودبه‌خود درست شد، چون از ردیف‌های
    اختتامیه ساخته می‌شود. `closing_entry` و `opening_entry` هم به
    `DIMENSION_COPYING_SOURCES` اضافه شدند.
  - **افتتاحیه‌ی دوم کلِ ترازنامه را دو برابر می‌کرد — بی‌صدا.** اختتامیه خودش را
    گارد می‌کند (بعدش مانده‌ها صفرند)، ولی افتتاحیه از *سندِ* اختتامیه ساخته
    می‌شود و آن سند سرِ جایش می‌ماند. هیچ گاردی هم صدا نمی‌کرد چون سند متوازن
    است. حالا رد می‌شود، با **شماره‌ی سندِ موجود در پیام**.
  - **پیوند با `reverses_entry_id`، بدونِ ستونِ تازه.** آن ستون از قبل همین معنا را
    داشت («جمعشان همیشه صفر است») که برای جفتِ اختتامیه/افتتاحیه مو‌به‌مو صادق
    است. یک ستون هم به §۹ (این مانده از کجا آمد؟) جواب می‌دهد و هم گاردِ بالا را
    ممکن می‌کند. بررسی شد که رفتارِ ابطال عوض نمی‌شود.
  - **سه بند عمداً ساخته نشدند:** برچسبِ ارز روی این دو سند (تسعیر روی
    `currency_code IS NOT NULL` فیلتر می‌کند، پس این دو برایش نامرئی‌اند و مانده‌ی
    ارزی از قبل درست از سال عبور می‌کند) · گزارشِ مقایسه‌ی اختتامیه با افتتاحیه
    (افتتاحیه *همان* سند وارونه است، پس گزارش فقط می‌تواند «✓» بدهد — اعتمادِ
    کاذب) · دستیارِ پایان سال (از قبل هست).
  - **تصحیحِ یک اشتباهِ خودم:** در مرورِ «صدور سند کل» نوشتم که کوبیتا چنین صفحه‌ای
    ندارد. **دارد** — `GeneralDocumentPage` از کامیتِ `61f156e`، و دقیقاً همان
    مدلِ مشتق را پیاده کرده. `OPEN_DECISIONS.md §۹` اصلاح شد: دوراهیِ باقی‌مانده
    فقط «آیا برگه‌ی شماره‌دارِ ماندگار لازم است؟» است، نه «بسازیم یا نه».
  - ۸ تستِ تازه، از جمله سه رگرسیون. **مهاجرت ندارد.**

- **۱۴۰۵/۰۶/۱۶ (2026-09-07) — «این سند از کدام عملیات آمد؟» (مهاجرتِ `0101`):**
  مرورِ «کارتابل صدور سند حسابداری»ِ سپیدار بیست‌ودو بند داشت و §۱۹ می‌گفت اول کد
  را ببین. دیدم، و نتیجه این بود که **کارتابل مسئله‌ای را حل می‌کند که کوبیتا
  ندارد**: کوبیتا صدورِ معوق ندارد و هر ماژول سندش را در همان تراکنشِ عملیات
  می‌سازد (در `inventory.py` سندِ فاکتور فروش حتی *پیش از خودِ فاکتور* flush
  می‌شود). پس صفِ «منتظرِ صدور»، صدورِ گروهی، و گاردِ ایدمپوتنسی موضوعیت ندارند —
  اگر سند ساخته نشود، خودِ عملیات rollback می‌خورد و کاربر همان‌جا دلیلِ دقیق را
  می‌بیند، نه یک صفِ خطا.
  - **آنچه واقعاً کم بود، بندِ ۱۳ است: ردیابی فقط یک‌طرفه بود.** جهتِ عملیات ←
    سند محکم است (شانزده مدل ستونِ `journal_entry_id` دارند و `_apply_void` رویش
    زندگی می‌کند). جهتِ معکوس نبود: `JournalEntry.source_id` تعریف شده بود، کامنت
    داشت، و **هیچ‌جا نوشته نمی‌شد** — تنها ارجاعش `voiding.py` بود که همان `None`
    را روی سندِ معکوس کپی می‌کرد. حسابرسی که سندِ ۸۵۰ را می‌دید می‌فهمید «فاکتور
    فروش» بوده، ولی **کدام** فاکتور را نه. همان کلاسِ نقصِ `salesperson_id`.
  - **جواب مشتق شد، نه ذخیره.** پرکردنِ `source_id` در سی‌وهفت نقطه دو ایراد
    داشت: انضباطش یک بار شکست خورده، و مهم‌تر، آن ستون **دومین جای ذخیره‌ی همان
    رابطه** بود و می‌توانست با `journal_entry_id` نخواند. `services/entry_source.py`
    از خودِ داده جواب می‌گیرد: `source_type` می‌گوید کدام جدول، `journal_entry_id`
    آن‌جا جواب را دارد. ستون دست‌نخورده ماند ولی کامنتش حالا می‌گوید استفاده
    نمی‌شود — کامنتِ قبلی مدعیِ ردیابی بود و خواننده را گمراه می‌کرد.
  - **`resolve_sources` دسته‌ای است**: یک کوئری به‌ازای هر *نوعِ* منبع، نه هر سند.
    تستِ شمارشِ کوئری قفلش می‌کند تا کسی حلقه‌ی per-entry نگذارد.
  - **رابطه یک‌به‌یک نیست.** حقوق و دستمزد یک سند برای کلِ دوره می‌زند و همه‌ی
    فیش‌ها به همان اشاره می‌کنند؛ پس شناسه و شماره فقط وقتی برمی‌گردند که منبع
    یکتا باشد و وگرنه فقط شمارش («حقوق و دستمزد — ۲۳ مورد»). نشان‌دادنِ یکی از
    بیست‌وسه فیش، سند را غلط توصیف می‌کرد.
  - **مهاجرتِ `0101` فقط ایندکس است** روی `journal_entry_id`ِ هر شانزده جدول.
    کلیدِ خارجی در PostgreSQL خودبه‌خود ایندکس نمی‌گیرد، پس لوکآپِ معکوس seq scan
    بود. هیچ `INSERT`/`UPDATE`ی ندارد، پس قاعده‌ی «روی جدولِ RLS ننویس» اصلاً
    موضوعیت پیدا نمی‌کند. مدل‌ها هم `index=True` گرفتند تا `create_all` و مهاجرت
    از هم جدا نیفتند.
  - **`PayrollSettlement` کشف شد**: ستونِ `journal_entry_id` دارد ولی هیچ سندی
    نمی‌زند و همیشه NULL است. در `MODELS_WITHOUT_ENTRIES` صریح معاف شد؛ اینکه
    عمدی است (پرداختش از خزانه می‌رود) یا شکافِ واقعی، تصمیمی است که باز مانده.
  - **تستِ کامل‌بودنِ رجیستری** با پیمایشِ `Base.registry` هر مدلی را که
    `journal_entry_id` دارد می‌سنجد. این تنها چیزی است که جلوی پوسیدنِ دوباره را
    می‌گیرد — مدلِ تازه بدونِ ثبت، تست را قرمز می‌کند. ۸ تستِ تازه.
- **۱۴۰۵/۰۶/۱۶ (2026-09-07) — «دفتر کل»، رگِ گم‌شده‌ی گزارش‌ها (بدونِ مهاجرت):**
  مرورِ «صدور سند کل»ِ سپیدار بیست‌وسه بند داشت و خودش هشدار می‌داد که این یکی را
  کورکورانه کپی نکنیم. ممیزی حق را به آن داد: تجمیعِ سطحِ **کل** از قبل در «گزارش
  ترازها» بود (انتخابگرِ سطح + `ancestorAtLevel`)، و دفترِ **روزنامه**ی قانونی هم
  زنده و مشتق (`get_legal_book`). آنچه نبود، رگِ میانی بود.
  - **`get_general_ledger` روی سرفصل دفترِ خالی می‌داد**، چون ردیفِ سند هرگز به
    حسابِ گروه نمی‌چسبد و انتخابگرِ صفحه هم عمداً `!is_group` بود. برای دیدنِ گردشِ
    «دارایی‌های جاری» باید تک‌تکِ زیرحساب‌ها جدا باز می‌شدند.
  - **حالا حساب به‌علاوه‌ی همه‌ی نوادگانش را می‌آورد**، و هر ردیف کد و نامِ
    *زیرحسابِ خودش* را دارد. **دوباره‌شماری ممکن نیست** و این تضمینِ کلِ کار است:
    `routers/accounts.py` از قبل جلوی «حسابی که هم ردیفِ مستقیم دارد هم فرزند» را
    می‌گیرد، پس هر حساب یا برگ است یا سرفصل. رفتارِ حسابِ برگ مو‌به‌مو همان ماند.
  - `_descendant_ids`ِ `accounting_ops` به `reports.descendant_account_ids` منتقل
    شد (جهتِ وابستگی همین را اجازه می‌دهد؛ عکسش حلقه می‌شد) — نسخه‌ی دوم ساخته نشد.
  - ترتیبِ ردیف‌ها `(تاریخ، شماره، seq، id)` شد. بدونِ دو تای آخر، دو ردیفِ یک سند
    در دفترِ کل جای هم عوض می‌شدند و مانده‌ی در حال اجرا ناپایدار می‌شد.
  - **رابط:** تبِ سومِ «دفتر کل» میانِ روزنامه و معین، با انتخابگرِ حساب‌های سطحِ کل
    (`levelOf === 2` — همان تابع و همان عددِ گزارش ترازها؛ سطح از عمقِ درخت می‌آید
    نه طولِ کد، چون چارت یکدست نیست: ۱ ← ۱۱ ← ۱۱۰۱ ولی ۵ ← ۵۱۰۱). ستونِ «زیرحساب»
    فقط در همین تب. یک کارت برای هر دو دفتر، نه دو نمای یک داده.
  - **«سند کل» به‌عنوان موجودیتِ ذخیره‌شده ساخته نشد** و دلیلش با چهار سؤالِ
    قطعی‌کننده در [OPEN_DECISIONS.md](OPEN_DECISIONS.md) ثبت شد: محتوایش صددرصد
    مشتق است، کوبیتا همین تصمیم را یک بار برای دفترِ قانونی گرفته و شیپ کرده، و
    تنها چیزِ مشتق‌نشدنی («تا کجا صادر شده» و شماره‌اش) واقعیتی درباره‌ی کاغذِ
    بیرونِ سیستم است.
  - ۷ تستِ تازه، از جمله دو رگرسیون: حسابِ برگ همان قبل، و سندِ باطل و معکوسش هر
    دو در دفتر می‌مانند. **مهاجرت ندارد.**
- **۱۴۰۵/۰۶/۱۶ (2026-09-07) — سندِ تسعیرِ ارز بُعدهای مانده را نگه می‌دارد (بدونِ مهاجرت):**
  مرورِ «سند تسعیر ارز»ِ سپیدار بیست‌ودو بند داشت و کوبیتا بیست‌ویکی را از قبل
  داشت — `is_fx`/`fx_revaluable`، نرخِ تاریخ‌دار، `missing_rates` به‌جای حدس،
  `source_type="fx_revaluation"`، `assert_period_open`، و ایمنیِ خودکار در برابرِ
  دوباره‌تسعیر (سند خودش ارزشِ دفتری را اصلاح می‌کند، پس اجرای دوم خالی است).
  بندِ ۱۲ — «تسعیر باید در سطحِ حساب + تفصیلی + ارز باشد» — برقرار نبود، و نبودنش
  فقط قابلیتِ کم نبود؛ **باگ بود**.
  - `fx_revaluation_preview` فقط روی `(حساب، ارز)` گروه می‌شد. «دریافتنیِ ارزی» با
    سه مشتری یک ردیف می‌شد و ردیفِ سند `analytic_id=None` می‌گرفت. در حالتِ
    **`strict` تسعیر اصلاً کار نمی‌کرد** (`assert_entry_has_tafsili` ردش می‌کرد،
    با پیامی که می‌گفت «تفصیلی را روی حساب تعیین کنید» — کاری که کاربر اینجا از
    دستش برنمی‌آمد). در `hybrid` و `floating` بی‌صدا دفترِ تفصیلی را از حساب جدا
    می‌کرد: جمع درست، تفکیک غلط، و چون سند ویرایشِ ردیف ندارد، برای همیشه.
  - **گروه‌بندی به هر سه بُعدِ ردیف رفت** — تفصیلی *و* مرکز هزینه — و
    `issue_fx_revaluation` هر دو را روی ردیفِ سند کپی می‌کند. فقط تفصیلی کافی
    نبود: گزارشِ فیلترشده بر مرکز همان ناسازگاری را یک بُعد آن‌طرف‌تر می‌داد.
    مانده‌ای که بُعد ندارد همچنان یک ردیف است، پس رفتارِ قبلی دست‌نخورده ماند.
  - **تسعیر مثل سندِ برگشتی از گاردِ تفصیلی مستثنا شد** (`DIMENSION_COPYING_SOURCES`).
    استدلالش عیناً همانی است که `tafsili.py` برای ابطال نوشته بود: ابعادش را از
    داده‌ی موجود کپی می‌کند، پس قاعده‌ای که *بعد از* ثبتِ اصل سخت‌تر شده، مانده را
    برای همیشه غیرقابلِ تسعیر می‌کرد. سوراخ نمی‌سازد — همان ردیف در
    `find_missing_tafsili` می‌ماند. **گزارش، نه گارد.**
  - `_rate_on` حالا تاریخِ نرخ را هم برمی‌گرداند و جدول نرخِ کهنه را قرمز نشان
    می‌دهد (بندِ ۱۶): نرخِ سه‌ماه‌پیش حدس نیست، ولی نرخِ روز هم نیست.
  - **دو بند عمداً ساخته نشدند:** انتخابگرِ حسابِ سود/زیانِ تسعیر (کوبیتا با
    `system_role` پیدایش می‌کند و کد و نامش در چارت قابلِ تغییر است — انتخابگر
    «دو نمای یک داده» می‌شد) و «توضیحات دوم» (همان بندی که در مرورِ صدور سند رد شد).
  - ۷ تستِ تازه، از جمله دو رگرسیون: `strict` دیگر نمی‌بندد، و مانده‌ی بی‌بُعد
    هنوز یک ردیف است. **مهاجرت ندارد** — `analytic_id` و `cost_center_id` از قبل
    روی `journal_lines` بودند و فقط پُر نمی‌شدند.

- **۱۴۰۵/۰۶/۱۶ (2026-09-07) — «دائم‌کردن» کنشِ خودش را در دفترِ حسابرسی گرفت (بدونِ مهاجرت):**
  مرورِ «تبدیل سند موقت به دائم»ِ سپیدار بیست‌ویک بند داشت و کوبیتا چرخه‌ی عمر را
  کامل داشت — `status` و `finalized_at` و `finalized_by_id`، عملیاتِ گروهی،
  فیلترِ گزارش بر وضعیت، ابطال با معکوس. یک بند اما جوابِ سختی داشت.
  - **«چه کسی این سند را نهایی کرد؟»** ردیابی وجود داشت ولی تغییرِ `status` با
    `action="update"` ثبت می‌شد و خلاصه‌اش می‌گفت «سند حسابداری شماره ۱۲۵ **ویرایش
    شد**» — که غلط توصیف می‌کند، چون هیچ‌چیز ویرایش نشده بود؛ سند رسمی شده بود.
    برای یافتنش باید `update`ها فیلتر می‌شدند و بعد داخلِ JSONِ `changes` دنبالِ
    `status` گشت.
  - **`finalize` کنشِ مستقل شد**، دقیقاً به الگوی `void` که یک خط بالاترش بود و
    کامنتِ `models/audit.py` دلیلش را از قبل نوشته بود: «اگر با بقیه‌ی UPDATEها در
    یک دسته بیفتد، پیدا کردنش نیاز به فیلتر روی محتوای JSON دارد؛ به‌عنوان action
    مستقل، یک کوئریِ ساده است». ترتیبِ تشخیص عمدی است: ابطال اول سنجیده می‌شود.
  - **سه بند عمداً ساخته نشدند و دلیلشان ثبت شد.** مهم‌ترینش **اعتبارسنجی پیش از
    دائم‌کردن** بود: توازن از قبل در `JournalEntryIn` تضمین شده و سندِ خودکار با
    ساختش متوازن است — ولی **سند ویرایشِ ردیف ندارد**، پس اعتبارسنجی‌ای که سرِ
    دائم‌کردن رد کند سندی می‌سازد که نه می‌شود درستش کرد نه دائمش. خطرِ واقعی‌ترش
    تفصیلی است: سطحِ اجبارش تنظیمِ متغیرِ کسب‌وکار است، و شرکتی که از «شناور» به
    «اجباری» برود همه‌ی اسنادِ موقتِ قدیمی‌اش یک‌شبه گیر می‌کردند — قاعده‌ای که در
    زمانِ ثبت وجود نداشت، بازگشتی اعمال شود. (دو تای دیگر: سیاستِ «فاکتور مستقیماً
    دائم شود» و مجوزِ جدا برای دائم‌کردن — هر دو تصمیمِ سازمانی، نه استنتاج.)
  - ۵ تستِ تازه، از جمله دو رگرسیون: ابطال هنوز `void` می‌ماند و ویرایشِ شماره
    فرعی هنوز `update`. **مهاجرت ندارد** — `action` ستونِ `String(20)`ِ بی‌قید است.

- **۱۴۰۵/۰۶/۱۶ (2026-09-07) — قاعده‌ای که نوشته شده بود و اجرا نمی‌شد: دائم‌کردنِ اسناد (بدونِ مهاجرت):**
  دنبالِ هم‌راستا کردنِ `finalize_entries` با `renumber_entries` رفتم و چیزِ
  جدی‌تری پیدا شد.
  - **`FinalizeIn` از روزِ اول در docstringش می‌گفت** «دستِ‌کم یکی از فیلترها لازم
    است — همه‌ی اسنادِ موقتِ تاریخ یک درخواستِ خطرناکِ بی‌قصد است و باید صریح گفته
    شود» — و **هیچ‌چیز اعمالش نمی‌کرد**. نه اعتبارسنجیِ اسکیما، نه گاردِ سرویس.
    `POST /api/accounting/entries/finalize` با بدنه‌ی `{}` همه‌ی اسنادِ موقتِ
    کسب‌وکار را دائم می‌کرد، و دائم‌کردن **یک‌طرفه** است — خودِ سرویس می‌گوید «راهِ
    برگشتی عمداً وجود ندارد». قاعده‌ای که نوشته شده و اجرا نمی‌شود از نبودنش بدتر
    است: خواننده‌ی کد فکر می‌کند محافظت هست.
  - **فهرستِ خالی هم همان تله را داشت** که در بازشماره‌گذاری بود: روتر
    `data.entry_ids or None` می‌داد، پس انتخابِ خالی به «فیلتری در کار نیست»
    ترجمه می‌شد. حالا `entry_ids` پیش‌فرضش `None` است و خالی خطاست.
  - **انتخابِ دستی جایگزینِ بازه شد، نه افزوده بر آن** — هم‌راستا با
    `_renumber_scope`. پیش از این هر دو اعمال می‌شدند، پس کاربری که سندی را تیک
    زده ولی بازه‌اش جای دیگری بود پیامِ «۰ سند» می‌گرفت بی‌آنکه بفهمد چرا.
  - هر سه فراخوانیِ رابط از قبل دستِ‌کم یک فیلتر می‌فرستادند، پس هیچ مسیرِ موجودی
    نشکست. ۷ تستِ تازه، از جمله یکی که از مسیرِ واقعیِ HTTP بدنه‌ی خالی را می‌فرستد.

- **۱۴۰۵/۰۶/۱۶ (2026-09-07) — ردِ حسابرسیِ بازشماره‌گذاری دروغ می‌گفت (بدونِ مهاجرت):**
  مرورِ «شماره‌گذاری مجدد اسناد»ِ سپیدار شانزده بند داشت و کوبیتا تقریباً همه را
  داشت — عطف دست‌نخورده (و **در پیش‌نمایش نشان داده می‌شود**)، فقط سندِ موقت،
  `assert_period_open`، پیش‌نمایشِ ردیف‌به‌ردیف، گاردِ تصادمِ شماره. **ولی بندِ
  «قابلِ ردیابی بودن» برعکسِ آنچه به نظر می‌رسید شکسته بود.**
  - **دو رکورد به‌ازای هر سند، و یکی‌شان ساختگی.** `renumber_entries` برای دور
    زدنِ قیدِ یکتای (مستأجر، شماره) دومرحله‌ای می‌نویسد: اول همه‌ی شماره‌ها منفی،
    بعد نهایی. و حسابرسیِ خودکار روی **هر** flush شلیک می‌کند. نتیجه:
    «سند حسابداری شماره **-۱** ویرایش شد» با `{number: {from: 5, to: -1}}`، و بعد
    `{from: -1, to: 3}`. یعنی رکوردِ اول وضعیتی را ثبت می‌کرد که هرگز واقعیت
    نداشت، و **هیچ رکوردی «۵ → ۳» را نشان نمی‌داد** — همان تنها چیزی که کسی در
    دفترِ حسابرسی دنبالش می‌گردد. بازشماریِ ۱۲۷ سند ۲۵۴ رکورد می‌ساخت، نیمی
    بی‌معنا، در جدولی که با trigger فقط‌افزودنی است و هرگز پاک نمی‌شود.
  - **`audit.suppressed()` و `audit.record_change()`** اضافه شدند و بازشماری حالا
    ثبتِ خودکار را برای آن دو flush خاموش می‌کند و **یک** رکورد با شماره‌ی واقعیِ
    قبل و بعد می‌نویسد. سندی که شماره‌اش عوض نشده هیچ رکوردی نمی‌گیرد.
    **چرا این نقضِ قاعده‌ی «خودکار نه صریح» نیست:** آن قاعده درباره‌ی *پوشش* است
    («هر مسیر تازه باید یادش بماند») و سرِ جایش می‌ماند؛ این‌جا پوشش کم نمی‌شود،
    دو رکوردِ غلط با یک رکوردِ درست جایگزین می‌شود. یک تست هم قفل می‌کند که پرچمِ
    خاموشی **نشت نمی‌کند** — وگرنه این اصلاح خودش سوراخِ بزرگ‌تری می‌ساخت.
  - **انتخابِ دستیِ اسناد** اضافه شد (`entry_ids`)، همان الگویی که `finalize_entries`
    و «ادغام» از قبل دارند. **جای** بازه می‌نشیند نه کنارش: اگر هر دو اعمال
    می‌شدند، کاربری که چند سند تیک زده و بازه‌اش روی «امسال» مانده بی‌صدا
    زیرمجموعه‌ای از انتخابش را می‌گرفت.
  - **یک باگ که تستِ همین کار پیدا کرد:** شرطِ `if entry_ids:` روی فهرستِ **خالی**
    falsy است، پس انتخابِ خالی به فیلترِ تاریخ می‌افتاد و اگر بازه هم خالی بود
    **کلِ دفتر بازشماری می‌شد**. حالا `is not None` است.
  - **انتخاب بر اساسِ محدوده‌ی شماره عمداً نیامد.** کلِ کارِ این عملیات درست‌کردنِ
    شماره‌هایی است که با تاریخ نمی‌خوانند؛ فیلترکردن بر همان شماره‌های به‌هم‌ریخته
    یعنی تکیه بر چیزی که خودش خراب است — همان دلیلی که `_renumber_plan` با
    `created_at` مرتب می‌کند نه با `number`.
  - ۷ تستِ تازه، `tsc -b` صفر، ممیزِ صفحه‌ها صفر خطا. **مهاجرت ندارد.**

- **۱۴۰۵/۰۶/۱۶ (2026-09-07) — ترتیبِ ردیف‌های سند، و سندِ قابلِ چاپ (مهاجرتِ ۰۱۰۰):**
  مرورِ ویدئوی «صدور سند حسابداری»ِ سپیدار بیست‌ودو مفهوم داشت که **بیشترشان از
  قبل بودند** — عطف، شماره فرعی، موقت/دائم، تفصیلی، ارز روی ردیف، پیگیری، ابطال
  با معکوس. دو چیز نبود، و به هم وصل بودند.
  - **ترتیبِ ردیف‌های سند تصادفی بود.** `order_by="JournalLine.id"` و `id` از
    `UUIDPKMixin` می‌آید با `default=uuid.uuid4` — یعنی مرتب‌سازی روی یک عددِ
    تصادفی. سندِ چهارردیفی به‌هم‌ریخته دیده می‌شد و بستانکار می‌توانست پیش از
    بدهکار بیاید. **بدتر از «همیشه غلط»، ناسازگار بود:** بلافاصله پس از ثبت،
    شیءِ درونِ session ترتیبِ ورود را داشت؛ پس از بازخوانی، ترتیبِ UUID. یعنی سند
    سرِ ثبت یک‌جور دیده می‌شد و سرِ بازکردن جورِ دیگر.
  - **backfill نشد، و این تصمیمِ اصلیِ مهاجرت بود.** ترتیبِ سندهای گذشته
    بازیابی‌شدنی نیست — نه `seq` داشتند و `JournalLine` حتی `TimestampMixin` هم
    ندارد که `created_at` بدهد. هر شماره‌گذاریِ ساختگی فقط همان ترتیبِ تصادفیِ
    امروز را رسمی می‌کرد و وانمود می‌کرد اطلاعاتی داریم که نداریم. پس صفر ماندند
    و مرتب‌سازیِ تازه `(seq, id)` است: سندِ قدیمی دقیقاً مثل قبل، سندِ تازه درست.
    همین ترتیبِ دوتایی حالتِ شکست را هم امن می‌کند — هر مسیری که `seq` نگذارد به
    رفتارِ امروز برمی‌گردد نه به چیزی شکسته.
  - **شماره‌گذاری در `number_lines`** ([common.py](backend/app/services/common.py))
    که `make_journal_entry` صدایش می‌زند (۴۵ مسیر) به‌علاوه‌ی **هفت جای مستقیم**
    (انبار، تولید، انبارگردانی، ابطال، روترِ سند). نقطه‌ی مشترکِ واحدی نبود، پس
    صریح بهتر از رویدادِ SQLAlchemy بود.
  - **سند حالا چاپ می‌شود** — تا امروز فاکتور و فیشِ حقوقی چاپ داشتند و سندِ
    حسابداری نه. `render_journal_entry` همان `_STYLE` و `fa_number` و
    `format_jalali`ِ چاپِ فاکتور را به کار می‌برد. **جمعِ بدهکار و بستانکار هر دو
    چاپ می‌شوند** چون خواننده باید توازن را روی همان کاغذ ببیند، نه اینکه به
    درستیِ نرم‌افزار اعتماد کند. سندِ باطل نشانِ ابطال می‌گیرد — وگرنه برگه‌ی
    چاپ‌شده‌ی باطل از معتبر قابلِ تشخیص نیست.
  - **دو چیز عمداً نیامدند.** «شماره روزنامه»: `number` دقیقاً همین نقش را دارد و
    ستونِ سوم یعنی «دو نمای یک داده». «شرح تکمیلی»: سند شرحِ خودش را دارد و هر
    ردیف شرحِ خودش؛ شرحِ سوم افزونه‌ی رابطِ سپیدار است نه یک مفهوم.
  - **۸ تستِ تازه.** مهم‌ترینشان `expire_all()` می‌زند: بدونِ آن تست فقط حافظه‌ی
    session را می‌خواند که همیشه ترتیبِ ورود را دارد — یعنی تستِ سبز بدونِ اینکه
    چیزی سنجیده باشد.

- **۱۴۰۵/۰۶/۱۶ (2026-09-07) — موبایل v1.8.0 ساخته و روی امولاتور آزموده شد؛ دو باگِ
  دیگر که فقط روی صفحه دیده می‌شدند:** امولاتورِ اندروید ۱۶ نصب شد و APKِ امضاشده
  رویش اجرا شد. tsc پاک و ۱۴۵ تست سبز بود، ولی صفحه‌ی شمارش دو مشکل داشت:
  - **مقادیر با ارقامِ لاتین** نشان داده می‌شدند (`12.000`، `0.000`) در ردیفی که
    کنارش «سیستم: ۱۲ عدد» فارسی بود — نقضِ قاعده‌ی ۵ در CLAUDE.md. ممیزِ R3 آن را
    نگرفت چون فقط `toLocaleString()` بدونِ آرگومان را می‌سنجد و اینجا اصلاً
    صدا زده نشده بود.
  - **و باگی که در دلِ اصلاحِ اولی بود:** `Number('۱۲')` در جاوااسکریپت `NaN`
    است. محاسبه‌ی مغایرت `Number(text) - Number(system)` بود، پس با ارقامِ فارسی
    `Math.abs(NaN) > 1e-9` نادرست می‌شد و **ردیفِ مغایرت‌دار بی‌صدا «بدونِ
    اختلاف» نشان داده می‌شد**. `lib/format.ts::parseQty` اضافه شد (ارقامِ فارسی و
    عربی‌ـهندی) با تستی که صریحاً `expect(Number('۱۲')).toBeNaN()` را می‌سنجد تا
    بگوید *چرا* لازم است. **۱۵۰ تستِ موبایل.**
  - **دوربینِ اختیاری — پاسخ به اخطارِ کافه‌بازار:** صرفِ داشتنِ مجوزِ `CAMERA`
    باعث می‌شد اندروید `uses-implied-feature: android.hardware.camera` بسازد،
    یعنی اپ رسماً «نیازمندِ دوربین» اعلام می‌شد. برای نرم‌افزارِ حسابداری هم غلط
    بود (ورودِ دستیِ بارکد همیشه کار می‌کند) هم دلیلِ اعتراضِ بازبین.
    `plugins/withOptionalCamera.js` حالا `required="false"` را برای
    `android.hardware.camera` و `camera.autofocus` می‌نشاند. در APK تأیید شد:
    `uses-feature-not-required`. افزونه است نه ویرایشِ دستی، چون `prebuild`
    مانیفست را از نو می‌سازد.
  - **تله‌ی ساخت:** gradle متغیرهای محیطی را ورودیِ task نمی‌داند. با عوض‌کردنِ
    `EXPO_PUBLIC_API_BASE_URL` باندلِ JS را «به‌روز» فرض می‌کند و همان باندلِ
    قبلی را بسته‌بندی می‌کند — APKِ به‌ظاهر تازه‌ای که به سرورِ قبلی وصل است.
    برای build با آدرسِ متفاوت باید
    `android/app/build/generated/assets/react/release` را دستی پاک کرد.
  - خروجی: `mobile/release/cubita-v9.apk` — امضا با همان کلیدِ نسخه‌ی ۸
    (SHA-256 تطبیق داده شد، پس روی نصبِ موجود آپدیت می‌شود)، versionCode ۹.

- **۱۴۰۵/۰۶/۱۶ (2026-09-07) — موبایل: تستِ سرتاسری روی سرورِ محلی، و باگی که فقط
  همان‌جا دیده شد:** فازِ ۴ با tsc و ۱۴۳ تست سبز بود؛ برای اطمینان کلِ مسیر روی
  بک‌اندِ واقعیِ محلی رانده شد. دو چیز بیرون آمد:
  - **خطرِ «بازنویسیِ بی‌صدا» بازتولید شد.** روی سرورِ واقعی: دو گوشی که هرکدام
    *فقط ردیفِ خودشان* را می‌فرستند، هر دو شمارش زنده می‌ماند؛ ولی فرستادنِ کلِ
    فهرست (کارِ ساده و طبیعی) هر دو را به موجودیِ سیستمی برگرداند — بی هیچ
    خطایی. این همان چیزی است که `mobile/src/stock/countDraft.ts` جلویش را
    می‌گیرد، و حالا اثباتِ عملی دارد نه فقط استدلال.
  - **🐞 فرمِ تحویل پولی می‌پرسید که هیچ‌جا نمی‌نشست.** بک‌اند دو مسیر دارد:
    اگر پخش‌کننده `require_delivery` را روشن نکرده باشد (پیش‌فرض)، *تأیید* خودش
    فاکتورهای دوطرفه را می‌زند و نقد را همان‌جا تسویه می‌کند؛ آن‌وقت
    `deliver_order` می‌بیند فاکتور از قبل هست و **کلِ بلوکِ نقد را رد می‌کند**.
    یعنی مأمورِ حمل ۳۳۳٬۳۳۳ وارد می‌کرد، پیام موفقیت می‌دید، و `cash_amount`
    صفر می‌ماند. `market/cod.ts::settlesOnDelivery` حالا از روی
    `retailer_purchase_invoice_id` تشخیص می‌دهد و فرم در حالتِ پیش‌فرض اصلاً
    مبلغ نمی‌پرسد — به‌جایش می‌گوید تسویه هنگامِ تأیید انجام شده.
  - راستی‌آزمایی: هر دو مسیر روی سرورِ واقعی سنجیده شد (A: بدونِ پرسش، صفر ✅ ·
    B: ۳۳۳٬۳۳۳ دقیقاً ثبت شد ✅)، به‌علاوه‌ی سندِ انبارگردانی که متوازن درآمد و
    گاردهای «ثبتِ دوباره» و «ویرایشِ جلسه‌ی بسته». **۱۴۵ تستِ موبایل.**
  - ابزارِ جانبی: `mobile/scripts/restore-signing.mjs` — `expo prebuild` پوشه‌ی
    `android/` را پاک می‌کند و کلیدِ امضای انتشار با آن می‌رود؛ کلید حالا در
    `mobile/release/signing/` (بیرون از دسترسِ prebuild) نگه داشته می‌شود.

- **۱۴۰۵/۰۶/۱۶ (2026-09-07) — موبایل: تحویلِ بارِ مأمورِ حمل، و دو باگِ شیپ‌شده‌ی بازار:**
  آخرین تکه‌ی فازِ ۴. اندپوینتِ `POST /api/marketplace/distributor/orders/{id}/deliver`
  از قبل بود؛ سرِ وصل‌کردنش دو چیز بیرون آمد که هر دو **روی نسخه‌ی زنده** بودند.
  - **«pending» اصلاً وضعیتِ سفارش نیست.** جدولِ `ORDER_STATUS` در موبایل از روی
    *وضعیتِ اتصال* کپی شده بود؛ `ORDER_STATUSES` واقعی
    `placed|confirmed|delivered|rejected|shipped|received|cancelled` است. پس
    سفارشِ تازه برچسبِ خامِ انگلیسیِ «placed» می‌گرفت و شرطِ `status === 'pending'`
    هرگز درست نمی‌شد — یعنی دکمه‌های تأیید/رد **هیچ‌وقت** روی موبایل ظاهر نمی‌شدند
    و پخش‌کننده از گوشی نمی‌توانست سفارشی را تأیید کند. `MpOrder.status` حالا
    اتحادِ رشته‌ایِ دقیق است، پس کامپایلر خودش محلِ استفاده را قرمز کرد.
  - **صفحه‌ی بازار نقش‌محور نبود.** `canSeeMarket` فقط `view` را می‌سنجید، ولی
    کنش‌ها مجوزهای متفاوت می‌خواهند: تأیید `approve`، رد و وضعیتِ اتصال `update`،
    پیام `create`. مأمورِ حمل که فقط `[view, deliver]` دارد هر سه دکمه را می‌دید و
    روی هرکدام ۴۰۳ می‌گرفت — همان چیزی که نقش‌محوریِ فازِ ۲ در نوارِ تب حذف کرد
    ولی داخلِ صفحه‌ی بازار نرسیده بود. کادرِ نوشتنِ چت هم برایش خوانده‌فقط شد.
  - **`marketDeliver` تنها ردیفِ چنداکشنیِ `access.ts` است** — بازتابِ دقیقِ
    `require_permission("marketplace", ("deliver", "approve"))`: مأمورِ حمل با
    `deliver` و مالک با `approve`. `canAccess` حالا آرایه را «هر کدام کافی است»
    می‌فهمد، مثلِ تاپلِ بک‌اند.
  - **ورودیِ تحویل مبلغ است نه درصد** (`mobile/src/market/cod.ts`): بک‌اند
    `cash_percent` می‌گیرد، ولی کسی که سرِ درِ مغازه ایستاده مبلغ در دستش دارد و
    محاسبه‌ی ذهنیِ درصد یعنی سندِ خزانه‌ی غلط در **دو** کسب‌وکار. `cashAmountFor`
    بازتابِ دقیقِ محاسبه‌ی بک‌اند است (گِردکردنِ نیم‌به‌بالا، بریدن به سقفِ سفارش)
    تا عددی که روی صفحه دیده شد همان باشد که در دفتر می‌نشیند؛ تستِ رفت‌وبرگشت تا
    صد میلیارد ریال. کارتِ سفارش هم «تحویل توسطِ …» را نشان می‌دهد.
  - راستی‌آزمایی: **۱۴۳ تستِ موبایل** (از ۱۲۲)، tsc پاک، ممیز بدونِ خطا،
    `expo export` سالم. فازِ ۴ کامل شد.

- **۱۴۰۵/۰۶/۱۶ (2026-09-07) — موبایل فازِ ۴: انبارگردانی با گوشی و اسکنِ بارکد (بدونِ تغییرِ بک‌اند):**
  فازِ ۲ نشان داد اپِ موبایل برای **انباردار هیچ محتوایی ندارد** — نقشی که کارش
  کاملاً میدانی است. این فاز آن را می‌بندد، و کلش یک *اتصال* بود نه یک ساخت:
  `app/routers/stock_taking.py` با شش اندپوینت از قبل بود و `warehouse_keeper`
  دقیقاً `inventory: [view, create, update]` داشت.
  - **تبِ تازه‌ی «انبارگردانی»** (`mobile/src/screens/stock/`): فهرستِ جلسه‌ها،
    صفحه‌ی شمارش با جست‌وجو و فیلترِ «شمرده‌ی من»/«مغایرت‌دار»، و نوبتِ اسکن.
  - **بازنویسیِ بی‌صدای شمارشِ همکار بسته شد.** `PUT /api/stock-counts/{id}/counts`
    یک *patchِ جزئی* است: هر `line_id` که بفرستی نوشته می‌شود، بقیه دست‌نخورده
    می‌مانند. انبارِ بزرگ را دو نفر با دو گوشی می‌شمارند؛ اگر اپ کلِ فهرست را
    بفرستد — کارِ ساده و طبیعی — شمارشِ نفرِ دوم با مقدارِ کهنه‌ی عکسِ اولیه پاک
    می‌شود، بی هیچ خطایی. `stock/countDraft.ts` **فقط** ردیف‌های دست‌خورده‌ی همین
    گوشی را می‌فرستد، و تست این را قفل می‌کند.
  - **ثبتِ نهایی وقتی چیزی ثبت‌نشده هست بسته است.** «ثبت» سندِ تعدیلِ حسابداری
    می‌سازد؛ با شمارشِ ماندهٔ روی دیسک، سند با اعدادِ قدیمی بسته می‌شد و کارِ یک
    روزِ انباردار در دفتر اثری نمی‌گذاشت.
  - **دروازه‌ی ضدِ اسکنِ تکراری** (`scan/gate.ts`): `onBarcodeScanned` مادامی که
    بارکد در کادر است ده‌ها بار در ثانیه شلیک می‌کند؛ بدونِ دروازه یک بار گرفتنِ
    گوشی جلوی جعبه یعنی «۳۰ عدد». جدا و خالص نوشته شد تا تست‌پذیر باشد.
  - **تطبیقِ بارکد محلی است** (`stock/scanResolve.ts`)، نه با
    `GET /api/items/by-barcode`: انبارِ فلزی آنتن ندارد. سه نتیجه‌ی جدا دارد —
    بارکدِ ناشناس، کالای خارج از جلسه (یعنی بعد از عکس‌برداری ساخته شده)، و پیدا
    شد؛ ادغامشان یعنی کالایی بی‌سروصدا از انبارگردانی جا می‌ماند.
  - **هر اسکن «۱+ از صفر»** است نه «۱+ روی مقدارِ فعلی»: `counted_qty`ِ سرور با
    *موجودیِ سیستمی* پر شده، پس افزودن به آن یعنی «۱۲ تای سیستم + ۱ تایی که شمردم
    = ۱۳» — غلطی که شبیهِ درست هم هست.
  - **اسکنر در انتخابِ کالای فاکتور هم هست** — ویزیتورِ سرِ مشتری.
  - **`mobile/src/lib/format.ts`**: `todayIso` در دو صفحه کپی شده بود و پاک‌سازیِ
    عدد در یکی؛ حالا یک‌جا و با شناختِ ارقامِ عربی‌ـهندی (٠١٢) که صفحه‌کلیدهای
    فارسیِ اندروید هم تولید می‌کنند. تاریخِ جلالی با **`jalaali-js`، همان
    کتابخانه‌ی دسکتاپ** — نه پیاده‌سازیِ جدا و نه `toLocaleDateString('fa-IR')` که
    به ICUِ خودِ گوشی وابسته است و روی دستگاه‌های مختلف یکسان نیست.
  - **وابستگیِ نیتیوِ تازه:** `expo-camera` با `recordAudioAndroid: false` — اپِ
    حسابداری نباید دسترسیِ میکروفون بخواهد. نسخه ۱.۸.۰ (`versionCode` ۹).
  - راستی‌آزمایی: **۱۲۲ تستِ موبایل** (از ۷۰)، ۱۸۱۶ تستِ بک‌اند، tsc پاک، ممیزِ
    ساختاری بدونِ خطا، و `expo export` سالم. تنها نقشی که هنوز در موبایل دستِ
    خالی است «مسئولِ حقوق و دستمزد» است.

- **۱۴۰۵/۰۶/۱۶ (2026-09-07) — عنوانِ دومِ مرکز هزینه، و یک فیلدِ سپیدار که عمداً نیامد (مهاجرتِ ۰۰۹۹):**
  فرمِ «مرکز هزینه»ی سپیدار پنج فیلد دارد: عنوان، عنوان انگلیسی، کد تفصیلی، نوع
  مرکز، وضعیت. سه‌تایش را کوبیتا داشت (`name`, `kind`, `is_active`) و از آن‌ها هم
  جلوتر بود (درختی، سرپرست، بازه، بودجه، گزارشِ حساب‌به‌حساب). دو تا نبود، و
  سرنوشتشان فرق کرد.
  - **«عنوان انگلیسی» اضافه شد** (`cost_centers.name2`). شکافِ واقعی بود: سه
    موجودیتِ هم‌رده از قبل داشتندش — `accounts.name2`، `analytic_accounts.name2` و
    `contacts.name2` — و مرکز هزینه تنها یکی بود که جا مانده بود. برای کسب‌وکارِ
    صادراتی که گزارشِ دوزبانه می‌خواهد همان کمبود است. اختیاری ماند (مثلِ `code`)،
    چون اجباری‌کردنش ساختنِ مرکزِ داخلی را ناممکن می‌کرد.
  - **«کد تفصیلی» عمداً نیامد — و این مهم‌ترین بندِ این ردیف است**، چون در نگاهِ
    اول شبیه یک شکاف است و دفعه‌ی بعد کسی دوباره فکر می‌کند جا افتاده.
    **سپیدار مرکز هزینه را *به‌عنوان* تفصیلی پیاده کرده**: بُعدِ جدایی برایش ندارد و
    با سطحِ تفصیلی شبیه‌سازی‌اش می‌کند. آن فیلد محصولِ معماریِ سپیدار است، نه یک
    قابلیت. کوبیتا همان چیز را درجه‌یک دارد — `journal_lines` هر دو ستون را جدا
    نگه می‌دارد (`cost_center_id` و `analytic_id`)، و docstringِ `AnalyticAccount`
    تفکیک را صریح ثبت کرده: «لحظه‌ای که چیزی ساختار بگیرد، جایش مرکز هزینه است نه
    اینجا». پیوند دادنشان یعنی یک چیز روی یک ردیفِ سند در دو بُعد بنشیند — همان
    «دو نمای یک داده»ی قیدِ ۶ `CLAUDE.md`، که دو عددِ متناقض در گزارش می‌سازد.
    (`contacts.analytic_id` نقضِ این نیست: طرف‌حساب **ذاتاً** یک تفصیلی است چون
    دریافتنی به نامش ثبت می‌شود، نه یک بُعدِ ساختاریِ موازی.)
  - **نوع‌های تازه هم اضافه نشد.** «تولیدی/اداری/فروش/خدمات» *نمونه‌های* «واحد
    سازمانی»اند نه نوعِ ساختاریِ تازه؛ شش نوعِ فعلی پوششش می‌دهد.
  - ۳ تستِ تازه، `tsc -b` صفر، ممیزِ صفحه‌ها صفر خطا. مهاجرت از صفر تا ۰۰۹۹ روی
    اسکیمای یک‌بارمصرف اجرا و چرخه‌ی `downgrade→upgrade`اش بسته شد.

- **۱۴۰۵/۰۶/۱۶ (2026-09-07) — فروشنده و نوعِ فروش بالاخره روی فاکتور می‌نشینند (بدونِ مهاجرت):**
  `sales_invoices.salesperson_id` و `sale_type_id` از روزِ اول روی جدول بودند و
  **هیچ‌جا نوشته نمی‌شدند** — نه در `SalesInvoiceIn` بودند، نه سرویس می‌نشاندشان، نه
  فرم می‌فرستدشان.
  - **پیامدش بدترین نوعِ خرابی بود: ساکت.** `preview_commission` با
    `salesperson_id.isnot(None)` فیلتر می‌کند، پس **محاسبه‌ی پورسانتِ فروشنده هر بار
    صفر ردیف برمی‌گرداند**. کاربر قاعده تعریف می‌کرد، «محاسبه» می‌زد، و خالی
    می‌گرفت بی‌آنکه هیچ خطایی ببیند — دو صفحه‌ی کاملِ ساخته‌شده با ورودیِ قطع.
    مهم‌ترین تستِ این دور همین را قفل می‌کند: اگر بشکند، هر دو صفحه دوباره بی‌فایده
    شده‌اند بی‌آنکه کسی خبردار شود.
  - **فروشنده باید عضوِ همین کسب‌وکار باشد.** `memberships` جدولِ سراسری و بی‌RLS
    است، پس فیلترِ `tenant_id` صریح آمد — بدونِ آن هر کاربری از هر کسب‌وکارِ دیگری
    روی فاکتور می‌نشست و در پورسانتِ آن‌ها ظاهر می‌شد.
  - **نوعِ فروشِ غیرفعال رد می‌شود**، همان قاعده‌ی مرکز هزینه و به همان دلیل:
    «غیرفعال» باید در سرور معنا داشته باشد وگرنه فقط یک فیلترِ سمتِ کلاینت است.
    ولی برخلافِ `resolve_cost_center_id` راهِ فرارِ `current=` ندارد، چون فاکتور
    ویرایش نمی‌شود (ابطال و صدورِ تازه می‌شود) پس مقدارِ «دست‌نخورده»ای وجود ندارد
    که قفل شود.
  - **`due_days` و `default_tax_rate`ِ نوعِ فروش عمداً وصل نشدند.** docstringِ خودِ
    مدل می‌گوید باید خودکار پر شوند، ولی پرکردنِ نرخِ مالیات **مبلغِ فاکتور را عوض
    می‌کند** — و این کار قرار بود فقط ستون‌های مرده را وصل کند بی‌آنکه هیچ عددی
    تکان بخورد. در [FUTURE_WORK.md](FUTURE_WORK.md) ثبت شد.
  - فرم و ویزاردِ فروش دو انتخابگر گرفتند و فهرستِ فاکتور فروشنده و نوعِ فروش را
    نشان می‌دهد؛ بدونِ نمایش، کاربر نمی‌فهمد پورسانت به نامِ چه کسی رفته.
  - **مهاجرت نداشت** — ستون‌ها از قبل بودند. ۸ تستِ تازه، `tsc -b` صفر، ممیزِ
    صفحه‌ها صفر خطا.

- **۱۴۰۵/۰۶/۱۵ (2026-09-06) — کارمزدِ واسطه و دو قاعده‌ی مرکز هزینه (مهاجرت‌های ۰۰۹۷ و ۰۰۹۸):**
  هر سه چیزِ این دور یک الگو داشتند: **تنظیمی که ذخیره می‌شد و هیچ اثری نداشت.**
  - **واسطه به فاکتور وصل شد** (`0097`). `contacts.is_broker` و `commission_rate` از
    قبل بودند و به هیچ معامله‌ای نمی‌چسبیدند. حالا `sales_invoices.broker_id` و
    `broker_commission` هست، سرور نقشِ «واسط» را می‌سنجد، و فرم/ویزارد/فهرستِ فاکتور
    همه سیم‌کشی شدند. **کارمزد در لحظه‌ی ثبت قفل می‌شود و مشتق نیست** — نرخِ روی
    طرف‌حساب فردا عوض می‌شود ولی کارمزدِ این فروش همان‌جا قطعی شده؛ اگر مشتق می‌ماند،
    بالابردنِ نرخ صورت‌حسابِ تسویه‌شده‌ی ماهِ پیش را هم بالا می‌برد. همان دلیلِ
    ذخیره‌شدنِ `tax_amount` کنارِ `tax_rate`. مبنا خالصِ پس از تخفیف و پیش از مالیات
    است، همان مبنای `basis="revenue"`ِ پورسانتِ فروشنده. **سندِ حسابداریِ خودکارِ
    کارمزد عمداً ساخته نشد**: لحظه‌ی شناسایی (فروش؟ وصول؟) و رفتارش هنگام ابطال
    تصمیمِ حسابداری است نه فنی.
  - **کدِ مرکز هزینه یکتا شد** (`0098`). هیچ قیدی نبود، پس دو مرکز با کدِ ۱۰۰۱ ممکن
    بود. ایندکس **جزئی** است (`WHERE code <> ''`) تا کد اختیاری بماند — با قیدِ ساده
    دومین مرکزِ بی‌کد به اولی گیر می‌کرد چون `''` هم یک مقدار است. محلی و production
    هر دو پیش از ساختِ مهاجرت بررسی شدند: صفر ردیف، پس هیچ داده‌ای نشکست.
  - **«مرکزِ غیرفعال» در سرور معنا پیدا کرد.** `resolve_cost_center_id` فقط *وجودِ*
    مرکز را می‌سنجید؛ چهار فرم در مرورگر فیلتر می‌کردند ولی فرمِ قراردادِ حقوق حتی
    همان را هم نمی‌کرد. تنشی که حل شد: گاردِ کورکورانه ویرایشِ قالبِ تکرارشونده‌ی
    قدیمی را می‌شکست، پس فقط وقتی می‌گیرد که مقدار **عوض شده باشد** (`current=`).
    **معافیتِ آفلاین عمداً ساخته نشد** (برخلافِ سقفِ اعتبار): صفِ آفلاین ردیفِ ناموفق
    را با `sync_error` نگه می‌دارد و دوباره می‌فرستد، پس فاکتور از بین نمی‌رود.
  - **یافته‌ای که ثبت شد ولی درست نشد:** `sales_invoices.salesperson_id` و
    `sale_type_id` هیچ‌جا نوشته نمی‌شوند، و `commission_run` با
    `salesperson_id.isnot(None)` فیلتر می‌کند — یعنی محاسبه‌ی پورسانتِ فروشنده هر بار
    صفر ردیف برمی‌گرداند. دو صفحه‌ی کامل با ورودیِ قطع. در
    [FUTURE_WORK.md](FUTURE_WORK.md) با شرح ثبت شد.
  - **۱۷۹۰ تستِ بک‌اند سبز** (۱۵ تای تازه)، `tsc -b` صفر، ممیزِ صفحه‌ها صفر خطا. هر دو
    مهاجرت از صفر روی اسکیمای یک‌بارمصرف اجرا و چرخه‌ی `downgrade→upgrade`شان بسته شد.
    **روی production مستقر شد** (نسخه‌ی `0098`، سرویس سالم، `acc.cubita.ir` → ۲۰۰).

- **۱۴۰۵/۰۶/۱۵ (2026-09-06) — کارِ دو‌نفره: دسترسیِ استقرار برای هر دو باز شد:**
  تا اینجا یک نفر مسئولِ استقرار بود و نفرِ دوم فقط کد می‌داد. دو مانع بود: قیدِ
  نوشته‌شده در `CLAUDE.md`، و — مهم‌تر — اینکه روی `authorized_keys`ِ سرور فقط یک
  کلید بود (`claude-deploy-ipnet`)، پس نفرِ دوم اصلاً نمی‌توانست وصل شود. هر دو
  برداشته شد: قید به «هر دو مجازند، ولی قبلش هماهنگ کن» تبدیل شد، و یک کلیدِ
  ed25519ِ **اختصاصی** (`cubita-benyamin`) روی سرور نصب شد. کلیدِ اختصاصی و نه
  اشتراکِ کلیدِ موجود، چون بستنِ دسترسیِ یک نفر باید حذفِ یک خط باشد، نه چرخاندنِ
  کلید برای همه. قیدِ `deploy.sh demo` سرِ جایش ماند و صریح شد که قاعده‌ی *درستی*
  است نه دسترسی. `CONTRIBUTING.md` بخشِ «دسترسی به سرور» با دستورهای واقعیِ
  استقرار گرفت. کافه‌بازار/زرین‌پال/ملی‌پیامک بیرونِ VPS‌اند و با SSH باز نمی‌شوند.
- **۱۴۰۵/۰۶/۱۵ (2026-09-06) — هشدارِ لیستِ سیاه، مانده‌ی اول دوره به‌تفکیکِ نقش، نوعِ نشانیِ «سایر» (مهاجرتِ ۰۰۹۳):**
  - **لیستِ سیاه از یک برچسبِ بی‌اثر به یک هشدارِ واقعی تبدیل شد.** `is_blacklisted`
    ذخیره می‌شد و هیچ‌جا دیده نمی‌شد. حالا `BlacklistBanner` در **هر چهار** ورودیِ
    فاکتور (فرم و ویزاردِ فروش، فرم و ویزاردِ خرید) هنگام انتخابِ آن طرف‌حساب هشدار
    می‌دهد. **عمداً بلاک‌کننده نیست** — تصمیمِ صریحِ کاربر: گاهی آگاهانه و موقتاً برای
    همان شخص فاکتور می‌زنیم، و اگر ثبت بسته می‌شد کاربر تیک را برمی‌داشت تا کارش راه
    بیفتد و نشانه برای همیشه از بین می‌رفت. همان قاعده‌ی بنرِ اعتبار: گزارش، نه گارد.
  - از **خودِ رکوردِ طرف‌حساب** خوانده می‌شود (`contacts` از قبل در هر دو هوکِ فاکتور
    بارگذاری شده)، نه از یک درخواستِ جدا — نمای دومِ همان داده ساخته نشد.
  - **مانده‌ی اول دوره حالا نقش‌آگاه است.** پیش از این فرم مانده‌ی «به‌عنوان
    تأمین‌کننده» را از کسی هم می‌پرسید که اصلاً تأمین‌کننده نبود. حالا هر مانده فقط
    با تیکِ نقشِ خودش دیده می‌شود، و مانده‌ی نقشِ تیک‌نخورده در ثبت **صفر** می‌رود —
    حتی اگر کاربر عددی تایپ کرده و بعد تیک را برداشته باشد. وگرنه ردیفی در سندِ
    افتتاحیه می‌نشست که معنایی نداشت.
  - **نوعِ نشانیِ «سایر» (مهاجرتِ ۰۰۹۳).** هفت نوعِ سپیدار همه‌ی حالت‌ها را نمی‌پوشاند
    (کارگاه، نمایشگاه، دفترِ موقت)؛ بی‌گزینه‌ی «سایر» کاربر یکی از هفت‌تا را دروغ
    انتخاب می‌کرد و از آن به بعد فیلترِ «ارسال کالا» نشانی‌هایی برمی‌گرداند که نشانیِ
    ارسال نبودند. فهرست باز نشد — «سایر» درِ خروج است و `title` می‌گوید دقیقاً چیست.
    مهاجرت فقط قیدِ بررسی را از نو می‌سازد؛ هیچ ردیفی دست نمی‌خورد.
  - **`FUTURE_WORK.md` ساخته شد** — فهرستِ کارهای شناخته‌شده‌ی هنوز انجام‌نشده، با
    قاعده‌ی روشن: هر ردیف باید بگوید چه کم است، چرا مهم است، و از کجا شروع کنیم؛ و
    وقتی انجام شد از آن‌جا حذف و به همین بخشِ ۱۰ منتقل می‌شود (دو فهرستِ موازی نه).
    سرفصلِ اولش: **نمایشِ نشانی روی نقشه** — `latitude`/`longitude` ذخیره می‌شوند و
    هیچ‌جا نشان داده نمی‌شوند، پس عملاً خالی می‌مانند.
  - **مالکیتِ جدول‌ها در دیتابیسِ محلی اصلاح شد:** `contact_channels` و
    `contact_addresses` به `postgres` رسیده بودند (مهاجرتِ ۰۰۹۱ و ۰۰۹۲ با نقشِ اشتباه
    اجرا شده بود) در حالی که بقیه‌ی جدول‌ها مالِ `cubita_migrate`اند. همین باعث شد
    ۰۰۹۳ با «must be owner» بشکند. مالکیت به `cubita_migrate` برگردانده شد.
  - **راستی‌آزمایی:** ۱۶۶۶ تستِ backend سبز؛ `tsc -b --force` صفر؛ `audit-pages.mjs`
    صفر خطا؛ `oxlint` صفر خطا.

- **۱۴۰۵/۰۶/۱۵ (2026-09-06) — چهار نقشِ هم‌زمانِ طرف حساب و ویرایشِ جزئی (بدونِ مهاجرت):**
  - **نقش‌ها از کشویی به چهار تیکِ مستقل تبدیل شدند:** مشتری، تأمین‌کننده، **واسطه**،
    سهامدار. پیش از این «نقش اصلی» یک `<select>` با سه گزینه بود و واسطه یک تیکِ
    جداافتاده زیرِ فیلدهای اعتبار — یعنی عملاً نقش نبود. یک آدم واقعاً می‌تواند
    هم‌زمان از ما بخرد، به ما بفروشد، مشتری معرفی کند و سهامدار باشد.
  - **ستونِ `contacts.type` دست نخورد.** همان سه مقدار را دارد و از دو تیکِ معاملاتی
    ساخته می‌شود؛ واسطه و سهامدار پرچمِ مستقل‌اند. طرف‌حسابی که هیچ‌کدام از دو نقشِ
    معاملاتی را ندارد **تأمین‌کننده** ثبت می‌شود، چون جریانِ پول از ما به اوست
    (پورسانت یا سودِ سهام) — و همین جمله زیرِ تیک‌ها نوشته شده تا نگاشت پنهان نباشد.
    مهاجرت لازم نشد: بدونِ ستونِ تازه، بدونِ backfill روی جدولِ RLS.
  - **واسطه ≠ فروشنده.** فروشنده (`CommissionRule` در `sales_ops`) کاربرِ داخلیِ
    سامانه است و پورسانتش پرداخت به کارمند؛ واسطه طرفِ بیرونی است و پورسانتش بدهی
    به یک طرفِ حساب. دو چیزِ متفاوت‌اند، پس `contacts.commission_rate` نمای دومِ
    قاعده‌ی پورسانتِ فروشنده نیست.
  - **`PATCH /api/contacts/{id}` واقعاً جزئی شد (`exclude_unset`).** ایرادِ واقعی و
    بی‌سروصدا: فرمِ ساده‌ی طرف‌حسابِ ماژولِ فروش فقط چند فیلدِ پایه می‌فرستد و
    `model_dump()`ِ کامل هر چیزِ نفرستاده را به پیش‌فرضِ اسکیما برمی‌گرداند — ویرایشِ
    یک شماره‌تلفن نقشِ واسطه و سهامدار، نرخِ پورسانت، مشخصاتِ شخصی و مانده‌ی اول
    دوره را پاک می‌کرد (و اگر افتتاحیه ثبت بود، ۴۰۹ می‌داد). حالا قفلِ افتتاحیه هم
    فقط وقتی می‌گیرد که فیلدهای مانده *فرستاده شده* باشند.
  - **`title2` در تفصیلی سه‌حالتی شد:** `None` = نفرستاده (دست نزن)، `""` = پاک کن.
    و در ویرایش، نامِ طرف‌حساب فقط وقتی جایگزینِ عنوانِ تفصیلی می‌شود که کاربر خودِ
    عنوان را فرستاده و خالی گذاشته باشد — وگرنه عنوانِ دستی با هر ویرایشِ نام
    بازنویسی می‌شد.
  - **فهرستِ طرف‌حساب‌ها:** ستونِ «نوع» شد «نقش» و همه‌ی نقش‌ها را نشان می‌دهد، به‌علاوه‌ی
    فیلترِ نقش. در فهرستِ قدیمیِ ماژولِ فروش هم نشانِ «واسطه»/«سهامدار» کنارِ نوع
    اضافه شد، وگرنه کاربر گمان می‌کرد نقش پاک شده.
  - **راستی‌آزمایی:** ۱۶۶۵ تستِ backend سبز (چهار تستِ تازه: چهار نقشِ هم‌زمان،
    ویرایشِ جزئی که نقش‌ها را نگه می‌دارد، عنوانِ تفصیلی که بازنویسی نمی‌شود، و
    قفلِ افتتاحیه که سرِ ویرایشِ جزئی نمی‌گیرد)؛ `tsc -b --force` صفر؛
    `audit-pages.mjs` صفر خطا؛ `oxlint` صفر خطا.

- **۱۴۰۵/۰۶/۱۵ (2026-09-06) — تب‌های نشانی/تلفن/افرادِ مرتبط/کارمند، نقشِ سهامدار، و پیشنهادِ خودکارِ تفصیلی (مهاجرتِ ۰۰۹۲):**
  - **`contact_addresses` جدا از `contact_channels` شد.** در ۰۰۹۱ یک جدول برای هر دو
    ساخته بودم به گمانِ اینکه شکلشان یکی است؛ فرمِ واقعیِ سپیدار نشان داد نیست —
    تلفن سه فیلد دارد و نشانی شانزده (شهر، کد پستی، مختصات، کد و عنوانِ مسیر/منطقه،
    کد شعبه). یک جدول یعنی سیزده ستونِ همیشه‌خالی برای هر شماره.
  - **نشانی نوع دارد** (رسمی/محل فعالیت/ارسال صورتحساب/**ارسال کالا**/انبار/منزل/پستی).
    کاربردِ عملیاتی‌اش ارسال است: نشانیِ «ارسال کالا» با مختصات و کدِ مسیر همان چیزی
    است که به مأمورِ ارسال می‌رود. **`route_code` همان «زون»ِ توزیع است** — فعلاً متن،
    و وقتی موجودیتِ زون ساخته شد باید کلیدِ خارجی شود نه فهرستی متنیِ موازی.
  - **شهرِ نشانی کلیدِ خارجیِ `geo_locations` است، نه متن** — وگرنه «تهران» و «طهران»
    دو شهر می‌شدند و گزارشِ منطقه‌ای درنمی‌آمد.
  - **«اصلی» در هر فهرست فقط یکی**؛ زدنِ آن روی ردیفِ تازه، قبلی را پایین می‌آورد
    به‌جای اینکه ثبت را رد کند — نیتِ کاربر روشن است.
  - **مشخصاتِ شخصی روی طرف‌حساب نشست، نه روی کارمند** (جنسیت، تأهل، تعداد فرزند،
    افرادِ تحتِ تکفل، تحصیلات). اینها واقعیتِ *شخص*اند نه شغلش؛ روی `employees` که
    می‌نشستند، مشتریِ غیرکارمند هیچ‌وقت نمی‌توانست داشته باشدشان. واقعیت‌های
    *استخدام* (حکم، تاریخِ استخدام، شماره حساب، مرخصی) سرِ جایشان در حقوق و دستمزد
    ماندند و `employee_id` پلِ این دو است.
  - **سهامدار: نقشِ چهارم**، مثلِ واسطه پرچمِ مستقل و نه مقدارِ تازه‌ی `type`.
  - **عنوانِ تفصیلی از نام و نام خانوادگی پیشنهاد می‌شود** و تا کاربر دستش را رویش
    نگذاشته با هر تایپ به‌روز می‌شود. **تکراری بودنش قرمز می‌شود ولی ثبت را نمی‌بندد**
    (`GET /api/contacts/tafsili-title-taken`) — دو نفرِ هم‌نام واقعاً ممکن‌اند و کاربر
    فقط باید *بداند*. **کدِ خالی خودکار ساخته می‌شود**؛ شماره‌ی تفصیلی ارجاعِ داخلی
    است و اجبار کردنش فقط یک مانع بود.
  - **صفحه به فایلِ خودش منتقل شد** (`pages/company/ContactFormPage.tsx`) — ۷۳۰ خط
    شده بود و جایش در `CompanyOpsPages` نبود.
  - **دو ایراد در آزمونِ زنده پیدا شد و هر دو تست گرفتند:** (۱) در حالتِ «ترکیبی»،
    عنوانِ تفصیلیِ بدونِ کد بی‌صدا دور ریخته می‌شد — حالا عنوان دادن یعنی «تفصیلی
    می‌خواهم» و کد خودکار ساخته می‌شود؛ تشخیصش به عنوانِ *صریحِ کاربر* گره خورد نه
    عنوانِ جایگزین، وگرنه هر طرف‌حسابی تفصیلی می‌گرفت. (۲) `name2`/`role2`ِ افرادِ
    مرتبط ستون داشتند ولی سرویس نه ذخیره‌شان می‌کرد نه برمی‌گرداند.
  - **قاعده‌ی خواندنِ فرمِ سپیدار ثبت شد:** «(۲)» = فیلدِ دومِ اختیاری (لاتین)، «*» =
    اجباری. در حافظه‌ی دائمی نگه داشته شد تا هر بار پرسیده نشود.
  - **`verify-pages.mjs` قابلِ اجرا نیست:** مهارتِ `cubita-page` می‌گوید حتماً اجرا
    شود، ولی `playwright` هیچ‌وقت به `package.json` اضافه نشده. تأییدِ مرورگری انجام
    *نشد*؛ به‌جایش رفت‌وبرگشتِ کاملِ API آزموده شد.
  - **راستی‌آزمایی:** ۱۶۶۱ تستِ بک‌اند سبز · `tsc -b --force` صفر خطا · ممیزِ صفحه‌ها
    صفر خطا · `oxlint` بدونِ خطا · آزمونِ زنده (ساخت + تفصیلیِ خودکار + هشدارِ تکراری
    + نشانیِ ارسال + تلفنِ انبار + فردِ مرتبط) و سپس پاک‌سازیِ کاملِ داده‌ی آزمایشی.

- **۱۴۰۵/۰۶/۱۵ (2026-09-06) — فرمِ کاملِ طرف حساب: پیوند به تفصیلی و کارمند، چند تلفن/نشانی، مانده‌ی اول دوره (مهاجرت‌های ۰۰۹۰ و ۰۰۹۱):**
  کاربر فرمِ «تعریف طرف حساب»ِ سپیدار را داد و خواست همان داده‌ها را بشود ثبت کرد.
  - **دو پیوند، نه دو کپی — تصمیمِ محوریِ این کار.** «کد/عنوان تفصیلی» ستونِ
    `contacts` **نشد**؛ به `analytic_accounts` وصل شد، همان جدولی که ردیفِ سند و
    «تفصیلی سایر» از آن می‌خوانند. همچنین تبِ «مشخصات کارمند» فقط `employee_id` است
    و نام/کد ملی/تاریخِ استخدام در ماژولِ حقوق می‌مانند. یک آدم، یک رکورد.
    `test_tafsili_is_a_link_not_a_copy` این را قفل می‌کند.
  - **قاعده‌ی تفصیلیِ طرف حساب از سطحِ اجبار می‌آید** (تنظیمات ← شخصی‌سازی):
    `strict` → کد اجباری، `hybrid` → اختیاری، `floating` → فیلد پنهان. کدِ آزادِ
    بعدی از سرور پیشنهاد می‌شود. **در `floating` تفصیلیِ موجود پاک نمی‌شود** —
    عوض‌کردنِ یک تنظیم نباید داده‌ی ثبت‌شده را ببرد.
  - **«واسطه» پرچمِ مستقل شد، نه مقدارِ تازه‌ی `type`.** ده‌ها فیلتر و گزارش روی
    (customer, supplier, both) تکیه دارند؛ افزودن به آن یعنی بازبینیِ همه‌شان.
  - **`name` نامِ نمایشی ماند** و `first_name`/`last_name` کنارش نشستند؛ سرویس
    نمایشی را از آن دو می‌سازد. شکستنِ `name` تغییرِ شکننده‌ای بود (فاکتور، گزارشِ
    فصلی، صورت‌حساب همه می‌خوانندش).
  - **سقفِ اعتبار بالاخره معنا پیدا کرد.** تا امروز ذخیره می‌شد ولی هیچ‌جا اعمال
    نمی‌شد؛ `credit_action` (none/warn/block) اضافه شد با پیش‌فرضِ `none` تا سقف‌های
    ثبت‌شده‌ی قبلی یک‌شبه جلوی فروش را نگیرند.
  - **`contact_channels`** برای تلفن/نشانیِ **اضافه**. `contacts.phone` و
    `contacts.address` سرِ جایشان ماندند و «اصلی» هستند، چون ده‌ها جا مستقیم
    می‌خوانندشان — پس هیچ ردیفی در جدولِ تازه کپیِ چیزی نیست.
  - **مانده‌ی اول دوره‌ی طرف‌حساب فقط یک عدد نشد.** الگویِ «موجودیِ اول دوره»ی کالا:
    عدد روی طرف‌حساب می‌نشیند و `create_opening_entry` از رویش ردیفِ سند می‌سازد
    (با تفصیلیِ خودِ طرف‌حساب). و مهم‌تر: در **کارتِ حساب** و **تحلیلِ سنی** هم دیده
    می‌شود — هر دو از فاکتور می‌خوانند نه از سند، پس بدونِ این کار عددی می‌شد که
    هیچ‌جا اثر ندارد. **پس از ثبتِ افتتاحیه قفل می‌شود** تا عددِ روی طرف‌حساب و ردیفِ
    سند از هم جدا نیفتند.
  - **`statement_type` مشتق ماند** و ستون نشد (دو منبعِ حقیقت).
  - **دکمه‌ی «طرف حساب جدید» به فهرستِ طرف‌حساب‌ها اضافه شد.** کاربر گزارش داد که
    از آن صفحه هیچ راهی به فرمِ ساخت نیست. ساخت همچنان یک‌جاست؛ این فقط میان‌بُر است.
  - **تصحیحِ روشِ راستی‌آزمایی:** `npx tsc --noEmit` روی ریشه **هیچ‌چیز را نمی‌سنجید**
    (`tsconfig.json` ریشه `"files": []` دارد و فقط ارجاع است). بررسیِ درست
    `npx tsc -b` است. نتیجه‌های «صفر خطا»ی پیش از این تاریخ با آن دستور بی‌اعتبارند.
  - **راستی‌آزمایی:** ۱۶۳۲ تستِ بک‌اند سبز · `tsc -b --force` صفر خطا · ممیزِ صفحه‌ها
    صفر خطا · `oxlint` بدونِ خطا · رفت‌وبرگشتِ واقعی روی سرورِ محلی (ساختِ طرف‌حساب با
    تفصیلی و مانده و کانال، سپس پاک‌سازیِ کامل).

- **۱۴۰۵/۰۶/۱۳ (2026-09-04) — تطبیق با مشخصاتِ «کدینگ حسابداری»: تفصیلیِ اجباری، سه‌گانه‌ی ارزی، خام‌سازی (بدونِ مهاجرت):**
  کاربر سندِ مشخصاتِ ماژولِ کدینگ (برگرفته از سپیدار) را داد و ده کمبود پیدا شد.
  همه ساخته شد. **هیچ ستونِ تازه‌ای لازم نشد** — کلِ کار روی ساختارِ موجود نشست.
  - **`accepts_tafsili` معنایش عوض شد و گاردِ درختی‌اش برداشته شد.** یک نسخه پیش‌تر
    ساختِ زیرحساب را کنترل می‌کرد؛ آن دو چیزِ متفاوت را یکی گرفته بود. زیرشاخه‌ی
    درختی (بانک ملی زیرِ بانک) بخشی از **کدینگِ چهارسطحی** است — کم‌تعداد، ثابت، در
    خودِ کد — و قیدِ واقعی‌اش همان «والد سندِ مستقیم نخورده باشد» است. `accepts_tafsili`
    حالا فقط درباره‌ی **تفصیلیِ شناور** (`analytic_accounts`) روی ردیفِ سند است:
    بُعدی متغیر و پرتعداد که اصلاً در کد نمی‌آید. کوبیتا هر دو را دارد و جدا نگه
    داشتنشان درست است؛ اشتباه فقط این بود که یک پرچم هر دو را ادعا می‌کرد.
  - **تفصیلی روی حسابِ تفصیل‌پذیر اجباری شد.** ردیفِ بدونِ تفصیلی سوراخی در همان
    گزارشی است که کاربر برایش پرچم را روشن کرده. گارد **پس از ارث‌بریِ سطحِ سند**
    می‌سنجد، وگرنه سندی که تفصیلی‌اش را یک‌بار بالا داده رد می‌شد.
    **مرزش عمدی است: فقط سندِ دستی.** ردیف‌هایی که ماژول‌ها می‌سازند (فاکتور، حقوق،
    خزانه) بُعدشان را از سندِ منبع می‌گیرند؛ اگر گارد به `make_journal_entry` منتقل
    شود، روشن‌کردنِ یک تیک در چارت می‌تواند ثبتِ فاکتور را از کار بیندازد.
  - **قفلِ تفصیلی‌پذیری یک‌طرفه است، نه دوطرفه.** مشخصات می‌گوید «بعد از یک سند قفل،
    مگر همه‌ی اسناد پاک شوند»؛ در کوبیتا آن یعنی قفلِ ابدی، چون سند هرگز پاک نمی‌شود
    و فقط باطل می‌شود. روشن‌کردن همیشه آزاد، خاموش‌کردن فقط تا وقتی هیچ ردیفی تفصیلی
    نگرفته — همان ضرر (گزارشِ نصفه‌پر) را می‌بندد بی‌آنکه قفل ابدی شود.
  - **سه‌گانه‌ی ارزی یا کامل است یا اصلاً نیست** (`JournalLineIn._fx_is_all_or_nothing`).
    این تنها موردِ فهرست بود که **نقصِ داده** بود نه قابلیتِ نداشته: بدونِ `fx_rate`
    نرخِ لحظه‌ی ثبت برای همیشه گم می‌شد؛ بدونِ `fx_amount` ردیف از فیلترِ
    `fx_revaluation_preview` بیرون می‌افتاد و تسعیرِ پایانِ دوره کمتر از واقعیت
    درمی‌آمد — هر دو بی‌هیچ خطایی. محاسبه‌ی `ریال = ارز × نرخ` از قبل در فرم بود و
    دست نخورد؛ فقط گاردِ سرور اضافه شد.
  - **`statement_type` مشتق شد، نه ستون.** صددرصد از `type` درمی‌آید و ذخیره‌کردنش
    دو منبعِ حقیقت می‌ساخت که می‌توانند با هم نخوانند. تنها جایی که ستونِ صریح موجه
    می‌شود، روزی است که حساب‌های *انتظامی* اضافه شوند و اشتقاق بشکند.
  - **`DELETE /api/accounts` — خام‌سازیِ درختواره**، با همان گاردی که مشخصات می‌گوید:
    فقط وقتی در کلِ کسب‌وکار هیچ سندی ثبت نشده. با یک سند هم رد می‌شود، نه «تا جایی
    که می‌شود پاک کن» — نتیجه‌ی نصفه بدتر از انجام‌نشدن است. حسابی که ارجاعِ غیرِسند
    دارد (بانک، دارایی، تنظیمات) با دلیل در `kept` برمی‌گردد.
  - **قالب‌ها از کد به داده رفتند** (`app/data/chart_templates.json`). لودر شکلِ فایل
    را لحظه‌ی بارگذاری می‌سنجد — نوعِ نامعتبر، ردیفِ ناقص، کدِ غیرعددی، و **والدی که
    هیچ‌جا تعریف نشده** — تا خرابیِ داده موقعِ بالاآمدن پیدا شود نه وسطِ درجِ قالب
    برای مشتری.
  - **رابط:** تأییدِ اجباری پیش از **درجِ** قالب (تا امروز فقط برگرداندن تأیید داشت،
    در حالی که درج همان کاری بود که یک‌بار چارت را پر از حسابِ بی‌ربط کرد) · پیامِ
    درختواره‌ی خالی که کاربر را به قالب‌ها می‌فرستد به‌جای بن‌بستِ «حسابی وجود ندارد» ·
    منوی راست‌کلیک روی سطر (**افزوده بر دکمه‌ها، نه جایگزین** — روی موبایل و وب
    راست‌کلیک نیست؛ «حذف» عمداً در آن نیست) · ستونِ تفصیلیِ هر ردیف در فرمِ سند که
    فقط برای حساب‌های تفصیل‌پذیر ظاهر می‌شود و پیش از رفتن به سرور می‌گوید *کدام
    ردیف* تفصیلی کم دارد · کارتِ «خام‌سازی درختواره» و نمایشِ صورتِ مالی در فرمِ
    ویرایشِ حساب.
  - **چهار بندِ مشخصات عمداً اجرا نشد** و دلیلشان ثبت است: قفلِ ویرایشِ `code`/`nature`
    برای حسابِ سنددار (کوبیتا عمداً برعکس عمل می‌کند، چون ثبتِ خودکار حساب را با
    `system_role` می‌شناسد نه با کدش)؛ «ساخت معین بدونِ کل» که از قبل با قاعده‌ی طول
    و پیشوندِ کد بسته است؛ و اشتقاقِ `statement_type` به‌جای ستون.
  - **دو تستِ دیروز بازنویسی شد، نه حذف:** `test_create_tafsili_under_an_unposted_moin`
    حالا **سه‌بار** عوض شدنش را با دلیل مستند می‌کند، و تستِ «تا زیرمجموعه دارد» جایش
    را به قفلِ نامتقارن داد.
  - **راستی‌آزمایی:** ۱۵۷۰ تستِ بک‌اند سبز · `tsc --noEmit` صفر خطا · ممیزِ صفحه‌ها
    صفر خطا · `oxlint` بدونِ خطا · رفت‌وبرگشتِ واقعی روی سرورِ محلی: صورتِ مالیِ
    مشتق، ردِ ردیفِ ارزیِ ناقص، ردِ سندِ بی‌تفصیلی، قفلِ یک‌طرفه، و گاردِ خام‌سازی.

- **۱۴۰۵/۰۶/۱۳ (2026-09-04) — ویژگی‌های حساب: شش پرچمِ فرمِ سپیدار و پیگیریِ ردیفِ سند (مهاجرتِ `0088`):**
  ادامه‌ی همان مقایسه با سپیدار؛ این بار قابِ «ویژگیهای حساب» در فرمِ ویرایشِ حساب.
  شش ستونِ بولی روی `accounts` و دو ستونِ اختیاری روی `journal_lines`.
  - **قیدِ حاکم: هیچ پیش‌فرضی رفتارِ موجود را عوض نکرد.** مهاجرت هیچ ردیفی را
    backfill نمی‌کند (پیش‌فرضِ DDL در PostgreSQL ۱۱+ در کاتالوگ می‌نشیند، پس هیچ
    `UPDATE`ی روی جدولِ RLS‌دار اجرا نمی‌شود) و هر شش پرچم طوری طراحی شده‌اند که
    پیش‌فرضشان چیزی را نبندد. تست‌های `test_account_traits.py` همین را قفل می‌کنند.
  - **کنترل ماهیت طی دوره** (`nature_control`) — جدا از خودِ `nature`: آن می‌گوید
    ماهیت *چیست*، این می‌گوید *رصدش کن*. پارامترِ `controlled_only` روی
    `/api/reports/nature-violations` گزارش را به همین‌ها محدود می‌کند؛ **پیش‌فرض
    خاموش** تا گزارشِ موجود بی‌صدا خالی نشود.
  - **ارزی / تسعیر پذیر** (`is_fx`, `fx_revaluable`) — قیدِ `ck_accounts_fx_revaluable`
    دومی را به اولی گره می‌زند (در فرم هم تا «ارزی» تیک نخورد خاکستری است).
    `fx_revaluable` عمداً **انصراف است نه انتخاب**: سندِ تسعیر همه‌ی حساب‌های دارای
    ردیفِ ارزی را می‌بیند، مگر حسابی صریحاً ارزی علامت خورده و این خاموش باشد. اگر
    انتخاب بود، پیش‌فرضِ خاموش صفحه‌ی تسعیرِ موجود را یک‌شبه خالی می‌کرد.
  - **تفصیلی پذیر** (`accepts_tafsili`) — ساختِ زیرحساب زیرِ حسابِ غیرِسرفصل حالا این
    تیک را می‌خواهد. **حسابی که از قبل فرزند دارد پذیرا شمرده می‌شود** (روتر با یک
    EXISTS می‌سنجد، نه با backfill) تا چارت‌های موجود قفل نشوند.
  - **پیگیری** (`has_tracking` + `journal_lines.tracking_no`/`tracking_date`) — ارجاعِ
    آزادِ ردیف. روی *ردیف* است نه سند، چون یک سند می‌تواند چند پیگیریِ متفاوت داشته
    باشد. جایگزینِ جدولِ `checks` نیست: چک موجودیتی با سررسید و وضعیت و گردش است.
    پیگیری روی حسابِ بی‌پرچم **رد می‌شود، نه اینکه بی‌صدا دور ریخته شود** — وگرنه سند
    ثبت می‌شد و شماره‌ی حواله برای همیشه گم بود. ابطال و ادغام هم پیگیری را منتقل
    می‌کنند، وگرنه جستجوی ارجاع نصفِ ماجرا را می‌آورد.
  - **نمایش در گزارشات مدیریتی** (`in_management_reports`) — تنها پرچمی که پیش‌فرضش
    *روشن* است. پارامترِ `management_only` روی `/api/reports/trial-balance`؛ پیش‌فرض
    خاموش، چون ترازِ کامل مبنای کنترلِ توازن است و نباید صافی بخورد.
  - **کشوی ویرایشِ حساب** (`AccountEditDrawer`) جانشینِ دو `window.prompt`ِ نام و کد شد.
    شش تیک و یک فهرستِ ماهیت در پرسشِ تک‌خطی جا نمی‌شوند، و وابستگیِ «تسعیر پذیر» به
    «ارزی» اصلاً در پرامپت نشان‌دادنی نبود. کد جدا فرستاده می‌شود چون سرور هم جدا
    می‌گیردش و می‌تواند ردش کند.
  - **گزارشِ «حساب‌های خلافِ ماهیت» بالاخره صفحه گرفت.** مهاجرتِ `0087` این نقطه‌ی
    پایانی را ساخته بود ولی هیچ صفحه‌ای مصرفش نمی‌کرد؛ حالا کارتی زیرِ «گزارش ترازها»
    است با صافیِ «فقط حساب‌های کنترل‌شده». بدونش، تیکِ `nature_control` هیچ اثرِ
    دیدنی نداشت.
  - **پیگیری در حالتِ آفلاین هم کار می‌کند:** `has_tracking` به کشِ SQLite اضافه شد،
    با یک `ensureColumn` چون `CREATE TABLE IF NOT EXISTS` روی نصبِ موجود کاری نمی‌کند.
  - **دو تستِ موجود که قاعده‌ی قدیم را ثبت کرده بودند بازنویسی شدند** (نه دور زده):
    `test_create_tafsili_under_an_unposted_moin` حالا تیک را روشن می‌کند، و دو تستِ
    تازه قاعده‌ی جدید و گراندفادرینگِ چارتِ موجود را مستند می‌کنند.
  - **راستی‌آزمایی:** ۱۵۵۲ تستِ بک‌اند سبز (۲۵ تازه) · `tsc --noEmit` صفر خطا ·
    ممیزِ صفحه‌ها صفر خطا (۱۳ هشدارِ از پیش موجود) · `oxlint` بدونِ خطای تازه ·
    رفت‌وبرگشتِ واقعی روی سرورِ محلی: گاردِ تسعیر، گاردِ پیگیری، ثبت و ابطالِ سندِ
    پیگیری‌دار، و صافیِ هر دو گزارش.

- **۱۴۰۵/۰۶/۱۳ (2026-09-04) — کدینگِ حساب: ماهیت، سطحِ تفصیلی، و جداشدنِ تنظیمات از درختواره (مهاجرتِ `0087`):**
  مقایسه‌ی مورد‌به‌موردِ کاربر با سپیدار چهار کمبود را نشان داد؛ هر چهار ساخته شد و
  دو ایرادِ ساختاری هم سرِ راه پیدا شد.
  - **ماهیتِ حساب** (`accounts.nature`): بدهکار/بستانکار/مهم نیست، به‌علاوه‌ی
    گزارشِ `/api/reports/nature-violations`. **ستون nullable است و backfill ندارد** —
    NULL یعنی «از `type` مشتق شو» (`NATURE_BY_TYPE`)، همان الگوی `account_code_widths`
    و `enabled_modules`. پس هیچ حسابِ موجودی معنایش عوض نشد و مهاجرت هیچ `UPDATE`ی
    روی جدولِ RLS‌دار نزد، یعنی در دامِ «backfill موفق گزارش داد و صفر ردیف عوض کرد»
    نیفتاد. **فقط گزارش می‌دهد، جلوی ثبت را نمی‌گیرد** — خلافِ ماهیت شدن گاهی درست
    است (اضافه‌برداشتِ بانکی، پیش‌دریافتِ مشتری)؛ تستِ
    `test_violation_does_not_block_posting` این را قفل می‌کند.
  - **عنوانِ دوم** (`accounts.name2`) برای گزارشِ دوزبانه، و **عنوانِ کامل** (مسیر از
    ریشه) در راهنمای سطر — دومی ستون نگرفت، در همان پیمایشِ درخت ساخته می‌شود.
  - **رنگ‌بندیِ سطوح** در درختواره (سبز گروه، آبی کل، زرد معین، بنفش تفصیلی) با
    نشانِ رنگی به‌جای رنگ‌کردنِ متن، تا کنتراست در پوسته‌ی تیره نشکند.
  - **سطحِ تفصیلی زیرِ معین.** درخت تا امروز فقط زیرِ `is_group` رشد می‌کرد، پس
    «بانک ملی» زیرِ «بانک» ساختنی نبود — با اینکه `account_code_widths` از اول
    چهارتایی بود و کامنتِ `BankAccount.gl_account_id` هم همین را پیش‌بینی کرده بود.
    حالا حسابِ معین هم فرزند می‌گیرد، **به شرطِ نداشتنِ سندِ مستقیم**: حسابی که هم
    ردیفِ خودش را دارد هم فرزند، مانده‌اش دو منبع پیدا می‌کند و هر گزارشی باید حدس
    بزند کدام را جمع بزند. (همان قیدی که سپیدار هم روی «تفصیل‌پذیری» دارد.)
  - **برگرداندنِ قالبِ صنفی** (`DELETE /api/accounts/templates/{key}`): درج آسان بود و
    برگشت نبود، پس کاربری که چهار قالب را امتحان می‌کرد چارتش پر از حسابِ بی‌ربطِ چهار
    صنف می‌شد و راهی جز حذفِ تک‌تک نداشت. فقط حسابِ **بی‌استفاده** را برمی‌دارد؛ سند،
    زیرحساب، نقشِ سیستمی و بودن در چارتِ پایه نگهش می‌دارند و دلیلِ هرکدام در `kept`
    برمی‌گردد — وگرنه کاربر از نتیجه‌ی نصفه فکر می‌کند عملیات شکست خورده.
  - **صفحه‌ی تازه‌ی «کدینگ» زیرِ تنظیمات.** قالب‌ها از درختواره و قاعده‌ی کدینگ از
    «روش‌های شماره‌گذاری» به این‌جا آمدند. هر دو تنظیم‌اند نه کارِ روزمره، و نشستنشان
    بالای درختواره همان اتفاقی را رقم زد که بالا آمد. قاعده‌ی کدینگ در «شماره‌گذاری»
    هم غریبه بود: آن صفحه شماره‌ی سریِ *اسناد* را تنظیم می‌کند، نه کدِ *حساب‌ها*.
  - **دو ایرادِ ساختاری که سرِ راه پیدا و رفع شد:** جمعِ مانده در درختواره
    (`balance = childSum` مانده‌ی مستقیمِ والد را پنهان می‌کرد؛ حالا `own + childSum`)،
    و تستِ `test_create_parent_must_be_group` که قاعده‌ی قدیمی را ثبت کرده بود —
    بازنویسی شد به دو تستِ تازه که قاعده‌ی جدید و دلیلش را می‌گویند.
  - **راستی‌آزمایی:** ۱۵۲۲ تستِ بک‌اند سبز (۲۴ تازه) · `tsc -b --force` صفر خطا ·
    ممیزِ صفحه‌ها صفر خطا · `oxlint` پاک · رفت‌وبرگشتِ واقعی روی سرورِ محلی: درجِ
    بازرگانی ۳۸→۶۲ و برگرداندن ۶۲→۳۸؛ حسابِ سنددار با دلیلِ صریح ماند؛ «بانک ملی» و
    «بانک صادرات» زیرِ «بانک» با کدِ `110201`/`110202` ساخته شدند و سرور سطحشان را
    «تفصیلی» شناخت؛ تلاش برای زیرحساب زیرِ صندوقِ سنددار ۴۰۹ داد.
- **۱۴۰۵/۰۶/۱۲ (2026-09-03) — ادغامِ گروهِ ناوبریِ «مشتریان و فروش» با «فروش»:**
  عملیات از استقرارِ سرور (ماژولِ فروش) جدا نبود؛ فقط نوارِ ناوبریِ دسکتاپ/وب دو
  گروهِ مجاورِ هم‌نام داشت — «مشتریان و فروش» (اشخاص، باشگاه مشتریان، صندوق
  فروشگاهی) و «فروش» (هجده عملیات/دوازده فهرست) — و کاربر باید حدس می‌زد «اشخاص»
  زیرِ کدام است. یک گروه شدند، با ترتیبِ طرفِ‌حساب اول، بعد گردشِ سند. فقط
  `NAV_GROUPS` در [navModel.tsx](desktop/src/lib/navModel.tsx) عوض شد — گیت‌بندیِ
  ماژول‌ها (`sales`/`pos`/`crm` هنوز سه کلیدِ مجزا در `OPTIONAL_MODULES`) دست‌نخورده
  ماند، چون سه‌تا در صنف‌بندی‌های مختلف (`INDUSTRY_TEMPLATES`) جدا از هم روشن/خاموش
  می‌شوند.
  **مستقر شد روی `acc.cubita.ir` (فقط باندلِ وب؛ بک‌اند از قبل روی مهاجرتِ `0086`
  بود، دست‌نخورده ماند).** `demo.cubita.ir` هنوز باندلِ قدیمی را دارد — طبقِ قاعده،
  فقط با `rsync -a --delete /opt/hesabdari/web/ /opt/cubita-demo/web/` به‌روز شود.

- **۱۴۰۵/۰۶/۱۲ (2026-09-03) — نسخه‌ی نصبیِ دسکتاپ ۱.۲.۸:** بعد از استقرارِ وب بالا،
  دسکتاپ (Electron) عقب مانده بود — نسخه‌ی نصب‌شده‌ی کاربرها هنوز ۱.۲.۷ (۲۶ مرداد)
  را داشت و از هیچ‌کدامِ تغییراتِ چندروزِ اخیر (ماژولِ فروش، شماره‌ی عطف/فرعی، ادغامِ
  ناوبری) خبر نداشت، چون این‌ها فقط در کدِ منبع و روی وب زنده بودند، نه در باندلِ
  نصب‌شده. `npm run dist` نصب‌کننده‌ی تازه ساخت، `npm run publish-update` سه فایل
  (exe، blockmap، `latest.yml`) را با آپلودِ تکه‌تکه (شبکه چند بار وسطِ انتقال قطع
  شد؛ retry خودش جبران کرد) روی `/opt/hesabdari/updates` گذاشت — کاربرهای فعلی با
  electron-updater خودکار آپدیت می‌گیرند. `Cubita-Setup.exe` (نامِ ثابتی که دکمه‌ی
  دانلودِ سایت به آن لینک می‌دهد، در [ConceptLanding.tsx](website/src/pages/ConceptLanding.tsx))
  هم دستی با نسخه‌ی تازه جایگزین شد — `publish-update.mjs` این نامِ ثابت را
  به‌روزرسانی نمی‌کند، فقط نامِ نسخه‌دار را.

- **۱۴۰۵/۰۶/۰۸ (2026-08-30) — ماژولِ «فروش»: هجده عملیات و دوازده دفتر (مهاجرتِ `0086`):**
  - **از تب‌دار به صفحه‌محور.** فروش سه تب داشت (فاکتور، پیش‌فاکتور، برگشتی) و هرچه دورِ
    فروش می‌چرخید بیرونِ سامانه انجام می‌شد. حالا مثلِ حسابداری و دریافت‌وپرداخت گروهِ
    ناوبریِ خودش را دارد. `SalesPage.tsx` و `QuotationsTab.tsx` حذف شدند و فرم‌های
    آزموده‌شان (ویزارد، صفِ آفلاین، رونوشت) بازاستفاده شدند — نه بازنویسی.
  - **هفده منوی خواسته‌شده + پیش‌فاکتور.** پیش‌فاکتور در فهرستِ کاربر نبود ولی قابلیتِ
    زنده بود؛ حذفش یعنی از دست دادنِ چیزی که کار می‌کرد، پس همان تقسیمِ عملیات/فهرست
    برایش هم اعمال شد (فرم در عملیات، دفتر با کنشِ تبدیل در فهرست).
  - **یازده جدولِ تازه** (`sales_ops.py`, مهاجرتِ `0086`, همه با RLS اجباری): نوعِ فروش،
    گروهِ کالای تخفیف + اعضا، عاملِ قیمت، بسته + ردیف‌ها، قاعده و محاسبه‌ی پورسانت +
    ردیف‌ها، اظهارنامه‌ی گمرکی، اعلامیه‌ی بدهکار/بستانکار. `sales_invoices` چهار ستون
    گرفت: `closed_at`/`closed_by_id` (بستنِ فاکتور)، `salesperson_id`، `sale_type_id`.
  - **اعلامیه‌ی قیمت جدولِ تازه نگرفت.** `price_lists` از قبل بود و به کالاها هم وصل؛
    جدولِ دوم یعنی دو حقیقتِ ممکن برای «قیمتِ این کالا چند است». فقط `effective_from`
    اضافه شد، چون اعلامیه بدونِ تاریخِ اجرا اعلامیه نیست.
  - **تخفیف و عاملِ افزاینده یک جدول‌اند** (`pricing_factors.kind`): شکلشان یکی است و
    تنها فرقشان جهتِ اثر. دو منوی عملیات روی همان جدول می‌نویسند و یک دفترِ مشترک با
    فیلتر دارند — استثنای دومِ قاعده‌ی نظیر.
  - **سه تصمیمِ تأییدشده‌ی کاربر:** قیمت‌گذاری *پیشنهاد* می‌دهد نه حکم (مسیرِ ثبتِ
    حسابداری دست‌نخورده می‌ماند) · پورسانت به کاربرِ فروشنده تعلق می‌گیرد و روی خالصِ
    فاکتور یا سودِ ناخالص حساب می‌شود · «بستن فاکتور» یعنی قفل از ویرایش، مثلِ دائم‌کردنِ سند.
  - **دو تصمیمِ محافظه‌کارانه:** محاسبه‌ی پورسانت *ذخیره* می‌شود نه زنده (وگرنه ابطالِ یک
    فاکتورِ قدیمی عددی را که پرداخت شده بی‌خبر عوض می‌کند) · اعلامیه‌ی بدهکار/بستانکار
    تنها چیزی در این ماژول است که سند حسابداری می‌زند، چون بدونِ آن صورت‌حسابِ طرف
    مقابل با دفتر نمی‌خواند.
  - **یک ایراد که آزمونِ زنده پیدا کرد:** نامِ تکراری ۵۰۰ می‌داد به‌جای ۴۰۹ (قیدِ یکتا
    کار می‌کرد، ولی خطای خامش «خطای داخلی سرور» نشان می‌داد). حالا چهار مسیرِ نام‌دار
    پیش از درج بررسی می‌کنند؛ دو تست هم برایش نوشته شد.
  - **راستی‌آزمایی:** ۱۴۹۸ تستِ بک‌اند سبز (۲۴ تازه + ۵۵ تستِ خودکارِ ایزولاسیون برای
    یازده جدول) · ممیز ۰ خطا (۱۲ قاعده) · tsc و oxlint پاک · هر ۳۰ صفحه روی ۱۴۴۰ و
    ۳۹۰ پیکسل با سرریزِ ۰ و خطای کنسولِ ۰ · رفت‌وبرگشتِ واقعی روی سرورِ محلی: پیشنهادِ
    قیمت (۵۰۰٬۰۰۰×۲ +۱۰٪ −۲۰٪ = ۸۸۰٬۰۰۰ ✓)، اعلامیه مانده‌ی دریافتنی را دقیقاً
    ۲۵۰٬۰۰۰ برد و ابطالش دقیقاً برگرداند، بستنِ ۹ فاکتور و ردِ بارِ دوم.

- **۱۴۰۵/۰۶/۰۸ (2026-08-30) — تمیزکاری: کدِ مرده، قابلیت‌های گم‌شده، آشغالِ سرور (بدونِ مهاجرت):**
  - **ممیزِ ارجاع (`scripts/find-dead-code.mjs`):** گرافِ importها را از `main.tsx` می‌پیماید؛
    هرچه نرسیدیم مرده است. `tsc` این را نمی‌گیرد (فایلِ بی‌مصرف خطا نیست) و چشم هم نگرفت —
    `JournalDaybookPanel` سه بازبینی را رد کرد. `.d.ts` مستثناست (اعلانِ محیطی).
  - **۹ فایلِ مرده حذف شد (~۱۱۰۰ خط):** `CheckForm` · `ChecksList` · `PettyCashPanel` ·
    `CheckWizard` · `PettyCashWizard` · `checkDraft` · `pettyCashDraft` (بازمانده‌ی `60c588b`،
    جایگزین: صفحه‌های «دریافت و پرداخت») و `JournalDaybookPanel` · `PeriodClosePanel`
    (بازمانده‌ی `61f156e`، جایگزین: صفحه‌های ماژولِ حسابداری).
  - **قاعده‌ی R12 در ممیز:** فایلِ بی‌ارجاع از این پس خطاست. با کاشتنِ عمدیِ یک فایل آزموده شد.
  - **دو قابلیتِ گم‌شده که ممیزِ صادرات پیدا کرد:**
    (۱) `setCalendarEventDone` بی‌مصرف بود — با جابه‌جاییِ تبِ تقویم در `33e7afb` دکمه‌ی
    «انجام شد» گم شده بود و دفتر فقط وضعیت را *نشان* می‌داد. برگردانده شد.
    (۲) `setEntrySubNumber` بی‌مصرف بود — اندپوینتِ اصلاحِ شماره فرعی ساخته شده ولی به رابط
    وصل نشده بود. حالا در دفترِ اسناد، روی سندِ موقت، قابلِ ویرایش است.
  - **۷ فراخوانِ بی‌مصرفِ `api.ts` حذف شد** (`updateFiscalYear`، `fetchItemByBarcode`،
    `submitInvoiceToMoadian`، `fetchLeaveRecords`، `fetchExpiringBatches`، `updateStockBatch`،
    `fetchAccountingOverview`) + تایپِ یتیمِ `AccountingOverview`. هیچ‌کدام قابلیتی نبودند:
    اسکنِ بارکد از نگاشتِ محلی می‌خواند و ارسالِ تکیِ مؤدیان با انتخابِ یک فاکتور انجام می‌شود.
  - **سرور:** ۲۱۳ مگابایت آزاد شد — ۶۳ آرشیوِ برگشتِ prod و ۵۰ تای دمو و ۲۵ پوشه‌ی
    `web.bak` قدیمی‌تر از ۱۴ روز. `web.old-*` و پشتیبان‌های دیتابیس دست‌نخورده.
  - **رفعِ علت:** `deploy.sh` آرشیوِ برگشت را نمی‌چرخاند (فقط پشتیبانِ دیتابیس را). حالا
    می‌چرخاند، با همان سیاستِ ۱۴روزه و *بعد* از موفقیتِ استقرار — مثلِ `backup.sh`.
  - **تلهٔ CRLF:** ویرایشِ اسکریپتِ شل با پایتونِ ویندوز `

` می‌نویسد و اسکریپت روی سرور
    می‌شکند (یک‌بار شکست و از پشتیبان برگشت). اسکریپت‌های `backend/scripts/*.sh` حالا در
    کپیِ کاری هم LF‌اند و هشِ مخزن و سرور یکی است.
  - **راستی‌آزمایی:** ۱۴۱۹ تست سبز · ممیز ۰ خطا (۱۲ قاعده) · tsc و oxlint پاک · ۰ فایلِ مرده،
    ۰ صادراتِ بی‌مصرف · دکمه‌ی تقویم و ویرایشِ شماره فرعی روی ۱۴۴۰ و ۳۹۰ آزموده شدند.

- **۱۴۰۵/۰۶/۰۸ (2026-08-30) — شماره عطف و شماره فرعیِ سند حسابداری (مهاجرتِ `0085`):**
  - **مسئله:** سند یک شماره داشت و همان یک شماره دو کارِ ناسازگار می‌کرد. «شماره‌گذاری مجدد»
    شماره‌ها را به‌ترتیبِ تاریخ جابه‌جا می‌کند (یعنی شماره متغیر است)، ولی ارجاعِ بیرونی —
    چاپِ سند، پیوستِ پرونده، نامه‌ی حسابرس — شماره‌ای می‌خواهد که هرگز تکان نخورد.
  - **شماره عطف (`journal_entries.atf_number`):** سرور لحظه‌ی ثبت و به‌ترتیبِ *ورود* می‌دهد و
    هیچ عملیاتی عوضش نمی‌کند. شمارنده‌ی جدا (`journal_atf` در `DOC_TYPES`) دارد چون ترتیبش
    با ترتیبِ شماره‌ی سند یکی نیست. قیدِ یکتای (مستأجر، عطف) در دیتابیس.
  - **شماره فرعی (`journal_entries.sub_number`):** ارجاعِ آزادِ کاربر — شماره‌ی سند در سیستمِ
    قبلی، شماره‌ی پرونده، کدِ دسته. `String(30)`، اختیاری، **غیریکتا** (چند سندِ یک دسته عمداً
    یک شماره می‌گیرند)، و روی سندِ دائم قفل (`PATCH …/sub-number` → ۴۰۹).
  - **تخصیص با رویداد نه با فراخوانی:** `_assign_atf_number` (رویدادِ `before_flush` در
    `services/numbering.py`) عطف را به هر سندِ تازه می‌دهد، از هر هشت مسیرِ ساختِ سند و هر
    مسیرِ آینده — همان الگویی که `tenant_id` با آن مهر می‌خورد. یک فراخوانیِ فراموش‌شده یعنی
    سندی با عطفِ خالی که دیده نمی‌شود. ترتیب با `number` گرفته می‌شود چون `session.new` مجموعه
    است و ترتیبش تصادفی.
  - **بک‌فیل (مهاجرتِ `0085`، زیرِ `rls_disabled`):** اسنادِ موجود به‌ترتیبِ `created_at` عطف
    گرفتند و شمارنده‌ی هر مستأجر روی بزرگ‌ترین عطفش نشست. راستی‌آزماییِ عددی روی دیتابیسِ
    توسعه: ۷۹ از ۷۹ سند، هر مستأجر ۱..n بدونِ شکاف و تکرار، ۰ مستأجرِ بی‌شمارنده.
  - **رابط:** «شماره فرعی» در فرمِ کلاسیک و ویزارد (+ پیش‌نمایشِ زنده)؛ ستون‌های «عطف» و
    «فرعی» در دفترِ اسناد و کارتابل؛ ستونِ «عطف (ثابت)» در پیش‌نمایشِ شماره‌گذاری مجدد تا
    کاربر پیش از زدنِ دکمه ببیند چه چیزی *تغییر نمی‌کند*؛ جستجو با عطف/فرعی/شماره/شرح.
  - **راستی‌آزمایی:** ۱۴۱۹ تستِ بک‌اند سبز (۱۴ تستِ تازه در `test_journal_atf.py`) · ممیز ۰ خطا ·
    tsc و oxlint پاک · صفحه‌ی سند و دفتر و شماره‌گذاری مجدد روی ۱۴۴۰ و ۳۹۰ با سرریزِ ۰ و
    خطای کنسولِ ۰ · رفت‌وبرگشتِ واقعی روی سرورِ محلی (ثبت، اصلاح، جستجو، دائم‌کردن، ۴۰۹).

- **۱۴۰۵/۰۶/۰۸ (2026-08-30) — قاعده‌ی نظیر: هر عملیاتِ رکوردساز، یک فهرست (بدونِ مهاجرت):**
  - **قاعده (اسکیلِ `cubita-page`، بخشِ ۱):** هر منوی عملیاتی که رکوردِ تازه ثبت می‌کند باید در
    کارتِ «فهرست»ِ همان ماژول دفترِ خودش را داشته باشد. الگوی مرجعِ خودِ کاربر: «فروش اقساطی»
    (عملیات: فرم + جدولِ کاری) ↔ «قراردادهای اقساطی» (فهرست: دفترِ خواندنی با جمع‌ها).
  - **سه استثنای تأییدشده‌ی کاربر:** (۱) عملیاتی که فقط وضعیت عوض می‌کند فهرست نمی‌خواهد
    (دفترش از قبل هست)؛ (۲) چند عملیات روی یک جدول، یک فهرستِ مشترک با فیلتر می‌گیرند؛
    (۳) عملیاتی که خودش فهرست/گزارش است سرِ جایش در کارتِ عملیات می‌ماند.
  - **ثبتِ ماشین‌خوان + گاردِ خودکار:** نگاشتِ `OPS_LIST_MAP` در
    [moduleLists.tsx](desktop/src/components/moduleLists.tsx) برای **هر** منوی عملیات یک ردیف
    دارد — یا کلیدِ فهرستش یا دلیلِ استثنا (`state`/`view`/`none`). قاعده‌ی **R11** در ممیز
    منویی را که ردیف ندارد خطا می‌دهد، پس منوی تازه بدونِ تصمیم درباره‌ی فهرستش از دست نمی‌رود.
    (با برداشتنِ عمدیِ یک ردیف آزموده شد: قاعده گرفت.)
  - **۱۲ دفترِ تازه:** دریافت و پرداخت ۶ (دسته‌چک‌ها، حساب‌های بانکی، دستگاه‌های کارتخوان،
    تسویه‌های کارتخوان، ردیف‌های صورت‌حساب بانکی، گردش تنخواه) · حسابداری ۱ (تفصیلی‌های سایر) ·
    شرکت ۳ (محل‌های جغرافیایی، گروه‌های طرف حساب، رویدادهای تقویم) · تنظیمات ۱ (روش‌های
    شماره‌گذاری). فهرستِ ماژول‌ها از ۲۳ به ۳۵ رسید.
  - **یک ادغام:** تبِ «فهرست رویدادها» از صفحه‌ی تقویم برداشته شد و به کارتِ فهرست رفت — همان
    حرکتی که پیش‌تر برای «تاریخچه ارسال‌ها» انجام شد.
  - **دامنه:** قاعده برای گروه‌هایی است که منوهایشان صفحه‌اند. در ماژول‌های تب‌دار (فروش،
    انبار، خرید، …) هر تب دفترِ خودش را داخلِ خودش دارد و قاعده همان‌جا برآورده است.
  - **۱۴۰۵ تستِ بک‌اند سبز** · ممیز ۰ خطا (۱۱ قاعده) · هر ۱۲ دفتر روی ۱۴۴۰ و ۳۹۰ پیکسل با
    سرریزِ ۰، خطای کنسولِ ۰، جدولِ بی‌کارتِ ۰ و سلولِ بی‌برچسبِ ۰.

- **۱۴۰۵/۰۶/۰۷ (2026-08-28) — ماژولِ «دریافت و پرداخت»: ۱۸ عملیات (مهاجرتِ `0084`):**
  - **ساختار:** گروهِ تک‌صفحه‌ایِ «چک و بانک» به **۱۸ عملیات** رسید، به‌ترتیبِ مسیرِ کار: فرآیند ·
    رسید دریافت · اعلامیه پرداخت · عملیات بانکی چک دریافتنی · تسویه حساب طرف مقابل · استرداد چک ·
    وصول چک پرداختنی · جستجوی چک · تسویه کارت خوان · صورت حساب بانکی · مغایرت بانکی · صندوق ·
    حساب بانکی · دستگاه کارت خوان · دسته چک · تنخواه دار · صورت هزینه تنخواه · مرور عملیات بانکی.
    کارتِ «فهرست» یک منو دارد: «دریافت‌ها و پرداخت‌ها».
  - **ادغامِ تکرارها:** صفحه‌ی «چک و بانک» (پنج تب) برچیده شد و پنل‌هایش هرکدام صفحه‌ی مستقلِ خودش
    را گرفت؛ تبِ «دریافت و پرداخت» هم از ماژولِ **اشخاص** برداشته شد — داده‌ی این ماژول در ماژولِ
    دیگری زندگی می‌کرد. رسید و اعلامیه عمداً دو صفحه شدند نه یک فرم با سوییچ: همان یک `select`
    تعیین می‌کرد سند به کدام سمت بخورد و اشتباهش یعنی سندِ وارونه.
  - **سه شکافِ ساختاری (`0084`):** جدولِ `checkbooks` + `checks.checkbook_id` (دسته‌چک: بازه‌ی برگ،
    شمارِ باقی‌مانده، پیشنهادِ شماره‌ی بعدی از بزرگ‌ترین شماره‌ی مصرف‌شده)، وضعیتِ `returned`
    (استرداد — معکوسِ لحظه‌ی دریافت، جدا از `bounced`)، و `treasury_transactions.settled_at` +
    `settlement_txn_id` (تسویه‌ی کارتخوان). نقشِ `bank_fee` روی همان کدِ ۵۱۱۱ِ قالب‌های صنفی نشست
    و مهاجرت حسابِ موجود را نقش‌دار کرد — درسِ تصادمِ ۴۱۰۴/۵۱۰۷ تکرار نشد.
  - **تسویه‌ی کارتخوان فقط کارمزد را ثبت می‌کند،** نه مبلغِ ناخالص را: رسیدِ کارتی در لحظه‌ی ثبت
    بانک را بدهکار کرده و ثبتِ دوباره‌ی ناخالص یعنی درآمدِ دوبار. کارمزدِ صفر هیچ سندی نمی‌سازد.
  - **صدورِ چک جای خودش را گرفت:** ثبتِ چکِ دریافتی در «عملیات بانکی چک دریافتنی» و صدورِ چکِ
    پرداختنی در «دسته چک» — جایی که شماره‌ی برگِ بعدی معلوم است.
  - **۱۴۰۵ تستِ بک‌اند سبز** (۱۵ تستِ تازه). ممیزِ صفحه‌ها ۰ خطا؛ هر ۱۸ صفحه روی ۱۴۴۰ و ۳۹۰
    پیکسل با سرریزِ ۰، خطای کنسولِ ۰، جدولِ بی‌کارتِ ۰ و سلولِ بی‌برچسبِ ۰.

- **۱۴۰۵/۰۶/۰۷ (2026-08-28) — قراردادِ صفحه‌ها: یک اسکیل، دو ابزارِ سنجش، و یکدست‌سازیِ کلِ پنل:**
  - **اسکیل:** [`.claude/skills/cubita-page/SKILL.md`](.claude/skills/cubita-page/SKILL.md) — از روی
    صفحه‌ی مرجعِ «مرکز هزینه» نوشته شد و ده بخش دارد: سه نوعِ صفحه (ماژول/عملیات/فهرست) و
    مرزشان، پوسته و دو تله‌اش (اختصاصیتِ `.page.panels > *`، پنهان‌شدنِ سرصفحه/KPI در پوسته‌ی
    راهنما)، **رفتار در پنج نقطه‌ی شکست** (۱۰۲۴/۹۸۰/۷۶۰/۶۴۰)، قراردادِ جدولِ کارت‌شونده،
    قواعدِ فارسی/RTL، چهار حالتِ صفحه، قواعدِ داده (فیلترِ سمتِ سرور، سقفِ ۲۰۰ ردیف، قفلِ پلن)،
    چک‌لیستِ هشت‌مرحله‌ایِ سیم‌کشیِ ناوبری، و روالِ راستی‌آزمایی.
  - **دو ابزار:** `desktop/scripts/audit-pages.mjs` (ممیزِ ایستا، ده قاعده، خروجِ ناصفر روی خطا)
    و `desktop/scripts/verify-pages.mjs` (Playwright: سرریزِ افقی، خطای کنسول، جدولِ بی‌کارت،
    سلولِ بی‌برچسب — روی ۱۴۴۰ و ۳۹۰).
  - **یکدست‌سازی:** ممیز با **۲۹۵ خطا** شروع شد و به **صفر** رسید. ۷۵ جدول کلاسِ کارتیِ موبایل
    گرفت، ۱۴۳ سلول برچسبِ ستون، ۸۵ جدول ظرفِ لغزش، ۳۳ سلولِ دکمه کلاسِ `card-actions`؛ سه
    جای تاریخ به `formatJalali` برگشت؛ جدولِ چاپی/ماتریسی با `table-plain` علامتِ صریح گرفت.
  - **باگی که همین ممیز پیدا کرد:** `limit=300` در فهرستِ اسناد از سقفِ ۲۰۰ِ سرور بیشتر بود،
    ۴۲۲ می‌گرفت و صفحه بی‌صدا «سندی نیست» نشان می‌داد — یعنی فهرستِ اسناد از روزِ ساختش خالی بود.
  - **راستی‌آزمایی:** ۱۵ صفحه‌ی نماینده روی هر دو عرض — سرریزِ ۰، خطای کنسولِ ۰، جدولِ بی‌کارتِ ۰،
    سلولِ بی‌برچسبِ ۰.

- **۱۴۰۵/۰۶/۰۶ (2026-08-27) — ماژولِ «سامانه مؤدیان»: بازچینی، چک‌لیستِ آمادگی و ارسالِ گروهی (بدونِ مهاجرت):**
  - **چرا:** صفحه به‌هم‌ریخته بود. فرمِ اعتبارنامه (هفت فیلد، دوتاشان PEMِ چندخطی) و کادرِ ارسال (یک
    انتخابگر) در `workspace-split` کنارِ هم می‌نشستند: ستونِ راست چنان باریک بود که textareaهای کلید و
    گواهی ~۱۷۰px می‌شدند و ستونِ چپ تقریباً خالی می‌ماند. متن‌های لاتین (PEM، آدرس، شناسه) هم بدونِ
    `dir="ltr"` در جریانِ RTL وارونه خوانده می‌شدند.
  - **ساختار:** کارتِ «عملیات» سه بخش به‌ترتیبِ کارِ واقعی — «وضعیت و آمادگی»، «ارسال صورتحساب»،
    «تنظیمات و اعتبارنامه» — و «تاریخچه ارسال‌ها» به‌عنوانِ تنها منوی کارتِ **«فهرست»** (صفحه‌ی
    `moadianhistory`)، چون دفترِ ارسال‌ها داده‌ی ذخیره‌شده است نه عملیات. فهرستِ خودکارِ قبلی
    (`MODULE_LISTS.moadian`) حذف شد. تنظیمات یک‌بار انجام می‌شود و ارسال هر روز؛ این دو نباید
    هم‌زمان روی صفحه جا بگیرند. فرم تمام‌عرض شد و PEMها با فونتِ یک‌عرضِ LTR.
  - **دو رفعِ ناوبری که همین بازچینی رو کرد:** (۱) `ModulePanels` برای صفحه‌ی *فهرستِ* یک گروهِ
    تک‌صفحه‌ای اصلاً کارتِ «عملیات» نمی‌ساخت و کاربر بدونِ راهِ برگشت می‌ماند. (۲) کارتِ «فهرست» زیرِ
    ۱۰۲۴px پنهان است و کشوی موبایل صفحه‌های فهرست را ندارد، پس تاریخچه روی موبایل از هیچ‌جا در
    دسترس نبود — میان‌برِ «تاریخچه ارسال‌ها» در سرصفحه‌ی «آمادگیِ ارسال» اضافه شد.
  - **سه اندپوینتِ تازه:** `GET /api/moadian/readiness` (چک‌لیست)، `GET /api/moadian/pending` (صفِ ارسال
    با دلیلِ مسدودی هر ردیف)، و `POST /api/moadian/submit-batch` (ارسالِ گروهی، سقفِ ۵۰). چک‌لیست عمداً
    **همان توابعِ مسیرِ ارسال** را صدا می‌زند (`_load_private_key`، `_cert_der_from_pem`، `_assert_stuff_ids`)
    نه یک کپیِ قاعده — وگرنه رابط «آماده» می‌گفت و ارسال بعد از مصرفِ سریال شکست می‌خورد. PEMِ خراب هم
    همین‌جا معلوم می‌شود، نه وسطِ ارسال.
  - **ارسالِ گروهی:** به‌جای «یک `select` و یک دکمه»، جدولِ تیک‌دار با مبالغ و خریدار و وضعیتِ هر ردیف.
    شکستِ یک فاکتور بقیه را متوقف نمی‌کند (`submit_invoice` برای هر کدام جدا commit می‌کند) و نتیجه‌ی
    تک‌تک برمی‌گردد. یک کلاینتِ HTTP بینِ همه‌ی ارسال‌ها به‌اشتراک گذاشته می‌شود.
  - **تاریخچه:** حالا می‌گوید **کدام فاکتور** رفته — `invoice_number` و `buyer_name` با join به
    `sales_invoices`/`contacts` اضافه شدند (پیش‌تر فقط شناسه‌ی مالیاتی و سریال بود). به‌علاوه فیلترِ چیپی،
    جست‌وجو و خروجیِ CSV.
  - **رفعِ باگِ قفلِ پلن:** صفحه‌ی مستقلِ مؤدیان `locked_features` را نمی‌سنجید، پس حسابِ آزمایشی به‌جای
    باکسِ «خرید پلن» چهار خطای ۴۰۲ می‌دید. حالا مثلِ تبِ قدیمیِ فروش `FeatureUpsell` می‌گیرد.
  - **حذفِ تکرار:** تبِ «سامانه مؤدیان» از ماژولِ «فروش» برداشته شد (همان ماژول در نوارِ بالا مستقل است)
    و با آن `MoadianWizard` و `MoadianPanel` هم رفتند؛ راهنما ورودیِ اختصاصیِ خودش را گرفت.
  - **۱۳۸۵ تستِ بک‌اند سبز** (۸ تستِ تازه‌ی مؤدیان). هر چهار بخش روی ۱۴۴۰ و ۳۹۰ پیکسل بدونِ سرریز.

- **۱۴۰۵/۰۶/۰۶ (2026-08-27) — ماژولِ «حسابداری»: ۱۸ عملیات و ۶ فهرست (مهاجرت‌های `0082`–`0083`):**
  - **سه شکافِ ساختاری (`0082`):** ستونِ `status` روی سند (`temporary`/`permanent`) + `finalized_at/by`،
    جدولِ `analytic_accounts` (بُعدِ تحلیلیِ آزاد: خودرو، قرارداد، پرونده)، و چهار ستونِ ارزیِ اختیاری
    روی ردیفِ سند (`currency_code`, `fx_amount`, `fx_rate`, `analytic_id`). ردیف‌های موجود `permanent`
    بک‌فیل شدند و پیش‌فرضِ سندِ تازه `temporary` است؛ `close_period` اسنادِ موقتِ بازه را خودکار دائم می‌کند.
  - **۱۸ عملیات + ۴ صفحه‌ی فهرست** روی سرویسِ تازه‌ی `accounting_ops.py` با سه قاعده‌ی نوشته‌شده: دفتر
    هرگز حذف نمی‌شود (جز سندِ موقتِ دستی هنگامِ ادغام)، عملیاتِ مرتب‌سازی مانده را تکان نمی‌دهد، و دوره‌ی
    بسته دست‌نخوردنی است. شماره‌گذاریِ مجدد دوفازی است (شماره‌ی منفیِ موقت) و فقط اسنادِ موقت را می‌بیند.
  - **ادغامِ صفحه‌های تکراری (خواسته‌ی صریحِ کاربر):** تراز آزمایشی/دفترِ کل/ارزش‌افزوده از هابِ «گزارش‌ها»
    درآمد، «بستن دوره» در «بستن حساب‌های سود و زیان» و «ارزها» در صفحه‌ی تسعیر ادغام شد، و بودجه/تکرارشونده/
    ارز/دوره‌های بسته به منوی «فهرست» رفتند.
  - **باگِ خودساخته که همان‌جا رفع شد (`0083`):** کدهای ۴۱۰۴/۵۱۰۷ برای سود/زیانِ تسعیر روی حساب‌های
    **قالب‌های صنفی** («تخفیفات و برگشت از فروش»، «هزینه آب/برق/گاز/تلفن») نشسته بودند؛ چون `apply_template`
    کدِ موجود را رد می‌کند، هر کسب‌وکارِ تازه‌ای که قالب می‌زد آن دو حساب را بی‌صدا نمی‌گرفت. تسعیر به
    ۴۱۰۶/۵۱۱۶ منتقل شد (مهاجرتِ نقش‌محور، نه کدمحور) + تستِ محافظ برای هر تصادمِ آینده.
  - **دو رفعِ رابط:** پس‌زمینه‌ی کارتِ صفحه‌ی «سال مالی» (و مؤدیان و پیمانکاری) که `page panels` نداشتند،
    و نشانِ گامِ «کدینگ حساب‌ها» در «عملیات اول دوره» که برچسبِ ثابت بود و هرگز سبز نمی‌شد — حالا از
    `GET /api/accounts/setup-status` می‌آید (معیار: چارت *گسترش یافته*، نه صرفِ داشتنِ حساب).
  - **بازنشستگیِ ماژولِ «فرآیند راه‌اندازی»:** هر سه ابزارش به ماژولِ خودش رفت — «ورود گروهی کالا» تبِ
    انبار، «ورود گروهی اشخاص» تبِ اشخاص، و «مانده اول دوره» عملیاتِ حسابداری. کلیدِ `onboarding` از
    رجیستریِ ماژول‌ها حذف شد (کلیدِ باقی‌مانده در داده‌ی مستأجرهای قدیمی بی‌اثر است — تستِ اختصاصی دارد).
    اندپوینت‌ها دست‌نخورده‌اند: از اول هم با مجوزِ انبار/فروش/حسابداری گیت می‌شدند، نه با ماژولِ راه‌اندازی.
  - **۱۳۷۷ تستِ بک‌اند سبز.** مستقر روی prod؛ دمو با rsync هم‌گام.

- **۱۴۰۵/۰۶/۰۵ (2026-08-26) — ماژولِ «شرکت»: ۹ عملیات + ۱۲ فهرست (مهاجرت‌های `0078`–`0079`):**
  - **ساختار:** گروهِ «شرکت» از دو آیتم به **۹ عملیات** رسید (فرآیند راه‌اندازی، طرف حساب جدید، تقسیط،
    مرکز هزینه، محل‌های جغرافیایی، گروه جدید، عملیات اول دوره، عملیات پایان سال، یادآوری پایان سال، تقویم)
    و کارتِ «فهرست» **۱۲ منو** گرفت (ارسال/دریافت اطلاعات، گزارش‌ساز، گزارش‌های پویا، فعالیت‌های روز،
    گزارش‌های مدیریتی، گزارش استفاده از نرم‌افزار، طرف حساب‌ها، افراد مرتبط، تقسیط، همه اقساط، مراکز هزینه).
  - **سه موجودیتِ تازه (`0078`):** گروهِ طرف‌حساب، محلِ جغرافیاییِ **درختی** (کشور←استان←شهر←منطقه) و
    فردِ مرتبط. سه قاعده در سرویس: حذف داده را یتیم نمی‌کند (۴۰۹ + پیشنهادِ «غیرفعال کن»)، درخت حلقه
    برنمی‌دارد، و «نفرِ اصلی» در هر طرف‌حساب یکی است.
  - **گزارش‌ساز (`0079`):** منابعِ داده از رجیستریِ **`MODULE_LISTS`** می‌آیند و ستون‌ها از خودِ داده کشف
    می‌شوند — پس هر فهرست/فیلدِ تازه‌ای در برنامه خودکار این‌جا هم پیدا می‌شود و فهرستِ موازیِ دستی لازم
    نیست. فقط *تعریف* ذخیره می‌شود نه نتیجه: «گزارش‌های پویا» هر بار زنده اجرا می‌کند.
  - **عملیات اول دوره / پایان سال** عمداً فرمِ تازه نیستند بلکه **راهنمای گام‌به‌گام**اند: وضعیتِ واقعی هر
    گام از سرور خوانده می‌شود (سالِ مالی، سندِ افتتاحیه/اختتامیه) و دکمه کاربر را به همان صفحه‌ی موجود
    می‌برد. «یادآوری پایان سال» رویدادِ تقویم می‌سازد، نه یک سازوکارِ یادآوریِ دوم.
  - **`/api/audit/summary`:** خلاصه‌ی استفاده سمتِ *سرور* جمع می‌شود؛ شمردنِ دفترِ صفحه‌بندی‌شده در مرورگر
    یعنی شمردنِ یک صفحه، و عددِ نادرست بدتر از عددِ نداشته است.
  - **تست:** [backend/tests/test_company_entities.py](backend/tests/test_company_entities.py) — ۱۵ تست
    (یتیم‌نشدن، حلقه‌ی درخت، نفرِ اصلی، تعریف‌نه‌نتیجه). کلِ بک‌اند **۱۲۹۷ سبز**؛ `tsc -b` و بیلد سبز؛
    ۳۹۰px بی‌سرریز.
  - **نکته‌ی ابزار:** `tsc --noEmit -p tsconfig.json` در این مخزن **هیچ‌چیز را چک نمی‌کند** (ریشه
    `files: []` است و فقط reference دارد). بررسیِ درست `tsc -b` است — همان که `npm run build` می‌زند.

- **۱۴۰۵/۰۶/۰۵ (2026-08-26) — استقرارِ کاملِ انباشته روی prod (۱۴ کامیت، مهاجرت‌های `0074`–`0077`) + دسکتاپ ۱.۲.۷:**
  - **بک‌اند/وب (`acc.cubita.ir`):** با `deploy.sh production` (تارِ `app`+`alembic` و باندلِ وب در `/tmp`).
    prod روی `0073` مانده بود؛ هر چهار مهاجرت پشتِ‌هم اجرا شد → head = `0077`. پشتیبانِ دستیِ پیش از استقرار
    (`/tmp/hesabdari_pre0077.dump`) افزون بر پشتیبانِ خودکارِ خودِ اسکریپت.
  - **راستی‌آزمایی:** `/api/health` = ۲۰۰؛ مسیرهای تازه (`/api/fiscal-years`، `/api/accounts/coding-rule`،
    `/api/modules`، `/api/storefront`، `/api/integration/settings`) همه **۴۰۱** — یعنی وجود دارند و پشتِ
    احرازند، نه ۴۰۴. هشِ باندلِ زنده با بیلدِ محلی یکی است.
  - **اثرِ `0077` روی داده‌ی واقعی:** پیش از اجرا بررسی شد — `storefront_settings` خالی، هر سه ردیفِ
    `storefronts` پیش‌نویس، بی‌کالای لیست‌شده و بی‌سفارش. پس هیچ‌کدام از ۸ مستأجر گرنت نگرفتند و
    «اتصال فروشگاه» برای همه خاموش شد؛ دقیقاً پیش‌فرضِ خواسته‌شده. `granted_modules`ِ موجود (تولید برای
    ۷ مستأجر) دست‌نخورده ماند.
  - **دمو:** `/opt/cubita-demo/web` با rsync از باندلِ prod هم‌گام شد (پشتیبان: `web.bak-20260826_145153`).
    طبقِ قاعده **هرگز** `deploy.sh demo` اجرا نشد.
  - **دسکتاپ ۱.۲.۷:** `npm run dist` + `publish-update` (latest.yml آخر)، و `Cubita-Setup.exe`ِ پایدار هم به
    همین نسخه کپی شد (sha256 یکسان). **گیرِ ساخت:** electron-builder با `EPERM` روی
    `rename win-unpacked.tmp` می‌شکست — علت، اپِ dev در حالِ اجرا بود که پوشه را قفل نگه می‌داشت؛
    با بستنِ `npm run dev` ساخت رد شد.
  - `ipnetcity.ir` دست‌نخورده (۲۰۰).

- **۱۴۰۵/۰۶/۰۵ (2026-08-27) — به‌روزرسانیِ زنده‌ی منو پس از گرنت + بازطراحیِ «شخصی‌سازیِ پنل»:**
  - **کلیدِ سوپرادمین حالا فوری در منو دیده می‌شود:** گیت درست کار می‌کرد ولی ناوبریِ همان جلسه تا
    بارگذاریِ دوباره‌ی برنامه دست‌نخورده می‌ماند، پس به‌نظر می‌رسید ماژول مانده و فقط دسترسی بسته شده.
    [AccountsAdminPage](desktop/src/pages/AccountsAdminPage.tsx) پس از هر تغییرِ موفق، اگر اکانتِ
    دست‌کاری‌شده همانی باشد که کاربر داخلش است، `/me` را تازه می‌کند. آزموده با Playwright در هر دو جهت.
  - **گرنت حالا یک کلید است، نه دو:** روی حسابی که پنلش را شخصی‌سازی کرده بود، گرنتِ سوپرادمین فقط
    `allowed` را باز می‌کرد و ماژول همچنان در منو نبود، چون کلید در `enabled_modules` نبود (قالبِ صنفش
    هرگز نداشته). این دقیقاً برای «تولید» رخ می‌داد. حالا `set_grants` ماژولِ *تازه‌گرنت‌شده* را در نمایش
    هم روشن می‌کند؛ لغوِ گرنت ترجیح را دست نمی‌زند، پس گرنتِ بعدی همان‌جا برمی‌گردد
    ([modules.py](backend/app/services/modules.py))، با ۳ تستِ تازه.
  - **«شخصی‌سازیِ پنل» بازطراحی شد — چگال به‌جای درشت:** چهارده `SectionCard`ِ جدا (که بیشترشان یک ردیف
    بیشتر نداشتند) جای خود را به **یک** کارت با سرگروه‌های کم‌رنگ داد؛ گروه‌ها با `column-width` در چند
    ستونِ خودتنظیم می‌نشینند، پس کلِ ماژول‌ها در یک نگاه جا می‌شوند (پیش‌تر ~۳ صفحه اسکرول).
  - **حالتِ روشن از ردیف به کلید منتقل شد:** پیش‌تر ردیفِ روشن کلاً رنگِ لهجه می‌گرفت و چون پیش‌فرض
    تقریباً همه روشن‌اند، صفحه یکدست رنگی می‌شد و هیچ‌چیز برجسته نبود. حالا کلیدِ کوچکِ استاندارد
    (۲۸×۱۶) حالت را می‌گوید و متنِ خاموش‌ها کم‌رنگ می‌ماند — یعنی چیزی که به چشم می‌آید *خاموش‌ها*ست.
    رنگِ دستگیره از `--accent-text` می‌آید نه سفیدِ ثابت، تا در پوسته‌ی طلاییِ برند هم خوانا بماند.
  - **نوارِ ذخیره فقط وقتی چیزی عوض شده** ظاهر می‌شود، با شمارِ تغییرها و دکمه‌ی انصراف؛ سرکارت هم
    «n از m ماژول روشن» و کلیدهای «همه»/«هیچ‌کدام» را دارد. موبایل ≤۷۶۰px: تک‌ستون، ردیفِ ۴۲px.

- **۱۴۰۵/۰۶/۰۴ (2026-08-26) — «اتصال فروشگاه» از تنظیمات به منوی اصلی + پیش‌فرض خاموش (مهاجرت `0077`):**
  - **ناوبری:** از فهرستِ گروهِ «تنظیمات» درآمد و گروهِ سطحِ‌بالای خودش شد، بلافاصله پس از «مشتریان و فروش»
    ([desktop/src/lib/navModel.tsx](desktop/src/lib/navModel.tsx)) — کانالِ فروشِ آنلاین کنارِ کانالِ حضوری،
    چون راه‌اندازیِ فروشگاه یک ماژولِ کاری است (کاتالوگ، سفارش، درگاه) نه یک گزینه‌ی پیکربندی.
  - **پیش‌فرض خاموش:** `integration` به `RESTRICTED_MODULES` اضافه شد و از هر چهار قالبِ صنفی حذف شد
    ([backend/app/services/modules.py](backend/app/services/modules.py)) — یعنی هیچ حسابِ تازه‌ای آن را نمی‌بیند
    و فعال‌سازی فقط با گرنتِ سوپرادمین است (مثلِ «تولید»).
  - **گیتِ سرور، نه فقط پنهان‌کردنِ منو:** `require_module("integration")` روی کلِ روترهای `/api/integration`
    و `/api/storefront` نشست. ترتیبِ گیت‌ها عمدی است — اول `require_feature("storefront")` (۴۰۲: «پلن بخر»،
    کاری که کاربر خودش می‌تواند بکند) و بعد گیتِ ماژول (۴۰۳: «با پشتیبانی تماس بگیر»). سطحِ عمومیِ
    `/api/shop/*` عمداً بیرونِ گیت ماند: آن‌جا کاربرِ احرازشده‌ای نیست و بستنش یعنی خاموش‌شدنِ سایتِ مشتری.
  - **کلیدِ سوپرادمین:** دکمه‌ی تک‌منظوره‌ی «تولید» در «مدیریت اکانت‌ها» به فهرستِ داده‌محورِ
    `RESTRICTED_MODULES` تبدیل شد؛ حالا «تولید» و «فروشگاه» هر دو کلیدِ روشن/خاموش دارند
    ([desktop/src/pages/AccountsAdminPage.tsx](desktop/src/pages/AccountsAdminPage.tsx)).
  - **دو تله‌ی «گرنت دادم ولی چیزی ندید» بسته شد:** `set_enabled` دیگر ماژولِ محدودی را که *از قبل* روشن بوده
    با هر ذخیره‌ی مالک پاک نمی‌کند، و `set_industry` گرنتِ موردیِ سوپرادمین را با بازنشانی به قالبِ صنف
    پس نمی‌گیرد. گیتِ واقعی در هر دو حالت همچنان `allowed` است.
  - **نوارِ بالا دیگر سرریز نمی‌کند (کشفِ ضمنیِ همین کار):** با ۱۲ گروه هم در ۱۲۸۰px نوار از لبه بیرون
    می‌زد و ماژول‌ها نادیدنی می‌شدند؛ افزودنِ گروهِ تازه بدترش می‌کرد. بریک‌پوینتِ ثابت جواب نمی‌دهد چون
    تعدادِ گروه‌ها از حسابی به حسابِ دیگر فرق دارد. حالا [TopNav](desktop/src/components/TopNav.tsx) عرضِ
    *طبیعیِ* نوار را می‌سنجد (`ResizeObserver`) و دو مرحله کوچک می‌شود: اول جست‌وجوی ماژول کنار می‌رود
    (`topnav--tight`، ~۱۳۰px)، و تنها اگر باز هم جا نشد کلِ منو به کشوی همبرگری می‌رود (`topnav--collapsed`).
    عناصرِ پنهان `display:none` نمی‌گیرند بلکه از جریان بیرون می‌روند، پس اندازه‌پذیر می‌مانند و نوار با
    بازشدنِ پنجره برمی‌گردد؛ هر دو مرحله در یک سنجش تصمیم می‌شوند تا نوسان نکند. سنجیده با Playwright در
    ۳۹۰–۱۹۲۰px: هیچ سرریزِ افقی نماند.
  - **تست:** [backend/tests/test_storefront_module_grant.py](backend/tests/test_storefront_module_grant.py)
    (۱۱ تست: ۴۰۳ بدونِ گرنت، ۲۰۰ با گرنت، بازماندنِ `/api/shop/*`، خاموشیِ پیش‌فرض، و دو تله‌ی بالا) +
    fixtureِ مشترکِ `grant_module` در conftest. کلِ سوییت: ۱۲۵۹ سبز.

- **۱۴۰۵/۰۶/۰۱ (2026-08-23) — ثبتِ داده در اپ موبایل + آماده‌سازیِ انتشار:**
  - **ثبتِ داده در حرکت (۱.۴.۰ و ۱.۵.۰):** خزانه (`POST /api/treasury/{receipts|payments}`) و فاکتورِ فروش
    (`POST /api/sales-invoices`) از داخلِ اپ. **بدونِ هیچ تغییرِ بک‌اندی** — همان قراردادِ دسکتاپ/وب، پس سند و
    بهای تمام‌شده سمتِ سرور محاسبه می‌شود. صفحه‌های `TreasuryScreen`/`NewInvoiceScreen` + پیکرهای طرف‌حساب/کالا؛
    کنش‌های سریعِ خانه با گیتِ مجوز (`checks_bank`/`invoices`).
  - **کلیدِ امضای اختصاصی (۱.۶.۰):** اپ تا نسخه‌ی ۱.۵.۰ با `debug.keystore`ِ اکسپو امضا می‌شد (نامناسب برای
    فروشگاه). حالا `android/app/cubita-release.keystore` + `android/keystore.properties` (هر دو **خارج از گیت**،
    نیازمندِ پشتیبانِ امن — با گم شدنشان آپدیتِ همین اپ ناممکن می‌شود). گریدل اگر فایل نباشد به debug برمی‌گردد.
    چون امضا عوض شد، مانیفستِ آپدیت فیلدِ `reinstall` گرفت و اپ به کاربر توضیح می‌دهد که یک‌بار حذف/نصب لازم است.
  - **اسکریپتِ ریلیز:** `--aab` (بسته‌ی فروشگاه) و `--reinstall` (هشدارِ تغییرِ امضا) اضافه شد.
  - **سایت:** کارتِ «اپِ اندروید» در بخشِ نسخه‌ها (`ConceptLanding`) با دانلودِ مستقیم از لینکِ پایدارِ
    `/updates/android/cubita-latest.apk`؛ صفحه‌ی حریمِ خصوصی بندِ اختصاصیِ اپ موبایل (مجوزها، FCM، بیومتریک) گرفت
    — الزامِ بازبینیِ کافه‌بازار. متن‌های ثبت در فروشگاه: [mobile/STORE_LISTING.md](mobile/STORE_LISTING.md).

- **۱۴۰۵/۰۵/۳۱ (2026-08-22) — Push فعال شد + مرکزِ هشدارها روی موبایل (کامیت‌های `d7ab664`، `69110b0`):**
  - **FCM فعال:** پروژه‌ی Firebase `acc-cubita` وصل شد (google-services + پلاگینِ گریدل؛ سرویس‌اکانت روی prod،
    `FCM_PROJECT_ID`/`FCM_CREDENTIALS_FILE` در `.env`؛ `google-auth`+`requests` در requirements). تستِ مینتِ OAuth موفق.
    نسخه‌ی ۱.۲.۰ با Push منتشر شد. [[mobile-app]]
  - **دایجستِ روزانه‌ی هشدارها (Push):** `services/alert_digest.py` + `alert_digest_cron.py` (`python -m app.alert_digest_cron`)
    برای هر مستأجرِ **دارای دستگاه**، در `tenant_scope` هشدارهای danger/warning را می‌سنجد و یک Push خلاصه با
    `route=alerts` می‌فرستد (بی‌حالت؛ ضدنویز = زمان‌بندیِ روزانه). تایمرِ systemd `cubita-alert-digest.timer`
    روزانه ۰۴:۳۰ UTC (۰۸:۰۰ تهران). اجرای زنده‌ی prod تأیید شد (`tenants:1, pushed:0`).
  - **اپ (نسخه ۱.۳.۰ / versionCode 4):** `AlertsScreen` (فهرستِ کاملِ `/api/alerts`، مرتب بر شدت) + `HomeStack`
    (تبِ خانه استک شد) + کارتِ هشدارِ خانه قابلِ‌لمس + deep-linkِ `route=alerts` هنگامِ لمسِ اعلان.

- **۱۴۰۵/۰۵/۳۱ (2026-08-22) — آپدیتِ خودکارِ درون‌برنامه‌ایِ اپ اندروید (کامیت `57dab0d`):**
  - **هدف:** پایانِ نصبِ دستی. اپ خودش را از سرورِ خودمان به‌روز می‌کند — همان الگوی دسکتاپ (electron-updater از `/updates`)،
    و نه GitHub/EAS، چون دسترسی از ایران باید مطمئن بماند.
  - **کلاینت (`mobile/src/update/`):** `updater.ts` مانیفستِ `latest.json` را می‌خواند، `versionCode`ِ نصب‌شده را
    (`expo-application`) می‌سنجد، APK را resumable دانلود می‌کند (`expo-file-system/legacy`) و با `content://` از FileProviderِ
    خودِ expo-file-system + `expo-intent-launcher` (`ACTION_INSTALL_PACKAGE`) نصب می‌کند. `AppUpdateProvider.tsx` = بررسیِ
    خودکارِ بی‌صدا در بدو اجرا + مودالِ برندی (نسخه/توضیح/نوارِ پیشرفت/اجباری). «بیشتر» = نسخه‌ی واقعی + دکمه‌ی بررسیِ دستی.
  - **پیش‌نیازِ نیتیو:** `app.json` → `version 1.1.0`، `android.versionCode 2`، مجوزِ `REQUEST_INSTALL_PACKAGES`. کلیدِ امضا باید
    ثابت بماند (`android/app/debug.keystore`)؛ **هرگز `expo prebuild --clean` نه** — امضا عوض می‌شود و آپدیتِ درجا می‌شکند.
  - **سرور:** `/opt/hesabdari/updates/android/` (کنارِ فیدِ دسکتاپ) + قاعده‌ی no-cacheِ nginx برای `latest.json`
    (قرینه‌ی `latest.yml`). نسخه‌ی راه‌اندازِ ۱.۱.۰ (versionCode 2، ۹۵MB) منتشر و از HTTPS تأیید شد.
  - **روالِ انتشار:** `bash mobile/scripts/release-apk.sh "توضیح"` → بیلد + `release/{cubita-vN.apk,latest.json}` → `scp` به سرور.
    versionCode باید هر بار +۱ شود.

- **۱۴۰۵/۰۵/۳۱ (2026-08-22) — شخصی‌سازیِ پنل بر اساسِ صنف/شرکت (بک‌اند + دسکتاپ/وب):**
  - **دو لایه‌ی ماژول:** «حقِ دسترسی» (entitlement) که سوپرادمین برای ماژول‌های **محدود** می‌دهد + در بک‌اند گیت می‌شود
    (`require_module` روی روترِ تولید → بدونِ گرنت ۴۰۳)، و «نمایش» (preference) که **مالک** برای کلِ کسب‌وکار روشن/خاموش می‌کند
    (فقط پنهان از منو، بی‌حذفِ داده).
  - **رجیستریِ واحد** `app/services/modules.py`: `CORE` (داشبورد/اشخاص/گزارش) · `OPTIONAL` (۱۴) · `RESTRICTED` (`manufacturing`) · ۵ قالبِ صنفی.
  - **مدل/مهاجرت `0073`:** `tenants.{industry,enabled_modules(NULL=همه، سازگاریِ عقب‌رو),granted_modules}`؛ بک‌فیلِ گرنتِ تولید برای
    حساب‌های دارای BOM/سفارشِ تولید.
  - **API:** `MeOut.{industry,enabled_modules,allowed_modules}` · `GET/PUT /api/modules` (فقط مالک) · سوپرادمین:
    `POST /api/admin/accounts/{id}/{industry,modules}`.
  - **فرانت:** `buildNav` ناوبری را با `enabled ∩ allowed` فیلتر می‌کند (Sidebar/TopNav/CommandPalette) · صفحه‌ی «شخصی‌سازیِ پنل»
    (مالک) · انتخابِ صنف + گرنتِ تولید در «مدیریت اکانت‌ها» (سوپرادمین).
  - **انتخابِ صنف در ثبت‌نامِ خودسرویس:** فیلدِ `industry` به `SignupIn` + فرمِ `SignupScreen`؛ ثبت‌نام قالبِ صنف را اعمال می‌کند
    ولی `set_industry(..., grant_restricted=False)` تا کاربر با اعلامِ صنف ماژولِ محدود (تولید) را خودش باز نکند — قفل تا گرنتِ سوپرادمین.
  - **راستی‌آزمایی:** ۱۱۶۷ تستِ بک‌اند (+۹ تستِ ماژول) سبز؛ مهاجرت `0073` up/down روی DBِ محلی؛ `tsc -b`+`vite build` پاک؛
    جریانِ کاملِ احرازشده روی سرورِ زنده (‏`/me`، `GET/PUT /api/modules`، گیتِ ۴۰۳، گرنت→۲۰۰). (head = `0073`.)

- **۱۴۰۵/۰۵/۳۰ (2026-08-21) — نقشِ «مامور حمل/انتقال» + گردشِ کارِ تحویل در بازار (بک‌اند + دسکتاپ/وب؛ کامیت‌های محلی، در انتظارِ پوش):**
  - **(۱) نقشِ «مامور حمل/انتقال» (`delivery_agent`):** نقشِ تازه با دسترسیِ فقط «بازار: view + deliver» به `DEFAULT_ROLES` افزوده شد
    و مهاجرت `0072` آن را برای همه‌ی مستأجرهای موجود بک‌فیل می‌کند (درجِ میان‌مستأجری با `rls_disabled`، مثلِ 0039/0043). اکشنِ
    اختصاصیِ `deliver` تا مامور حمل نتواند سفارش تأیید/رد یا مرجوعی تأیید کند (آن‌ها `approve` می‌خواهند). `require_permission` حالا
    تاپلِ اکشن هم می‌پذیرد؛ اندپوینتِ تحویل با `("deliver","approve")` باز می‌شود (مالک هم می‌تواند تحویل بزند). `deliver` به `WRITE_ACTIONS` رفت.
  - **(۲) گردشِ کارِ تحویل (`require_delivery`، اختیاری per-distributor):** با روشن‌بودن، تأییدِ سفارشِ اعتباری فقط آن را می‌پذیرد
    (بدونِ سند)؛ **ورودِ کالا به انبارِ فروشگاه + پستِ دوطرفه + تسویه‌ی نقدِ COD** هنگامِ ثبتِ «تحویل» (`deliver_order`) انجام می‌شود.
    `marketplace_orders.{delivered_at,delivered_by_name}` برای پاسخ‌گویی. سفارشِ از قبل سنددار (مثلِ آنلاینِ پرداخت‌شده) با تحویل فقط
    علامت می‌خورد، سند دوباره نمی‌سازد. خاموش = رفتارِ قبلی دست‌نخورده. مرجوعی حالا روی `delivered` هم مجاز است (شرط: فاکتورِ خرید صادر شده).
    UI: تاگلِ گردشِ کار + راهنمای نقشِ مامور حمل در تنظیماتِ پخش‌کننده؛ دکمه‌ی «تحویل شد» + دیالوگِ نقدِ تحویل روی سفارشِ منتظرِ تحویل
    ([DistributorPage](desktop/src/pages/DistributorPage.tsx))؛ نمایشِ «تأییدشده — در انتظارِ تحویل» و «تحویل‌شده» سمتِ فروشگاه.
  - **(۳) بارکد (مورد قبلیِ کاربر، قبلاً در `b94328a` انجام‌شده — تأیید شد):** یکتاییِ بارکد با ۴۰۹ و نامِ کالای متصادم (`_assert_barcode_free`
    + ایندکسِ `uq_items_tenant_barcode` مهاجرت `0069`) و انتقالِ خودکارِ بارکد به کالای فروشگاه هنگامِ فروش (`_resolve_retailer_item`).
  - **راستی‌آزمایی:** **۱۱۵۸ تستِ بک‌اند سبز** (۴ تازه: تعویقِ سند تا تحویل + idempotency، ردِ تحویلِ تأییدنشده، تحویلِ سفارشِ از قبل سنددار،
    مجوزِ نقشِ مامور حمل)؛ `tsc -b` + `vite build` دسکتاپ پاک؛ مهاجرت `0072` روی DBِ محلی اعمال و بک‌فیلِ نقش تأیید شد.
    **استقرارِ prod در انتظارِ برگشتِ VPS.** (head = `0072`.)
  - **هات‌فیکسِ استقرارِ prod (RLS در مهاجرت‌های داده):** اولین اجرای `deploy.sh production` سرِ `0069` شکست خورد چون
    قدمِ «خالی‌کردنِ بارکدهای تکراری» یک `UPDATE items` بدونِ زمینه‌ی مستأجر بود؛ روی prod که `items` تحتِ FORCE RLS است ۰ ردیف
    دید (de-dup بی‌اثر) و بعد `CREATE UNIQUE INDEX` روی بارکدهای تکراریِ واقعیِ prod شکست خورد. رفع: `0069` (de-dup) و `0070`
    (بک‌فیلِ `received_qty`) در `rls_disabled` پیچیده شدند (مثلِ 0039/0043/0072). `deploy.sh` هم اصلاح شد تا خطای واقعیِ مهاجرت
    را در `/tmp/cubita_migrate_*.log` نگه دارد و روی شکست چاپ کند (قبلاً با grep پنهان می‌شد). با بارکدِ تکراریِ کاشته‌شده روی
    DBِ محلی، زنجیره‌ی `0068→0072` تمیز رد شد.

- **۱۴۰۵/۰۵/۲۹ (2026-08-20) — زونِ مشتری + مرجوعیِ بازار + قیمتِ مصرف‌کننده/تاریخِ بچ (بک‌اند + دسکتاپ/وب؛ کامیت‌های محلی، در انتظارِ پوش):**
  - **(۱) زونِ ارسال (پخش‌کننده):** پخش‌کننده زون‌های خودش را تعریف می‌کند (نه نقشه) و هر فروشگاهِ متصل را در یک زون می‌گذارد.
    جدولِ سراسریِ `marketplace_zones` + `marketplace_connections.zone_id`. سرویس/روترِ CRUD + تخصیص. UI: تبِ «زون‌ها» ([MpZonesPanel](desktop/src/components/MpZonesPanel.tsx))
    + انتخابگرِ زون در تبِ «اتصال‌ها».
  - **(۲) مرجوعیِ بازار (درخواستِ فروشگاه → تأییدِ پخش‌کننده):** جدول‌های سراسریِ `marketplace_returns`/`marketplace_return_lines`.
    `request_return` (پارشال به‌ازای ردیف، با اعمالِ مهلتِ روز و سقفِ باقی‌مانده)، `approve_return` (پستِ دوطرفه با بازاستفاده از
    `post_sales_return`/`post_purchase_return`)، `reject_return`. سیاستِ مرجوعی (متن + مهلتِ روز) روی `marketplace_settings` و در تنظیماتِ پخش‌کننده.
    UI: تبِ «مرجوعی‌ها»ی پخش‌کننده ([MpDistributorReturns](desktop/src/components/MpDistributorReturns.tsx)) و تبِ «مرجوعی»ِ فروشگاه
    ([MpRetailerReturns](desktop/src/components/MpRetailerReturns.tsx)؛ سیاست + مهلت را می‌بیند). `OrderLineOut.id` و سیاست به `OrderOut` افزوده شد.
  - **(۳) قیمتِ مصرف‌کننده + تاریخِ بچ:** `marketplace_listings.consumer_price` → روی کاتالوگ **حاشیه‌ی سود** (خرید/فروش/درصد) دیده می‌شود و
    هنگامِ تأییدِ سفارش **خودکار روی بچِ فروشگاه** می‌نشیند (`_fulfill`). `stock_batches.{consumer_price,production_date}` + فرمِ بارِ دستی
    (تاریخِ تولید/انقضا، قیمتِ خرید، قیمتِ مصرف) و نمایشِ حاشیه‌ی سود در درایورِ بار.
  - **راستی‌آزمایی:** **۱۱۵۴ تستِ بک‌اند سبز** (۸ تازه: زون CRUD/گیت، قیمتِ مصرف روی کاتالوگ و بچ، مرجوعیِ دوطرفه/سقف/مهلت/رد، سیاست)؛
    `tsc -b` + `vite build` دسکتاپ پاک. **استقرارِ prod در انتظارِ برگشتِ VPS.** (head = `0071`.)

- **۱۴۰۵/۰۵/۲۸ (2026-08-19) — دسته‌بندیِ کاتالوگِ بازار + جداسازیِ بارِ ورودی/سریالِ کارتن (کامیت‌های محلی، در انتظارِ پوش):**
  - **(۱) فیلترِ دسته در کاتالوگِ بازار:** در تبِ «کاتالوگ»ِ [بازارِ خرید](desktop/src/pages/MarketplacePage.tsx)، ردیفِ چیپ‌های دسته
    (از فیلدِ `category`ِ موجودِ لیستینگ) اضافه شد؛ فروشگاه با یک کلیک بین دسته‌ها فیلتر می‌کند (به‌علاوه‌ی جست‌وجوی متنی). هر دو پلتفرم.
  - **(۲) جداسازیِ بارِ ورودی (بچ) + سریالِ کارتن + ردیابیِ کسری/معیوب/ضایعات — بک‌اند + دسکتاپ/وب:**
    - **بک‌اند:** `stock_batches` از «دفترِ انقضا» به «دفترِ بارِ ورودی» ارتقا یافت (`received_qty`/`unit_cost`/`source_type`/`source_id`).
      هر ردیفِ **فاکتورِ خرید** خودکار یک بار می‌سازد (`post_purchase_invoice`؛ شماره‌ی `P{شماره}-{ردیف}`). جدولِ تازه‌ی `stock_batch_serials`
      (سریالِ کارتن؛ افزودنِ دستی یا **تولیدِ توالیِ خودکار**، نشانه‌گذاریِ معیوب). اندپوینتِ `POST /api/stock-batches/{id}/adjust` کسری/معیوب/
      ضایعات را از موجودی و حسابداری کم می‌کند (بازاستفاده از `post_stock_adjustment`) و به `stock_adjustments.batch_id` گره می‌زند تا معلوم
      بماند کدام بار مشکل داشت. مهاجرت `0070`.
    - **دسکتاپ/وب:** [BatchesPanel](desktop/src/components/BatchesPanel.tsx) ستون‌های منشأ/ورودی‌مانده/کسری/سریال گرفت؛ درایورِ تازه‌ی
      [BatchDetailDrawer](desktop/src/components/BatchDetailDrawer.tsx): مدیریتِ سریالِ کارتن (دستی/توالی) + ثبتِ کسری/معیوب/ضایعات.
    - **موبایل:** بخشِ سریالِ کارتنِ اپراتورِ انبار به **فازِ بعد** موکول شد (طبقِ تصمیمِ کاربر).
  - **راستی‌آزمایی:** **۱۱۴۶ تستِ بک‌اند سبز** (۵ تازه: بچ‌سازیِ خودکارِ خرید، سریالِ دستی/توالی، کسری/معیوب با اثر روی موجودی، سقفِ کسری)؛
    `tsc -b` + `vite build` دسکتاپ پاک. **استقرارِ prod در انتظارِ برگشتِ VPS.** (head = `0070`.)

- **۱۴۰۵/۰۵/۲۸ (2026-08-19) — ۵ بازخوردِ کاربران: صندوق/بارکد/ثبت‌کننده (کامیت‌های محلی، در انتظارِ پوش چون VPS پایین است):**
  - **(۱) چاپِ رسیدِ حرارتی در صندوق:** بعد از هر تسویه (نقدی/کارتی)، رسیدِ ۸۰م‌م چاپ می‌شود.
    [PosReceipt.tsx](desktop/src/components/PosReceipt.tsx) با پورتال به `document.body` + قاعده‌های `@media print` در App.css
    (همه‌چیز پنهان جز رسید؛ `@page size:80mm auto`). تاگلِ **«چاپِ خودکار بعد از فروش»** (ماندگار در localStorage) + دکمه‌ی
    «چاپِ رسیدِ آخر». رسید شاملِ نامِ فروشگاه، **صندوق‌دار**، تاریخ/ساعت، شماره‌ی فاکتور، اقلام، جمع‌ها، و نقدی/کارتی+مرجع. هر دو پلتفرم.
  - **(۲) نمایشِ ثبت‌کننده‌ی فاکتور:** `created_by_id` از قبل بود؛ حالا خروجیِ API نام و نقش را هم می‌دهد
    (`_attach_creators` در [invoices.py](backend/app/routers/invoices.py)، join به users/memberships؛ فیلدهای
    `created_by_name/created_by_role` روی `SalesInvoiceOut`/`PurchaseInvoiceOut`). در جزئیاتِ فاکتور («ثبت‌کننده: نام (نقش)») و روی رسیدِ صندوق دیده می‌شود.
  - **(۳) بارکدِ یکتا:** دو کالا دیگر نمی‌توانند بارکدِ یکسان بگیرند. ایندکسِ جزئیِ `uq_items_tenant_barcode` (مهاجرت `0069`،
    با پاک‌سازیِ تکراری‌های موجود: نگه‌داشتن روی جدیدترین، NULL‌کردنِ بقیه) + اعتبارسنجیِ نرمِ ۴۰۹ در create/update (`_assert_barcode_free`).
  - **(۴) بهبودِ بارکدخوانِ گوشی:** [BarcodeScanner.tsx](desktop/src/components/BarcodeScanner.tsx) — دادنِ فهرستِ صریحِ فرمت‌های
    خرده‌فروشی به `BarcodeDetector`، رزولوشنِ `1280×720`، دکمه‌ی **فلاش** (torch)، و throttleِ تشخیص به ~۸/ثانیه (کاهشِ داغ‌شدن/کندی).
  - **(۵) انتقالِ خودکارِ بارکد از پخش‌کننده:** هنگامِ تأییدِ سفارشِ بازار، بارکدِ کالای پخش‌کننده روی کالای متناظرِ فروشگاه می‌نشیند
    (`_resolve_retailer_item` در [marketplace.py](backend/app/services/marketplace.py)؛ بک‌فیل روی کالای لینک‌شده‌ی بی‌بارکد، و
    صرف‌نظر در صورتِ تصادمِ بارکد). فروشگاه دیگر لازم نیست دستی تنظیم کند.
  - **راستی‌آزمایی:** **۱۱۳۶ تستِ بک‌اند سبز** (۸ تازه: یکتاییِ بارکد، انتقالِ بارکدِ بازار، ثبت‌کننده‌ی فاکتور)؛ drift/isolation سبز؛
    `tsc -b` دسکتاپ پاک. **استقرارِ prod در انتظارِ برگشتِ VPS.** (head مهاجرت = `0069`.)

- **۱۴۰۵/۰۵/۲۷ (2026-08-18) — اپ اندرویدِ کوبیتا (`mobile/`) + M1 بک‌اند: رفرش‌توکن و Push (کامیت‌های محلی، در انتظارِ پوش چون VPS پایین است):**
  - **اپ اندروید (`mobile/`، Expo SDK 57 / React Native):** یک اپ نیتیوِ **مستقل از طراحیِ دسکتاپ/وب** با UI/UX مخصوصِ اندروید
    (تمِ تیره‌ی فینتک نزدیک‌مشکی + لهجه‌ی طلایی، RTL، ارقامِ فارسی). ۵ تب: **خانه** (KPI + نمودارِ SVG + هشدارها)، **گزارش‌ها**
    (سود/زیان، ترازنامه، مطالبات/بدهی، موجودی + اشتراکِ PDF)، **بازار** (اتصال/سفارش + گفتگو با پولینگ)، **اشخاص** (کارتِ حساب + PDF)،
    **بیشتر** (پروفایل/بیومتریک/خروج). همان بک‌اندِ prod؛ فقط شکلِ DTOها بازتاب داده شد، هیچ CSSای کپی نشد. آیکون/اسپلشِ برند،
    `ir.cubita.app`، deep-link `cubita://`. مایلستون‌های M0/M2/M3/M4/M5 (کامیت‌های `d4748e9`،`8b2606d`،`9d5c7b8`،`219caf0`).
  - **M1 بک‌اند — رفرش‌توکن (برای «همیشه‌واردمانده»):** جدولِ سراسریِ `refresh_tokens` (مهاجرت `0067`، در `GLOBAL_TABLES`)،
    سرویسِ [refresh.py](backend/app/services/refresh.py) با **چرخشِ توکن** (توکنِ ارائه‌شده باطل، تازه صادر) و گره به `user.token_version`
    (تغییرِ رمز همه‌ی رفرش‌ها را هم می‌کشد). اندپوینت‌های `POST /api/auth/refresh` و `/logout`؛ `login` علاوه بر access یک refresh هم می‌دهد
    (وب/دسکتاپ نادیده می‌گیرند). عمرِ ۶۰ روزه.
  - **M1 بک‌اند — Push (FCM):** جدولِ سراسریِ `device_tokens` (مهاجرت `0068`)، روترِ [devices.py](backend/app/routers/devices.py)
    (`POST/DELETE /api/devices`، upsert بر پایه‌ی توکن)، سرویسِ [push.py](backend/app/services/push.py) با **no-opِ بی‌خطا وقتی FCM
    کانفیگ نشده** (مثلِ SMTP/پیامک). قلاب‌های رویداد در روترِ بازار: پیامِ تازه‌ی چت (اتصال/سفارش)، درخواستِ اتصال، و سفارشِ تازه →
    `safe_notify_tenant` (شکستِ اعلان هرگز تراکنش را نمی‌شکند)، همراهِ `route` برای deep-link.
  - **سمتِ اپ:** کلاینت خودش روی ۴۰۱ **رفرشِ تک‌پروازه** می‌زند و درخواست را یک بار دوباره اجرا می‌کند؛ AuthContext هر دو توکن را در
    SecureStore نگه می‌دارد و توکنِ چرخیده را ماندگار می‌کند؛ خروج، رفرش را سمتِ سرور هم باطل می‌کند. (ثبتِ توکنِ FCM سمتِ اپ نیازمندِ
    پروژه‌ی Firebase است و به فازِ بعد موکول شد؛ زیرساختِ بک‌اند و اندپوینت آماده است.)
  - **آدرسِ بک‌اند:** [config.ts](mobile/src/api/config.ts) **پیش‌فرض همیشه prod** (`https://acc.cubita.ir`) است — یعنی اپ به همان حساب‌هایی
    وصل می‌شود که با برنامه ساخته شده‌اند. اتصال به شبکه‌ی داخلی **opt-inِ صریح** است: `EXPO_PUBLIC_API_BASE_URL` (در `mobile/.env` یا هنگامِ
    اجرا/ساخت؛ نمونه در `mobile/.env.example`). پلاگینِ `expo-build-properties` با `android.usesCleartextTraffic:true` تا APKِ داخلی روی HTTP
    وصل شود (prod همچنان HTTPS).
  - **قراردادِ اپ↔بک‌اند تأیید شد:** [test_mobile_api_contract.py](backend/tests/test_mobile_api_contract.py) یک حسابِ استانداردِ واقعی
    (ثبت‌نامِ برنامه) می‌سازد، با توکنِ Bearerِ واقعی login می‌کند، و **همه‌ی اندپوینت‌هایی که ۵ تبِ اپ صدا می‌زنند** (me، خلاصه/داشبورد/هشدار،
    سود‌و‌زیان/ترازنامه/مطالبات/موجودی، اشخاص + کارتِ حساب + اعتبار، unreadِ بازار) را ۲۰۰ می‌گیرد؛ حسابِ استاندارد در تبِ بازار خطا نمی‌دهد.
  - **راستی‌آزمایی:** **۱۱۲۳ تستِ بک‌اند سبز** (۱۶ تازه: [test_refresh_token.py](backend/tests/test_refresh_token.py)،
    [test_devices.py](backend/tests/test_devices.py)، [test_push.py](backend/tests/test_push.py))؛ drift/isolation سبز (هر دو جدول در
    `GLOBAL_TABLES`). اپ: `tsc` پاک + باندلِ Metro موفق. **استقرارِ prod در انتظارِ برگشتِ VPS است.**

- **۱۴۰۵/۰۵/۲۶ (2026-08-17) — پشتیبان‌گیری/بازیابیِ کامل + ذخیره‌ی محلیِ خودکار (کامیت `1d99b5e`، دسکتاپ v1.2.4):**
  - **بک‌اند:** روترِ [backup.py](backend/app/routers/backup.py) — `GET /api/backup/export` با پیمایشِ `Base.metadata.sorted_tables`
    و تکیه بر RLS، کلِ داده‌ی مستأجر را در یک JSON می‌دهد (بدونِ برشمردنِ دستیِ جدول‌ها؛ جدولِ تازه خودکار پوشش می‌گیرد).
    `POST /api/backup/import` = جایگزینیِ کامل: پاک‌کردن (معکوسِ FK) سپس درج (والد پیش از فرزند + مرتب‌سازیِ self-refِ چارتِ حساب)،
    tenant_id بازنگاشت، پرچمِ `app.audit_purge` برای پاک‌کردنِ دفترِ حسابرسی؛ همه در یک تراکنش (خطا → rollbackِ کامل، داده‌ی قبلی سالم).
    جدول‌های ساختاریِ ارجاع‌شده از جدول‌های سراسری (مثلِ `roles` ← `memberships`) خودکار کنار می‌مانند. هر دو فقط `role.key=='owner'`.
    تستِ round-trip روی Postgres واقعی: [test_backup.py](backend/tests/test_backup.py). **۱۱۰۷ تستِ بک‌اند سبز.** بدونِ مهاجرت (فقط روتر).
  - **دسکتاپ:** [electron/backup.ts](desktop/electron/backup.ts) — پس از هر همگام‌سازیِ موفق یک نسخه در `userData/backups` می‌نویسد و
    ۲۰ نسخه‌ی آخر را نگه می‌دارد؛ IPC: `backup:auto|saveToFile|listLocal|openFolder|restoreFromFile`. کارتِ [BackupCard](desktop/src/components/BackupCard.tsx)
    در پروفایلِ مالک: ذخیره روی کامپیوتر (دیالوگ)، پوشه‌ی پشتیبان‌ها، بازیابی از فایل (تأییدِ صریح). **وب:** دانلودِ Blob + بازیابی با `<input file>`.
  - **استقرار:** بک‌اندِ prod (surgical؛ safety-diffِ main.py = فقط ۲ افزوده)، وبِ acc+demo، نصب‌کننده‌ی ۱.۲.۴ روی فید. تأیید: `export`/`import`=۴۰۱ بی‌توکن.

- **۱۴۰۵/۰۵/۲۴ (2026-08-15) — «ساختارِ جدید» روی هر سه تم + Tipalti پیش‌فرض (کامیت `80ac8eb`، فقط فرانت):**
  - **هدف:** ساختارِ تمِ جدید (نوارِ افقی + کارهای مرحله‌ای + داشبوردِ اقدام‌محور + کارت‌های موبایل) روی
    هر سه تم اجرا شود؛ تیره و روشن فقط **پالتِ رنگ**شان فرق کند. و تمِ پیش‌فرضِ سایت = Tipalti.
  - **کلیدِ معماری:** جداکردنِ «ساختار» از «رنگ» با یک صفتِ تازه. [applyTheme](desktop/src/lib/theme.ts) علاوه‌بر
    `data-theme` (رنگ) حالا `data-structure` (=content) هم می‌گذارد. تیره/روشن → `shell:'topnav'`, `content:'guided'`.
  - **CSS:** ۹۴ قاعده‌ی ساختاریِ [App.css](desktop/src/App.css) از `:root[data-theme='tipalti']` به `:root[data-structure='guided']`
    رِسکوپ شد (هم‌تخصصِ (0,2,0) → بدونِ شکستِ نبردهای specificity). مقیاسِ هوادار (fs/sp/radius/control-h) از بلوکِ
    رنگِ Tipalti به بلوکِ مشترکِ `:root[data-structure='guided']` در [index.css](desktop/src/index.css) منتقل شد؛ بلوکِ **رنگِ**
    Tipalti دست‌نخورده ماند. توکن‌های `--topnav-*` از قبل در هر تم تعریف بودند (تیره→تیره، روشن→روشن).
  - **صفرِ تغییرِ TSX:** همه‌ی شاخه‌ها از قبل `theme.content==='guided'`/`theme.shell==='topnav'` بودند (نه id تم).
  - **راستی‌آزمایی:** Playwright — هر سه پالت روی ساختارِ جدید درست تمی می‌شوند (topnav navy/تیره/روشن؛ دکمه‌ی اصلی
    gold/آبی/آبی)؛ سوییچِ زنده رنگ‌ها را بازمحاسبه می‌کند. باندل `index-DtzD0jCN.js`. prod+دمو هر دو ۲۰۰.

- **۱۴۰۵/۰۵/۲۴ (2026-08-15) — موبایل: فهرست‌های تخت به‌جای اسکرولِ افقی، کارتِ فشرده می‌شوند (کامیت `832f7ad`، فقط فرانت):**
  - **مشکل:** فهرست‌هایی مثلِ فاکتورها/پیش‌فاکتورها که بلوکِ کارتِ موبایلِ اختصاصی نداشتند، روی ≤۷۶۰px داخلِ `.table-scroll`
    افقی می‌لغزیدند. راهِ‌حل: یک **ابزارِ عمومیِ CSS** به‌جای بلوکِ per-table.
  - **`.cards-on-mobile`** ([App.css](desktop/src/App.css)): کافی‌ست به خودِ `<table>` این کلاس و به هر `<td>` صفتِ `data-label`
    داده شود. روی موبایل هر ردیف یک **کارتِ گریدِ ۲ستونه‌ی برچسب:مقدار** می‌شود؛ سلولِ سرِ کارت `.card-title` (بالای کارت،
    پررنگ)، دکمه‌ها `.card-actions` (ته، قابل‌لمس)، ردیفِ جزئیاتِ بازشده `.card-full`، سلولِ بی‌معنی `.card-hide`. جمع‌وجور.
  - **پوشش:** فاکتورهای فروش/خرید ([InvoiceList](desktop/src/components/InvoiceList.tsx))، پیش‌فاکتورها ([QuotationsList](desktop/src/components/QuotationsList.tsx))،
    تاریخچه‌ی مؤدیان، بودجه، بستنِ دوره، جوایز، کارتخوان‌ها، انبارگردانی (جلسات+ریزِ شمارش)، خریدهای سایت، و جدول‌های
    بازار ([MarketplacePage](desktop/src/pages/MarketplacePage.tsx)/[DistributorPage](desktop/src/pages/DistributorPage.tsx): پخش‌کننده‌ها، سفارش‌های من، کاتالوگ لیستینگ، سفارش‌ها، اتصال‌ها، کمیسیون).
  - **استقرار:** tsc پاک؛ Playwright در ۳۶۰/۳۹۰px بدونِ سرریزِ افقی. باندل `index-MmooZ7Iy.js`. سواپِ فقط‌وب روی prod+دمو؛ هر دو ۲۰۰.

- **۱۴۰۵/۰۵/۲۴ (2026-08-15) — ویزاردِ «ثبت لیستینگ» در پخشِ من (کامیت `76826b2`، فقط فرانت):**
  - هوکِ مشترکِ [useListingDraft](desktop/src/lib/listingDraft.ts) (state + اعتبارسنجی + submit) از فرمِ `Catalog`ِ
    [DistributorPage](desktop/src/pages/DistributorPage.tsx) استخراج شد تا فرمِ کلاسیک و ویزارد یک منبعِ منطق داشته باشند.
  - [ListingWizard](desktop/src/components/wizard/ListingWizard.tsx) روی `TaskFlow`: چهار مرحله (نوع و مشخصات / قیمت و عکس /
    محدودیت و انتشار / بازبینی و ثبت) + پیش‌نمایشِ زنده؛ «ویرایش»ِ فهرست هم به همین ویزارد می‌رود. `Catalog` در پوسته‌ی
    `guided` ویزارد و در تیره/روشن همان فرمِ کلاسیک را می‌دهد (لیستِ کاتالوگ در هر دو یکی است).
  - **استقرار:** tsc پاک (app+electron)؛ باندل `index-BiWeeG2l.js`. سواپِ فقط‌وب روی prod+دمو؛ هر دو ۲۰۰.

- **۱۴۰۵/۰۵/۲۴ (2026-08-15) — تکمیلِ صفحه‌بندیِ ۱۰تایی برای فهرست‌های جامانده + جابه‌جاییِ «اتصال فروشگاه» (کامیت `7bacfa7`، فقط فرانت):**
  - **صفحه‌بندی:** ارتقای `bfac44a` بعضی فهرست‌های انباشتیِ تازه/جامانده را نداشت. با همان `usePagination`/`Pager` (۱۰ ردیف،
    مثلِ چارتِ حساب‌ها) به این‌ها هم افزوده شد: **دفتر روزنامه** ([JournalDaybookPanel](desktop/src/components/JournalDaybookPanel.tsx))،
    **فاکتورها** ([InvoiceList](desktop/src/components/InvoiceList.tsx))، **تاریخچه‌ی مؤدیان** ([MoadianPanel](desktop/src/components/MoadianPanel.tsx))،
    **قراردادهای اقساط** ([InstallmentsPage](desktop/src/pages/InstallmentsPage.tsx))، **بودجه** ([BudgetPanel](desktop/src/components/BudgetPanel.tsx))،
    **جلسه‌های انبارگردانی** ([StockCountPanel](desktop/src/components/StockCountPanel.tsx))، **دوره‌های بسته‌شده** ([PeriodClosePanel](desktop/src/components/PeriodClosePanel.tsx))،
    **جوایز** ([RewardsPanel](desktop/src/components/RewardsPanel.tsx))، **کارتخوان‌ها** ([PosTerminalsPanel](desktop/src/components/PosTerminalsPanel.tsx))،
    **فرمول‌ها و سفارش‌های تولید** ([ManufacturingPage](desktop/src/pages/ManufacturingPage.tsx))، **مدیریت اکانت‌ها** ([AccountsAdminPage](desktop/src/pages/AccountsAdminPage.tsx))،
    **خریدهای سایت** ([PurchasesAdminPanel](desktop/src/components/PurchasesAdminPanel.tsx))، و جدول‌های بازار (پخش‌کننده/کاتالوگ/سفارش‌های من در [MarketplacePage](desktop/src/pages/MarketplacePage.tsx)؛ لیستینگ در [DistributorPage](desktop/src/pages/DistributorPage.tsx)).
    **کنار گذاشته‌شده به‌عمد:** جدول‌های گزارشی با جمعِ tfoot (مزایا/کمیسیون/تراز/سود‌وزیان/سنین/دفترکل)، جدول‌های ویرایشیِ کاری (ردیف‌های فاکتور/سند/BOM/سبد)، و صف‌های کوتاهِ pending/done.
  - **ناوبری:** «اتصال فروشگاه» از گروهِ «فروش و مشتریان» به گروهِ «ابزار» منتقل شد ([navModel.tsx](desktop/src/lib/navModel.tsx)) — منبعِ واحدِ هر دو پوسته.
  - **استقرار:** tsc پاک؛ باندل `index-C-PfCxU0.js`. سواپِ فقط‌وب روی prod+دمو؛ هر دو ۲۰۰.

- **۱۴۰۵/۰۵/۲۴ (2026-08-14) — «نسخه‌ی جدید»: ساختارِ مرحله‌ای (ویزارد) + داشبوردِ اقدام‌محور برای پوسته‌ی Tipalti (فقط فرانت):**
  - **ایده:** برای پوسته‌ی Tipalti یک «نسخه/ساختارِ جدید» — محتوای مرکزیِ صفحه‌ها به روشِ **مرحله‌ای (ویزارد)** با تاییدِ
    مرحله‌به‌مرحله (الهام از گردشِ Tipalti bill review) + داشبوردِ **اقدام‌محور**. گِیت‌شده به تم: فیلدِ تازه‌ی
    `content:'classic'|'guided'` روی `ThemeDef` ([lib/theme.ts](desktop/src/lib/theme.ts))؛ تنها `tipalti` = `guided`، پس تیره/روشن
    **دقیقاً مثلِ قبل** (فرمِ تک‌صفحه‌ای، overviewِ کلاسیک) می‌مانند.
  - **چارچوبِ ویزارد (تازه [TaskFlow.tsx](desktop/src/components/wizard/TaskFlow.tsx)):** استپرِ شماره‌دار (انجام‌شده✓/فعال/پیشِ‌رو، بازگشت آزاد،
    پرش به جلو نه) + بدنه‌ی مرحله‌ی فعال + **پنلِ پیش‌نمایشِ زنده‌ی کناری** + فوترِ «قبلی/تایید و ادامه/ثبت». اعلامی و قابل‌استفاده‌ی مجدد.
  - **تسکِ مرجع — فاکتور فروش:** منطق از فرمِ کلاسیک به هوکِ مشترکِ [useSalesInvoiceDraft](desktop/src/lib/salesInvoiceDraft.ts)
    استخراج شد (یک منبعِ حقیقت — کلاسیک هم همان را مصرف می‌کند؛ `CreditBanner` صادر شد). [SalesInvoiceWizard.tsx](desktop/src/components/wizard/SalesInvoiceWizard.tsx)
    چهار مرحله: سربرگ/طرف‌حساب ← اقلام (گِیت: ≥۱ ردیفِ معتبر) ← تخفیف/مالیات/پرداخت ← بازبینی و ثبت. ثبت همان مسیرِ فعلی (سرور/صفِ آفلاین).
    [SalesPage](desktop/src/pages/SalesPage.tsx) تبِ فاکتور را `guided?Wizard:Form` شاخه می‌زند.
  - **داشبوردِ اقدام‌محور (تازه [GuidedDashboard.tsx](desktop/src/components/GuidedDashboard.tsx)):** خوش‌آمد + ۴ KPI (بازاستفاده‌ی
    `StatCard` + `fetchTrialBalance/IncomeStatement`) + **مرکزِ اقدام** (لانچرها از [taskRegistry.tsx](desktop/src/lib/taskRegistry.tsx) → `onNavigate`)
    + **صفِ کارها** (بازاستفاده‌ی `AlertsPanel`). [Dashboard](desktop/src/components/Dashboard.tsx) overview را `guided?GuidedDashboard:OverviewPage` شاخه می‌زند.
  - **استایل:** بلوکِ تازه در [App.css](desktop/src/App.css) (`.taskflow*`, `.live-preview*`, `.action-hub/-card`, `.guided-dash`) — کاملاً
    اسکوپ‌شده به `:root[data-theme='tipalti']` + موبایل (استپرِ فشرده «مرحله n از m» + پنلِ پیش‌نمایش زیرِ بدنه).
  - **راستی‌آزمایی:** typecheck app+electron ✓، build ✓؛ Playwright روی هارنسِ CSSِ واقعی (دسکتاپ ۱۴۴۰: استپرِ سه‌حالته + پنلِ پیش‌نمایش +
    اکشن‌هابِ ۶تایی؛ موبایل ۳۹۰px: پنلِ بالا + کارت‌های ردیف)، بدونِ سرریزِ افقی. کلاسیک ساختاراً دست‌نخورده (اجزای قبلی).
  - **استقرار (فقط‌فرانت):** سواپِ دستیِ وب روی `acc.cubita.ir` + آینه‌ی `demo.cubita.ir`؛ بدونِ قطعی/بک‌اند/دیتابیس؛ `web.old` نقطه‌ی بازگشت.
    تأییدِ زنده: هر دو `index--mCrWluA.js` + `index-DoC1pmub.css`؛ کلاس‌های `.taskflow-step-num`/`.action-hub-grid` و رشته‌های ویزارد در باندلِ زنده.
  - **رفعِ باگِ تقویم (پیرو بازخورد):** پاپ‌اورِ `JalaliDatePicker` روی فیلدهای باریک اعدادِ سمتِ چپ را بیرون می‌ریخت. ریشه:
    سلولِ روز یک `<button>` بود و `min-height:var(--control-h)` (۳۶px در Tipalti) می‌گرفت و `aspect-ratio:1` این را به عرض هم
    تحمیل می‌کرد، پس ۷ سلول (۲۶۴px) از عرضِ پاپ‌اور بیرون می‌زد. رفع در [App.css](desktop/src/App.css): `.jalali-date-cell{min-height:0;min-width:0}`
    (+`max-width:100%` روی پاپ‌اور تا از عرضِ فیلد بیرون نزند). تأییدِ Playwright: `gridOverflows:false`، تقویم کاملاً داخلِ کارت.
    مستقر روی prod+demo — باندلِ `index-Dvntd2zy.js` + `index-BopNAvE8.css`.
  - **موجِ دومِ تسک‌ها (همان روز) — پیش‌فاکتور + برگشت از فروش + سامانه مؤدیان (فقط فرانت):** هر سه با همان الگو (هوکِ مشترک +
    ویزارد + شاخه‌ی guided/classic) مرحله‌ای شدند؛ کلاسیک دست‌نخورده.
    - **پیش‌فاکتور:** [useQuotationDraft](desktop/src/lib/quotationDraft.ts) + [QuotationWizard](desktop/src/components/wizard/QuotationWizard.tsx)
      (مشتری/سربرگ ← اقلام ← بازبینی)؛ شاخه در [QuotationsTab](desktop/src/components/QuotationsTab.tsx)؛ حالتِ ویرایش هم پشتیبانی می‌شود.
    - **برگشت از فروش:** [useSalesReturnDraft](desktop/src/lib/salesReturnDraft.ts) + [SalesReturnWizard](desktop/src/components/wizard/SalesReturnWizard.tsx)
      (انتخاب فاکتور ← اقلام برگشتی ← بازبینی)؛ جدولِ قابل‌برگشت و فهرستِ برگشت‌ها به‌صورتِ اجزای صادرشده مشترک شد.
    - **سامانه مؤدیان (بازنشسته — بخشِ ۱۰، ۱۴۰۵/۰۶/۰۶):** ویزارد و پنلِ کلاسیک با ماژولِ چهاربخشیِ
      [MoadianModulePage](desktop/src/pages/moadian/MoadianModulePage.tsx) جایگزین شدند. توضیحِ زیر تاریخِ آن روز است:
      [useMoadianPanel](desktop/src/lib/moadianPanel.ts) + `MoadianWizard`
      (اعتبارنامه ← محیط و تستِ اتصال ← ارسال صورتحساب) با **`allowJump`** (مراحل مستقل، پرشِ آزاد) — چون کاربرِ قبلاً تنظیم‌کرده
      باید مستقیم به «ارسال» برود؛ KPIها بالای ویزارد و تاریخچه‌ی ارسال‌ها زیرِ آن. `MoadianPanel` به اجزای صادرشده (Stats/Credential/
      Toggles/Hints/Send/History) شکسته شد تا پنل و ویزارد یک منبع داشته باشند. قابلیتِ تازه‌ی [TaskFlow](desktop/src/components/wizard/TaskFlow.tsx): پراپِ `allowJump`.
    - **راستی‌آزمایی/استقرار:** typecheck app+electron ✓، build ✓؛ Playwright روی چیدمانِ مرحله‌ی مؤدیان (textareaها) و جدولِ برگشت
      بدونِ سرریز. مستقر روی prod+demo — باندلِ `index-Do_398IF.js` + `index-BopNAvE8.css`.
  - **حذفِ بالای تب‌ها در صفحه‌های ماژول (پیرو بازخورد؛ فقط guided):** سربرگ (آیکن+عنوان+توضیح) و ردیفِ KPI بالای تب‌ها در
    صفحه‌های ماژول برداشته شد تا محتوا/تب‌ها مستقیم بالا بیایند (داشبورد خودش KPI دارد و نوارِ بالا ماژولِ فعال را نشان می‌دهد).
    یک بلوکِ CSS در [App.css](desktop/src/App.css): `:root[data-theme='tipalti'] .app-content > .page:has(> .tabs) > .page-header,
    …> .stat-grid { display:none }`. با `:has(> .tabs)` فقط صفحه‌های تب‌دار هدف‌اند — داشبوردِ اقدام‌محور (بدونِ .tabs) و
    stat-gridهای داخلِ تب (مثلِ KPIهای مؤدیان) دست‌نخورده. تأییدِ Playwright: header/stat-grid بالا `none`، تب‌ها و KPIِ داخلِ تب سالم.
    مستقر — باندلِ `index-D57zMc_3.js` + `index-CIJv54H0.css`.
  - **موجِ سومِ تسک‌ها (خرید و انبار؛ فقط فرانت):** چهار فرمِ ثبت با همان الگو (هوکِ مشترک + ویزارد + شاخه‌ی guided/classic)
    مرحله‌ای شدند؛ پنل‌های فهرست/گزارشِ انبار (کالاها/موجودی/کاردکس/…) عمداً همان ماندند.
    - **فاکتور خرید:** [usePurchaseInvoiceDraft](desktop/src/lib/purchaseInvoiceDraft.ts) + [PurchaseInvoiceWizard](desktop/src/components/wizard/PurchaseInvoiceWizard.tsx)
      (سربرگ/تأمین‌کننده ← اقلام ← مالیات/تخفیف ← بازبینی)؛ ساختِ سریعِ کالا (QuickItemForm) حفظ شد.
    - **برگشت از خرید:** [usePurchaseReturnDraft](desktop/src/lib/purchaseReturnDraft.ts) + [PurchaseReturnWizard](desktop/src/components/wizard/PurchaseReturnWizard.tsx)؛
      جدولِ اقلامِ قابلِ برگشت (`ReturnableTable`) به شکلِ حداقلی عمومی شد تا بینِ فروش و خرید مشترک باشد.
    - **تعدیل دستیِ موجودی:** [useStockAdjustmentDraft](desktop/src/lib/stockAdjustmentDraft.ts) + [StockAdjustmentWizard](desktop/src/components/wizard/StockAdjustmentWizard.tsx) (کالا/انبار ← مقدار/دلیل).
    - **انتقال بین انبار:** [useTransferDraft](desktop/src/lib/transferDraft.ts) + [TransferWizard](desktop/src/components/wizard/TransferWizard.tsx) (مبدأ/مقصد ← اقلام).
    - شاخه در [PurchasesPage](desktop/src/pages/PurchasesPage.tsx) و [InventoryPage](desktop/src/pages/InventoryPage.tsx). راستی‌آزمایی: typecheck app+electron ✓،
      build ✓، Playwright روی چیدمانِ انتقال (سطرهای select) و تعدیل بدونِ سرریز. مستقر روی prod+demo — باندلِ `index-CuiJl_so.js` + `index-CIJv54H0.css`.
  - **رفعِ خروج از حساب هنگامِ رفرشِ مرورگر (فقط فرانت):** روی وب توکنِ ورود هیچ‌جا ذخیره نمی‌شد — با هر «رفرش»
    `token` دوباره `null` می‌شد و کاربر به صفحه‌ی ورود پرت می‌شد. راه‌حل: هلپرِ تازه‌ی [session.ts](desktop/src/lib/session.ts)
    توکن را در `localStorage` (کلیدِ `cubita.auth.token`) نگه می‌دارد — **فقط وب**؛ در Electron همان مسیرِ `window.cubita.setAuthToken`
    (دیتابیسِ محلی + موتورِ همگام‌سازی) دست‌نخورده می‌ماند. [App.tsx](desktop/src/App.tsx): توکن با تابعِ اولیه از storage خوانده
    می‌شود؛ یک `useEffect` آن را با `fetchMe` اعتبارسنجی می‌کند (توکنِ باطل → پاک و بازگشت به ورود، بدونِ حلقه)؛ تا آن لحظه به‌جای
    فلاش‌خوردنِ صفحه‌ی ورود یک اسپلشِ کوتاه (`.app-boot`) نشان داده می‌شود؛ ورود/ثبت‌نام ذخیره و خروج پاک می‌کند. لینکِ بازیابیِ
    رمز مقدم می‌ماند (جلسه‌ی ذخیره‌شده نادیده گرفته می‌شود). راستی‌آزمایی: Playwright (بدونِ توکن→ورود بدونِ گیرکردن؛ توکنِ نامعتبر→
    اسپلش سپس پاک‌شدن و بازگشت به ورود). مستقر — باندلِ `index-DE4uN1yx.js`.
  - **یکدست‌سازیِ ظاهرِ «دارایی ثابت» و «حقوق و دستمزد» با بقیه‌ی صفحه‌ها (فقط فرانت):** این دو صفحه در [Dashboard.tsx](desktop/src/components/Dashboard.tsx)
    با `className="page"` رندر می‌شدند نه `"page panels"` — پس سطحِ سفیدِ یکپارچه‌ی پنلی (که ۱۹ صفحه‌ی دیگر دارند) را نداشتند:
    دارایی ثابت «روی یک کارت» جمع نمی‌شد و حقوق‌ودستمزد کارتِ سفیدِ زیرِ محتوا نداشت. اصلاح: افزودنِ کلاسِ `panels` به هر دو
    (بدونِ تغییرِ CSS؛ همان قواعدِ اثبات‌شده‌ی `.page.panels`). مستقر — باندلِ `index-It8HFdjs.js`.
  - **حذفِ سربرگ/KPI بالای *همه‌ی* صفحه‌های guided + یکدست‌سازیِ سه صفحه‌ی باقی‌مانده (فقط فرانت):** پیرو بازخورد،
    قاعده‌ی حذفِ بالای صفحه در [App.css](desktop/src/App.css) از `:has(> .tabs)` به `:not(.guided-dash)` گسترده شد تا صفحه‌های
    **بدونِ تب** هم (دارایی ثابت، گزارش‌ها، …) سربرگ و ردیفِ KPI‌شان حذف شود — نوارِ بالا خودش ماژولِ فعال را نشان می‌دهد.
    داشبوردِ اقدام‌محور (`.guided-dash`) مستثنا؛ stat-gridهای داخلِ تب (KPIهای مؤدیان) چون فرزندِ مستقیمِ `.page` نیستند دست‌نخورده.
    همچنین `panels` به سه صفحه‌ی `integration`/`billing`/`reports` در [Dashboard.tsx](desktop/src/components/Dashboard.tsx) افزوده شد (هر سه
    یک `<section>`/محتوای ساده برمی‌گردانند که قواعدِ پنل پوششش می‌دهند). راستی‌آزمایی: هارنسِ Playwright روی CSSِ واقعی —
    سربرگ+KPIِ صفحه‌ی بدونِ تب `display:none`، فرم/فهرست دیده می‌شوند، و KPIِ داشبورد و KPIِ درون‌تبِ مؤدیان دست‌نخورده (PASS).
    مستقر — باندلِ `index-CyTTGY0X.js` + `index-Dkj4UhRk.css`.
  - **رفعِ گترِ خاکستریِ صندوقِ فروشگاهی (فقط فرانت):** دو باگِ ویژگی‌محورِ CSS باعث می‌شد `.pos-grid`
    (که با `background: var(--border-strong)` + `gap:1px` خطِّ ۱px جداکننده می‌سازد) دورِ کارت‌ها خاکستری نشان دهد:
    (۱) قاعده‌ی عمومیِ `.page.panels>*:not(.stat-grid):not(.tabs)` (۰,۴,۰) از `padding:0`ِ pos-grid (۰,۳,۰) قوی‌تر بود →
    شبکه ۲۶px padding می‌گرفت؛ رفع با افزودنِ `:not(.pos-grid)`. (۲) `:root[data-theme='tipalti'] .page section` (۰,۳,۱)
    سایه/گوشه‌گِردِ کارت را به ستون‌ها برمی‌گرداند → قاعده‌ی `.app-shell--topnav … .pos-grid>section` (عمداً ۰,۴,۱:
    قوی‌تر از restyle، ضعیف‌تر از قواعدِ ۰,۵,۱ گوشه‌های پایین) تختش کرد. تأیید با هارنسِ Playwright.
    مستقر — باندلِ `index-jqe6zXhX.js` + `index-gfutZmFb.css`.
  - **موجِ چهارمِ تسک‌ها (مالی و بانکی؛ فقط فرانت):** چهار فرمِ ثبت با همان الگو (هوکِ مشترک + ویزارد + شاخه‌ی guided/classic):
    (۱) **ثبت چک** ([checkDraft](desktop/src/lib/checkDraft.ts) + [CheckWizard](desktop/src/components/wizard/CheckWizard.tsx)): نوع/طرف‌حساب ← مشخصاتِ چک؛
    (۲) **ثبت سند حسابداری** ([journalEntryDraft](desktop/src/lib/journalEntryDraft.ts) + [JournalEntryWizard](desktop/src/components/wizard/JournalEntryWizard.tsx)):
    سربرگ ← ردیف‌ها و موازنه (گِیتِ متوازن‌بودن + پیش‌نمایشِ بدهکار/بستانکار)؛ `JournalLinesTable` مشترکِ فرم و ویزارد؛
    (۳) **تنخواه‌گردان** ([pettyCashDraft](desktop/src/lib/pettyCashDraft.ts) + [PettyCashWizard](desktop/src/components/wizard/PettyCashWizard.tsx)):
    نوعِ گردش (شارژ/هزینه) ← جزئیات، با پیش‌نمایشِ «موجودیِ بعد»؛ `PettyCashLedger` مشترک؛
    (۴) **دارایی ثابت** ([fixedAssetDraft](desktop/src/lib/fixedAssetDraft.ts) + [FixedAssetWizard](desktop/src/components/wizard/FixedAssetWizard.tsx)):
    مشخصات ← بها/استهلاک، با تخمینِ استهلاکِ ماهانه؛ `FixedAssetFields`/`FixedAssetsList`/`DepreciationRun` مشترک؛ ویرایش از فهرست
    (با `formVersion`) ویزارد را به مرحله‌ی اول برمی‌گرداند. شاخه‌ها در `BankingPage`/`AccountingPage`/`Dashboard`.
    راستی‌آزمایی: typecheck (app+electron) ✓، build ✓، رشته‌های هر ۴ ویزارد در JSِ زنده تأیید شد. مستقر — باندلِ `index-ChBxvt53.js`.
  - **موجِ پنجمِ تسک‌ها (حقوق و دستمزد؛ فقط فرانت):** [PayrollPanel](desktop/src/components/PayrollPanel.tsx) که مونولیت بود بازنویسی شد؛
    منطق به سه هوک رفت و زیرکامپوننت‌های مشترک اکسپورت شدند تا کلاسیک و ویزارد یک منبع داشته باشند:
    (۱) **افزودن پرسنل** ([employeeDraft](desktop/src/lib/employeeDraft.ts) + [EmployeeWizard](desktop/src/components/wizard/EmployeeWizard.tsx)): هویت ← استخدام؛
    (۲) **ثبت حکم حقوقی** ([salaryContractDraft](desktop/src/lib/salaryContractDraft.ts) + [SalaryContractWizard](desktop/src/components/wizard/SalaryContractWizard.tsx)):
    کارمند/تاریخ ← حقوق و مزایا، با تخمینِ ناخالص؛
    (۳) **کارکرد و صدور فیش** ([payrollRunDraft](desktop/src/lib/payrollRunDraft.ts) + [PayrollRunWizard](desktop/src/components/wizard/PayrollRunWizard.tsx)):
    ویزاردِ سه‌مرحله‌یِ واقعی — دوره ← کارکردِ همه‌ی پرسنل ← صدور فیش (دکمه‌ی صدور = ثبتِ فوترِ مرحله‌ی آخر)؛ بدونِ پنلِ کناری
    چون جدول‌ها پهن‌اند؛ `PeriodPicker`/`AttendanceTable`/`PayslipResults` مشترک؛ draftِ فرایند در سطحِ پنل نگه‌داشته می‌شود تا
    با جابه‌جایی بین تب‌ها حفظ شود. تب‌های «مزایا»/«تنظیماتِ حقوق» دست‌نخورده. شاخه‌ها در `PayrollPanel`.
    راستی‌آزمایی: typecheck (app+electron) ✓، build ✓، رشته‌های هر ۳ ویزارد در JSِ زنده تأیید شد. مستقر — باندلِ `index-tCPoOFt8.js`.
  - **کامندپالتِ سراسری (Ctrl/⌘+K؛ فقط فرانت، guided):** [CommandPalette](desktop/src/components/CommandPalette.tsx) — اورلیِ جست‌وجو
    برای پرش به هر صفحه یا شروعِ یک کار؛ منبعِ فرمان‌ها همان [navModel](desktop/src/lib/navModel.tsx) (با گیتِ نقش) + [taskRegistry](desktop/src/lib/taskRegistry.tsx).
    Ctrl/⌘+K باز/بسته، Esc بستن، ↑/↓ پیمایش، Enter انتخاب؛ mount در شاخه‌ی topnav با گیتِ `theme.content==='guided'`. استایلِ `.cmdk-*` توکن‌محور.
    مستقر — `index-Cw59YvlU.js`.
  - **موبایلِ پوسته‌ی جدید (فقط فرانت):** روی ≤۷۶۰، کفِ clamp افقیِ `.app-content` (۳۲px) گاترِ خاکستریِ زیادی می‌ساخت و کارت‌ها کوچک
    می‌شدند؛ برای موبایل padding افقی ۱۰px، paddingِ درونِ پنل‌ها ۱۶px، و `scrollbar-gutter: auto` — کارت تقریباً تمامِ عرض را می‌گیرد
    (گاترِ ۱۰px، تأییدِ Playwright ۳۹۰px). اسکوپِ tipalti+مدیا. مستقر — `index-D0CR_H36.css`.
  - **ماندگاریِ پیش‌نویسِ نیمه‌کاره (فقط فرانت/وب):** [usePersistentState](desktop/src/lib/usePersistentState.ts) — مثلِ useState اما مقدار را
    در localStorage نگه می‌دارد (فقط وب؛ Electron دست‌نخورده، مثلِ session). روی فرم‌های بلند اعمال شد: **سند حسابداری، پیش‌فاکتور،
    فاکتور فروش، فاکتور خرید** (ورودی‌های کاربر؛ نه دیتای سرور/پیام). با ثبتِ موفق، resetِ خودِ هوک خالی را ذخیره می‌کند (پاک‌سازیِ خودکار).
    فرم‌های دارای «ویرایش/رونوشت» با فلگِ `disabled=!!editing/!!prefill` ماندگاری‌شان خاموش می‌شود تا دیتای آن حالت آلوده نشود.
    typecheck app+electron ✓، boot-smoke ✓. مستقر — `index-CaSxlwAu.js`.
  - **فازِ بعد:** لایه‌ی تاییدِ چندنقشی (گردش‌کار) — نیازمندِ کارِ بک‌اند (وضعیت/نقشِ تأیید، مهاجرتِ DB)، پس فقط‌فرانت نیست و باید جدا برنامه‌ریزی شود.

- **۱۴۰۵/۰۵/۲۴ (2026-08-14) — سیستمِ «پوسته/تم» + تمِ Tipalti (سرمه‌ای/طلایی) با چیدمانِ افقیِ Xero (فقط فرانت):**
  - **ایده:** یک بخشِ «ظاهر و پوسته» که بشود تم‌های تازه اضافه کرد؛ اولین تمِ تازه ظاهرِ رقیب را می‌سازد: **پالتِ Tipalti**
    (نوارِ سرمه‌ای #16223d + دکمه‌های طلاییِ #ffc72c با متنِ تیره، محتوای روشن) روی **چیدمانِ افقیِ Xero** (نوارِ منوی افقیِ بالا
    با دراپ‌داون، بدونِ نوارِ کناری). فارسی/RTL با ماژول‌های خودمان. **انتخابی** — پیش‌فرضِ VSCodeِ تیره دست‌نخورده.
  - **رجیستریِ تم ([lib/theme.ts](desktop/src/lib/theme.ts)):** تم از یک صفتِ ساده به `ThemeDef` (id/label/kind/**shell**/swatches)
    ارتقا یافت؛ `THEMES` رجیستریِ سه‌تایی (dark/light/tipalti). افزودنِ تمِ تازه = یک ردیف اینجا + یک بلوکِ `:root[data-theme='id']`.
    حالتِ تم **سراسری/مشترک** با `useSyncExternalStore` تا تعویضِ تم، **چیدمان را زنده** عوض کند (نه فقط رنگ).
  - **توکن‌ها ([index.css](desktop/src/index.css)):** بلوکِ `tipalti` (کلِ توکن‌های موجود + `--topnav-*` تازه). چون هیچ
    کامپوننتی رنگِ ثابت ندارد، همین بلوک کلِ برنامه را تمی می‌کند.
  - **چیدمان:** مدلِ ناوبریِ مشترک به [lib/navModel.tsx](desktop/src/lib/navModel.tsx) منتقل شد (Sidebar و TopNav هر دو از
    `buildNav` می‌خوانند). [TopNav.tsx](desktop/src/components/TopNav.tsx) تازه: نوارِ افقیِ سرمه‌ای با دراپ‌داونِ گروه‌ها،
    جست‌وجوی سریعِ ماژول، سوییچرِ کسب‌وکار، منوی کاربر، و **کشوی همبرگریِ موبایل** (≤۱۰۲۴px). [Dashboard.tsx](desktop/src/components/Dashboard.tsx)
    بر اساسِ `theme.shell` بینِ نوارِ کناری و افقی شاخه می‌زند؛ سویچِ رندرِ صفحه مشترک ماند.
  - **بخشِ «ظاهر و پوسته»:** [ThemeGallery.tsx](desktop/src/components/ThemeGallery.tsx) — کارت‌های تم با پیش‌نمایشِ کوچکِ
    رنگ+چیدمان؛ صفحه‌ی `theme` (nav ثانویه) و از منوی کاربرِ TopNav.
  - **راستی‌آزمایی:** تایپ‌چکِ app+electron ✓، build ✓، لینت تمیز؛ اسکرین‌شاتِ Playwright (دسکتاپ: نوارِ سرمه‌ای + دراپ‌داونِ
    باز + دکمه‌های طلایی؛ موبایلِ ۳۹۰px: کشوی همبرگری). پیش‌فرضِ تیره/سایدبار دست‌نخورده. باندلِ `index-CA3HKH89.js`.
  - **دنباله (همان روز) — بازطراحیِ «محتوای صفحه» به سبکِ Xero/Tipalti (فقط فرانت):** فقط رنگ‌ونوار کافی نبود؛ درونِ صفحه‌ها
    هم باید مثلِ اسکرین‌شات‌های رقیب «هوادار» شود. چون همه‌چیز توکن‌محور است، بلوکِ `:root[data-theme='tipalti']` در
    [index.css](desktop/src/index.css) حالا **مقیاسِ فونت/فاصله/ارتفاعِ کنترل** را هم بازتعریف می‌کند (base 13→14px, lh 1.6,
    control-h 36px, تیترها درشت‌تر) — چون `font:` روی همان ریشه با `var(--fs-base)` بازمحاسبه می‌شود، کلِ متنِ برنامه یک‌جا بزرگ
    می‌شود. + بلوکِ اسکوپ‌شده در [App.css](desktop/src/App.css) برای صیقلِ ساختاری: سرصفحه‌ی درشتِ Xero با آیکونِ آرامِ طلایی‌تینت
    (نه گرادیانِ رنگین‌کمانی)، کارت‌های سایه‌محورِ گوشه‌نرمِ شناور، جدول‌های ردیف‌بلندِ روشن، KPIهای عددِدرشت، و **تب‌های زیرخط‌دارِ
    Xero** (به‌جای کپسول‌های جعبه‌ای؛ فقط دسکتاپ — موبایل کپسولِ گریدی می‌ماند). همه با پیشوندِ تم اسکوپ‌اند؛ پوسته‌های پیش‌فرض
    (تیره/روشن) با اسکرین‌شات تأیید شد که **بایت‌به‌بایت دست‌نخورده** ماندند (تب‌های پیل، تراکمِ ۱۳px، آبیِ VSCode). Playwright
    روی صفحه‌ی نمونه‌ی ماژول (سرصفحه + KPI + تب + جدول + فرم) در دسکتاپ ۱۲۸۰ و موبایل ۳۹۰px بدونِ سرریزِ افقی.
  - **رفعِ عرضِ محتوا (پیرو بازخوردِ زنده):** در چیدمانِ topnav سایدبار حذف شده ولی `.page` هنوز `max-width`ِ چیدمانِ سایدباری
    (`clamp(1160,82vw,1760)`) را داشت، پس محتوا باریک می‌ماند و به‌اندازه‌ی جای سایدبارِ قبلی کنارش خالی می‌شد (کاربر: «کارت‌ها
    رفتن سمت راست و جای منوی قبلی سمت چپ خالی مونده»). دو قاعده‌ی اسکوپ‌شده به **شِل** (نه تم): (۱) `.app-shell--topnav
    .app-content > .page { max-width: none }` تا محتوا کلِ عرضِ آزادشده را پر کند و کارت‌ها بزرگ‌تر شوند؛ (۲) `scrollbar-gutter:
    stable both-edges` روی `.app-content`ِ topnav تا اسکرول‌بارِ عمودیِ RTL (که سمتِ چپ می‌آید) فاصله‌ی دو طرف را نامتوازن نکند.
    تستِ ۱۹۱۲px: عرضِ صفحه ۱۷۷۲px، حاشیه‌ی چپ/راست ۷۰/۷۰. باندلِ `index-CTfOx7R-.js` + `index-Dtota8w7.css`.
  - **استقرار (فقط‌فرانت):** کلِ این batch (بازطراحیِ محتوا + تمام‌عرض) با سواپِ دستیِ وب روی `acc.cubita.ir` مستقر و به
    `demo.cubita.ir` آینه شد؛ بدونِ قطعی، بدونِ لمسِ بک‌اند/دیتابیس. تأییدِ زنده: هر دو سایت `index-Dtota8w7.css` را می‌دهند و
    CSSِ زنده شاملِ `max-width:none` و `scrollbar-gutter` است. (رگرسیون: `web.old` روی سرور نقطه‌ی بازگشت.)
  - **حذفِ toggleِ سریعِ روز/شب + بزرگ‌کردنِ همبرگرِ موبایل:** چون بخشِ «ظاهر و پوسته» هر سه تم را می‌دهد، دکمه‌های سریعِ
    تیره/روشن از فوترِ [Sidebar.tsx](desktop/src/components/Sidebar.tsx) و منوی کاربرِ [TopNav.tsx](desktop/src/components/TopNav.tsx)
    حذف شدند (importهای `useTheme`/`Sun`/`Moon` هم پاک شد). **هیچ تمی از رجیستری حذف نشد** — dark/light/tipalti دست‌نخورده و
    «ظاهر و پوسته» تنها مسیرِ تعویض شد. همبرگرِ TopNav از ۳۸→**۴۴px** (هدفِ لمسیِ استاندارد) + آیکونِ ۲۰→۲۴. مستقر روی prod+demo،
    تأییدِ زنده: برچسب‌های toggle از JS رفت، هر سه تم مانده، همبرگر ۴۴px. باندلِ `index-CTkX9q6t.js` + `index-6zXlyWMY.css`.

- **۱۴۰۵/۰۵/۲۶ (2026-08-16) — گفتگوی زیرِ هر سفارشِ بازار + نشانِ خوانده‌نشده روی منو (مهاجرت `0066`):**
  - **ایده:** علاوه بر رشته‌ی کلیِ اتصال، حالا هر **سفارش** رشته‌ی گفتگوی جداگانه‌ی خودش را دارد (برای هماهنگیِ دقیقِ همان سفارش). و یک **نشانِ خوانده‌نشده** روی منوی «بازارِ خرید»/«پخشِ من» رسیدنِ پیامِ تازه را هرجای برنامه نشان می‌دهد.
  - **مدلِ داده:** `marketplace_messages.order_id` (FK→سفارش، CASCADE) افزوده و `connection_id` تهی‌پذیر شد؛ `CheckConstraint`ِ `num_nonnulls(connection_id, order_id)=1` تضمین می‌کند هر پیام دقیقاً به یک رشته تعلق دارد. دو ستونِ `distributor/retailer_last_read_at` روی `marketplace_orders` برای شمارشِ خوانده‌نشده‌ی رشته‌ی همان سفارش.
  - **بک‌اند:** `load_order_for_member` (بی‌گیتِ وضعیت — طرفین در هر وضعیتی حرف می‌زنند) + `list/post_order_message`، `mark_order_read`، `order_unread_count`؛ `order_dict` حالا `unread_count`/`last_message_*`ِ سمتِ بیننده را می‌دهد؛ `total_unread` (نشانِ منو) هم رشته‌های اتصال و هم رشته‌های سفارش را جمع می‌زند. اندپوینت‌ها: `GET/POST /api/marketplace/orders/{id}/messages`.
  - **فرانت:** [MarketplaceChatDrawer](desktop/src/components/MarketplaceChatDrawer.tsx) به **callback-محور** بازطراحی شد (`threadKey`/`loadMessages`/`sendMessage`) تا با یک کامپوننت هم رشته‌ی اتصال و هم رشته‌ی سفارش کار کند. دکمه‌ی «گفتگو» + نشانِ خوانده‌نشده روی هر ردیفِ سفارش در [DistributorPage](desktop/src/pages/DistributorPage.tsx) و [MarketplacePage](desktop/src/pages/MarketplacePage.tsx). نشانِ منو در [TopNav](desktop/src/components/TopNav.tsx) با پولِ ~۲۵ ثانیه‌ایِ `GET /unread` در [Dashboard](desktop/src/components/Dashboard.tsx).
  - **راستی‌آزمایی:** ۵۷ تستِ بازار (۵ تازه برای گفتگوی سفارش: ارسال/فهرست، جداسازیِ ۴۰۴، بی‌گیتِ وضعیت، خوانده‌نشده در فهرستِ سفارش، جمعِ `/unread`) + drift سبز؛ اجرای زنده‌ی محلیِ دوطرفه: نشانِ منو «۲» (اتصال+سفارش)، نشانِ ردیفِ سفارش «۱»، بازکردنِ کشو و ارسالِ پاسخ.
  - **استقرار:** بک‌اند روی prod (diffِ ایمنی → `alembic 0065→0066` → ری‌استارت) + باندلِ وبِ `index-Bfb7JEHN.js` روی `acc.cubita.ir` + آینه‌ی `demo.cubita.ir`؛ health ok، اندپوینت‌ها زنده (۴۰۱)، هر دو سایت ۲۰۰.

- **۱۴۰۵/۰۵/۲۵ (2026-08-15) — گفتگوی زنده‌ی متنی بین فروشگاه و پخش‌کننده در بازار (مهاجرت `0065`):**
  - **ایده:** روی هر اتصالِ **approvedِ** بازار یک **رشته‌ی گفتگوی متنیِ دائم** بین دو کسب‌وکار — برای هماهنگیِ موجودی/قیمت/ارسال/سؤالِ سفارش.
  - **مدلِ داده:** جدولِ تازه‌ی `marketplace_messages` (**سراسری/بدونِ RLS** مثلِ بقیه‌ی جدول‌های بازار؛ جداسازی در روتر با `load_connection_for_member`
    که tenantِ فراخوان را با یکی از دو سمتِ اتصال تطبیق می‌دهد وگرنه ۴۰۴، و فقط `approved` را می‌پذیرد وگرنه ۴۰۹) + دو ستونِ
    `distributor_last_read_at`/`retailer_last_read_at` روی `marketplace_connections` برای شمارشِ خوانده‌نشده.
  - **API:** `GET/POST /api/marketplace/connections/{id}/messages` (هر دو نقش)، `GET /api/marketplace/unread`؛ `connection_dict` حالا
    `unread_count`/`last_message_*`ِ سمتِ بیننده را هم می‌دهد. باز/پول‌کردنِ رشته آن را «خوانده» می‌کند.
  - **فرانت (وب+دسکتاپ):** [MarketplaceChatDrawer](desktop/src/components/MarketplaceChatDrawer.tsx) — کشوی RTL با حباب‌های تفکیک‌شده،
    **پولینگِ ~۴ ثانیه** (نه WebSocket — مناسبِ VPS تک‌هسته/۲گیگ)، composer (Enter=ارسال). دکمه‌ی «گفتگو» + نشانِ خوانده‌نشده روی ردیف‌های
    اتصالِ approved در [DistributorPage](desktop/src/pages/DistributorPage.tsx) و [MarketplacePage](desktop/src/pages/MarketplacePage.tsx). موبایل: کشو تمام‌صفحه. توکن‌محور، هر سه تم.
  - **راستی‌آزمایی:** ۵۲ تستِ بازار (۷ تازه) + drift سبز؛ اجرای زنده‌ی محلیِ دوطرفه (پخش‌کننده↔فروشگاه) با تحویلِ پولینگ و نشانِ خوانده‌نشده و موبایلِ ۳۹۰px.
  - **استقرار:** بک‌اند روی prod (کپیِ فایل‌ها + `alembic 0064→0065` + ری‌استارت؛ diffِ ایمنیِ پیش از کپی) + سواپِ باندلِ وبِ `acc.cubita.ir` + آینه‌ی `demo.cubita.ir`.
    اندپوینت‌ها زنده (۴۰۱ بدونِ توکن)، health ok، هر سه سایت ۲۰۰. باندلِ `index-CctQo8S2.js`.

- **۱۴۰۵/۰۵/۲۴ (2026-08-14) — اتصالِ سیستمِ حسابداری به دستگاهِ کارتخوان (POS Terminal) (مهاجرت `0064`):**
  - **ایده:** فروشگاه‌دار با یک دکمه مبلغِ فاکتور را به دستگاهِ کارتخوان می‌فرستد؛ مشتری کارت می‌کشد؛ نتیجه **خودکار به‌صورتِ رسیدِ بانکی**
    در حسابداری می‌نشیند (با شماره‌ی مرجع/RRN برای مغایرت‌گیری). دکمه در **سه جا**: صندوقِ فروشگاهی، فرمِ فاکتورِ فروش، و «دریافت وجه».
  - **مدلِ حسابداری:** پرداختِ کارتی = **رسیدِ بانکی** (`method="bank"` به معینِ حسابِ تسویه‌ی همان ترمینال). فاکتور/دریافتِ طرف‌حساب‌دار
    مانده‌ی دریافتنی را تسویه می‌کند؛ فروشِ **گذری** (بی‌طرف‌حساب) علیهِ طرف‌حسابِ سیستمیِ «فروشِ کارتیِ گذری» (`contacts.is_system`) ثبت
    و بلافاصله با رسیدِ کارت خنثی می‌شود → اثرِ خالص: بدهکار بانک/بستانکار درآمد+مالیات. **idempotent روی RRN** (رسیدِ دوم ساخته نمی‌شود).
  - **بک‌اند:** ۶ ستونِ متادیتای کارت روی `treasury_transactions` + ایندکسِ یکتای جزئیِ RRN؛ جدولِ `pos_terminals` (RLS)؛ `treasury.record_card_payment`
    + گسترشِ `create_receipt`؛ endpointهای `/api/treasury/card-payment` (گیت با `invoices/create` تا صندوق‌دار هم بتواند) و CRUDِ `/api/pos-terminals`.
  - **معماریِ سخت‌افزار (فقط دسکتاپ):** لایه‌ی درایورِ قابل‌افزونه در الکترون (`electron/pos/` — رابط `PosTerminalDriver`، درایورهای
    **شبیه‌ساز** (کاملِ کارکردی برای دمو/تست)، **تحت‌شبکه TCP** (اسکلت: تستِ اتصالِ واقعی + پرداختِ فعلاً رد با پیامِ روشن)، و استاب‌های سریال/SDK)؛
    IPCهای `pos:pay`/`pos:status` و پلِ `window.cubita.posTerminal` در preload. مرورگر (وب) به سخت‌افزار وصل نمی‌شود، پس دکمه با feature-detect
    پیامِ «فقط دسکتاپ» می‌دهد؛ ولی **پیکربندیِ ترمینال‌ها و ثبتِ حسابداری سرور-ساید است و در هر دو نسخه دیده می‌شود**.
  - **فرانت:** پنلِ «کارتخوان‌ها» (تبِ تازه در «چک و بانک») برای CRUD + «تستِ اتصال»؛ مودالِ `CardPaymentDialog` (انتخابِ دستگاه → «کارت را بکشید» →
    ثبت) با آگاهیِ وب/دسکتاپ؛ دکمه در POS/فاکتور/دریافت. نمای موبایل (۳۹۰px) با هارنسِ Playwright تأیید شد. تایپ‌ها در `api.ts`/`electron.d.ts`.
  - **راستی‌آزمایی:** ۱۳ تستِ نوِ بک‌اند (رسیدِ بانکی، فروشِ گذری با خنثی‌شدنِ دریافتنی، تسویه‌ی طرف‌حساب، idempotencyِ RRN، متادیتا،
    پنهان‌بودنِ طرف‌حسابِ سیستمی، CRUD + یکتاییِ پیش‌فرض، اعتبارسنجی) — کلِ بک‌اند **۱۰۹۱ سبز**؛ + اسموک‌تستِ درایورهای الکترون.

- **۱۴۰۵/۰۵/۲۴ (2026-08-14) — رفعِ بیرون‌زدگیِ کلیدِ مبلغ/درصدِ تخفیفِ کلِ فاکتور (CSS، فقط فرانت):**
  - **مشکل:** در فرمِ فاکتور (فروش/خرید/پیش‌فاکتور)، وقتی عرضِ ناحیه تنگ می‌شد، `.seg-toggle`ِ کنارِ «تخفیف کل فاکتور» در ردیفِ `.qty-with-unit`
    له/بریده می‌شد و از کادر بیرون می‌زد؛ دکمه‌ی دوم («٪») دیده نمی‌شد. با Playwright روی عرض‌های ۲۱۰/۲۶۰/۳۴۰px بازتولید و تأیید شد.
  - **اصلاح:** `.seg-toggle { flex-shrink: 0 }` (کلید هیچ‌جا له/بریده نشود) + `.adj-field .qty-with-unit { flex-wrap: wrap }` (اگر جا کم بود کلید
    به خطِ بعد برود). ظاهرِ عرض‌های معمولی بی‌تغییر ماند. استقرار فقط‌فرانت؛ باندلِ `index-5JjP0ImK.js`، health ok.

- **۱۴۰۵/۰۵/۲۴ (2026-08-14) — رفعِ بریدگی/بیرون‌زدگیِ متنِ راهنمای ورودی‌ها (CSSِ سراسری، فقط فرانت):**
  - **مشکل:** جمله‌های راهنمای (placeholder) بلند در فیلدهای «برچسب-بالای-ورودی» که در `.invoice-form` نبودند، عرضِ ذاتیِ ~۲۰ کاراکترِ مرورگر را
    می‌گرفتند و متن روی لبه بریده/ناخوانا می‌شد.
  - **اصلاح (در [index.css](desktop/src/index.css)):** (۱) `text-overflow: ellipsis` سراسری روی input/select/textarea تا سرریز با «…» داخلِ کادر تمام شود.
    (۲) با `:has()`، هر `label` که **مستقیماً** یک ورودیِ متنی/انتخابگر/تکست‌اریا دارد ستونی و ورودی **تمام‌عرض** می‌شود تا راهنما کامل جا شود.
    استثناها امن‌اند: چک‌باکس/رادیو/رنج/رنگ/تاریخ، عرض‌های inline (NumberInputِ باریک با style حفظ می‌شود)، و فیلترهای بی‌برچسبِ نوارابزار.
  - **راستی‌آزمایی:** با Playwright روی فرمِ برچسب‌ساده + نوارابزار + چک‌باکس + عرضِ‌ثابت + بازه‌ی تاریخ (JalaliDatePicker) در تمِ روشن/تیره — فیلدها
    تمام‌عرض شدند و بقیه دست‌نخورده ماندند. استقرار فقط‌فرانت (swapِ وب + آینه به demo)؛ باندلِ `index-BEH-1fxI.js`، health ok.

- **۱۴۰۵/۰۵/۲۴ (2026-08-14) — کاتالوگِ «بازارِ خرید» به‌شکلِ کارت‌های محصولِ فروشگاهِ اینترنتی (فقط فرانت):**
  - **چه:** جدولِ کاتالوگِ سمتِ فروشگاه (`MarketplacePage`) به **گریدِ کارت‌های محصول** تبدیل شد (مثلِ سایتِ فروشگاهِ اینترنتی): عکسِ بزرگِ
    مربعی، بَجِ «پک» و بَجِ دسته روی عکس، عنوانِ دو‌خطی، نامِ پخش‌کننده، محدودیت‌ها، قیمتِ درشت، و دکمه‌ی «افزودن به سبد» که با افزودن به
    **استپرِ تعداد (− عدد +)** تبدیل می‌شود.
  - **چیدمان:** `.workspace-split.shop` ستون‌ها را برعکسِ حالتِ عادی می‌کند تا **کاتالوگ ستونِ پهن** و **سبد سایدبارِ باریک** شود؛ کاتالوگ در DOM
    اول می‌ماند تا روی موبایل محصولات بالاتر از سبد بیایند. گرید `auto-fill minmax(185px, 1fr)`. کارت‌ها hover-lift دارند و در تمِ روشن/تیره کار می‌کنند.
  - **بدونِ تغییرِ بک‌اند** (همان دادهٔ `fetchMpCatalog`)؛ منطقِ سبد (`addToCart/setQty/removeLine`) بازاستفاده شد. تست‌شده در موبایل (۴۰۰px).
  - **استقرار:** فقط‌فرانت (بدونِ قطعی) — swapِ وب + آینه به demo؛ باندلِ `index-C_MV0uuR.js`، health ok.

- **۱۴۰۵/۰۵/۲۴ (2026-08-14) — بازار: تقسیمِ نقد/اعتباری هنگام تأیید + محدودیتِ سفارش‌گذاریِ لیستینگ (مهاجرت `0063`):**
  - **تقسیمِ نقد/اعتباری:** پخش‌کننده موقعِ **تأییدِ سفارش** درصدِ نقد (۰..۱۰۰) را تعیین می‌کند؛ سهمِ نقد **همان لحظه در خزانه‌ی هر دو طرف**
    سند می‌خورد (دریافتِ نقدِ پخش‌کننده + پرداختِ نقدِ فروشگاه، با `treasury.create_receipt/payment`)، بقیه به‌صورتِ طلب/بدهی (اعتباری) می‌ماند.
    سرویس: `confirm_order(cash_percent)` → پس از `_fulfill`، `_settle_cash` (مبلغِ جزئی، الگوی تسویه‌ی آنلاین). ستونِ `MarketplaceOrder.cash_amount`.
    اندپوینت: بدنه‌ی اختیاری `OrderConfirmIn{cash_percent}`؛ نبودنش = ۰٪ (کاملاً اعتباری، رفتارِ قبلی). فرانت: **دیالوگِ تأیید** با اسلایدرِ درصد و
    پیش‌نمایشِ زنده‌ی نقد/اعتباری؛ در جزئیاتِ سفارشِ تأییدشده هم نقد/اعتباری نشان داده می‌شود.
  - **محدودیتِ سفارش‌گذاری (per listing):** سه ستونِ نو روی `marketplace_listings` (۰ = بی‌حد): `min_order_qty`/`max_order_qty` (کف/سقفِ هر سفارش)
    و `daily_order_limit` (**سقفِ دفعاتِ سفارش در روز، هر فروشگاه**). اعمال در `place_order` (کف/سقف روی qty؛ شمارشِ سفارش‌های امروزِ فروشگاه که
    شاملِ این لیستینگ‌اند با `_orders_today_with_listing` و `current_date`ِ DB). فرم کاتالوگ ورودی‌ها را می‌گیرد؛ کاتالوگِ فروشگاه محدودیت‌ها را نشان می‌دهد.
  - **تست:** ۵ تستِ نو (roundtrip محدودیت‌ها، ردِ کف/سقف، سقفِ روزانه، تقسیمِ نقد→خزانه‌ی دو طرف، اعتباریِ کامل بدونِ سندِ خزانه) — کلِ بک‌اند **۱۰۷۴ سبز**.
  - **استقرار:** `deploy.sh production` (مهاجرت `0062→0063`) + آینه به demo؛ هر دو باندلِ `index-IisFmzGW.js`، health ok، FORCE RLS ۷۵.

- **۱۴۰۵/۰۵/۲۳ (2026-08-13) — بنرِ هشدارِ «غیرفعال در بازار» برای پخش‌کننده (رفعِ تلهٔ UX، فقط فرانت):**
  - **مشکلِ گزارش‌شده:** پخش‌کننده کاتالوگِ منتشرشده (حتی با عکس) داشت ولی فروشگاه او را در «بازارِ خرید» نمی‌دید و در فرمِ درخواستِ اتصال نبود.
  - **ریشه:** کشفِ پخش‌کننده در `list_distributors_for_retailer` فقط پخش‌کننده‌های `MarketplaceSettings.is_active=True` را نشان می‌دهد؛
    پخش‌کننده کاتالوگ ساخته بود ولی سوییچِ «حضور در بازار فعال باشد» را در تبِ تنظیمات روشن نکرده بود — و هیچ نشانه‌ای این را نمی‌گفت (تلهٔ خاموش).
    (نوعِ حساب‌ها و انتشار درست بودند؛ تنها گیت همین `is_active` بود.)
  - **اصلاح:** بنرِ هشدارِ تمام‌عرض در بالای ماژولِ «پخشِ من» وقتی `is_active=false` — با دکمه‌ای که مستقیم به تبِ «تنظیمات» می‌پرد
    (`useNavSection().setSection('settings')`). `SettingsPanel` با `onActiveChange` بنر را زنده به‌روز می‌کند (ذخیره → بنر محو می‌شود).
  - **استقرار:** فقط‌فرانت (بدونِ قطعی) — swapِ وب + آینه به demo؛ باندلِ `index-CbFZCx2N.js`، health ok.

- **۱۴۰۵/۰۵/۲۳ (2026-08-13) — بارکدخوانِ دوربینی برای صندوقِ فروشگاهی و فرمِ کالا (فقط فرانت، بدونِ وابستگی):**
  - **مسئله:** صندوق فقط ورودیِ متنی داشت (بارکدخوانِ USB که تایپ می‌کند)؛ روی موبایل هیچ راهی برای اسکن با دوربین نبود — «دکمه‌ی اسکن» عملاً کاری نمی‌کرد.
  - **راهکار:** کامپوننتِ نو `BarcodeScanner` با API بومیِ مرورگر **`BarcodeDetector`** (بدونِ افزودنِ کتابخانه): `getUserMedia` با دوربینِ عقب،
    حلقه‌ی تشخیص روی `requestAnimationFrame`، لرزش/بوقِ تأیید، و ضدِتکرار (کدِ یکسان تا ۱٫۵ ثانیه دوباره ثبت نمی‌شود). روی **Chromeِ اندروید**
    (وبِ موبایل) و Chromium/الکترون با وب‌کم کار می‌کند؛ جای پشتیبانی‌نشده (آی‌اواس/سافاری، فایرفاکس) پیامِ روشن + ورودِ دستی سرِ جایش می‌ماند.
  - **کجا سیم شد:** دکمه‌ی «دوربین» در نوارِ اسکنِ **صندوق** (`PosPage`؛ حالتِ پیوسته — چند کالا پشت‌سرِهم) و دکمه‌ی دوربین کنارِ فیلدِ **بارکدِ فرمِ
    کالا** (`ProductsPanel`؛ حالتِ `once` — با اولین اسکن بسته و فیلد را پر می‌کند). منطقِ جست‌وجوی کد در `lookupAndAdd` مشترک شد (متن و دوربین).
  - **الکترون:** `setPermissionRequestHandler`/`setPermissionCheckHandler` فقط برای `media` تا دوربین در نسخه‌ی دسکتاپ هم فعال شود.
  - **راستی‌آزمایی:** فرانت‌بیلد تمیز؛ نمای اسکنر (کادرِ نشانه‌گیری، راهنما، حالتِ خطا) و دکمه‌ها در تمِ روشن/تیره و موبایل (۳۹۰px) با Playwright بازبینی شد.
  - **استقرار:** فقط‌فرانت (بدونِ توقفِ سرویس) — swapِ وب روی prod + آینه به demo؛ هر دو باندلِ `index-95MpY4p1.js`، health ok. (تغییرِ `electron/main.ts`
    فقط در سورس است و با نصبِ بعدیِ دسکتاپ می‌آید — طبقِ «نصب‌کننده تا تکمیل نه».)

- **۱۴۰۵/۰۵/۲۳ (2026-08-13) — عکسِ محصول در کاتالوگِ بازار (پخش‌کننده)، فشرده و سقف‌دار (بدونِ مهاجرت):**
  - **ایده:** پخش‌کننده می‌تواند به هر لیستینگِ کاتالوگ **عکس** بیفزاید؛ فروشگاه هم عکس را در کاتالوگِ خرید می‌بیند. عمداً
    **حجم کنترل‌شده** تا فضای پایگاه‌داده باد نکند (ستونِ `images(JSONB)` از قبل روی `marketplace_listings` بود؛ فقط سیم‌کشی و گارد لازم بود).
  - **کجا ذخیره:** به‌صورتِ **data URIِ JPEGِ فشرده** در همان `images(JSONB)` (نه فایلِ روی دیسک) — برای هر دو استقرارِ ابری (Postgres)
    و اپِ استندالون (SQLite) یکدست، بدونِ نیاز به سرو‌کردنِ فایلِ ایستا یا پاک‌سازیِ دستی.
  - **کنترلِ حجم (دو لایه):** (۱) **کلاینت**: `desktop/src/lib/imageCompress.ts` عکس را روی canvas به بلندترین‌ضلعِ ۱۰۲۴px کوچک و با
    کیفیتِ JPEG ~۰٫۷۲ (پله‌ای پایین‌تر تا زیرِ سقف) رمزگذاری می‌کند (~۴۰–۱۵۰KB). (۲) **سرور**: گاردِ `ListingIn.images` در
    `schemas/marketplace.py` — فقط data URIِ JPEG/PNG/WebP، حداکثر **۴ عکس**، هر عکس ≤ **۴۰۰KB** (رمزگشایی‌شده)؛ وگرنه ۴۲۲.
  - **فرانت:** کامپوننتِ نو `ImageUploader` (پیش‌نمایش + حذف + فشرده‌سازیِ خودکار) در فرمِ کاتالوگِ «پخشِ من»؛ تصویرِ کوچک (`list-thumb`)
    کنارِ نامِ محصول در جدولِ کاتالوگِ **هر دو** سمت (پخش‌کننده و «بازارِ خرید») و در **جزئیاتِ سفارش** (نمای بازشده‌ی هر سفارش، دو سمت).
    عکسِ ردیفِ سفارش snapshot نمی‌شود بلکه در `order_dict` از روی `listing_id` خوانده می‌شود (`OrderLineOut.image`؛ اگر لیستینگ حذف شده باشد None).
    تست‌شده در تمِ روشن/تیره و موبایل (≤۷۶۰px).
  - **تست:** ۵ تستِ نو (عکسِ کوچکِ معتبر + رفت‌وبرگشت، ردِ >۴ عکس، ردِ غیرِ‌data-URI، ردِ عکسِ >۴۰۰KB، عکسِ ردیفِ سفارش از روی listing) — کلِ بک‌اند **۱۰۶۹ سبز**.
  - **استقرار:** `deploy.sh production` (نسخه ۰۶۲؛ مهاجرت no-op) + آینه به demo؛ هر دو باندلِ `index-npa0svRs.js`، health ok.

- **۱۴۰۵/۰۵/۲۳ (2026-08-13) — کمیسیونِ ۲٪ِ پلتفرم روی بازارِ عمده‌فروشی (مهاجرت `0062`):**
  - **ایده:** هر سفارشِ بازار که پخش‌کننده **قطعی/ارسال** می‌کند، **۲٪ از جمعِ فاکتور** کمیسیونِ پلتفرم (کوبیتا) است که پخش‌کننده
    **ماهانه به حسابِ سوپرادمین** (`acc.cubita@gmail.com`) واریز می‌کند. سازوکار با یک **ماژولِ اختصاصی** شفاف شد.
  - **تصمیم‌های کاربر:** روشِ پرداخت = **واریزِ دستی + تأییدِ سوپرادمین** (نه درگاه)؛ ثبت = **فقط در ماژولِ کمیسیون** (هیچ سندی در دفترِ
    خودِ پخش‌کننده نمی‌خورد). نرخ در config قابلِ تغییر (`MARKETPLACE_COMMISSION_RATE=0.02`).
  - **بک‌اند:** جدولِ سراسریِ `marketplace_commissions` (مهاجرت `0062`، در `GLOBAL_TABLES`، یک رکورد به‌ازای هر سفارش با `order_id`
    یکتا؛ `period` ماهِ شمسی، `base_amount/rate/amount` اسنپ‌شات، `status` pending/settled). ثبت در **`_fulfill`** (تنها نقطه‌ی قطعی‌شدن —
    هم مسیرِ اعتباری، هم آنلاین؛ idempotent). سرویس: `_record_commission` + `commission_summary`/`commission_overview`/
    `settle_commission_period`. روتر: `/api/marketplace/admin/commissions[/overview|/settle]` (پشتِ `require_super_admin`) و
    `/api/marketplace/distributor/commissions` (صورتِ خودِ پخش‌کننده).
  - **فرانت:** صفحه‌ی سوپرادمینِ **«کمیسیونِ بازار»** (نوارِ آمار + «در انتظارِ دریافت» با دکمه‌ی تسویه + «تسویه‌شده‌ها») و تبِ
    **«کمیسیون»** در «پخشِ من» (صورتِ ماهانه‌ی پخش‌کننده). گیت‌شده با `is_super_admin`/`tenant_kind`؛ با تمِ پنل‌های همین بازطراحی.
  - **راستی‌آزمایی:** **۷ تستِ تازه** (ثبتِ ۲٪، idempotency، summary/overview/settle، گاردِ سوپرادمین، ایزولاسیونِ پخش‌کننده)؛ **کلِ بک‌اند
    ۱۰۶۴ سبز**؛ فرانت‌بیلد سبز + اسکرین‌شاتِ صفحه‌ی سوپرادمین. **استقرارِ prod با `deploy.sh production`**: بکاپ، مهاجرت `0061→0062`،
    نسخه 0062، health ok، FORCE RLS ۷۵، آینه به demo؛ اندپوینت‌ها زنده (۴۰۱ بدونِ توکن).

- **۱۴۰۵/۰۵/۲۲ (2026-08-12) — رول‌اوتِ الگوی «تک‌سطحی» به صفحه‌های ماژول (شروع: فروش؛ فقط فرانت):**
  - **الگوی قابلِ‌تکرار:** کلاسِ عمومیِ `panels` روی `.page` در [App.css](desktop/src/App.css) — کلِ صفحه یک سطحِ واحد با
    کادرِ بیرونی؛ `page-header`/`stat-grid`/`tabs`/`section`‌ها پنل‌های جدا با خطِّ ۱px (`--border-strong`). تب‌ها به **سبکِ ادیتورِ
    VSCode** (نوارِ تخت + زیرخطِ آبیِ فعال). **بدونِ `overflow:hidden`** (تا انتخابگرِ کالا/تاریخ بریده نشود) و گوشه‌ها با رُندِ
    اولین/آخرین پنل. نوارِ آمار با **inset box-shadow** خط می‌خورد (نه gap) تا وقتی تعدادِ کارت‌ها ردیف را کامل نمی‌کند (مثلِ ۵ کارتِ فروش)
    فضای خالی رنگِ خط نگیرد.
  - **صفحه‌ی اول:** [SalesPage.tsx](desktop/src/pages/SalesPage.tsx) فقط با افزودنِ `panels` به `className` (`page` → `page panels`).
  - **رول‌اوتِ کامل (batch):** `panels` به **۱۶ صفحه‌ی دیگر** هم اضافه شد (خرید، انبار، حسابداری، چک‌وبانک، اشخاص، CRM، تولید،
    بازار، پخش‌کننده، اقساط، مدیریت‌اکانت‌ها، تقویم، راه‌اندازی، راهنما، پروفایل، کاربران) — همه فقط با یک‌خط `page`→`page panels`.
    **`PosPage` (صندوق فروشگاهی)** هم انجام شد اما با CSSِ اختصاصی: `.pos-toolbar` یک پنل، و `.pos-grid` دو پنلِ کنارِ هم
    (سبدِ خرید | تسویه) با خطِّ ۱px و هم‌ارتفاع (align-items:stretch) و کارت‌های داخلی تخت. **پس هر ۱۸ صفحه** تک‌سطحی شدند (POS تنها استثنای CSS‌دار).
    راستی‌آزمایی: بیلد سبز + اسکرین‌شاتِ Playwrightِ سه حالت (تب‌دار مثلِ فروش، فقط‌سکشن مثلِ اقساط، و POS) تیره/روشن؛
    زنده روی acc + آینه‌ی demo، CSS حاویِ `.page.panels`/`.pos-grid`، health ۲۰۰.
  - **چندستونیِ فرم|فهرست (`.workspace-split`):** با یک قاعده‌ی CSS، هر ۲۷ کاربردِ `.workspace-split` (در ~۱۵ صفحه/پنل: اشخاص،
    کالاها، انبارها، سرفصل‌ها، حساب‌های بانکی، لیست‌های قیمت، …) داخلِ `.panels` به **دو ناحیه‌ی هم‌ارتفاعِ فرم|فهرست با خطِّ ۱px عمودی**
    تبدیل شد (به‌جای کارتِ فرمِ گرادیانیِ شناور)؛ روی موبایل خطِّ افقی. راستی‌آزماییِ Playwright تیره/روشن + زنده.
  - **`.split-2col` (auto-fit، ۴ کاربرد):** هم هماهنگ شد — لبه‌به‌لبه (با `margin-inline` منفی برای خنثیِ paddingِ تب‌پنل) و خطِّ ۱px
    با `gap` که خودش جهتِ درست را می‌گیرد (عمودی وقتی دوستون، افقی وقتی روی‌هم). **پس کلِ الگوهای چندستونیِ برنامه یکدست شدند.**

- **۱۴۰۵/۰۵/۲۲ (2026-08-12) — داشبوردِ «تک‌سطحی» با پنل‌های VSCode (پایلوتِ چیدمانِ شبکه‌ای؛ فقط فرانت، بدونِ مهاجرت):**
  - **چرا:** صفحه‌ها از کارت‌های شناورِ جدا ساخته می‌شدند؛ کاربر «تک‌صفحه با تقسیم‌بندیِ شبکه‌ایِ منظم» خواست. تصمیم‌ها:
    ظاهر = **پنل‌های VSCode** (سطحِ واحد + خطِّ ۱px)، چیدمان = **چندستونیِ هوشمند** (کوچک‌ها کنارِ هم، عریض‌ها full)، شروع = **داشبورد (پایلوت)**.
  - **روش:** به `.page`ِ داشبورد کلاسِ `dash` اضافه شد و در [App.css](desktop/src/App.css) یک بلوکِ **scoped** با تکنیکِ
    **gap-as-hairline** (ظرف به رنگِ `--border`، پنل‌ها به رنگِ `--surface`، فاصله‌ی ۱px = خطِّ تقسیم). کارت‌های آمار و
    `overview-columns` و `dashboard-analytics` بدونِ کادر/سایه/شعاعِ مستقل، سلول‌های یک گریدِ متحد شدند. **فقط داشبورد** تغییر
    کرد (بقیه‌ی صفحه‌ها هنوز کارتی‌اند تا فازِ رول‌اوت). تنها تغییرِ JSX: `className="page"` → `"page dash"` در
    [OverviewPage.tsx](desktop/src/pages/OverviewPage.tsx).
  - **راستی‌آزمایی/استقرار:** فرانت‌بیلد سبز؛ اسکرین‌شاتِ Playwright روی مارک‌آپِ آینه‌ی دقیقِ DOM (تیره/روشن/موبایل) — سطحِ
    واحد با خط‌های ۱px، سلول‌های آمار، ستونِ رویداد|نمودار، و نوارِ فروش تمام‌عرض. **زنده بدونِ قطعی** روی acc + آینه به demo؛
    CSS حاویِ `.page.dash`، health ۲۰۰.
  - **اصلاحِ فالو‌آپ (همان روز):** نمودارِ «روند نقدینگی» از **خطی** به **سطحیِ گرادیانی و ریسپانسیو** تبدیل شد
    ([TrendChart.tsx](desktop/src/components/TrendChart.tsx) با `ResizeObserver` → viewBox=پیکسل، بی‌کشیدگی/بی‌سرریز؛ `overflow:visible`→`hidden`).
    خط‌های تقسیمِ داشبورد پررنگ‌تر شد (`--border`→`--border-strong`) و پنل‌های هر ردیف هم‌ارتفاع (`align-items:stretch`) تا پس‌زمینه‌ی خط زیرِ پنلِ کوتاه‌تر لو نرود.
    بخشِ تحلیلِ فروش هم دوستونی شد: نمودارِ فروش/خرید سمتِ راست (رفعِ حاشیه‌ی خالیِ letterboxِ `xMidYMid meet`)، و «پرفروش‌ترین کالاها»/«بهترین مشتریان» زیرِ هم سمتِ چپ (روی موبایل تک‌ستونی).
  - **باقی‌مانده:** رول‌اوتِ همین الگو به صفحه‌های ماژول (با تگ‌کردنِ بخش‌های باریک/عریضِ هر صفحه).

- **۱۴۰۵/۰۵/۲۲ (2026-08-12) — سیستمِ طراحیِ توکن‌محور، فازِ ۱ (گریدِ ۴pt + مقیاسِ فونت + ارتفاعِ کنترل؛ فقط فرانت، بدونِ مهاجرت):**
  - **چرا:** اندازه‌فونت‌ها ۲۰ مقدارِ بی‌مقیاس بودند (پر از نیم‌پیکسل ۱۲.۵/۱۳.۵/…)، فاصله‌ها خارج‌از‌گرید (۵/۷/۹/۱۳/۱۸)، و
    شعاع‌ها ناهمگون (۱۶ جای هاردکدِ `12px`). هدف: طراحیِ **شبکه‌ای و استانداردِ** اپ‌های روز (Linear/GitHub Primer).
  - **توکن‌ها (در [desktop/src/index.css](desktop/src/index.css)):** گریدِ فاصله ۴pt (`--sp-1..7` = 4/8/12/16/24/32/48)؛ مقیاسِ
    فونتِ صحیح (`--fs-xs..3xl` = 11/12/13/14/16/20/24/30 با `--lh:1.5`, `--lh-tight:1.25`)؛ `--control-h:32px`؛ شعاع `4/6/10` + `--radius-pill`.
    **تراکمِ «فشرده»**: خطِ پایه از `14.5px/1.75` به **`13px/1.5`** رفت (کاربر انتخاب کرد).
  - **اعمال (فازِ ۱ = پایه + اجزای مشترک):** `h1/h2/h3` روی مقیاس؛ `input/select/textarea/button/.btn-file` روی `--control-h:32px`
    (همه هم‌ارتفاع)؛ کارتِ `.page section` → `--radius-lg` + `padding: --sp-5`. **توکنی‌سازیِ سراسریِ فونت** در [App.css](desktop/src/App.css):
    نیم‌پیکسل‌ها رُند شدند (≤۰.۵px) و صحیح‌ها به `--fs-*` نگاشت شدند؛ همه‌ی `border-radius:12px`‌ها → `--radius-lg`. تمپلیت‌های چاپ
    (Payslip/Installments، رنگِ کاغذِ سفید) دست‌نخورده. **باقی‌مانده برای فازِ ۲:** مقادیرِ off-scaleِ ۱۵/۱۷/۱۸/۲۲px و جاروی
    فاصله‌های per-module.
  - **راستی‌آزمایی:** فرانت‌بیلد سبز؛ اسکرین‌شاتِ قبل/بعدِ Playwright (تیره/روشن × دسکتاپ/موبایل) روی مارک‌آپِ واقعی — کارت‌ها روی
    گریدِ `repeat(auto-fill,minmax(200px,1fr))`، کنترل‌ها هم‌ارتفاع، جدول‌ها فشرده‌تر. **استقرارِ زندهِ بدون‌قطعی** روی `acc.cubita.ir`
    (بکاپ + extract-over) و آینه به `demo.cubita.ir`؛ CSS حاویِ `--fs-base`/`--control-h`، health ۲۰۰.

- **۱۴۰۵/۰۵/۲۲ (2026-08-12) — پوسته‌ی VSCode «Dark Modern / Light Modern» ۲۰۲۶ برای کلِ برنامه + حذفِ آیکونِ زیرمنوها (فقط فرانت، بدونِ مهاجرت):**
  - **رنگ‌ها:** دو بلوکِ متغیرِ CSS در [desktop/src/index.css](desktop/src/index.css) بازنویسی شد به پالتِ رسمیِ VSCode:
    **Dark Modern** (Side Bar `#181818`، ادیتور `#1f1f1f`، کارت `#252526`، متن `#cccccc`/تأکید سفید، آبیِ `#0078d4`، لینک `#4daafc`) و
    **Light Modern** (کروم `#f8f8f8`، کارتِ سفید، متن `#3b3b3b`، آبیِ `#005fb8`، مرز `#e5e5e5`). status/سری‌ها از رنگ‌های `charts.*`
    (سبز `#89d185`، قرمز `#f14c4c`، زرد `#cca700`). چون کلِ برنامه از همین متغیرها ساخته می‌شود، **همه‌ی صفحه‌ها/اجزا** خودکار
    پوسته گرفتند. شعاعِ گوشه‌ها کم شد (`4/6/9px`) برای حسِّ مربعیِ VSCode؛ دکمه‌ی اصلی از گرادیانِ آبی-بنفش به **آبیِ تختِ VSCode** با
    hoverِ کمی‌تیره‌تر تغییر کرد. چند رنگِ سختِ باقی‌مانده (دکمه‌ی بستنِ تایتل‌بار، بَجِ ترایال، حلقه‌ی فوکوس، ردیفِ انتخاب‌شده) به متغیر گره خوردند.
  - **منو:** آیکونِ **زیرمنوها (سطح‌سوم `sidebar-subitem`) حذف شد** (متنِ تودرفته‌ی تمیز، مثلِ فایل‌های زیرِ پوشه در Explorer)؛
    آیکونِ **منوهای اصلی (ماژول/گروه) و حالتِ انتخاب** حفظ شد. حالتِ فعال حالا انتخابِ خاکستریِ Explorer است (`#37373d`/`#e8e8e8`).
  - **راستی‌آزمایی:** فرانت‌بیلد سبز؛ چهار اسکرین‌شاتِ Playwright روی مارک‌آپِ واقعیِ نوار (تیره/روشن × دسکتاپ/موبایل) بازبینی شد.
    **استقرارِ زنده:** سواپِ بدون‌قطعیِ باندلِ وب روی `acc.cubita.ir` (`/opt/hesabdari/web`، با بکاپ و حفظِ chunkهای قدیمی برای نشست‌های باز)
    و آینه به درِ ترایالِ `demo.cubita.ir`؛ هر دو ۲۰۰ و CSS حاویِ `#1f1f1f`.

- **۱۴۰۵/۰۵/۲۲ (2026-08-12) — بازارِ B2B عمده‌فروشی (پخش‌کننده ↔ فروشگاه) + منوی سبکِ VSCode (مهاجرت‌های `0058`→`0061`):**
  - **ایده:** بازارِ عمده‌فروشیِ درون‌پلتفرمی. **۲ نوعِ حسابِ انحصاریِ تازه** (فیلدِ `Tenant.kind`: `standard|distributor|retailer`):
    شرکتِ **پخش** کاتالوگ (کالای تکی یا **پکِ چندمحصولی**) منتشر می‌کند؛ **فروشگاه** از پخش‌کننده‌های متصل سفارش می‌دهد و با
    تأیید، کالا **خودکار در انبارش ثبت و تعدادش اضافه** می‌شود، با سندِ حسابداریِ دوطرفه.
  - **چالشِ معماری:** کلِ داده per-tenant با RLSِ اجباری است، پس بازار نمی‌تواند روی `items`ِ RLS‌دار باشد. **راهکار:** ۷ جدولِ
    **سراسری** (بدونِ RLS، در `GLOBAL_TABLES` مثلِ `tenants`) + جداسازیِ **صریح در کدِ روتر** با فیلترِ tenant/نقش/وضعیتِ اتصال
    (تستِ نشتیِ اختصاصی الزامی). نوشتنِ دوطرفه با `tenant_scope(db, tenant_id)` روی همان تراکنش.
  - **بک‌اند (سرویس/روتر/اسکیمای `marketplace`):** M1 پایه (`kind` + مهاجرت `0058`، `tenant_kind` در `MeOut`، ساختِ حسابِ typed در
    سوپرادمین/ثبت‌نام، ۷ جدولِ سراسری مهاجرت `0059`). M2 کاتالوگِ پخش‌کننده (تنظیمات + CRUDِ لیستینگِ تکی/پک با گاردِ مالکیت).
    M3 اتصال‌ها (درخواست/تأیید/رد/بلاک؛ کاتالوگِ فروشگاه فقط از پخش‌کننده‌های `approved`). M4 **سفارش + تأییدِ دوطرفه** (هسته):
    `confirm_order` با دو `tenant_scope` → `post_sales_invoice` سمتِ پخش‌کننده + `post_purchase_invoice` سمتِ فروشگاه؛ **بازکردنِ پک**
    و تسهیمِ قیمت با `_allocate_discount` (قیمتِ واحدِ صحیح + تخفیفِ باقی‌مانده، چون ستون‌ها `Numeric(18,0)`)؛ **لینکِ ضدِتکرارِ کالا**
    (`marketplace_item_links`) و طرف‌حسابِ خودکارِ دو سمت (مهاجرت `0060`)؛ idempotent + قفلِ ردیف. M5 **تسویه‌ی آنلاین** (مهاجرت
    `0061`): پرداخت با **درگاهِ خودِ پخش‌کننده** (بازاستفاده از آداپتورهای زرین‌پال/زیبال/آی‌دی‌پیِ `payment_providers`)، callbackِ
    عمومی → verify → پستِ دوطرفه + **تسویه‌ی خزانه‌ی هر دو طرف** (AR/AP به صفر)؛ idempotent.
  - **فرانت:** ماژولِ **«پخشِ من»** (کاتالوگ/سفارش‌ها/اتصال‌ها/تنظیمات) برای `distributor` و **«بازارِ خرید»** (پخش‌کننده‌ها/کاتالوگِ
    سبد‌دار/سفارش‌های من با پرداختِ آنلاین) برای `retailer` — گیت‌شده با `tenant_kind` در `Sidebar`/`Dashboard`. نوعِ حساب در پنلِ
    «مدیریت اکانت‌ها» تنظیم می‌شود.
  - **منوی سبکِ VSCode (فقط CSS):** نوارِ کناری به Explorerِ VSCode نزدیک شد — twistie در لبه‌ی پیشرو، ردیف‌های جمع‌وجورِ تخت و
    سرتاسری، سکشن‌هدرِ کم‌رنگ، حذفِ «کارتِ» گروه، و خطِّ راهنمای تورفتگی برای هر سطح. `letter-spacing` از سرتیترهای فارسی برداشته شد.
  - **راستی‌آزمایی:** **۳۴ تستِ تازه‌ی بازار** (گیتِ نوع، مالکیت، اتصال، تأییدِ دوطرفه با کسر/افزودنِ انبار، idempotency، تسویه‌ی
    آنلاینِ mock، و **ایزولاسیونِ میان‌مستأجری**)؛ کلِ بک‌اند سبز؛ فرانت‌بیلد سبز؛ مهاجرت‌ها روی DBِ محلی اعمال و زنجیره راستی‌آزمایی شد.

- **۱۴۰۵/۰۵/۲۰ (2026-08-10) — سه فیچرِ انبار + تکمیلِ `sstid` مؤدیان + استقرارِ prod (نسخه `0057`):**
  - **انبار/خرید (کامیت `738b276`):** (۱) **ساختِ کالای جدید از داخلِ فرمِ خرید** — دکمه‌ی «+ ساختِ کالا» در
    انتخاب‌گرِ کالا (پراپِ اختیاریِ `onCreateNew`؛ فروش/صندوق دست‌نخورده) → مودالِ `QuickItemForm` → کالای تازه
    همان‌جا در ردیف انتخاب می‌شود و با ثبتِ فاکتور موجودیِ انبار بالا می‌رود؛ گیتِ فرم شل شد تا حتی بدونِ هیچ کالای
    موجود هم اولین کالا از خرید ساخته شود. (۲) **تبِ مستقلِ «کاردکس»** در انبار (`KardexPanel`، بازاستفاده از
    `fetchKardex`؛ کاردکسِ کشوییِ قبلی هم می‌ماند). (۳) **«دارایی ثابت»** از زیرتبِ حسابداری به ماژولِ مستقل در
    گروهِ «مالی و بانکی» منتقل شد (PageKeyِ `fixedassets` + مسیریابیِ Dashboard).
  - **مؤدیان — `sstid` (مهاجرت `0057`):** تنها گلوگاهِ کدیِ باقی‌مانده بسته شد. `sstid` دیگر از `sku` نمی‌آید؛ از
    `items.tax_stuff_id` (per-item) و اگر نبود از `moadian_settings.default_stuff_id` (پیش‌فرضِ کسب‌وکار). پیش از
    مصرفِ سریال، نبودِ کدِ ۱۳رقمیِ معتبر ارسال را با پیامِ روشن (نامِ همان کالا) متوقف می‌کند. UI: فیلد در فرمِ کالا +
    ساختِ سریعِ خرید + «شناسه‌ی پیش‌فرض» در تنظیماتِ مؤدیان. (رقمِ کنترلیِ `taxid` با Verhoeff از قبل کامل بود؛
    یادداشتِ «placeholder» کهنه بود.)
  - **راستی‌آزمایی:** ۴ تستِ تازه‌ی sstid؛ **۱۰۲۹ تستِ بک‌اند سبز**؛ فرانت‌بیلد (tsc+vite) سبز.
  - **استقرار:** `deploy.sh production` (۰۰۵۶→۰۰۵۷)؛ health ok، `/api/members`→۴۰۱، RLS روی ۷۵ جدول، بکاپِ قبل/بعد
    سالم. راستی‌آزماییِ زنده: `acc.cubita.ir`→۲۰۰ و باندلِ مستقر حاویِ نشانه‌های نو (`item-picker-create`،
    `kardex-picker`، `default_stuff_id`). **demo:** باندلِ acc روی `/opt/cubita-demo/web` کهنه است؛ تازه‌سازیِ امنش
    (بدونِ `deploy.sh demo` تا درِ ترایال نشکند) به‌خاطرِ گاردِ ایمنیِ SSH بلاک شد و به کاربر واگذار شد. کدِ مؤدیان/sstid
    هنوز در گیت کامیت‌نشده است (مثلِ WIPِ قبلیِ مؤدیان).
  - **مانده (کاربر):** واردکردنِ کدهای ۱۳رقمیِ رسمیِ کالا/خدمت (یا «پیش‌فرض») + اعتبارنامه/گواهیِ واقعی برای ارسالِ زنده.

- **۱۴۰۵/۰۵/۱۶ (2026-08-06) — متمرکزکردنِ ورودِ ترایال روی prod + رفعِ رگرسیونِ درِ ترایال (فقط‌وب):**
  - **علت:** دکمه‌ی «۱۴ روز رایگان»ِ سایت به `demo.cubita.ir` می‌رفت و ثبت‌نام در دیتابیسِ جدا (`cubita_demo`) می‌نشست
    که از `acc.cubita.ir` قابلِ لاگین نبود. **بدتر:** سه استقرارِ `deploy.sh demo`ِ همان‌روز (برای مؤدیان) باندلِ
    `/opt/cubita-demo/web` را به‌خودش (demo) برگردانده و «درِ ترایال» را دوباره شکسته بود — دقیقاً همان دامِ مستند در
    [[demo-instance-reseed]] («هرگز `deploy.sh demo` را برای اپ اجرا نکن»).
  - **رفع:** (۱) `App.tsx` حالا با پارامترِ `?signup` مستقیم روی صفحه‌ی ثبت‌نام باز می‌شود (`wantsSignup()`). (۲) همه‌ی
    CTAهای «۱۴ روز رایگان»ِ صفحه‌ی زنده (`ConceptLanding`/`ConceptPricing`) به `https://acc.cubita.ir/?signup` تغییر
    کردند. (۳) درِ ترایالِ `demo.cubita.ir` با باندلِ درست (`VITE_API_URL=acc VITE_SIGNUP_FIRST=true`) به‌صورتِ سواپِ
    دستی روی `/opt/cubita-demo/web` بازگردانده شد.
  - **استقرار (سه سواپِ فقط‌وب، بی‌ری‌استارتِ بک‌اند، با گاردِ صحت):** `acc.cubita.ir/web`، `cubita-demo/web` (به acc، نه
    demo)، و `cubita-marketing/dist`. راستی‌آزماییِ زنده: `acc.cubita.ir/?signup`→۲۰۰، `demo.cubita.ir`→۲۰۰ و باندلش به
    acc، «۱۴ روز رایگان»ِ cubita.ir → `acc.cubita.ir/?signup`.

- **۱۴۰۵/۰۵/۱۶ (2026-08-06) — سامانه مؤدیانِ v2 (پروتکلِ واقعی) + مهاجرت `0056` (بندِ ۴ از [[commercialization-roadmap]]):**
  - **علت:** پیاده‌سازیِ قبلی (مهاجرت `0029`) یک placeholder بود که به مسیرِ کهنه‌ی `/req/api/self-tsp/sync/invoices`
    **بدونِ توکن و بدونِ رمزنگاری** POST می‌کرد — ریشه‌ی خطای HTTP-500 در سندباکس. با سندِ اتصال `RC_TICS.IS_v1.6`
    پروتکلِ کاملِ نسخه‌ی دوم پیاده شد.
  - **پروتکل (`services/moadian.py` بازنویسیِ کامل):** `GET /nonce` → ساختِ **توکنِ JWS** (RS256، هدرِ `x5c`=گواهیِ DER،
    `sigT` UTC، `crit=["sigT"]`؛ payload={nonce, clientId=شناسه‌حافظه}) → `Authorization: Bearer` (یک‌بارمصرف)؛
    `server-information` برای کلیدِ رمزنگاریِ ۴۰۹۶بیتیِ سرور؛ امضای صورتحساب → **JWS**، سپس رمزنگاری → **JWE**
    (`alg=RSA-OAEP-256`, `enc=A256GCM`, AAD=ASCII(BASE64URL(header)))؛ `POST /requestsmanager/api/v2/invoice`؛
    استعلامِ وضعیت با `GET /inquiry-by-reference-id`. همه با `cryptography` (بدونِ وابستگیِ تازه). مسیرِ پایه:
    سندباکس `sandboxrc.tax.gov.ir`، واقعی `tp.tax.gov.ir`.
  - **مدل/مهاجرت `0056`:** فیلدِ تازه‌ی `certificate_pem` روی `moadian_settings` (برای هدرِ `x5c`). راز است — مثلِ
    کلیدِ خصوصی هرگز در پاسخ برنمی‌گردد، فقط `has_certificate`. اندپوینتِ تازه `POST /api/moadian/inquiry/{id}`
    (مجوزِ `view`). UI: تکست‌اریِ «گواهیِ امضا»، هشدارِ «کلید+گواهی هر دو لازم»، و دکمه‌ی «استعلام وضعیت» در تاریخچه.
  - **راستی‌آزمایی:** ۱۷ تستِ انطباقیِ رمزنگاری (امضای JWS با کلیدِ عمومی verify، رفت‌وبرگشتِ JWE، تطبیقِ بخشِ
    تاریخِ `taxid` با نمونه‌ی سند `A1121604C22`)؛ **کلِ سوئیت ۱۰۱۶ سبز**؛ فرانت build سبز.
  - **استقرار:** `deploy.sh production` + `deploy.sh demo` (هر دو به نسخه `0056`؛ دمو ۶ مهاجرت عقب بود و جبران شد).
    راستی‌آزماییِ زنده: health هر دو ok، `/api/moadian/settings`→۴۰۱، اندپوینتِ تازه‌ی inquiry→۴۰۱ (زنده)،
    باندلِ درست روی هر دو دامنه، بکاپِ قبل/بعد سالم.
  - **رفعِ فوری (همان روز، بعد از تستِ کاربر با گواهیِ واقعیِ ParsSign):** گواهیِ مراکز میانیِ ایرانی گاهی
    فیلدهایی مثل `CN=Omid Arash [Sign]` را با کاراکترهای خارج از مجموعه‌ی مجازِ **PrintableString** (`[`/`]`)
    در همان نوع کد می‌کنند؛ OpenSSL می‌پذیرد ولی `cryptography` (asn1 راست) با `InvalidValue…PrintableString`
    ردش می‌کرد و گواهیِ سالم «نامعتبر» اعلام می‌شد. چون فقط بایت‌های DER برای هدرِ `x5c` لازم است، `_load_certificate`
    (پارسِ x509) با `_cert_der_from_pem`/`_cert_x5c` جایگزین شد که DER را **مستقیم از بدنه‌ی Base64ِ PEM** می‌گیرد
    (بدونِ پارسِ ASN.1؛ فقط کنترلِ تگِ `0x30`). ۳ تستِ رگرسیون (پذیرشِ PrintableStringِ نامنطبق، تطابقِ x5c با DER،
    ردِ ورودیِ خالی/خراب)؛ **۱۰۱۹ کل سبز**. مستقرِ مجدد روی prod+demo (کدِ فقط‌بک‌اند، بی‌مهاجرت).
  - **رفعِ دومِ همان‌روز (تشخیص از دیتابیسِ زنده):** ارسال باز هم «گواهی معتبر نیست» می‌داد. بازرسیِ فقط‌خواندنیِ
    `moadian_settings` روی prod نشان داد گواهی درست و **تک‌نسخه** ذخیره شده ولی خطِ پایانی یک تیره کم دارد
    (`-----END CERTIFICATE----` با ۴ تیره) — کاربر موقعِ کپی از چت آخرین `-` را انداخته بود. رجکسِ استخراج نسبت
    به تعدادِ تیره **رواگیر** شد (`BEGIN CERTIFICATE-*(.+?)-*END CERTIFICATE`) چون فقط بدنه‌ی Base64 مهم است؛
    گواهیِ ازقبل‌ذخیره‌شده بدونِ واردکردنِ مجدد پذیرفته شد. +۱ تستِ رگرسیون، **۱۰۲۰ کل سبز**، مستقرِ مجدد prod+demo.
    درسِ عملیاتی: دسترسیِ فقط‌خواندنیِ DBِ prod (`sudo -u postgres psql`، بایپسِ RLS) برای دیباگِ داده‌ی زنده کلیدی بود.
  - **رفعِ سومِ همان‌روز + فیچرِ «تستِ اتصال»:** با گواهیِ سالم، ارسال به سندباکس رسید ولی
    `GET server-information` → **HTTP 401**. probeِ سندباکس از VPS نشان داد `nonce` عمومی است (۲۰۰) ولی
    `server-information` توکنِ معتبر می‌خواهد و توکنِ ما (شناسه‌حافظه A44DOG) را رد می‌کند → یعنی A44DOG در محیطِ
    **سندباکس ثبت نیست** (سندباکس ثبت‌نامِ جدا دارد؛ اعتبارنامه‌ی production در آن کار نمی‌کند). دو بهبود:
    (۱) پیامِ خطای ۴۰۱/۴۰۳ خوانا شد (`_auth_failure_hint` — به‌جای `HTTPStatusError`ِ خام، راهنماییِ محیط/شناسه‌حافظه/گواهی).
    (۲) فیچرِ تازه **`POST /api/moadian/test-connection`** + دکمه‌ی «تستِ اتصال» در پنل: فقط `nonce→توکن→server-information`
    را می‌زند (GETِ فقط‌خواندنی، **بی‌مصرفِ سریال/بی‌ارسالِ فاکتور**)، پس حتی روی محیطِ واقعی هم بی‌خطر است و
    اعتبارنامه/محیط را قبل از ارسالِ واقعی می‌سنجد. +۴ تستِ رگرسیون، **۱۰۲۴ کل سبز**، مستقرِ prod+demo (کد+وب).
    **بلوکِ باقی‌مانده (کاربر):** یا ثبت‌نامِ سندباکس، یا تیکِ سندباکس را بردار و با «تستِ اتصال»ِ فقط‌خواندنی روی
    production احراز را بسنج؛ اگر ✅ شد یک فاکتورِ واقعیِ کنترل‌شده بفرست.
  - **تستِ زنده‌ی production موفق + رفعِ رقمِ کنترلیِ Verhoeff (همان‌روز):** «تستِ اتصال» روی محیطِ **واقعی** ✅ شد
    (احراز + دریافتِ کلیدِ رمزنگاری) — یعنی رمزنگاری/امضا/اتصال کاملاً درست. A44DOG اعتبارنامه‌ی production است (نه سندباکس).
    سپس یک فاکتورِ تشخیصیِ واقعی فرستاده شد؛ سامانه بسته را **رمزگشایی/تأیید کرد** (شماره مرجع گرفت) و در استعلام سه
    خطای **محتوایی** داد: (۱) `sstid` باید `^\d{13}$`، (۲) سریالِ صورتحساب `^[a-fA-F0-9]{10}$`، (۳) شماره مالیاتی
    `^[a-zA-Z0-9]{6}[a-fA-F0-9]{15}[0-9]{1}$`. ریشه‌ی (۲)+(۳): کاراکترِ بیست‌ودومِ taxid باید **یک رقم** باشد، ولی
    placeholderِ mod-32 گاهی حرف می‌داد (`…P`). **رفع:** رقمِ کنترلی با الگوریتمِ رسمیِ **Verhoeff** بازپیاده شد
    (`_verhoeff_check_digit`؛ حروفِ شناسه‌حافظه→کدِ اسکی، تاریخ→۶رقمِ ده‌دهی، سریال→۱۲رقمِ ده‌دهی)، با **نمونه‌ی مرجعِ
    رسمی `DEF5GH0481F000000000C2`** در تست قفل شد. taxidِ ما حالا `A44DOG050BE00000000032`. +۱ تست، **۱۰۲۵ سبز**،
    مستقرِ prod+demo. **آخرین بلوک:** `sstid` باید شناسه کالا/خدمتِ رسمیِ ۱۳رقمی باشد (نیازمندِ فیلدِ اختصاصی روی کالا +
    دادهٔ واقعیِ کاربر). منبعِ الگوریتم: [maliatman](https://maliatman.com) + repo `arjavand/moadian`.
  - **مانده (نیازمندِ کاربر) برای تستِ زنده:** (۱) واردکردنِ `A44DOG` + گواهی/کلیدِ واقعیِ یک مرکز میانی در پنلِ مؤدیانِ
    فقط روی مستأجرِ **acc.cubita** (نه سایر مستأجرها)؛ (۲) تأییدِ اینکه A44DOG سندباکس‌ثبت هست یا تستِ کنترل‌شده‌ی
    production. نکته‌ی داده‌ای: `sstid` هر ردیف باید شناسه‌کالا/خدمتِ رسمیِ ۱۳رقمی باشد (فعلاً از sku)؛ کاراکترِ کنترلیِ
    `taxid` هنوز placeholder (منتظرِ `RC_DCPS.SN`).

- **۱۴۰۵/۰۵/۱۵ (2026-08-05) — بازیابیِ رمز با پیامک (کامیت `15cd9e6`، بک‌اند+فرانت، بدونِ مهاجرت):**
  - **علت:** «فراموشیِ رمز» فقط ایمیلی بود (بندِ ۵ از [[commercialization-roadmap]]). حالا مسیرِ پیامکی هم هست.
  - **بک‌اند:** دو اندپوینتِ ناشناس در `routers/auth.py` — `POST /api/auth/forgot-password/sms` (کدِ ۶رقمی به
    شماره؛ همیشه ۲۰۲ِ یکسان ضدِ enumeration؛ فقط برای شماره‌ی «تأییدشده‌ی یکتا») و `POST /api/auth/reset-password/sms`
    (کد+رمزِ تازه → ورودِ خودکار؛ خطای یکسان برای شماره‌ی ناموجود/کدِ غلط). روی purposeِ از‌قبل‌موجودِ
    `PURPOSE_SMS_PASSWORD_RESET` و همان الگوی OTP (بی‌نیاز از الگوی تازه در پنل). نرخ‌گیری: `limit_password_reset_for_phone`
    (per-phoneِ بی‌صدا) + `limit_sms_reset_verify` (IP). `consume_code` سقفِ ۵ تلاش/انقضا/یک‌بارمصرفی را می‌دهد.
    اسکیما: `ForgotPasswordSmsIn`/`ResetPasswordSmsIn`. **۱۲ تستِ امنیتی سبز؛ ۱۲۷ تستِ مرتبط بی‌رگرسیون.**
  - **فرانت:** `LoginScreen` تبِ «ایمیل/پیامک» گرفت؛ فلوی پیامکیِ دو‌گامه (شماره → کد+رمز → ورود). `api`:
    `requestPasswordResetSms`/`resetPasswordSms`. موبایل/دسکتاپ در دو تم با Playwright تأیید شد.
  - **استقرار:** `deploy.sh production` (کد+وب، نسخه 0055 no-op) + سواپِ وبِ دمو. راستی‌آزماییِ زنده: هر دو
    endpoint روی prod (۴۰۵ GET/۴۲۲ POST/۲۰۲ عمومی)، محتوای تازه در باندلِ هر دو دامنه، هر دو ۲۰۰.

- **۱۴۰۵/۰۵/۱۵ (2026-08-05) — بازنویسیِ کاملِ راهنما: پوششِ همه‌ی ماژول‌ها (کامیت `07a9932`، فقط فرانت):**
  - **علت:** بررسیِ «کارهای مانده تا تجاری‌سازی» نشان داد `HelpPage` فقط ۱۵ ماژول را پوشش می‌داد و ۵ ماژولِ کاملِ
    غایب + ده‌ها زیرتبِ تازه توضیحی نداشتند (بندِ ۱ از فهرستِ تجاری‌سازی — [[commercialization-roadmap]]).
  - **۵ ورودیِ تازه:** صندوق فروشگاهی (POS)، فروش اقساطی، باشگاه مشتریان (CRMِ ۷تبی: سرنخ/پیگیری/امتیاز/
    بخش‌بندیِ RFM/سطوح/جوایز/تولد)، تولید و بهای تمام‌شده (BOM)، راه‌اندازی و ورود اطلاعات.
  - **زیرتب‌های تازه در ماژول‌های موجود:** فروش←مؤدیان؛ انبار←نیازمندِ سفارش/لیستِ قیمت/بچ‌وانقضا؛ حسابداری←
    دفترِ روزنامه/داراییِ ثابت/بودجه/مراکزِ هزینه/ارز؛ چک‌وبانک←حساب‌های بانکی/تنخواه؛ اشخاص←سنینِ مطالبات؛
    تقویم←کارهای امروز؛ اتصالِ فروشگاه←فروشگاهِ بومیِ کوبیتا؛ پروفایل←تأییدِ شماره با پیامک. ۴ پرسشِ تازه‌ی
    FAQ (ترایالِ ۱۴روزه، خرید/ارتقای پلن، فروش در برابر صندوق). ترتیبِ راهنما هم‌ترازِ گروه‌بندیِ نوارِ کناری شد.
  - **استقرار:** `tsc -b` پاک؛ سواپِ فقط‌وب روی prod (acc.cubita.ir) و demo (demo.cubita.ir)، بک‌اند دست‌نخورده.
    راستی‌آزماییِ زنده: محتوای تازه در باندلِ هر دو دامنه حاضر (POS/RFM/فروشگاهِ بومی/ترایال)، هر دو ۲۰۰.

- **۱۴۰۵/۰۵/۱۴ (2026-08-04) — رفعِ باگِ صفحه‌ی قیمت‌گذاری روی موبایل (سایتِ مارکتینگ cubita.ir):**
  - **علامت:** روی موبایل، کارت‌های پلن دیده نمی‌شدند و خطای قرمزِ «امکان دریافت پلن‌ها نیست». (بک‌اند/CORS سالم بود — `/api/plans`=۲۰۰ و preflight درست.)
  - **دو ریشه:** (۱) `website/src/api.ts` روی **GET** هم `Content-Type` می‌فرستاد → درخواست «غیرِساده» → **preflightِ OPTIONS**؛ round-tripِ دوم روی موبایلِ کند شکننده بود.
    (۲) کارت‌ها با framer-motion `whileInView` (`opacity:0` اولیه) بودند و تشخیصِ in-view روی موبایل گاهی نمی‌زد → کارتِ نامرئی.
  - **رفع:** GET حالا درخواستِ ساده (بی‌هدر) + **retry×۳ با backoff + timeout ۱۲s** (فقط GET، نه POSTِ خرید)؛ کارت‌ها `animate` روی mount؛ دکمه‌ی «تلاش دوباره» روی خطا.
  - راستی‌آزماییِ زنده روی ۳۹۰px: `fetch(/api/plans)`=۲۰۰/۳ پلن، **۳ کارت رندر شد، بی‌خطا**. مستقر روی cubita.ir (بکاپِ dist نگه‌داشته شد؛ shop-demo دست‌نخورده).

- **۱۴۰۵/۰۵/۱۴ (2026-08-04) — «فروشگاهِ بومی» فازِ ۲: زیبال/آی‌دی‌پی + پیش‌رندرِ سئو + گالری/دموی زنده:**
  - **درگاه‌های چندگانه:** `services/payment_providers.py` — لایه‌ی آداپتور (start/parse_callback/verify) برای **زرین‌پال + زیبال + آی‌دی‌پی**.
    `shop_payment.py` provider-agnostic شد (`active_gateway`/`gateway_for`)؛ callbackِ `/api/shop/pay/callback` به **GET+POST** تعمیم یافت
    (POSTِ فرمِ آی‌دی‌پی) و با `?provider=` تفکیک می‌شود. بدونِ مهاجرتِ DB (جدولِ `payment_gateways` از قبل هر سه را می‌پذیرفت).
  - **پیش‌رندرِ سئو:** `services/seo_render.py` — زمانِ بیلد، index.html با متای OG/Twitter + JSON-LD (Store+ItemList) و بلوکِ پیش‌رندرِ کالاها
    غنی می‌شود؛ برای هر کالا `p/<slug>/index.html` (Product+BreadcrumbList JSON-LD، ری‌دایرکتِ نرم به SPA)؛ `robots.txt`/`sitemap.xml`/`llms.txt`.
    `build_site_bundle(seo=…)` این‌ها را داخلِ ZIP می‌گذارد.
  - **گالری/مارکت‌پلیس:** `components/StorefrontGallery.tsx` — کارتِ قالب با پیش‌نمایشِ مینیاتوری، ویژگی‌ها، «پیش‌نمایشِ زنده» و «انتخاب»
    (برای آزمایشی: CTAی خرید پلن). بالای تبِ «بساز» و بالای FeatureUpsellِ آزمایشی. دسکتاپ+وب، موبایلِ ≤۷۶۰ تأییدِ بصری‌شده (تیره/روشن).
  - **دموی خوداتکا:** قالب حالتِ `demo:true` گرفت (`storefront/assets/api.js` از `window.SHOP_DEMO` می‌خواند)؛ `storefront-demo/config.js`
    داده‌ی نمونه (۸ کالا، تصاویرِ SVGِ درون‌خطی) دارد → سایتِ دموی بدونِ بک‌اند برای میزبانی روی VPS.
  - تست: **۹۷۸ سبز** (+۷: zibal/idpay/seo). tsc پاک.

- **۱۴۰۵/۰۵/۱۴ (2026-08-04) — 🚀 استقرارِ «فروشگاهِ بومی» روی prod + دمو (مهاجرت 0054 زنده):**
  - **prod (acc.cubita.ir):** `deploy.sh production` اجرا شد — بکاپ، مهاجرت **0053→0054**، نسخه‌ی نهایی 0054،
    health ok، **RLS روی ۷۳ جدول** (۶۶→۷۳)، بکاپِ بعد از استقرار سالم. راستی‌آزماییِ زنده: `/api/shop/catalog`=۴۰۱،
    `/api/storefront`=۴۰۱، CORS `Allow-Origin: *` روی `/api/shop/*`، preflight=۲۰۴، `/api/shop/pay/callback`=۳۰۳.
  - **پیش‌نیازهای استقرار:** پوشه‌ی `storefront/` جدا به `/opt/hesabdari/storefront` رفت (چون deploy.sh نمی‌شناسدش و
    مسیرِ پیش‌فرضِ `site_build` روی چیدمانِ سرور غلط است)، و `.env` prod دو کلید گرفت: `STOREFRONT_API_BASE=https://acc.cubita.ir`
    و `STOREFRONT_TEMPLATE_DIR=/opt/hesabdari/storefront`.
  - **دمو (demo.cubita.ir):** سواپِ فقط‌وب (باندلِ signup-first به prod) — پنلِ تازه + رنگِ چک‌باکس. بک‌اندِ خفته دست‌نخورده.
  - **باندلِ وب:** رنگِ برندِ چک‌باکس/رادیو (accent-color) هم در همین باندل رفت. هر دو دامنه ۲۰۰.
  - **⚠️ نکته‌ی زنده:** پرداختِ زرین‌پال کدش مستقر ولی **تا مستأجری درگاهِ فعال تنظیم نکند بی‌اثر است** (هیچ پولِ واقعی جابه‌جا نمی‌شود)؛
    اولین تراکنشِ واقعی باید کنترل‌شده با مرچنتِ واقعی آزموده شود. واحدِ پولِ پیش‌فرض «تومان».

- **۱۴۰۵/۰۵/۱۴ (2026-08-04) — «فروشگاهِ بومی»: CORالسطحِ عمومی (آماده‌سازیِ استقرار):**
  - میدل‌ورِ `shop_public_cors` در `main.py`: برای `/api/shop/*` هدرِ `Access-Control-Allow-Origin: *` (+ preflightِ
    OPTIONS با ۲۰۴). چرا `*`: سطحِ عمومی روی دامنه‌ی هاستِ **خودِ مستأجر** اجرا می‌شود (cross-origin) و کوکی/اعتبارنامه‌ی
    ambient ندارد — احراز فقط با کلیدِ publishable است، پس origin مرزِ امنیتی نیست. CORالسراسری فقط دامنه‌های اپ را می‌شناسد.
  - **این بلاکرِ کارکردیِ استقرار بود:** بدونش، سایتِ روی هاستِ مستأجر موقعِ fetchِ `/api/shop/*` در مرورگر بلاک می‌شد.
    +۱ تست (هدرِ wildcard + preflight). **۹۷۱ سبز.** `allowed_origin` برای ریدایرکتِ callback می‌ماند. **مستقر نشده.**

- **۱۴۰۵/۰۵/۱۴ (2026-08-04) — «فروشگاهِ بومی»: پرداختِ آنلاینِ زرین‌پالِ مستأجر:**
  - **بک‌اند** `services/shop_payment.py` (زرین‌پال v4، از `D:\Sample` برداشته و پرمستأجر شده): `POST /api/shop/orders/{id}/pay`
    از درگاهِ **فعالِ خودِ مستأجر** (`payment_gateways`) authority می‌گیرد و لینکِ StartPay می‌دهد؛ `GET /api/shop/pay/callback?shop=<slug>`
    (بی‌زمینه؛ مستأجر از slug حل می‌شود) verify و در موفقیت `confirm_payment`→فاکتور. **مبلغِ ریالی** بر پایه‌ی
    `theme_config.currency` (تومان پیش‌فرض → ×۱۰). اگر پرداختْ موفق ولی ثبتِ فاکتور ناموفق (موجودی)، سفارش «پرداخت‌شده»
    با نشانِ بررسیِ دستی می‌ماند (پول از دست نمی‌رود). `ShopContext.slug` و `ShopInfoOut.has_online_payment` اضافه شد.
  - **سایت:** تسویه بر پایه‌ی `has_online_payment` یا به درگاه می‌برد یا سفارشِ دستی نشان می‌دهد؛ صفحه‌ی `#/order-result`.
    **پنل:** انتخابگرِ **واحدِ پول (تومان/ریال)** در «ظاهر و سئو» (تصمیمِ پولِ واقعی).
  - **تست:** ۶ تستِ mock‌شده (info، pay+مبلغِ ریالی، callback→فاکتور، شکست، بدونِ درگاه). **۹۷۰ سبز**؛ tsc پاک. **مستقر نشده.**
    زیبال/آی‌دی‌پی فعلاً موکول.

- **۱۴۰۵/۰۵/۱۴ (2026-08-04) — «فروشگاهِ بومی»: خطِ بیلد + دانلودِ بسته‌ی سایت:**
  - **بک‌اند** `GET /api/storefront/site-bundle`: قالبِ `storefront/` را می‌خواند، `assets/config.js` را با
    `apiBase`(از `storefront_api_base`/`backend_url`)+`slug`+`publishable_key`ِ همین مستأجر جایگزین می‌کند، فایلِ
    «آموزش-اتصال.txt» می‌افزاید، و **ZIP** برمی‌گرداند (`Response` با `application/zip`). `README.md`ِ داخلی حذف می‌شود.
    `last_built_at` به‌روز می‌شود. سرویس `services/site_build.py`؛ دو تنظیمِ تازه‌ی `config.py`.
  - **فرانت:** دکمه‌ی **«ساخت و دانلودِ سایت»** در کارتِ «پیوند و انتشار» (`downloadStorefrontBundle` → Blob → دانلود) +
    بخشِ **راهنمای اتصال** (details). `.btn-download`/`.sf-guide` CSS.
  - **تست:** بسته unzip می‌شود و config با slug/keyِ واقعی و **بدونِ placeholderِ REPLACE_WITH** تأیید شد. **۹۶۴ سبز**؛ tsc پاک. **مستقر نشده.**

- **۱۴۰۵/۰۵/۱۴ (2026-08-04) — «فروشگاهِ بومی»: قالبِ سایتِ عمومی (`storefront/`):**
  - قالبِ **بی‌نام‌ونشانِ** فروشگاه که روی هاستِ خودِ مستأجر آپلود می‌شود و از `/api/shop/*` تغذیه می‌کند.
    **⚠️ عمداً نامِ ipnetcity ندارد** (کاربر سایتِ دوربینِ زنده با همین نام روی VPS دارد — [[do-not-touch-ipnetcity]]).
    به‌جای تطبیقِ verbatimِ ۶۷۰۰ خطِ کدِ دوربینی/ادمینِ `D:\Sample`، یک قالبِ عمومیِ تمیز ساخته شد که زبانِ بصری را بازاستفاده می‌کند.
  - **وانیلا-جی‌اسِ استاتیک (بی‌بیلد، هر هاست):** `index.html` + `assets/{config,api,app,style}.js/css`. `config.js` (apiBase/slug/key)
    هنگامِ «ساخت و دانلود» بیک می‌شود. برند/رنگ/نام از `/api/shop/info`. صفحات: خانه/کاتالوگ+دسته+جست‌وجو، محصول،
    سبد (localStorage)، تسویه→`POST /api/shop/orders`. طراحیِ RTLِ روشنِ برندپذیر (`--brand`).
  - Playwright روی خانه/محصول/سبد/تسویه در **دسکتاپ+موبایل** تأیید شد (رنگِ برند از theme_config درست اعمال شد).
  - مانده تا MVP سایت: خطِ بیلد (config+ZIP)، پیش‌رندرِ سئو، پرداختِ آنلاینِ زرین‌پال. **مستقر نشده.**

- **۱۴۰۵/۰۵/۱۴ (2026-08-04) — «فروشگاهِ بومی»: پنلِ مدیریت در برنامه (فرانت):**
  - ماژولِ «اتصال فروشگاه» **دوتب** شد: **«فروشگاهِ کوبیتا (بساز)»** (`NativeStorefrontPanel`) + «اتصال به سایتِ موجود» (`IntegrationPanel`ِ قبلی).
  - `NativeStorefrontPanel` به `/api/storefront/*` وصل است: کارت‌های **پیوند/انتشار** (شناسه، کلیدِ publishable با کپی/چرخش،
    originِ مجاز، انتشار)، **ظاهر/سئو** (عنوان/توضیح/رنگ/تلفن)، **کالاهای فروشگاه** (تیکِ نمایش + slug + ذخیره، صفحه‌بندی)،
    **درگاهِ پرداخت** (زرین‌پال/زیبال/آی‌دی‌پی، مرچنت راز)، و **فیدِ سفارش** (تأیید پرداخت→فاکتور، تغییرِ وضعیتِ ارسال).
  - api.ts توابع/تایپ‌های تازه گرفت. `sf-*` CSS + حالتِ کارتیِ موبایل (کلاسِ مشترکِ `:is(.integration-table, .sf-table)`).
  - tsc پاک؛ Playwright روی **تیره/روشن/موبایلِ ۳۹۰px** ([[mobile-view-rules]]) تأیید شد. **هنوز مستقر نشده** (وابسته به بک‌اندِ 0054).

- **۱۴۰۵/۰۵/۱۴ (2026-08-04) — «فروشگاهِ بومی»: مدیریت + حلقه‌ی سفارش (بدونِ مهاجرتِ تازه):**
  - **API مدیریت `/api/storefront/*`** (احرازشده، `require_feature("storefront")` + مجوزِ inventory): تنظیمات
    (تم/رنگ/سئو/تماس/`allowed_origin`)، **تولید/چرخشِ کلیدِ publishable**، انتشار/لغوِ انتشار، **فهرست‌کردنِ کالا**
    (`is_listed`/عکس/توضیح/slug/badge)، و **درگاهِ پرداختِ مستأجر** (zarinpal/zibal/idpay؛ `merchant_id` راز، فقط
    `has_merchant`). فایل‌ها: `routers/storefront.py`, `services/storefront_manage.py`, `schemas/storefront_native.py`.
  - **حلقه‌ی سفارش:** `POST /api/shop/orders` (عمومی) سفارشِ «در انتظار» می‌سازد با **قیمتِ سمتِ سرور** (نه کلاینت) +
    چکِ موجودی؛ `POST /api/storefront/orders/{id}/confirm-payment` آن را با `post_sales_invoice` به فاکتورِ فروشِ
    انبارِ **ONLINE** تبدیل می‌کند (**کسرِ خودکارِ موجودی، idempotent**)؛ `GET /orders` فید و `PUT .../fulfillment`
    وضعیت. این درزِ همان‌جایی است که بعداً callbackِ زرین‌پال می‌نشیند. موجودیِ کاتالوگ هم از انبارِ ONLINE خوانده
    می‌شود تا «نمایش = قابلِ فروش» باشد.
  - **تست:** +۱۲ (۷ مدیریت + ۵ حلقه‌ی سرتاسری: سفارش→فید→تأیید→فاکتور+کسرِ موجودی، idempotency، ضدِ اضافه‌فروشی،
    رازماندنِ مرچنت). **کلِ سوییت ۹۶۳ سبز.** بدونِ مهاجرتِ تازه (head همان `0054`). **هنوز روی prod مستقر نشده.**

- **۱۴۰۵/۰۵/۱۴ (2026-08-04) — «فروشگاهِ بومیِ کوبیتا»: فازِ ۰ (شالوده‌ی بک‌اند) (مهاجرت `0054`):**
  - **بستر:** بازطراحیِ «اتصال فروشگاه» از مدلِ sync‌شونده‌ی بیرونی به یک **سازنده‌ی فروشگاهِ بومی**؛ کاتالوگ = همان
    `items`، سفارش → مستقیم فاکتورِ فروش. نقشه‌ی کامل و تصمیم‌ها: **`STOREFRONT_PLAN.md`** ([[storefront-native-rebuild]]).
    تصمیم‌های قفل‌شده: میزبانی روی هاستِ خودِ مستأجر (ZIP+آموزش)، رندرِ SSG با بازاستفاده از فرانتِ ipnetcity،
    پرداختِ داخلِ سایت با زرین‌پالِ خودِ مستأجر، اولین قالب «عمومی».
  - **مدل + مهاجرت `0054`:** ۷ جدولِ مستأجرمحور + RLS روی هر ۷ (`storefronts`, `item_storefront`,
    `storefront_categories`, `storefront_customers`, `storefront_orders`+`_lines`, `payment_gateways`). فایلِ مدل
    `app/models/storefront_native.py` (جدا از `storefront.py`ِ اتصالِ بیرونی که برای ipnetcity زنده می‌ماند).
  - **API عمومیِ باریک** (`app/routers/shop.py` + `services/shop.py` + `schemas/shop.py`): `/api/shop/{info,catalog,
    categories,product/{slug}}`. **احرازِ کلیدِ publishable** بدونِ کاربر — بوت‌استرَپِ مستأجر از `tenants.slug`
    (سراسری)، سپس کلید داخلِ زمینه با `secrets.compare_digest`؛ منتشرنشده=۴۰۳. **سریالایزرِ کاست‌پنهان** (`ShopProductOut`
    هرگز `average_cost`/سود نمی‌دهد). CORS per-storefront عمداً به فازِ تحویل موکول شد (TODO در روتر).
  - **تست:** ۸ تستِ تازه (کاست‌پنهان، فیلترِ لیست‌شده، موجودی، اعتبارسنجیِ کلید/انتشار، HTTP، و **نشتِ کاتالوگِ بین دو
    مستأجرِ واقعی**) + پوششِ خودکارِ پارامتریِ نشت روی ۷ جدولِ تازه. **کلِ سوییت ۹۵۱ سبز.** مهاجرت تا head اجرا و RLS
    (`force=true` + policy) روی هر ۷ جدول راستی‌آزمایی شد. **هنوز روی prod مستقر نشده** (فازِ درحال‌ساخت).

- **۱۴۰۵/۰۵/۱۳ (2026-08-04) — صفحه‌بندیِ عددیِ فهرست‌های داده در کلِ برنامه (کامیت `bfac44a`، فقط فرانت):**
  - **هدف:** تعمیمِ صفحه‌بندیِ «آخرین رویدادها» به همه‌ی فهرست‌های افزودنی؛ با گذر از ۱۰ ردیف، جدول صفحه‌بندیِ
    عددی می‌گیرد. اندازه‌ی صفحه **وفقی**: فهرست‌های کارتی/جمع‌وجور ۴تایی (از قبل)، جدول‌های داده ۱۰تایی.
  - **پوشش (۲۷ جدول، ۱۹ فایل) با `usePagination`/`Pager`:** محصولات، رویدادها، اشخاص+خزانه، چک‌ها، پیش‌فاکتورها،
    انبارها، مراکزِ هزینه، ارزها+نرخ‌ها، لیست‌های قیمت+قیمتِ کالاها، بچ‌ها، دارایی+استهلاک، حساب‌های بانکی، تنخواه،
    کدینگ، CRM(سرنخ/پیگیری/باشگاه)، پرسنل+فیش‌ها، موجودیِ انبار، کاربران، اسنادِ تکراری. `resetKey` صفحه را با فیلتر ری‌ست می‌کند.
  - **مستثنا (عمدی):** گزارش‌های مالی، ردیف‌های ورودِ فاکتور و جدول‌های داده‌ورودی (براکت مالیات/انبارگردانی/نگاشتِ فروشگاه)، کشوها.
  - **استقرار:** tsc پاک؛ Playwright جدولِ ۱۲ردیفی+Pager دو پوسته+موبایل. سواپِ فقط‌وب روی prod+دمو؛ هر دو ۲۰۰.


- **۱۴۰۵/۰۵/۱۳ (2026-08-04) — یکدست‌سازیِ استانداردِ فیلدها + نرم‌کردنِ پوستهٔ روشن (کامیت `ba3dd1a`، فقط فرانت):**
  - **هدف:** پس از استانداردِ تمیزِ گرید، فیلدهای فرمِ کلِ برنامه هنوز استایلِ سنگینِ قدیمی داشتند
    (باکسِ تیره‌ی «فرورفته»، اعدادِ چپ‌چین در فرمِ RTL)؛ و پوستهٔ روشن با کارت‌های سفیدِ خالص خیره‌کننده بود.
  - **پایه‌ی سراسریِ فیلد (index.css):** پس‌زمینه --bg → --surface-2 (هم‌رنگِ کارت)، هاورِ مرزِ پررنگ‌تر،
    فوکوسِ حلقه‌دار؛ `.item-picker-trigger` هم‌تراز شد. **رفعِ RTL:** `NumberInput` کلاسِ `.num-input`
    (text-align:right) گرفت تا اعداد راست‌چین و ستون‌ها زیرِ هم بیایند.
  - **پوستهٔ روشنِ کم‌خیرگی:** `--surface` #ffffff→#f6f7fb (آف‌وایت)، `--page` #f5f7fc→#e7eaf2 (خاکستری-آبیِ آرام)،
    `--surface-2`/hover/border نرم‌تر، سایدبار #ffffff→#edf0f7. کنتراستِ متن حفظ (AA). دارک دست‌نخورده.
  - **استقرار:** tsc پاک؛ Playwright فرم/گرید/سایدبار دو پوسته + موبایل. سواپِ فقط‌وب روی prod+دمو؛ هر دو ۲۰۰.

- **۱۴۰۵/۰۵/۱۳ (2026-08-04) — بازطراحیِ گریدِ ورودِ اقلام به سبکِ دفتری (کامیت `8fc9893`، فقط فرانت/CSS):**
  - **هدف:** ردیف‌های آیتم‌دارِ فرم‌ها (`.invoice-lines`) مجموعه‌ای از باکس‌های گِردِ بوردردارِ شناور بودند؛
    حسِ «جدول» نداشتند و غیرحرفه‌ای دیده می‌شدند (تشخیصِ کاربر درست بود؛ مشکل افقی‌بودن نبود، استایل بود).
  - **تغییر (یک CSS، ۹ فرم):** جدولِ یکپارچه‌ی بوردردار با ردیفِ سرستونِ پس‌زمینه‌دار + جداکنندهٔ ملایمِ
    ردیف + هاور؛ ورودی‌ها داخلِ خانه محو (بوردر فقط هاور/فوکوس، مثلِ صفحه‌ی گسترده)؛ اعدادِ ستونی راست‌چین.
    موبایل حالتِ «کارتِ برچسب‌دار» را نگه داشت (فیلدها آنجا دوباره مرزدار می‌شوند).
  - **پوشش:** فروش، خرید، برگشتِ فروش/خرید، پیش‌فاکتور، انتقالِ انبار، سندِ دستی، اسنادِ تکراری، BOMِ تولید.
  - **استقرار:** tsc پاک؛ Playwright روی فروش (item-picker) و سندِ دستی (select+بدهکار/بستانکار) در دو پوسته و
    موبایل. سواپِ فقط‌وب روی prod (acc.cubita.ir) و درِ ورودیِ ترایال (demo.cubita.ir)؛ هر دو ۲۰۰، CSSِ تازه در باندل.
  - **دنباله (کامیت `61d2b42`):** ستونِ «مبلغِ ردیف» به فروش/خرید/پیش‌فاکتور اضافه شد — سلولِ نمایشیِ
    `.line-amount` (تعداد×قیمت−تخفیف؛ پیش‌فاکتور بدونِ تخفیف). برگشت‌ها عمداً کنار ماندند (مبلغشان سمتِ
    سرور با تخفیفِ نسبی حساب می‌شود و گریدشان مقدارمحور است). tsc پاک؛ مستقر روی prod+دمو.

- **۱۴۰۵/۰۵/۱۳ (2026-08-04) — گروه‌بندیِ سرفصل‌دارِ نوار کناری (کامیت `6be028e`، فقط فرانت):**
  - **هدف:** ساید‌بار یک فهرستِ تختِ ۱۶‌تایی بود؛ کاربر باید کلِ لیست را اسکن می‌کرد و همجواری‌ها بی‌منطق
    بودند (اتصال فروشگاه دور از فروش، راه‌اندازی لای ماژول‌های روزمره).
  - **تغییر:** ماژول‌ها زیرِ ۶ سرفصلِ کم‌رنگ گروه‌بندی شدند، مرتب بر اساسِ گردشِ کار: میزکار · فروش و مشتریان
    (فروش/صندوق/اقساطی/باشگاه/اتصال فروشگاه) · خرید و انبار · مالی و بانکی · اطلاعات و گزارش (اشخاص/گزارش‌ها) ·
    ابزار (تقویم/راه‌اندازی). آیتم‌های ادمین در گروهِ مشروطِ «مدیریت سامانه» ته فهرست.
  - **کد:** `Sidebar.tsx` از آرایه‌ی تخت به `NAV_GROUPS` (heading+items) رفت؛ `.sidebar-nav-group` +
    `.sidebar-nav-heading` در `App.css`. همان ساید‌بار روی وب و درایورِ موبایل استفاده می‌شود.
  - **استقرار:** tsc پاک؛ Playwright روی دسکتاپ (تیره/روشن) و موبایلِ ۳۹۰px. سواپِ فقط‌وب (بدونِ لمسِ بک‌اند)
    روی prod (acc.cubita.ir) و درِ ورودیِ ترایال (demo.cubita.ir)؛ هر دو ۲۰۰ و نشانِ گروه در باندلِ زنده حاضر.

- **۱۴۰۵/۰۵/۱۲ (2026-08-03) — قیمت‌گذاریِ چنددوره‌ای + کارت‌های رقابتی (کامیت `a8c1fc3`، مهاجرت 0053):**
  - **هدف:** توضیحاتِ لاغرِ پلن پایه (۳ قلم) و نبودِ دوره‌های صورتحساب. با بررسیِ رقبا (حسابفا/فینتو/هلو)
    ساختار هم‌ردیف شد: سه دوره‌ی **ماهانه/شش‌ماهه/سالانه** و کارت‌های پرجزئیات.
  - **بک‌اند:** `plans.prices` (JSONBِ نگاشتِ دوره→قیمت) + `purchases.billing_period`. `days_for_period`
    شش‌ماهه=۱۸۰. `create_purchase_request` مبلغِ دوره‌ی انتخابی را به زرین‌پال می‌فرستد و ثبت می‌کند؛
    `_extend_subscription` روزها را از دوره‌ی خرید می‌گیرد (نه پیش‌فرضِ پلن).
  - **قیمت‌ها (رقابتی، زیرِ رقبا):** پایه ۴۵۰هزار/ماه · سالانه ۴٫۳۲م (۲ کاربر)، حرفه‌ای ۸۹۰هزار · ۸٫۵۴م
    (۵ کاربر)، سازمانی ۱٫۴۹م · ۱۴٫۳م (نامحدود). تخفیفِ شش‌ماهه ۱۰٪، سالانه ۲۰٪.
  - **کارت‌ها/فهرست‌ها:** امکاناتِ واقعیِ گسترده (پایه ۱۰، حرفه‌ای ۹، سازمانی ۸). سایت: سگمنت‌کنترلِ دوره
    قیمتِ همه‌ی کارت‌ها را عوض می‌کند + درصدِ تخفیف/قیمتِ خط‌خورده/«معادلِ X در ماه». دسکتاپ/موبایل با Playwright تأیید شد.
  - **استقرار:** مهاجرت 0053 + **به‌روزرسانیِ رکوردهای زنده‌ی پلن** (seed فقط‌درج است، پس مقادیرِ prod دستی
    از `SAMPLE_PLANS` به‌روز شد) + سایتِ cubita.ir. تست‌ها سبز (۹۰۸). `/api/plans` قیمت‌های سه‌دوره‌ای می‌دهد.

- **۱۴۰۵/۰۵/۱۲ (2026-08-03) — تأیید ایمیل هنگام ثبت‌نام (کامیت `26c96e1`، مهاجرت 0052):**
  - **هدف:** ثبت‌نامِ «۱۴ روز رایگان» ایمیلِ واقعی بخواهد. **verify-before-create**: کد پیش از ساختِ حساب
    سنجیده می‌شود، پس هیچ حسابی با ایمیلِ تأییدنشده به‌وجود نمی‌آید.
  - **بک‌اند:** جدولِ سراسریِ `email_verification_codes` (بدونِ RLS — پیش از احراز هویت و بی‌زمینه‌ی مستأجر؛
    کلیدش ایمیل است نه user_id، پس در `email_verification_codes` جدا و نه `auth_tokens`) + ستونِ
    `users.email_verified_at`. سرویسِ `email_verification` (issue/consume، نمک با ایمیل، انقضای ۱۵د، سقفِ ۵ تلاش).
    اندپوینتِ `signup/request-code` (گامِ اول، سقفِ IP + per-email) و `signup` که حالا `code` می‌خواهد و
    `email_verified_at` را ست می‌کند. `email_verified` روی MeOut. اکانتِ دستیِ مدیر کد نمی‌خواهد (از سرویس رد می‌شود).
  - **فرانت:** `SignupScreen` دومرحله‌ای (مشخصات → کدِ OTPِ ۶رقمی با «ارسال دوباره»/شمارش/«ویرایش اطلاعات»).
    دسکتاپ و موبایلِ ۳۹۰px با Playwright تأیید شد.
  - **راستی‌آزماییِ زنده روی prod:** مهاجرت 0052 (جدول + ستون)، request-code واقعی `sent=true` (SMTP)، مسیرهای
    ۴۲۲/۴۰۰، چرخه‌ی سرویس درست؛ کلِ سوییت سبز (۹۰۵). فقط prod مستقر (بک‌اندِ دموی خفته migrate نشد). هر دو باندلِ وب هم‌گام.

- **۱۴۰۵/۰۵/۱۲ (2026-08-03) — صفحه‌بندیِ عددیِ فهرست‌های عمودیِ داشبرد (کامیت `ea53a83`، فقط فرانت):**
  کامپوننتِ قابل‌استفاده‌ی مجددِ `Pager` + هوکِ `usePagination` (۴ در هر صفحه؛ شماره‌ی صفحه با میان‌بُرِ …؛
  فلشِ RTL؛ فقط وقتی >۱ صفحه). روی `ActivityFeed` (آخرین رویدادها، تا ۴۰ موردِ اخیر)، `AlertsPanel`
  (نیازمند رسیدگی) و `ReminderCenterPanel` (کارهای امروزِ تقویم — صفحه با تغییرِ فیلتر ریست می‌شود) اعمال شد؛
  پیش‌تر مابقیِ موارد بعد از ۴/۱۲/۱۴ پنهان می‌ماند. موبایل ۳۹۰px با اسکرین‌شاتِ Playwright تأیید شد. بک‌اند بی‌تغییر.

- **۱۴۰۵/۰۵/۱۲ (2026-08-03) — دیدِ حساب‌های آزمایشیِ ۱۴روزه در «مدیریت اکانت‌ها» (کامیت `12f9576`):**
  - **بک‌اند:** سه فیلدِ `is_trial`/`trial_days_left`/`trial_expired` (از `subscriptions.trial_info`) به
    `AccountRowOut` و ردیفِ `list_accounts` افزوده شد. `list_accounts` از قبل همه‌ی tenantها را برمی‌گرداند؛
    فقط علامتِ آزمایشی نداشت.
  - **فرانت (`AccountsAdminPage`):** کارتِ KPIِ «آزمایشیِ رایگان (۱۴روزه)»، نوارِ فیلترِ «همه/آزمایشیِ رایگان/پولی»
    با شمارش، و نشانِ بنفشِ «آزمایشیِ رایگان»/«آزمایشی منقضی» روی هر کارت؛ ردیفِ انقضا برای آزمایشی «X روز از
    ۱۴ روزِ آزمایشی مانده» را نشان می‌دهد. CSSِ `status-badge.tone-trial` و `.account-filter`. موبایل‌ویو با
    flex-wrap امن است.
  - **رفعِ باگِ نهفته:** `admin_accounts.extend_account` هنگامِ تمدیدِ دستی پرچمِ `is_trial` را پاک نمی‌کرد، پس
    اکانتِ آزمایشیِ تمدیدشده به‌غلط «آزمایشی» می‌ماند (مؤدیان/فروشگاه قفل، نمایشِ بی‌معنیِ «۳۷۸ روز از ۱۴»). حالا
    تمدیدِ دستی `is_trial=False` می‌کند (قرینه‌ی `provision_for_purchase`). یک رکوردِ کهنه‌ی prod (اکانتِ «تست»)
    هم اصلاح شد.
  - تست‌ها: +۳ در `test_admin_accounts` (۱۵→۱۸)؛ admin+trial+lifecycle سبز (۲۹). فقط prod مستقر شد (بک‌اندِ دموی
    :8002 خفته و ستونِ is_trial ندارد)؛ هر دو باندلِ وب هم‌گام شدند.

- **۱۴۰۵/۰۵/۱۲ (2026-08-03) — رفعِ باگِ درِ ورودیِ ترایال (کامیت `a2e6af8`):**
  - **علت (رگرسیونِ استقرار):** باندلِ `demo.cubita.ir` در ساختِ قبلی با `VITE_API_URL=demo.cubita.ir`
    بیلد شده بود، پس nginx مسیرِ `/api/` را به **بک‌اندِ دموی خفته‌ی :8002 (مهاجرت 0050، بدونِ سیستمِ ترایال)**
    می‌فرستاد. دو خرابیِ گزارش‌شده‌ی کاربر از همین یک اشتباه بود: (۱) حسابِ ساخته‌شده با «۱۴ روز رایگان»
    در دیتابیسِ دمو می‌نشست و از «ورود به برنامه اصلی» (`acc.cubita.ir`) قابلِ لاگین نبود؛ (۲) پاسخِ `/me`
    آن بک‌اندِ کهنه فیلدِ `locked_features` نداشت، پس `me.locked_features.includes(...)` در صفحه‌ی فروش
    (تبِ مؤدیان) و اتصالِ فروشگاه استثنا می‌داد و **صفحه سفید** می‌شد.
  - **رفع:** باندلِ درِ ورودیِ ترایال دوباره با `VITE_API_URL=https://acc.cubita.ir VITE_SIGNUP_FIRST=true`
    ساخته و روی `/opt/cubita-demo/web` مستقر شد (طبقِ طرحِ مستند؛ CORS پرودِ `demo.cubita.ir` از قبل باز بود).
    به‌علاوه گاردِ دفاعیِ `(me.locked_features ?? [])` در `Dashboard`/`SalesPage` تا نبودِ این فیلد هرگز صفحه را
    سفید نکند. پرودِ اپ هم با همین گارد بازساخته و مستقر شد.
  - **راستی‌آزماییِ زنده روی prod:** signup→۲۰۱، `/me`→`is_trial=true` و `locked_features=[moadian,storefront]`،
    login→۲۰۰؛ حسابِ آزمایشیِ تست با `purge_tenant` پاک شد. هر دو دامنه HTTP 200. **دام برای آینده:** هر بار
    باندلِ دمو را بازمی‌سازی باید به `acc.cubita.ir` اشاره کند نه به خودش — [[demo-instance-reseed]].

- **۱۴۰۵/۰۵/۱۰ (2026-08-01) — نسخه‌ی آزمایشیِ رایگانِ ۱۴روزه + بازطراحیِ دمو (مهاجرت ۰۰۵۱):**
  - **مدلِ یکپارچه:** حسابِ آزمایشی یک مستأجرِ عادی در دیتابیسِ **اصلی** است با «اشتراکِ
    ۱۴روزه‌ی بدونِ پلن» و پرچمِ `Tenant.is_trial`. `signup_new_business` این را ست می‌کند.
    **خرید با همان ایمیل، حساب را سرِ جا به واقعی تبدیل می‌کند و دیتا حفظ می‌شود**
    (`provision_for_purchase` → `is_trial=False`) — همان وعده‌ی «برای حفظِ اطلاعاتت پلن بخر».
  - **گیتِ پلن:** «سامانه مؤدیان» و «اتصال فروشگاه» برای آزمایشی قفل‌اند (`deps.require_feature`
    روی هر دو روتر، ۴۰۲) → فرانت باکسِ `FeatureUpsell` («مشاهده پلن‌ها») نشان می‌دهد.
  - **قفلِ پایانِ ترایال:** آزمایشیِ منقضی **کاملاً** قفل می‌شود (خواندن هم، در `require_permission`)،
    برخلافِ انقضای مشتریِ واقعی که فقط‌خواندنی است — چون داده‌ی آزمایشی سندِ قانونیِ کسی نیست.
    `/me` و `/subscription` و صفحه‌ی خرید باز می‌مانند. فرانت `TrialExpiredScreen` را نشان می‌دهد.
  - **چرخه‌ی عمر (کرونِ روزانه `app.trial_cron`، systemd timer روی prod):** یادآوریِ ایمیلیِ
    یک‌باره وقتی ≤۳ روز مانده، و **حذفِ حساب فقط بعد از انقضا + بافرِ ۷روزه** (تا خریدِ دیرهنگام
    دیتا را نجات دهد). `Tenant.trial_reminder_sent_at` جلوی یادآوریِ تکراری را می‌گیرد.
  - **فرانت:** `SignupScreen` («۱۴ روز رایگان» — اپ قبلاً ثبت‌نامِ خودسرویس نداشت)، `TrialBanner`
    («X روز مانده»، `MeOut.trial_days_left` با **ceil** تا روزِ صفر ۱۴ باشد)، پرچمِ `VITE_SIGNUP_FIRST`.
  - **زیرساخت:** `demo.cubita.ir` دیگر «دیجی‌مارکت»ِ فقط‌خواندنی نیست؛ حالا **درِ ورودیِ ترایال**
    است که فرانتش (signup-first) به بک‌اندِ **prod** وصل می‌شود (`VITE_API_URL=acc.cubita.ir` +
    `demo.cubita.ir` به `ALLOWED_ORIGINS`ِ prod اضافه شد). دموی دیجی‌مارکت در `web.old-digimarket`
    نگه داشته شد. مستقر روی prod؛ ثبت‌نام + گیت + نوار + قفل زنده در ۳۶۰/۳۹۰px بی‌سرریز تست شد.
  - تست‌ها: ۱۶ تستِ تازه (trial + trial_lifecycle) + رگرسیونِ سبز. کامیت‌ها: `5da1ff9`، `ea495bc`،
    `4242214`، `04c29f0`، `ad883a6`.

- **۱۴۰۵/۰۵/۱۰ (2026-08-01) — سرویسِ پیامک (ملی‌پیامک) + فعال‌سازیِ شماره‌ی موبایل با OTP (مهاجرت ۰۰۵۰):**
  - **حاملِ پیامک `app/services/sms.py`** (قرینه‌ی mailer): کنسولِ جدیدِ ملی‌پیامک (`console.melipayamak.com`)،
    دو مسیر — `send_pattern`/`send_verification_code` (الگوی خطِ اشتراکی، برای کدها؛ فیلترنشدنی و فوری) و
    `send_text` (خطِ اختصاصی، برای متنِ آزاد). یکدست‌سازیِ شماره (فارسی/‏+۹۸/۰۰۹۸ → ۰۹...)، رفتارِ dev/prod
    مثلِ mailer، بی‌استثنا. تنظیماتِ سراسری در `.env`: `MELIPAYAMAK_API_KEY/SENDER/OTP_PATTERN_ID`.
  - **یافته‌ی مهم:** خطِ فعلی (`50004001455878`) خطِ **اشتراکی**ست، پس فقط **الگو** می‌فرستد نه متنِ آزاد.
    ارسالِ الگو زنده تست شد (`recId` موفق، ~۲ واحد هزینه). اطلاع‌رسانیِ مشتری هم قرار شد با الگو برود (فازِ بعد).
  - **فعال‌سازیِ شماره (OTP):** ستون‌های `users.phone_verified_at` و `auth_tokens.attempts`؛ کدِ ۶رقمیِ عددی
    در `tokens.issue_code/consume_code` (نمک‌خورده با کاربر+هدف، انقضای ۵دقیقه، سقفِ ۵ تلاش، حذفِ کدِ قبلی).
    اندپوینت‌های `POST /api/auth/phone/{send-code,verify}` (احرازشده، سقفِ نرخ)، `phone_verified` روی `MeOut`.
    تغییرِ شماره (چه از verify چه از PATCH /me) تأییدِ قبلی را باطل می‌کند. **۱۱ تستِ امنیتی/کارکردی پاس‌.**
  - **فرانت:** ویجتِ «تأیید شماره با پیامک» زیرِ فیلدِ تلفن در پروفایل — دکمه → ورودیِ کد + شمارشِ «ارسال دوباره»
    + نشانِ سبزِ «تأییدشده». موبایل‌ویو در ۳۶۰px زنده‌آزمایی شد (بی‌سرریز، خوانا).
  - **استقرار:** مهاجرت ۰۰۴۹→۰۰۵۰ روی prod و demo؛ کلیدها فقط روی **prod** (دموی عمومی عمداً بی‌کلید تا اعتبار
    تخلیه نشود). صحتِ کانفیگ + فراخوانیِ زنده‌ی اعتبار از خودِ prod تأیید شد. **فازِ بعد:** بازیابیِ رمز با پیامک،
    و پیامکِ مشتری (یادآوری/رسید) با الگوهای تازه.
  - _(نکته‌ی زیرساخت: `deploy.sh` دمو یک باگِ نهفته داشت — خطِ `ls …|head -1` بدونِ `|| true` زیرِ `pipefail`
    با انباشتِ پشتیبان‌ها SIGPIPE می‌داد؛ با هم‌ترازکردن با نسخه‌ی prod رفع شد.)_

- **۱۴۰۵/۰۵/۱۰ (2026-08-01) — رفعِ باگِ موبایلِ صندوق فروشگاهی:** کارت‌های `.pos-grid` روی موبایلِ ≤۴۱۳px از
  لبه‌ی چپ بیرون می‌زدند (کارت `left=-46` در عرضِ ۳۶۰). علت: آیتم‌های گرید `min-width:0` نداشتند؛ چون گریدِ
  تک‌ستونه min-contentِ آیتم‌ها را share می‌کند، حداقلِ ~۳۹۲px یک کارت هر دو کارت را به همان عرض می‌کشید و در
  RTL به چپ سرریز می‌کرد. با افزودنِ `.pos-grid > *` به قاعده‌ی مشترکِ `min-width:0` (کنارِ workspace-split/
  stat-grid) حل شد. زنده‌آزمایی با مرورگرِ headless روی دمو در ۳۶۰/۳۹۰/۴۰۲px: بی‌سرریز. مستقر روی prod و demo.

- **۱۴۰۵/۰۵/۱۰ (2026-08-01) — ارتقای حرفه‌ای ماژولِ اتصال فروشگاه (فقط فرانت، بدونِ تغییرِ بک‌اند):**
  - **بنرِ وضعیتِ اتصال + کارت‌های KPI:** وضعیت (تنظیم‌نشده/غیرفعال/فعال‌ومتصل) از روی تنظیمات مشتق می‌شود؛
    کارت‌های «کل کالاها / نگاشت‌شده / بدون‌نگاشت / وضعیت» دیدِ یک‌نگاهی می‌دهند.
  - **ستونِ وضعیت + فیلتر + جستجو:** هر کالا نشانِ «متصل/بدون‌نگاشت» می‌گیرد؛ فیلترِ (همه/نگاشت‌شده/بدون‌نگاشت)
    و جستجو، مدیریتِ نگاشتِ ده‌ها کالا را شدنی می‌کند.
  - **نتیجهٔ هم‌گام‌سازی ساختارمند:** کارت‌های شمارشیِ (وارد‌شده/رد‌شده/به‌روزشده/ناموفق) به‌جای یک خطِ متن،
    به‌علاوه‌ی فهرستِ دلایلِ رد/ناموفق. دکمهٔ sync فقط در حالتِ «فعال» فعال می‌شود.
  - **موبایل‌ویو:** جدولِ نگاشتِ کالا در ≤۷۶۰px کارتِ برچسب‌دار می‌شود. **بک‌اند بی‌تغییر.** مستقر روی prod و demo.

- **۱۴۰۵/۰۵/۱۰ (2026-08-01) — ارتقای حرفه‌ای ماژولِ فروش اقساطی (فقط فرانت، بدونِ تغییرِ بک‌اند):**
  - **نوارِ پیشرفتِ وصول:** در لیستِ قراردادها (ستونِ «پیشرفت») و سرِ جدولِ زمان‌بندی — پرداخت‌شده ÷ تسهیم‌شده،
    با درصد و متنِ «X از Y ریال وصول شده · سررسید بعدی».
  - **فیلترِ قراردادها:** همه/فعال/دارای معوق/تسویه‌شده/لغوشده با شمارش.
  - **پیش‌نمایشِ تسهیم در فرمِ ساخت:** مبلغِ تسهیم‌شده، مبلغِ هر قسط و قسط آخر پیش از ثبت — دقیقاً همان منطقِ
    سرور (کف(financed÷n) و باقی‌مانده به آخری) تا کاربر قبلِ ثبت رقم را ببیند.
  - **چاپِ جدولِ اقساط:** نمای مستقلِ چاپی با مشخصاتِ قرارداد + خلاصه‌ی وصول + همه‌ی اقساط، برای نسخه‌ی مشتری
    (بدونِ نیاز به اندپوینتِ سرور).
  - **موبایل‌ویو:** جدول‌های قرارداد و زمان‌بندی در ≤۷۶۰px کارتِ برچسب‌دار می‌شوند؛ ردیفِ پرداختِ درجا تمام‌عرض
    می‌ماند. **بک‌اند بی‌تغییر.** مستقر روی prod و demo.

- **۱۴۰۵/۰۵/۱۰ (2026-08-01) — ارتقای حرفه‌ای ماژولِ تولید و بهای تمام‌شده (فقط فرانت، بدونِ تغییرِ بک‌اند):**
  - **تحلیلِ بها و حاشیهٔ سودِ فرمول:** لیستِ فرمول‌ها حالا **بهای تمام‌شده‌ی هر واحد** را از میانگینِ موزونِ
    اجزا (÷ بازده) نشان می‌دهد و در برابرِ **قیمتِ فروشِ محصول**، **حاشیهٔ سود** (مبلغ + درصد) را با نشانِ رنگی —
    قلبِ «بهای تمام‌شده» که تا امروز فقط هنگامِ ثبتِ تولید دیده می‌شد.
  - **ویرایشِ فرمول + تعویضِ فعال/غیرفعال:** `PATCH /boms/{id}` در بک‌اند بود ولی UI فقط ساخت/حذف داشت. حالا فرمِ
    ساخت دوکاره است (ساخت/ویرایش)؛ محصولِ فرمول در حالتِ ویرایش قفل است (تغییرِ محصول = فرمولِ تازه).
  - **برگهٔ بهای تمام‌شده (`ProductionCostDrawer`):** با کلیک روی هر سفارشِ تولید، ریزِ مصرفِ هر جزء (مقدار ×
    بهای واحد)، جمعِ مواد، سربار و بهای هر واحد — همان اعدادِ اسنپ‌شات‌شده در `production_order_lines` در لحظه‌ی تولید.
  - **موبایل‌ویو:** جدول‌های فرمول/سفارش/برگهٔ بها در ≤۷۶۰px کارتِ برچسب‌دار می‌شوند. **بک‌اند بی‌تغییر.** مستقر روی prod و demo.

- **۱۴۰۵/۰۵/۱۰ (2026-08-01) — ارتقای حرفه‌ای ماژولِ تقویم و یادآوری (فقط فرانت، بدونِ تغییرِ بک‌اند):**
  - **تبِ «کارهای امروز» (`ReminderCenterPanel`) — قلبِ «یادآوری»:** نمای کاملِ `/api/alerts` که تا امروز
    فقط به‌صورتِ خلاصه در «نمای کلی» بود و در خودِ ماژولِ تقویم‌ویادآوری غایب. چک‌های سررسید، اقساط و
    مطالباتِ معوق، عبور از سقفِ اعتبار، اسنادِ تکرارشونده‌ی سررسیدشده، یادآوری‌های تقویم و موجودیِ منفی —
    یک‌جا، با کارت‌های شمارشِ شدت (فوری/هشدار/اطلاع)، چیپ‌های فیلترِ دسته و فهرستِ رنگ‌بندی‌شده بر پایه‌ی فوریت.
    (چکِ زنده روی دمو: ۱۲ آیتمِ واقعی — ۲ چک + ۱۰ مطالباتِ معوق.)
  - **رفعِ باگِ نهفته:** دسته‌ی `installment` به `AlertCategory` و `CAT_ICON` افزوده شد؛ پیش از این اگر قسطی معوق
    می‌شد، پنلِ هشدارِ «نمای کلی» با آیکونِ `undefined` می‌شکست.
  - **موبایل‌ویو:** جدولِ فهرستِ رویدادها در ≤۷۶۰px کارتِ برچسب‌دار می‌شود؛ شبکه‌ی ماه جمع‌وجورتر (خانه‌های
    کوتاه‌تر، چیپِ ریزتر) تا در ۳۶۰px خفه نشود. **بک‌اند بی‌تغییر.** مستقر روی prod و demo.

- **۱۴۰۵/۰۵/۱۰ (2026-08-01) — ارتقای حرفه‌ای ماژولِ گزارش‌ها (فقط فرانت، بدونِ تغییرِ بک‌اند):**
  - **بازه‌ی زمانیِ مشترک — رفعِ نقصِ اصلی:** تا امروز همه‌ی ۱۴ گزارش «از ابتدا تا امروز» بودند و
    هیچ فیلترِ دوره‌ای نداشتند، با آنکه بک‌اند از `date_from`/`date_to`/`as_of` کاملاً پشتیبانی می‌کرد.
    حالا نوارِ مشترکِ بازه (از ابتدا / این ماه / این فصل / امسال / بازه‌ی دلخواهِ شمسی) بالای گزارش‌هاست و
    با تغییرش گزارشِ فعال خودکار دوباره بار می‌شود. گزارش‌های لحظه‌ای (ترازنامه/موجودی/سنی) `as_of = پایانِ بازه`.
  - گسترشِ پنج fetcher (`trial-balance`، `income-statement`، `balance-sheet` با `as_of`، `general-ledger`،
    `inventory` با `as_of`) برای پذیرشِ تاریخ؛ بقیه از قبل می‌پذیرفتند.
  - **کارت‌های خلاصه (KPI):** سود و زیان (درآمد/هزینه/سود خالص)، ترازنامه (+نشانِ «تراز است/نامتوازن»)،
    جریان نقد (ابتدا/تغییر/انتها)، صورت‌حساب، دفتر کل و کاردکس (ابتدا/جمع/انتها).
  - **موبایل‌ویو:** همه‌ی جدول‌های چندستونی در ≤۷۶۰px کارتِ برچسب‌دار می‌شوند
    (تراز/دفترکل/صورت‌حساب/کاردکس/سنی/موجودی/بودجه/مرکزهزینه/فصلی)؛ جدول‌های دوستونی ذاتاً جا می‌شوند. **بک‌اند بی‌تغییر.** مستقر روی prod و demo.

- **۱۴۰۵/۰۵/۱۰ (2026-08-01) — ارتقای حرفه‌ای ماژولِ حقوق و دستمزد (فقط فرانت، بدونِ تغییرِ بک‌اند/مهاجرت):**
  - **تنظیماتِ حقوق (`PayrollSettingsPanel`) — رفعِ نقصِ بحرانی:** بک‌اند تا وقتی نرخِ بیمه/مالیاتِ واقعیِ سال ثبت نشده باشد
    صدورِ فیش را مسدود می‌کرد، ولی **هیچ رابطی برای ثبتِ این تنظیمات نبود** (ماژول از UI قابلِ تکمیل نبود). حالا ویرایشگرِ
    نرخِ بیمه‌ی سهمِ کارمند/کارفرما، معافیتِ مالیاتیِ سالانه و **پلکانِ مالیاتِ ویرایش‌پذیر** (افزودن/حذفِ ردیف؛ ردیفِ آخر
    خودبه‌خود نامحدود) + دکمه‌ی «بارگذاریِ ارقامِ نمونه» و نشانِ آماده/ناقصِ سال. (`GET`/`PUT /api/payroll-settings` از قبل بود؛
    فقط `fetchPayrollSettings`/`upsertPayrollSettings` اضافه شد.)
  - **فیشِ حقوقیِ چاپی (`PayslipDrawer`):** ریزِ کاملِ درآمد/کسورات (پایه/مزایا/اضافه‌کاری/ناخالص/بیمه/مالیات/خالص) با دکمه‌ی
    چاپ که یک نمای مستقلِ چاپی می‌سازد (بدونِ نیاز به اندپوینتِ سرور). نامِ کارمند به جدولِ فیش‌ها اضافه شد.
  - **افزودنِ فیلدِ «سایر مزایا» به فرمِ حکمِ حقوقی** (فیلدِ `other_allowance` از قبل در بک‌اند بود، در فرم نبود).
  - **موبایل‌ویو:** جدول‌های پرسنل/کارکرد/فیش/مزایا/پلکانِ مالیات در ≤۷۶۰px کارتِ برچسب‌دار می‌شوند (بی‌اسکرولِ افقی، تأیید ۳۹۰/۳۶۰px).
  - **بک‌اند بی‌تغییر (۸۶۲ سبز).** tsc پاک. مستقر روی prod و demo (نسخه `0049`).

- **۱۴۰۵/۰۵/۱۰ (2026-08-01) — ارتقای حرفه‌ای ماژولِ باشگاه مشتریان (فقط فرانت، بدونِ تغییرِ بک‌اند):**
  - **قیفِ فروشِ بصری:** نوارِ خلاصه‌ی مرحله‌ای (تعداد + ارزشِ تخمینیِ هر مرحله) بالای سرنخ‌ها، رنگ‌بندی‌شده به تفکیکِ وضعیت.
  - **آگاهی از سررسید:** نشانِ «عقب‌افتاده» روی سرنخ/پیگیریِ سررسیدشده (`next_action_date`/`activity_date < today`)؛
    فیلترِ پیگیری‌ها (باز/عقب‌افتاده/همه).
  - **تاریخچه‌ی امتیازِ مشتری (`LoyaltyHistoryDrawer`):** گردشِ کسب/مصرفِ امتیاز با ماندهٔ در حال اجرا، از
    `GET /api/crm/loyalty/transactions?contact_id=` (اندپوینت از قبل بود؛ فقط `fetchLoyaltyTransactions` اضافه شد).
  - جدول‌های سرنخ/پیگیری/امتیاز در ≤۷۶۰px کارتِ برچسب‌دار می‌شوند. **بک‌اند بی‌تغییر (۸۶۲ سبز).** مستقر روی prod و demo (نسخه `0049`).

- **۱۴۰۵/۰۵/۱۰ (2026-08-01) — ارتقای حرفه‌ای ماژولِ اشخاص (فقط فرانت، بدونِ تغییرِ بک‌اند):**
  - **صورت‌حسابِ طرف حساب درجا (`ContactStatementDrawer`):** کارتِ حساب مشتری/تأمین‌کننده — همه‌ی فاکتورها،
    دریافت/پرداخت و چک‌ها با ماندهٔ در حال اجرا (بدهکار/بستانکار)، با استفاده‌ی دوباره از `GET /api/reports/contact-statement/{id}`.
  - **ماندهٔ حساب + وضعیتِ اعتبار در لیست:** ستونِ «مانده» و نشانِ «فراتر از سقف»، از گزارشِ سنین
    (`fetchAging` مطالبات + بدهی؛ نگاشتِ contact_id→مانده). KPIهای «مطالباتِ باز» و «بدهی به تأمین‌کنندگان».
  - **تبِ سنینِ مطالبات (`AgingPanel`):** جاری/۳۱–۶۰/۶۱–۹۰/۹۰+ با تعویضِ مطالبات↔بدهی و جمعِ کل.
  - جدول‌های اشخاص/دریافت‌پرداخت/سنین در ≤۷۶۰px کارتِ برچسب‌دار می‌شوند. **بک‌اند بی‌تغییر (۸۶۲ سبز).** مستقر روی prod و demo (نسخه `0049`).

- **۱۴۰۵/۰۵/۱۰ (2026-08-01) — ارتقای حرفه‌ای ماژولِ چک و بانک (بدونِ مهاجرت):**
  - **حساب‌های بانکی — مدیریت + کارتِ حساب:** `PATCH /api/bank-accounts/{id}` (نام/بانک/شماره/شبا/فعال‌بودن).
    `BankAccountsPanel` — ساخت/ویرایش/فعال‌سازی + دکمه‌ی «کارتِ حساب» که با استفاده‌ی دوباره از `AccountLedgerDrawer`،
    گردش و موجودیِ **دفترِ کلِ** حسابِ متناظر را نشان می‌دهد (شاملِ دریافت/پرداختِ خزانه و چک‌ها — نه فقط واریز/برداشتِ دستی؛
    چون treasury رکوردِ BankTransaction نمی‌سازد، دفترِ کل مرجعِ درستِ موجودی است).
  - **تنخواه‌گردان — تبِ مستقل (`PettyCashPanel`):** موجودیِ زنده + فرم‌های شارژ/هزینه + دفترچه‌ی گردش با ماندهٔ در حال اجرا
    (اندپوینت‌های `/api/petty-cash/balance` و `/api/petty-cash` از قبل بودند ولی UI نداشتند).
  - **لیستِ چک‌ها حرفه‌ای‌تر:** فیلترِ نوع/فقط‌باز، نشانِ «سررسید گذشته»/«نزدیکِ سررسید»، و کارتِ برچسب‌دار روی موبایل.
  - `BankAccountUpdateIn` تازه؛ `BankingPanel` قدیمی حذف شد. **۳ تست جدید، ۸۶۲ کل سبز.** مستقر روی prod و demo (نسخه `0049`).

- **۱۴۰۵/۰۵/۱۰ (2026-08-01) — ارتقای حرفه‌ای ماژولِ حسابداری (مهاجرت `0049`):**
  - **چارتِ حساب‌های قابلِ مدیریت:** تا امروز فقط‌خواندنی بود. `POST/PATCH/DELETE /api/accounts` (ساختِ
    زیرحساب/سرفصل با نوعِ ارث‌بری‌شده از والد و کدِ یکتا؛ تغییرِ نام/فعال‌بودن؛ حذفِ فقط بی‌سند/بی‌زیرحساب/غیرسیستمی).
    حسابِ نقش‌دار (`system_role`) محافظت می‌شود. UI: `ChartOfAccountsPanel` + `AccountLedgerDrawer` (کارتِ حساب/دفتر کل درجا).
  - **دفتر روزنامه + ابطالِ سند:** `POST /api/journal-entries/{id}/void` (نمایان‌کردنِ `void_journal_entry`؛ فقط
    سندِ دستی، با سندِ معکوس). UI: `JournalDaybookPanel` — فهرستِ اسناد با جزئیاتِ باز/بسته‌شونده، نشانِ باطل/برگشتی، و ابطال.
  - `AccountOut` += `is_active`/`system_role`؛ `JournalEntryOut` += `voided_at`/`reverses_entry_id`.
  - جدول‌های تازه در ≤۷۶۰px کارتِ برچسب‌دار می‌شوند (موبایل‌پسند). **۱۳ تست جدید، ۸۵۹ کل سبز.** مستقر روی prod و demo (نسخه `0049`).

- **۱۴۰۵/۰۵/۰۹ (2026-07-31) — ارتقای حرفه‌ای ماژولِ انبار (مهاجرت `0048`):**
  - **نقطه‌ی سفارش + هشدارِ کسری:** ستونِ `items.reorder_point`؛ فیلد در فرمِ کالا؛ اندپوینتِ
    `GET /api/stock/low` (کالاهای فعالِ غیرخدماتی با موجودیِ کل ≤ نقطه‌ی سفارش، مرتب بر بیشترین کمبود)؛
    تبِ «نیازمندِ سفارش»، KPI و نشانِ ردیف در جدولِ موجودی.
  - **ارزشِ ریالیِ موجودی:** `GET /api/stock` حالا `unit_cost` و `stock_value` (موجودی × میانگین موزون)
    می‌دهد؛ KPIِ «ارزشِ موجودی» و ستون‌های بها/ارزش در جدول (جمعِ ارزش با ماندهٔ حسابِ «موجودی کالا» هم‌خوان).
  - **کاردکسِ درجا:** `KardexDrawer` — با کلیک روی هر ردیفِ موجودی/کسری، تاریخچه‌ی ورود/خروج + مانده و
    خلاصه (از همان `GET /api/reports/kardex/{item}`) در درایورِ لغزان باز می‌شود.
  - **مدیریتِ انبارها:** `PATCH /api/warehouses/{id}` (نام/فعال‌بودن؛ کد ثابت)؛ تبِ «انبارها» با ساخت/
    تغییرِ نام/فعال‌سازی.
  - **UI:** `pages/InventoryPage.tsx` + کامپوننت‌های تازه `KardexDrawer`/`LowStockPanel`/`WarehousesPanel`؛
    جدول‌های تازه در ≤۷۶۰px کارتِ برچسب‌دار می‌شوند (موبایل‌پسند). **۸ تست جدید، ۸۴۶ کل سبز.** مستقر روی prod و demo (نسخه `0048`).

- **۱۴۰۵/۰۵/۰۹ (2026-07-31) — رفعِ موبایلِ صندوق فروشگاهی (بدونِ مهاجرت):** سبدِ خرید در ≤۸۶۰px کارتِ برچسب‌دار
  می‌شود (نامِ کالا سرِ کارت، تعداد/قیمت/جمع برچسب‌دار، حذفِ تمام‌عرض)؛ نوارِ ابزار روی موبایل تمام‌عرض؛ بدونِ اسکرولِ افقی.

- **۱۴۰۵/۰۵/۰۹ (2026-07-31) — فعالیتِ کاربران در پنلِ مدیریت (مهاجرت `0047`):**
  - **ستونِ `users.last_login_at`** (nullable) که `security.record_login` در سه مسیرِ اعتبارسنجی‌شده پرش می‌کند:
    `/api/auth/login`، `/api/auth/reset-password`، و `members.accept_invite`. NULL = هرگز وارد نشده.
  - **`AccountRowOut` سه فیلد گرفت:** `owner_last_login_at`، `last_activity_at` (بیشینه‌ی ورودِ همه‌ی کاربرانِ اکانت)،
    و `users[]` (نام، ایمیل، وضعیتِ عضویت، `is_owner`، `last_login_at` — مالک اول، سپس تازه‌ترین ورود).
  - **UI (`AccountsAdminPage`):** خطِ «آخرین فعالیت» روی هر کارت (نسبی + شمسی)، KPIِ تازه‌ی **«فعال در ۷ روزِ اخیر»**،
    و بخشِ باز/بسته‌شونده‌ی **«کاربرها»** با آخرین ورودِ هر کاربر. **۲ تست جدید، ۸۳۸ کل سبز.** مستقر روی prod و demo (نسخه `0047`).

- **۱۴۰۵/۰۵/۰۹ (2026-07-31) — ماژولِ «مدیریت اکانت‌ها» (فقط سوپرادمین، بدونِ مهاجرت):**
  - **گیتِ تازه‌ی super-admin:** env `SUPER_ADMIN_EMAILS` (پیش‌فرضِ کد = `acc.cubita@gmail.com`) —
    سخت‌گیرانه‌تر از `PLATFORM_ADMIN_EMAILS`. `require_super_admin` در `deps.py`. `is_super_admin`
    به `/me` اضافه شد؛ آیتمِ ساید‌بار و صفحه فقط برای همین ایمیل. **ipnetcity (platform-admin) این ماژول را نمی‌بیند.**
  - **اندپوینت‌ها** (`routers/admin_accounts.py`، سرویس `services/admin_accounts.py`، prefix `‎/api/admin/accounts`):
    فهرستِ همه‌ی اکانت‌ها (نام، مالک، وضعیتِ اشتراک، تاریخِ انقضا + **روزهای مانده**، پلن، کاربر) روی جدول‌های
    **سراسری**؛ ساختِ دستیِ اکانت (`signup_new_business` + `subscriptions.grant`) با رمزِ اولیه؛ تمدید/تعیینِ
    انقضا؛ تعلیق/فعال‌سازی؛ بازنشانیِ رمزِ مالک (`set_password` → نشست‌ها باطل)؛ حذفِ کامل (`purge_tenant`).
    گاردها: مدیر نمی‌تواند اکانتِ خودش را تعلیق/حذف کند.
  - **تعلیق حالا در `get_principal` هم اعمال می‌شود** — کسب‌وکارِ `status != active` حتی با توکنِ معتبر رد می‌شود
    (پیش‌تر فقط جلوی ورودِ تازه گرفته می‌شد).
  - **UI:** `pages/AccountsAdminPage.tsx` — کارتِ هر اکانت (موبایل‌پسند)، KPI (کل/فعال/رو‌به‌انقضا/منقضی‌ـتعلیق)،
    فرمِ ساخت، و اقدام‌های تمدید (پیش‌فرض‌های +۳۰/۹۰/۱۸۰/۳۶۵)/تعلیق/رمز/حذف با تأییدِ قوی. **۱۳ تست جدید، ۸۳۶ کل سبز.**

- **۱۴۰۵/۰۵/۰۹ (2026-07-31) — ارتقای ماژولِ خرید (آینه‌ی فروش، مهاجرت `0046`):**
  - **P۱ کمبوباکسِ کالا** (`ItemPicker`) در فرمِ خرید + **حالتِ کارتیِ موبایل**: روی `≤۶۴۰px` هر ردیفِ
    فاکتور (فروش/خرید/پیش‌فاکتور/برگشت‌ها) به کارتِ برچسب‌دار (`data-label`) تبدیل می‌شود — هر فیلد یک خط،
    به‌جای جدولِ فشرده/اسکرولِ افقی. پاپ‌آورِ `ItemPicker` هم روی موبایل عرضِ خوانا (≥۲۶۰px، چسبیده به
    لبه‌ی راستِ دکمه، داخلِ صفحه، ارتفاعِ محدود) گرفت. → [[mobile-view-rules]]
  - **P۲ خلاصه‌ی خرید سمت‌سرور:** `GET /api/purchase-invoices/summary` (قرینه‌ی فروش، بدون سود) + KPIِ صفحه‌ی خرید.
  - **P۳ تخفیفِ کلِ فاکتور خرید (مهاجرت `0046`):** ستونِ `invoice_discount` روی `purchase_invoices`؛ همان
    تسهیمِ فروش — اثر بر ارزش‌گذاریِ موجودی (میانگین موزون) و اعتبارِ مالیاتی؛ در فرم مبلغی/درصدی. **رند
    عمداً نیست** (فاکتور خرید عیناً از سندِ تأمین‌کننده ثبت می‌شود).
  - **P۴ برگشت خرید کامل‌تر:** `GET /api/purchase-invoices/{id}/returnable` + نمایشِ باقی‌مانده در فرم +
    ستونِ فاکتور اصلی + چاپِ سندِ برگشت (`GET /api/purchase-returns/{id}/print`).
  - **P۵ رونوشتِ فاکتور خرید:** دکمه‌ی رونوشتِ `InvoiceList` به خرید هم تعمیم یافت + پیش‌پرشدنِ فرمِ خرید.
  - **کیفیت:** **۸۲۳ تستِ بک‌اند سبز** (+۱۱ نو)، `tsc -b` و `vite build` پاک.

- **۱۴۰۵/۰۵/۰۹ (2026-07-31) — ارتقای جامعِ ماژولِ فروش (۶ مرحله، مهاجرت `0045`):**
  - **۱) کمبوباکسِ جست‌وجوی کالا (`ItemPicker`).** جایگزینِ `<select>`های بلندِ کالا در
    فاکتور فروش/پیش‌فاکتور/صندوق — جست‌وجو با نام/کد/بارکد و نرمال‌سازیِ فارسی (ی/ک عربی،
    ارقام، اعراب). پاپ‌آور با **portal + موقعیتِ fixed** رندر می‌شود تا داخلِ جدولِ اسکرول‌دار
    بریده نشود. پیمایش با کلید.
  - **۲) موجودیِ زنده در فرمِ فاکتور اصلی و صندوق.** ستونِ «موجودی انبار» با هشدارِ «بیش از
    موجودی» — کاربر پیش از ثبت کسری را می‌بیند، نه بعد از خطای ۴۰۰ (هم‌سطحِ پیش‌فاکتور).
  - **۳) نمای جزئیاتِ فاکتور + سودِ ناخالص + endpointِ خلاصه.** هر ردیفِ فهرستِ فاکتورها باز
    می‌شود و اقلام + بهای تمام‌شده + **سود و حاشیه** را نشان می‌دهد؛ ستونِ «طرف حساب» و دکمه‌ی
    «رونوشت». `GET /api/sales-invoices/summary` شاخص‌ها را **سمت سرور** جمع می‌زند (به‌جای دانلودِ
    کلِ فاکتورها؛ رفعِ کندی و شکستِ سقفِ صفحه‌بندی). کارتِ KPIِ «سود ناخالص» به صفحه‌ی فروش اضافه شد.
  - **۴) تخفیفِ کلِ فاکتور (مبلغی/درصدی) + رند کردن (مهاجرت `0045`).** ستون‌های `invoice_discount`
    و `rounding` روی `sales_invoices`. **تخفیفِ کل** هنگام ثبت به‌نسبتِ خالصِ هر ردیف **تسهیم** می‌شود
    (`_allocate_discount`، بیشترین‌باقی‌مانده) پس درآمد/مالیات/برگشت/مؤدیان همه خودکار درست می‌مانند.
    **رند** تعدیلِ پس‌ازمالیاتِ علامت‌دار است که به حسابِ نقشِ تازه‌ی `sales_rounding` می‌رود (سندِ
    مؤدیان دست‌نخورده، دقیق). فرمِ فروش و صندوق کنترلِ تخفیف (مبلغ/٪) و رند (بدون/۱٬۰۰۰/۵٬۰۰۰/۱۰٬۰۰۰
    به پایین) گرفتند؛ چاپ/PDF ردیف‌های تازه را نشان می‌دهند. ۹ تست جدید.
  - **۵) برگشت فروش کامل‌تر.** `GET /api/sales-invoices/{id}/returnable` (باقی‌ماندهٔ قابلِ برگشتِ
    هر کالا)؛ فرم حالا **باقی‌مانده** را نشان می‌دهد نه «فروخته‌شده»، با سقفِ ورودی و دکمه‌ی «همه».
    فهرستِ برگشت‌ها ستونِ «فاکتور اصلی» + دکمه‌ی **چاپ** (`GET /api/sales-returns/{id}/print`) گرفت. ۳ تست جدید.
  - **۶) رونوشتِ فاکتور.** دکمه‌ی «رونوشت» در فهرستِ فاکتورهای فروش، فرمِ بالای صفحه را با اقلامِ
    همان فاکتور به‌عنوان پیش‌نویسِ تازه پیش‌پر می‌کند (تاریخ و کلیدِ یکتاسازی نو؛ تخفیفِ سطری عیناً
    منتقل، تخفیفِ کل/رند صفر تا دوبار حساب نشود).
  - **رفعِ دو خطای تایپِ از-قبل‌مانده:** `PAGE_TITLES` دو کلیدِ `installments`/`onboarding` را نداشت
    (عنوانِ صفحه‌شان undefined می‌شد)؛ importِ بی‌استفاده در `OnboardingPage`. حالا `tsc -b` پاک است.
  - **کیفیت:** **۸۱۲ تستِ بک‌اند سبز** (+۱۷ نو)، `tsc -b` و `vite build` پاک.

- **۱۴۰۵/۰۵/۰۹ (2026-07-31):**
  - **چاپِ مستقلِ پیش‌فاکتور + رفعِ توضیحاتِ خودکار + خدمات/موجودی (بدونِ مهاجرت).** پیش‌فاکتور
    تا امروز **هیچ چاپی نداشت** (تنها راهِ چاپ، تبدیل به فاکتور بود که نامِ مشتریِ دستی را گم و
    توضیحاتِ خودکار اضافه می‌کرد). (۱) **چاپِ پیش‌فاکتور:** اندپوینتِ `GET /api/sales-quotations/{id}/print`
    (HTML، همان قالبِ فاکتور با عنوانِ «پیش‌فاکتور») + دکمه‌ی «چاپ» در لیستِ پیش‌فاکتورها. نامِ مشتری =
    طرف‌حسابِ انتخاب‌شده، وگرنه نامِ دستی (`customer_name`)، وگرنه «مشتری»؛ توضیحاتِ خالی → فقط
    «پیش‌فاکتور» بدونِ جمله‌ی پیش‌فرض. (۲) **تبدیل به فاکتور** دیگر جمله‌ی «از پیش‌فاکتور شماره X» را
    به توضیحات نمی‌چسباند — فقط توضیحاتِ خودِ کاربر منتقل می‌شود (ردیابی در `converted_invoice_id`).
    (۳) **کالای خدماتی:** در فرمِ پیش‌فاکتور، اگر کالا خدماتی باشد ستونِ «موجودی انبار» برایش
    «خدمات (بدون موجودی)» نشان می‌دهد نه عدد. ۴ تست جدید، **۷۹۵ کل سبز**.
  - **بهبودِ فرمِ پیش‌فاکتور (مهاجرت `0044`).** سه قابلیتِ درخواستیِ کاربر: (۱) **انتخابِ مشتری**
    — کلیدِ دوحالته «از فهرستِ اشخاص / دستی»؛ حالتِ فهرست از طرف‌حساب‌ها (زنده، آفلاین پنهان)،
    حالتِ دستی نامِ آزاد در ستونِ تازه‌ی `customer_name` (وقتی طرف‌حساب انتخاب شده، نامِ دستی
    نادیده و null می‌شود). (۲) **قیمت از انبار یا دستی** — با انتخابِ کالا، قیمتِ واحد خودکار از
    `sales_price`ِ همان کالا پر می‌شود و دکمه‌ی «قیمتِ انبار» بازنشانی می‌کند؛ قابلِ ویرایشِ دستی.
    (۳) **موجودی از انبار یا دستی** — کلیدِ دوحالته؛ در حالتِ «از انبار» موجودیِ در دسترسِ هر کالا
    در انبارِ انتخاب‌شده نمایش داده می‌شود (از `/api/stock`)، با هشدارِ «بیش از موجودی» و دکمه‌ی
    «استفاده» برای پر کردنِ تعداد با موجودی؛ حالتِ دستی این ستون را پنهان می‌کند. لیستِ پیش‌فاکتورها
    ستونِ «مشتری» گرفت (نامِ طرف‌حساب یا نامِ دستی). ۴ تست جدید (`test_quotations.py`)، **۷۹۱ کل سبز**.
  - **واحدِ پول تومان → ریال (فقط برچسب، بدونِ تغییرِ داده).** به‌خواستِ کاربر و برای سازگاری با
    فاکتورهای چاپی که **از قبل ریال** بودند، همه‌ی برچسب‌های «تومان» در برنامه به «ریال» تغییر کرد
    (۲۵ مورد در ۱۱ فایلِ فرانت). هیچ عددی در پایگاه‌داده تغییر نکرد (گزینه‌ی انتخابیِ کاربر: «فقط
    برچسب‌ها»). **استثنا:** پنلِ ادمینِ پلتفرم (`PurchasesAdminPanel` — فروشِ اشتراکِ خودِ کوبیتا)
    عمداً تومان ماند چون مبالغش واقعاً تومان‌اند و به منطقِ ×۱۰ زرین‌پال گره خورده‌اند.
  - **واحدِ کالا قابلِ انتخاب + محاسبه‌ی هر-واحد.** در «انبار → کالاها» واحد از ورودیِ متنی به
    **کشویی** با ۱۹ واحدِ رایج (عدد/متر/متر مربع/کیلوگرم/لیتر/…) + گزینه‌ی «سایر (دستی)» تبدیل شد.
    برچسبِ قیمت حالا «قیمت فروش (ریال، هر <واحد>)» است و در ردیف‌های فاکتورِ فروش/خرید و سبدِ صندوق
    واحدِ کالا کنارِ تعداد نشان داده می‌شود؛ تعدادِ **اعشاری** (مثلاً ۲٫۵ متر) همه‌جا کار می‌کند
    (`step="any"` در فروش/خرید از قبل بود، به صندوق هم اضافه شد). فقط فرانت، بدونِ مهاجرت.

- **۱۴۰۵/۰۵/۰۸ (2026-07-29):**
  - **ماژولِ تازه: فروش اقساطی (مهاجرت `0043`).** دو جدولِ مستأجرمحور با RLS
    (`installment_plans`, `installments`) + شمارنده‌ی `installment_plan` (بک‌فیل برای مستأجرهای
    موجود). قرارداد **فقط زمان‌بندیِ وصول** است؛ بدهی از فاکتورِ نسیه می‌آید و هر پرداختِ قسط
    یک **دریافتِ خزانه‌ی واقعی** (`create_receipt`) می‌سازد که مانده‌ی دریافتنی را کم می‌کند —
    پس حسابداری دوباره‌کاری ندارد. زمان‌بندی: مبلغِ تأمین‌مالی (کل − پیش‌پرداخت) به N قسط تقسیم
    می‌شود (باقیمانده روی قسطِ آخر)، سررسیدها با **ماهِ شمسی** جلو می‌روند (`add_jalali_months`
    در `jalali.py`، روزِ ماه حفظ می‌شود، ۳۱→۳۰/۲۹). وضعیتِ هر قسط (در انتظار/جزئی/پرداخت‌شده/
    معوق) از `paid_amount`/`due_date` مشتق می‌شود (بدونِ زمان‌بندِ پس‌زمینه). گاردها: پرداختِ
    بیشتر از باقیمانده رد (۴۰۰)، فقط قراردادِ فعال پرداخت می‌گیرد، دوره‌ی بسته رعایت می‌شود.
    اقساطِ معوق به **مرکز هشدارها** اضافه شدند (`category: installment`). اندپوینت‌ها:
    `GET/POST /api/installment-plans`، `/{id}/installments/{iid}/pay`، `/{id}/cancel`. فرانت:
    صفحه‌ی «فروش اقساطی» با KPI + فرمِ قرارداد + فهرست + زمان‌بندیِ اقساط با پرداختِ درون‌خطی.
    ۷ تست جدید (`test_installments.py`)، **۷۸۷ کل سبز**. RLS ۶۴→۶۶ جدول. مستقر روی prod و demo.
  - **ماژولِ تازه: راه‌اندازی و ورودِ اطلاعات (بدونِ مهاجرت).** بزرگ‌ترین اصطکاکِ مهاجرتِ یک
    کسب‌وکار از نرم‌افزارِ قبلی را حذف می‌کند. روترِ `onboarding` با سه ابزار: (۱) **ورودِ گروهیِ
    کالا** (`POST /api/import/items`) و (۲) **ورودِ گروهیِ اشخاص** (`/import/contacts`) — از فایلِ
    CSV؛ اعتبارسنجی + ردِ تکراری (کالا با SKU، شخص با نام) + گزارشِ خطای هر ردیف. (۳) **مانده‌های
    اول دوره / سند افتتاحیه** (`/opening-balances` + `/status`): یک سندِ افتتاحیه‌ی متوازن می‌سازد؛
    موجودیِ اول دوره حرکتِ انبارِ `opening` می‌سازد، میانگینِ بها را ست می‌کند و ارزشش خودکار
    بدهکارِ «موجودی کالا» می‌شود؛ اختلافِ تراز به حسابِ سرمایه بسته می‌شود. **گاردها:** فقط یک
    سند افتتاحیه مجاز است (۴۰۹ برای دومی)، دوره‌ی بسته رعایت می‌شود، نامتوازنِ بدونِ حسابِ تراز
    رد می‌شود (۴۰۰). فرانت: صفحه‌ی «راه‌اندازی» با سه تب + قالبِ نمونه‌ی CSV + پیش‌نمایش +
    گزارشِ نتیجه؛ `parseCsv` تازه در `lib/csv.ts`. بدونِ جدولِ تازه (از `journal_entries`/`stock_ledger`
    استفاده می‌کند). ۵ تست جدید (`test_onboarding.py`)، **۷۷۰ کل سبز**. مستقر روی prod و demo.
  - **ماژولِ تازه: گزارشِ معاملاتِ فصلی (ماده ۱۶۹ ق.م.م) — مهاجرت `0042`.** الزامِ قانونیِ
    اکثرِ کسب‌وکارهای ایرانی که رقبا دارند و ما نداشتیم. چهار ستونِ هویتیِ اختیاری روی `contacts`
    (`entity_type` حقیقی/حقوقی، `national_id` کد/شناسه‌ی ملی، `economic_code` کد اقتصادی،
    `postal_code`) بدونِ جدولِ تازه (RLS همان ۶۴ جدول). سرویسِ `get_seasonal_report(year, quarter)`:
    بازه‌ی هر فصلِ **شمسی** (بهار/تابستان/پاییز/زمستان یا کلِ سال) را با `jalali_to_gregorian`
    می‌سازد و فاکتورهای فروش/خرید را به تفکیکِ طرف حساب تجمیع می‌کند (خالص/تخفیف/مالیات/تعداد)؛
    فاکتورهای باطل حساب نمی‌شوند و **برگشت‌ها از طریقِ فاکتورِ اصلی از مبلغِ همان طرف حساب کسر**
    می‌شوند. فروشِ نقدیِ بدونِ طرف حساب در ردیفِ تجمیعیِ «معاملاتِ خرد» جمع می‌شود. اندپوینت
    `GET /api/reports/seasonal?year=&quarter=` (مجوزِ `accounting:view`). فرانت: فیلدهای هویتیِ
    تازه در فرمِ «اشخاص» (برچسبِ کد/شناسه‌ی ملی با نوعِ شخص عوض می‌شود) + تبِ «معاملات فصلی (م۱۶۹)»
    در گزارش‌ها با انتخاب‌گرِ سال/فصل، جدولِ فروش و خرید، و خروجیِ اکسل. **رفعِ باگ:** ستونِ
    تکراریِ `default_price_list_id` در مدلِ Contact پاک شد. ۵ تست جدید (`test_seasonal_report.py`)،
    **۷۶۵ کل سبز**. مستقر روی prod و demo.

- **۱۴۰۵/۰۵/۰۷ (2026-07-29):**
  - **ماژولِ تازه: باشگاه مشتریان / CRM (مهاجرت `0037`).** سه جدولِ مستأجرمحور با RLS:
    `crm_leads` (قیفِ فروش: new→contacted→qualified→won/lost + تبدیل به مشتری)،
    `crm_activities` (پیگیری: تماس/جلسه/یادداشت/کار روی سرنخ یا مشتری)، و
    `crm_loyalty_transactions` (امتیازِ وفاداری؛ مانده = جمعِ تراکنش‌ها). روتر/سرویس/اسکیمای
    کامل + مجوزِ `crm` (فروشنده view/create/update، حسابدار و دمو view، مالک از `*`). فرانت:
    صفحه‌ی «باشگاه مشتریان» با سه تب + آیتمِ ساید‌بار. ۷۰۴ تستِ بک‌اند سبز (شاملِ ایزوله‌سازیِ
    سه جدولِ تازه + drift). مستقر روی prod و demo (بک‌اند مهاجرت‌شده + باندلِ وب).
  - **ماژولِ دوم: صندوق فروشگاهی / POS + بارکد (مهاجرت `0038`).** ستونِ `barcode` روی
    `items` + اندپوینتِ `GET /api/items/by-barcode` + فیلدِ بارکد در فرمِ کالاها. صفحه‌ی
    «صندوق فروشگاهی»: اسکنِ بارکد → سبد (تعداد/قیمتِ ویرایش‌پذیر) → تسویه با محاسبه‌ی
    باقی‌مانده. از همان منطقِ فاکتورِ فروش استفاده می‌کند (بدونِ مشتری=نقدی به صندوق؛ با
    مشتری=نسیه به AR)، پس حسابداری/انبار/بهای‌تمام‌شده دوباره‌کاری ندارد. ۷۰۷ تستِ بک‌اند سبز.
    مستقر روی prod و demo.
  - **ماژولِ سوم: تولید و بهای تمام‌شده / BOM (مهاجرت `0039`).** چهار جدولِ مستأجرمحور با RLS
    (`boms`, `bom_lines`, `production_orders`, `production_order_lines`) + شمارنده‌ی
    `production_order` (بک‌فیل برای مستأجرهای موجود). منطق: اجزا با میانگینِ موزون مصرف،
    بهای هر واحدِ محصول=(بهای اجزا+سربار)/تعداد، میانگینِ محصول به‌روز؛ سند فقط برای سربار
    (بدهکار موجودی/بستانکار صندوق — چون اجزا و محصول همان حسابِ موجودی‌اند). صفحه‌ی «تولید»
    با دو تب (فرمول‌ها، تولید) + پیش‌نمایشِ زنده‌ی بها. ۷۳۲ تستِ بک‌اند سبز. مستقر روی prod و demo.
  - **ماژولِ چهارم: انبار پیشرفته (مهاجرت `0040`).** سه جدولِ مستأجرمحور با RLS: `price_lists`
    + `price_list_items` (یکتاییِ (tenant,list,item)) برای چند سطحِ قیمت، و `stock_batches`
    برای ثبتِ بچ/سری با تاریخِ انقضا و هشدارِ نزدیکِ انقضا. دو تبِ تازه در «انبار» (لیست قیمت،
    بچ و انقضا) + یکپارچگی با صندوقِ فروشگاهی (انتخابِ لیست → قیمتِ سبد از همان لیست). مجوز از
    همان `inventory`. ۷۵۰ تستِ بک‌اند سبز. مستقر روی prod و demo. **هر ۴ ماژولِ درخواستی کامل شد.**
  - **عمیق‌سازیِ ماژول‌ها (مهاجرت `0041`).** (الف) **لیستِ قیمتِ پیش‌فرضِ مشتری:**
    `contacts.default_price_list_id`؛ در صندوق با انتخابِ مشتری خودکار انتخاب می‌شود و در فاکتورِ
    فروش با انتخابِ کالا قیمتِ همان لیست پر می‌شود. (ب) **کسبِ خودکارِ امتیازِ وفاداری:** جدولِ
    `loyalty_settings` (فعال + هر N تومان=۱ امتیاز) و هوکِ امن در `post_sales_invoice` (فقط
    مشتری‌دار و فعال؛ فروشِ نقدی/غیرفعال بی‌اثر). کارتِ تنظیمات در CRM و انتخابگرِ لیست در فرمِ
    اشخاص. **رفعِ باگ:** حذفِ لیستِ قیمتِ متصل به مشتری دیگر ۵۰۰ نمی‌دهد. ۷۶۰ تستِ بک‌اند سبز.
  - **اصلاحاتِ همان روز:** رفعِ ناخوانا بودنِ باکس‌های ردیفِ فروش/خرید/سند (انتخابگرها با
    `appearance:none`)، رفعِ افتادنِ آیکونِ صفحه‌ی ورود روی متن (`:where` برای تخصیصِ صفر)،
    ساختِ حساب‌های واقعی (mohamadi/mehdi)، بازنویسیِ کاملِ صفحه‌ی راهنما، بازچینشِ صفحه‌ی
    اصلیِ سایت به سبکِ رقیب (با حفظِ پالت)، و رفعِ سالِ میلادیِ فوترِ سایت.
- **۱۴۰۵/۰۵/۰۶ (2026-07-28):**
  - **تستِ سرتاسریِ UI مثل یک حسابدار (Playwright):** کلِ برنامه روی نسخه‌ی وبِ محلی
    (بک‌اند :8000 + باندلِ استاتیک :5173، حسابِ ایزوله) خودکار پیمایش شد: ساختِ کالا، اشخاص،
    فاکتورِ خرید و فروش، سندِ دستی، دریافت، حقوق، چک، تقویم، پروفایل و همه‌ی گزارش‌ها. نتیجه:
    موتورِ حسابداری درست است (ترازِ آزمایشی دقیقاً متوازن؛ بدهکار=بستانکار=۶۲۰م، بهای
    تمام‌شده با میانگینِ موزون، تفکیکِ مالیات)، تاریخ‌ها همه‌جا شمسی، و ماژولِ حقوق با تقویمِ
    شمسیِ تازه درست کار می‌کند. دو نقص پیدا و رفع شد.
  - **رفعِ دو نقصِ کشف‌شده در تست:** (۱) ترازِ آزمایشی ردیفِ «جمع» نداشت تا توازنِ
    بدهکار/بستانکار یک‌نگاهی دیده شود — افزوده شد (نمایش + CSV؛ ستونِ مانده عمداً خالی چون
    ماندهٔ طبیعی است). (۲) شاخص‌ها و فهرستِ صفحه‌ی فروش/خرید بعد از ثبتِ فاکتور تا ترکِ صفحه
    به‌روز نمی‌شدند — حالا onQueued فهرست را دوباره می‌گیرد و بی‌درنگ تازه می‌شود. مستقر روی prod + demo.
  - **تقویمِ حقوق و دستمزد از میلادی به شمسی شد:** کلِ ماژولِ حقوق (دوره‌ها، تنظیمات،
    عیدی/سنوات/مرخصی) سال/ماه را میلادی تفسیر می‌کرد — ماه‌ها ژانویه..دسامبر و بازه‌ی عیدی
    ۱ ژانویه..۳۱ دسامبر — که برای قانونِ کارِ ایران غلط بود. حالا سال/ماه = شمسی و مرزهای
    دوره به تاریخِ میلادیِ متناظر (که در دفتر ذخیره می‌شود) تبدیل می‌شوند. ماژولِ تازه‌ی
    [backend/app/jalali.py](backend/app/jalali.py) شاملِ `jalali_to_gregorian` + کمک‌توابعِ
    مرزِ سال/ماهِ شمسی؛ درستیِ رفت‌وبرگشت روی هر روزِ ~۲۵۰ سال بی‌خطا تست شد
    ([tests/test_jalali.py](backend/tests/test_jalali.py)). `benefits`/`payroll` و UI
    (ماه‌ها فروردین..اسفند، پیش‌فرضِ سال/ماه = امروزِ شمسی، برچسب «سال (شمسی)») به‌روز شدند.
    بدونِ مهاجرت (فقط بازتفسیرِ معنایِ سال/ماه). ۶۷۹ تست سبز. مستقر روی prod + demo.
  - **رفعِ سالِ میلادیِ فوترِ سایت:** «۲۰۲۶» با `Intl.DateTimeFormat('fa-IR')` به سالِ شمسی
    («۱۴۰۵») تبدیل شد. تنها جای نمایشِ میلادی در سایت بود؛ خودِ برنامه همه‌جا از `formatJalali`
    /تقویمِ `fa-IR` استفاده می‌کند. مستقر روی `cubita.ir`.
  - **بازنویسیِ کاملِ صفحه‌ی راهنما:** از ۹ بخشِ کوتاه به ۱۵ بخشِ گام‌به‌گام با پاراگرافِ
    مقدمه‌ی روان؛ بخش‌های جاافتاده (اشخاص، تقویم، کاربران) و ویژگی‌های تازه (کالاها، پروفایل من،
    تمِ روشن/تیره) اضافه شد؛ متنِ نادرستِ «اتصال فروشگاه» (اشاره به ipnetcity.ir) عمومی شد.
  - **ممیزیِ ایزوله‌سازیِ داده:** تأیید شد ۵۳ جدولِ مستأجرمحور همگی `tenant_id`+RLS+FORCE+policy
    دارند و ۶۷۹ تست (شاملِ نشتِ رفتاریِ بین دو حساب) سبزند — هیچ داده‌ای بین حساب‌ها مشترک نیست.

- **۱۴۰۵/۰۵/۰۵ (2026-07-27):**
  - **صفحه‌ی «پروفایل من»:** ویرایشِ اطلاعاتِ حساب که قبلاً اصلاً ممکن نبود. کارتِ اطلاعاتِ
    کاربری (نام/تلفن آزادانه، ایمیل با تأییدِ رمزِ فعلی + یکتایی)، کارتِ اطلاعاتِ کسب‌وکار
    (فقط مالک: تغییرِ نام)، و تغییرِ رمز (از تبِ «کاربران» به اینجا منتقل و به کامپوننتِ
    مشترک `ChangePasswordCard` تبدیل شد). بک‌اند: `PATCH /api/auth/me` و `PATCH /api/auth/business`؛
    `MeOut` حالا `phone` دارد و از هِلپرِ `_me_out` ساخته می‌شود؛ بعد از ذخیره `me`ی فرانت
    تازه می‌شود. ۷ تست. مستقر روی prod + demo با `deploy.sh`.
  - **افزودن/ویرایش/حذفِ کالا از داخلِ برنامه (تبِ «کالاها» در انبار):** تا این تاریخ برنامه
    هیچ رابطی برای ساختِ محصول نداشت و کالاها فقط با `seed` ساخته می‌شدند (بک‌اند
    `POST /api/items` را داشت ولی کلاینت صدایش نمی‌زد). `ProductsPanel` اضافه شد:
    ثبتِ کالا/خدمات با SKU یکتا، ویرایشِ نام/قیمت، فعال/غیرفعال‌سازی، و **حذف**؛ فهرست زنده
    (مثل «اشخاص») و `onChanged`ی که کشِ سراسری را تازه می‌کند. `createItemLive`/`updateItemLive`/
    `deleteItemLive` در `api.ts`. **`DELETE /api/items/{id}`** با گاردِ SAVEPOINT: کالای
    استفاده‌شده در سند/موجودی با ۴۰۹ رد می‌شود (پیام: غیرفعال کنید)، کالای بی‌ردپا کامل پاک
    می‌شود (۳ تست). روی هر دو پلتفرم؛ مستقر روی prod + demo با `deploy.sh`.
  - **حسابِ واقعیِ ورود ساخته شد** (`mohamadi@cubita.ir`) از مسیرِ رسمیِ `POST /api/auth/signup`
    (تنانتِ کاملاً provision‌شده، نقشِ owner، اشتراکِ حالتِ «none» = دسترسیِ کاملِ نوشتن).
  - **رفعِ چند اشکالِ UI:** خوانا شدنِ انتخابگرهای ردیفِ فروش/خرید/سند (`appearance:none`
    + کارتِ سفارشی)، و رفعِ افتادنِ آیکونِ ایمیل/رمزِ صفحه‌ی ورود روی متن (پیچیدنِ زنجیره‌ی
    `:not()` سراسری در `:where()` تا تخصیصش کامپوننت‌ها را نشکند). مستقر روی prod + demo.

- **۱۴۰۵/۰۵/۰۳ (2026-07-25):**
  - **سایت معرفی:** دکمه‌ی «ورود به برنامه» در هدر → `acc.cubita.ir`؛ بازطراحیِ جذاب‌ترِ
    صفحه‌ی اصلی (بخش «چرا کوبیتا»، «مناسبِ کدام کسب‌وکارها»، تراست‌چیپ، انیمیشنِ اسکرولِ
    فقط-CSS)؛ کاهشِ تکرارِ دکمه‌ی دمو. همه روی `cubita.ir` مستقر.
  - **دموِ کامل (`seed_demo`):** دیتای دوربین‌مداربسته با یک فروشگاهِ اینترنتیِ لوازم
    دیجیتال به سبک دیجی‌کالا («دیجی‌مارکت») جایگزین شد — ۳۵ کالا/۹ دسته، ۲۰ طرف‌حساب،
    ۸ خرید، ۵۲ فروش، بانک/چک/هزینه/پرسنل؛ GL متوازن. دیتابیس ایزوله‌ی `cubita_demo`
    ری‌ست + reseed شد (با اجازه‌ی کاربر؛ روشِ ری‌ست چون journal فقط‌افزودنی و
    downgradeها ناقص‌اند در حافظه‌ی `demo-instance-reseed` ثبت شد). ورودِ دمو: `demo@cubita.ir`.
  - **بهای تمام‌شده‌ی کالا قابلِ ویرایش** (`PATCH /api/items` + ستون در `IntegrationPanel`).
  - **اتصالِ فروشگاهِ پرمستأجر (`0036`)** — تنظیماتِ اتصال از `.env`ِ سراسری به جدولِ
    `storefront_settings` هر کسب‌وکار.
  - **ممیزیِ تکمیل + فاز ۱ امنیت:** حذفِ `purchase_id` از URL نتیجه‌ی پرداخت (کمینه‌سازیِ
    افشا؛ containment نشتِ PII صورت‌حساب که هسته‌اش از قبل با `PLATFORM_ADMIN_EMAILS`
    انجام شده بود). راستی‌آزمایی شد که ایمیل (SMTP)، اجرای پرداخت، بکاپِ روزانه و تمدیدِ
    خودکارِ TLS همگی روی production فعال‌اند. ۶۶۹ تست سبز. `master` یکدست شد.

- **۱۴۰۵/۰۵/۰۲ (2026-07-24):**
  - **ماژول مالیات بر ارزش افزوده (VAT):** فاکتور فروش/خرید `tax_rate`/`tax_amount`
    گرفتند (مهاجرت `0024`)، سندِ خودکار حساب‌های `vat_payable` (2105) و
    `vat_receivable` (1107) را می‌زند، و گزارش `/api/reports/vat` خالصِ قابل‌پرداخت
    را می‌دهد. **برگشت‌ها** هم مالیات را قرینه برمی‌گردانند (مهاجرت `0025`،
    ۷ تست). فرم‌های فاکتور/برگشت و گزارش روی هر دو پلتفرم. مستقر روی demo.
  - **ماژول دارایی‌های ثابت و استهلاک:** مدل `fixed_assets` + `depreciation_entries`
    با RLS (مهاجرت `0026`)، حساب‌های `fixed_assets`(1201)/`accumulated_depreciation`
    (1202)/`depreciation_expense`(5106) در seed. سرویس استهلاکِ خط مستقیم:
    `POST /api/depreciation/run` یک سندِ دوره می‌سازد (Dr هزینه استهلاک، Cr استهلاک
    انباشته)، با سقفِ ارزش اسقاط و قیدِ یکتای (دارایی، دوره) برای جلوگیری از
    دوباره‌کاری. UI: تب «دارایی ثابت» در صفحه‌ی حسابداری با KPI + فرم + اجرای
    استهلاک + تاریخچه (`FixedAssetsPanel`). ۵ تست، ۴۶۷ کل سبز. مستقر روی demo
    (`0026`، RLS روی ۴۰ جدول). **هنوز روی production مستقر نشده و در git کامیت نشده.**
  - **ماژول بودجه‌بندی:** مدل `budget_lines` (بودجه‌ی هر حساب برای هر ماه، قیدِ
    یکتای (مستأجر، حساب، دوره)) با RLS (مهاجرت `0027`). سرویس بودجه با منطقِ
    **upsert** (ثبتِ دوباره‌ی همان حساب/ماه رقم را به‌روزرسانی می‌کند، نه ردیفِ دوم)
    و گزارشِ **بودجه در برابر عملکرد** (`GET /api/budgets/report`): برای هر حسابِ
    بودجه‌دار، رقمِ برنامه در برابر ماندهٔ واقعیِ همان بازه (از همان `_signed_balance`
    گزارش‌های دیگر)، با انحراف، درصد، و پرچمِ «مطلوب/نامطلوب» (درآمدِ بیشتر یا
    هزینه‌ی کمتر مطلوب). مجوزها از ماژول `accounting` استفاده می‌کنند (بدون تغییرِ
    نقش‌ها؛ حذف فقط برای owner). CRUD در `POST/PUT/DELETE /api/budgets`. UI: تب
    «بودجه‌بندی» در صفحه‌ی حسابداری با انتخاب‌گر ماه/سالِ شمسی + KPI + فهرست
    (`BudgetPanel`)، و تبِ «بودجه در برابر عملکرد» در گزارش‌ها. روی هر دو پلتفرم.
    ۶ تست جدید (`test_budgeting.py`)، **۴۷۸ کل سبز**. مستقر روی demo (`0027`،
    RLS روی ۴۱ جدول). **هنوز روی production مستقر نشده و در git کامیت نشده.**
  - **ماژول صورت جریان وجوه نقد:** گزارشِ افزودنی (بدون تغییر دیتابیس) به روشِ
    **مستقیم از دفتر** — `GET /api/reports/cash-flow`. حساب‌های نقد = نقش‌های
    `cash`/`bank`/`petty_cash` به‌علاوه‌ی `gl_account_id`ِ هر حساب بانکی. برای هر
    سندی که خطِ نقد دارد، تغییرِ نقد = جمعِ (بستانکار−بدهکار)ِ خطوطِ غیرنقدِ همان
    سند، و هر خطِ غیرنقد به فعالیتِ خودش طبقه‌بندی می‌شود: **سرمایه‌گذاری** (دارایی
    ثابت/گروه ۱۲)، **تأمین مالی** (حقوق صاحبان سرمایه و بدهیِ بلندمدتِ گروه ۲۲)،
    **عملیاتی** (بقیه). جمعِ سه فعالیت دقیقاً با تغییرِ ماندهٔ نقد آشتی می‌کند
    (`net_change == closing − opening`) — همین صحت را تضمین می‌کند. انتقالِ داخلیِ
    نقد (بانک↔تنخواه) خودبه‌خود صفر می‌شود. UI: تبِ «جریان وجوه نقد» در گزارش‌ها با
    سه بخشِ فعالیت + ماندهٔ ابتدا/انتها. روی هر دو پلتفرم. ۵ تست جدید
    (`test_cash_flow.py`)، **۴۸۳ کل سبز**. مستقر روی demo (`0027`، بدون مهاجرت).
    **هنوز روی production مستقر نشده و در git کامیت نشده.**
  - **ماژول مراکز هزینه/پروژه:** یک بُعدِ تازه برای سنجشِ سودِ هر پروژه. جدولِ
    `cost_centers` (با RLS) و ستونِ اختیاریِ `cost_center_id` روی `journal_lines`،
    `sales_invoices` و `purchase_invoices` (مهاجرت `0028`، همه nullable — داده‌ی
    قدیمی نمی‌شکند). **مرکز در سطحِ سند** انتخاب می‌شود و به همه‌ی ردیف‌های سندش به
    ارث می‌رسد: فاکتور فروش/خرید (`post_*_invoice` برچسب را به ردیف‌های سند و خودِ
    فاکتور می‌زند) و سند دستی (`JournalEntryIn.cost_center_id`). `resolve_cost_center_id`
    اعتبارِ برچسب را می‌سنجد. CRUD در `/api/cost-centers` (مجوزِ `accounting`؛ حذف
    وقتی سندی ارجاع دارد بسته است — باید «غیرفعال» شود). گزارشِ سودِ مرکز
    `GET /api/reports/cost-center`: درآمد/هزینه/سود هر مرکز از ردیف‌های برچسب‌خورده،
    به‌علاوه‌ی سطرِ «بدون مرکز». UI: تبِ «مراکز هزینه» در حسابداری (`CostCentersPanel`)،
    انتخاب‌گرِ مرکز در فرم‌های فروش/خرید/سند دستی (زنده؛ آفلاین که نشد پنهان و بی‌اثر —
    مسیر outbox کلِ payload را می‌فرستد پس در Electron هم کار می‌کند)، و تبِ «سود
    پروژه/مرکز هزینه» در گزارش‌ها. روی هر دو پلتفرم. ۷ تست جدید (`test_cost_centers.py`)،
    **۴۹۵ کل سبز**. مستقر روی demo (`0028`، RLS روی ۴۲ جدول).
    **هنوز روی production مستقر نشده و در git کامیت نشده.**
  - **گزارش تحلیل سنیِ مطالبات/بدهی‌ها (AR/AP Aging):** گزارشِ افزودنی (بدون تغییر
    دیتابیس) — `GET /api/reports/aging?kind=receivable|payable&as_of=`. مانده‌ی هر
    شخص = فاکتورهای اعتباریِ باطل‌نشده منهای تسویه‌ها (`TreasuryTransaction`
    دریافت/پرداخت) و برگشت‌ها. چون تسویه در سطحِ شخص ثبت می‌شود (نه فاکتوربه‌فاکتور)،
    مبلغِ تسویه به روشِ **FIFO روی قدیمی‌ترین فاکتورها** اعمال و باقیمانده‌ی هر فاکتور
    در سطلِ سنی‌اش (۰–۳۰ / ۳۱–۶۰ / ۶۱–۹۰ / بالای ۹۰ روز نسبت به `as_of`) می‌نشیند.
    اسنادِ دستیِ بدونِ شخص در گزارش نمی‌آیند. UI: تب‌های «سنی مطالبات» و «سنی بدهی‌ها»
    در گزارش‌ها با سطل‌ها + جمع، و برجسته‌سازیِ ستونِ «بالای ۹۰ روز». روی هر دو پلتفرم.
    ۶ تست جدید (`test_aging.py`)، **۵۰۱ کل سبز**. مستقر روی demo و **production**
    (`0028`).
  - **گزارش صورت‌حساب اشخاص (کارت حساب):** گزارشِ افزودنی —
    `GET /api/reports/contact-statement/{contact_id}?date_from=&date_to=`. گردشِ
    کاملِ یک طرف‌حساب (فاکتور فروش/خرید، برگشت‌ها، دریافت/پرداختِ خزانه) به‌ترتیبِ
    تاریخ با ماندهٔ در حال اجرا و ماندهٔ اول دوره. قرارداد علامت از دیدِ کسب‌وکار:
    بدهکار = طلبِ ما بیشتر می‌شود (فروش، پرداختِ ما، برگشتِ خرید)، بستانکار = کمتر
    (دریافت، برگشتِ فروش، خریدِ ما)؛ ماندهٔ مثبت = شخص به ما بدهکار است. اسنادِ باطل
    کنار می‌روند. UI: تبِ «صورت‌حساب اشخاص» در گزارش‌ها با انتخاب‌گرِ زنده‌ی طرف‌حساب +
    جدولِ کارت حساب. روی هر دو پلتفرم. ۵ تست جدید (`test_contact_statement.py`)،
    **۵۰۶ کل سبز**. مستقر روی demo (`0028`، بدون مهاجرت).
    **هنوز روی production مستقر نشده و در git کامیت نشده.**
  - **گزارش ارزش‌گذاری موجودی انبار (Inventory Valuation):** گزارشِ افزودنی —
    `GET /api/reports/inventory?warehouse_id=&as_of=`. موجودیِ هر کالا (جمعِ حرکاتِ
    `stock_ledger`) × بهای میانگین موزون (`Item.average_cost`) = ارزش ریالی. جمعِ کل
    باید با ماندهٔ حسابِ «موجودی کالا» بخواند. کالای خدماتی و موجودیِ صفر حذف می‌شوند؛
    فیلترِ اختیاریِ انبار و تاریخ. مجوز `accounting:view` (هم‌راستا با بقیه‌ی گزارش‌ها).
    UI: تبِ «ارزش موجودی انبار» در گزارش‌ها با برجسته‌سازیِ موجودیِ منفی. روی هر دو
    پلتفرم. ۵ تست جدید (`test_inventory_report.py`)، **۵۱۱ کل سبز**. مستقر روی demo
    (`0028`، بدون مهاجرت). **هنوز روی production مستقر نشده و در git کامیت نشده.**
  - **کاردکس کالا (Item Kardex):** گزارشِ افزودنی —
    `GET /api/reports/kardex/{item_id}?warehouse_id=&date_from=&date_to=`. همه‌ی
    ورود/خروج‌های یک کالا با موجودیِ در حال اجرا و موجودیِ ابتدای دوره. **ترتیب بر
    اساس `stock_ledger.seq` است نه تاریخ** — چون `entry_date` فقط روز را دارد و چند
    حرکت در یک روز ترتیبِ مشخصی نمی‌گیرند، در حالی که ماندهٔ در حال اجرا به ترتیب
    وابسته است. برچسبِ فارسیِ منشأ (`STOCK_SOURCE_LABELS`) برای هر هفت نوعِ حرکت:
    فاکتور خرید/فروش، برگشت خرید/فروش، تعدیل انبار، انتقال ورود/خروج. UI: تبِ
    «کاردکس کالا» در گزارش‌ها با انتخاب‌گرِ زنده‌ی کالا (خدماتی‌ها حذف). روی هر دو
    پلتفرم. ۵ تست جدید (`test_kardex.py`)، **۵۱۶ کل سبز**. مستقر روی demo.
  - **ماژول سامانه مؤدیان (صورتحساب الکترونیکی):** مهاجرت `0029` — دو جدولِ
    `moadian_settings` (اعتبارنامه‌ی هر مستأجر، شاملِ **کلید خصوصیِ امضا**) و
    `moadian_submissions` (ردِ هر ارسال: شناسه مالیاتی، سریال، وضعیت، درخواست و
    پاسخ)، هر دو با RLS.
    - **امضا (قطعی):** RSA/SHA-256، PKCS#1 v1.5، خروجی base64، روی JSONِ قطعی
      (`sort_keys` + بدون فاصله) — چون امضا روی بایت‌هاست و سریال‌سازیِ متفاوت
      امضا را می‌شکند. تست، امضا را با کلید عمومی راستی‌آزمایی می‌کند.
    - ⚠️ **وابسته به نسخه‌ی دستورالعمل:** ترکیبِ «شناسه یکتای مالیاتی» (به‌ویژه
      کاراکترِ کنترلی) و نامِ فیلدهای بسته، عمداً در `generate_tax_id` و
      `build_invoice_packet` **ایزوله** شده‌اند تا اصلاحشان موضعی باشد؛ تست‌ها
      رفتار فعلی را پین می‌کنند. **قبل از استفاده‌ی واقعی با PDF نسخه‌ی جاری تطبیق
      داده شود.**
    - **امنیت:** کلید خصوصی هرگز در پاسخ API برنمی‌گردد (فقط `has_private_key`)؛
      نقشِ `demo` عمداً هیچ مجوزِ `moadian` ندارد؛ حسابدار `view`+`approve` دارد ولی
      `update` ندارد، پس تغییرِ اعتبارنامه فقط در اختیار مالک است. تستِ اختصاصی
      نبودِ کلید در پاسخ را می‌سنجد.
    - **ایمنیِ عملیاتی:** پیش‌فرض **سندباکس + غیرفعال**؛ تا مالک اعتبارنامه را وارد
      و «فعال» نکند هیچ ارسالی ممکن نیست. ارسالِ دوباره‌ی یک فاکتور بسته است (دو
      شناسه‌ی قانونی برای یک فروش). خطای شبکه/سامانه به‌جای پرتاب، در وضعیت
      `failed`/`rejected` ثبت می‌شود تا سابقه گم نشود.
    - UI: تبِ «سامانه مؤدیان» در صفحه‌ی فروش — KPI وضعیت، فرم اعتبارنامه (کلید فقط
      یک‌بار وارد می‌شود)، انتخاب و ارسالِ فاکتور، و تاریخچه‌ی ارسال‌ها.
    - ۱۲ تست جدید (`test_moadian.py`، همه با `httpx.MockTransport` و بدون تماس
      واقعی)، **۵۳۸ کل سبز**. مستقر روی demo (`0029`).
  - **تخفیف روی فاکتور (مهاجرت `0030`):** تا پیش از این اصلاً امکان ثبتِ فروشِ
    تخفیف‌دار نبود — تا جایی که در بسته‌ی مؤدیان مجبور بودیم `dis`/`tdis` را صفر
    هاردکد کنیم. حالا `discount` روی ردیف‌ها و `total_discount` روی سرِ فاکتور.
    - **قرارداد معنایی:** `total_amount` همچنان «خالص» است ولی از این پس یعنی
      **خالصِ پس از تخفیف**. به همین دلیل سند حسابداری، محاسبه‌ی VAT، گزارش‌ها و
      مطالبات هیچ‌کدام تغییر نکردند و همچنان درست‌اند — درآمد به مبلغِ پس از تخفیف
      ثبت می‌شود و مالیات روی همان پایه، که رفتارِ درستِ «تخفیف تجاری» است.
    - **خرید:** بهای موجودی هم پس از تخفیف ارزش‌گذاری می‌شود (`average_cost` و
      `stock_ledger.unit_cost` هر دو از خالصِ ردیف)، وگرنه انبار گران‌تر از پولِ
      پرداختی در دفاتر می‌نشست و سودِ فروشِ بعدی کمتر از واقع گزارش می‌شد. قیمتِ
      فهرستِ تأمین‌کننده روی ردیف دست‌نخورده می‌ماند.
    - **برگشت‌ها:** چون قیمتِ واحدِ مؤثر از تقسیمِ «مبلغ ردیف بر تعداد» می‌آید،
      کافی بود مبلغ را پس از تخفیف جمع بزنیم تا برگشت همان چیزی را برگرداند که
      طرف واقعاً پرداخت کرده — وگرنه تخفیف عملاً دو بار داده می‌شد.
    - **اعتبارسنجی:** تخفیفِ منفی یا بیشتر از مبلغِ ردیف در همان اسکیما رد می‌شود
      (نه گردکردنِ بی‌صدا) تا اشتباهِ ورودِ اطلاعات همان‌جا دیده شود.
    - نمای چاپی ستونِ تخفیف و سطرهای «جمع ناخالص/جمع تخفیف» گرفت (فقط وقتی تخفیفی
      باشد، تا فاکتورهای بدون تخفیف دقیقاً مثل قبل چاپ شوند). مؤدیان حالا
      `tprdis`/`tdis`/`tadis` واقعی می‌فرستد.
    - ۸ تست جدید (`test_invoice_discount.py`) شاملِ تستِ سازگاری با گذشته،
      **۵۴۶ کل سبز**. مستقر روی demo (`0030`).
  - **خروجی اکسل/CSV گزارش‌ها (فقط frontend، بدون مهاجرت):** ابزارِ کوچکِ
    `desktop/src/lib/csv.ts` سمتِ کلاینت CSV می‌سازد — چون داده‌ی هر گزارش همین
    حالا برای نمایش گرفته شده و ۱۵ اندپوینتِ سرور برایش اضافی بود. **BOM عمدی** تا
    اکسل ویندوزی UTF-8 فارسی را درست بخواند؛ اعداد لاتین و دست‌نخورده می‌مانند تا
    اکسل «عدد» بفهمد، فقط تاریخ‌ها متنِ شمسی می‌روند. دکمه‌ی «دانلود اکسل» روی هر ۱۳
    تبِ گزارش (وقتی داده بارگذاری شده)؛ در رندررِ الکترون هم کار می‌کند (کرومیوم).
  - **داشبورد تحلیل فروش (بدون مهاجرت):** `GET /api/reports/dashboard?months=` —
    روند ماهانه‌ی فروش/خرید + پرفروش‌ترین کالاها + بهترین مشتریان، همه در بازه‌ی
    N ماه اخیر. **ماه‌ها شمسی سطل‌بندی می‌شوند نه میلادی** (تبدیل با
    `printing.gregorian_to_jalali`)، چون یک ماه میلادی روی دو ماه شمسی می‌افتد و
    برچسبِ ماهِ اشتباه روند را بی‌معنا می‌کند؛ کفِ درشتِ کوئری + سطل‌بندیِ دقیق در
    پایتون. پرفروش‌ها بر اساس درآمدِ **پس از تخفیف**؛ فاکتورهای باطل حذف.
    - **نمودارها (SVG/HTML سبک، بدون کتابخانه‌ی بیرونی — طبق اسکیل dataviz):** فروش
      و خرید ماهانه = «تغییر در زمان» → میله‌ی گروهیِ دو‌سری با لجندِ همیشگی و
      راهنمای فاصله (`MonthlyBarsChart`، رنگ‌های ثابتِ `--series-1/2`)؛ پرفروش‌ها و
      بهترین مشتریان = «بزرگی/رتبه» → میله‌ی افقیِ تک‌سری بدون لجند (`RankBars`،
      HTML). متن‌ها با توکنِ جوهر نه رنگِ سری؛ اعداد بزرگ با `faCompact` روی محور و
      کاملشان در tooltip. در نمای کلی (`OverviewPage`) زیرِ روند نقدینگی.
    - ۴ تست جدید (`test_sales_dashboard.py`)، **۵۵۰ کل سبز**. مستقر روی demo.
  - **سقف اعتبار مشتری (مهاجرت `0031`):** فیلدِ `credit_limit` روی `contacts`
    (صفر = بدون سقف). سرویس `credit.py` مانده‌ی خالصِ مطالبات یک مشتری را **دقیقاً با
    همان تعریفِ گزارشِ سنی** حساب می‌کند (فروشِ باطل‌نشده − دریافت‌ها − برگشت‌ها)، پس
    دو جا یک عدد می‌دهند. اندپوینت `GET /api/contacts/{id}/credit` مانده/سقف/قابلِ
    استفاده/`over_limit` را برمی‌گرداند.
    - **کشفِ مهم حین کار:** فرمِ فاکتور فروش (آفلاین و وب) اصلاً مشتری نمی‌گرفت — یعنی
      گزارش‌های سنی/کارت‌حساب/اعتبار عملاً بی‌داده بودند. پس **انتخاب‌گر مشتری به فاکتور
      فروش اضافه شد** با همان الگویِ زندهٔ «مراکز هزینه»ی همین فرم (واکشیِ زنده،
      آفلاین به‌آرامی پنهان می‌شود) تا پلامبینگِ حساسِ sync دست‌نخورده بماند و parity
      روی هر دو پلتفرم حفظ شود. `contact_id` از طریق همان payloadِ outbox رد می‌شود.
    - **هشدارِ اعتبار عمداً بلاک‌کننده نیست:** بنرِ زیرِ انتخاب‌گر مانده/سقف را نشان
      می‌دهد و اگر این فاکتور مانده را از سقف عبور دهد هشدار می‌دهد — ولی جلوی ثبت را
      نمی‌گیرد، چون تصمیمِ فروش با کاربر است و فاکتورهای آفلاین نباید سمت سرور رد شوند.
      فرمِ اشخاص هم فیلدِ سقف و ستونِ «سقف اعتبار» گرفت.
    - ۶ تست جدید (`test_credit.py`)، **۵۵۶ کل سبز**. مستقر روی demo (`0031`).
  - **انتخاب‌گر تأمین‌کننده روی فاکتور خرید (فقط frontend، بدون مهاجرت):** قرینه‌ی
    کارِ سمتِ فروش — همان الگویِ زندهٔ «مراکز هزینه»، `contact_id` از طریق payloadِ
    outbox. **نکته‌ی حسابداری:** سرویس از قبل با وجودِ طرف‌حساب فاکتور را به «حساب‌های
    دریافتنی/پرداختنی» می‌برد نه صندوق (خرید نسیه vs نقدی)؛ پس این تغییر گزارشِ **سنیِ
    بدهی‌ها** و کارت‌حسابِ تأمین‌کننده را هم داده‌دار می‌کند و خریدِ نسیه را درست ثبت
    می‌کند. مستقر روی demo/production.
  - **ماژول انبارگردانی (مهاجرت `0032`):** یک لایه‌ی **جلسه‌ای** بالای تعدیلِ
    تک‌کالاییِ موجود (`post_stock_adjustment`). `create_session` از موجودیِ سیستمیِ
    همه‌ی کالاهای غیرخدماتیِ یک انبار **عکس‌برداری** می‌کند (`system_qty` + `unit_cost`
    = میانگین موزون در آن لحظه؛ `counted_qty` پیش‌فرض = سیستمی تا کاربر فقط اختلاف‌ها
    را دست بزند). چرخه: باز → (ثبت | لغو)؛ فقط جلسه‌ی باز قابل ویرایش است و یک انبار
    همزمان بیش از یک جلسه‌ی باز ندارد.
    - **ثبت:** مغایرت هر ردیف (`counted − system`) به `stock_ledger` می‌رود
      (`source_type='stock_count'`, `source_id=session`) و **یک سندِ تجمیعی** صادر
      می‌شود: اضافیِ خالص → بدهکارِ موجودی/بستانکارِ مغایرت انبار؛ کسریِ خالص → برعکس
      (همان قرارداد `INVENTORY`/`INVENTORY_ADJUSTMENT`). اگر ارزشِ خالص صفر شود سندی
      صادر نمی‌شود ولی مقدارِ **هر** کالا جداگانه تعدیل می‌شود (تست دارد). میانگینِ
      موزون با تعدیلِ مقداری تغییر نمی‌کند چون مغایرت به همان بهای عکس‌برداری‌شده
      ارزش‌گذاری می‌شود.
    - **عکس‌برداری هنگام ایجاد** (نه هنگام ثبت) یعنی مغایرت همان چیزی است که شمارنده
      دید؛ پس بازه‌ی شمارش را کوتاه نگه دارید (در سند مدل ذکر شده).
    - `StockCountPanel` (زنده، آنلاین‌محور) در تبِ «انبارگردانیِ» صفحه‌ی انبار؛ تبِ
      قدیمیِ تعدیلِ تک‌کالایی به «تعدیل دستی» تغییرِ نام یافت. مغایرت و ارزش زنده حین
      تایپ محاسبه می‌شوند. ۱۱ تست جدید (`test_stock_taking.py`)، **۵۷۷ کل سبز**.
      RLS از ۴۴ به ۴۶ جدول. مستقر روی demo/production (`0032`).
  - **ماژول اسناد تکرارشونده (مهاجرت `0033`):** قالبِ یک سندِ حسابداریِ دوره‌ای
    (اجاره/بیمه/اقساط) با تناوبِ هفتگی/ماهانه/سالانه × فاصله، تاریخِ شروع/پایانِ
    اختیاری، مرکز هزینه‌ی اختیاری، و ردیف‌های ثابتِ متوازن.
    - **تولید بر اساس تقاضا، نه زمان‌بندِ پس‌زمینه:** پروژه worker/cron ندارد، پس
      اندپوینتِ `POST /api/recurring-entries/run` همه‌ی سررسیدهای گذشته تا امروز را
      می‌سازد و `next_run_date` را جلو می‌برد. **درستی به این تاریخِ ذخیره‌شده وصل
      است** — اجرای دوباره چیزی را دوباره نمی‌سازد (idempotent). دکمه‌ی «تولید
      سررسیدها» + تولیدِ تک‌قالبی هم هست.
    - **دو تصمیمِ لبه‌ای:** سررسیدی که در **دوره‌ی مالیِ بسته** بیفتد ساخته نمی‌شود
      ولی تاریخ باز هم جلو می‌رود تا قالب گیر نکند (شمارشِ skipped برمی‌گردد)؛
      پیش‌رویِ ماهانه روزِ ماه را به **آخرین روزِ ماهِ مقصد می‌چسباند** (۳۱ →
      ۲۸/۳۰). ویرایشِ قالبِ **اجراشده** `next_run_date` را عقب نمی‌برد تا گذشته
      دوباره ساخته نشود. حذفِ قالب، اسنادِ **تولیدشده**‌ی واقعی را دست نمی‌زند.
    - `RecurringEntriesPanel` (زنده) در تبِ «اسناد تکرارشونده»ی صفحه‌ی حسابداری، با
      نشانِ «سررسید» و اعتبارسنجیِ توازن. ۱۵ تست جدید (`test_recurring.py`)،
      **۶۰۲ کل سبز**. RLS ۴۶→۴۸. مستقر روی demo/production (`0033`).
  - **مرکز هشدارها / اعلان‌های درون‌برنامه‌ای (بدون مهاجرت، فقط‌خواندنی):**
    `GET /api/alerts` از شش منبعِ موجود آیتم‌های «نیازمند رسیدگی» را زنده تجمیع
    می‌کند: چک‌های نزدیک/گذشته‌ی سررسید (وضعیت‌های فعال)، مطالباتِ معوق و عبور از
    سقف اعتبار (هر دو از یک فراخوانیِ `get_aging`)، اسناد تکرارشونده‌ی سررسیدشده،
    یادآوری‌های تقویمِ گذشته/پیش‌رو، و موجودیِ منفی. خروجی یک **آیتمِ یکدست**
    (`category`/`severity`/`title`/`detail`/`alert_date`/`amount`/`ref_id`) است تا
    UI یک فهرستِ مرتبِ danger→warning→info بسازد.
    - چون سند نمی‌سازد و جدول ندارد، **کم‌ریسک‌ترین شکلِ ماژول** است. **پیامک/ایمیل
      عمداً بیرون گذاشته شد:** ترانسپورت به providerِ ایرانی + کلید نیاز دارد؛ وقتی
      تعیین شد همین آیتم‌ها ارسال‌شدنی‌اند.
    - گاتچا: فیلد را نباید `date` نامید — کلاسِ `datetime.date` را سایه می‌اندازد و
      annotationِ `date | None` می‌شکند؛ به `alert_date` تغییر یافت (همان دلیلِ
      `txn_date` در کارت‌حساب).
    - `AlertsPanel` بالای نمای کلی؛ آفلاین بی‌صدا چیزی نشان نمی‌دهد. ۷ تست جدید
      (`test_alerts.py`)، **۶۰۹ کل سبز**. مستقر روی demo/production.
  - **خروجی PDF واقعی فاکتور (بدون مهاجرت):** نمای HTML همچنان هست، ولی حالا اندپوینت
    `GET /api/{sales|purchase}-invoices/{id}/pdf` یک **فایلِ PDF قابلِ‌دانلود** می‌دهد
    (برای پیوست ایمیل/بایگانی) — همان مسیری که docstring نمای HTML از ابتدا پیش‌بینی
    کرده بود.
    - **چرا fpdf2 و نه WeasyPrint:** WeasyPrint به pango/cairo (libِ سیستمیِ سنگین)
      نیاز دارد. `fpdf2` خالصِ پایتون است و شکل‌دهیِ حروفِ چسبان و RTL را با
      `uharfbuzz` (چرخِ نصب، بدون libِ سیستمی) انجام می‌دهد. فونتِ **Vazirmatn** داخلِ
      مخزن (`app/assets/fonts/`) جاسازی شده و در خروجی هم embed می‌شود؛ پس متن
      **انتخاب/جست‌وجو** می‌شود و روی هر دستگاه یکسان دیده می‌شود. امکان‌سنجی و کیفیتِ
      فارسیِ RTL قبل از ساخت بصری تأیید شد.
    - جدولِ فاکتور دستی راست‌به‌چپ کشیده می‌شود (سربرگ/اقلام/جمع‌ها)، با تاریخِ شمسی،
      رقمِ فارسی و «مبلغ به حروف»؛ ساختِ آرگومان‌ها بین `print` (HTML) و `pdf` مشترک
      است (`_sales/_purchase_render_kwargs`). دکمه‌ی «PDF» کنار «چاپ» در `InvoiceList`
      (دانلودِ blob با هدرِ توکن، در الکترون و وب).
    - **نکته‌ی استقرار:** دو وابستگیِ تازه (`fpdf2`، `uharfbuzz`) به `requirements.txt`
      اضافه شد و باید در venvِ هر مقصد **قبل از** استقرار نصب شوند (وگرنه کدِ جدید که
      import می‌کند بالا نمی‌آید و rollback می‌خورد). ۶ تست جدید (`test_pdf_invoice.py`
      — هدرِ معتبر، فونتِ embed، ToUnicode، صفحه‌بندی)، **۶۱۵ کل سبز**. مستقر روی
      demo/production.
  - **ماژول چندارزی (مهاجرت `0034`) — دامنه‌ی عمداً کران‌دار:** **دفترها پایه (ریال)
    می‌مانند**؛ ارز یک لایه‌ی تراکنش/نمایش روی فاکتور است. جدول‌های `currencies`
    (ارزهای خارجیِ کاربر؛ ریال ضمنی) و `exchange_rates` (نرخِ هر ارز در یک تاریخ =
    چند ریال به‌ازای یک واحد) + سرویسِ «آخرین نرخ». فاکتورها دو ستونِ `currency_code`
    (NULL = پایه) و `exchange_rate` گرفتند.
    - **چرا کم‌بلاست:** مبالغِ سطرها هنگام ثبت **در کلاینت به پایه تبدیل** می‌شوند و
      به همان شکلِ ریالیِ همیشگی به سرور می‌روند؛ پس سند حسابداری، بهای موجودی،
      مالیات و همه‌ی گزارش‌ها **بدون تغییر** و درست‌اند (تستِ تریال‌بالانس این را
      پین می‌کند). سرور فقط `currency_code`+`exchange_rate` را به‌عنوان متادیتای نمایش
      ذخیره می‌کند. **بازارزیابیِ ماندهٔ ارزیِ باز و سود/زیانِ تسعیر عمداً بیرون است**
      (نیازمند لایه‌ی کاملِ چندارزیِ دفتر).
    - `CurrenciesPanel` (تبِ «ارزها و نرخ ارز»ی حسابداری) ارز/نرخ را مدیریت می‌کند.
      فرم‌های فاکتور فروش/خرید انتخاب‌گرِ ارز + نرخ گرفتند (نرخِ روز از «آخرین نرخ»
      پیش‌پرمی‌شود؛ کاربر مبالغ را ارزی وارد می‌کند و «معادل ریالی» زنده نشان داده
      می‌شود). PDF/چاپ هم نوارِ «ارز/نرخ/معادل» می‌گیرند. ۱۰ تست جدید
      (`test_currencies.py`)، **۶۳۵ کل سبز**. RLS ۴۸→۵۰. مستقر روی demo/production.
  - **تکمیل حقوق و دستمزد — عیدی/سنوات/مرخصی (مهاجرت `0035`):** لایه‌ی «مزایا» بالای
    ماژول حقوق (`benefits.py`, `GET /api/payroll/benefits?year=`).
    - **عیدی:** دو ماه حقوقِ پایه، محدود به بازه‌ی [۲×، ۳×] حداقلِ حقوقِ ماهانه‌ی سال
      (`min_base_wage`؛ صفر = بدون سقف)، و به‌نسبتِ روزهای کارکرد. **سنوات:** پایه ×
      (روزهای سابقه ÷ ۳۶۵). **مرخصی:** استحقاقیِ سالانه (پیش‌فرض ۲۶) به‌نسبتِ کارکرد
      منهای مرخصیِ استفاده‌شده (`leave_records`)؛ طلبِ مرخصی = ماندهٔ روز × (پایه÷۳۰).
    - **سال = سالِ میلادیِ داخلی** (همان قراردادِ `PayrollPeriod.year`). صدورِ هر مزیت
      یک `benefit_runs` ثبت می‌کند تا **دوباره صادر نشود** (گارد + تست)، و یک سندِ
      دوطرفه می‌زند (بدهکارِ `payroll_expense`، بستانکارِ `payroll_payable` — همان
      حساب‌های فیشِ حقوق، بدونِ نیاز به حسابِ تازه). `assert_period_open` رعایت می‌شود.
    - `BenefitsPanel` کنارِ `PayrollPanel`: گزارشِ هر کارمند (عیدی/سنوات/ماندهٔ مرخصی/
      طلب)، دکمه‌های صدورِ عیدیِ سال و سنوات/بازخریدِ مرخصیِ فردی، تنظیمِ سریعِ
      حداقل‌حقوق/روزِ مرخصی (`PUT /api/payroll/benefit-settings`)، و ثبتِ مرخصی.
      ۱۱ تست جدید (`test_benefits.py`)، **۶۵۶ کل سبز**. RLS ۵۰→۵۲. مستقر روی
      demo/production.


- **۱۴۰۵/۰۵/۰۱ (2026-07-23):**
  - **ماژول مالیات بر ارزش افزوده (VAT):** فاکتور فروش/خرید حالا فیلد `tax_rate`
    (پیش‌فرض ۱۰٪) دارد؛ backend `tax_amount` را محاسبه و ذخیره می‌کند و سندِ دوطرفه
    را کامل می‌زند — فروش: بستانکارِ **۲۱۰۵ مالیات بر ارزش افزوده پرداختنی**؛ خرید:
    بدهکارِ **۱۱۰۷ اعتبار مالیاتی**. `total_amount` = خالص (بدون تغییرِ معنا)، مبلغِ
    قابل‌پرداخت = خالص+مالیات. حساب‌های مالیاتِ چارت برای مستأجرهای قدیمی به‌صورت
    lazy ساخته می‌شوند (`get_or_create_account`، RLS-safe)؛ برای مستأجرهای جدید در
    seed. مهاجرت `0024` فقط دو ستون. ابطالِ فاکتور مالیات را هم خودکار برمی‌گرداند
    (سند معکوسِ عمومی). گزارشِ تازه `/api/reports/vat` (خالص/مالیاتِ فروش و خرید و
    خالصِ قابل‌پرداخت). فرانت: نرخِ مالیات + تفکیکِ خالص/مالیات/قابل‌پرداخت در فرمِ
    فروش و خرید، ستونِ مبلغ در فهرست = مبلغِ کل، تبِ «مالیات بر ارزش افزوده» در
    گزارش‌ها، و تفکیک در نمای چاپ. ۴ تست جدید (`test_invoice_vat.py`) + ۴۴۵ تستِ
    قبلی سبز. **روی `demo.cubita.ir` مستقر شد** (`deploy.sh demo`، مهاجرت `0023→0024`).
    هنوز روی production نه. فقط فاکتورها پوشش دارند؛ برگشت‌ها فعلاً بدون VAT‌اند (کارِ بعدی).

- **۱۴۰۵/۰۴/۳۱ (2026-07-22):**
  - سایت معرفی: پس‌زمینه‌ی رگه‌های رنگیِ شلوغ با یک پس‌زمینه‌ی **ساده و آرام**
    (`SoftBackground`، هاله‌ی آبیِ ملایم) جایگزین شد و **روی cubita.ir مستقر شد**؛
    فایل خرابِ `TermsPage` هم درست شد.
  - راه‌اندازی استک توسعه‌ی محلی و ساخت حساب تست `acc.cubita@gmail.com`.
  - ساخت همین فایل مرجع (`PROJECT_OVERVIEW.md`).
  - **ماژول جدید «تقویم و یادآوری»**: backend (مدل `calendar_events` مستأجرمحور +
    RLS، مهاجرت `0023`، schema/service/router `/api/calendar-events` با CRUD کامل
    و toggle-done؛ مجوز `calendar` به نقش‌های پیش‌فرض اضافه شد) و frontend
    (`CalendarPage` با نمای ماهانه‌ی شمسی + فهرست، ثبت/ویرایش/حذف). روی هر دو
    پلتفرم. محلی تست شد (CRUD کامل سبز). **روی `demo.cubita.ir` مستقر شد** با
    `deploy.sh demo` (کد backend + وب + مهاجرت `0022→0023`؛ راستی‌آزمایی سبز،
    RLS روی ۳۸ جدول). **هنوز روی production (acc.cubita.ir) مستقر نشده**، فایل
    نصبیِ دسکتاپ ساخته نشده (به‌خواست کاربر تا کامل‌شدن برنامه)، و در git کامیت نشده.
  - **بازطراحی حرفه‌ای UI (زبانِ طراحیِ جدید):** رفعِ فضای خالی و ظاهرِ premium.
    کلاس‌های قابل‌استفاده‌ی مجدد در `App.css`: `.workspace-split` (فرم+لیست دوستونه)
    با حاشیه‌ی گرادیانیِ کارتِ فرم (تکنیک `background-clip`)، `.split-2col` (دو کارت
    هم‌ارزش کنار هم)، **فرمِ سراسری چندستونه** (`.invoice-form` حالا گریدِ `auto-fit`
    است — همه‌ی فرم‌های همه‌ی ماژول‌ها فضا را پر می‌کنند)، `.entity-avatar`/
    `.entity-table`/`.type-badge` و ردیفِ KPI با `StatCard`. صفحه‌ی **اشخاص**
    پرچم‌دارِ کامل (KPI + دوستونه + آواتار + بج)، **انبار** دوستونه شد. **روی
    `demo.cubita.ir` مستقر شد** (فقط وب). گسترشِ کاملِ KPI/آواتار به بقیه‌ی ماژول‌ها
    مرحله‌به‌مرحله ادامه دارد.
  - **ریسپانسیوِ موبایل (اپ وب):** روی موبایل نوار کناری نصفِ صفحه را می‌گرفت و
    محتوا له می‌شد. حالا زیر `max-width: 760px` نوار کناری به **کشوی روی‌هم (drawer)**
    با دکمه‌ی منو (☰) در تاپ‌بار + overlay تبدیل می‌شود و محتوا تمام‌عرض می‌شود
    (`Sidebar` پراپ‌های `open`/`onClose`، `Dashboard` state/دکمه، CSS در `App.css`).
    Electron (حداقل عرض ۸۶۰px) بی‌تأثیر است. روی `demo.cubita.ir` مستقر شد.
    همچنین جدول‌های پرستون روی موبایل بیرون می‌زدند. روشِ اولِ `display:block`
    ستون‌ها را ناهم‌تراز کرد؛ روشِ درست: جدول **جدولِ واقعی** می‌ماند (ستون‌ها
    هم‌تراز) و داخل یک **ظرفِ اسکرول‌دار** (`.table-scroll` یا `.entity-table-wrap`)
    افقی می‌لغزد؛ روی موبایل سلول‌ها `white-space: nowrap` می‌شوند. جدول‌های
    اشخاص و فهرست فاکتورها پیچیده شده‌اند؛ بقیه‌ی فهرست‌ها (چک، پیش‌فاکتور،
    گزارش‌ها، حقوق، ...) باید به‌تدریج در همین ظرف پیچیده شوند. به‌جای چیدن بخش‌ها زیر هم، یک کامپوننت تب افقی
    قابل‌استفاده‌ی مجدد ساخته شد ([desktop/src/components/Tabs.tsx](desktop/src/components/Tabs.tsx))
    و صفحه‌های فروش، خرید، اشخاص، انبار، حسابداری و چک‌وبانک به تب تقسیم شدند
    (روی هر دو پلتفرم، چون کدپایه مشترک است). استایل در `App.css` بخش «تب‌های ماژول».
    **روی `demo.cubita.ir` مستقر شد** (build با `VITE_API_URL=https://demo.cubita.ir`،
    جابه‌جایی در `/opt/cubita-demo/web`، بکاپ `web.old`). هنوز روی `acc.cubita.ir`
    (production) و نسخه‌ی نصبیِ دسکتاپ مستقر **نشده**.

- **۱۴۰۵/۰۶/۱۵ (2026-09-06) — حقوق و دستمزد: قراردادِ سپیدار و جدول‌های مرجعش:**
  ماژولِ حقوق تا امروز «حکمِ حقوقی»ِ ساده‌ای داشت (تاریخ اثر + چهار ستونِ مبلغ). حالا
  فرمِ «قرارداد جدید»ِ سپیدار با همه‌ی تب‌هایش هست، به‌علاوه‌ی پنج جدولِ مرجعی که از
  آن‌ها انتخاب می‌کند. **مهاجرت `0094`** (شش جدولِ تازه با RLS + بیست‌وچند ستون روی
  `salary_contracts`).
  - **جریانِ استخدام از طرف‌حساب شروع می‌شود.** شخص یک‌بار در «شرکت ← طرف حساب جدید»
    با تیکِ «کارمند» ثبت می‌شود؛ فهرستِ «نام کارمند»ِ فرمِ قرارداد از همان‌جا می‌آید
    (`/api/payroll/employee-candidates`، جست‌وجو سمتِ سرور) و **اولین قرارداد پرونده‌ی
    حقوق و دستمزدش را می‌سازد** و `contacts.employee_id` را وصل می‌کند. یک آدم، یک رکورد.
  - **«استخدام» فقط یک بار.** اولین قراردادِ هر شخص استخدام است و بعدی‌ها «اصلاح
    قرارداد»؛ نه دو استخدام ممکن است نه اصلاحِ بی‌استخدام. تصمیمش سمتِ سرور است
    (`/api/payroll/contract-types`) و فرم فقط همان را نشان می‌دهد.
  - **سه تاریخ، سه معنا** و هیچ‌کدام جای دیگری را نمی‌گیرد: تاریخ صدور
    (`effective_from` — محاسبه از این‌جا شروع می‌شود)، تاریخ اعتبار (`valid_until`)،
    تاریخ پایان خدمت (`service_end_date`، که `employees.termination_date` را هم
    می‌بندد). **تاریخ استخدام چهارمی است و ستونِ قرارداد نیست**: واقعیتِ شخص است، روی
    `employees.hire_date` می‌نشیند و عمداً می‌تواند سال‌ها قبل‌تر باشد — شرکتی که تا
    دیروز با اکسل حقوق می‌داد، کارمندش را که تازه استخدام نکرده.
  - **چهار ستونِ مبلغ دیگر دستی پر نمی‌شوند.** کاربر ردیف‌های عوامل را می‌نویسد
    (`salary_contract_lines`) و سرویس `base_salary`/`housing_allowance`/
    `food_allowance`/`other_allowance` را از رویشان می‌سازد؛ `PayrollFactor.system_key`
    نگاشتشان می‌کند و هر عاملِ ساخته‌ی کاربر در «سایر مزایا» جمع می‌شود. یک منبعِ
    ویرایش، نه دو — و موتورِ فیشِ حقوقی دست‌نخورده ماند. قراردادهای قدیمیِ بی‌ردیف
    همان‌طور کار می‌کنند.
  - **پنج جدولِ مرجع** با منوی خودشان زیرِ «حقوق و دستمزد»: محل خدمت، شغل (با رسته و
    کدِ شغلِ بیمه)، عوامل حقوق و مزایا، گروه مالیاتی، و شعبِ بیمه و مالیات. **هیچ ردیفی
    در مهاجرت کاشته نشد** — جدول‌ها RLS دارند و `INSERT`ِ مهاجرت یا صفر ردیف می‌گذارد
    یا به مستأجرِ اشتباه می‌رود؛ به‌جایش دکمه‌ی صریحِ «ساخت عوامل پیش‌فرض» چهار عاملِ
    اصلی را داخلِ مستأجرِ خودِ کاربر می‌سازد (ایدمپوتنت).
  - **ساختِ درجا**، همان رفتارِ سپیدار: اگر «محل خدمت» یا «شغل»ِ موردنظر در فهرستِ
    فرمِ قرارداد نبود، همان‌جا ساخته و انتخاب می‌شود بی‌آنکه فرم ترک شود و ورودی‌ها بروند.
  - صفحه‌های تازه: `pages/payroll/ContractFormPage.tsx` (پنج تب: اطلاعات استخدامی،
    حقوق و مزایای ثابت، سایر مبالغ، سایر اطلاعات، شرح)، `ContractListPage.tsx` (دفترِ
    نظیر)، `PayrollRefPages.tsx` (چهار صفحه‌ی مرجع). ۲۲ تستِ تازه؛ **۱۷۱۸ تستِ بک‌اند
    سبز**، `tsc -b` صفر، ممیزِ صفحه‌ها صفر خطا.

- **۱۴۰۵/۰۶/۱۵ (2026-09-06) — طرف حساب: چهار نقشِ هم‌زمان، لیستِ سیاه، و ویرایشِ جزئی:**
  - **نقش‌ها چهار تیکِ مستقل شدند** (مشتری · تأمین‌کننده · واسطه · سهامدار · کارمند)
    به‌جای یک کشوییِ تک‌انتخابی + دو تیکِ گم‌شده در ته تب. `contacts.type` همان سه
    مقدارِ قبلی را نگه می‌دارد (فیلترها و شاخص‌ها رویش تکیه دارند) و از دو تیکِ اولِ
    فرم ساخته می‌شود؛ واسطه و سهامدار و کارمند پرچمِ جداگانه‌اند. **پس هر پنج نقش
    هم‌زمان ممکن‌اند** بی‌آنکه چیزی از قبل بشکند. ستونِ «نوع»ِ فهرستِ طرف‌حساب‌ها به
    «نقش» تبدیل شد و همه‌ی نقش‌ها را نشان می‌دهد، با فیلترِ نقش.
  - **مانده‌ی اول دوره فقط برای نقشی که تیک خورده.** سهامدار و واسطه مانده‌ی ابتدای
    دوره ندارند و کارمند مانده‌اش در حقوق و دستمزد است؛ فیلدی که هیچ‌وقت نباید پر شود
    دیگر دیده نمی‌شود، و مبلغِ نقشِ تیک‌نخورده صفر می‌رود حتی اگر قبلاً تایپ شده باشد.
  - **«لیست سیاه» از برچسب به هشدار تبدیل شد.** `is_blacklisted` ذخیره می‌شد ولی
    هیچ‌جا دیده نمی‌شد؛ حالا در هر چهار نقطه‌ی صدور فاکتور (فرم و ویزاردِ فروش و خرید)
    بنر می‌دهد. **عمداً بلاک‌کننده نیست** — گاهی آگاهانه و موقتاً برای همان شخص فاکتور
    می‌زنیم، و اگر ثبت را می‌بست کاربر تیک را برمی‌داشت و نشانه برای همیشه از بین
    می‌رفت. همان قاعده‌ی بنرِ اعتبار: گزارش می‌کنیم، جلو نمی‌گیریم.
  - **`PATCH /api/contacts/{id}` ویرایشِ جزئی شد.** پیش از این `model_dump()`ِ کامل
    می‌گرفت، پس فرمِ ساده‌ی «فروش ← طرف حساب» — که فقط چند فیلدِ پایه می‌فرستد — هر بار
    نقشِ واسطه و سهامدار، نرخِ پورسانت، مشخصاتِ شخصی و مانده‌ی اول دوره را به پیش‌فرضِ
    اسکیما برمی‌گرداند (و روی طرف‌حسابِ دارای افتتاحیه ۴۰۹ می‌داد). قفلِ افتتاحیه هم
    فقط وقتی می‌گیرد که مانده واقعاً فرستاده شده باشد.
  - **نوعِ نشانیِ «سایر»** (مهاجرت `0093`): هفت نوعِ سپیدار کارگاه و نمایشگاه و دفترِ
    موقت را نمی‌پوشاندند و کاربر مجبور بود یکی را دروغ انتخاب کند — که فیلترِ «ارسال
    کالا» را خراب می‌کرد.
  - **فایلِ تازه‌ی [FUTURE_WORK.md](FUTURE_WORK.md)**: کارهای شناخته‌شده‌ی هنوز
    انجام‌نشده (نمایشِ نشانی روی نقشه با مختصات، زون به‌عنوان موجودیت، پورسانتِ واسطه،
    اعمالِ `credit_action`، …). وقتی کاری انجام شد از آن‌جا حذف و به همین‌جا منتقل می‌شود.
